# 00 · 评审清单

> 评审强制检查点：事前占位待办 + 事后勾选。**未勾选 = 评审未完成，禁止进入下一批。**
> 与 `00-学习档案.md` 评审记录表分工：本清单管「待办勾选」，档案管「结论摘要」。
>
> ⚠️ 当前 `course-reviewer` 子 agent 未创建，评审走**主 agent 内联**方式（pedagogy + learner 双视角），独立性受限。

## 待评审清单

### 备课

- [x] 大纲评审（4 阶段 12 课 36 知识点 + 故事主线）—— 2026-09-08 通过，P0=0（P1×3 已采纳修订）

### 阶段 1 · 地基与对象模型

- [x] 阶段 1·课 1《版本控制与第一次提交》 — pedagogy + learner 双视角（2026-09-08 通过，P0=0，见下方记录）
- [ ] 阶段 1·课 2《Git 的对象数据库》 — pedagogy + learner 双视角
- [ ] 阶段 1·课 3《提交的身份与历史图》 — pedagogy + learner 双视角

### 阶段 2 · 个人工作流

- [x] 阶段 2·课 4《差异与撤销》 — pedagogy + learner 双视角（2026-09-08 通过，P0=0，见下方记录）
- [x] 阶段 2·课 5《分支的本质》 — pedagogy + learner 双视角（2026-09-08 通过，P0=0，见下方记录）
- [x] 阶段 2·课 6《合并与冲突》 — pedagogy + learner 双视角（2026-09-08 通过，P0=0，见下方记录）

### 阶段 3 · 协作与共享

- [x] 阶段 3·课 7《远端仓库与同步》 — pedagogy + learner 双视角（2026-09-09 通过，P0=0，见下方记录）
- [x] 阶段 3·课 8《变基与提交整理》 — pedagogy + learner 双视角（2026-09-09 通过，P0=0，见下方记录）
- [x] 阶段 3·课 9《分支策略与发布》 — pedagogy + learner 双视角（2026-09-09 通过，P0=0，见下方记录）

### 阶段 4 · 排查、救援与工程实践

- [x] 阶段 4·课 10《历史排查与定位》 — pedagogy + learner 双视角（2026-09-09 通过，P0=0，见下方记录）
- [x] 阶段 4·课 11《误操作救援》 — pedagogy + learner 双视角（2026-09-09 通过，P0=0，见下方记录）
- [x] 阶段 4·课 12《仓库工程实践》 — pedagogy + learner 双视角（2026-09-09 通过，P0=0，见下方记录）

### 收尾（课程制必做）

- [x] **结课实战项目**（Phase 3）— ✅ 2026-09-09 完成：`wf-lab` 工作流沙盘，1321 行 / 52 函数 / 7 文件，**68 条断言全通过**，复杂度四门槛全达标，实测抓出 6 个真 bug（见 DECISIONS.md）
- [x] **实战经验 / 排障速查手册 / 场景解法库**（Phase 5）— ✅ 2026-09-09 完成：12 个故障模式 + 按症状倒查手册 + 16 个场景片段，另有 final-课程手册.md 全课索引；全部数字来自本机实测，零死链

### 结构校验

- [ ] 链接可达性检查（含两个索引 + 各阶段 overview + 课尾上一课/下一课）—— 全课程交付后跑

## 评审记录（勾选后追加结论摘要）

| 日期 | 评审对象 | 评审方式 | P0 数 | 意见摘要 |
|------|----------|----------|-------|----------|
| 2026-09-08 | 大纲评审 | 主 agent 内联（pedagogy） | 0 | P1×3：课 3 偏重、缺 tag 与 diff、课 10/11 顺序；均已采纳修订 |
| 2026-09-08 | 课 1《版本控制与第一次提交》 | 主 agent 内联（pedagogy + learner） | 0 | P1×2 已采纳：①起源段"构思始于 2005 年前"含糊 → 精确为 2004 年 11/12 月起约四个月（联网核实 Linus 2025 访谈）②知识点 2 示例不可复现（只给输出没给命令）→ 补实测可复现脚本。P2×1 已采纳：补 rm 清理。评审中发现并补入 2 个实测新坑：commit 提交整个暂存区（实测 b.txt 被误带入，2 files changed）、core.quotepath 中文文件名转义。另修正 .git/index 数据（137 字节=1 个文件，非 2 个） |
| 2026-09-08 | 课 2《Git 的对象数据库》 | 主 agent 内联（pedagogy + learner） | 0（修复后） | 初版 P0×1：第四幕第 5 步 `git cat-file -s` 对未写入对象库的 blob 报错，照抄卡住学员 → 已改为 `hash-object -w` 先写入。P1×1：轻量标签字节数 40 与实测 41 矛盾 → 已统一为 41。P2×1：plumbing/porcelain 词源不完整 → 已核实为马桶比喻，并补 `--porcelain` 选项用途。另实测补入：`git gc` 后引用打包进 `.git/packed-refs`、分支文件精确 41 字节 |
| 2026-09-08 | 课 3《提交的身份与历史图》 | 主 agent 内联（pedagogy + learner） | 0（修复后） | 初版 P0×1：`git shortlog -s` 实测输出为空 → 改为 `git shortlog -sn HEAD`（两处）。P2×1：`.gitignore` 模板 `build/` 与上文陷阱矛盾 → 补提示。**新增必查项 #29：涉及 `--global` 的实验命令必须先隔离 HOME**（本课第一版脚本误改真实全局 `user.name`，已还原） |
| 2026-09-08 | 课 4《差异与撤销》 | 主 agent 内联（pedagogy + learner） | 0（修复后） | 初版 P1×1：「--hard 后提交彻底消失」属未实测推断 → 补完整证据链（cat-file 可读 / reflog 有记录 / git reset --hard <sha> 可完整恢复），表述改为「失去引用」。新增必查项 #30：凡断言「数据被销毁/无法恢复」必须先实测（此条若写错会让学员误以为无法救援而放弃）。实测补入 3 条真命令行为：reset --hard <path> 报 Cannot do hard reset with paths（exit 128）、revert 合并需 -m 1（否则 exit 128）、amend 日期差需 sleep≥3s 才可见 |
| 2026-09-08 | 课 5《分支的本质》 | 主 agent 内联（pedagogy + learner） | 0（修复后） | 初版 P1×2：① 大仓库循环首文件必报错（目录未先建）→ 改为循环前 mkdir -p；② 冲突标记示例分支名 feat 与第四幕 feature 不一致 → 统一。**新增必查项 #31：讲义中的分支名/文件名必须全文一致**（示例与实操对不上会让学员怀疑自己敲错）。实测新增 4 条硬数据：分支 41 字节/约 180 字节磁盘/2.6ms 与规模无关、`switch -m` 不移动分支指针、`branch -f` 对当前分支 exit 128、detached 提交进 reflog+fsck 且有 Git 主动警告 |
| 2026-09-08 | 课 6《合并与冲突》 | 主 agent 内联（pedagogy + learner） | 0（修复后） | 初版 P1×2：① 「提交前用 `git diff --check` 查冲突标记」**实测在 `git add` 之后失效**（exit 0 不报警），而误提交恰发生在 add 之后，照此建议会给出虚假安全感 → 改为以 `git grep` 为准，补 `--cached` 差异说明与实测可用的 pre-commit 钩子（commit exit=1 成功拦截），计入必查项 #30；② 实验 1 用 `wc -l` 统计父数量得 1，与正文「2」矛盾 → 改 `wc -w`。**另修 P0 级脚本 bug**：第 22 节 `$(git branch --show-current）` 误用中文全角括号，bash 报 unexpected EOF 并中断整份脚本 → 改半角。实测新增 6 条硬数据：默认策略为 **ort**（非老教程的 recursive，Git 2.34 起变更，已联网核实）、带冲突标记可正常提交（exit 0 且 status clean）、`--squash` 不标记分支为已合并（`-d` 拒，exit 1）、`stash pop` 默认丢索引状态、`--quit` 后 `--continue` 报 exit 128、重命名检测可跨改名合并改动 |
| 2026-09-09 | 课 7《远端仓库与同步》 | 主 agent 内联（pedagogy + learner） | 0（修复后） | 初版 P1×4：① 「`push --delete` 后本方 `origin/topic` 仍残留」**实测推翻**——执行删除方其跟踪引用会一并清掉，仅他方残留 → 按实测改写，计入必查项 #30；② 脚本 6 处 `git cmd | head` 后取 `$?`，拿到 head 的 0 而非真实 128（第 3/7/9/12/13/18 节）→ 全改为"重定向文件→取 `$?`→再 head"；③ `--force-with-lease` 场景设计失效（本方落后触发 `fetch first` 而非 `stale info`）→ 改为"本方先推使领先→他人偷偷推→本方未 fetch"，实测 `stale info` exit 1 拦住；④ 核验脚本 V1 在 `alice/` 内 clone 致 bob 落入子目录、`cd` 失败**脚本第 21 行中断** → 补 `cd "$R"`。实测新增 7 条硬数据：**`git pull` 分叉 2.34 起直接 fatal**（联网核实：2.27 加警告 → 2.34 升级 fatal → 2.35 修"已最新"误报；与课 6 的 ort 同属 2.34 变更）、远程跟踪分支纯本地（远端 mv 走后 status exit 0、仅 fetch 128，且 `update-ref` 可直接改写 exit 0）、`--force-with-lease` 报 `stale info` 拒绝 vs `--force` 致 2 提交消失、引用可能打包进 `packed-refs`（直接翻文件扑空）、推非裸仓库检出分支被拒（`refusing to update checked out branch`）、`add/add` 冲突（状态码 `AA`）、普通 fetch 不清理已删分支引用需 `--prune` |
| 2026-09-09 | 课 8《变基与提交整理》 | 主 agent 内联（pedagogy + learner） | 0（修复后） | 初版 P1×6：① 探索脚本 7 处 `HEAD~N` 越界（`fatal: invalid upstream` exit 128，仓库仅 3 提交时 `HEAD~3` 不存在）→ 每个 `rebase -i` 实验前先造 `B: base` 垫底，并在实验 23 显式写坑；② **实验 32 场景两度不成立**（一版 bob2 在强推后克隆致 rebase no-op，二版 bob2 从未拿到 A1、注释与输出矛盾）→ 最终让 bob2 先 fetch+ff 拿到 A1 再建 B2 成为真实受害者，实测 `add/add` 冲突后 `grep -c "add config v1"` = 0，并顺带暴露"rebase 会重放 A1 本身"这一比原设计更有说服力的对比；③ `--skip` 只演示一半 → 连 skip 两次；④ `edit` 演示以冲突收场 → 补解决+第二次 continue；⑤ `--onto`/`--rebase-merges` 越界 → 改显式 `$BASE`；⑥ cherry-pick `-x` 在脏仓库跑得"变空" → 另起干净仓库。**实测新增 7 条硬数据**：`--force-with-lease` 防不住"脚下站人"（**课 7 结论的重要补丁**：它只检查远端指针，实测强推 exit 0 通过照样出事）、rebase 保留 Author 只改 Committer、变空提交静默跳过（exit 0 但 count=0）、**默认后端 Git 2.26 起由 apply 改 merge**（已联网核实）、`--rebase-merges` 生成 label/merge -C 指令（2.18 引入、2.22 取代 --preserve-merges，已联网核实）、`merge-base` 无 `--short`、冲突后 `git commit` 是假成功。核验脚本 V1–V20 全部通过 |
| 2026-09-09 | 课 9《分支策略与发布》 | 主 agent 内联（pedagogy + learner） | 0（修复后） | 初版 P1×5 / P2×2：① **实验 18/19 标签打偏**——`v1.0.0 HEAD~6` / `v1.1.0 HEAD~3` 致 CHANGELOG 只有 3 条、版本判定三段全"无需升版本"，**知识点收益演示整体失效** → 三度调整至 HEAD~12/HEAD~2，三段判定得 MINOR/MAJOR/PATCH，并把"别数提交数、直接写哈希"写进正文；② **实验 20 字节对比无区分度**（两种标签引用文件都是 41 字节）→ 改为展示引用文件指向 + `cat-file -s`（228 vs 143）；③ 实验 28 冗余报错 `src refspec does not match any` → 补重建并说明冒号语法；④ 实验 29 注释"第 2 个提交"与实测 `v1.0.0-1-gfe0170d`（第 1 个）矛盾 → 按实测改写；⑤ 实验 18 echo 写死"11 个提交"而实际 13 条 → 按实测改正并**如实标注脚本偏差**。P2×2 均为**核验脚本自身误报**（V14 场景构造不成立、V45 断言次数写死 1 而实际 4 次）→ 按"先分辨脚本 vs 文档"原则修脚本不改文档。**实测新增 6 条硬数据**：**已推送标签普通 push 覆盖不了（exit 1 `already exists`），与分支行为完全相反**、`ls-remote --tags` 里附注标签有 `^{}` 行（远端区分两种标签的唯一手段）、`git describe` 无标签时 exit 128（CI 需兜底）、`v` 前缀混用让 `1.2.4` 排到最后、三策略远端分支数 **4/1/1** 且 Git Flow 的 master 与 develop 真的分叉（各独有 1 个）、`tag -s` 无密钥 exit 128 但说明保留在 `.git/TAG_EDITMSG`。核验脚本 V1–V45（46 条断言）全部通过 |
| 2026-09-09 | 课 10《历史排查与定位》 | 主 agent 内联（pedagogy + learner） | 0（修复后） | 初版 P1×4 / P2×2：① **实验 3 注释与实测不符**（第 2 轮注释写"标 good"实际执行 `bisect bad`）→ **不改会让读者照抄往错误方向收敛**；② **实验 10 步数统计错误**（10 提交仓库跑 `HEAD~15` 越界 + 把 start 的 2 条初始标记算进轮数）→ 改为"总 6 减 2 = 4 轮"，与 log₂16 吻合；③ 实验 15 只展示"删"未展示"加"，且未标注 `git log` 新在前的排序陷阱 → 补 diff 与说明；④ 实验 27 注释误导（docs/svc/tools 消失被误认为 cone 排除）→ 如实说明并补 `git ls-files` 证明仍在库。P2×2：实验 19 `-M` 未显出差别 → **如实标注**不夸大；探索脚本偏移越界 → 正式脚本改为与仓库深度匹配。**实测新增 8 条硬数据**：**bisect 后不 reset 的完整后果链**（提交 exit 0 → 不属于任何分支 → reset 警告 → reflog 仍在，**自然引出课 11**）、**125 与 128 行为完全不同**（125 遍历后 exit 2；128 一条中止）、**`bisect start -- <path>` 一步直达**、**单个 `-C` 不够必须 `-C -C`**（输出额外显示来源文件名）、**worktree 的 `.git` 仅 58 字节**、**cone 配置成对规则 `/*`+`!/*/` 逐层开洞**、**sparse-checkout 不省磁盘**（5→2 但 ls-files 仍 5）、**`-S` 看不见改值 `-G` 能**。核验脚本 V1–V56（56 条断言）全部通过 |
| 2026-09-09 | 课 11《误操作救援》 | 主 agent 内联（pedagogy + learner） | 0（修复后） | 初版 P1×4 / P2×2：① **命令回显缺引号**——带空格的中文参数输出为裸文本，读者照抄会被 shell 拆词 → `run()` 改为对含空格参数自动加单引号；② **实验 8 退出码注释错误**——`git config --get` 未命中实测返回 **1** 而非 128 → 改正；③ **实验 24 引用探索阶段旧哈希**（`878acb8...8b5b17a`）与本次输出 `86c19e5...a979850` 不符 → 改实测值；④ **实验 3 `HEAD@{0}` 与 `HEAD@{2}` 相同未解释**（中间是切到 demo 再切回，HEAD 转一圈回原地）→ 补解释并指出应改用 `master@{n}`。P2×2：实验 6 setup 命令无回显 → 补；探索脚本 `rebase --onto ... HEAD` 参数错误致进 detached HEAD 且未删掉目标提交 → 正式脚本改 `... master`。**实测新增 7 条硬数据**：**`--force-with-lease` 失效路径**（被拒 → fetch → 成功覆盖，推翻"它很安全"的认知）、**分支删掉后自己的 reflog 也没了**（exit 128，救援最常见卡点）、**锚定值救不了"本来就不该推"**、**无 tracking 分支时 lease 直接拒绝**（安全的失败）、**revert vs rebase --onto 哈希对照**（证明历史被改写）、**revert the revert**（撤销本身可被撤销）、**gc 后 cat-file 返回 128**（唯一真丢）。核验脚本 V1–V81（81 条断言）全部通过 |
| 2026-09-09 | 课 7《远端仓库与同步》 | 主 agent 内联（pedagogy + learner） | 0（修复后） | 初版 P1×4：① 「`push --delete` 后本方 `origin/topic` 仍残留」**实测推翻**——执行删除方其跟踪引用会一并清掉，仅他方残留 → 按实测改写，计入必查项 #30；② 脚本 6 处 `git cmd | head` 后取 `$?`，拿到 head 的 0 而非真实 128（第 3/7/9/12/13/18 节）→ 全改为"重定向文件→取 `$?`→再 head"；③ `--force-with-lease` 场景设计失效（本方落后触发 `fetch first` 而非 `stale info`）→ 改为"本方先推使领先→他人偷偷推→本方未 fetch"，实测 `stale info` exit 1 拦住；④ 核验脚本 V1 在 `alice/` 内 clone 致 bob 落入子目录、`cd` 失败**脚本第 21 行中断** → 补 `cd "$R"`。实测新增 7 条硬数据：**`git pull` 分叉 2.34 起直接 fatal**（联网核实：2.27 加警告 → 2.34 升级 fatal → 2.35 修"已最新"误报；与课 6 的 ort 同属 2.34 变更）、远程跟踪分支纯本地（远端 mv 走后 status exit 0、仅 fetch 128，且 `update-ref` 可直接改写 exit 0）、`--force-with-lease` 报 `stale info` 拒绝 vs `--force` 致 2 提交消失、引用可能打包进 `packed-refs`（直接翻文件扑空）、推非裸仓库检出分支被拒（`refusing to update checked out branch`）、`add/add` 冲突（状态码 `AA`）、普通 fetch 不清理已删分支引用需 `--prune` |
