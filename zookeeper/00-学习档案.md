# ZooKeeper 课程学习档案

> 主题：Apache ZooKeeper 系统学习（决策参考导向）
> 创建日期：2026-08-27
> 模式：课程制（原 4 阶段 / 12 课 / 36 知识点；2026-08-31 大纲调整 → **5 阶段 / 15 课 / 47 知识点**）

## 学习者画像

| 项目 | 值 | 来源 |
|------|-----|------|
| 主题 | ZooKeeper 系统学习 | 用户指定 |
| 当前水平 | 入门（听说过/装过/简单用过，未系统理解机制） | 用户指定 |
| 学习目标 | 决策参考（评估要不要在项目里用 ZK，与 etcd/Consul 等对比做选型） | 用户指定 |
| 输出格式 | Markdown + SVG/Mermaid，每课带「决策视角」小结 | 默认值 |
| 关联背景 | 已学 kafka-basics：了解 Kafka 曾依赖 ZK、KRaft 去 ZK 化 | 仓库档案 |
| **零基础补丁**（2026-08-31） | 原大纲实操为纸面推演，默认读者已能把 ZK 跑起来，对零基础不成立 → 新增阶段 0 · 上手篇（L0.1–L0.3）：原生安装启动 / zkCli 增删改查 / Python kazoo 客户端；环境方式按用户指定**只用原生安装，不引入 Docker**；客户端语言按用户指定**只用 Python kazoo** | 用户反馈 |

## 课程故事主线

以「电商公司后端工程师评估是否引入 ZooKeeper」贯穿：评审会上被问"你自己跑过吗"（L0.1–L0.3 上手）→ 大促夜双主故障（L1）→ 弄懂机制（L2–L6）→ 算清成本（L7–L8）→ 横向对比与生态趋势（L9–L10）→ 选型决策树与场景演练（L11–L12）。终点产物：一份能向团队汇报的《协调服务选型决策》。

## 知识点级进度表

| 阶段 | 课 | 知识点 | 状态 | 完成日期 | 小测成绩 |
|------|-----|--------|------|----------|----------|
| 0 | 课0.1 | JDK 与发行包 | ✅ 已完成 | 2026-08-31 | - |
| 0 | 课0.1 | zoo.cfg 最小配置 | ✅ 已完成 | 2026-08-31 | - |
| 0 | 课0.1 | 启停与状态检视 | ✅ 已完成 | 2026-08-31 | - |
| 0 | 课0.2 | CLI 命令地图 | ✅ 已完成 | 2026-08-31 | - |
| 0 | 课0.2 | 四种节点亲手创建 | ✅ 已完成 | 2026-08-31 | - |
| 0 | 课0.2 | 版本 CAS 与报错对照 | ✅ 已完成 | 2026-08-31 | - |
| 0 | 课0.2 | Watch 与临时节点初体验 | ✅ 已完成 | 2026-08-31 | - |
| 0 | 课0.3 | kazoo 安装与连接骨架 | ✅ 已完成 | 2026-08-31 | - |
| 0 | 课0.3 | CRUD 与 bytes 陷阱 | ✅ 已完成 | 2026-08-31 | - |
| 0 | 课0.3 | 连接状态监听 | ✅ 已完成 | 2026-08-31 | - |
| 0 | 课0.3 | DataWatch 与 retry | ✅ 已完成 | 2026-08-31 | - |
| 1 | 课1 | 双主与脑裂的本质 | ✅ 已完成 | 2026-08-27 | - |
| 1 | 课1 | 三道协调难题 | ✅ 已完成 | 2026-08-27 | - |
| 1 | 课1 | 为什么 MySQL+Redis 凑不齐 | ✅ 已完成 | 2026-08-27 | - |
| 1 | 课2 | 一句话定位与能力地图 | ✅ 已完成 | 2026-08-28 | - |
| 1 | 课2 | 起源与生态位 | ✅ 已完成 | 2026-08-28 | - |
| 1 | 课2 | 小而 CP 的设计哲学 | ✅ 已完成 | 2026-08-28 | - |
| 2 | 课3 | ZNode 树与路径 | ✅ 已完成 | 2026-08-28 | - |
| 2 | 课3 | 四种节点类型 | ✅ 已完成 | 2026-08-28 | - |
| 2 | 课3 | zxid 与版本控制 | ✅ 已完成 | 2026-08-28 | - |
| 2 | 课4 | 服务端角色与过半机制 | ✅ 已完成 | 2026-08-28 | - |
| 2 | 课4 | ZAB 两阶段流程 | ✅ 已完成 | 2026-08-28 | - |
| 2 | 课4 | 读写路径与性能特点 | ✅ 已完成 | 2026-08-28 | - |
| 2 | 课5 | 会话与心跳超时 | ✅ 已完成 | 2026-08-28 | - |
| 2 | 课5 | 临时节点生命周期 | ✅ 已完成 | 2026-08-28 | - |
| 2 | 课5 | Watch 触发机制与演进 | ✅ 已完成 | 2026-08-28 | - |
| 2 | 课6 | 配置中心与服务发现 | ✅ 已完成 | 2026-08-28 | - |
| 2 | 课6 | Leader 选举实现 | ✅ 已完成 | 2026-08-28 | - |
| 2 | 课6 | 分布式锁与 Curator | ✅ 已完成 | 2026-08-28 | - |
| 3 | 课7 | 部署形态与容量规划 | ✅ 已完成 | 2026-08-28 | - |
| 3 | 课7 | 事务日志与快照 | ✅ 已完成 | 2026-08-28 | - |
| 3 | 课7 | 监控与日常运维 | ✅ 已完成 | 2026-08-28 | - |
| 3 | 课8 | Watch 风暴与羊群效应 | ✅ 已完成 | 2026-08-28 | - |
| 3 | 课8 | Session 超时类故障 | ✅ 已完成 | 2026-08-28 | - |
| 3 | 课8 | 客户端反模式 | ✅ 已完成 | 2026-08-28 | - |
| 4 | 课9 | 四候选速览 | ✅ 已完成 | 2026-08-28 | - |
| 4 | 课9 | 关键维度对比 | ✅ 已完成 | 2026-08-28 | - |
| 4 | 课9 | ZAB 与 Raft 差异 | ✅ 已完成 | 2026-08-28 | - |
| 4 | 课10 | Kafka 去 ZK 时间线 | ✅ 已完成 | 2026-08-28 | - |
| 4 | 课10 | 云原生时代的定位 | ✅ 已完成 | 2026-08-28 | - |
| 4 | 课10 | 社区现状 | ✅ 已完成 | 2026-08-28 | - |
| 4 | 课11 | 选型决策树 | ✅ 已完成 | 2026-08-28 | - |
| 4 | 课11 | 成本收益清单 | ✅ 已完成 | 2026-08-28 | - |
| 4 | 课11 | 向团队汇报要点 | ✅ 已完成 | 2026-08-28 | - |
| 4 | 课12 | 注册中心选型 | ✅ 已完成 | 2026-08-28 | - |
| 4 | 课12 | 配置管理选型 | ✅ 已完成 | 2026-08-28 | - |
| 4 | 课12 | 分布式锁方案 | ✅ 已完成 | 2026-08-28 | - |

## 评审记录

> 评审方式：`主 agent 内联`（A：技术事实核查；B：零基础教学体验。course-reviewer 子 agent 未创建，独立性受限，可随时让我按 create-sub-agent 规范创建以获得更独立视角）。

| 日期 | 评审对象 | 评审方式 | 意见摘要 | 处置 |
|------|----------|----------|----------|------|
| 2026-08-31 | **阶段 0 · 上手篇批次**（L0.1 环境搭建 / L0.2 zkCli 增删改查 / L0.3 Python kazoo 入门，共 11 知识点）+ 大纲与目录联动调整 | 主 agent 内联（A 事实核查 + B 零基础教学体验） | **P0×0 / P1×2 / P2×2，全部当批修复。** P1——①`判案三连 #3` 原写"`get -w /x`（/x 不存在）能收到 NodeCreated"**错误**：按官方 Watch 事件矩阵 Created 事件只能由 exists 触发，且 getData 对不存在节点直接抛 NoNode；已改为 `stat -w /x`（zkCli 的 stat 对应 API 层 exists），并在答案中补"get -w 设不上监听"的反向提醒。②清华镜像 URL 原写死 `zookeeper-3.9.5` 目录（未核实该镜像是否已同步 3.9.5）；已改为给镜像根目录 + 说明"各镜像同步有先后，没有就退回官网或用 3.8.6"并标 ⏳。P2——①kazoo 状态迁移图中 `CONNECTED→LOST: stop()` 一条补注来源（官方 Valid State Transitions 表中 CONNECTED→LOST 仅列"认证凭据无效"，stop() 一条来自官方散文说明，二者并列需标明以免误读为官方表格原文）；②L0.1 提示符说明补充：官方文档简写 `[zkshell: N]` 与实际终端 `[zk: host:port(CONNECTED) N]` 为同一物，避免读者困惑。事实面：3.9.5 current / 3.8.6 stable / 3.7.2 EOL 2024-02-02（官网 releases 原文）、`-bin` 包区分、JDK 官方原文 1.8+ 且不支持 9/10（JDK 17 标 ⏳ 中）、Support Matrix 平台表（macOS 仅 Development Only）、单机 zoo.cfg 三行与 dataDir "existing (empty to start with)"、zkServer.sh 八子命令与 upgrade 废弃、zookeeper.out 默认落启动目录（ZOO_LOG_DIR）、zkCli 连接参数与三重提示符、help 完整清单（无 exists、未列 addWatch）、七条报错文案、Watch 事件格式与 stat/get/ls 三类触发、addWatch 标 ⏳ 中、kazoo 2.11.0/2026-03-21/Python 3.8–3.14/Production-Stable、KazooClient(hosts,timeout=10.0) 与 create/set/get/delete/ensure_path 全签名、KazooState 五条合法迁移与官方"LOST 后临时节点被清、锁需重新获取"原文、DataWatch 立即回调+返回 False 停止、TypeError 精确文案（源码行可证）——**全部核实无误**。教学体验面：三课均满足"类比/冲突开场 + Mermaid 图 + 可照抄命令 + 预期输出 + 🐞 误区 + 速查卡"；L0.3 附全课程实验地图解决"阶段 0 学完阶段 2 又变回只读"的断层 | 通过，落盘 |
| 2026-08-27 | 多阶段大纲 | 主 agent 内联（pedagogy） | P0×0 / P1×0 / P2×1（课程目录随开讲标注状态，已采纳）；4 阶段 12 课依赖链无缺口（问题→机制→成本→决策），决策参考导向与实操轻量化定位匹配 | 通过，落盘 |
| 2026-08-27 | 课 1 批次（为什么需要 ZooKeeper） | 主 agent 内联（A 事实核查 + B 教学体验） | P0×0 / P1×2 / P2×2，全部当批修复：①"Redis 是单点的"绝对化表述收敛为限定场景+故障转移窗口说明；②多数派补"两个过半集合必有交集"通俗解释；③PX 毫秒单位注释；④补术语速查卡。事实面：双主/脑裂、多数派、SET NX PX、GC 停顿锁过期论证均核实无误 | 通过，落盘 |
| 2026-08-28 | 课 2 批次（ZooKeeper 是什么：定位与能力地图） | 主 agent 内联（A 事实核查 + B 教学体验） | P0×0 / P1×0 / P2×2，当批修复：①立项年份"2006 年"软化为"2006 年前后"（各来源 2006/2007 两说）；②"官网定义"精确为"官方文档定义"。事实面：jute.maxbuffer 默认 1048575 字节与"KB 级设计"官方原文、TLP 时间 2010-11（ASF 董事会纪要）、名字由来（作者自述）、CAP/CP 定位与少数派分区拒写、zxid/Watch/临时节点语义均核实无误 | 通过，落盘 |
| 2026-08-28 | 课 3 批次（数据模型：ZNode 树与四种节点） | 主 agent 内联（A 事实核查 + B 教学体验） | P0×0 / P1×0 / P2×2，当批修复：①"会话一断即自动删除"补超时窗口限定（表格措辞+⚠️提示+演练答案三处收敛，防"断连即删"错误心智模型）；②zkCli 示例引号统一为官方演示的双引号风格。事实面：zxid 64 位结构（高 32 epoch/低 32 counter，官方 Internals）、stat 11 字段语义、临时节点禁子节点、顺序节点 %010d 与父节点计数器、容器/TTL 节点 3.5.3 与默认关闭、version CAS 与 BadVersion、-1 跳过检查均核实无误；判案 stat 输出内部一致（cversion/numChildren/pzxid/mzxid 时间线自洽） | 通过，落盘 |
| 2026-08-28 | 课 4 批次（集群与 ZAB：过半派的生存智慧） | 主 agent 内联（A 事实核查 + B 教学体验） | P0×0 / P1×0 / P2×2，当批修复：①演练 B 答案中 s5 恢复方式精确化——s5 落点在新 Leader 已提交位置之前应走 DIFF 补账，TRUNC 属于"原 Leader 复活带着未确认提案"的另一类机器；②演练 B 题干补"无在途未确认提案"假设，消除最后落盘 zxid 语义歧义。事实面：ZAB 两模式与广播/恢复流程（PROPOSE→落盘→ACK→过半即提交→COMMIT，无中断分支）、FLE 比较规则（epoch>zxid>myid）与新 Leader epoch+1、Observer 3.3.0 非投票定位与"加 Follower 拖慢写"（官方 Observers 文档）、2F+1 与奇数原则（Admin Guide 原文"6 台只容 2 台"）、过半="能互相通信的多数"、DIFF/TRUNC/SNAP（Apache cwiki 一手协议文档）、读写路径与 sync()（官方 Programmer's Guide）、选举 <200ms（官方基准）均核实无误；演练 zxid 数值与选举推演经内部一致性验算 | 通过，落盘 |
| 2026-08-28 | 课 5 批次（会话与 Watch：临时节点和一次性通知） | 主 agent 内联（A 事实核查 + B 教学体验） | P0×0 / P1×0 / P2×4，当批修复：①第一幕积木拆解表述精确化（过半是集群机制非积木，zxid"谁分配"在 L4 拆完）；②演练 A 超时改 15 秒使 kill→删除 12~14 秒与工单"十几秒"算术自洽（原 10 秒口径仅 7~9 秒）；③watch 遗漏场景"唯二"纠正为"唯一"（官方原文 "There is one case"）；④保证②括号注释改准确表述（防误读为"不漏事件"）+演练 B set 命令统一双引号风格。事实面：会话凭据（64 位 sessionId+password 任何服务器可验）、超时协商钳制 [2×tickTime, 20×tickTime]（Admin Guide）、任何请求续期+空闲 PING（官方原文）、过期由集群裁决/判死即删临时节点+全网通知/死亡通知等复活送达（官方原文）、Watch 定义/两张表/事件矩阵/触发规则/三保证/双注册单回调/断连 safe mode/重连重注册+exists 漏网之鱼（官方 Programmer's Guide r3.9）、3.6.0 持久 Watch 与递归不发 NodeChildrenChanged（官方 + AddWatchMode API）、removeWatches 均核实无误；心跳 1/3、SessionTracker 分桶标注为实现细节 | 通过，落盘 |
| 2026-08-28 | 课 6 批次（四大经典用法：从配置中心到分布式锁） | 主 agent 内联（A 事实核查 + B 教学体验） | P0×0 / P1×1 / P2×2，当批修复：P1——"失败的抢主 create 是过半写"纠正（NodeExists 前置检查失败不产生提案不付过半写，真实代价是 31 次通知 + 30 次请求涌向唯一 Leader，事故①/败局一两处）；P2——败局二"ZK 锁挨得更深"软化为"也不例外"（Kleppmann 批评对 Redis/ZK 同等成立）、总表"zxid 全序防双主"改"全序编号保证最小者唯一"。事实面：选主配方原文（朴素方案官方点名羊群效应与 getChildren 操作尖峰、watch 前驱 "not having all clients watching the same znode"、"最小号≠知道自己是主"官方 Note 与就任宣告建议）、锁配方五步（getChildren 不带 watch 避羊群、"每个节点恰好只被一个客户端 watch，节点删除只唤醒一个"、exists 返回 false 回第 2 步重校准、释放=delete、无轮询无超时）、羊群效应官方定义（releasing a herd when in fact only a single or a small number of machines can proceed）、sync 边界（同连接通知后重读新鲜/跨客户端因果链需要，官方 A/B 示例）、fencing token（Kleppmann 原文"可用 zxid 或 znode 版本号"）、czxid 后创建者必更大、Curator（官方 Recipes 除 2PC 全实现、InterProcessMutex 可重入、LeaderLatch/LeaderSelector 自动重排队、CuratorCache 取代三件已废弃缓存、curator-x-discovery、recipes 自动建 CONTAINER 父节点、5.x 配 ZK 3.5+/CuratorCache 与持久 watch 需 3.6+/最后 3.4 兼容版 4.2——官方 committer 邮件列表）均核实无误；演练 A 守望链推演与演练 B 时间线经内部一致性验算 | 通过，落盘 |
| 2026-08-28 | 课 7 批次（部署与运维成本：3 节点起步的小集群） | 主 agent 内联（A 事实核查 + B 教学体验） | P0×0 / P1×0 / P2×3，当批修复：①"fuzzy snapshot"出处由"官方 Internals 称之为"软化为"常被称为"（通行称谓，避免点名无法确证的官方原文出处）；②第一幕"四个模块"改"四张配方"对齐 L6 用语；③演练 B 案3 补新节点先以 Observer 身份入队同步的稳妥做法并标注"社区通行做法"（官方文档无此明确步骤原文）。事实面：2F+1/奇数/故障独立性（同交换机电源制冷为关联故障）、事务日志专用设备（"专用分区都不够"）与顺序写不寻道/共享盘 multi-second delays、"The disk is death to ZooKeeper"与 4G 机器 3G 堆/压测估算、单存储设备时调大 snapCount 官方缓解法、dataLogDir 分离"对吞吐与稳定延迟影响巨大"、与其他应用争抢资源 performance will suffer markedly、默认不清快照与日志（operator 责任原文）、PurgeTxnLog cron+>3 份防损坏、snapCount 默认 10 万+[snapCount/2+1,snapCount] 随机防全员同时快照、preAllocSize 64M、"快照取代之前所有日志"、autopurge 3.4.0+（snapRetainCount 默认/最小 3、purgeInterval 默认 0）、tickTime 2000/initLimit 10（数据量大调大）/syncLimit 5（掉队太远被丢弃）/clientPort 2181、maxClientCnxns 60（防 DoS 与 fd 耗尽）、globalOutstandingLimit 1000（超额限流）、myid 1~255、2888=Follower 连 Leader/3888=选举、4lw.commands.whitelist 3.5.3 起默认仅 srvr（zkServer.sh 自用）与报错原文、AdminServer 3.5+ HTTP 8080、Prometheus MetricsProvider 3.6+（className/httpPort 7000、官方"最简单方式"推荐语）、Monitor Guide 参考告警 7 条原文（znode_count>100 万/连接>50/数据>1GB/watch>1 万/选举发生/fd>300/fsync rate>100）、fsync.warningthresholdms 3.3.4+ 默认 1000ms、reconfig（3.5.0 前滚动重启官方定性"manually intensive and error-prone, has caused data loss and inconsistency in production"、3.5.0+ "performed immediately, just like other operations"、存 /zookeeper/config、客户端自动更新连接串、3.5.3 起默认禁用需 reconfigEnabled=true 与 3.5.3 前无安全强制的理由、滚动升级 3.4→3.5 先经 3.4.6）、跨机房 2+1 少数派拒写（L4 已核机制推导）、Yahoo 双核 2G 专用物理机（Cloudera 社区经验，已标注非官方）均核实无误 | 通过，落盘 |
| 2026-08-28 | 课 12 批次（场景演练：三个真实项目的选型决策·收官） | 主 agent 内联（A 事实核查 + B 教学体验） | P0×0 / P1×0 / P2×2，当批修复：①注册中心结论的"Pinterest 案例"标注升级为⚠️汇报提醒（二手来源，方向与官方机制一致，但公司名与数字不得写进汇报，应改写官方机制原文"CoreDNS 自动为每个 Service 建 DNS 记录、Pod 不自己注册、只有 readiness 通过的 Pod 才收流量"）；②锁获取延迟统一标注（etcd/ZK 各来源分别给出 5–10ms/10–50ms/50–200ms 等多种二手口径，差异大，决策时只取"共识锁需过半写、比 Redis 慢一个量级"的方向性结论，不写具体数值进汇报）。事实面：K8s 官方文档（CoreDNS 监视 Kubernetes API 为每个 Service 自动创建 DNS 记录、Pod 经 /etc/resolv.conf 指向 CoreDNS 按服务名解析、Pod 不自己注册、Endpoints/EndpointSlice 控制器按 label selector 自动维护后端、只有 readinessProbe 通过的 Pod 才收流量、环境变量方式需先建 Service 否则不可见故 DNS 为主流）——手；Curator 官方 Shared Reentrant Lock（InterProcessMutex 可重入、同一线程多次 acquire 须等量 release、实例可复用、makeRevocable 协作式撤销、强烈建议 ConnectionStateListener 监听 SUSPENDED 与 LOST——SUSPENDED 不能确定仍持锁除非随后 RECONNECTED、LOST 确定已不再持锁）；fencing token 机制（ZK 用 zxid/czxid、etcd 用 revision/mod_revision 作单调 token，资源层记住最高 token 并拒绝更旧者；"The lock service is not the lock; the resource's rejection of stale tokens is the lock"；Chubby sequencer 为工业界最早的 fencing token 形态；资源层不支持条件写时任何锁服务都无法保证互斥）；Redis 单实例锁故障转移 SAFETY VIOLATION 与 Redlock 争论及 Kleppmann "no matter what lock service you use"（L6 已核沿用）；Nacos/Apollo/etcd 配置中心能力对比与"求简 Nacos 求全 Apollo"口诀（二手，产品定位与官网自述一致，已标注勿写公司名与数字进汇报） | 通过，落盘 |

## 大纲调整记录

| 日期 | 调整内容 | 原因 |
|------|----------|------|
| 2026-08-27 | 初始大纲落盘（4 阶段 / 12 课 / 36 知识点） | 用户要求系统学习 ZooKeeper，画像：入门 + 决策参考 |
| 2026-08-31 | **新增阶段 0 · 上手篇（L0.1–L0.3，11 知识点）→ 5 阶段 / 15 课 / 47 知识点**；同步更新 01-学习路径总览（阶段地图 + 画像补丁 + 节奏说明）、02-课程目录（新增阶段 0 表 + 顶部零基础提示）、L1 头部与尾部导航（回指 L0.3） | 用户反馈：原课程"缺少基础用法和讲解，是给有一定基础的人看的，不是零基础小白"。核查属实——原 12 讲的「第四幕 · 实操演练」全部为纸面推演（判案/选材质/算容量），全课程 0 处可运行命令 |

## 阶段 0 补充决策记录（2026-08-31）

| 决策项 | 选择 | 依据 |
|--------|------|------|
| 新手内容放哪 | 新增独立「阶段 0 · 上手篇」，编号 L0.1–L0.3 | 用户从三方案中选定。**关键收益：L1–L12 原编号与全部交叉链接零改动**（另两方案需改动 12 个已交付文件） |
| 环境方式 | **只用原生安装（JDK + tar 包 + zoo.cfg），不引入 Docker** | 用户明确指定："不要使用 docker 作为实例，而是使用原生的安装方式" |
| 客户端语言 | **Python + kazoo**（不给 Java/Curator，不做双份） | 用户选定推荐项。Java Curator 已在 L6/L12 引用，此处不重复 |
| 与 L1–L12 的分工 | 阶段 0 只教"怎么做"和"看到什么"，"为什么"留给 L3/L5/L6 | 避免与已交付的机制课重复；先让手熟再让脑通 |
| 衔接设计 | L0.3 末尾附「全课程实验地图」，把 L3–L8 每课配一个可亲手验证的实验；L1 头部/尾部加回指链接 | 解决"阶段 0 学完、阶段 2 又变回只读"的断层 |

## 事实核查记录

| 条目 | 结论 | 置信度 | 核查时间 |
|------|------|--------|----------|
| ZK 起源：Yahoo 开发，2008 年进入 Apache（Hadoop 子项目），后成为顶级项目 | 已核实 | 高 | 2026-08-27 |
| 成为 Apache 顶级项目：ASF 董事会纪要 2010-11-17 决议 "Establish the Apache ZooKeeper Project"；部分材料记 2011 年 1 月（脱离 Hadoop 收尾） | 已核实：讲义采用"2010 年 11 月"（以 ASF 董事会会议纪要为准） | 高 | 2026-08-28 |
| 当前版本：3.9.5（2026-02-11 发布，current 线）/ 3.8.6（stable 线）/ 3.7 已 EOL | 已核实（官网 releases + news 页） | 高 | 2026-08-27 |
| Kafka KRaft 去 ZK 时间线（2.8 预览→3.3 生产→4.0 移除 ZK） | 已核实（kafka-basics 课程已核，沿用） | 高 | 2026-08 |
| Redis `SET key val NX PX 30000` 语义（NX 仅不存在时写入 / PX 毫秒级过期） | 已核实 | 高 | 2026-08-27 |
| GC 停顿/网络分区致分布式锁双持有（Kleppmann 对 Redlock 的批评论点） | 已核实 | 高 | 2026-08-27 |
| jute.maxbuffer 默认 0xfffff（1048575 字节，略小于 1MB）；官方文档原文 "ZooKeeper is designed to store data on the order of kilobytes"，不建议生产调大 | 已核实（官方 Administrator's Guide） | 高 | 2026-08-28 |
| 名字由来：作者自述（ZooKeeper 原书）——Yahoo 动物项目众多，"快成动物园了"的玩笑 + "分布式系统本身就是个 zoo" | 已核实（原书出处） | 高 | 2026-08-28 |
| 官网自我定位 "a proven coordination kernel"；经典定义句（configuration/naming/distributed synchronization/group services）仍在现行官方文档 | 已核实（官网 + 官方文档） | 高 | 2026-08-28 |
| zxid 结构：64 位，高 32 位 epoch（Leader 任期）/ 低 32 位 counter；zxid 单调递增，比较大小即发生先后（官方 ZooKeeper Internals） | 已核实（官方 Internals 文档） | 高 | 2026-08-28 |
| stat 结构 11 字段语义（czxid/mzxid/pzxid/ctime/mtime/version/cversion/aversion/ephemeralOwner/dataLength/numChildren）；pzxid 只记录直接子节点名单变动，孙辈变更与子节点改数据不影响 | 已核实（官方 Programmer's Guide r3.9） | 高 | 2026-08-28 |
| 顺序节点：计数器 %010d 十位补零、由父节点维护、单调递增不复用；带符号 32 位 int 溢出为负数（官方文档标注的冷知识；未引用具体溢出值——各版本文档有 -2147483647/-2147483648 两说） | 已核实（官方 Programmer's Guide） | 高 | 2026-08-28 |
| 临时节点不能有子节点；容器节点与 TTL 节点为 3.5.3 新增，TTL 默认关闭（extendedTypesEnabled=true 开启）且仅作用于持久类节点 | 已核实（官方 Programmer's Guide r3.9） | 高 | 2026-08-28 |
| setData 携带 version 不匹配抛 KeeperException.BadVersion；传 -1 跳过版本检查（CAS/乐观锁语义）；version 创建时为 0、每改一次 +1 | 已核实（官方 API 文档） | 高 | 2026-08-28 |
| ZAB 两模式与流程：消息广播=类 2PC（PROPOSE→Follower 落盘事务日志→ACK→Leader 收过半 ACK（含自己）→广播 COMMIT→全网按 zxid 序 apply；无中断/回滚分支）；崩溃恢复=选举+同步，恢复两铁律（已提交不丢、未提交不复活）；新 Leader 让 epoch+1 | 已核实（官方 zookeeperOver + Apache cwiki "Zab in words" 一手协议文档） | 高 | 2026-08-28 |
| Fast Leader Election：选票 (epoch, zxid, myid) 字典序比较，epoch 大者优先→zxid 大者优先→myid 大者兜底；初始投自己、得过半选票者当选；epoch 占 zxid 高 32 位故整体 zxid 比较天然先比朝代 | 已核实（多源一致 + cwiki） | 高 | 2026-08-28 |
| Observer：3.3.0 引入（ZOOKEEPER-368，Fix Version 3.3.0；官方发布公告 "observers - non-voting members of the ensemble, scale reads"）；非投票成员、不计入 quorum、挂掉不影响可用性、可跨机房部署；官方文档明确"投票成员越多写性能越降" | 已核实（官方 Observers 文档 + JIRA + 3.3.0 发布公告） | 高 | 2026-08-28 |
| 容量公式：容忍 F 台故障需部署 2F+1 台；"6 台只能容忍 2 台故障，剩 3 台不是多数"→官方建议奇数台；过半定义="能互相通信的多数"（机器活着但分区隔离不算） | 已核实（官方 Administrator's Guide 原文） | 高 | 2026-08-28 |
| 读写路径：读由所连服务器本地服务（可能读到旧值，读前 sync() 补救，官方 A/B 客户端示例）；所有写请求转发给唯一 Leader，写不可水平扩展；官方定位"读远多于写的场景性能尤佳"；官方基准观察新 Leader 选举 <200ms | 已核实（官方 zookeeperOver + Programmer's Guide） | 高 | 2026-08-28 |
| 恢复期同步三件套：DIFF（补差异）/ TRUNC（截断超出新 Leader 已提交位置的未确认账）/ SNAP（落后过远时整卷快照） | 已核实（Apache cwiki 协议文档：DIFF(13)/TRUNC(14)/SNAP(15)） | 高 | 2026-08-28 |
| 会话机制：64 位 sessionId + password 凭据（换机重连出示、任何服务器可验证）；客户端随机挑一台连接、失败自动换下一台；断连期间客户端自动轮询重连（官方不建议手动新建会话，仅 SessionExpired 时强制新建） | 已核实（官方 Programmer's Guide） | 高 | 2026-08-28 |
| 超时协商：客户端申请、服务端钳制到 [minSessionTimeout, maxSessionTimeout]；默认 [2×tickTime, 20×tickTime]，tickTime 默认 2000ms → 窗口 4~40s；两个配置项 3.3.0+；超范围静默拉回、客户端可查询协商值 | 已核实（官方 Programmer's Guide + Administrator's Guide） | 高 | 2026-08-28 |
| 过期裁决：官方原文 "Session expiration is managed by the ZooKeeper cluster itself, not by the client"；判死=超时窗口内无任何消息；判死即删该会话全部临时节点并立即通知所有相关 watch 客户端；断连期间收不到过期通知、重连建立后才收到 Expired；任何请求给会话续期、空闲才发 PING（PING 时机保守；Java 实现约 1/3 空闲发 PING、约 2/3 无响应换机——实现细节）；SessionTracker 分桶轮检、桶粒度对齐 tickTime（实现细节） | 已核实（官方 Programmer's Guide 原文 + 源码分析标注实现细节） | 高 | 2026-08-28 |
| Watch 语义：官方定义 "a watch event is one-time trigger"；exists/getData/getChildren 注册；数据表（getData·exists）与子名单表（getChildren）；事件矩阵（Created←exists；Deleted←三者；Changed←exists·getData；Child←getChildren）；create 打新节点数据表+父节点子表、delete 打三张表、setData 打数据表；三保证（全局有序/先通知后见新数据/事件序=更新序）；同一 watcher 双注册单次变更只回调一次；断连期间收不到任何 watch 事件+session 事件广播全部 watcher（safe mode 建议）；重连自动重注册、exists 挂未出生节点在断连期间被创建又删除则通知永久丢失（官方唯一遗漏场景）；cannot reliably see every change；WatchedEvent 仅 state/type/path 不带数据 | 已核实（官方 Programmer's Guide r3.9.4） | 高 | 2026-08-28 |
| Watch 演进：removeWatches 显式退订（local 标志可离线移除）；3.6.0 addWatch 持久 Watch——PERSISTENT（等价 exists+getData 常驻）、PERSISTENT_RECURSIVE（整棵子树递归）；触发 NodeCreated/NodeDeleted/NodeDataChanged；递归模式不触发 NodeChildrenChanged（官方原文 "as it would be redundant"）；removeWatches(WatcherType.Any) 移除；除一次性外其余保证与标准 watch 相同 | 已核实（官方 Programmer's Guide r3.9 + AddWatchMode javadoc） | 高 | 2026-08-28 |
| 官方选主配方：create("ELECTION/guid-n_", SEQUENCE|EPHEMERAL)，最小号当主；朴素方案（全员 watch 最小节点）官方点名羊群效应——"全体惊醒后集体 getChildren 造成操作尖峰"；正解 watch 前驱（"not having all clients watching the same znode"）；官方 Note"最小号≠知道自己是主"（应用可另建节点宣告 leader 已就任）；Curator LeaderLatch（持有式，直到 close/会话死）/LeaderSelector（回调式 takeLeadership，执行完自动重新排队） | 已核实（官方 Recipes and Solutions r3.4.14 + Curator 官方文档） | 高 | 2026-08-28 |
| 官方锁配方：create("lock-", EPHEMERAL|SEQUENTIAL) → getChildren 不带 watch（官方强调避羊群）→ 最小号持锁 → exists(前驱, watch=true)，返回 null 回第 2 步重新校准；解锁=delete 自己的节点；官方原文"每个节点恰好只被一个客户端 watch，节点删除只唤醒一个客户端""没有轮询、没有超时"；NodeExists 失败的 create 在 Leader 前置检查即拒绝、不产生提案不付过半写（代价是通知风暴+请求涌向唯一 Leader）；抢椅子版每次释放通知全部等待者，N 轮约 O(N²) 次通知 vs 链式 O(N)；配置中心=getData(watch)+setData 触发+重读（官方 recipes 描述）；服务发现=临时节点注册+getChildren(watch) 名单（官方 Use Cases） | 已核实（官方 Recipes and Solutions + 官网 Use Cases；机制推断处已标注） | 高 | 2026-08-28 |
| 配方层一致性边界与 fencing：Watch 通知只能由已 apply 该变更的服务器发出，同连接"通知→重读"新鲜；跨客户端因果链（A 写完经旁路通知 B 读）读前 sync()（官方 A/B 示例，L4 已核沿用）；fencing token——Kleppmann 原文"用 ZooKeeper 做锁服务时，可以用 zxid 或 znode 版本号当 fencing token"；锁节点 czxid 后创建者必更大（zxid 全序单调）；双持有窗口无法从锁服务侧根除（进程停顿对一切租约锁成立，Kleppmann 原文 no matter what lock service you use） | 已核实（官方 Programmer's Guide + Kleppmann《How to do distributed locking》原文） | 高 | 2026-08-28 |
| Curator：官方 Recipes 配方除两阶段提交外全部实现；InterProcessMutex 可重入、InterProcessSemaphoreMutex 不可重入、InterProcessReadWriteLock、Multi Shared Lock；CuratorCache 取代 NodeCache/PathChildrenCache/TreeCache（官方标注 deprecated，replace by CuratorCache；CuratorCacheBridge 在支持持久 watch 时用 CuratorCache 否则退回 TreeCache）；curator-x-discovery（ServiceInstance/ServiceProvider 负载策略）；recipes 自动把路径父节点建为 CONTAINER（官方 caution note）；版本兼容——Curator 5.x 支持 ZK 3.5+（最后的 3.4 兼容版 4.2），CuratorCache 与 PersistentWatcher 依赖 ZK 3.6 特性（官方邮件列表 committer 回复） | 已核实（Curator 官方文档/apidocs/邮件列表） | 高 | 2026-08-28 |
| 部署与硬件（Admin Guide）：2F+1 容错公式与奇数原则（L4 沿用）；故障独立性——"多数机器共用同一交换机则交换机故障是关联故障"，电源/制冷同理；事务日志"must be on a dedicated device（专用分区都不够）"、ZK 顺序写日志不寻道、共享日志设备致争用与 multi-second delays；swap 禁令——"The disk is death to ZooKeeper"，处理一个请求换出磁盘则队列全部跟着换出，4G 机器建议 3G 堆（OS/缓存留内存）、压测是唯一推荐估算法；只有一个存储设备时调大 snapCount 官方缓解法（"不能根除问题"）；dataLogDir 分离——"专用日志设备对吞吐与稳定延迟影响巨大"；ZK 与其他应用争抢存储/CPU/网络/内存时 performance will suffer markedly；社区经验（Cloudera，非官方）：Yahoo 双核 2G 专用物理机部署、别与 Kafka/HDFS 等磁盘大户混部 | 已核实（官方 Administrator's Guide + Cloudera 社区文章已标注） | 高 | 2026-08-28 |
| 参数与维护（Admin Guide）：tickTime 默认 2000ms；initLimit 默认 10 tick（Follower 启动连接同步 Leader 时限，数据量大时调大）；syncLimit 默认 5 tick（运行中请求到 ACK 容忍，"Follower 落后太远会被丢弃"）；clientPort 默认 2181；server.x=host:port1:port2（port1=Follower 连 Leader、port2=选举）；myid 位于 dataDir、ASCII 数字、1~255；dataDir 存快照（未设 dataLogDir 时事务日志同目录）；snapCount 默认 100,000，各服务器在 [snapCount/2+1, snapCount] 随机取触发点防全员同时快照（现行文档）；preAllocSize 默认 64M 预分配防 seeks；"快照取代之前所有日志"；默认不清旧快照与日志（operator 责任原文）；PurgeTxnLog 可 cron 日跑、保留 >3 份（防最近日志损坏留 3 份备份）；autopurge 3.4.0+：snapRetainCount 默认 3（最小 3）、purgeInterval 默认 0=禁用；maxClientCnxns 默认 60（单 IP 到单成员并发连接上限，防 DoS/fd 耗尽，0=不限）；globalOutstandingLimit 默认 1000（积压超额限流客户端）；fsync.warningthresholdms 3.3.4+ 默认 1000ms（fsync 超时打 warning） | 已核实（官方 Administrator's Guide） | 高 | 2026-08-28 |
| 四候选对比（官方一手 + 教学二手，已标注层级）：ZK 官方 Internals 原文（读请求 handled locally at each server 并打 zxid、ZAB 三保证 reliable delivery/total order/causal order、zxid 高 32 位 epoch + 低 32 位 counter、依赖 TCP FIFO、timeouts 只用于活性不用于正确性、quorum 两两至少一交集）；etcd 官方 API guarantees（KV 严格可串行化、watch 五性 Ordered/Unique/Reliable/Atomic/Resumable + Bookmarkable、lease 为分布式协调而生、线性读默认且 serializable 为每次请求可带的降级选项、健康集群 watch 事件约 10ms 而病态集群可能永不到达）；Consul 官方（三种读模式 default/consistent/stale、**DNS 查询默认 stale 且不可逐请求覆盖**、Raft 管服务目录 + Serf gossip 管成员与健康检查传播、每 DC 独立 leader 与不相交 peer set、anti-entropy 以 agent 本地状态为准、BUSL 1.1 自 2023-09 起）；Redis 官方 Redlock 文档（三保证、**故障转移场景官方定性 SAFETY VIOLATION**、页面末尾"一致性免责声明"建议读 Kleppmann 与 antirez 双方文章）；Kleppmann/antirez 论战双方论点（含 antirez 把 NPC 异常完整套用到 ZK 临时节点锁的推演，证明"暂停+锁过期"非 Redlock 独有）；ZAB vs Raft 四条差异（本地读+zxid vs ReadIndex 线性读、Fast Leader Election 主动靠拢(zxid→myid) vs 随机超时+选举限制被动筛选、DIFF/TRUNC/SNAP 恢复前置 vs 边服务边拉齐、Joint Consensus vs ZK 3.5.0 前停机重启）；etcd 默认配额 2GB 超限进只读告警、watch 想续传的 revision 被压缩则被取消（compact_rev 提示） | 已核实（ZK/etcd/Consul/Redis 官方文档 + USENIX ATC14 Raft 论文 + ZK 官方 Internals/Zab1.0；Rutgers 课程讲义已标注为教学二手） | 高 | 2026-08-28 |
| **【阶段 0】** 当前版本：Apache ZooKeeper **3.9.5 = current**，**3.8.6 = latest stable**，3.7.2 **已于 2024-02-02 EOL**；官方同时支持 current 与 stable 两条分支，新 minor 发布后旧 stable 线约 6 个月内退役 | 已核实（官网 releases 页原文） | 高 | 2026-08-31 |
| **【阶段 0】** 发行包必须选 `-bin`（`apache-zookeeper-3.9.5-bin.tar.gz` 为已编译二进制包）；不带 `-bin` 的是源码分发包，需自行 Maven 编译 | 已核实（官网 releases 页两项并列 + 多方一致） | 高 | 2026-08-31 |
| **【阶段 0】** JDK 要求：官方 Administrator's Guide "System Requirements" 原文 "ZooKeeper runs in Java, release 1.8 or greater (JDK 8 LTS, JDK 11 LTS, JDK 12 - Java 9 and 10 are not supported)"。**官方文档未点名 JDK 17/21**——实践上 3.9.x 跑 JDK 11/17 极普遍（二手来源推荐 17） | 已核实（官方原文）；**JDK 17 标注 ⏳ 置信度：中**，讲义已示警"生产用高版本 JDK 务必先在测试环境压一遍" | 高（官方原文） / **中（JDK 17）** | 2026-08-31 |
| **【阶段 0】** 平台支持矩阵：GNU/Linux 全组件支持开发+生产；Windows / Solaris / FreeBSD 的 Java Client+Server 支持开发+生产（Native Client 与 Contrib 不支持）；**Mac OS X 仅 Development Only** | 已核实（官方 Support Matrix 表格原文） | 高 | 2026-08-31 |
| **【阶段 0】** 单机最小 `zoo.cfg` 三行：`tickTime=2000` / `dataDir=/var/lib/zookeeper` / `clientPort=2181`；官方对 dataDir 的要求原文 "an existing (empty to start with) directory"；tickTime "the minimum session timeout will be twice the tickTime"；单机无复制，进程挂则服务挂（官方原文 if the ZooKeeper process fails, the service will go down） | 已核实（官方 Getting Started Guide 原文） | 高 | 2026-08-31 |
| **【阶段 0】** `zkServer.sh` 用法 `{start\|start-foreground\|stop\|version\|restart\|status\|upgrade\|print-cmd}`（`upgrade` 已废弃 Deprecated）；`status` 输出含 `Mode: standalone`（集群下为 leader/follower）；`zookeeper.out` 默认生成在**执行启动命令时的当前目录**（由 `zkEnv.sh` 的 `ZOO_LOG_DIR` 决定） | 已核实（官方 Tools 文档 + 3.9.5 源码树同名文档） | 高 | 2026-08-31 |
| **【阶段 0】** `zkCli.sh` 连接：`-server 127.0.0.1:2181`（不带则默认 localhost:2181）、`-timeout 3000`（毫秒）、`-waitforconnection`、`-client-configuration`；连接成功输出 `Welcome to ZooKeeper!` + `JLine support is enabled` + 提示符 `[zk: 127.0.0.1:2181(CONNECTED) N]`（官方文档简写为 `[zkshell: N]`，讲义已注明二者为同一物） | 已核实（官方 Getting Started + CLI 文档原文） | 高 | 2026-08-31 |
| **【阶段 0】** zkCli `help` 完整命令清单（3.9.x）：addauth / close / config / connect / **create [-s] [-e] [-c] [-t ttl] path [data] [acl]** / **delete [-v version] path** / deleteall / delquota / **get [-s] [-w] path** / getAcl / getAllChildrenNumber / getEphemerals / history / listquota / **ls [-s] [-w] [-R] path** / printwatches / quit / reconfig / redo / removewatches / **set [-s] [-v version] path data** / setAcl / setquota / **stat [-w] path** / sync。**清单中无 `exists`**（判存在请用 `stat`），**亦未列出 `addWatch`** | 已核实（官方 Getting Started 文档 help 原文，两份独立抓取一致） | 高 | 2026-08-31 |
| **【阶段 0】** 报错文案：`Node not empty: /x`（删非空）、`Node does not exist: /x`（路径不存在）、`Node already exists: /x`（重名）、`version No is not valid : /x`（CAS 版本不符）、`Ephemerals cannot have children: /x`、`KeeperErrorCode = NoNode for /x`、`KeeperErrorCode = Unimplemented for /x`（TTL 未开 extendedTypesEnabled） | 已核实（官方 CLI 文档 + 多份真实会话输出一致） | 高 | 2026-08-31 |
| **【阶段 0】** Watch 事件格式 `WATCHER::` + `WatchedEvent state:SyncConnected type:NodeDataChanged path:/x`；`get -w` 触发 NodeDataChanged、`ls -w` 触发 NodeChildrenChanged、`stat -w` 语法支持 | 已核实（官方 CLI 文档示例原文） | 高 | 2026-08-31 |
| **【阶段 0】** `addWatch [-m PERSISTENT\|PERSISTENT_RECURSIVE] path`（3.6.0+，默认 PERSISTENT_RECURSIVE）在 zkCli 中**实际可用**，但**官方 Getting Started 的 help 清单未列出此命令** | ⏳ **置信度：中**——多处真实 zkCli 会话输出可证其可用，但与官方 help 清单冲突；讲义已标注"以你手上版本实际敲 help 为准"，持久 Watch 的正式语义留待 L5 从官方 Programmer's Guide 讲 | **中** | 2026-08-31 |
| **【阶段 0】** kazoo 版本 **2.11.0（2026-03-21 发布）**；Python 支持 **3.8–3.14**（CPython + PyPy），已移除 Python 2；`Development Status :: 5 - Production/Stable`，无弃用/停维护警告；Apache License 2.0 | 已核实（PyPI 官方页 classifiers + changelog） | 高 | 2026-08-31 |
| **【阶段 0】** kazoo API（官方 Basic Usage + Public API 文档原文）：`KazooClient(hosts='127.0.0.1:2181', timeout=10.0)`；`start()` 阻塞至连上或超时；`create(path, value=b'', acl=None, ephemeral=False, sequence=False, makepath=False, include_data=False)` 返回真实路径 str；`set(path, value, version=-1)`；`get()` 返回 `(bytes, ZnodeStat)`；`delete(path, version=-1, recursive=False)`；`ensure_path()` 递归建但不能设数据；`retry(func, *args)`；`KazooRetry(max_tries=3, ignore_expire=False)`；value 上限 1MB 抛 ZookeeperError | 已核实（kazoo 2.11.0 官方文档） | 高 | 2026-08-31 |
| **【阶段 0】** kazoo 连接状态：`KazooState` 三态 LOST / CONNECTED / SUSPENDED 与五条合法迁移（新建为 LOST；CONNECTED→LOST **仅**发生于连接后认证凭据无效）；官方原文 "When a connection transitions to LOST, any ephemeral nodes that have been created will be removed by ZooKeeper… Locks will need to be re-acquired"；官方建议原文：用 Lock 配方或建临时节点时 **highly recommended** 注册 `add_listener` | 已核实（kazoo 官方 Basic Usage 原文） | 高 | 2026-08-31 |
| **【阶段 0】** kazoo `DataWatch` / `ChildrenWatch`：注册时**立即调用一次**，之后每次变更调用，**直到回调返回 `False`**；节点不存在时 `data` 为 `None` | 已核实（kazoo 官方 Basic Usage 原文） | 高 | 2026-08-31 |
| **【阶段 0】** kazoo 传 `str` 给 value 抛 `TypeError: Invalid type for 'value' (must be a byte string)` | 已核实（kazoo 客户端源码 `if value is not None and not isinstance(value, bytes_types): raise TypeError(...)` + 独立 issue 复现一致） | 高 | 2026-08-31 |
| **【阶段 0】** 顺序节点十位补零 `%010d`、计数器由父节点维护且只增不复用；临时节点不能有子节点；容器/TTL 节点 3.5.3+ 且 TTL 默认关闭 | 沿用 L3 已核实结论（官方 Programmer's Guide r3.9），本阶段亲手复现未冲突 | 高 | 2026-08-31 |
| **【阶段 0】** 四字命令 3.5.3 起默认白名单仅 `srvr`；AdminServer 3.5+ 默认 8080 | 沿用 L7 已核实结论（官方 Admin Guide），本阶段引用一致 | 高 | 2026-08-31 |
| Session 超时与客户端反模式（官方 FAQ / Programmer's Guide / JIRA）：CONNECTION_LOSS 结果未知（请求可能已到服务端成功、也可能未发出，客户端库无法区分），程序员须自行判断重试，官方探活示例=检查要创建的节点是否存在 / 检查要修改的 znode 的值；客户端库内置处理 herd effect 的启发式，断连不新建会话、仅 SessionExpired 才新建（mandatory）；SESSION_EXPIRED 自动关闭句柄、"在一个正确运行的集群里你不应该看到 SESSION_EXPIRED"、客户端应视自己已死进入恢复（重建临时节点/争夺领导角色/重建已发布状态），库作者不应尝试恢复而应返回致命错误；会话过期四大诱因（客户端 GC STW 停顿 / 服务端 GC 或 fsync 尖峰 / 网络闪断·防火墙·非对称路由·MTU·DNS / Leader 切换），Curator 默认会话超时 60 秒正是为 GC 留余量（云与虚拟化环境官方明确 5 秒过低）；陈旧临时节点阻塞重注册——新会话建同名路径撞上旧残留（ZOOKEEPER-4837 影响 3.9.2+ 且截至 3.9.5 未修复、更早的 ZOOKEEPER-2919），变通为记下路径+owner sessionId+version 后手工删除；官方 Gotchas 原文：必须测试 ZK 服务器故障（在实验室用多节点集群挨个重启验证）、客户端服务器列表须与集群列表匹配（客户端列表为子集可跑但不最优、列了集群外的服务器则不行）、watch 未出生节点在断连期间被创建又删除则通知永久丢失 | 已核实（官方 FAQ + Programmer's Guide + ZOOKEEPER-4837/2919 JIRA + Netdata 运维指南） | 高 | 2026-08-28 |

## 学习记录

- 2026-08-27：建档开课，画像确认「入门 + 决策参考」；大纲落盘并评审通过；课 1 讲义完成，A/B 双视角评审 P0=0（P1×2、P2×2 当批修复），已交付。
- 2026-08-28：据用户反馈补齐课 1 结尾「下一批接力提示词」+「课程导航」段（仓库既有约定，kafka-basics/design-patterns 每课均有，此前 PromQL L1 也曾漏过）；已将该检查项写入评审清单「每课交付必查项」，后续课次交付前强制核对。
- 2026-08-28：课 2 讲义完成（一句话定位与能力地图 / 起源与生态位 / 小而 CP 的设计哲学），A/B 双视角评审 P0=0（P2×2 当批修复），已交付；阶段 1（问题与定位）全部完成，课 2 结尾必查两段（接力提示词 + 课程导航）齐全。
- 2026-08-28：课 3 讲义完成（ZNode 树与路径 / 四种节点类型 / zxid 与版本控制），进入阶段 2（核心机制）；A/B 双视角评审 P0=0（P2×2 当批修复），已交付；新建阶段 2 目录与导览（overview.md），课 1/课 2 结尾"下一课"链接同步指向已交付课次。
- 2026-08-28：课 4 讲义完成（服务端角色与过半机制 / ZAB 两阶段流程 / 读写路径与性能特点），A/B 双视角评审 P0=0（P2×2 当批修复），已交付；L1"双主之夜"与 L3"epoch 钩子"在本课闭环，课 3 结尾"下一课"链接同步指向课 4。
- 2026-08-28：课 5 讲义完成（会话与心跳超时 / 临时节点生命周期 / Watch 触发机制与演进），A/B 双视角评审 P0=0（P2×4 当批修复），已交付；L2 四块积木集齐，L3"断连即删⚠️"、L4 结尾三问与 sync() 伏笔在本课闭环，课 4 结尾"下一课"链接同步指向课 5。
- 2026-08-28：课 6 讲义完成（配置中心与服务发现 / Leader 选举实现 / 分布式锁与 Curator），A/B 双视角评审 P0=0（P1×1、P2×2 当批修复），已交付；阶段 2（核心机制）全部收官，L1"GC 停顿论证→fencing"、L2"Curator/积木配方"、L3"顺序积木+容器节点"、L4"sync 边界+写瓶颈"、L5"三板斧+子名单坑"在本课全部闭环，课 5 结尾"下一课"链接同步指向课 6。
- 2026-08-28：课 7 讲义完成（部署形态与容量规划 / 事务日志与快照 / 监控与日常运维），A/B 双视角评审 P0=0（P2×3 当批修复），已交付；新建阶段 3 目录与导览（overview.md），L4"2F+1/奇数/tickTime·syncLimit 容量实操"、L2/L3"事务日志+快照 L7 讲"、L5"连接资源与 tickTime 超时钳制"、L6"运维账单预告"在本课全部闭环，课 6 结尾"下一课"链接同步指向课 7；新钩子 watch_count→L8、TCO→L11 已埋。
- 2026-08-28：课 12 讲义完成（注册中心选型 / 配置管理选型 / 分布式锁方案），A/B 双视角评审 P0=0（P2×2 当批修复），已交付；**全部 4 阶段 12 课 36 知识点完结**。L1 双主与协调本质、L6 锁配方与 fencing、L8 坑即边界、L9 对比表与"ZAB/Raft 安全性等价"、L10"结论不可移植、方法可移植"、L11 三件套（决策树/成本账/五段式汇报）在本课全部兑现为三个场景的五段式推演与收官交付物《协调服务选型决策》+8 条自检表；课 11 结尾"预告 L12 收官"已兑现；新钩子"收官后三个方向（写交付物 / 考核 / 延伸）"已埋。
- 2026-08-28：课 11 讲义完成（选型决策树 / 成本收益清单 / 向团队汇报要点），A/B 双视角评审 P0=0（P2×2 当批修复），已交付；L1 双主与协调价值、L2 能力地图、L6 锁的边界与 fencing、L7 运维三张账单、L9 对比表（健康检查/多 DC/一致性档位/运维形态/锁正确性）、L10"单一案例不可外推"在本课全部闭环为可执行判据——决策树把"哪个更好"翻译成"在我的约束下哪个总成本最低"；课 10 结尾"预告 L11"已兑现；新钩子"三场景演练产出《协调服务选型决策》"→L12 已埋。
- 2026-08-28：课 10 讲义完成（Kafka 去 ZK 时间线 / 云原生时代的定位 / 社区现状），A/B 双视角评审 P0=0（P2×2 当批修复），已交付；L4 写不可扩展与 ZAB、L5 watch 一次性语义、L8 重注册间隙丢事件与 watch 数量受限、L9 对比表与"ZAB/Raft 安全性等价、etcd 取代 ZK 是运维原因"在本课全部闭环——KIP-500 一手原文揭示"controller 与 ZK 状态发散、重启 controller 是唯一解法"正是 L5/L8 那些坑在 Kafka 极端用例上的总爆发；L9 结尾"预告 L10"已兑现；新钩子"决策树与成本收益清单"→L11 已埋。

## 断点信息

- 当前位置：**全部 4 阶段 / 12 课 / 36 知识点已完结**（2026-08-28 收官于课 12）；待用户产出收官交付物或选择后续方向
- 下一步：三选一（用户可任选或另提）
  1. **完成收官交付物**（推荐）：用户按课 12 演练 B 模板写出真实项目的《协调服务选型决策》，发给 AI 按 8 条自检表逐条挑刺（重点查：①段是否真的无工具名、⑤段触发条件是否可判定、许可证与看不见的成本是否算过）
  2. **考核巩固**："考我一下 全课程"（跨 12 课选择题，自动评分 + 跨轮次追踪进步）或指定某课（如"考我一下 L5"）
  3. **延伸**：把本课决策方法迁移到别的技术选型（消息队列/缓存/网关），或深入某课机制细节（ZAB 源码级 / Curator 源码级）
- 续学规则：先读 `00-评审清单.md`，存在未勾选条目时先补对应评审，再继续下一批
