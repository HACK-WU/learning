# Apache Doris 学习档案

## 学习者画像

| 项目 | 值 | 来源 |
|------|-----|------|
| 主题 | Apache Doris 系统学习 | 用户指定 |
| 当前水平 | 零基础 | 沿用本仓库 ES / Redis / RabbitMQ 课程画像（用户未另行指定，可调整） |
| 学习目标 | 理解概念 + 动手实操 + 应试认证 + 决策参考（四目标全要） | 沿用本仓库既有课程画像 |
| 输出格式 | Markdown（主载体，Mermaid + SVG 图表） | 默认值 |
| 模式 | 课程制 | 自动判定："系统学习 Doris" 属完整体系 |
| 实操环境 | WSL Ubuntu（Docker 29.4.1，26G 可用内存） | 2026-09-02 实测 |
| 实操对象 | Apache Doris 4.1.3（`apache/doris:all-in-one-4.1.3`，核查于 2026-09） | 2026-09-02 联网核查 |

> 若画像（水平 / 目标）需要调整，告知后按「大纲是活文档」规则同步更新本表 + `01-学习路径总览.md` + 各课实操比重。

## 知识点级进度表

| 阶段 | 课 | 知识点 | 状态 | 完成日期 |
|------|-----|--------|------|----------|
| 1 | 课 1 | OLTP 与 OLAP 的鸿沟 | ✅ 已完成 | 2026-09-02 |
| 1 | 课 1 | Doris 是什么 | ✅ 已完成 | 2026-09-02 |
| 1 | 课 1 | Doris 的典型场景 | ✅ 已完成 | 2026-09-02 |
| 1 | 课 2 | FE 与 BE 架构 | ✅ 已完成 | 2026-09-02 |
| 1 | 课 2 | 用 Docker 起集群 | ✅ 已完成 | 2026-09-02 |
| 1 | 课 2 | 第一张表与第一條查询 | ✅ 已完成 | 2026-09-02 |
| 2 | 课 3 | Duplicate 明细模型 | ✅ 已完成 | 2026-09-02 |
| 2 | 课 3 | Aggregate 聚合模型 | ✅ 已完成 | 2026-09-02 |
| 2 | 课 3 | Unique 主键模型 | ✅ 已完成 | 2026-09-02 |
| 2 | 课 4 | 分区 | ✅ 已完成 | 2026-09-02 |
| 2 | 课 4 | 分桶 | ✅ 已完成 | 2026-09-02 |
| 2 | 课 4 | 分区分桶裁剪 | ✅ 已完成 | 2026-09-02 |
| 2 | 课 5 | Key 列排序与前缀索引 | ✅ 已完成 | 2026-09-02 |
| 2 | 课 5 | 索引家族 | ✅ 已完成 | 2026-09-02 |
| 2 | 课 5 | 同步物化视图（Rollup） | ✅ 已完成 | 2026-09-02 |
| 3 | 课 6 | Stream Load | ✅ 已完成 | 2026-09-02 |
| 3 | 课 6 | Routine Load | ✅ 已完成 | 2026-09-02 |
| 3 | 课 6 | Broker Load 与 INSERT INTO | ✅ 已完成 | 2026-09-02 |
| 3 | 课 7 | MPP 执行流程 | ✅ 已完成 | 2026-09-02 |
| 3 | 课 7 | 向量化执行与列存 | ✅ 已完成 | 2026-09-02 |
| 3 | 课 7 | EXPLAIN 与 Profile | ✅ 已完成 | 2026-09-02 |
| 3 | 课 8 | Join 与分布式 Join 策略 | ✅ 已完成 | 2026-09-02 |
| 3 | 课 8 | 复杂类型与半结构化数据 | ✅ 已完成 | 2026-09-02 |
| 3 | 课 8 | 异步物化视图与查询改写 | ✅ 已完成 | 2026-09-02 |
| 4 | 课 9 | 多副本与自动修复 | ✅ 已完成 | 2026-09-03 |
| 4 | 课 9 | FE 高可用 | ✅ 已完成 | 2026-09-03 |
| 4 | 课 9 | 扩缩容与数据均衡 | ✅ 已完成 | 2026-09-03 |
| 4 | 课 10 | Workload Group 与资源隔离 | ✅ 已完成 | 2026-09-03 |
| 4 | 课 10 | 内存管理与 Spill to Disk | ✅ 已完成 | 2026-09-03 |
| 4 | 课 10 | 查询并发与队列控制 | ✅ 已完成 | 2026-09-03 |
| 4 | 课 11 | Schema Change | ✅ 已完成 | 2026-09-03 |
| 4 | 课 11 | 备份与恢复 | ✅ 已完成 | 2026-09-03 |
| 4 | 课 11 | 监控告警与集群升级 | ✅ 已完成 | 2026-09-03 |
| 4 | 课 12 | Doris 与同类系统对比 | ✅ 已完成 | 2026-09-03 |
| 4 | 课 12 | 存算分离架构 | ✅ 已完成 | 2026-09-03 |
| 4 | 课 12 | 典型场景架构与反模式 | ✅ 已完成 | 2026-09-03 |
| — | Phase 3 | Task 1 · 需求与建模（四层建模 + 维表） | ✅ 已完成 | 2026-09-03 |
| — | Phase 3 | Task 2 · 数据接入（批量 + 实时双链路） | ✅ 已完成 | 2026-09-03 |
| — | Phase 3 | Task 3 · 查询加速与调优（Rollup/MV/Colocate/Profile） | ✅ 已完成 | 2026-09-03 |
| — | Phase 3 | Task 4 · 生产化（副本/隔离/变更/备份） | ✅ 已完成 | 2026-09-03 |
| — | Phase 3 | Task 5 · 验收与边界（五标准 + 五反模式） | ✅ 已完成 | 2026-09-03 |

## 评审记录

> 评审方式：`use_agent 委派`（course-reviewer 子 agent 已创建）或 `主 agent 内联`（子 agent 未创建，独立性受限）。
> 本仓库 `course-reviewer` 子 agent **尚未创建**，当前一律走「主 agent 内联」：先以教学法视角（pedagogy）执行一轮 5 维度，再以学习者视角（learner）执行一轮 6 维度，输出两份独立意见，冲突由主 agent 裁决。

| 日期 | 评审对象 | 评审方式 | 意见摘要 | 处置 |
|------|----------|----------|----------|------|
| 2026-09-03 | 实战经验 / 排障速查手册 / 场景解法库（Phase 5） | 主 agent 内联（pedagogy + learner 双视角，叠用 **B1-B8 / C1-C7 专项评审维度**；子 agent 未创建，独立性受限） | P0×0 / P1×2。**三份一次生成**（同源压缩：从经验层的 10 条故障模式压缩出手册的 10 条 QRH 条目，未分开写）。共 1090 行。P1-1【learner·L2 可导航性】`final-课程手册.md` 仍标注排障手册「待生成」，但文件已产出 → 更新为三份完整导航链接。P1-2【B8 汇总一致性】手册「交付状态」表仍把 Phase 5 标 ⬜ 待生成 → 改 ✅ 已生成并附链接。**专项维度核查**：B1-B8（经验层+手册）全达标——五反模式、10 条五段式（症状可观测 / 根因 / 排查 / 修复 / 预防）、四组 Checklist、2 个可溯源事故；手册五硬特征齐全（索引表 23 处锚点全有效、一眼识别、止血优先、条件-动作分支、升级出口无死胡同）。C1-C7（解法库）全达标——7 场景共 31 个解法（每个 ≥4）、14 个 `<details>` 折叠（提示+解法各一）、每场景知识点回指课时、推荐路径递进、不适用边界明确。**证据纪律**：🟢本机实测 109 / 🔵联网核查 10 / ⚪公认实践 14；⏳低置信 3 处**均未入排障手册**（只留经验层并标注），符合「手册只收确定项」。**宁缺毋滥落实**：解法库从 12 候选筛 7 场景，记录 5 条淘汰理由。**自动化核查**（`assets/tmp-verify-phase5.sh`）：跨文件引用 12 条全可达、23 处锚点全有效、10 条故障模式全部被手册回指、五项硬要求全达标 | 全部采纳并修订落盘后交付。⚠️ 记录一条**工具自身的坑**：锚点校验脚本最初用 sed 做归一化，`\x{4e00}-\x{9fff}` 范围语法 sed 不支持（报 `Invalid range end`），改用 python heredoc 后通过。**这是本轮第二次遇到「核查工具报错 ≠ 内容有错」**（第一次是 Phase 4 的全角括号锚点误报），教训已成形：核查脚本报错时先判是内容错还是工具错，别盲目改内容 |
| 2026-09-03 | 课程手册汇总（Phase 4） | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | P0×0 / P1×2。用户指定**方案 A（索引型手册）**，未走 16000+ 行全量拼接。P1-1【pedagogy·内部一致性】开头写「4 个阶段」，但同文件「学习路径总览」表有 5 个阶段行（含「阶段 5 综合实战」），读者会对不上数 → 改为「4 个教学阶段 + 1 个实战阶段」。P1-2【learner·L2 可导航性】「怎么用这份手册」中「生产上出事了 → 去 `09-排障速查手册.md`」指向 Phase 5 尚未生成的文件，读者点击必然落空 → 标注「待生成」。**汇总完整性核查**（skill 四项硬要求）全达标：12 课全部纳入（脚本校验课条目数 = 12）、路径总览与阶段概览在开头、各课「一图总结」全部保留（19 张 SVG 逐个验证存在）、实战项目已收录。**自动化核查**（`assets/tmp-verify-manual.sh`）：22 条 md 链接全可达、19 张 SVG 全存在、3 个锚点全有效、10 项关键数字与 overview/原文交叉核对一致 | 全部采纳并修订落盘后交付。⚠️ 记录一条**核查脚本误报**：锚点核查首次报「#四个静默失败全课程最贵的收获」失效，定位为脚本归一化未剥离全角括号（GitHub 锚点规则会剥离），已修正脚本逻辑复验通过，**手册内容未改动** —— 教训是「核查脚本本身也会错，报错后先判是内容错还是工具错，不要盲目改内容」。**手册自主新增三块内容**（原课无、手册首次跨课整合）：①「四个静默失败」汇总表（跨 Task 2 / Task 5 / 课 11 提炼：order_id 撞车丢 58%、分区 MV 三环节全说谎、「单行」DELETE 删 331 行、RESTORE 默认 3 副本），共同规律是「命令对了但假设错了，只可能被 SELECT 抓到」；②「决策清单」（该不该用五反模式 / 建模决策三问 / 关键配置决策表 / 存算一体 vs 分离对照）；③「考点速记」（12 课高频数字表 + 9 组易混点对照 + 常考「唯一·第一·默认」+ 常考「不能·不支持」） |
| 2026-09-03 | Phase 3 综合实战（Task 1-5） | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | P0×3 / P1×2。P0-1【learner·L2 可运行性】Task 4 脚本 3.2 用 `ADD COLUMN ... DEFAULT ''` 加列，导致 3.3 改宽必然失败（报 `Can not change default value`），而脚本还错误 echo「已改为 VARCHAR(128)」——读者照抄会以为改宽成功 → 改为正反对比演示（不带 DEFAULT 可改宽 / 带 DEFAULT 两条路全堵），删掉那行错误提示，并在 task-4 文档 4.2 节补完整复现与规避办法。P0-2【learner·L2 可运行性】Task 5 脚本 2.3 用 `docker exec doris-learn ... kafka-console-producer.sh` 投递，实测该文件不在 doris-learn 里（`No such file or directory`），实时性验收必然失败 → 改用独立容器 `doris-kafka`，broker 地址 `kafka:9092`，实测 19 秒可见。P0-3【pedagogy·数据真实性】Task 5 脚本 1.7 用 `WHERE order_date='2025-06-15'` 做「BE 真读到数据」的验证，实测该日期在 DWD 里无数据（返回 NULL），读者会误判成「表坏了」→ 改用 `>= '2025-06-01' AND < '2025-07-01'`，并在文档 2.5 节把「验证失败先确认数据存在」写成教学点。P1-1【pedagogy·章节引用】task-5 文档第 0 节写「见第 4.1 节」指向 order_id 误删事故，实际在 5.2 节 → 已修正。P1-2【pedagogy·口径说明】README 任务清单五条仍标「⬜ 未开始」，与已交付事实不符 → 全部改为 ✅ 并补实测摘要 | 全部采纳并修订落盘，P0 清零后交付。另记录四项自主发现（非评审项）：①🔥**「单行」DELETE 实际删了 331 行** —— `dwd_orders` 唯一键是 `(order_date, user_id, order_id)`，而 `order_id` 是 `ROW_NUMBER() OVER (PARTITION BY order_date ...)` 生成的**只在当天唯一**（实测 `distinct_oid=52784` vs `rows_total=9999563`，平均 1 个 order_id 对应 190 行）；`DELETE ... WHERE order_id=7088` 实测删 331 行且**不报任何错**；正确写法必须带全唯一键（实测只删 1 行）。这是本项目**第四个静默失败**，也是四个里最危险的（前三个是数据变少，这个是直接误删）。②🔥**分区 MV 刷不出数据** —— 6 组对照实验：分区 MV（IMMEDIATE / DEFERRED+COMPLETE / 单表 / 加 partition_sync_limit）全部 0 行，非分区 MV 全部 96 行；且建表、REFRESH、SHOW PARTITIONS（28 个分区全 NORMAL）三个环节**全部显示正常**，只有 SELECT 才发现是空的 → 本项目改用非分区 MV。③**`information_schema.partitions` 没有 `REPLICATION_NUM` 列**（报 `Unknown column 'REPLICATION_NUM' in 'table list'`），查副本数要用 `SHOW CREATE TABLE` 的 `replication_allocation`。④**`SHOW RESTORE` 不能用 awk 按列号取 Status** —— Info 列含 `\n` 转义的 JSON（分区名列表）会打乱整行 tab 结构，`awk '{print $14}'` 取到 NULL；改用 `grep -oE "replication num should be less than[^\"\\\\]*"`，JobId/Label/State/ReplicAlloc 在前 8 列、JSON 之前，用 awk 取是安全的 |
| 2026-09-03 | 阶段 4·课 12 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | P0×1 / P1×2。P0-1【pedagogy·交叉验证】骨架硬性要求「讲存算分离时须回头对照课 9，说明存算分离下数据可靠性由共享存储层承担，副本语义与扩缩容行为均不同」——初稿 2.6 节只写了「可靠性交给共享存储」，未点明归属、也未给出课 9 概念的具体映射 → 已补「课 9 概念 → 存算分离下变成什么」5 行对照表 + 运维关注点迁移说明（从 ReplicaMissingNum/UnrecoverableNum 变成缓存命中率与存储可用性）。P1-1【learner·L2 可运行性】正文 1.3 节 chinese parser 代码块中 INSERT 后直接查询，评审 1 复现到先返回 COUNT(*)=0 而非报错（统计信息未刷新），读者照抄会以为没复现 → 代码块加 sleep 提示，setup 脚本同步加注释。P1-2【learner·完整性】S3 TVF 的三个语法坑（缺 uri 报 props must contain uri / 缺 use_path_style 报 Property minio.endpoint is required / csv_schema 不支持 varchar(n) 报 unsupported column type）与 CREATE EXTERNAL TABLE ENGINE=S3 被拒（报 Do not support external table with engine name = olap，注意报错信息与输入不对应），此前只存在于探测脚本 → 已补 2.3 节专节并保留 4 条报错原文 | 全部采纳并修订落盘，P0 清零后交付。另记录三项自主发现（非评审项）：①**测量方法论教训，本课最重要**——第一版测点查用 200 次 docker exec，得出 130 ms/次，险些写进正文；做空连接对照（200 次只发 SELECT 1）后发现两者差值仅 -1.6~+1.5 秒，26 秒几乎全是连接开销；改单连接内串行发 200 条 SQL 后为 1.13-1.35 秒，单次 5.7-6.6 ms、约 148-176 QPS。规矩：**测任何延迟类指标，先做最小对照把基线开销减掉**。②**ROLLBACK 静默无效**——转账场景（A 扣 30、B 加 30）执行 ROLLBACK 后 SELECT 出来 amount=70.00 与 30.00，钱扣了账加了什么都没撤，且不报错；SHOW VARIABLES 仍显示 transaction_isolation=REPEATABLE-READ（协议兼容的显示，非真实支持）。这是本课程第四次遇到静默失败（前三次：课 6 的 docker exec 缺 -i、课 11 的 RESTORE 失败藏在 SHOW RESTORE、本课 1.3 的 COUNT(*)=0）。③**存算分离的本地性代价有明确边界**——不是所有查询都变慢：聚合 GROUP BY 本地 0.16-0.20s vs 共享存储 0.20-0.23s（1.2 倍，几乎无差），明细带谓词扫描 0.13-0.15s vs 0.40-0.42s（差 3 倍）。原因是聚合瓶颈在计算、列式 parquet 只需传少数列；明细过滤涉及的列都得读，且无本地 page cache 兜底 |
| 2026-09-03 | 阶段 4·课 11 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | P0×1 / P1×1。P0-1【learner·L2 可运行性】支持矩阵里把 `MODIFY COLUMN` 列为「可改」，但实测改带 DEFAULT 值的列会报 `Can not change default value`（显式写 `DEFAULT NULL` 同样被拒），读者照抄必失败 → 支持矩阵加 ⚠️ 限制说明 + 补绕过办法（加新列 → UPDATE 回填 → 删旧列 → 改名），被拒表与速览表各补一条。P1-1【learner·L2 可运行性】cleanup 脚本用 `DROP SNAPSHOT` 清理快照，实测 **4.1.3 根本没有这条语句**，四种写法全部报 `no viable alternative at input 'DROP SNAPSHOT'(line 1, pos 5)` → 改为「删仓库 + 物理清 S3 目录（`docker exec doris-minio rm -rf /data/doris-demo/backup11/`）」，并在正文新增 2.10 节把这条发现讲成教学点 | 全部采纳并修订落盘，P0 清零后交付。另记录四项自主发现（非评审项）：①**`CANCEL ALTER TABLE COLUMN` 只在作业进行中有效** —— 单机 100 万行加列 1-3 秒即完成，CANCEL 报 `Table[sc_cancel] is not under SCHEMA_CHANGE.`；且 4.1.3 不支持 `CANCEL ... WHERE JobId=xxx`（报 `mismatched input 'WHERE'`），只能用 `FROM db.tbl`。正文如实呈现这个失败面，并给出生产 `WAITING_TXN` 场景的可行复现办法（会话 A 开事务 `SELECT SLEEP(30)` 不提交，会话 B 发起 ALTER 会停在 WAITING_TXN）。②**表处于 SCHEMA_CHANGE 时再 ALTER 报 `state(SCHEMA_CHANGE) is not NORMAL. Do not allow doing ALTER ops`** —— 连续两次改表必须等第一个作业 FINISHED。③**`RESTORE` 的 `replication_num` 默认是 3、不沿用原表的 1 副本**，且**语句提交时不报错**，失败信息藏在 `SHOW RESTORE` 的 Status 列（`replication num should be less than the number of available backends. replication num is 3, available backend num is 2`）—— 本课最隐蔽的坑，必须显式写 `'replication_num'='1'`。④**`SHOW RESTORE` / `SHOW BACKUP` 取最新作业状态须 `tail -1`** —— 探测脚本最初读的是最老一条记录，导致连续多轮误判为 CANCELLED（实际早已 FINISHED），已封装 `laststate()` / `waitjob()` 函数 |
| 2026-09-03 | 阶段 4·课 10 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | P0×1 / P1×2。P0-1【learner·L2 可运行性】正文 1.4 节把 `memory_high_watermark` 列为"可用属性"，但单独设 `'memory_high_watermark'='70%'` 会报 `memory_high_watermark(70) should bigger than memory_low_watermark(75)` —— 低水位**默认是 75%**，只改高水位反而比默认值小 → 属性清单加⚠️标注 + 补"必须成对配置"的正确写法与报错原文。P1-1【pedagogy·数据真实性】正文主线三数字用的是探测期首次值（无隔离 1223–1719 / 有隔离 212–265），交付前脚本实跑得到第二组（无隔离 1122–1642 / 有隔离 217–270），两处引用不一致 → 全文统一为验收数据，并加"两次独立重跑对比"说明块，明确"看倍数关系不看绝对值"。P1-2【pedagogy·数据真实性】Spill 数据同样存在探测值与验收值差异（0.35-0.38 / 8.79-13.77 / 峰值 36MB / 慢 25-40 倍 vs 0.30-0.33 / 5.24-11.85 / 峰值 40MB / 慢 17-39 倍）→ 正文 5 处 + 第四幕预期输出 + 速览 + 第五幕 + 误区 2 + 接力提示词全部统一为验收值 | 全部采纳并修订落盘，P0 清零后交付。另记录三项自主发现（非评审项）：①**容器 cgroup 只读挂载**（`cgroup2 (ro,...)`）导致 `max_cpu_percent` 完全不生效 —— 实测 100% 组 1.42-1.60s、5% 组 1.03-1.18s，受限组反而更快，差异纯属噪声；已在正文用 🟡 标注而非编造数字。②**内存超限的真实机制是"暂停→尝试回收→不行才报错"**，不是"超了就杀" —— BE 日志证据 `workload_group_manager.cpp:301] Insert one new paused query ... TotalPausedPeriodSecs=18`。③**spill 不是万能的**：`orders` 自关联在 128MB 直接报错（0.22s）、256MB 挣扎 88.63 秒后仍报错、512MB 成功但要 480 秒、1024MB 成功只要 1.06 秒 —— 代价非线性 |
| 2026-09-03 | 阶段 4·课 9 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | P0×2 / P1×2 / P2×1。P0-1【learner·L2 可运行性】正文第四幕脚本清单写 `lesson09-step2.sh` / `lesson09-step3.sh`，但这两个文件不存在（内容已合并进 setup），读者按名找不到 → 清单改为实际存在的 5 个脚本（add-be2 / setup / step4 / step5 / cleanup）。P0-2【pedagogy·实验有效性】宕机演练"预期报错"落空：`repl3` 的 tablet 已被均衡搬离 BE2，kill BE2 后查询照常返回 → 新增 `pinned_be2` 表并动态判断 tablet 落点、据此决定宕哪个节点，保证必然命中。P1-1【pedagogy·数据真实性】`SHOW DATA` 统计延迟导致 cost1 显示 499228 行 / 1.260 MB（实际 100 万行 / 2.553 MB）→ 等待延长到 75 秒 + 加 `SELECT COUNT(*)` 交叉校验 + 正文如实记录该坑。P1-2【pedagogy·数值稳定性】正文 tablet 数字（3850→3848、15→13、611→603）是首次实验值，重跑会变（第二次 3868/26、641→617）→ 全部加注"首次实测值，判据是趋势不是绝对值"。P2-1【learner·L3】`history_tablets` 是累计值不归零（第二次跑已到 1000），易被误读为"本次搬了多少" → 正文加说明 | 全部采纳并修订落盘，P0 清零后交付。另记录两项自主发现（非评审项）：①**`COUNT(*)` 走元数据优化不扫 BE**，用它验证宕机会得出完全错误的结论，须改用 `LIMIT 3` / `SUM()`；②**`ADMIN MIGRATE TABLET` 在 4.1.3 报语法错误**，tablet 落点无法手动指定，可靠办法是"先建表→查落点→再决定宕谁" |
| 2026-09-02 | 阶段 3·课 8 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | P0×6 / P1×2 / P2×1。P0-1【pedagogy·数据一致性】第一幕"489 到 654 毫秒"与后文 387-475ms 矛盾 → 统一为"稳定在 400 毫秒上下（387/416/475ms）"并补两者非同一条 SQL 的口径说明。P0-2【learner·L2】正文知识点 1 用探测期表名 `dim_mid`（79/82/117/240/1433/1439/1700 行），脚本建的是 `non_colo_dim` → 全部替换。P0-3【learner·L2】知识点 1.2/1.7 用探测期表名 `dim_province`（178/188/379/395/408 行），脚本建的是 `dim_region` → 替换时**实测发现 `orders JOIN dim_region` 实际出 BUCKET_SHUFFLE 而非 BROADCAST**（orders 分桶键也是 province，"右表够小"与"命中分桶键"两条件同时成立），Broadcast 证据改用 `fact_1m JOIN dim_region`，该"反常"已写成正文教学点 + step2 新增 2.1b。P0-4【learner·L2】Colocate 依赖 `fact_prov` 但脚本未建 → 补进 setup 步骤 1.6。P0-5【learner·L2】动态 schema 依赖 `v_probe` 但脚本未建 → 补进 step4 新增 4.3b。P0-6【learner·L2】3.6 节用探测期临时表 `mv_probe` → 改为 `mv_prov_pay_daily`。P1-1【pedagogy·数据真实性】step5 参考值注释过时（26-27/23-37/156-171ms、3.962/3.990/8.344MB）→ 更新为实测 26-46/16-26/129-156ms、3.464/3.494/7.365MB 并加浮动说明。P1-2【learner·L3】课 9 导航链接层级错误（`../4-...` 解析到不存在的 `stages/3/4-...`）→ 改为 `../../4-分布式运维与生产落地/lessons/...`。P2-1【learner·L3】step7.7 注释"104 行变 106 行"实测为"86 变 88" → 修正并补"判断标准是 SUM(cnt) 跟上基表" | 全部采纳并修订落盘，P0 清零后交付 |
| 2026-09-02 | 多阶段大纲 | 主 agent 内联（pedagogy，子 agent 未创建，独立性受限） | P0×0 / P1×2 / P2×2：P1-1 同步 Rollup（课 5）与异步物化视图（课 8）易混；P1-2 阶段 4 缺 Schema Change；P2-1 课 1 鸿沟要量化不要纯概念；P2-2 课 9 需提示存算分离下副本语义不同 | 全部采纳：课 5 限定"同步 Rollup"+ 课 8 加交叉引用；Schema Change 并入课 11；两项 P2 写入对应课骨架备注 |
| 2026-09-02 | 阶段 3·课 7 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | P0×3 / P1×2 / P2×2。P0-1【learner·L2 可运行性】setup 脚本 docker exec 缺 -i，不转发 stdin，echo "$SQL" 管道给 docker exec mysql 静默无输出，建表/造数全部失败却不报错，后续 7 步全对不上 → 补 docker exec -i + 注释说明。P0-2【learner·L2 可运行性】SHOW DATA 紧跟 INSERT 返回 0（依赖后台统计，实测延迟约 45 秒），正文预期输出写 5.17MB，读者照抄必然对不上 → 脚本加 sleep 60 + 正文说明。P0-3【pedagogy·数据真实性】第四幕步骤 4 的 1/2/3 个 pad 列耗时与 OutputBlockBytes 为未经实测的推测值（940MB/1.88GB/2.82GB），实测为 3.92MB/7.84MB/8.00MB → 全部替换为实测数据，并补口径说明。P1-1【learner·L3 预期一致】步骤 3/4 只给绝对值未说明浮动范围，读者数字对不上会误判跑错 → 补浮动范围 + 「判断跑对看三件事」清单。P1-2【learner·L3】grep -A 12 取 OutputBlockBytes 得单 instance 值（8MB 上限）非全局（2.82GB），与步骤 2 口径冲突 → 补口径说明，声明本步骤不依赖该字段。P2-1【pedagogy·P3 图表一致】Profile 三层结构图未标注每层回答什么问题 → 已在图中补。P2-2【learner·L1】Block 切分规则未解释宽列为何块数更多（264→368）→ 补说明 | 全部采纳并修订落盘，P0 清零后交付 |
| 2026-09-02 | 阶段 3·课 6 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | P0×2 / P1×1 / P2×2。P0-1【learner·L2 可运行性】第四幕步骤 3/6/7 依赖表内特定基线行数（10950、10000），但正文未交代 TRUNCATE 前置，读者从步骤 2.6 直接照抄必然对不上 → 三处均补 TRUNCATE + 基线重建指令；P0-2【learner·L2 可运行性】步骤 1（Kafka）与步骤 5（MinIO）缺 `docker pull`，首次运行必失败 → 均补拉取指令 + 幂等提示。P1-1【learner·L5 速览】Group Commit 实测 100 次≈15 秒与第二幕 1000 次≈34 秒口径不同，易误读为线性关系 → 补口径说明块。P2-1【L1 零基础可懂】`retention` 术语首次出现未解释 → 补 Kafka 消息保留期说明；P2-2【P3 一图总结一致】“快 200 倍”表述歧义（数据量还多 10 倍）→ 改为“数据量多 10 倍，耗时仅 1/200” | 全部采纳并修订落盘，P0 清零后交付 |
| 2026-09-02 | 阶段 1·课 2 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | P0×2 / P1×2。P0-1【learner·L2 可运行性】第四幕"完整流程"步骤 ③④ 依赖 CSV 文件但正文未交代来源，且课 1 容器可能已删除，读者照抄必卡住 → 补「📦 关于测试数据」引导块（两个选择：复现课 1 / 直接 INSERT 少量数据）+ 补 `lesson02-load-data.sh` 链接 + 补注 docker cp 在 Windows 下的路径坑。P0-2【learner·L1 零基础可懂】Stream Load 的 7 个参数全部未解释，读者照抄不理解 → 补参数详解表格（含 `--location-trusted`、`max_filter_ratio` 双刃剑警告）。P1-1【pedagogy·P4 类比准确性】餐厅类比中"实习生"易被误解为"迟早能转正接班"，与 Observer 永不参与选主冲突 → 类比后补⚠️说破。P1-2【pedagogy·P1 情节一致性】知识点 3 误区 2 提前剧透第四幕分桶倾斜结论 → 改为悬念式设问（"这是个错误示范，第四幕摆数据给你看"） | 全部采纳并修订落盘，P0 清零后交付 |
| 2026-09-02 | 阶段 1·课 1 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | P0×3 / P1×3 / P2×2。P0-1【pedagogy】知识点 2 建表示例含空的 `PARTITION BY RANGE(order_date) ()`，不可运行（零基础读者照抄必失败）→ 删除分区声明，改为最小可运行语句并标注"本课不要求跑通"；P0-2【pedagogy】京东/小米/海底捞三个官方案例数字未标明采集年份，与"核查于 2026-09"并列易误导为当前实时水位 → 三处均补"文档未标注采集年份，理解为该场景曾达到的量级"；P0-3【learner】第四幕复现步骤缺 `docker cp` 一步，读者直接执行必失败 → 补步骤 2。P1-1【learner】165/20→8 倍只有结论无推导，零基础看不懂 → 增加「🧮 算给你看」段落 + 提示"记住算法别记数字"；P1-2【learner】知识点 2 的 Mermaid 图中 BE1/BE2/BE3 无"Shuffle/交换中间结果"的代价说明 → 已在类比边界与误区 5 中说明（保留）；P1-3【pedagogy】小米案例"平均 10 秒"与全文"亚秒级"定位表面冲突 → 补注：即席查询吞吐要求与报表场景指标不同。P2-1【learner】可补充一句"本课不要求启动 Doris"降低焦虑 → 已在第四幕开头写明；P2-2【pedagogy】章节"官方文档"链接可增补英文官网 → 已增补 | 全部采纳并修订落盘，P0 清零后交付 |

## 大纲调整记录

| 日期 | 调整内容 | 原因 |
|------|----------|------|
| 2026-09-02 | 建档：4 阶段 / 12 课 / 36 知识点 | 首次规划 |
| 2026-09-02 | 课 11 由「监控、备份与升级」改为「日常运维：Schema Change、备份与升级」 | 大纲评审 P1-2：Schema Change 是生产高频操作，原大纲缺失 |

## 断点信息

- **当前位置**：**Phase 5 收尾三份产物已完成（2026-09-03）—— 课程制全流程收官** —— 12 课 36 知识点 + Phase 3 五个 Task + Phase 4 索引型手册（`final-课程手册.md`，720 行）+ Phase 5 三份收尾产物（`08-实战经验.md` 355 行 / `09-排障速查手册.md` 294 行 / `10-场景解法库.md` 441 行）全部交付。三份一次生成、同源压缩，双视角评审 P0=0，五项自动化核查全通过。
- **Phase 5 交付要点（供后续复用）**：① **同源压缩是硬约束** —— 手册的 10 条 QRH 条目是从经验层 10 条故障模式压缩而来（压缩不重写），每条回指「故障模式 N」编号（**只写编号不写锚点**，因症状名运行时才确定、锚点会失效）；② **三份分工不能混** —— 手册只给动作、经验层讲原理、解法库给权衡，混了会同时毁掉三个；③ **证据纪律** —— 🟢本机实测 109 / 🔵联网核查 10 / ⚪公认实践 14，⏳低置信 3 处均**未入排障手册**（只留经验层标注），符合「手册只收确定项」；④ **宁缺毋滥已落实** —— 解法库从 12 候选筛 7 场景并记录 5 条淘汰理由；⑤ **工具自身的坑（本轮第二次）** —— 锚点校验脚本用 sed 的 `\x{4e00}-\x{9fff}` 范围语法报错，改用 python heredoc 通过。**连续两轮（Phase 4 全角括号、Phase 5 sed 范围）遇到「核查工具报错 ≠ 内容有错」**，已成形为固定教训：核查脚本报错时先判是内容错还是工具错，别盲目改内容手册覆盖 12 课逐课索引 + 实战项目收录 + 四个静默失败汇总 + 决策清单 + 考点速查；链接/图片/锚点/关键数字四项自动化核查全通过，双视角评审 P0=0。
- **Phase 4 手册交付要点（供 Phase 5 参考）**：① 方案选择 —— 12 课正文共 16223 行，用户选择**索引型手册**而非全量拼接（理由：手册价值是回望全局时快速定位，不是再存一份正文，全量拼接会导致重复维护且没人会从头翻）；② 手册三块自主新增内容（原课无、首次跨课整合）—— 「四个静默失败」汇总表 / 「决策清单」/ 「考点速查」，均为 skill 对「应试认证 + 决策参考」双目标的附加要求；③ **核查脚本误报教训** —— `assets/tmp-verify-manual.sh` 的锚点校验首次报「#四个静默失败全课程最贵的收获」失效，实为脚本归一化未剥离全角括号，修正脚本后复验通过、手册未改动；④ 手册已标注 `09-排障速查手册.md` 为「待生成」，Phase 5 产出后需回来去掉该标注
- **下一批**：**Phase 6 知识点对齐 · 选择题（可选，仅用户要求时触发）** —— 用户说「考我一下」「出几道题」「测测我」时进入，可独立触发。机制与 module-teach Phase 8 完全一致（历史读取、错题巩固 ≥1/2、指纹去重、交互评分、`round-NN` 落盘、INDEX 追加、进步对比）。题目素材已备：`final-课程手册.md` 的「考点速记」段（12 课高频数字 + 9 组易混点对照 + 常考「唯一·第一·默认」/「不能·不支持」），外加 `10-场景解法库.md` 的 7 道设计题可改造成选择题。
- **再之后（可选补测，均因环境所限暂缺）**：①换物理机或 cgroup 可写容器补测 Workload Group 的 CPU 配额；②补测分区裁剪的**性能**收益（需数据量上亿，本机 2050 万行被 0.13 秒固定开销吃掉）；③多真机环境补做「3 副本 + 宕 1 台 → 查询不断」（2 BE 同 host 触发反亲和）；④存算分离需另部署 cloud mode；⑤ClickHouse / ES / Hive 横向对比需另起容器`09-排障速查手册.md`（使用态：按症状倒查的 QRH 式，五硬特征 = 索引表 / 症状可观测 / 先止血后定位 / 分支判定 / 有升级出口）、`10-场景解法库.md`（设计态：开放设计题，先想后看 `<details>` 折叠解法）。**为什么必须一次生成**：三份的素材源是同一批「领域典型失败模式 + 常见高难度场景」，分开生成 = 同一个坑写两遍 = 必然双源漂移。素材已备：全课程 25 条本机复现故障（含报错原文 + 实测数字），四个静默失败为主轴，可筛 8 条进排障手册、6-8 个设计题进解法库。生成后须回写四处档案并去掉手册里「待生成」标注。
- **再之后（可选补测，均因环境所限暂缺）**：①换物理机或 cgroup 可写容器补测 Workload Group 的 CPU 配额；②补测分区裁剪的性能收益（需数据量上亿）；③多真机环境补做「3 副本 + 宕 1 台 → 查询不断」；④存算分离需另部署 cloud mode
- **续学入口**：先读 `00-评审清单.md` 确认无漏评审条目 → 再按进度表找第一个非「已完成」的知识点（当前已全部完成）
- **环境状态**：Doris 集群在本机运行中（容器 `doris-learn`，端口 9030/8030/8040，healthy）。课 6 新增两个容器：**`doris-kafka`**（`apache/kafka:3.9.1`，桥接网络 `doris-net`，主机名 `kafka`，topic `doris_orders` 3 分区）、**`doris-minio`**（`minio/minio:RELEASE.2023-03-20T20-16-18Z`，同网络，主机名 `minio`，bucket `doris-demo`）。三者同在 `doris-net` 网络，互访须用**容器主机名**而非 localhost。`shop` 库现有表：`orders`（2150 万行，按 province 分 8 桶，倾斜比 3.00）、`orders_dup`、`orders_agg`、`orders_uniq_mow` / `orders_uniq_mor`、`t_nopart_prov`（province 分桶对照）、`t_nopart_user`（user_id 分桶，倾斜比 1.006）、`t_part_month`（按月 45 分区）、`t_part_day`（按天 365 分区，2920 tablet）、`k_date_first` / `k_prov_first`（Key 顺序对照）、`rollup_demo`（含 rollup_pc）、`empty_t`（空表，固定开销基线）。课 6 新增：`load_demo`、`s3_orders_ext`、`kafka_orders`、`uniq_load_demo`（均在 `shop` 库）。课 7 新增：`perf_wide`（200 万行，8 字段含 3 个 500B 填充列，磁盘仅 5.17 MB）、`perf_wide_big`（400 万行，scale 对照用）。课 8 新增（跑 `lesson08-cleanup.sh` 可一键清理）：`dim_region`（8 行，省份→大区映射）、`non_colo_dim`（10 万行，user_id 分桶，非 colocate 对照）、`fact_1m`（100 万行，province 分 8 桶，`prov_group` 成员）、`fact_prov`（200 万行，`prov_group` 成员，Colocate 右表）、`v_probe`（VARIANT 动态 schema 演示）、`log_typed` / `log_variant` / `log_json`（三种存法对照）、`mv_prov_pay_daily` / `mv_part_daily` / `mv_sched`（异步物化视图）。**已清理**：`t_part_day`、`idx_demo`、`rollup_v2`、`gc_on` / `gc_off` / `gc_demo` / `vt` / `version_demo`。**课 9 重大环境变化**：集群从 `1 FE + 1 BE` 变成 **`1 FE + 2 BE`** —— 在同一个容器内用不同端口拉起了第二个 BE 进程（`/opt/be2`，heartbeat 19050），使扩缩容/数据均衡/宕机自愈第一次成为可真跑的实验。启动：`docker exec -d doris-learn bash /opt/be2/launch.sh`；移除：`bash lesson09-cleanup.sh --remove-be2`。⚠️ 两台 BE 的 host 都是 127.0.0.1（伪多节点），反亲和规则导致无法真正验证"多副本扛宕机"。⚠️ `disable_balance` 出厂默认 `true`，课 9 已改为 `false`（集群现在会自动均衡），课 10 若要做确定性实验可能需先关掉。课 9 新增表（跑 `lesson09-cleanup.sh` 可清理）：`cost1`、`repl3`、`ha_demo`、`ha_demo2`、`pinned_be2`。**课 8 探测期残留**（`dim_mid`、`dim_province` 系列、`mid_orders`、`mid_small`、`t_bucket_8` 等）已全部并入 `lesson08-cleanup.sh` 的 DROP 清单，跑一次即可清空
- **Phase 3 环境状态与遗留待办（2026-09-03，供后续复现参考）**：① **新建 `dw` 库五张表 + 一张对照表** —— `ods_orders`（Duplicate，11002637 行）、`dwd_orders`（Unique MoW，9999563 行 / 25097795339.16）、`dws_prov_month`（Aggregate，96 行 / 25097795339.16）、`ads_prov_month_top`（Duplicate，96 行）、`dim_province`（8 行）、`rollup_test`（Duplicate，Rollup `r_prov` 演示用）；MV `mv_region_month`（**非分区版**，96 行）；实时表 `ods_orders_rt`（Routine Load `rl_orders_rt` 持续 RUNNING，Lag 全 0）。全部 28 个分区、1 副本。② **S3 仓库 `p3_repo` + 快照 `p3_dws`（2026-09-03-10-40-24）保留**；Workload Group 保留 `wg_report`(40%/20/50) 与 `wg_etl`(60%/3/10)。③ ⚠️ **`dwd_orders` 比理论值少约 331 行** —— Task 5 脚本 5.2 节为演示「order_id 非全局唯一」真实执行了一次 `DELETE ... WHERE order_id=7088`，实际删 331 行；这是**预期内的演示副作用**，恢复完整数据重跑 `bash assets/phase3-task2-load.sh`（只重灌 DWD，DWS/MV/ADS 需按 task-3 脚本重建）。④ **⚠️ 重建 DWS 的三个列顺序坑** —— `dws_prov_month` 的列是 `(stat_month, province, order_cnt, uv, total_amount, max_amount)`，INSERT 的 SELECT 必须**按此顺序**（`uv` 是 bitmap 列，必须用 `bitmap_union(TO_BITMAP(user_id))`），漏掉 `max_amount` 或顺序写错都报 `insert into cols should be corresponding to the query output`（报错信息不告诉你哪一列错了，排查靠逐列比对）。⑤ **Phase 3 交付物清单** —— 文档：`projects/1-电商实时数仓/`（README + 5 个 Task 文档，共约 3516 行）；脚本：`assets/phase3-task1-setup.sh` ~ `phase3-task5-accept.sh`（共约 1118 行）。执行顺序 Task 1 → 5，前一任务的输出是后一任务的输入。⑥ **Phase 3 四个静默失败（本项目最贵的收获）** —— order_id 撞车丢 58% 数据（Task 2）、维表凭空编造丢 4 个省（Task 1/3）、RESTORE 默认 3 副本恢复表为空（Task 4）、「单行」DELETE 实际删 331 行（Task 5）。全部实测复现 + 修复 + 写入文档。⑦ **Phase 3 未实测项（如实标注，勿编数字）** —— 分区裁剪的**性能收益**（0.12s 固定开销淹没，只验证了 EXPLAIN 的 `partitions=1/28`）、Workload Group **CPU 配额**（cgroup 只读）、**多副本抗宕机**（2 BE 同 host 触发反亲和）、**存算分离**（cloud mode only）
- **课 1 的 MySQL 容器已释放**：`doris-mysql-demo` 已 **stop**（数据已落盘到课 1 讲义），腾出约 7.7 GB 内存；需要复现课 1 实验时 `docker start doris-mysql-demo` 即可恢复
- **课 12 遗留待办（全课程收束，供 Phase 3 综合实战参考）**：① **全课程 36/36 知识点已完成**（2026-09-03），阶段 4 进度 4/4 课；课 12 是最后一课，产出含全课程回扣与三句话方法论。② **本机是存算一体架构，存算分离功能全部不可用** —— `SHOW COMPUTE GROUPS` 报 `Command only support in cloud mode.`、`SHOW STORAGE VAULT` 报 `Storage Vault is only supported for cloud mode`、`SHOW CACHE HOTSPOTS` 报 `no viable alternative at input 'SHOW CACHE'(line 1, pos 5)`；`SHOW BACKENDS` 的 `RemoteUsedCapacity = 0.000` 是存算一体的铁证。要练存算分离须另部署 cloud mode。③ **S3 TVF 的四个语法坑（4.1.3 实测）** —— 必须用 `'uri'` 属性（写 `s3.endpoint`+`s3.bucket` 报 `Can not build s3(): props must contain uri`）；MinIO 必须加 `'use_path_style'='true'`（不加报 `Property minio.endpoint is required.`，报错信息与真实原因不对应）；`csv_schema` 不支持 `varchar(n)` 也不支持裸 `varchar`（报 `unsupported column type`）；`CREATE EXTERNAL TABLE ... ENGINE=S3` 被拒（报 `Do not support external table with engine name = olap`，同样文不对题）。所以本课用 **parquet 格式**（自带 schema）而非 csv。④ **中文分词器不可用** —— BE 上 `/opt/be2/dict/` 目录压根不存在，chinese parser 查询报 `chinese tokenizer dict file not found: /opt/be2/dict/jieba.dict.utf8`；且**表里有数据才报错**，空表查询返回 0。课 12 因此改用 `parser='english'`。生产部署要自己装 jieba 字典。⑤ **`MATCH_PHRASE` 要求 term 严格相邻** —— `'user 1355961 bought ...'` 查 `MATCH_PHRASE 'user bought'` 返回空（中间隔了 user_id），单 term `'bought'` 能命中；`MATCH_ALL` 只要求都出现。⑥ **Doris 的三个「不该用」场景及替代方案** —— 高频单行点查（5.7-6.6 ms/次、约 148-176 QPS 单连接串行 → Redis/HBase，Redis 约 0.1 ms 差 50 倍）；多语句事务（**ROLLBACK 静默无效且不报错** → MySQL/PG）；高频单行删改（标记删除堆积 + compaction 跟不上 → 批量/UPSERT/换分区）。⑦ **Doris 的 BEGIN/COMMIT/ROLLBACK 是 MySQL 协议兼容**，不提供跨语句原子性；每条 DML 自己原子。`SHOW VARIABLES` 仍显示 `transaction_isolation=REPEATABLE-READ`，是显示层兼容不是真支持。⑧ **课 12 环境收尾状态**：`enable_sql_cache=false`、`enable_spill=false`、`enable_profile=true`（沿用课 7/课 9 设定，未恢复）；实验表 anti_kv / anti_txn / anti_txn2 / log_search / log_cn / local_1m / local_10k 与 MinIO 上的 `l12/` 目录已由 `lesson12-cleanup.sh` 清理；集群健康（shop 库 647/647 tablet 全部健康、0 缺副本）；容器 doris-learn（1 FE + 2 BE，4.1.3-rc02-7126cf65d96）、doris-minio（bucket doris-demo）、doris-kafka（topic doris_orders）均保留供 Phase 3 使用
- **课 11 遗留待办**：① **`light_schema_change=true` 是 Doris 4.x 出厂默认值** —— 加列走「只改 FE 元数据」路径，ALTER 返回即生效（毫秒级），不会有"ALTER 完了查不到新列"的等待期。要复现传统 heavy schema change，必须建表时显式写 `"light_schema_change"="false"`。② **`CREATE REPOSITORY` 不支持 `IF NOT EXISTS`**、**`DROP REPOSITORY` 不支持 `IF EXISTS`** —— 都报 `mismatched input 'IF' expecting {...}`（超长关键字表）。脚本里直接 `DROP REPOSITORY <name>`，不存在时忽略即可；重复建同仓库会报 `Repository [xxx] already exists`。③ **MinIO 必须加 `'use_path_style'='true'`** —— 不加走虚拟主机风格会连不上。④ **`RESTORE` 的 `replication_num` 默认是 3，不沿用原表** —— 这是本课最隐蔽的坑：语句提交时不报错，失败信息藏在 `SHOW RESTORE` 的 Status 列（`replication num should be less than the number of available backends. replication num is 3, available backend num is 2`）。本机 2 台 BE，恢复 1 副本表必须显式写 `'replication_num'='1'`。⑤ **一个库同一时刻只能跑一个 backup/restore 作业** —— 连发两条第二条会报 `Currently, this DB is under backup or restore.`，串行排队要自己控制。⑥ **4.1.3 没有 `DROP SNAPSHOT` 语句** —— 清理快照只能「删仓库 + 物理清 S3 目录」（`docker exec doris-minio rm -rf /data/doris-demo/backup11/`）。⑦ **`RESTORE` 必须带 `backup_timestamp`**，漏了报 `Missing backup_timestamp property`；值从 `SHOW SNAPSHOT ON <repo>` 取。⑧ **`CANCEL ALTER TABLE COLUMN ... WHERE JobId=xxx` 在 4.1.3 不支持**（报 `mismatched input 'WHERE'`），只能用 `FROM db.tbl`；且 CANCEL 只在作业进行中有效，作业已 FINISHED 会报 `Table[xxx] is not under SCHEMA_CHANGE.`。单机 100 万行加列 1-3 秒完成，抓不到 CANCEL 窗口 —— 想复现必须先造长事务把作业卡在 `WAITING_TXN`。⑨ **表处于 SCHEMA_CHANGE 时再 ALTER 报 `state(SCHEMA_CHANGE) is not NORMAL. Do not allow doing ALTER ops`**。⑩ **`MODIFY COLUMN` 不能改带 DEFAULT 值的列**（报 `Can not change default value`，写 `DEFAULT NULL` 也不行），绕过办法是"加新列 → UPDATE 回填 → 删旧列 → 改名"。⑪ **`ORDER BY (dt, id)` 只写部分列会报 `Reorder stmt should contains all columns`** —— 重排 Key 列必须写全所有列。⑫ **`SHOW RESTORE` / `SHOW BACKUP` 取最新作业状态要 `tail -1`** —— 默认输出里第一条是最老的记录。⑬ **课 11 环境收尾状态**：`enable_sql_cache=false`、`enable_spill=false`、`enable_profile=true`（沿用课 7/课 9 设定，未恢复）；Workload Group 只剩 `normal`；S3 仓库 `s3_repo` 与实验表 `sc_light` / `sc_heavy` / `bk_orders` / `bk_part` 已由 `lesson11-cleanup.sh` 清理；容器 `doris-minio`（课 6 建）保留，课 12 若演示存算分离可继续复用
- **课 10 遗留待办**：① **容器 cgroup 是只读挂载**（`cgroup on /sys/fs/cgroup type cgroup2 (ro,...)`），导致 Workload Group 的 **CPU 配额（`max_cpu_percent`）完全不生效** —— 实测 100% 组 1.42/1.60/1.46 秒、5% 组 1.03/1.00/1.18 秒，受限组反而更快，差异纯属噪声。同理 `scan_thread_num` 也测不出稳定差异（该查询瓶颈在聚合/Join 不在扫描）。**这个边界已在正文用 🟡 明确标注，没有编造数字**。若后续换成物理机或 cgroup 可写的容器，应回来补测。② **`enable_spill` 出厂默认是 `false`** —— 课 10 实验中临时打开过但**未持久化**，cleanup 脚本已恢复为 false。生产环境建议显式打开（这是本课最容易被忽略的一条）。③ **Workload Group 属性名在 4.1.3 有 4 个已废弃**：`memory_limit`、`cpu_share`、`cpu_hard_limit`、`enable_memory_overcommit` 会直接报 `Property xxx is not supported, maybe it is deprecated`；`tag` 也废弃（改用 compute group）。可用的是百分比系列（`min/max_cpu_percent`、`min/max_memory_percent` 等）+ `max_concurrency`/`max_queue_size`/`queue_timeout`/`scan_thread_num`/`read_bytes_per_second`。④ **`memory_high_watermark` 必须比 `memory_low_watermark` 大，且低水位默认是 75%** —— 只设高水位 70% 会报 `should bigger than memory_low_watermark(75)`。配水位务必成对配置。⑤ **一个 Compute Group 下最多 15 个 Workload Group**，超了报 `Workload group number in Compute Group default can not exceed 15`。课 10 探测期反复建组曾触碰这个上限，导致后续建组全部失败 —— 排查时注意先 `SHOW WORKLOAD GROUPS` 数字数。⑥ **`SET workload_group` 后 `SELECT @@workload_group` 返回空字符串**（实测），不代表没生效。验证要去看 `SHOW WORKLOAD GROUPS` 的 `running_query_num` 列。⑦ **spill 不是万能的**：`orders` 自关联在 128MB 直接报错（0.22 秒）、256MB **挣扎 88.63 秒后仍报错**、512MB 成功但要 **480 秒**、1024MB 成功只要 **1.06 秒**。代价高度非线性，内存差太远时 spill 也救不了。⑧ **内存超限的真实机制是"暂停→尝试回收→不行才报错"**，不是"超了就杀" —— BE 日志 `workload_group_manager.cpp:301] Insert one new paused query`，`TotalPausedPeriodSecs=18` 说明该查询累计被暂停 18 秒。⑨ `docker exec` 必须带 `-i`（课 7 就踩过，课 10 在 setup 脚本里又强调了一次）。
- **课 9 遗留待办**：① **`disable_balance` 已从默认 `true` 改为 `false`**，集群现在会自动均衡。课 10 若要做确定性实验（比如固定 tablet 分布测资源组），需先改回 `true`。② **`COUNT(*)` 不能用来验证"数据是否可查"** —— 它走元数据行数优化、不扫 BE，节点宕机了照样返回结果。验证必须用 `SELECT ... LIMIT 3` / `SUM()` / 带谓词聚合。这个坑在课 10 测资源组时同样会踩（会误判"资源组没生效"）。③ **tablet 落在哪个 BE 无法手动指定**：`ADMIN MIGRATE TABLET` 在 4.1.3 报语法错误（`no viable alternative at input 'ADMIN MIGRATE'`）；反复建表 6 次也全部落 BE1。可靠办法：先建表 → `SHOW TABLETS` 查落点 → 再决定操作哪个节点。④ **多副本扛宕机仍未实测**（同主机反亲和所限）：报错原文 `Failed to find enough backend ... or maybe all be on same host`。若后续课程拿到多台真机，应回来补做"3 副本 + 宕 1 台 → 查询不断"的验证。⑤ **`DROP BACKEND` 被拒绝、需故意拼错成 `DROPP` 才能执行** —— 这是 Doris 的防呆设计，清理环境时如需强制移除节点才知道这个后门。⑥ 课 8 遗留的 `enable_sql_cache = false` **仍然是关的**（课 9 未恢复），课 10 测性能可继续沿用，不测请恢复 `SET GLOBAL enable_sql_cache = true`。
- **课 8 遗留待办**：① **全局 `enable_sql_cache` 仍是 `false`**（课 7 关的，课 8 测 Join / VARIANT / MV 性能继续沿用）。课 9 若不测性能，务必 `SET GLOBAL enable_sql_cache = true` 恢复生产默认；若还要测，请明确知道"关缓存"是有意选择而非环境异常。② **课 5 遗留的异步 MV 语法问题已澄清**：`CREATE MATERIALIZED VIEW ... BUILD IMMEDIATE REFRESH AUTO ON MANUAL ... AS SELECT` 在本机 Doris 4.1.3 上一次建成功，课 5 报的 `Duplicate column name` 是**同步 Rollup**（`ADD ROLLUP`）语法路径的问题，与异步 MV 无关——两者不可混用删除语句。③ **`SHOW MATERIALIZED VIEWS` / `SHOW MVS` / `SHOW CREATE MV` 在本机全部报 `no viable alternative at input 'SHOW MATERIALIZED'`**，五种写法都试过；查 MV 只能用 `SHOW CREATE MATERIALIZED VIEW <name>`，或 `SHOW TABLES`（MV 会出现在列表里）。④ **`SET` 会话变量跨连接失效**：`runq "SET x=1;"` 与 `runq "EXPLAIN ..."` 是两个独立连接，SET 会丢。必须写成 `runq "SET x=1; EXPLAIN ...;"` 塞进同一连接——课 8 探测期因此卡了两轮，后续课继续沿用这个写法。⑤ **透明改写不稳定是正常现象**：同一条聚合 SQL 交替执行时，有时 `MaterializedViewRewriteSuccessAndChose`（18ms）有时不改写（475ms），取决于优化器基于代价的选择，不是配置错误。判断改写是否生效要看 EXPLAIN 的 MATERIALIZATIONS 段，不能只看耗时。⑥ **MV 分区名 ≠ 基表分区名**：基表 `p202501` 在 MV 里叫 `p_20250101_20250201`（系统生成），按分区刷新前必须 `SHOW PARTITIONS FROM <mv>` 查真实名字，直接用基表分区名会报 `partition not exist`。⑦ **`REFRESH` 是异步提交**：返回成功不代表刷完，需等几秒再查。⑧ **VARIANT 子列不能直接 GROUP BY / ORDER BY**，必须先 `CAST(payload['x'] AS VARCHAR(32))`；WHERE 里不需要 CAST。报 `hll, bitmap, array, map, struct, jsonb, variant column must use with specific function` 就是这个原因。⑨ **数组下标从 1 开始**：`tags[0]` 静默返回 NULL（不报错），`tags[1]` 才是第一个元素。⑩ 课 7 遗留的 `VisibleVersionCount` 恒返回 -1、`information_schema.tablets` 不可用，课 9 讲 Compaction 时若有新版指标可补测。
- **课 7 遗留待办**：① 全局设置当前为 `enable_profile=true`、`enable_sql_cache=false`（课 7 实验留下的），课 8 测完记得恢复 `SET GLOBAL enable_sql_cache = true`；② **向量化开关 `enable_vectorized_engine` 在本机测不出差异**（关：20/18/15ms，开：18/17/17ms；扫宽列 191ms vs 189/214ms）——Doris 4.x 已全面向量化，该开关是历史遗留，两种设置下算子名都是 `OLAP_SCAN_OPERATOR`。结论已诚实写入讲义，用 `BlocksProduced`/`batch_size` 作机制证据，未编造性能数字；③ 课 7 实测确认 **Profile 抓取三板斧**：`SET GLOBAL enable_profile=true` → `SHOW QUERY PROFILE '/'` 按 SQL 文本 grep 取 QID（避开 mysql 探针 `select @@version_comment limit 1`）→ `curl -s -u root: "http://127.0.0.1:8030/api/profile?query_id=$QID"` 抓正文（`SHOW QUERY PROFILE '/<QID>'` 只列目录，试了 5 种写法均无效）；④ **`docker exec` 管道传 SQL 必须带 `-i`**——不带时静默无输出、失败不报错，课 7 setup 脚本踩到，已修；⑤ **`SHOW DATA` 紧跟 INSERT 返回 0**（后台统计延迟约 45 秒），需 sleep 后再查
- **课 6 遗留待办**：① Routine Load 作业 `kafka_rl_orders` 仍在 RUNNING，课 7 开始前可 `STOP ROUTINE LOAD`；② ✅ 课 6 实验残留表 `gc_on` / `gc_off` / `gc_demo` / `vt` / `version_demo` 已全部 DROP，Routine Load 作业 `kafka_rl_orders` 已 STOP（容器 `doris-kafka` / `doris-minio` 保留，课 8 可能复用）；③ **课 6 未能验证 tablet 版本堆积**：本机 `VisibleVersionCount` 恒返回 -1、`information_schema.tablets` 不可用，版本数改用「每次写入一个事务」的原理讲解，未编造数字——课 9 讲 Compaction 时若有新版指标可补测；④ **Group Commit 实测无效**（100 次单行 INSERT：窗口 100ms 15.17 秒 vs 10000ms 15.62 秒），结论已写入讲义；若后续发现是本机配置问题需回来修正
- **课 5 遗留待办（已清理 2026-09-02）**：`t_part_day`、`idx_demo`、`rollup_v2` 三张表均已 DROP。剩余待办：① `t_part_day`（2920 tablet）是本机最大的元数据负担，课 6 结束后建议 DROP；② `idx_demo` 占 578MB（两个索引），课 6 若需空间可 DROP 或删索引；③ `rollup_v2` 是建 Rollup 失败的残留表，可直接 DROP；④ **`CREATE MATERIALIZED VIEW ... AS SELECT` 建聚合 Rollup 在本机 Doris 4.1.3 上一直报 `Duplicate column name`，未建成——课 8 讲异步物化视图时需重新验证语法**（课 5 讲义已诚实标注）
- **评审发现的模式（务必警惕）**：连续三课（课 3、课 4、课 5）都抓到 P0，其中**"建表语句/命令写成省略形式或与建法不配对"复发两次**（课 3 列省略、课 5 用 `DROP MATERIALIZED VIEW` 删 `ADD ROLLUP` 建的索引）。课 6 起，第四幕的每条命令都要自问：**读者照抄能跑通吗？**
- **待办提醒**：① `course-reviewer` 子 agent 尚未创建，当前一律走主 agent 内联评审（独立性受限）；② ✅ `00-评审清单.md` 中阶段 1·课 2 条目已于 2026-09-02 补勾（此前评审结论已记录但条目漏勾）
- **待办提醒**：`course-reviewer` 子 agent 尚未创建，当前一律走主 agent 内联评审（独立性受限）；如需真正独立的双视角评审，可按 `create-sub-agent` 规范创建后切回委派模式
