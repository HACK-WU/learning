# Apache Doris 评审清单

> 强制检查点：每批内容生成后，先在本文件找到对应未勾选条目，完成双 agent 评审且 P0 清零后，才能勾选并交付。**未勾选 = 评审未完成，禁止进入下一批。**

## 待评审清单

- [x] 大纲评审（Phase 1）— pedagogy 视角，P0=0 通过（2026-09-02）
- [x] 阶段 1·课 1《数据分析的困境与 Doris 的诞生》 — pedagogy + learner 双视角，P0=0 通过（2026-09-02）
- [x] 阶段 1·课 2《跑起来第一个 Doris》 — pedagogy + learner 双视角，P0=0 通过（2026-09-02，补勾：评审结论此前已记录但条目漏勾）
- [x] 阶段 2·课 3《三种数据模型》 — pedagogy + learner 双视角，P0=0 通过（2026-09-02）
- [x] 阶段 2·课 4《分区与分桶》 — pedagogy + learner 双视角，P0=0 通过（2026-09-02）
- [x] 阶段 2·课 5《键列、索引与同步物化视图》 — pedagogy + learner 双视角，P0=0 通过（2026-09-02）
- [x] 阶段 3·课 6《数据导入全家桶》 — pedagogy + learner 双视角，P0=0 通过（2026-09-02）
- [x] 阶段 3·课 7《查询引擎与执行计划》 — pedagogy + learner 双视角，P0=0 通过（2026-09-02）
- [x] 阶段 3·课 8《多表关联与高级 SQL》 — pedagogy + learner 双视角，P0=0 通过（2026-09-02）
- [x] 阶段 4·课 9《副本、高可用与扩缩容》 — pedagogy + learner 双视角，P0=0 通过（2026-09-03）
- [x] 阶段 4·课 10《资源隔离与负载管理》 — pedagogy + learner 双视角，P0=0 通过（2026-09-03）
- [x] 阶段 4·课 11《日常运维：Schema Change、备份与升级》 — pedagogy + learner 双视角，P0=0 通过（2026-09-03）
- [x] 阶段 4·课 12《选型、存算分离与场景落地》 — pedagogy + learner 双视角，P0=0 通过（2026-09-03，补勾：评审结论此前已记录但条目漏勾）
- [x] **结课实战项目**（Phase 3）— pedagogy + learner 双视角 + **复杂度四门槛核查**，P0=0 通过（2026-09-03：Task 1-5 全部交付，README 五条验收标准逐条实测核对）
- [x] **课程手册汇总**（Phase 4）— pedagogy + learner 双视角 + **汇总完整性核查**，P0=0 通过（2026-09-03：索引型手册 720 行，12 课全覆盖 + 实战项目收录 + 决策清单 + 考点速查；链接/图片/锚点/关键数字四项自动化核查全通过）
- [x] **实战经验 / 排障速查手册 / 场景解法库**（Phase 5）— pedagogy + learner 双视角 + **证据纪律核查**，P0=0 通过（2026-09-03：三份一次生成共 1090 行，10 条故障模式 + 10 条 QRH 条目 + 7 个设计题；五硬特征 / 四硬要求 / 四要素 / 锚点 / 交叉引用五项自动化核查全通过）

## 每课交付必查项

> 本仓库既有约定（适用于 learning 仓库下所有课程讲义），评审时逐项核对：

- [ ] 课文件末尾含「🚀 下一批接力提示词」段（可直接复制发给 AI 的续学 prompt）
- [ ] 课文件末尾含「🧭 课程导航」段（上一课 / 下一课 / 返回课程目录）
- [ ] 交付后回写四处档案：`00-学习档案.md`（进度表本课行 + 评审记录）、本清单（勾选）、对应阶段 `overview.md`（本课标 ✅）、`02-课程目录.md` 与 `01-学习路径总览.md`（索引链接与进度）

## 评审记录（勾选后追加结论摘要）

| 日期 | 评审对象 | 评审方式 | P0 数 | 意见摘要 |
|------|----------|----------|-------|----------|
| 2026-09-03 | **产物仓库卫生检查（Phase 5 收尾）** | 五步法（列清单 → 状态分流 → 三分类判定 → 只追加本轮 → 校验未误伤） | 0 | 扫描范围按规范固定为 `doris/`（非全仓库，避免误伤用户源码）：364 → **189 个文件**（2.9M → 2.1M）。**判定表**：✅ 要提交 = 30 个 `*.md` + 29 个 `stages/assets/*.svg` + `learning-path-overview.svg` + 66 个讲义正文引用的脚本（setup / stepN / cleanup / lab / taskN）；🚫 要忽略 = **174 个**写课过程一次性探针与非最终迭代版（`probe` / `diag` / `final-check` / `deep-dive` / `refill` / `broker-load1-4` / `profile2-6` / `-bench2` 等），实测数字早已固化进讲义与 `final-课程手册.md`，脚本本身未被任何讲义引用；🚫 空目录 `doris/doris/assets/`（误建、无内容，已删）。**⚠️ 踩坑两处，均由第 5 步校验抓出并修复**（这恰恰证明校验步不可省）：① 首版规则 `lesson[0-9][0-9]-*[0-9].sh` 本意忽略 `-2`/`-3` 迭代版，却连带吞掉 `lesson12-step3.sh`（讲义正文直接引用）——典型的「宽泛通配误伤同名段教学产物」，已回退为精确匹配 `-*-[0-9].sh` 并对 35 个 `step*`/`bench*`/`taskN` 脚本逐条写 `!` 例外；② 补录规则时把 `block-lab.sh` / `vec-lab.sh` 也写进了忽略，与首轮「lab 系列是教学脚本」的保守判定自相矛盾，已删除并注明不再写 lab 相关忽略规则。**最终校验**：现存 130 个 assets 文件 `git check-ignore` **0 命中**（无教学产物被吞）；讲义引用的 66 个脚本**全部存在**（无幽灵引用）；空目录 0 个；本轮 17 个 `hygiene-*` / `z*.py` 临时脚本全部删除。`.gitignore` 新增 doris 规则 41 行（全部锚定 `doris/` 路径，不用 `/dist/` 这类波及全仓库的写法）|
| 2026-09-03 | 实战经验 / 排障速查手册 / 场景解法库（Phase 5） | 主 agent 内联（pedagogy + learner，叠用 B1-B8 / C1-C7 专项维度） | 0 | 三份一次生成（同源压缩，未分开写），共 1090 行。P0×0。评审发现并修复 P1×2：①【learner·L2 可导航性】`final-课程手册.md` 仍标注排障手册「待生成」，但文件已产出，读者会以为缺失 → 更新为三份产物的完整导航链接；②【B8 汇总一致性】手册「交付状态」表仍把 Phase 5 标为 ⬜ 待生成 → 改为 ✅ 已生成并附链接。**五硬特征**（排障手册）齐全：索引表按症状倒查 23 处锚点全部有效 / 每条有「一眼识别」/ 第一步永远是「止血」/ 用条件-动作分支 / 每条末尾有「若无效」升级出口。**四硬要求**（解法库）齐全：7 场景每个 ≥4 个解法（共 31 个，均带代价与适用边界）/ 14 个 `<details>` 折叠（提示+解法各一）/ 每个场景有知识点挂钩回指课时 / 有推荐路径递进与不适用边界。**四要素**（经验层）齐全：五反模式 + 10 条五段式故障模式 + 四组上线 Checklist + 2 个联网核查的真实事故案例。**证据纪律**：🟢本机实测 109 处、🔵联网核查 10 处、⚪公认实践 14 处，⏳低置信 3 处且**均未入排障手册**（只留在经验层并标注）。**宁缺毋滥已落实**：解法库从 12 个候选筛出 7 个场景，并记录 5 个淘汰理由避免重复讨论。**自动化核查**（`assets/tmp-verify-phase5.sh`）：跨文件引用 12 条全可达、23 处锚点全有效、10 条故障模式全部被手册回指、五项硬要求全达标 |
| 2026-09-03 | 课程手册汇总（Phase 4） | 主 agent 内联（pedagogy + learner） | 0 | 索引型手册（用户指定方案 A）720 行，覆盖 12 课 + 实战项目 + 决策清单 + 考点速查。P0×0；评审发现并修复 P1×2：①开头写「4 个阶段」但与「学习路径总览」表的 5 个阶段行（含「阶段 5 综合实战」）自相矛盾 → 改为「4 个教学阶段 + 1 个实战阶段」；②「怎么用这份手册」指向 `09-排障速查手册.md` 但该文件属 Phase 5 尚未生成，读者点击必然落空 → 标注「待生成」。**汇总完整性核查**（skill 四项硬要求）全达标：✅ 12 课全部纳入（课条目数脚本校验 = 12）；✅ 路径总览与各阶段概览在开头（学习路径图 + 阶段速查表 + 36 知识点地图）；✅ 各课「一图总结」全部保留（19 张 SVG 引用，逐个验证文件存在）；✅ 实战项目已收录（需求 + 知识点地图 + 五条设计决策摘要 + 五个 Task 链接）。**自动化核查**（`assets/tmp-verify-manual.sh`）：22 条内部 md 链接全可达、19 张 SVG 全存在、3 个锚点全有效、10 项关键数字与 overview/原文交叉核对一致。⚠️ 锚点核查首次报「#四个静默失败全课程最贵的收获」失效，定位为**校验脚本误报**（GitHub 锚点规则剥离全角括号，脚本归一化未处理），已修正脚本后复验通过，**未改动手册内容**。**手册自主新增内容**（原课无、手册首次整合）：「四个静默失败」汇总表（跨 Task 2 / Task 5 / 课 11 提炼，全课程最贵的收获）、「决策清单」（该不该用 / 建模决策 / 关键配置决策 / 存算一体 vs 分离四组）、「考点速记」（高频数字按课归类 + 9 组易混点对照 + 常考「唯一·第一·默认」+ 常考「不能/不支持」） |
| 2026-09-03 | Phase 3 结课实战（Task 1-5） | 主 agent 内联（pedagogy + learner） | 0 | P0×3 均已修复：①Task 4 脚本 3.2 用 `ADD COLUMN ... DEFAULT ''` 加列，导致 3.3 改宽必然失败（`Can not change default value`）而脚本还错误打印「已改为 VARCHAR(128)」，读者照抄会以为成功 → 改为正反对比演示（不带 DEFAULT 可改宽 / 带 DEFAULT 两条路全堵）+ 删掉错误提示 + task-4 文档 4.2 节补全复现与四步规避法；②Task 5 脚本 2.3 在 `doris-learn` 里找 `kafka-console-producer.sh`（实测 `No such file or directory`，该文件在独立容器 `doris-kafka`，broker 是 `kafka:9092`），实时性验收必然失败 → 已改，实测 19 秒可见；③Task 5 脚本 1.7 用 `order_date='2025-06-15'` 验证「BE 真读到数据」，实测该日在 DWD 无数据返回 NULL，读者会误判成「表坏了」→ 改用月份区间，并在文档 2.5 节把「验证失败先确认数据存在」写成教学点。P1×2：①task-5 文档第 0 节章节引用错误（「见第 4.1 节」应为 5.2 节）已修正；②README 任务清单五条仍标「⬜ 未开始」与已交付事实不符，已全改 ✅ 并补实测摘要 + 两处口径说明（ODS 少一半是设计如此 / 副本是 1 非生产的 3）。**复杂度四门槛核查**：✅ 跨 12 课知识点（四层建模 / 双链路接入 / Rollup+MV+Colocate+Profile / 副本+隔离+变更+备份 / 验收+边界）；✅ 五个 Task 有明确依赖链（1→2→3→4→5，已按序验证重跑）；✅ 全部数据在真机实测（无编造数字，未实测项均标 🔴 并在文档说明）；✅ 有可判定验收标准（五条，逐条实测核对）。另自主发现四项（本项目最贵的收获）：①🔥**「单行」DELETE 实际删 331 行** —— `order_id` 是 `ROW_NUMBER() OVER (PARTITION BY order_date ...)` 生成的**只在当天唯一**（`distinct_oid=52784` vs `rows_total=9999563`），`DELETE ... WHERE order_id=7088` 删 331 行且不报错，必须带全唯一键才能精准删 1 行 —— 本项目**第四个静默失败，也是最危险的**（前三个是数据变少，这个是直接误删）；②🔥**分区 MV 刷不出数据** —— 6 组对照（分区×IMMEDIATE / DEFERRED+COMPLETE / 单表 / partition_sync_limit 全 0 行，非分区全 96 行），且建表 / REFRESH / SHOW PARTITIONS（28 分区全 NORMAL）三个环节全部显示正常，只有 SELECT 才发现是空的 → 改用非分区 MV；③`information_schema.partitions` 无 `REPLICATION_NUM` 列，查副本数要用 `SHOW CREATE TABLE` 的 `replication_allocation`；④`SHOW RESTORE` 不能用 awk 按列号取 Status（Info 列含 `\n` 转义 JSON 打乱列位置），改用 `grep -oE` |
| 2026-09-03 | 阶段 4·课 12 | 主 agent 内联（pedagogy + learner） | 0 | 收官课（36/36 知识点）。P0×1（骨架的交叉验证硬要求未完全兑现：2.6 节只写了「可靠性交给共享存储」，未点明「数据可靠性由共享存储层承担」+ 副本语义与扩缩容行为的具体变化 → 已补课 9 概念对照表 5 行 + 运维关注点迁移说明）已修复。P1×2：①正文 1.3 节 chinese parser 代码块 INSERT 后直接查询，读者照抄可能因统计未刷新而先返回 0（评审 1 复现到该现象），已加 sleep 提示并在 setup 脚本同步加注释；②S3 TVF 的三个语法坑（缺 uri / 缺 use_path_style / csv_schema 不支持 varchar(n)）与 CREATE EXTERNAL TABLE ENGINE=S3 被拒，仅存在于探测脚本未进正文，已补 2.3 节「S3 TVF 的三个语法坑」专节并保留 4 条报错原文。另自主发现三项：①**测延迟必须先剥离连接开销**——第一版测得点查 130 ms/次，做空连接对照后发现 26 秒几乎全是 docker exec 开销，改单连接串行后为 5.7-6.6 ms/次（约 148-176 QPS），差点把错误结论写进正文；②**ROLLBACK 静默无效**——转账场景钱扣了账加了，ROLLBACK 不报错但什么都没撤，transaction_isolation 仍显示 REPEATABLE-READ；③**存算分离的本地性代价只落在明细扫描**——聚合查询本地 0.16-0.20s vs 共享存储 0.20-0.23s（1.2 倍，几乎无差），明细扫描 0.13-0.15s vs 0.40-0.42s（差 3 倍），原因是聚合瓶颈在计算、列式 parquet 只需传少数列 |
| 2026-09-03 | 阶段 4·课 11 | 主 agent 内联（pedagogy + learner） | 0 | P0×1（`MODIFY COLUMN` 改带 DEFAULT 值的列报 `Can not change default value`，显式写 `DEFAULT NULL` 也不行；正文支持矩阵已加 ⚠️ 限制说明 + 绕过办法「加新列→UPDATE→删旧列→改名」，被拒表与速览表各补一条）已修复；P1×1（cleanup 用 `DROP SNAPSHOT` 清理快照失败，实测 4.1.3 无此语句，四种写法均报 `no viable alternative at input 'DROP SNAPSHOT'`，已改为「删仓库 + 物理清 S3 目录」，并新增 2.10 节讲这条发现）已补。另自主发现四项：①`CANCEL ALTER TABLE COLUMN` 只在作业进行中有效，单机 100 万行加列 1-3 秒即完成导致复现失败，正文如实呈现失败面并给出生产 `WAITING_TXN` 场景的复现办法；②表处于 SCHEMA_CHANGE 时再 ALTER 报 `state(SCHEMA_CHANGE) is not NORMAL. Do not allow doing ALTER ops`；③`RESTORE` 的 `replication_num` 默认是 3、不沿用原表，且语句提交时不报错，失败信息藏在 `SHOW RESTORE` 的 Status 列；④`SHOW RESTORE` / `SHOW BACKUP` 取最新作业状态须 `tail -1`，否则读到历史 CANCELLED 记录 |
| 2026-09-03 | 阶段 4·课 10 | 主 agent 内联（pedagogy + learner） | 0 | P0×1（正文 1.4 把 `memory_high_watermark` 列为可用属性，但单独设 70% 会因低水位默认 75% 而报 `should bigger than memory_low_watermark(75)`，已加⚠️标注 + 补"必须成对配置"的正确写法与报错原文）已修复；P1×2（①主线三数字正文用探测期首值、脚本实跑得第二组，两处不一致，已全文统一为验收数据并加"两次独立重跑对比"说明块；②Spill 数据同样存在探测值与验收值差异，正文 5 处 + 第四幕预期输出 + 速览 + 第五幕 + 误区 2 + 接力提示词已全部统一）已补。另自主发现三项：容器 cgroup 只读挂载导致 `max_cpu_percent` 完全不生效（已用 🟡 标注而非编造数字）；内存超限真实机制是"暂停→尝试回收→不行才报错"（BE 日志 `Insert one new paused query`）；spill 代价高度非线性（128MB 报错 / 256MB 挣扎 88 秒仍报错 / 512MB 成功但要 480 秒 / 1024MB 只要 1.06 秒） |
| 2026-09-03 | 阶段 4·课 9 | 主 agent 内联（pedagogy + learner） | 0 | P0×2（①正文脚本清单写 `lesson09-step2.sh`/`lesson09-step3.sh` 但文件并不存在，已改为实际交付的 5 个脚本 add-be2 / setup / step4 / step5 / cleanup；②宕机演练「预期报错」落空，根因是 tablet 未落在宕机节点上，已新增 `pinned_be2` 表 + 脚本动态选择宕机节点保证必然命中）均已修复；P1×2（`SHOW DATA` 统计延迟导致 cost1 显示 499228/1000000、1.260MB，已改 75 秒等待 + COUNT 交叉校验并在正文记录该坑；正文 tablet 数字为首次实测值、重跑会变，已标注「判据是趋势不是绝对值」）已补；P2×1（`history_tablets` 是累计值不归零，易被误读为本次搬迁量）已加说明。另自主发现两项前九课均未注意：`SELECT COUNT(*)` 走元数据行数优化根本不扫 BE，不能用来验证数据可查；`ADMIN MIGRATE TABLET` 在 4.1.3 报语法错误，tablet 落点无法手动指定 |
| 2026-09-02 | 阶段 3·课 8 | 主 agent 内联（pedagogy + learner） | 0 | P0×6（①第一幕 489-654ms 与后文 387-475ms 矛盾；②正文用探测期表名 `dim_mid`，脚本建的是 `non_colo_dim`；③正文用 `dim_province`，脚本建的是 `dim_region`，且实测 `orders JOIN dim_region` 出的是 BUCKET_SHUFFLE 不是 BROADCAST，Broadcast 证据改用 `fact_1m JOIN dim_region` 并把该反常写成教学点 + step2 新增 2.1b；④Colocate 依赖 `fact_prov` 但脚本未建 → 补 setup 1.6；⑤动态 schema 依赖 `v_probe` 但脚本未建 → 补 step4 4.3b；⑥3.6 节用探测期临时表 `mv_probe` → 改为 `mv_prov_pay_daily`）均已修复；P1×2（step5 参考值注释过时已更新为实测；课 9 导航链接层级错误已改 `../../4-...`）已补；P2×1（step7.7 行数注释 104→106 实测为 86→88）已修正 |
| 2026-09-02 | 多阶段大纲 | 主 agent 内联（pedagogy） | 0 | P1×2 / P2×2：Rollup 与异步 MV 易混、缺 Schema Change；均已修订采纳 |
| 2026-09-02 | 阶段 2·课 5 | 主 agent 内联（pedagogy + learner） | 0 | P0×1（知识点 3 示例演示用 `DROP MATERIALIZED VIEW` 删除 `ADD ROLLUP` 建的索引，语法不配对，读者照抄必报错）已补建/删语法对照表；P1×2（SVG 里 NGram 存储增量写成 +208MB，实为 +276MB；`gram_size` 未解释 gram 是什么）已改 |
| 2026-09-02 | 阶段 2·课 4 | 主 agent 内联（pedagogy + learner） | 0 | P0×1（第四幕表 B/C 写成"列定义完全相同"注释省略，**与课 3 同类问题第二次复发**）已补全三份完整 DDL + 导入语句 + 建分区提示；P1×3（银行柜台类比按"姓氏首字母"暗示范围划分、误导 Hash 语义；Random 分桶未说明适用场景；第五幕未回扣第二幕"分区越细越好"设问）已改 |
| 2026-09-02 | 阶段 2·课 3 | 主 agent 内联（pedagogy + learner） | 0 | P0×1（第四幕建表语句写成省略形式 `CREATE TABLE orders_dup (... 13 列 ...)`，与课 1 同类问题复发，零基础照抄必失败）已补完整 DDL；P1×3（月度汇总表类比未说清"导入时自动累加"、HLL/BITMAP 未解释 UV 概念、"啊哈"口语刻意）已改 |
| 2026-09-02 | 阶段 3·课 7 | 主 agent 内联（pedagogy + learner） | 0 | P0×3（①setup 脚本 docker exec 缺 -i，不转发 stdin 致建表/造数**静默失败不报错**，后续 7 步全对不上；②SHOW DATA 紧跟 INSERT 返回 0（统计延迟约 45s），正文预期输出对不上；③第四幕步骤 4 的 1/2/3 个 pad 列耗时与 OutputBlockBytes 为**未实测的编造值**，已全部替换为实测数据）均已修复；P1×2（步骤 3/4 预期输出未说明数字浮动范围，读者易误判跑错；grep -A 12 取到的 OutputBlockBytes 是单 instance 值非全局，口径冲突）已补说明；P2×2（Profile 三层结构图未标注各层回答什么问题；Block 切分规则未解释宽列为何块数更多）已补 |
| 2026-09-02 | 阶段 3·课 6 | 主 agent 内联（pedagogy + learner） | 0 | P0×2（第四幕步骤 3/6/7 缺 TRUNCATE 前置致预期行数对不上；步骤 1/5 缺 docker pull 致首次运行失败）已补；P1×1（Group Commit 15 秒与 34 秒口径差异未说明）已补口径块；P2×2（retention 术语未解释、“快 200 倍”表述歧义）已改 |
| 2026-09-02 | 阶段 1·课 2 | 主 agent 内联（pedagogy + learner） | 0 | P0×2（复现步骤 CSV 来源缺失致读者卡住 / Stream Load 参数未解释）已补；P1×2（餐厅类比"实习生"误导 Observer 永不参与选主、知识点 3 剧透第四幕结论）已改悬念式设问 |
