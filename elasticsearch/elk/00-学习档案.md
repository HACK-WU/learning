# ELK 系统学习 · 学习档案

> 本档案是 **elasticsearch 主题下的子教程**（ES 主课 5 阶段 / 15 课 / 46 知识点已全部完成）。
> 断点续学时**先读本文件**：画像 → 环境 → 进度表 → 评审记录。

## 学习者画像

| 项目 | 值 | 来源 |
|------|-----|------|
| 主题 | ELK（Elasticsearch + Logstash + Kibana + Beats）系统学习 | 用户指定 |
| 定位 | `elasticsearch/` 下的**子教程**；ES 主课已完结，本教程补上"数据怎么进来、怎么变好看"的半条链路 | 用户指定 |
| 当前水平 | **ES 已系统学完**（46 知识点）；**Logstash / Kibana / Beats 几乎没用过** | 用户确认（2026-09-06） |
| 学习目标 | 全目标：①理解原理 ②动手实操 ③生产落地与运维 ④决策参考 | 用户指定（四项全要） |
| 输出格式 | Markdown 主载体（Mermaid + SVG 图表） | 默认值 |
| 模式 | 课程制 · **4 阶段 / 12 课 / 36 知识点** | 用户选择 |
| 实操环境 | **macOS arm64 / 18GB 内存 / Docker Compose 起全套容器** | 环境探测 + 用户选择 |
| 版本基线 | **Elastic Stack 9.5.3**（2026-09-03 发布） | 联网核查（2026-09-06） |

> 说明：`course-reviewer` 子 agent 尚未创建，评审由主 agent 内联执行（教学法视角 + 学习者视角），评审独立性受限。可随时让我按 `create-sub-agent` 规范创建独立 agent。

## 环境探测结论（2026-09-06）

| 探测项 | 结果 | 对课程的影响 |
|--------|------|--------------|
| 操作系统 | **macOS（Darwin 25.6.0，arm64，Apple 芯片）** | ⚠️ **与 ES 主课的 Windows 11 环境完全不同** |
| Docker | **29.4.1 已安装，但守护进程未运行**（`~/.docker/run/docker.sock` 不存在） | 实操前**必须先启动 Docker Desktop**；daemon 未起时所有 `docker compose` 命令失败 |
| 物理内存 | 18 GB | 四件套（ES + Kibana + Logstash + Filebeat）可同时起，需给 Docker Desktop 分配 ≥ 6GB |
| 磁盘 | 460 GiB，可用 210 GiB | 充足 |
| Java | 17.0.9（本机） | 容器镜像自带 JDK；本机 Java 仅作对照，不作为运行依赖 |
| Node / Python | v22.15.1 / 3.9.6 | 需要写验证脚本时用 Node |
| 已有 Elastic 软件 | 无（home 下无 elasticsearch / kibana / logstash / filebeat 目录） | 全新环境，无历史包袱 |
| 9200 端口 | 无服务 | ES 主课那套 Windows 单机/三节点集群**不在本机**，不可引用其索引与密码 |

### ⚠️ 与 ES 主课的环境差异（写作时务必遵守）

ES 主课档案中记录的**全部 Windows 约束在本课程一律作废**：

| ES 主课（Windows） | 本课程（macOS） |
|--------------------|-----------------|
| `curl.exe`、PowerShell 引号坑、无 Git Bash、CMD 续行符 `^`、无 `grep` | **zsh + 标准 `curl`（单引号可用）+ `grep`** |
| 原生 zip 发行版，进程与目录裸露 | **Docker Compose 容器**，命令以 `docker compose ...` 为主 |
| ES 主课索引（l6_shop / l9_orders 等）与密码 | 一律**不可引用**，本课数据全部重新生成 |

### 🔌 当前环境状态（断点续学必读 · 2026-09-06 更新）

课 2 交付后，本机**有容器正在运行**。续学前先确认它们还在：

```bash
cd elasticsearch/elk/playground/01-minimal-stack && docker compose ps
```

| 项目 | 值 |
|------|-----|
| Docker | Desktop **29.4.1**，守护进程**已启动**（⏰ 默认不开机自启，新会话可能要 `open -a Docker`） |
| Docker VM 资源 | **MemTotal 8.2GB**（aarch64 / linux） |
| 运行中容器 | **课 3 的三件套**：`elk-es`（9200，healthy）+ `elk-kibana`（5601）+ `elk-filebeat`（读 `logs/demo-app.log`） |
| 访问地址 | ES `http://localhost:9200` ｜ Kibana `http://localhost:5601` |
| 凭据 | 超管 `elastic` / `ELKlearn2026` ｜ 服务账号 `kibana_system` / `KibanaSys2026` |
| 数据落点 | data stream **`filebeat-9.5.3`**（课 3/5 + 课 9 路径 C）｜ 课 9 新增 `fb-proc-2026.09.07`（路径 A）、`audit-demo-2026.09.07`（脱敏验证）、`ls-nginx-2026.09.06` / `ls-orders-2026.09.06`（课 8 路径 B）｜ 课 10 新增 `logs-nginx.access-default`（三段式命名演示）、`logs-demo-nginx-default`（ILM 自动滚动/删除演示）、普通索引 `explosion-strict` / `explosion-lenient` / `dt-demo`（字段爆炸与动态模板演示）｜ **课 11 新增** data stream `logs-appdemo-default`（600 条 3h 内演示日志，**为可复现删旧重建**）+ Kibana 端 saved objects：Data View `app-demo`（title=logs-appdemo-*, timeField=@timestamp，4 个 Lens metric + 1 个 2×2 Dashboard） |
| 已建 Data View | `filebeat-9.5.3`（时间字段 `@timestamp`） |
| 运行中 Beat | **课 6 是两个 Beat**：`elk-filebeat`（日志）+ `elk-metricbeat`（指标，system 模块 cpu/memory，10s 一次） |
| 编排文件 | **课 11 用** `playground/10-kibana-visual/`（**纯 ES + Kibana API 实验，不起新容器**；含 `gen-app-logs.py` 造 600 条 3h 内演示日志 + `mk-dashboard.py` 一键建 4 个 metric Lens + 1 张 2×2 Dashboard + assets/ 12 张实拍截图）｜ 课 10 `playground/09-storage-strategy/`（纯 ES API 实验）｜ 课 9 `08-where-to-process/`（两个直连 ES 的 Filebeat；**只加 Filebeat，ES/Kibana/Logstash 复用 07 栈**，故它声明外部网络 `07-reliability_default`，**07 不在跑则起不来**）｜ 课 8 `07-reliability/`（双管道 Logstash + 持久化队列 + 双 Filebeat，课 9/10/11 期间**保持运行**）｜ 课 7 `06-logstash-pipeline/`、课 6 `05-beats-family/`、课 5 `04-modules-processors/`、课 4 `03-filebeat-internals/`、课 3 `02-end-to-end/`、课 2 `01-minimal-stack/`（端口均冲突，已 down） |
| 安全配置 | `xpack.security.http.ssl.enabled=false`（**HTTP 层 TLS 已关**，认证仍开） |
| **结课实战栈**（Phase 3，最后运行） | `projects/日志平台生产化/实现/` —— 容器 `cap-es` / `cap-setup`（一次性）/ `cap-kibana` / `cap-logstash` / `cap-filebeat`；端口同为 9200 / 5601 / 5044 / 9600（**与 playground 栈冲突，跑前须先 down 掉 07-reliability / 08-where-to-process**）；三个命名卷 `es-data` / `logstash-queue` / **`filebeat-registry`**（第 4 个坑的解法）；凭据同上（`elastic`/`ELKlearn2026`、`kibana_system`/`KibanaSys2026`）；数据落点 data stream **`logs-shopapi-default`**（gen1 1000 / gen2 90）+ 告警落盘索引 **`shopapi-alerts`**；`scripts/bootstrap.py` 一键幂等重放全部 ES/Kibana 资产 |

> ⚠️ **执行过 `docker compose down -v` 之后**：卷删除 → 数据清空 → **`kibana_system` 密码也失效**，必须重新设置（命令见课 2 第四幕第 3 步）。

## 故事主线

| 要素 | 内容 |
|------|------|
| 主角 | 一条日志（从应用 stdout 一路走到 Kibana 告警） |
| 冲突 | 线上报警、5 台机器，工程师逐台 ssh 上去 `grep`——**找不到、留不住、看不懂** |
| 阶段推进 | ① 看得见全貌（阶段 1）→ ② 拿得到（阶段 2 采集）→ ③ 看得懂（阶段 3 处理）→ ④ 留得住、用得上（阶段 4 存储可视化） |
| 收束 | 一条从"日志产生"到"告警触发"的完整链路 + 一份「该不该上 ELK、上到哪一步」的决策 |

## 知识点级进度表

| 阶段 | 课 | 知识点 | 状态 | 完成日期 |
|------|-----|--------|------|----------|
| 1 | 课1 | 日志为什么难查 | ✅ 已完成 | 2026-09-06 |
| 1 | 课1 | ELK 四个组件各管什么 | ✅ 已完成 | 2026-09-06 |
| 1 | 课1 | ELK 的起源与生态位 | ✅ 已完成 | 2026-09-06 |
| 1 | 课2 | Compose 编排四件套 | ✅ 已完成 | 2026-09-06 |
| 1 | 课2 | 首次启动的安全配置 | ✅ 已完成 | 2026-09-06 |
| 1 | 课2 | 容器环境的观测与重建 | ✅ 已完成 | 2026-09-06 |
| 1 | 课3 | 最小链路亲手跑通 | ✅ 已完成 | 2026-09-06 |
| 1 | 课3 | 一条数据长什么样 | ✅ 已完成 | 2026-09-06 |
| 1 | 课3 | 组件间的契约 | ✅ 已完成 | 2026-09-06 |
| 2 | 课4 | harvester 与 input 机制 | ✅ 已完成 | 2026-09-06 |
| 2 | 课4 | registry 与 at-least-once | ✅ 已完成 | 2026-09-06 |
| 2 | 课4 | 多行合并 | ✅ 已完成 | 2026-09-06 |
| 2 | 课5 | modules 开箱即用 | ✅ 已完成 | 2026-09-06 |
| 2 | 课5 | processors 轻量加工 | ✅ 已完成 | 2026-09-06 |
| 2 | 课5 | 输出与背压 | ✅ 已完成 | 2026-09-06 |
| 2 | 课6 | 家族成员各管什么 | ✅ 已完成 | 2026-09-06 |
| 2 | 课6 | 采集端选型对比 | ✅ 已完成 | 2026-09-06 |
| 2 | 课6 | 什么时候不该用 Beats | ✅ 已完成 | 2026-09-06 |
| 3 | 课7 | 管道结构与事件模型 | ✅ 已完成 | 2026-09-06 |
| 3 | 课7 | grok 解析（增量·回指 ES 课 11） | ✅ 已完成 | 2026-09-06 |
| 3 | 课7 | date 与 mutate | ✅ 已完成 | 2026-09-06 |
| 3 | 课8 | 持久化队列与 at-least-once | ✅ 已完成 | 2026-09-06 |
| 3 | 课8 | 背压与吞吐调优 | ✅ 已完成 | 2026-09-06 |
| 3 | 课8 | 多管道与隔离 | ✅ 已完成 | 2026-09-06 |
| 3 | 课9 | Ingest Pipeline 日志配法（回指 ES 课 11） | ✅ 已完成 | 2026-09-07 |
| 3 | 课9 | 处理位置之争（决策表） | ✅ 已完成 | 2026-09-07 |
| 3 | 课9 | ECS 字段规范与脱敏 | ✅ 已完成 | 2026-09-07 |
| 4 | 课10 | data stream 日志配法（回指 ES 课 13/15） | ✅ 已完成 | 2026-09-07 |
| 4 | 课10 | ILM 滚动与冷热 | ✅ 已完成 | 2026-09-07 |
| 4 | 课10 | 模板在日志场景的配法 | ✅ 已完成 | 2026-09-07 |
| 4 | 课11 | Data View 与 Discover | ✅ 已完成 | 2026-09-08 |
| 4 | 课11 | KQL 与查询过滤 | ✅ 已完成 | 2026-09-08 |
| 4 | 课11 | Lens 与 Dashboard | ✅ 已完成 | 2026-09-08 |
| 4 | 课12 | 告警规则与连接器 | ✅ 已完成 | 2026-09-10 |
| 4 | 课12 | 容量、权限与备份 | ✅ 已完成 | 2026-09-10 |
| 4 | 课12 | 该不该上 ELK（决策收束） | ✅ 已完成 | 2026-09-10 |

**进度：36 / 36 ✅ 全部完成**（阶段 1-4 全部交付，2026-09-10）

> 🎓 **结课综合实战项目《日志平台生产化》已完成**（2026-09-10）—— 详见 [projects/日志平台生产化/README.md](projects/日志平台生产化/README.md)。
> 📕 **汇总手册 [`final-课程手册.md`](final-课程手册.md) 已交付**（Phase 4，2026-09-13，约 980 行）—— 11 章把 12 课 + 实战项目压成一册：逐课「一图收束 + 三句话 + 实测数字 + 误区 + 命令速查卡 + 官方文档」、5 张决策表、4 个生产级坑对照表；第十一章含**结课后三条深入方向**。
> 🧯 **Phase 5 三态三件套已交付**（2026-09-13）—— [`08-实战经验.md`](08-实战经验.md)（**学习态**，383 行：7 行适用边界 + 12 条反模式 + 9 行假故障表 + 10 条故障模式五段式 + 33 项 Checklist + 2 个联网核实真实事故）/ [`09-排障速查手册.md`](09-排障速查手册.md)（**使用态 · 机长 QRH**，337 行：症状倒查索引 16 行 🔴4/🟡10/⚪2 + 10 条症状 + 通用止血 + 官方依据表）/ [`10-场景解法库.md`](10-场景解法库.md)（**设计态**，497 行：8 场景 43 解法 + 16 折叠块）。评审记录见 [`收尾产物评审记录.md`](收尾产物评审记录.md)。
>
> 🎉 **课程完整闭环**：4 阶段 / 12 课 / 36 知识点 + 结课实战项目 + 汇总手册 + 三态三件套；`00-评审清单.md` **16 / 16 全部勾选**；`08`↔`09` 故障模式编号三向双向映射 1-10 全覆盖。
> 下一站 = **可选 Phase 6 知识点对齐**（说一句"考我一下 ELK"即触发，可指定模块）；或按汇总手册第十一章的**结课后三条深入方向**走（Phase 6 / 真环境迁移 / 打通可观测三支柱）。

## 与 ES 主课的重叠处理原则

ES 主课已覆盖 **Ingest Pipeline / grok / Data Stream / ILM / 索引模板 / 别名 / RBAC / 快照** 等内容。本教程对这些知识点的处理一律是：

1. **回指链接**（相对路径指向 ES 主课对应课时），不重复讲基础
2. **只讲 ELK 视角的增量**（日志场景怎么配、什么时候该换位置）
3. 在课内显式标注"ES 主课已讲，详见链接"

## 评审记录

> 评审方式：**主 agent 内联**（`course-reviewer` 子 agent 未创建，独立性受限）。批次内容评审为双视角（A：事实核查 / 教学法；B：零基础教学体验），冲突由主 agent 裁决。

| 日期 | 评审对象 | 评审方式 | 意见摘要 | 处置 |
|------|----------|----------|----------|------|
| 2026-09-06 | 多阶段大纲（初版） | 主 agent 内联（pedagogy 视角：阶段划分合理性 / 知识点递进 / 前置依赖 / 覆盖面 / 故事主线） | P0×0 / **P1×3** / P2×0。覆盖面检查发现三处缺口：① 生产运维目标下**缺"缓冲层"这一真实生产模式**（Beats → Kafka → Logstash）② 日志场景**缺"字段爆炸（mapping explosion）"**——来源杂时动态映射暴涨拖垮集群，是最高发的存储事故 ③ Kibana 是三件套零基础者的主战场，**缺"入口地图"**，第一次进 Kibana 会迷路 | 三处均已当批补进对应课骨架（课 8.2 / 课 10.3 / 课 11.1）的关键点与填充要点清单，标注「大纲评审补，P1」，供 Phase 2 写正文时展开 |
| 2026-09-06 | 阶段 1 课 1《一条日志的旅程》首次交付 | 主 agent 内联（A 事实核查 + B 三件套零基础教学体验），**命令全部本机实测** | P0×0 / **P1×2** / P2×0（均已当批修复，已清零）。**A 视角 P1**：收束 Mermaid 图 Logstash 节点只有入边无出边，语义成死胡同 → 补 `L --> E`。**B 视角 P1**：第四幕用 macOS 系统日志演示，零基础读者会脱节"我要管的是应用日志"→ 补「为什么拿系统日志举例子」说明段。**核心实测**：近 1h 系统日志 **2,766,622 条** / error **138,249 条** / grep 命中 **1,520,807 条**耗时 **12.9 秒**；`~/Library/Logs` 161 个 .log（15M）+ `/var/log` 31M；5 台×30 天外推 **9,959,839,200 条**。**事实核查**：8 条官方文档链接 curl 全部 200；Loki「只索引标签不索引全文」经官方 GitHub README 核实。**新环境坑**：zsh 的 `log` 是内置命令，须用 `/usr/bin/log` | 全部当批修复后交付；环境坑已写入命令速查卡与档案事实核查记录 |
| 2026-09-06 | 阶段 1 课 2《把 ELK 跑起来》首次交付 | 主 agent 内联（A 事实核查 + B 部署零基础教学体验），**容器真起真跑** | P0×2 / **P1×1** / P2×0（均为自查发现，当批修复后清零）。**P0-1**：`playground` 相对链接写成 `../../`（少跳一级），5 处全部断链 → 已改 `../../../`，并加批量链接校验（13 条全通）**P0-2**：ES 写入响应录入时多处一个引号，与真实输出不符 → 已按原文修正。**P1**：Mermaid 用自环表达"等待"易渲染歧义 → 改为显式等待节点。**核心实测**：`up -d kibana` 输出 `elk-es Waiting → Healthy → elk-kibana Starting`（**`condition: service_healthy` 的实证**）；ES **40 秒 healthy**；Kibana 就绪 **000→503→200**；`getent hosts elasticsearch` → **172.20.0.2**（容器网络按服务名互访实证）；**`down` vs `down -v` 对照实验**——卷保留 vs `Volume ... Removed`、文档 `found:true` vs 404 `no such index [elk-lesson02-test]`、**`kibana_system` 密码连带失效（401）**，ES 日志原文 `failed to authenticate user [kibana_system]`；不带凭据访问 ES **401**（`missing authentication credentials for REST request [/]`）、带凭据 **200**（版本 9.5.3 / Lucene 10.5.1）。**诚实标注**：试图复现 OOM（1GB 容器上限 + 900MB 堆）**未成功**，ES 正常启动 GREEN、`OOMKilled=false` → 该部分只给排查方法，不编造报错原文 | 全部当批修复后交付；新增配套文件 `playground/01-minimal-stack/docker-compose.yml`，环境现状已记入下方「当前环境状态」 |
| 2026-09-06 | 阶段 1 课 3《端到端走一遍》首次交付（**阶段收官**） | 主 agent 内联（A 事实核查 + B 三件套零基础教学体验），**三件套真跑通** | P0×1 / P1×0 / P2×0（自查发现，当批修复后清零）。**P0**：跨阶段链接写成 `../2-采集层Beats/...`（少跳一级）→ 改 `../../2-...`，已批量校验 25 条链接全通。**真实翻车记录（已原样写入讲义）**：初始配置自定义索引名 `demo-app-%{+yyyy.MM.dd}` + `setup.template.name`，Filebeat 启动即反复报 `no matching index template found for data stream [demo-app]`（HTTP 400），整条链路起不来；删掉自定义改用默认行为后一次跑通。**核心实测**：10 条日志进 data stream **`filebeat-9.5.3`**（后备索引 `.ds-filebeat-9.5.3-2026.09.06-000001`，**yellow**：1 副本 + 单节点）；一条文档完整字段结构（`@timestamp` / `message` / `host.name` / `agent.*` / `log.file.*` / `ecs.version`）；**`@timestamp` 是采集时间 07:26:22Z（北京 15:26）而日志内容写的是 14:20——差 1 小时 6 分**（知识点 3.2 最有力的证据）；Data View 创建成功（title=`filebeat-9.5.3`、timeFieldName=`@timestamp`、`@timestamp` 识别为 `date`）；**类型冲突实测**：往 date 字段塞中文 → HTTP 400 `document_parsing_exception` → `failed to parse date field [不是时间] with format [strict_date_optional_time||epoch_millis]`，并出现 `failure_store: not_enabled`；Filebeat 自带模板 `filebeat-9.5.3` 含 `total_fields.limit: 12500`、ILM 策略 `filebeat`、`refresh_interval: 5s` | 当批修复后交付；新增 `playground/02-end-to-end/`（三件套 compose + filebeat.yml + 示例日志）；**阶段 1 全部完成（9/36）** |
| 2026-09-06 | 阶段 2 课 4《Filebeat 把文件读进来》首次交付（**阶段 2 开场**） | 主 agent 内联（A 事实核查 + B 三件套零基础教学体验），**全课实测** | P0×0 / P1×0 / P2×0。**核心实测**：① 3 个 input 启动日志（`Loading Inputs: 3` + 3 个 `Starting input (ID: ...)` + `Enabled inputs: 3`）② **registry 结构**：`data/registry/filebeat/{log.json, meta.json}`，memlog 格式，key 为 `filestream::demo-app::fingerprint::<hash>`（**证明 9.x 用 fingerprint 而非 inode 标识文件**），实测 offset **303**（demo-app）/ **588**（stack-error）③ **断点续传**：重启 12 条 → **12 条**（零重复）④ **多行合并决定性对照**：同结构 9 行堆栈，未配 multiline = **9 条**文档、配了 = **1 条**且 message 完整保留 9 行 ⑤ **文件轮转**：改名后新建同名文件 → 被识别为新文件采集（9 → 10 条）。**诚实标注 3 处未实测**：a) 删 registry 导致重复（命令未获批准）b) `close_*` 策略 c) 三类漏数据场景——均只给原理与官方依据。**链接校验**：3 条新增官方链接 curl 200、7 条相对链接全通 | 直接交付；新增 `playground/03-filebeat-internals/`（三件套 + 3 个 input 对照配置 + 3 个示例日志） |
| 2026-09-06 | 阶段 2 课 5《模块与处理器》首次交付 | 主 agent 内联（A 事实核查 + B 三件套零基础教学体验），**全课实测** | P0×0 / P1×0 / P2×0。**核心实测**：① **modules**：不配 `filebeat.config.modules.path` 时报 `modules management requires ... setting`；配好后 `modules list` 显示 `Enabled: nginx`；**ES 自动装载 `filebeat-9.5.3-nginx-access-pipeline`**（含完整 grok）② 一行 nginx 文本解析为 `source.ip` / `http.response.status_code 201` / `url.path` / `user_agent.original` / `event.dataset=nginx.access` / `service.type=nginx`，且 **`event.original` 取代了 `message`** ③ **`@timestamp` 被模块解析为日志里的真实事件时间**（`08:30:01Z` = 北京 `16:30:01`）——**正面解决课 3 遗留的"采集时间 vs 事件时间"偏差** ④ 模块内部 `input.type: log`（非 filestream）⑤ **processors 三项全部生效**：`env`/`lesson` 全员、`tags` 仅 ERROR 那条、`ecs` 被清空、`agent.ephemeral_id` 消失 ⑥ **背压/重试实测**：`docker compose stop elasticsearch` 后 Filebeat 持续 `Attempting to reconnect ... with N reconnect attempt(s)`（N 增至 6），恢复后 `Connection ... established`，**停机期间写入的 orderId=30011 完整送达（count:1），文档 10 → 13 一条不丢**。**两个真实踩坑**已写入讲义：a) modules 前提配置 b) **课 3/课 4 把宿主机日志挂到 `/usr/share/filebeat/logs`（Filebeat 自己的日志目录）导致其写不了日志**（`read-only file system`）——本课已修正为 `/var/log/app` | 直接交付；新增 `playground/04-modules-processors/`（nginx 模块 + processors + 背压实验） |
| 2026-09-06 | 阶段 2 课 6《Beats 家族与采集选型》首次交付（**阶段 2 收官**） | 主 agent 内联（A 事实核查 + B 三件套零基础教学体验），**两个 Beat 同台实测** | P0×0 / P1×0 / P2×0。**核心实测**：① **Filebeat + Metricbeat 各自的 data stream 并存**（`.ds-filebeat-9.5.3-...` 7 条 yellow / `.ds-metricbeat-9.5.3-...` 11 条 **green**——后者因 `index.number_of_replicas: 0`，顺带复习课 3 的 yellow 知识）② `metricbeat modules list` → `Enabled: system` ③ 指标与日志的形态对比（数值 vs 文本、周期驱动 vs 事件驱动、`event.dataset: system.cpu` vs `nginx.access`、`agent.type: metricbeat` vs `filebeat`）④ **发现 `system.cpu.total.pct` 是按核累加的陷阱**：实测 `cores=11` / `total.pct=0.1827` / `total.norm.pct=0.0166`，验算 `0.0166 × 11 = 0.1826 ≈ 0.1827` ✅——多核机器上把它当"使用率"会导致告警规则永不触发 ⑤ **共享 libbeat 的证据**：两 Beat 配置均含 `*.config.modules.path` + 相同的 `output.elasticsearch` + `modules.d/` 目录结构。**诚实标注 3 类未实测**：其余 4 位 Beat（Packetbeat/Heartbeat/Auditbeat/Winlogbeat 受权限与系统限制）、采集端选型对比（定性非性能实测）、容器/K8s 场景 | 直接交付；新增 `playground/05-beats-family/`（Filebeat + Metricbeat 同台）；**阶段 2 全部完成（18/36）** |
| 2026-09-06 | 阶段 3 课 7《Logstash 管道三件套》首次交付（**阶段 3 开场**） | 主 agent 内联（A 事实核查 + B 三件套零基础教学体验），**全课实测** | P0×0 / P1×0 / P2×0。**核心实测**：① `--config.test_and_exit` 输出 **`Configuration OK`**（含 `ecs_compatibility: v8` 提示行）② **grok 自定义多模式依次尝试**（订单日志两种形态：amount / reason）与 `%{PATTERN:field:int}` / `:float` 类型捕获（实测 `order_id: 50005` 整型、`amount: 99.5` 浮点）③ **`_grokparsefailure` 分流实证**：脏数据进 `logstash-failed-2026.09.06`（1 条），正常数据进 `logstash-demo-2026.09.06`（**8 条**），脏数据 tags 含 `_grokparsefailure` ④ **date 校准实测**：`17:45:01 +0800` → `@timestamp: 2026-09-06T09:45:01.000Z` ⑤ **mutate 四件套实测**：`gsub`（u405→user-405）、`rename`（service_name→service）、`add_field`、`remove_field`（log_time/@version 消失）。**⚠️ 重大发现（如实写入讲义）**：9.5.3 的 ECS 兼容模式下，`%{COMMONAPACHELOG}` 输出的是 **`source.address` / `http.request.method` / `url.original` / `http.version` / `http.response.status_code` / `http.response.body.bytes`**，**不是**老教程里的 `clientip`/`verb`/`request`/`bytes`；作者自己的 `convert => { "bytes" => "integer" }` 因此成了**空操作**（不报错也不生效），已作为真实踩坑案例写进讲义与自检题。**链接**：5 条 Logstash 官方文档 curl 200（**需带浏览器 UA，默认 UA 返回 404**） | 直接交付；新增 `playground/06-logstash-pipeline/`（Filebeat → Logstash → ES） |
| 2026-09-06 | 阶段 3 课 8《可靠性与吞吐》首次交付 | 主 agent 内联（A 事实核查 + B 三件套零基础教学体验），**全课实测** | P0×0 / P1×0 / P2×0。**核心实测**：① **双管道独立运行**（`[nginx] queue.type=persisted in=3 out=3` + `[orders] queue.type=persisted in=3 out=3`）② `queue.max_bytes=1gb` 生效（`max_queue_size_in_bytes=1073741824`）③ **ES 停机退避重试日志**（`Attempted to send a bulk request but there are no living connections` + `will_retry_in_seconds: 64`）④ **决定性实验：持久化队列不丢**——停 ES + 写 2 条新日志 + **重启 Logstash** + 恢复 ES → `orderId=60010`/`60011` 均 `count:1`、订单索引 **3→5 一条不丢**。**真实踩坑**：只读挂载 `logstash.yml` → `Read-only file system`（Logstash 启动会写回设置），改用**环境变量**传全局参数（queue.type 等）。**诚实标注 3 处未实测**：内存队列 vs 持久化的丢数据对照、吞吐参数压测对比、队列 events 指标语义 | 直接交付；新增 `playground/07-reliability/`（双管道 + 持久化队列 + 双 Filebeat） |
| 2026-09-07 | 阶段 3 课 9《该在哪处理》首次交付（**阶段 3 收官**） | 主 agent 内联（A 事实核查 + B 三件套零基础教学体验），**全课实测** | P0×0 / P1×0 / P2×0（自查抓到 2 处 P0 + 1 处断链，均当批修复后清零）。**核心实测**：① **模块自动装载 `filebeat-9.5.3-nginx-access-pipeline`，30 个处理器**（description 原文 `Pipeline for parsing Nginx access logs. Requires the geoip and user_agent plugins.`；前 12 个处理器含 `set event.ingested` / `rename message→event.original` / grok / uri_parts / split / remove）② **决定性对照实验**：课 8 的 common 格式日志 → grok **失败**（`Provided Grok expressions do not match field value`，字段数 **19**，`@timestamp` 停留在入库时间 13:53:29）；追加 combined 格式（带 Referer + UA）→ **成功**（字段数 **39 / 38**，`source.ip` / `url.path` / `user_agent.os.name` = Mac OS X 全部拆出，`@timestamp` 变为**事件时间** `22:10:01+0800 → 14:10:01Z`）③ **CPU 归属（决策表的硬证据）**：`GET _nodes/stats/ingest` → `filebeat-9.5.3-nginx-access-pipeline: count=4, time_in_millis=98`，其中 **date 31ms + geoip 21ms + UA 5ms + grok 6ms**——4 条日志吃掉 ES 节点 98ms，约 25ms/条 ④ **三条路径同台实测**：路径 A（Beats processors，`fb-proc-*`，8 个顶层键、原文整条原样）／路径 B（Logstash，`ls-nginx-*`，拆出 `source.address` 等）／路径 C（ES Ingest，`filebeat-9.5.3`，38–39 字段）⑤ **`default_pipeline` 实证**：建模板设 `index.default_pipeline` 后**故意不带 `?pipeline=`** 写 bulk，脱敏照样生效 ⑥ **脱敏三件套落库验证**：密码 / 卡号原文 / IP 原文**全部消失**；`4111111111111111 → ************1111`；**audit-1 与 audit-3（同一 IP）的 `ip_hash` 完全相同**（`tAXpa3Yyu/...`），audit-2 不同（`93BxKeQg...`）——证明 fingerprint 稳定可统计 UV。**⚠️ 三个真实踩坑（已原样写入讲义）**：a) **ES Ingest 没有 `hash` 处理器**——`No processor type exists with name [hash]`（骨架里写的"hash 三件套"是错的，实为 fingerprint / gsub / remove）b) **也没有 `comment` 处理器**——pipeline 里不能写注释 c) grok `%{DATA:event.outcome}` 非贪婪，行尾匹配成空字符串 → 改 `%{NOTSPACE}` 修复。**另发现一个反直觉现象**（已写入误区表）：同为 Filebeat 9.5.3，模块路径写出的 `ecs.version` 是 **1.12.0**，普通 filestream 写出 **8.0.0**——`ecs.version` 是数据遵循的规范版本，**不是**组件版本。⏳ 成因未深究，只标现象。**自查修复**：相对链接少跳一级（`../4-存储可视化与落地/…` → `../../4-…`；`../../02-课程目录.md` → `../../../02-…`）；`../../README.md` 文件根本不存在 → 改指向 `01-学习路径总览.md` + `02-课程目录.md`。**链接校验**：6 条官方链接 curl 200（ECS current / ingest / simulate API / fingerprint processor / filebeat modules / configuring-ingest-node），8 条相对链接全通。**诚实标注**：Logstash 侧 `_node/stats` 实测 `orders in=0 filtered=2 out=2`（07 栈无新日志流入），故吞吐对比为定性结论，未做压测 | 当批修复后交付；新增 `playground/08-where-to-process/`（两个直连 ES 的 Filebeat + 脱敏 pipeline + bulk 样例）；**阶段 3 全部完成（27/36）** |
| 2026-09-07 | 阶段 4 课 10《日志的存储策略》首次交付（**阶段 4 开场**） | 主 agent 内联（A 事实核查 + B 三件套零基础教学体验），**全课实测** | P0×0 / P1×0 / P2×0。**核心实测**：① **四个 data stream 真实现状对照**——`filebeat-9.5.3`（模板 filebeat-9.5.3 / ILM **filebeat**）、`logs-nginx.access-default`（模板 logs / ILM **logs**）、**`fb-proc-2026.09.07`（ILM=无 → 永不滚动，课 9 自建模板的后果）**、`audit-demo-2026.09.07`（无）② **⚠️ 默认 ILM 策略只有 hot、没有 delete**：`filebeat` 与 `logs` 两个策略实测均是 `hot: rollover(max_age=30d, max_primary_shard_size=50gb)`——**日志永不自动删除，且小集群上 50gb 阈值可能一年都碰不到，等于没滚** ③ **三段式命名实测**：`POST /logs-nginx.access-default/_doc` → 落进自动创建的 `.ds-logs-nginx.access-default-2026.09.07-000001`，并**自动匹配 `logs` 模板（pattern `logs-*-*`）与 `logs` 策略** ④ 手动 rollover：generation **1→2**，`-000001`→`-000002`，旧索引保留 ⑤ **ILM 全自动闭环（本课高潮）**：自建策略 `hot(max_docs=2) + warm(0s) + delete(1m)` + 把 `poll_interval` 从默认 10m 缩到 10s → 写 3 条 → **40s 后自动滚动**（`-000001` phase=**warm**/action=complete，`-000002` phase=hot/check-rollover-ready）→ **再 80s 后 `-000001` HTTP 404 真被删除、`_count=0`** ⑥ `_tier_preference` 实测 = `data_hot` ⑦ 后备索引真实配置：**1 主分片 + 1 副本 + refresh 5s** ⑧ **模板组织两种风格实测**：Filebeat 自带模板 `composed_of=[]`（内联，priority 150，`total_fields.limit=12500`，12 条 dynamic_templates）vs 现代 `logs` 模板 `composed_of=['logs@mappings','logs@settings','logs@custom','ecs@mappings']`，其 `logs@settings` 含 **`ignore_dynamic_beyond_limit=true` + `ignore_malformed=true` + `codec=best_compression`** ⑨ **字段爆炸两模式决定性对照**：严格模式（limit=20）→ `document_parsing_exception: Limit of total fields [20] has been exceeded while adding new fields [22]`（**整条日志被拒**）；宽松模式（+`ignore_dynamic_beyond_limit`）→ 写入成功，但 mapping 只收 **11** 个字段、搜 `custom.field_25` **0 命中**、`_source` 里值仍在 → **"能存不能查"** ⑩ 动态模板 `strings_as_keyword` 实测：新字段类型 = **keyword**（不是 text，也无 `.keyword` 子字段）。**诚实标注**：单节点角色 `cdfhilmrstw` 同时含 c/f/h/w，冷热迁移只改标记、物理上没挪，真冷热需多节点——已写入讲义与误区表第 10 条；实验后已把 `poll_interval` 恢复为默认 **10m**（恢复后 `_cluster/settings` 确认 `defaults...poll_interval=10m`）。**链接校验**：8 条官方链接 curl 200（data-streams / ILM overview / ilm-rollover / data-tiers / index-templates / dynamic-templates / mapping-settings-limit / indices-rollover-index），11 条相对链接全通 | 直接交付；新增 `playground/09-storage-strategy/`（ILM 策略 + 索引模板 + 字段爆炸生成脚本与样例）；**阶段 4 开场（30/36）** |
| 2026-09-08 | 阶段 4 课 11《Kibana 从 Discover 到 Dashboard》首次交付 | 主 agent 内联（A 事实核查 + B 三件套零基础教学体验），**全课浏览器实测** | P0×0 / P1×0 / P2×0（自查抓到 1 处 P0，已当批修复后清零）。**核心实测**：① **浏览器实拍 Kibana 9.5.3 zh-CN 真实导航**——顶级分组 = Home / Analytics / Elasticsearch / Observability / Security / 启动平台 / 管理；Analytics 子项 = **Discover / Dashboards / Maps / Machine Learning / Visualize library**（菜单名中英混排：顶级中文、Analytics 内子项仍是英文）；管理 → Stack Management → Kibana → **数据视图** 路径走通 ② **⚠️ 撞坑实证：默认 15 min + 数据是 2 天前的 600 条 → 真实 0 命中**，作为"时间窗陷阱"素材写进讲义；切到 Last 2 days 后 600 条全部回归（**最有力的"时间窗不对"教学素材**）③ **删旧 data stream + 重新造 3h 内 600 条**做正式演示数据（脚本 `playground/10-kibana-visual/gen-app-logs.py`）④ **字段类型决定玩法实测对照**：`message`（`match_only_text`）terms 聚合报 `illegal_argument_exception: match_only_text fields do not support sorting and aggregations`（KQL 可搜、聚合不能）；`url.path`（`wildcard` 来自 ECS）聚合成功（**`/api/pay/submit` 96 条**）；`log.level`/`host.name`/`service.name`（keyword）、`http.response.status_code`/`duration_ms`（long）全部可聚合 ⑤ **10 条 KQL 全部实测数字**（Last 3 h）：空查询=579（后跑=516 因窗口漂移）/ `log.level: ERROR`=40 / `… and host.name: web-03`=23 / `status_code >= 500`=40 / `url.path: /api/pay*`=148 / `message: *timeout*`=11 / `… (ERROR or WARN) and not message: *connect error*`=105 / `log.level: DEBUG`=121 / `web-03 and duration_ms>1000`=10 / `event.duration>200000000 and status_code>=400`=40 ⑥ **Data View `app-demo` API 创建实测**：`POST /api/data_views/data_view`（**单数路径**，复数 `/api/data_views` 在 9.5 报 400 "exists but is not available with the current configuration"） ⑦ **4 个 metric Lens + 1 个 2×2 Dashboard API 拼装实测**：4 数字 = **39 / 39 / 63 / 516**（Last 3 h 漂移 1 是窗口边界）⑧ **⚠️ 三个真实踩坑（已写入讲义）**：a) **复杂 lnsXY 复刻 API 失败**（"无法加载页面"——9.5.3 schema 严）→ 改用 4 个简单 lnsMetric 拼盘 b) saved object 顶层 `references` 嵌进 `attributes` 报 400 "Additional properties are not allowed" c) **默认进 Discover 自动选内置"所有日志"只读 view**（id 形如 `discover-observability-solution-all-logs`），要手动下拉切回"应用演示日志"。**自查修复**：图片相对路径多跳 1 级——`../../../../playground` 应为 `../../../playground`（因 `elk/` 嵌在 `elasticsearch/` 下而非顶层，13 处全改）。**链接校验**：8 条官方链接 curl 200（Data Views / Discover / KQL / Lens / Dashboard / Dev Tools / API create / release notes），12 张图 + 13 条相对路径全通，4 个 Lens + 1 个 Dashboard 实际打开渲染验证数字 | 当批修复后交付；新增 `playground/10-kibana-visual/`（gen-app-logs.py + mk-dashboard.py + assets/ 12 张）；**阶段 4 进度 33/36** |
| 2026-09-10 | 阶段 4 课 12《告警与生产落地》首次交付（**全课收官**） | **双子 agent 并行评审**（A 事实核查 + B 零基础体验，独立性优于内联），**全课实测** | P0×9（A×4 + B×5，全部当批修复清零）+ P1×12（择要修复）。**A 视角 P0**：① 状态机把规则执行状态 `ok` 误当实例状态（官方 `kibana.alert.status` 只有 active/recovered/untracked）→ 已重写并区分两层状态；delayed 实测行为已标"官方枚举未记载" ② value=6 FIRING 后无 RECOVERED 的验收承诺 → 改为引用 23:10/23:11 真实一轮闭环 ③ 课际数字 45→39（课 11 口径）④ 脚本"3 行"实为 48 行。**B 视角 P0**：① 建 Connector/Rule 补上完整可复制 curl（含真实 payload）② 密码占位说明 ③ `$RULE`/`$INSTANCE` 取法补齐（`jq -r '.data[0].id'`）④ action group 先定义后使用 ⑤ CWD 基准统一 `11-alerting/`。**核心实测**：① **告警全链路闭环**：trial 解锁 `.webhook`（basic 403 报错原文入讲义）→ 连接器 → `.es-query` 规则 → **webhook 真实收到 FIRING(value=3, 23:10:31)→RECOVERED(23:11:31) 恰好一轮闭环** ② **防抖**：`alert_delay:{active:2}` → 实例 `status=delayed`、`consecutive_matches=1` ③ **静默**：规则级/实例级 mute 均 204；**instance id = `kibana.alert.instance.id`（= 动作组名 `query matched`），拿文档 `_id` 调静默 404** ④ **维护窗口决定性实验**：30 分钟窗口 → 注入 ERROR → 实例照常 `active` + `maintenance_window_ids` 非空、**webhook 零投递**；窗口 API 在 `/api/maintenance_window`（不在 alerting 下）⑤ **容量取证**：66 索引/66 primary shard、系统索引 60 个；**event log 两天 140,574→288,379 条（59MB），其中 140,516 条是 taskManager 心跳**；实测改 DLM 保留期 90d→7d ⑥ **权限五连击**：`log-reader`+`ops1` 查白名单 200 / 他人索引 403 / 写 403 / 安全接口 403 / 删索引 403 ⑦ **备份双路线**：快照 33 索引 SUCCESS（64MB）+ SLM 策略（`policy` 为 metadata 保留字段名实测 400）+ **Saved Objects 误删复生演练**（导出 8 对象→删 Dashboard→导入 successCount=8）；系统索引只能按 feature state 恢复；**导入的 alert 默认 enabled=false**（实测发现 + 官方 "Rules are disabled on export" 佐证）⑧ **6 张实测截图**落 `playground/11-alerting/assets/`。**新增 compose 配置**：Kibana `XPACK_ENCRYPTEDSAVEDOBJECTS_ENCRYPTIONKEY`、ES `path.repo`+快照卷（均带注释写入 `07-reliability/docker-compose.yml`） |
| 2026-09-10 | 结课综合实战项目（Phase 3）《日志平台生产化》首次交付 | 主 agent 内联 + **`course-reviewer` 专项维度 A1-A6**（知识点覆盖 / 决策权衡 / 反例对照 / 验收可操作 / 全链路图 / 可重建性）+ pedagogy 5 维 + learner 6 维（`course-reviewer` 子 agent 未创建，独立性受限） | P0×2 / P1×1（均当批修复清零）。**产出（落 `projects/日志平台生产化/`）**：README（201 行，**36 知识点逐个映射到具体落点** + 📚 官方文档 13 条 curl 200）/ 设计决策（161 行，**4 个决策点**：解析放哪层 · `host.name` 保留还是覆盖 · ILM 绑定方式 · 告警往哪落；每点「候选对比表 → 选择理由 → 付出的代价 → 何时改选」）/ 反例对照（195 行，**8 条"能跑但很糟"**的写法 + 为什么糟 + 判据）/ 验收清单（318 行，**18 项**：A 起得来 3 / B 采集解析 6 / C 存储 3 / D 消费 2 / E 可重建 2，另 5 项进阶自测）/ `assets/data-flow-pipeline.svg`（**复杂图固定 SVG**：1240×860，5 泳道分区 + 11 编号步骤 + 5 格对象形态变化，XML 校验通过）+ **`实现/` 子目录可运行工程**（compose 五服务含 setup 容器 · `bootstrap.py` ~330 行幂等重放 · `gen-logs.py` · Logstash 管道 · Filebeat 配置）。**端到端实测（1000 条基线，全绿）**：① 采集 600 应用（427 JSON + 173 文本）+ 400 nginx = **1000 条，与文件行数一致** ② **解析失败标签 0**（`_json_parse_failed` / `_mutate_error` / `_grokparsefailure` 全为 0；期间一度误判 130 条失败，根因是**旧数据残留 + `tags` 聚合口径错**——字段名就叫 `tags` 不是 `tags.keyword`）③ JSON 分支 `duration_ms=63`（int）、文本分支 `logger`/`thread`、nginx 分支 `source.address`/`http.request.method`/`http.response.body.bytes` 全部正确 ④ `host.name` 三机分布 **web-01 374 / web-02 339 / web-03 287，无容器 ID 污染** ⑤ rollover **gen1 1000 → gen2 90**，且**两代 backing index 映射完全一致**（`log.level=keyword`）⑥ 告警规则 `enabled`，`.index` 连接器落盘 `shopapi-alerts`，mustache 渲染出真实 `hit_count`/`hit_host`/`hit_level`/`hit_path` ⑦ 快照 `snap-final` **SUCCESS 32/32 shards** ⑧ Saved Objects 误删复生演练（导出 3 对象 → 删规则 → 导入 `successCount=3` → **规则回来但 `enabled=false`**，与课 12 结论一致）。**⚠️ 实战真实暴露 4 个生产级坑（本项目最有价值的部分，全部写成"真权衡"素材入文档）**：① **`add_field` 是追加不是覆盖**——ECS 的 `[host][name]` 已由 Filebeat 写入容器 ID，`add_field` 变数组 `["09d21480270c","web-01"]` → 聚合出假主机；解法：先 `remove_field [host][name]` 再写 ② **multiline pattern 只认日期开头** → JSON 行不匹配被当续行吞掉；改 pattern `'^([0-9]{4}-[0-9]{2}-[0-9]{2}|\{)'` 同时认 `{` ③ **本项目最凶险**——自定义索引模板 priority=200 **顶掉**内置 `logs` 模板（priority=100），未声明 `composed_of` → ECS 映射全失效、`log.level` 退化为动态映射 `text` → `term` 精确查询**永 0 命中** → **告警永远 0 条、静默不触发**（用探针 data stream 对照证明：term ERROR 全时段 118 / 近 5 分钟 50 / **交集 0**）④ **Filebeat registry 未落命名卷** → 容器 `recreate` 后整文件重读、**1000 条变 2420 条**（数据没丢、查询不报错，只是每个数字翻倍）。**另 2 个环境坑**：`kibana_system` 密码只由官方 `setup` 容器设置（否则 Kibana 容器 Up 但 `/api/status=unavailable`、所有 API 400）；中文目录名须用 `.env` 指定 `COMPOSE_PROJECT_NAME`。**学习档案数字**：另记录两条接口口径坑——告警 Saved Object 类型是 **`alert`**（隐藏类型，`_find` 查不到但可导出；`alerting-rule` 不可导出）；规则 PUT 时 `rule_type_id` 与 `consumer` 是**创建后不可变字段**，必须同时摘掉（一次只报一个错）。**评审修复**：P0×2——README 实测数字未标注随机性 → 加"量级与失败标签为 0 不会变、绝对数字会变"说明；验收清单 `hit_count` 硬编码 40 与基线 37 不符 → 改 37 并加说明。P1×1——README 补 📚 官方文档小节（13 条链接逐条 curl 200） | 当批修复后交付；新增 `projects/日志平台生产化/`（四份主文档 + SVG + `实现/` 可运行工程）；**Phase 3 结课实战完成**，流程上仅剩 Phase 4 手册汇总与 Phase 5 三态三件套 |
| 2026-09-13 | **Phase 5 收尾三件套** `08-实战经验.md` / `09-排障速查手册.md` / `10-场景解法库.md` 首次交付（**课程收官**） | 主 agent 内联 + **`course-reviewer` 专项维度 B1-B8**（经验与手册）+ **C1-C7**（场景解法库）+ pedagogy 5 维 + learner 6 维（`course-reviewer` 子 agent 与 `agents/course-reviewer/agent.md` **均不存在**，B1-B8 / C1-C7 口径取自仓库内权威实测记录 `surrealdb/收尾产物评审记录.md`，独立性受限，已在评审记录中如实标注） | P0×0 / **P1×4** / P2×2（均当批修复清零）。**产出（三份一次生成，素材同源）**：① [`08-实战经验.md`](08-实战经验.md) = 383 行：7 行「不该用 ELK」表 + 12 条反模式 + 9 行「被误判为故障的正常现象」表 + **10 条故障模式**（五段式 **10/10**，【实测】11 处 /【官方】3 处）+ **33 项落地 Checklist**（A 采集 6 / B 处理 6 / C 存储 7 / D 消费告警 7 / E 安全备份 7）+ **2 个联网核实真实事故** + 5 条淘汰候选 + 3 项 ⏳ ② [`09-排障速查手册.md`](09-排障速查手册.md) = 337 行：**症状倒查索引 16 行（🔴4 / 🟡10 / ⚪2）** + 「动手前 10 秒：假故障」表 9 行 + **10 条症状**（五硬特征 **10/10**；症状 10 含 A/B/C/D 子变体）+ 通用止血 6 行 + **官方依据 10 行表**（11 条官方原文，全部 curl 200）③ [`10-场景解法库.md`](10-场景解法库.md) = 497 行：**8 场景 × 43 解法**（6/6/4/5/5/6/6/5，每行含代价与边界）+ **16 折叠块**（配对 16/16）+ **34 条知识点挂钩**（回指 10 门课）+ 25 处跨文件回指 + 3 条淘汰场景 + 4 项 ⏳。**素材同源链**：结课项目 10.6 的 **4 个生产级坑**（`host.name` 数组 / multiline 吞 JSON / `composed_of` 缺失致告警静默 / registry 不落卷 1000→2420）→ `08` 故障模式 1/2/5/6 → `09` 症状 1/2/5/6 → `10` 场景 1/2/3/7 的坑。**B1 编号三向双向映射脚本核验 1-10 全覆盖**；**⏳ 项跨文件检索验证为零进入 `09` 确定区**。**两个真实事故（联网核实）**：**Meow 攻击 2020-07**（5 天超 3,000 个数据库被清、7 月底超 4,000；ES 1,395 / MongoDB 383 / Redis 54；留 `-meow` 后缀索引；一个 30TB / 约 120 亿条集群被删大半；**含 Elastic 官方回应"受影响集群都没开任何安全特性"**）/ **GitLab 2017-01-31**（约 300GB 误删；**五套备份机制全失效**——`pg_dump` 9.2/9.6 版本不匹配静默失败数月 + DMARC 致告警邮件被拒 + LVM 快照是无关项目临时打的 + Azure 快照只覆盖 NFS + WAL 归档从未配置；靠 6 小时前的临时快照恢复，耗时 18 小时）。**评审当批修复 P1×4**：① `final-课程手册.md` **缺磁盘水位线口径**（85/90/95%）而三份都在引用它 → 课 10 补误区第 11 条（flood stage 阻塞写入 + 降到 high 自动解除）+ 命令速查卡补 3 行；② `09` 的 event log 口径写"几倍"与手册/课 12 的 **600 倍 / 288,379 条 / 59 MB** 不同源 → `08`/`09` 统一；③ `09` 头部**未显式声明「使用态」**三态定位 → 已补；④ `final-课程手册.md` 有两处（共 **6 行**）仍标 `⏳ Phase 5 待生成`（「怎么用这本手册」表 3 行 + 第十一章表 3 行）且接力提示词仍指向已完成的 Phase 5 → 全部改为带链接的 ✅ + 交付日期 + 内容规模，第十一章改写为**结课后三条深入方向**（改后残留 ⏳ 仅 1 处，属 `07-知识点对齐.md` 的 Phase 6 可选标注，正确保留）。**P2×2**：① `08` 声明【公认】等级但正文 0 处标注 → 加「标注密度说明」（不硬凑标注充数）；② 手册第十一章把 `08` 的 Checklist 写成 **30 项**（实际 **33 项** = A 6 / B 6 / C 7 / D 7 / E 7）→ 按脚本清点改正。**事实核查闸门**：候选官方链接 **54 条**（首轮 33 + 本轮 21）→ **50 条 200 / 4 条 404**；`forcemerge.html` → `indices-forcemerge.html`、`snapshots-searchable.html` → `searchable-snapshots.html`、`rest-apis/roll-over-index` 弃用改 `indices-rollover-index.html`；**案例 1 三个来源之一回检变 403（WAF）→ 替换为 BleepingComputer / The Register / TechTarget（均 200）**，正文数字未变（三来源交叉一致）；磁盘水位线、searchable snapshots 许可（**需 Enterprise**）、`.alerts-*` 别名、`_enable` 路径（`POST /api/alerting/rule/{id}/_enable` → 204）均走官方文档核实并标（核查于 2026-09）。**机械自检**：三份本地相对链接 **17 条去重 / 命中 17 / 0 断链**；`<details>` 16/16；12 个关键数字四份产物同源比对一致；**3 处假报警先判真伪后未盲改文档**（`\| **修复** \|` 正则过严、症状 3 用「止血 1/2/3」变体、emoji 字符类不匹配代理对）。评审记录见 [`收尾产物评审记录.md`](收尾产物评审记录.md) | 当批修复后交付；**Phase 5 完成 = 课程完整闭环**（4 阶段 / 12 课 / 36 知识点 + 结课实战 + 汇总手册 + 三态三件套），评审清单 16/16 全勾；三份生产级坑素材已固化为课程内唯一素材源 |
| 2026-09-13 | 课程手册汇总（Phase 4）`final-课程手册.md` 首次交付 | 主 agent 内联 + **`course-reviewer` 专项维度 A1-A6**（知识点覆盖 / 决策权衡 / 反例对照 / 验收可操作 / 全链路图 / 可重建性）+ pedagogy 5 维 + learner 6 维（`course-reviewer` 子 agent 与 `agents/course-reviewer/agent.md` **均不存在**，维度口径沿用 Phase 3 已验证版本，独立性受限） | P0×0 / **P1×2** / P2×2（均当批修复清零）。**产出**：`elasticsearch/elk/final-课程手册.md`（约 960 行，落**主题目录顶层**），11 章——「怎么用这本手册」（5 种阅读路径 + 数字随机性免责）→ 学习目标四项 → 故事主线 + 路径总览 SVG → 阶段总览 + 阶段依赖 Mermaid → **四～七 逐课手册（12 课全覆盖：一图收束 Mermaid + 三句话记住 + 实测数字 + 常见误区 + 📋 命令速查卡 + 📚 官方文档）** → 状态与方法（重叠处理原则 + **跨课方法论 7 条**）→ 决策清单（**5 张表**：该不该上 ELK / 采集端选谁 / 解析放哪层 / 存储策略 / 告警落点与不扰民）→ 综合实战项目收录（R1-R6 + 36 知识点覆盖地图 + 4 决策点 + 8 条反例 + 1000 条基线实测 + **4 个生产级坑对照表**）→ 下一步 + 🚀 接力提示词 + 🧭 课程导航。**A5 图**：11 个 Mermaid 块 + 1 张阶段路径 SVG；**课 11 如实标注**其「体系收束」为纯文字版（主线是 UI 操作路径而非机制链路）故不配独立收束图。**A6 可重建性 100% 通过**：相对链接逐条实查全部命中（4 入口文档 / 4 阶段 overview / 12 课原文 / 4 张 `stage-N-path.svg` + 1 张 `learning-path-overview.svg` / **11 个 playground 目录** / 4 份项目主文档 / 5 个项目内工程文件）；**58 条官方链接并发 curl（带浏览器 UA）实测 100% HTTP 200、0 断链**（此前 6 条抽查已扩展为全量）。**数字一致性抽查 12 组全部与源文件同源**（`2,766,622` / `138,249` / `1,520,807` 条 / `12.9` 秒 / `99.6` 亿 / `1 小时 6 分` / `08:30:01Z` = 北京 `16:30:01` / `30` 个处理器 / `10→13` / offset `303`·`588` / `9 条 vs 1 条` / `3→5` / `17:45:01+0800`→`09:45:01Z` / KQL `40`·`23`·`148`·`11` / Lens `39`·`39`·`63`·`516` / `1000` = `600` + `400` / `gen1 1000 → gen2 90` / 快照 `32/32` / `1000→2420`），抽查方法是**回读 `stages/**/lessons/*.md` 与 `projects/日志平台生产化/*.md` 原文逐条比对**（非凭记忆）。**评审当批修复**：**P1-1** 手册把 Phase 5 的 `08-实战经验.md` / `09-排障速查手册.md` / `10-场景解法库.md` 与可选 `07-知识点对齐.md` 当作"去哪"的目的地引用，却**未标注它们尚未生成**（读者会去找不存在的文件）→ 在「怎么用这本手册」表与「十一、下一步」表两处加 ⏳ 标注，并补「⏳ = Phase 5 产物，截至本手册交付时尚未生成」说明行。**P1-2** `02-课程目录.md` 头部仍写 `24 / 36`、阶段 4 仍写 `⬜（0/9）`、课 9 三个知识点仍 ⬜、📕 汇总手册仍写"未开始" —— 与手册 / 档案 / 路径总览**四处口径打架**（上一批回写漏项）→ 一并改正为 `36 / 36 ✅ 全部完成`、阶段 4 `✅（9/9 · 2026-09-10 全部完成）`、课 9 补日期与配套 `playground/08-where-to-process/` 链接、📕 汇总手册改为完整入口 + 9 行章节表，并把接力提示词改为**只指向 Phase 5**。**P2-1** 手册 10.5 表把 `427 JSON + 173 文本` 写成确定值（源 README 标"单次实测"）→ 改为「JSON 与文本约 7:3，单次实测 427 + 173」。**P2-2** 手册末尾缺 skill 要求的接力提示词区块 → 补 🚀 接力提示词（指向 Phase 5，含"三份须与本手册同源"的口径约束）+ 🧭 课程导航。**配套回写**：`00-评审清单.md`（勾选 Phase 4 条目 + 评审记录行）、`00-学习档案.md`（本行 + 进度注记）、`02-课程目录.md`、`AGENTS.md` | 全部当批修复；生产级坑 4 条与 1000 条基线数字已固化为 Phase 5 三份的**唯一素材源**，避免双源漂移 |

## 大纲调整记录

> 大纲是活文档，每次调整在此追加一行（与 `01-学习路径总览.md` / 阶段 `overview.md` 同步更新；结构性调整须重新过评审）。

| 日期 | 调整内容 | 原因 |
|------|----------|------|
| 2026-09-06 | 初始大纲落盘（4 阶段 / 12 课 / 36 知识点），骨架由 4 个子 agent 并行生成 + 主 agent 补齐课 6 | 用户要求「在 elasticsearch 下新增子教程 elk」；画像=三件套零基础、四项全要、完整课程制 |

## 事实核查记录

| 条目 | 结论 | 置信度 | 核查时间 |
|------|------|--------|----------|
| Kibana event log 保留机制 | `.kibana-event-log-ds` 自 **8.10 起由 ILM 改用 Data stream lifecycle（DLM）**管理，**默认保留 90 天**（Kibana PR #163210 + 现行官方文档）；本机 9.5.3 实测 `GET _data_stream/.kibana-event-log-ds/_lifecycle` → `data_retention: 90d`，可用 PUT 改小（实测改 7d 生效） | 高 | 2026-09-10（联网核查 Kibana release notes/commit + 本机实测） |
| Loki 索引模型 | 官方原文："does not index the contents of the logs, but only indexes metadata about your logs as a set of labels"——只索引标签、正文压缩块进对象存储 | 高（官方文档原句） | 2026-09-10（[Loki overview](https://grafana.com/docs/loki/latest/get-started/overview/)） |
| 各方案"便宜几倍"的具体倍数 | 社区口径 3~20 倍不等，差异巨大（受保留天数/压缩比/副本影响）→ 讲义**不采信任何单一倍数**，只采信架构性差异；云厂商刊例价单列并标"以官网为准" | ⏳ 低（社区估算） | 2026-09-10 |
| 腾讯云 CLS / 阿里云 SLS 刊例 | CLS：写入 ¥0.18/GB、索引 ¥0.35/GB、标准存储 ¥0.0115/GB/日（腾讯云开发者社区对比文，与 SLS 同价位） | 中（价格随官方调整，讲义已标"以官网为准"） | 2026-09-10 |
| 思科收购 Splunk 时点 | 2024 年宣布、2025 年完成收购 | 中 | 2026-09-10 |
| 导入规则默认禁用 | 官方原文 "Rules are disabled on export. You are prompted to re-enable the rule on successful import." + 本机实测一致 | 高 | 2026-09-10 |
| ECS 是什么 / 是否强制 | ECS 是**开源规范**（open source specification），定义字段命名与数据类型；官方明示为 **permissive（宽松）**，不强制；ECS 大版本与 Stack 大版本对齐，**ECS 9.5.0 已随 9.5 发布** | 高 | 2026-09-07（联网核查 [ECS Reference](https://www.elastic.co/guide/en/ecs/current/index.html) + [ECS release notes](https://www.elastic.co/docs/release-notes/ecs)） |
| ES Ingest 是否有 `hash` / `comment` 处理器 | **都没有**。实测 `No processor type exists with name [hash]`、`property isn't a map... [comment]`；脱敏应使用 `fingerprint` + `gsub` + `remove` | 高（本机 9.5.3 实测） | 2026-09-07 |
| `ecs.version` 的含义 | 是**数据遵循的 ECS 规范版本**，不是组件版本。实测同为 Filebeat 9.5.3：模块路径写出 `1.12.0`，普通 filestream 写出 `8.0.0` | 现象高（实测）／⏳ **成因置信度：低**（未深究，讲义只标现象不解释成因） | 2026-09-07 |
| Filebeat 模块自带 pipeline 名称与规模 | `filebeat-9.5.3-nginx-access-pipeline`，**30 个处理器**；`output.elasticsearch` 时启动自动装载，`output.logstash` 时**不装载** | 高（本机实测） | 2026-09-07 |
| `indices.lifecycle.poll_interval` 默认值 | **10m（10 分钟）**。ILM 按此间隔轮询检查策略条件，配完策略不会立刻生效 | 高（官方文档 ilm-settings + 本机 `_cluster/settings` 默认值复核） | 2026-09-07 |
| Filebeat / logs 默认 ILM 策略内容 | 两者实测**完全相同**：只有 `hot` 阶段，`rollover(max_age=30d, max_primary_shard_size=50gb)`，**没有 warm / cold / delete** | 高（本机 9.5.3 实测） | 2026-09-07 |
| data stream 命名约定 | 现代三段式 `<type>-<dataset>-<namespace>`（如 `logs-nginx.access-default`）；但**独立 Filebeat 默认名是扁平的 `filebeat-9.5.3`**，不带 dataset/namespace | 高（本机实测两种都存在） | 2026-09-07 |
| `ignore_dynamic_beyond_limit` 语义 | 超限字段**不建索引**但**保留在 `_source`**；实测搜该字段 0 命中、`_source` 仍有值。与"不限制"是两回事 | 高（本机实测） | 2026-09-07 |
| 单节点能否演示冷热迁移 | 不能。本机节点角色 `cdfhilmrstw` 同时含 `data_hot/warm/cold/frozen`，迁移只改 `_tier_preference` 标记 | 高（本机 `_cat/nodes` 实测） | 2026-09-07 |
| 当前 Elastic Stack 最新稳定版 | **9.5.3**（2026-09-03 发布） | ✅ 高 | 2026-09-06 |
| Logstash 起源（Jordan Sissel，2009-08-02 创建；2013 被 Elasticsearch 公司收购，ELK Stack 成为官方用语） | 已初步检索（百度百科 + 中文社区同源），**课 1 正文撰写时须再核官方/英文来源** | 中 | 2026-09-06 |
| Kibana 起源（Rashid Khan，2013） | 同上，待课 1 正文时复核 | 中 | 2026-09-06 |
| Beats 发布与 ELK → Elastic Stack 改名（Beats 2015 加入后改名） | 同上，待课 1 正文时复核 | 中 | 2026-09-06 |
| 本机环境（macOS arm64 / Docker 29.4.1 daemon 未运行 / 18GB / Java 17 / Node 22.15.1） | **本机实测** | 高 | 2026-09-06 |
| **ELK 起源三节点**：Logstash 2009-08-02 由 **Jordan Sissel** 创建（当时为 DreamHost 运维工程师，也是打包工具 fpm 作者）；**Kibana 2013** 由 **Rashid Khan** 创建；**Beats 2015** 加入后 ELK 逐渐改称 Elastic Stack | 多来源交叉（百度百科 + Wikiwand + Grokipedia + ELKstack 中文指南 + Earthly Blog 英文） | 中高；**改名节点：中**（各来源表述不一，正文已用"2015 年 Beats 加入后逐渐改称"的稳妥表述并标 ⏳） | 2026-09-06 |
| Loki「只索引标签、不索引全文」 | 官方 GitHub README 原文："does not do full text indexing on logs…only indexing metadata" | 高 | 2026-09-06 |
| 8 条官方文档链接全部可达（`elastic.co/docs` / `.../beats/filebeat` / `.../logstash` / `.../kibana` / `.../explore-analyze/visualize` / `.../ecs` / `.../release-notes` / `.../downloads/past-releases`） | curl `-L` 实测全部 HTTP 200 | 高 | 2026-09-06 |
| **本机日志实测量级**：近 1h 系统日志 **2,766,622 条**、error 级别 **138,249 条**；`grep -c "bluetoothd"` 命中 **1,520,807 条**、耗时 **12.9 秒**（13.05s user / 0.70s system / 12.923 total）；`~/Library/Logs` 下 **161 个 .log**（15M）、`/var/log` 31M；HealthMonitor 目录 7 个轮转文件 | 本机实测 | 高 | 2026-09-06 |
| **⚠️ 环境坑：zsh 的 `log` 是 shell 内置命令** —— `log show --last 1h` 报 `(eval):log:1: too many arguments`（命令被内置 `log` 拦截）；正确写法是 `/usr/bin/log show ...`，实测可正常返回数据 | 本机实测 | 高 | 2026-09-06 |
| Docker Desktop 启动后环境：ServerVersion **29.4.1** / OS=linux / Arch=**aarch64** / **MemTotal=8216915968（8.2GB）** | 本机实测（`docker info`） | 高 | 2026-09-06 |
| **ES 9.5.3 官方镜像提供 linux/arm64**（manifest 同时含 amd64 与 arm64 两个 platform），Apple 芯片可直接跑 | 本机实测（`docker manifest inspect`） | 高 | 2026-09-06 |
| ES 9.5.3 容器真实版本响应：`number=9.5.3` / `build_type=docker` / `build_date=2026-09-01T16:11:59` / `lucene_version=10.5.1` / `minimum_wire_compatibility_version=8.19.0` | 本机实测（容器启动后 `curl`） | 高 | 2026-09-06 |
| **9.x 不带凭据访问 ES → HTTP 401**，报错原文 `missing authentication credentials for REST request [/]`（type=security_exception，header 含 `WWW-Authenticate: Basic realm="security"`）；带 `elastic` 凭据 → 200 | 本机实测 | 高 | 2026-09-06 |
| **ES 容器 healthcheck 约 40 秒转 healthy**（实测 10s/20s/30s 均 starting，40s healthy） | 本机实测 | 高 | 2026-09-06 |
| **`depends_on: condition: service_healthy` 实证**：`up -d kibana` 输出依次为 `elk-es Waiting` → `elk-es Healthy` → `elk-kibana Starting` | 本机实测 | 高 | 2026-09-06 |
| **Kibana 就绪三态**：`/api/status` 轮询 10s=**000**（未监听）→ 20s=**503**（已启动未就绪）→ 30s=**200**（可用） | 本机实测 | 高 | 2026-09-06 |
| **容器网络按服务名互访实证**：`docker compose exec kibana getent hosts elasticsearch` → `172.20.0.2  elasticsearch` | 本机实测 | 高 | 2026-09-06 |
| **`down` vs `down -v` 对照**（本课核心证据）：`down` → 卷 `01-minimal-stack_es-data` 保留、文档 `found:true`；`down -v` → 输出含 `Volume ... Removed`、文档 **404 `no such index [elk-lesson02-test]`**、且 **`kibana_system` 密码连带失效（HTTP 401）**，ES 日志原文 `Authentication of [kibana_system] was terminated by realm [reserved] - failed to authenticate user [kibana_system]` | 本机实测 | 高 | 2026-09-06 |
| Kibana API 认证对照：`GET /api/spaces/space` 不带凭据 **401**，带 `elastic` 凭据 **200**（返回 default space）；`GET /api/status` 无需凭据即 200 | 本机实测 | 高 | 2026-09-06 |
| **反证（未复现，勿当实测结论引用）**：试图用「容器内存上限 1GB + JVM 堆 900MB」复现 OOM，**未能触发** —— ES 正常启动且集群 GREEN，`docker inspect` 显示 `OOMKilled=false`、退出码 143（被超时命令 SIGTERM，非 OOM）。更极端参数（堆 1500MB）**未完成实测** | 本机实测（负结果） | 高（结论可信：未复现） | 2026-09-06 |
| 新增 2 条官方文档链接可达：`/docs/deploy-manage/deploy/self-managed/install-elasticsearch-with-docker`、`.../install-kibana-with-docker` 均 HTTP 200 | curl 实测 | 高 | 2026-09-06 |
| **踩坑：Kibana 的 `/api/security/user` 端点在 9.5.3 返回 404**，不能用作登录验证；改用 `/api/spaces/space`（401/200 对照清晰） | 本机实测 | 高 | 2026-09-06 |
| **⚠️ 本课最大踩坑：Filebeat 自定义索引名导致链路起不来** —— 配置 `index: "demo-app-%{+yyyy.MM.dd}"` + `setup.template.name: "demo-app"` + `setup.ilm.enabled: false` 后，Filebeat 反复报 `could not put data stream: 400 Bad Request: illegal_argument_exception: no matching index template found for data stream [demo-app]`，`_cat/indices` 空。**原因**：9.5.3 下自定义索引名会触发 data stream 创建，模板对不上即失败。**解法**：最小链路阶段删掉自定义索引名与模板配置，用 Filebeat 默认行为（自带模板 + 默认 data stream） | 本机实测（真实翻车 + 修复） | 高 | 2026-09-06 |
| Filebeat 9.5.3 默认写入 **data stream `filebeat-9.5.3`**，后备索引命名 `.ds-filebeat-9.5.3-2026.09.06-000001`；状态 **yellow**（默认 1 副本 + 单节点，数据一条不少） | 本机实测 | 高 | 2026-09-06 |
| **`@timestamp` 默认是采集时间而非事件时间**（决定性证据）：日志内容写 `2026-09-06 14:20:01`，而 `@timestamp` 为 `2026-09-06T07:26:22.494Z`（= 北京时间 15:26），**相差约 1 小时 6 分**。修正需 Logstash `date` 过滤器或 Ingest Pipeline date processor（阶段 3 课 7） | 本机实测 | 高 | 2026-09-06 |
| 一条 Filebeat 文档的完整字段：`@timestamp` / `input.type=filestream` / `host.name`（本例为容器 ID `d18f7139d627`，生产上应为业务机名）/ `agent.type=filebeat`、`agent.version=9.5.3` / `ecs.version=8.0.0` / `log.file.path` + `log.offset` / `message` | 本机实测 | 高 | 2026-09-06 |
| **Data View 创建实测**：`POST /api/data_views/data_view` 带 `kbn-xsrf: true` 与 `{"data_view":{"title":"filebeat-9.5.3","timeFieldName":"@timestamp"}}` → HTTP 200，返回 id `38ce53e5-...`，`@timestamp` 识别为 `date`；**字段列表含 activemq.* / zoom.* 等上千个预置字段**（来自模板） | 本机实测 | 高 | 2026-09-06 |
| 9.5 Data View 创建 endpoint 是**单数** `/api/data_views/data_view`；复数 `/api/data_views` 在 9.5 报 400 "exists but is not available with the current configuration" | 本机实测（9.5.3 容器内复现） | 高 | 2026-09-08 |
| 9.5.3 Discover 默认进的是**内置只读 data view** `discover-observability-solution-all-logs`（跨 `logs-*` / `*logs*` 模式），**不在** `GET /api/data_views` 列表中（内部 saved object）；用户自建 data view 不会自动被选为默认，需顶部下拉切 | 本机实测 | 高 | 2026-09-08 |
| `message` 字段是 `match_only_text` 类型，**不可聚合不可排序**：terms 聚合报 `illegal_argument_exception: match_only_text fields do not support sorting and aggregations`；KQL 可搜可前缀/前后通配 | 本机实测（`GET _field_caps` + terms aggregation 复现） | 高 | 2026-09-08 |
| `url.path` 字段是 `wildcard` 类型（来自 ECS mappings），**可以**做 terms 聚合；实测 `/api/pay/submit` 出现 96 次 | 本机实测 | 高 | 2026-09-08 |
| 9.5.3 Kibana 9.5.3 zh-CN 左侧主导航实测：Home / Analytics / Elasticsearch / Observability / Security / 启动平台 / 管理；Analytics 子项 = Discover / Dashboards / Maps / Machine Learning / Visualize library（顶级中文 + Analytics 内子项英文）；管理 → Stack Management → Kibana → **数据视图** = `/app/management/kibana/dataViews` | 浏览器实测（playwright-core + 本机 Chrome 截图） | 高 | 2026-09-08 |
| 9.5.3 新建可视化弹窗有 3 个 tile：**可视化**（data-test-subj=`visType-lens`，描述"使用点击式编辑器生成图表、指标和表格"）/ Maps / Vega；**"旧版" tab** 下还有 Aggregation-based / TSVB 等老式图 | 浏览器实测 | 高 | 2026-09-08 |
| saved object 顶层 `references` 必须与 `attributes` **平级**，嵌进 `attributes` 会 400 "Additional properties are not allowed" | 本机实测 | 高 | 2026-09-08 |
| **复杂 lnsXY Lens（带 X/Y 轴）API 复刻在 9.5.3 容易 render 失败**（报"无法加载页面"，疑似 schema 校验严）；简单 lnsMetric API OK；**生产推荐 UI 拖 + 顶右"保存"** | 本机实测（先失败后回退到 metric） | 高 | 2026-09-08 |
| **字段类型冲突实测（三层错误链）**：往 date 字段写 `"不是时间"` → HTTP 400 `document_parsing_exception: [1:15] failed to parse field [@timestamp] of type [date] ...` → `caused_by illegal_argument_exception: failed to parse date field [不是时间] with format [strict_date_optional_time||epoch_millis]` → `caused_by date_time_parse_exception: Failed to parse with all enclosed parsers`；响应末尾含 `failure_store: "not_enabled"`（9.x 失败存储机制，本课未展开） | 本机实测 | 高 | 2026-09-06 |
| Filebeat 自动加载的索引模板 `filebeat-9.5.3`：`index_patterns=["filebeat-9.5.3"]`、`index.lifecycle.name="filebeat"`（默认挂 ILM）、`index.mapping.total_fields.limit="12500"`（字段爆炸防线）、`index.refresh_interval="5s"`、`index.max_docvalue_fields_search="200"` | 本机实测 | 高 | 2026-09-06 |
| 新课 3 引用的 2 条官方文档链接可达：`/docs/reference/beats/filebeat`、`/docs/reference/beats/filebeat/exported-fields` 均 HTTP 200 | curl 实测 | 高 | 2026-09-06 |
| **Filebeat 9.5.3 用 fingerprint 而非 inode 标识文件**（registry key 实测：`filestream::demo-app::fingerprint::<sha>`，value 含 `fingerprint_len` 与 `source`）。这是 `filestream` 取代老 `log` input 的核心原因：inode 会被 OS 复用导致认错文件 | 本机实测（registry 原文） | 高 | 2026-09-06 |
| **registry 实测**：路径 `/usr/share/filebeat/data/registry/filebeat/{log.json,meta.json}`，memlog 格式（每行 `{"op":"set","id":N}` + 数据行）；实测 cursor offset **303**（demo-app.log 3 行）与 **588**（stack-error.log 9 行），均为文件末尾 | 本机实测 | 高 | 2026-09-06 |
| **断点续传实测**：`docker compose restart filebeat` 前后文档数均为 **12**，零重复 | 本机实测 | 高 | 2026-09-06 |
| **多行合并决定性对照**（同结构 9 行堆栈，同一 Filebeat 内）：未配 `parsers.multiline` → **9 条**文档（`stack-error.log`）；配了 `multiline{pattern:'^\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}', negate:true, match:after}` → **1 条**文档且 message 完整含 9 行（`stack-multiline.log`） | 本机实测 | 高 | 2026-09-06 |
| **文件轮转实测**：`mv stack-error.log stack-error.log.1` 后新建同名文件并写入 1 行 → 该文件文档数 **9 → 10**，新文件被当作全新文件采集（fingerprint 不同） | 本机实测 | 高 | 2026-09-06 |
| 课 4 引用的 3 条官方文档链接可达：`filebeat-input-filestream`、`how-filebeat-works`、`multiline-examples` 均 HTTP 200 | curl 实测 | 高 | 2026-09-06 |
| ⏳ **未实测（诚实标注）**：① 删除 registry 后重启导致重复读取 ② `close_*` 系列关闭策略 ③ 三类漏数据场景——均只有原理与官方依据，无本机数据 | — | — | 2026-09-06 |
| **modules 前提**：不配 `filebeat.config.modules.path` 时，`filebeat modules list` 报 `Error in modules manager: modules management requires 'filebeat.config.modules.path' setting` | 本机实测 | 高 | 2026-09-06 |
| **nginx 模块自动装载 ingest pipeline**：启用后 ES 里出现 `filebeat-9.5.3-nginx-access-pipeline`，内含 grok（`%{NGINX_ADDRESS_LIST:nginx.access.remote_ip_list}` / `%{HTTPDATE:nginx.access.time}` 等） | 本机实测 | 高 | 2026-09-06 |
| **模块解析结果（实测字段）**：一行 nginx 访问日志 → `source.ip` / `http.request.method` / `http.response.status_code` / `http.response.body.bytes` / `url.path` / `user_agent.original` / `event.module=nginx` / `event.dataset=nginx.access` / `service.type=nginx` / `fileset.name=access` | 本机实测 | 高 | 2026-09-06 |
| **⚠️ 模块解析后 `message` 字段消失**，原始文本改存 **`event.original`** → 依赖 `message` 的 processor `when` 条件**静默失效**（不报错） | 本机实测 | 高 | 2026-09-06 |
| **模块自动把 `@timestamp` 解析为事件真实时间**（实测 `2026-09-06T08:30:01.000Z` = 日志内容里的 `16:30:01 +0800`）——解决课 3 遗留的采集时间偏差 | 本机实测 | 高 | 2026-09-06 |
| **模块内部使用 `input.type: log`**（非我们教的 `filestream`），这是 Filebeat 模块的既有实现 | 本机实测（文档字段） | 高 | 2026-09-06 |
| **processors 三项实测生效**：`add_fields`（env/lesson 全员）、`add_tags` + `when.contains.message:ERROR`（**仅 ERROR 那条有 tags**）、`drop_fields`（`agent.ephemeral_id` 消失、`ecs` 变空对象） | 本机实测 | 高 | 2026-09-06 |
| **ES 停机重试与恢复实测**：`docker compose stop elasticsearch` 期间 Filebeat 日志 `Attempting to reconnect to backoff(elasticsearch(...)) with N reconnect attempt(s)`（N 递增至 6）+ `Error dialing lookup elasticsearch on 127.0.0.11:53: no such host`；恢复后 `Connection to backoff(...) established`，**停机期间写入的 orderId=30011 送达（count:1），总数 10 → 13 一条不丢** | 本机实测 | 高 | 2026-09-06 |
| **⚠️ 恢复后数据不会立刻可见**：ES 刚 healthy 时查文档数仍为 10（一度误判为丢数据），等待并追加新事件后才刷出全部 13 条 | 本机实测 | 高 | 2026-09-06 |
| **⚠️ 挂载坑（课 3 预判被课 5 证实）**：把宿主机日志目录挂到 `/usr/share/filebeat/logs`（Filebeat 自身日志目录）且为只读 → 报 `failed to open new log file for writing: ... read-only file system`。本课已改为挂载到 `/var/log/app` | 本机实测 | 高 | 2026-09-06 |
| ⏳ `bulk_max_size`=50 / `worker`=1 / `max_retries`=3 三个默认值来自**官方文档**，未实测 | 官方文档 | 中高（默认值） | 2026-09-06 |
| 课 5 引用的 3 条官方文档链接可达：`filebeat-modules`、`defining-processors`、`elasticsearch-output` 均 HTTP 200 | curl 实测 | 高 | 2026-09-06 |
| **两个 Beat 写入独立 data stream（实测）**：`.ds-filebeat-9.5.3-2026.09.06-000001`（7 条，**yellow**：副本 1 + 单节点）与 `.ds-metricbeat-9.5.3-2026.09.06-000001`（11 条，**green**：metricbeat.yml 设了 `index.number_of_replicas: 0`）。数字为查询时刻快照，指标每 10 秒持续增长 | 本机实测 | 高 | 2026-09-06 |
| `metricbeat modules list` → `Enabled: system`（与 Filebeat 一样需先配 `metricbeat.config.modules.path`） | 本机实测 | 高 | 2026-09-06 |
| **⚠️ 指标陷阱：`system.cpu.total.pct` 是按 CPU 核数累加的**，不是"使用率百分比"。实测 `system.cpu.cores=11`、`total.pct=0.1827`、`total.norm.pct=0.0166`，验算 `0.0166 × 11 = 0.1826 ≈ 0.1827` ✅。多核机器上把 pct 当使用率会导致告警永不触发，应使用 `norm.pct` | 本机实测（含验算） | 高 | 2026-09-06 |
| 指标文档结构（实测）：`@timestamp` / `system.cpu.{cores,total,user,system,idle,iowait,irq,softirq,nice,steal}` / `host.name` / `event.dataset=system.cpu` / `agent.type=metricbeat` | 本机实测 | 高 | 2026-09-06 |
| **共享 libbeat 的实测证据**：filebeat.yml 与 metricbeat.yml 均含 `*.config.modules.path`、完全相同的 `output.elasticsearch` 段、同样的 `modules.d/` 目录约定，文档里靠 `agent.type` 区分 | 本机实测 | 高 | 2026-09-06 |
| 课 6 引用的 2 条官方文档链接可达：`/docs/reference/beats/metricbeat`、`/docs/reference/beats` 均 HTTP 200 | curl 实测 | 高 | 2026-09-06 |
| ⏳ **未实测（课 6）**：① Packetbeat / Heartbeat / Auditbeat / Winlogbeat（分别需网络抓包权限、拨测配置、内核审计权限、Windows 系统）② 采集端选型对比（定性，基于公开定位与社区共识，非性能/部署实测）③ 容器与 K8s 场景（本机为 Docker Compose 单机） | — | — | 2026-09-06 |
| **`--config.test_and_exit` 实测**：输出 `Configuration OK` + `Config Validation Result: OK`，并含 `[INFO] Pipeline 'main' is configured with 'pipeline.ecs_compatibility: v8' setting`（后者是字段名重命名的根源提示）。另有两个无害警告（忽略 pipelines.yml、UserMetric type mismatch） | 本机实测 | 高 | 2026-09-06 |
| **grok 多模式与类型捕获实测**：订单日志两种形态（含 `amount=` / 含 `reason=`）用两个 pattern 依次尝试；`%{NUMBER:order_id:int}` → `order_id: 50005`（整型）、`%{NUMBER:amount:float}` → `amount: 99.5`（浮点）。不写类型则全为字符串，数值聚合会失败 | 本机实测 | 高 | 2026-09-06 |
| **`_grokparsefailure` 分流实证**：故意写入一条不符合格式的中文脏数据 → 进 `logstash-failed-2026.09.06`（1 条），`tags: ["beats_input_codec_plain_applied", "_grokparsefailure"]`；正常数据进 `logstash-demo-2026.09.06`（**8 条**）。**证明 Logstash 失败时打标签而非丢弃**，可用条件分支分流 | 本机实测 | 高 | 2026-09-06 |
| **date 校准实测**：日志内容 `2026-09-06 17:45:01`（+0800）→ `@timestamp: 2026-09-06T09:45:01.000Z`；配置 `match=>["log_time","yyyy-MM-dd HH:mm:ss"]` + `timezone=>"Asia/Shanghai"` + `target=>"@timestamp"` | 本机实测 | 高 | 2026-09-06 |
| **mutate 四件套实测**：`gsub => ["user_id","^u","user-"]` 实测 `u405 → user-405`；`rename`（service_name→service）；`add_field`（processed_by=logstash-lesson07）；`remove_field`（log_time、@version 消失） | 本机实测 | 高 | 2026-09-06 |
| **⚠️ ECS 兼容模式字段重命名（本课最重大发现）**：9.5.3 默认 `ecs_compatibility: v8`，`%{COMMONAPACHELOG}` 实际输出 **`source.address`**（非 clientip）、**`http.request.method`**（非 verb）、**`url.original`**（非 request）、**`http.version`**（非 httpversion）、**`http.response.status_code`**（非 response）、**`http.response.body.bytes`**（非 bytes）；`timestamp`（HTTPDATE 捕获）保留原名。实测值：192.168.1.10 / GET / /api/order/create / 1.1 / 201 / 1234 | 本机实测（rubydebug 输出 + ES 文档双重确认） | 高 | 2026-09-06 |
| **空操作实证**：`mutate { convert => { "bytes" => "integer" } }` 在 ECS 模式下**不报错也不生效**（字段已改名）；正确写法为 `convert => { "[http][response][body][bytes]" => "integer" }` | 本机实测 | 高 | 2026-09-06 |
| **⚠️ 文档链接验证的坑**：`elastic.co/docs` 系列 URL 用 curl 默认 UA 会返回 **404**，带浏览器 User-Agent 后返回 **200**。已验证课 7 引用的 5 条 Logstash 官方文档（参考总览 / grok / date / mutate / beats input）均为 200 | curl 实测 | 高 | 2026-09-06 |
| **⚠️ Logstash 只读挂载 logstash.yml 会启动失败**：报 `error: /usr/share/logstash/config/logstash.yml: Read-only file system`。原因：Logstash 启动时把环境变量里的设置写回该文件。解法：全局参数（queue.type 等）改用**环境变量**传，或可写挂载 | 本机实测 | 高 | 2026-09-06 |
| **双管道 + 持久化队列实测**：`_node/stats` 显示 `管道数: 2`、两条均 `queue.type=persisted`、各 `in=3 out=3 filtered=3`；`queue.max_bytes=1gb` 生效（`max_queue_size_in_bytes=1073741824`） | 本机实测 | 高 | 2026-09-06 |
| **ES 停机退避重试日志（Logstash 侧）**：`[ERROR] Attempted to send a bulk request but there are no living connections in the pool (perhaps Elasticsearch is unreachable or down?) ... will_retry_in_seconds: 64` + `Failed to perform request {message: "elasticsearch: Name or service not known", exception: Manticore::ResolutionFailure}` | 本机实测 | 高 | 2026-09-06 |
| **决定性实验：持久化队列不丢数据**：`docker compose stop elasticsearch` → 写 2 条新日志（orderId=60010/60011）→ **`docker compose restart logstash`** → 恢复 ES → 两条均 `count:1`，订单索引 **3 → 5 条**。证明：ES 停机期间进队列的事件，在 Logstash 重启 + ES 恢复后完整送达 | 本机实测 | 高 | 2026-09-06 |
| **队列指标观察（诚实记录）**：ES 停机期间查 `queue.events` 显示 **0**（非积压正数），推断事件处于 output 重试的 in-flight 状态不计入队列计数；最终以"恢复后 60010/60011 均送达"为硬证据。**该指标语义未进一步确认，正文已诚实标注** | 本机实测（观察） | 中（语义未确认） | 2026-09-06 |
| 课 8 引用的 4 条官方文档链接可达：`persistent-queues`、`multiple-pipelines`、`tuning-logstash`、`monitoring-logstash` 均 HTTP 200 | curl 实测（带 UA） | 高 | 2026-09-06 |
| ⏳ **未实测（课 8）**：① 内存队列 vs 持久化队列的**丢数据对照**（只实测了持久化不丢，内存"会丢"是理论+官方文档依据）② 吞吐三兄弟（batch.size/delay/workers）不同取值的**压测对比**（参数已配置生效，无压测数据） | — | — | 2026-09-06 |
