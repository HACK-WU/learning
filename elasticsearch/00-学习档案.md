# Elasticsearch 系统学习 · 学习档案

## 学习者画像

| 项目 | 值 | 来源 |
|------|-----|------|
| 主题 | Elasticsearch 系统学习 | 用户指定 |
| 当前水平 | 零基础（类比要多、术语逐个通俗化、不假设任何前置） | 默认值 + 与既有课程一致 |
| 学习目标 | 全目标：①理解概念 ②动手实操 ③应试认证 ④决策参考 | 用户指定（1-4 全都要） |
| 输出格式 | Markdown（主载体，Mermaid + SVG 图表） | 默认值 |
| 模式 | 课程制（完整体系，5 阶段 / 15 课 / 46 知识点） | 自动判定（2026-09-01 增补课 15） |
| 实操环境 | Windows 11 本机 + ES 原生 zip 发行版（非 Docker） | 环境探测结论 |

> 说明：course-reviewer 子 agent 尚未创建，大纲与批次评审由主 agent 内联执行（教学法视角 + 学习者视角），评审独立性受限。可随时让我按 create-sub-agent 规范创建独立 agent 以获得更独立的评审视角。

## 环境探测结论（2026-08-28）

| 探测项 | 结果 | 对课程的影响 |
|--------|------|--------------|
| Docker | **未安装**（`docker` 命令不存在） | 官方 `start-local` 脚本不可用（它依赖 Docker） |
| WSL | 已装 Ubuntu，版本 2，状态 Running | 备选路径：可在 WSL 内跑 Docker/ES，但本课程不走这条路 |
| 物理内存 | 63.4 GB | 充足，可跑多节点集群与压测 |
| 操作系统 | Windows 11 | ES 提供 **原生 windows-x86_64.zip** 发行版，自带 OpenJDK，直接 `bin\elasticsearch.bat` 启动 |

**决策**：实操主线采用 **ES 原生 zip 发行版**（版本 9.5.1，官方 Windows zip 直链已核实），不依赖 Docker。
- 首次启动会自动开启安全：生成 `elastic` 用户密码 + HTTP/TLS 证书 + Kibana enrollment token
- Windows 服务方式启动时 TLS 不自动配置，密码需用 `elasticsearch-reset-password` 生成 —— 课程统一用**命令行前台启动**以拿到完整自动配置
- Kibana 可后续按需加装（本课程核心不依赖 Kibana）
- **请求工具统一用 `curl.exe`**（2026-08-31 实测修订）：本机 Windows PowerShell 5.1 的 `Invoke-RestMethod` 连不上 ES 9.5.1 的 HTTPS 端点（无 `-SkipCertificateCheck`，强制 TLS 1.2/1.3 亦失败），**根因未定位**；且本机未安装 pwsh 7。`curl.exe` 为系统自带，CMD / PowerShell / Git Bash 通用
- **首次启动必须在终端直接跑**（不可重定向输出）：实测无 TTY 时 ES 跳过密码生成，须用 `elasticsearch-reset-password -u elastic -i` 重设

## 知识点级进度表

| 阶段 | 课 | 知识点 | 状态 | 完成日期 | 认证映射 |
|------|-----|--------|------|----------|----------|
| 1 | 课1 | 数据库模糊查询的痛点 | ✅ 已完成 | 2026-08-28 | - |
| 1 | 课1 | 搜索引擎是什么 | ✅ 已完成 | 2026-08-28 | - |
| 1 | 课1 | ES 能干什么（四大场景） | ✅ 已完成 | 2026-08-28 | - |
| 1 | 课2 | ES 起源与定位 | ✅ 已完成 | 2026-08-28 | - |
| 1 | 课2 | 核心概念全景 | ✅ 已完成 | 2026-08-28 | - |
| 1 | 课2 | ES vs 数据库与其他方案对比 | ✅ 已完成 | 2026-08-28 | - |
| 2 | 课3 | 本机安装与首次启动 | ✅ 已完成 | 2026-08-31 | Installation |
| 2 | 课3 | 索引/文档 CRUD | ✅ 已完成 | 2026-08-31 | Indexing Data |
| 2 | 课3 | 用 _cat API 观察集群 | ✅ 已完成 | 2026-08-31 | Cluster Admin |
| 2 | 课4 | 倒排索引原理 | ✅ 已完成 | 2026-08-31 | - |
| 2 | 课4 | 分词与分析器 | ✅ 已完成 | 2026-08-31 | Mapping/Analysis |
| 2 | 课4 | 中文分词与 IK | ✅ 已完成 | 2026-08-31 | Mapping/Analysis |
| 2 | 课5 | 映射 Mapping 设计 | ✅ 已完成 | 2026-08-31 | Data Management |
| 2 | 课5 | 动态映射与模板 | ✅ 已完成 | 2026-08-31 | Data Management |
| 2 | 课5 | 多字段 multi-fields | ✅ 已完成 | 2026-08-31 | Mapping/Analysis |
| 3 | 课6 | Query DSL 结构 | ✅ 已完成 | 2026-08-31 | Queries |
| 3 | 课6 | 全文查询 vs 词项查询 | ✅ 已完成 | 2026-08-31 | Queries |
| 3 | 课6 | 布尔组合与过滤 | ✅ 已完成 | 2026-08-31 | Queries |
| 3 | 课7 | 相关性打分 BM25 | ✅ 已完成 | 2026-08-31 | Queries |
| 3 | 课7 | 排序·分页·高亮 | ✅ 已完成 | 2026-08-31 | Queries |
| 3 | 课7 | 相关性调优 | ✅ 已完成 | 2026-08-31 | Queries |
| 3 | 课8 | 桶聚合与指标聚合 | ✅ 已完成 | 2026-08-31 | Aggregations |
| 3 | 课8 | 子聚合与管道聚合 | ✅ 已完成 | 2026-08-31 | Aggregations |
| 3 | 课8 | ES\|QL 入门 | ✅ 已完成 | 2026-08-31 | Queries |
| 4 | 课9 | 分片与副本机制 | ✅ 已完成 | 2026-08-31 | Cluster Admin |
| 4 | 课9 | 分布式写流程 | ✅ 已完成 | 2026-08-31 | - |
| 4 | 课9 | 分布式读流程 | ✅ 已完成 | 2026-08-31 | - |
| 4 | 课10 | 集群健康与故障转移 | ✅ 已完成 | 2026-08-31 | Cluster Admin |
| 4 | 课10 | 分片设计与容量规划 | ✅ 已完成 | 2026-08-31 | Cluster Admin |
| 4 | 课10 | 诊断与修片 | ✅ 已完成 | 2026-08-31 | Cluster Admin |
| 4 | 课11 | Ingest Pipeline | ✅ 已完成 | 2026-08-31 | Data Processing |
| 4 | 课11 | Reindex 与 Update By Query | ✅ 已完成 | 2026-08-31 | Data Processing |
| 4 | 课11 | 快照与恢复 | ✅ 已完成 | 2026-08-31 | Cluster Admin |
| 4 | 课12 | 客户端选型与集成 | ✅ 已完成 | 2026-09-01 | - |
| 4 | 课12 | 数据同步模式 | ✅ 已完成 | 2026-09-01 | - |
| 4 | 课12 | 搜索应用架构 | ✅ 已完成 | 2026-09-01 | - |
| 4 | 课15 | 索引模板体系（组件模板 + priority + `_simulate_index`） | ✅ 已完成 | 2026-09-01 | Index Management |
| 4 | 课15 | 别名（原子切换 / 过滤别名 / is_write_index） | ✅ 已完成 | 2026-09-01 | Index Management |
| 4 | 课15 | ILM 生命周期（五阶段 + rollover + poll_interval） | ✅ 已完成 | 2026-09-01 | Index Management |
| 4 | 课15 | 索引运维（forcemerge / shrink / close / split） | ✅ 已完成 | 2026-09-01 | Index Management |
| 5 | 课13 | 日志/可观测场景 | ✅ 已完成 | 2026-09-01 | - |
| 5 | 课13 | 向量检索与 RAG | ✅ 已完成 | 2026-09-01 | - |
| 5 | 课13 | 安全与权限 | ✅ 已完成 | 2026-09-01 | Cluster Security |
| 5 | 课14 | 该不该用 ES（选型清单） | ✅ 已完成 | 2026-09-01 | - |
| 5 | 课14 | 认证备考指南 | ✅ 已完成 | 2026-09-01 | 全部 |
| 5 | 课14 | 知识体系收束 | ✅ 已完成 | 2026-09-01 | - |

## 评审记录

> 评审方式：`主 agent 内联`（子 agent 未创建）。批次内容评审为双模式（教学法视角 + 学习者视角），冲突时主 agent 裁决并标注。

| 日期 | 评审对象 | 评审方式 | 意见摘要 | 处置 |
|------|----------|----------|----------|------|
| 2026-08-28 | 多阶段大纲（初版） | 主 agent 内联（pedagogy + learner） | P0×0 / P1×0 / P2×1（课14 补"知识体系收束"以闭环全课主线） | 已采纳 P2，知识点 41→42 |
| 2026-08-28 | 课1 首次交付 | 主 agent 内联 + 评审清单 10 项 | P0×0 / P1×0 / P2×0；10 项必查全通过（含结尾两段齐全、全程中文、Mermaid 无反引号、跨课引用均已核实） | 无修改，直接交付 |
| 2026-08-28 | 课2 首次交付 | 主 agent 内联 + 评审清单 10 项 | P0×0 / P1×0 / P2×0；10 项必查全通过。已澄清：Type 已废弃、ES 非数据库、许可证 2021 变更；明确本课聚焦 ES 本身、Logstash/Kibana 仅必要处提及 | 无修改，直接交付 |
| 2026-08-31 | 课3 首次交付 | 主 agent 内联 + 评审清单 10 项（**命令全部本机实测**） | P0×1 / P1×2 / P2×2。双视角一致指向"命令可运行性"，无冲突需裁决。P0 为知识点 2 六处 curl 示例混用 CMD 续行符与转义引号（与第四幕 Git Bash 写法冲突，复制必失败） | 全部当批修复后交付：统一 Git Bash 写法；mermaid 去反斜杠；角色拆分改指"阶段 4"；`docs.deleted` 去掉对不上的算术改定性描述；补 Kibana 三件套说明表 |
| 2026-08-31 | 课4 首次交付 | 主 agent 内联 + 评审清单 10 项（**IK 插件真装实测**） | P0×0 / P1×1 / P2×1 + 2 处准确性修正。双视角无冲突。B 视角确认「搜人民命中 4 条、3 条是垃圾」的证据链有冲击力，两个认知冲突均已在第三幕回答 | 当批修复后交付：段 segment 与分片 shard 加对照说明（避免混淆，分片归课 9）；装 IK 补齐重启命令；第一幕"5 条"改"4 条"；`termvectors` 补视角说明 |
|
| 2026-08-31 | 课6 首次交付 | 主 agent 内联 + 评审清单 10 项（**20+ 组命令本机实测，新建 l6_shop 8 条数据集**） | P0×0 / P1×2 / P2×1。A 视角发现并修正 3 处：① **SVG 里「手机」词项命中的文档列表写错**（原写 doc1,doc2,doc3,doc6,doc8，实测 doc6 是华为 MateBook 笔记本电脑、不含"手机"，应为 doc1,doc2,doc3,doc8——并集 6 条才与 match 结果吻合）② SVG 底部「0.99+0.72」为估算，替换为实测精确值（0.9894+0.7262=1.7156）③ **速查表列了 `exists` 与 `multi_match` 却未实测**，补测后补入结果。B 视角：开场「term 0 条 vs match 6 条」反差强烈；`filter` 三组对照实验（1.6706/0.7262/对照组）证据链完整。**主动标注 filter 缓存在 8 条数据量级下测不出**（`query_cache.hit_count` 恒为 0），未夸大性能收益 | 全部当批修复：SVG 两处按实测更正；新增「match 分数 = 各词项得分之和」实测拆解段（0.9894+0.7262=1.7156，呼应课 7 BM25）；速查表补 exists/multi_match 实测结果；误区五明确说明用 filter 的首要理由是语义与分数干净而非性能。10 项必查全通过（五幕 5、知识点 3、结尾 2、`^` 续行 0、转义引号 0、Mermaid 反引号 0） |
| 2026-08-31 | 课7 首次交付 | 主 agent 内联 + 评审清单 10 项（**40+ 组命令本机实测，新建 l7_news 6 篇数据集**） | P0×0 / P1×2 / P2×1。A 视角**发现并纠正一处算错**：`function_score` 的 `field_value_factor` 手算 3.98 与实测 2.59 不符——本想归因为「ES 内部 queryNorm 归一化」，但那是**编造解释**；翻 `explain` 原文后定位到两处真实记错：① `factor` 在取对数**之前**乘进字段值（是 `log1p(views × factor)` 而非 `log1p(views) × factor`）② **`log1p` 用 log10 而非自然对数 ln**。修正后 4 篇文档交叉验证误差 ~1e-7。该纠错过程已原样写入讲义当方法论（"对不上先翻 explain，别怪内部黑盒"）。另实测踩坑：ES 9.5.1 **不能对 `_id` 排序**（`Fielddata access on the _id field is disallowed`），须改用 `_doc` 做决胜键 |
| 2026-08-31 | 课9 首次交付（阶段4开局） | 主 agent 内联 + 双 agent 交叉评审（**新建 3 节点学习集群真跑分布式**，40+ 组命令实测） | P0×0 / P1×3 / P2×0。**A 视角发现并修正 2 处快照失真**：① 讲义 1.3 分片分布表按初始状态写 P2→node-1，但故障转移重启后 ES 重新均衡、实测已变 P2→node-3——已补「不要背这张表，要背规律（主副本永不同节点、主分片尽量均摊）」② 1.7 自然分布「6/2/4」是最初 12 条的快照，后续写入后变 8/8/4，已注明「数字对不上正常，确认不均匀即可」。**A 视角另纠正 1 处归因错误（P1）**：讲义原对「停 node-3 后集群 red」的解释含猜测成分（"加上部分索引的主分片需要重新分配"）——重跑实验后拿到决定性对照：**有副本的索引（l9_orders/l9_rep1，rep=1）转 yellow 且数据零丢失；0 副本的索引（l9_hi/l9_trap2 等 7 个）转 red，`unassigned_primary_shards=7`**。已用该对照表取代原猜测段，并据此新增踩坑第 7 条「为消 yellow 而设 0 副本 → 节点一挂真丢数据」，同时辨析单节点设 0 副本（合理）与多节点设 0 副本（危险）的区别。**B 视角（零基础读者）逐字复制 7 组代码块全部执行通过**，另发现 routing 实验「5 条」依赖执行次数——已补说明「执行几次就是几条，重点是全在同一分片」。**关键实测成果**：真实触发多分片聚合误差（`l9_trap2` 1245 条 / 1200+ 基数，3 次复现 `doc_count_error_upper_bound=3`；`shard_size` 25/50/100→3，500/1000→0；单分片对照组恒 0），**正面回答课 8 伏笔** |
此处不是文件内容，因为上下文过长已省略
| 2026-08-31 | 课11 首次交付 | 主 agent 内联 + 双 agent 交叉评审（**跨集群快照恢复真跑**，40+ 组命令实测） | P0x1 / P1x1 / P2x0。**B 视角（零基础读者）抓到 1 处 P0：执行链断裂**——清理阶段删掉了 l11_shop，而讲义 2.1/2.2/2.4 三节都依赖它，逐字复制报 no such index [l11_shop]（实测 5 项 FAIL）。已重建该索引至 1.8 节状态，并在 2.1 节开头补「前置依赖」提示，重跑后 4a/4b/5 三组全部复现（total:2,updated:0 + field [price_str] not present / updated:2 / created:3）。**A 视角（事实核查）抓到 1 处 P1：仓库大小 83.3 KB 是删 snap_1 那一刻的时点值**，SLM 后续又生成新快照，复核时 93.5 KB，已改为「看增量比例约 22%，别看绝对值」。A 视角另发现只读仓库 l11_readonly 被误删致报错原文不可复现，已重建重验，报错原文一字不差。**核心实测成果**：① _update_by_query 默认重跑 default_pipeline，导致 total:2,updated:0 且报 field [price_str] not present，加 ?pipeline=_none 后 updated:2——本课最高价值的坑 ② reindex 把 1 主 0 副改成 3 主 1 副，兑现课 9「分片数不可改」伏笔 ③ 快照删除后恢复一条不差（l11_shop_v2 3 条 + l9_orders 23 条，_source 里 pipeline/reindex/update_by_query 三层加工痕迹全在）④ 删掉 snap_1 后从 snap_4 恢复仍完整（106.7→83.3 KB，仅释放 23.4 KB，证明段文件复用）⑤ **跨集群恢复成功**：9201 集群 snap_4 → 恢复到 9200 集群，24 条、3 主 1 副、连 _id 都原样 ⑥ SLM 自动生成 l11-daily-snap-2026.08.31-<随机串> 且定时真的执行（10:59:59 又跑一次）⑦ url 只读仓库可读不可写 ⑧ 仓库类型探测：fs/url/s3/gcs/azure/source 可用，**hdfs 不支持**。**附带修复一个历史事故**：9200 单节点集群的 elasticsearch.yml 在课 9 被整体改写成 node-3 配置（cluster.name: l9-cluster / http.port: 9203），导致 9200 长期无法启动；重写配置后又撞 CorruptStateException（元数据被污染）。处置：先 robocopy 全量备份 data（0.64 MB / 417 文件）→ 清空 _state → 重置 elastic 密码 → 用 _dangling 找回 19 个孤儿索引 → 逐个 accept_data_loss=true 恢复，最终 l6_shop 8 / l7_news 6 / l8_orders 24 / l8_orders_v2 24 / l8_text_demo 24 / news_ik 5 **与档案记录完全一致**。该过程已写入讲义作为「元数据坏了不等于数据坏了」的教材 |
| 2026-08-31 | 课10 首次交付 | 主 agent 内联 + 双 agent 交叉评审（**真实销毁 node-3 数据盘**，40+ 组命令实测） | P0×2 / P1×0 / P2×0。**A 视角（事实核查）抓到 1 处 P0：分片延迟对照数据不可复现**——讲义原写 1/3/50 片为 3.8/3.4/12.2ms 且"50 片慢 3.6 倍"，复现实测却是 15.6/16.4/15.8ms、50 片甚至非最慢。根因：**首轮查询含冷启动开销（实测 54–65ms）**，5 次取平均被彻底带偏。改用「预热 10 次丢弃 → 取 20 次稳定值 → 两轮交叉验证」重测，得 2-2.5/2-3/5ms，结论修正为「50 片约慢 2 倍」，同时发现 store.size 的 24kb 是建索引未写数据时读取（实为 359.9kb）。该纠错过程已原样写入讲义当方法论。**B 视角（零基础读者）抓到 1 处 P0：本机未装 Git Bash**——三个常见路径均不存在，PATH 中 bash.exe 为 WSL 启动器无响应，故课 3 定的「Git Bash 写法」在本机不可执行；且实测四种 JSON 传参写法，**PowerShell 下仅 `--data-binary @文件` 可靠**（CMD 下反斜杠转义亦可）。已把 21 处内联 JSON 命令全改为「CMD 转义版 + 文件版」双写法，新增第三幕开头的环境说明段，并把该约束写入踩坑记录第 1 条。**核心实测成果**：真实移走 node-3 的 data 目录，证明 0 副本主分片**永久丢失**（l9_hi 300→197 且报 no_shard_available_action_exception）、有副本索引零丢失；ES 官方 explain 原文给出 `no_valid_shard_copy` + 建议 `restore from a recent snapshot`（直通课 11）；只剩 1 节点时 `_cluster/health` 返 **HTTP 503**（法定人数防护实证）；主节点被杀后 node-2 自动接任且写入正常 |

| 2026-09-01 | 课12 首次交付（阶段4收官） | 主 agent 内联 + 双 agent 交叉评审（**真装官方 JS 客户端 9.5.1 跑通全链路**，7 组脚本实测） | P0×0（评审后当批修复 3 处 P1、已清零）/ P1×0 / P2×0。**A 视角（事实核查）抓到 2 处 P1**：① 第 2 步脚本示例把 `await` 写在 `.js` 顶层，零基础读者复制会报 `SyntaxError: await is only valid in async functions`（实测脚本本包在 async main 里）→ 已改示例加 `async main()` 包装并补 `.mjs` 替代方案 ② 第 3 步 curl 用单引号 `-H '...'`，仅 PowerShell 可用、CMD 不认单引号（课 10 已实测本机无 Git Bash）→ 已补「PowerShell 单引号 / CMD 双引号」双写法说明。**B 视角（零基础读者）抓到 1 处 P1**：知识点 2 三种同步模式全依赖 MySQL，而本机无 MySQL、第四幕无对应实操，读者会误以为漏讲 → 已加显式说明「为何不可实操」，并点明同步的两个可练机制（bulk 批量 + `_id` 幂等）已在第 4、5 步实测；另补 sniffOnStart 可复现对照脚本（B 视角「由浅入深」项）。**核心实测成果**：① 抓到版本协商铁证——客户端请求头 `Accept: application/vnd.elasticsearch+json; compatible-with=9` + `Content-Type: ...compatible-with=9`，User-Agent 为 `elasticsearch-js/9.5.1 (win32...; Node.js 22.14.0; Transport 9.4.0)` ② 用 curl 模拟三种兼容版本：`compatible-with=8` 成功、`=9` 成功、**`=7` 报 HTTP 400**，报错原文 `Accept version must be either version 9 or 8, but found 7. Accept=application/vnd.elasticsearch+json; compatible-with=7`（type: media_type_header_exception）——「只跨一个大版本」规则的实证 ③ **bulk 部分失败**：4 条里 1 条把 `abc` 写给 long 字段 → `errors=true`，另 3 条确实落库（`_id=4` 不存在、文档数 3），报错 `document_parsing_exception: failed to parse field [price] of type [long]` ④ **幂等实证**：同一 `_id` 连写 3 次，result=updated 且 `_version` 2→3→4，**文档总数恒为 3** ⑤ **连接池均摊**：显式列三节点后连发 6 次请求，分布 **2/2/2** 完美均摊；而 **`sniffOnStart:true` 实测不生效**（连接池仍为 1）⑥ **深分页撞墙**：max_result_window 改 10 后请求 `from=20,size=5` → 外层报 `search_phase_execution_exception / all shards failed`，**根因藏在 root_cause**：`Result window is too large, from + size must be less than or equal to: [10] but was [25]`——「别只看最外层报错」的教材 ⑦ **search_after 绕过**：用 `[{views:'asc'},{_doc:'asc'}]` 排序连翻 4 页（到第 20 条）不受限制，sort 值 `[5,4]`。**环境限制如实标注**：本机仅装 Node.js v22.14.0（Python 为 Store 存根、Java/Go/.NET 均未装），故客户端实测全部用 JS 客户端，其他语言例子仅作对照未在本机运行。**事实核查**：官方客户端兼容矩阵、REST API 兼容模式、bulk 部分失败机制、search_after 决胜键（`_shard_doc` 为官方推荐、`_id` 因 doc_values 被禁用而不推荐）均已联网核实并写入讲义 |

| 2026-09-02 | 课程手册汇总（Phase 4） | 主 agent 内联（pedagogy + learner）+ 链接脚本校验 | P0=0。相对链接 54/54 存在、0 断链；课时覆盖 15/15；每课一图总结 15 个（11 mermaid + 4 复用既有 SVG）。另补根级 `assets/learning-path-overview.svg` |
| 2026-09-02 | 结课实战项目《商品搜索服务》（Phase 3） | 主 agent 内联（A 事实核查 + B 教学体验）+ 专项维度 A1-A6 | P0=2 均已修 / P1=2 均已修 / P2=0。四门槛均达标（跨 5/5 阶段、5 项非功能约束、5 个真权衡决策、13 个源文件四层目录）。A6 可运行性**实测通过**：`npm run demo` 从零退出码 0、7 步全过。**关键实测**：38 条幂等同步；脏数据 2 条死信（`document_parsing_exception` / `strict_dynamic_mapping_exception`）；切换前后 23 → 19 条、v1 独有 4 条全是配件；RBAC 写拒 403、错密码 401 |

## 大纲调整记录

> **2026-09-01 · 课13 评审记录**：主 agent 内联 + 双 agent 交叉评审（三大战场**各跑通一遍**，9 组实测）。**P0×0**（评审后当批修复 3 处 P1、已清零）/ P1×0 / P2×0。**A 视角（事实核查）抓到 2 处 P1**：① 第 1 步与第 4 步的 PowerShell 续行写法不一致（一处无反引号），读者会困惑 → 已统一为反引号并加说明 ② 知识点 2 提了 `semantic_text` 却无落地、未测本机可行性 → 已补「自动挡 vs 手动挡」选型对比 + **本机 basic license 实测**：`semantic_text` **能建索引、能建映射，但一写入即报 `current license is non-compliant for [inference]`**，且单独建 inference endpoint 同样被拒。此坑狡猾在「建时不报错、写时才炸」，已写入讲义。**B 视角（零基础读者）抓到 1 处 P1**：知识点 3 只说「字段级/文档级安全需付费 license」却未给本机预期 → 已补三级粒度对照表（索引级 ✅ 可用 / 字段级 ❌ / 文档级 ❌）并明示「本机尝试会得 license 报错，非配置错误」；另补「关键词搜索到底解决不了什么」三例过渡（宠物/退款/卡顿），降低向量检索的认知跨度。**核心实测成果**：① **Data Stream 全链路**：ILM 策略 `l13_logs_policy`（hot 滚动 50gb/1d + 30d 删除）→ index template → 自动创建 `.ds-l13-logs-app-2026.09.01-000001`，`managed_by: Index Lifecycle Management` ② **rollover 实证**：手动滚动生成 000002，新写 2 条全部进 000002、旧索引停在 3 条，验证「写入自动路由、旧索引封存」③ **搜索透明性**：搜 data stream 名得 **2 条**（跨两个后备索引）vs 搜单个后备索引得 **1 条** ④ **ILM 状态**两索引均在 `hot / check-rollover-ready` ⑤ **关键词 vs 向量决定性对照**：关键词搜「宠物」仅命中 1 条（猫是一种宠物）；**向量 kNN 命中 3 条，且「狗是人类的朋友」score 0.9992 排在「猫」0.9985 之前**——字面无「宠物」却因语义最近排第一 ⑥ 换科技语义向量召回立即切换（手机 0.9994 / 电脑 0.9992）⑦ **向量维度不匹配**：外层 `search_phase_execution_exception`，根因 `The query vector has a different number of dimensions [2] than the document vectors [3]`（再次印证课 12 的 root_cause 教训）⑧ **RAG 最小闭环**：5 个 chunk 知识库，退款问题精确召回 policy.md 两条并拼出 Prompt；换物流问题召回切至 shipping.md ⑨ **RBAC 权限矩阵全验证**：只读用户读 `l8_orders` 得 24 条 ✅；写入、删索引、访问未授权索引 **均 403**，且报错末尾**直接列出所需权限名**（`granted by the index privileges [create_doc,create,index,write,all]`）；`monitor` 集群权限允许看健康 ✅；无凭据与错密码**均 401**——401/403 边界清晰。**环境事实**：两集群 license 均为 **basic**（`_license` 实测确认）。**时效性核查**：Elastic 认证考纲于 **2026-09-01 正式从 8.15 升级至 9.3**（官方 FAQ + 考试页双源确认）：新增 Architecture 大类、`semantic search`、ES|QL、Streams；移除 runtime fields、跨集群搜索/复制、searchable snapshot 等——已写入讲义与下批接力提示词


> **2026-09-01 · 课14 评审记录（全课收官）**：主 agent 内联 + 双 agent 交叉评审（4 组选型实测 + 考纲逐条探测）。**P0×0 / P1×0 / P2×0**（评审后当批修复 3 处 P1、已清零）。**A 视角（事实核查）抓到 3 处 P1**：① **缺五幕结构**（必查项 #2，课 13 有而本课无）→ 已重构为「场景引入→认知冲突→层层揭示→实操验证→体系收束」② **缺「🚀 下一批接力提示词」段**（必查项 #1，PromQL L1 / ZK L1 曾两次遗漏）→ 已补，且因是收官课给了「备考冲刺 / 实战项目」两个方向 ③ **缺「🧭 课程导航」段**（必查项 #1）→ 已补。**B 视角（零基础读者）抓到 1 处 P2**：讲义只有脚本引用、无可直接复制的命令 → 已在第四幕补 4 组可复制命令（含 `--data-binary @agg.json` 文件写法，规避本机无 Git Bash 的限制），并**逐字验证通过**（0 vs 82、77.3kb vs 361.9kb 与讲义完全一致）。**核心实测成果**：① **分片数对照决定性证据**（复用课 10 的 `l10_shard_1/3/50`，各 3000 条同数据、唯一变量是分片数）：p95 延迟 **16.58 / 16.48 / 35.90 ms**（50 片慢 2.2 倍）、存储 **77.3 / 88.9 / 361.9 kb**（50 片 4.7 倍）、段数量 1 vs 50；**p50 几乎无差别**——证明多分片的代价在长尾而非均值 ② **多分片聚合失真实测**：单分片 `doc_count_error_upper_bound: 0` 且每桶精确 100；50 分片误差上界 **82**，BRAND-0 只剩 95、BRAND-1 只剩 90——本课最有力的一组证据 ③ **乐观锁边界**：正确 `_seq_no` 更新成功（0→8）、过期 `_seq_no` 再更新 **HTTP 409 `version_conflict_engine_exception`**，证明 ES 事务只到单文档 ④ **无跨表关联**：ES\|QL 写 SQL 风格 JOIN 报 `parsing_exception: mismatched input 'o'`，对照普通 ES\|QL 正常返回——四选一替代方案（反范式/nested/parent-child/应用层拼） ⑤ **近实时五连测**：默认写后立刻搜 **0 条** → 等 1.2 秒 1 条 → `refresh=true` 立刻可见 → **GET 单文档实时**（走 translog）vs SEARCH 近实时（走段）→ `refresh_interval=-1` 后即使等待也搜不到 ⑥ **9.3 考纲逐条本机探测**：Architecture 节点角色（三节点均 `dim`）、ES\|QL、kNN、ILM（48 个策略）、别名、ingest pipeline、async search、快照（`l11_repo`）、RBAC（`l13_reader` 读 24 条）、IK（苹果/手机）**全部可用**；**`semantic_text` ❌**（basic license，`non-compliant for [inference]`，建时不报错写时才炸）、Streams ❌（未启用）；已移除的 runtime fields **本机仍能跑**（API 在、只是不考）。**踩坑两则（已写入讲义当备考教材）**：① **kNN 字段名写错会静默返回 0 条、不报错**（实测把 `embedding` 写成 `my_vector` 得 0 条，改回后得 3 条）② **runtime field 偶发 `script_exception`，重试即可**（4 个索引重跑全通过，非字段缺值；真规律是 `doc[]` 读列存、`params._source` 读原始 JSON，可用 `value_count` 探测）。**时效性**：Elastic 认证考纲 2026-09-01 起从 8.15 升级至 9.3（官方考试页 + FAQ 双源确认），已按官方 `*` / `-` 标记给出新增/保留/移除三清单。**收官交付**：42 知识点全景地图 + 14 课一句话索引 + 十个结论 + 四层排查法 + 本机环境清单

> 大纲是活文档，每次调整在此追加一行（与 `01-学习路径总览.md` / 阶段 `overview.md` 同步更新；结构性调整须重新过评审）。

| 日期 | 调整内容 | 原因 |
|------|----------|------|
| 2026-08-28 | 初始大纲落盘（5 阶段 / 14 课 / 42 知识点） | 用户要求系统学 ES，零基础 + 四大目标全要 |
| 2026-08-28 | 实操主线由 Docker 改为原生 zip 发行版 | 环境探测：本机 Docker 未安装，WSL 虽有但原生 zip 更直接且自带 JDK |
| 2026-09-01 | **增补课 15《索引管理与生命周期策略》**（阶段 4，4 知识点），规模 14 课/42 知识点 → **15 课/46 知识点** | 用户指出「教程缺少 ES 索引管理及其相关策略的内容」。索引模板体系、别名、ILM、索引运维在原大纲中确实没有独立成课，属内容缺口 |
| 2026-09-01 | **补 Phase 5 三份收尾产物**（08 实战经验 / 09 排障速查手册 / 10 场景解法库） | 用户指出「还少了部分内容，比如场景库」。根因是 Phase 1 未给收尾产物占位，断点续学时未被发现；已在评审清单补占位并写入流程教训 |
| 2026-09-02 | **补 Phase 4 课程手册**（`final-课程手册.md` + `assets/learning-path-overview.svg`） | 用户指出「elasticsearch 好像缺少课程手册」。对比 kafka / redis / rabbitmq 均已有 `final-课程手册.md`，ES 独缺；另发现 ES 目录也缺根级学习路径 SVG，一并补齐 |
| 2026-09-02 | **补 Phase 3 结课综合实战项目**（`projects/商品搜索服务/`） | 用户要求「补一下实战项目」。评审清单里该条目一直未勾选且 `projects/` 不存在，属结构缺口；已产出并勾选，课程不再有未完成环节 |

## 事实核查记录

| 条目 | 结论 | 置信度 | 核查时间 |
|------|------|--------|----------|
| 当前 ES 最新稳定版 | **9.5.2**（2026-08-20 发布）；官方 Windows zip 直链为 **9.5.1**（`https://artifacts.elastic.co/downloads/elasticsearch/elasticsearch-9.5.1-windows-x86_64.zip`） | ✅ 高 | 2026-08-28 |
| 版本支持线（endoflife.date + versionlog 交叉） | 9.5 支持中（2026-08-04 起）、9.4 支持中（2026-05-05 起）、9.3 已于 2026-08-04 结束支持、8.19 支持至 2027-07-15、7.x 于 2026-01-15 结束 | ✅ 高 | 2026-08-28 |
| ES 起源（Shay Banon，前身为 Compass 2004，ES 首个版本 2010-02-08；Elastic NV 2012 成立；2015 更名 Elastic；2018-10-05 纽交所上市） | 已核实 | 高 | 2026-08-28 |
| 许可证变更（7.11 起由 Apache 2.0 改为 SSPL + Elastic License 双许可；AWS 于 2021-04 分叉为 OpenSearch；8.16.0（2024-08）起新增 AGPL 第三选项） | 已核实 | 高 | 2026-08-28 |
| 倒排索引与分词（正向索引 vs 倒排索引；Lucene 底层；标准分词器逐字拆中文；IK 两种模式 ik_max_word 最细粒度 / ik_smart 最粗粒度；最佳实践：索引用 max_word、查询用 smart） | 已核实 | 高 | 2026-08-28 |
| 相关性算法 | ES 默认用 **BM25**（8.x 起为默认，替代 TF-IDF），考虑词频饱和度与文档长度归一化 | 已核实 | 高 | 2026-08-28 |
| Windows zip 安装与安全默认（9.x 首次启动自动生成 elastic 密码 + TLS 证书 + Kibana enrollment token（30 分钟有效）；Windows 服务方式启动 TLS 不自动配置、需用 elasticsearch-reset-password 生成密码；含自带 OpenJDK，无需手动配 JAVA_HOME） | 已核实（官方 zip-windows 文档 + 多来源交叉） | 高 | 2026-08-28 |
| Elastic 认证（Elastic Certified Engineer；3 小时、纯实操 performance-based、约 10-12 任务、$400；**2026-09-01 起考纲由 8.15 升级到 9.3**；考试可查官方文档；通过率约 50-60%） | 已核实（官方培训页 + 多来源交叉） | 高 | 2026-08-28 |
| **本机实测：ES 9.5.1 Windows zip 安装全链路**（zip 677,799,024 字节 / 646 MB；顶层目录 bin·config·jdk·lib·modules·plugins·logs，**无 data 目录**（首次启动后创建）；自带 OpenJDK **26.0.1**；63 GB 机器自动分配堆 **31 GB**（`-Xms31744m -Xmx31744m`）；首次启动后 yml 被自动写入 xpack.security.enabled/http.ssl/transport.ssl/cluster.initial_master_nodes/http.host） | **本机实测**（2026-08-31） | 高 | 2026-08-31 |
| **无 TTY 时不生成 elastic 密码**（日志原文：`Auto-configuration will not generate a password ... as we cannot determine if there is a terminal attached`）→ 重定向输出会跳过密码生成，须 `elasticsearch-reset-password` 重设 | 本机实测 | 高 | 2026-08-31 |
| **Windows curl 8.19.0 用 Schannel 后端**：`--cacert http_ca.crt` 报 `(60) schannel: the revocation status is unknown`，须加 `--ssl-revoke-best-effort` 才能通过；`http://` 访问 HTTPS 端口报 `(52) Empty reply from server` | 本机实测 | 高 | 2026-08-31 |
| **Windows PowerShell 5.1.26100 的 `Invoke-RestMethod` 连不上 ES 9.5.1 HTTPS**（无 `-SkipCertificateCheck` 参数；绕过证书回调 + 强制 TLS 1.2/1.3 均报"基础连接已经关闭: 发送时发生错误"）；本机未装 pwsh 7 | 本机实测 | 高（**根因未定位**，已如实标注） | 2026-08-31 |
| **CRUD 实测**：`_version` 随 created/updated/deleted 递增（1→2→3→4，删除后不归零）；自动创建索引默认 1 副本 → 单节点集群 yellow（active_shards_percent 83.3%），`_cluster/allocation/explain` 返回 `reason: INDEX_CREATED` + `last_allocation_status: no_attempt`，改副本为 0 后回 green | 本机实测 | 高 | 2026-08-31 |
| **动态映射实测**：`19.9`→`float`（非 double）；字符串→`text` + `keyword` 子字段（ignore_above 256）；`"2026-08-31"`→`date`；`true`→`boolean` | 本机实测 | 高 | 2026-08-31 |
| **近实时实测**：写入后立刻搜 hits.total=1，`sleep 1.2` 后=2，`?refresh=true` 写入后立刻=3 | 本机实测 | 高 | 2026-08-31 |
| **倒排索引起源**（历史事实，已联网核实）：思想可追到图书馆卡片目录；**20 世纪 50 年代 IBM 的 Hans Peter Luhn** 提出用词对文档建索引、按匹配度检索的方法，被视为倒排文档技术雏形；**60 年代 Gerard Salton** 在康奈尔 SMART 系统中形式化；1999 Apache Lucene 引入开源 | 已核实（百度百科 + 科普中国/维基同源 + 英文 Grokipedia 交叉） | 中高（**"谁第一个提出"存在分歧**：另有资料归功于 1950 年代的 Connie M. Weaver，课文中已如实标注分歧、不做定论） | 2026-08-31 |
| **IK 分词插件**：现由 **INFINI Labs** 维护（原 medcl 仓库已移交），安装地址 `https://get.infini.cloud/elasticsearch/analysis-ik/{ES版本}`，**版本号必须与 ES 严格一致**；9.5.1 构建存在（HTTP 200）；安装需**先停节点**，装完**必须重启**生效；安装输出两条警告（legacy Security Policy 文件 + 需 `outbound_network` 权限，后者用于远程词典热更新） | 本机实测（已真装，日志确认 `loaded plugin [analysis-ik]`） | 高 | 2026-08-31 |
| **中文分词实测对比**（同一句「中国人民银行发行数字货币」）：`standard` → 12 个**单字**；`ik_max_word` → 12 词（中国人民银行/中国人民/中国人/中国/国人/人民银行/人民/银行/发行/**行数**/数字/货币，含跨界噪声"行数"）；`ik_smart` → 4 词（中国人民银行/发行/数字/货币） | 本机实测 | 高 | 2026-08-31 |
| **中文搜索精度对比实测**（同样 5 条文档搜「人民」）：standard 索引**命中 4 条**（3 条不相关）；IK 索引（ik_max_word 索引 + ik_smart 搜索）**命中 1 条**（精确） | 本机实测 | 高 | 2026-08-31 |
| **索引时/搜索时分词不同步的实测铁证**：对 `news_ik` 同一句话，`_analyze` 用 **ik_max_word 切 12 词**，`_validate/query?explain=true` 显示搜索时只用了 **ik_smart 的 4 词**（title:中国人民银行 title:发行 title:数字 title:货币） | 本机实测 | 高 | 2026-08-31 |
| **段（Segment）与 refresh 实测**：`_cat/segments` 初始 1 段 `_0`（4 docs，committed=false）；`refresh_interval=-1` 后写入搜不到；`POST _refresh` 后新增段 `_1`（1 doc）→ 立刻可搜 | 本机实测 | 高 | 2026-08-31 |
| **词向量实测**：`_termvectors/1` 返回 doc1 的 title 共 **11 个去重 term**（原文 12 字），「行」词频 2、位置 [5, 7] | 本机实测 | 高 | 2026-08-31 |
| **分析器三段式实测**：`html_strip` + `standard` + `lowercase` + `stop` 作用于 `<p>The Quick BROWN Foxes jumped over the lazy dog</p>` → quick / brown / foxes / jumped / over / lazy / dog（**`over` 未被过滤**，不在 ES 默认 `_english_` 停用词表内） | 本机实测 | 高 | 2026-08-31 |
| **动态映射推断实测**（同一文档 7 字段）：`7999`→**`long`**（非 integer）、`19.9`→**`float`**（非 double，官方为省磁盘）、`"2026-08-31"`→`date`、`true`→`boolean`、字符串→`text`+`keyword`(ignore_above 256)、数组 `["手机","苹果"]`→`text`+`keyword`（**ES 映射无数组类型**） | 本机实测 | 高 | 2026-08-31 |
| **long 字段写入小数的截断实测**：字段为 `long`，写入 `19.9` → **`_source` 保留 19.9，但索引里是 `19`**。证据：`docvalue_fields` 返回 `{"fields":{"price":[19]}}`；`term:19` 命中 1 条；`term:19.9` 命中 0 条。**不报错、只静默算错** | 本机实测 | 高 | 2026-08-31 |
| **映射不可改实测**：`price` 由 `long` 改 `text` → `illegal_argument_exception: mapper [price] cannot be changed from type [long] to [text]`；但**新增字段允许**（`{"acknowledged":true}`）。官方：改类型须新建索引 + reindex；`ignore_above` 等少数参数可改 | 本机实测 + 官方文档 | 高 | 2026-08-31 |
| **dynamic 三态实测**：`strict` 写入未知字段 → `strict_dynamic_mapping_exception`（整篇拒收）；`false` → 写入成功、`_mapping` 无该字段、`_source` 有该字段、**搜索 0 条**（存在但未索引） | 本机实测 | 高 | 2026-08-31 |
| **text 聚合报错实测原文**（brand 为 text）：`Fielddata is disabled on [brand] in [l5_shop_bad]... Alternatively, set fielddata=true... Note that this can use significant memory.`；官方把它列为备选而非推荐。同聚合换 `keyword` 字段 → `buckets:[{"key":"Apple","doc_count":1}]` | 本机实测 | 高 | 2026-08-31 |
| **多字段实测**（`title` 主字段 ik_max_word + `title.std` standard + `title.raw` keyword，同一句「中国人民银行发行数字货币」）：主字段 12 词（含噪声「行数」）、`title.std` 12 单字、`title.raw` **1 整句**。搜索：`match title「中国人民银行」`→1 条；`term title.raw「中国人民银行」`→**0 条**（keyword 整句匹配非包含）；`term title.raw` 全句→1 条 | 本机实测 | 高 | 2026-08-31 |
| **ignore_above 实测**：`tag` 设 `ignore_above:5`，写 `abc`(3字符) 与 `abcdefghij`(10字符) → 两者均 `created` 成功，但 `term:"abc"` 命中 1 条、`term:"abcdefghij"` 命中 **0 条**（超长不进索引，值仍在 `_source`） | 本机实测 | 高 | 2026-08-31 |
| **动态模板实测**：`match_mapping_type:string` → keyword 的模板，使 `author`/`status` 变为 `keyword`，而**显式定义在 `properties` 里的 `content` 保持 `text`+IK**（优先级：显式 properties > 动态模板 > 默认规则） | 本机实测 | 高 | 2026-08-31 |
| **索引模板实测**：`_index_template` 配 `index_patterns:["l5_logs-*"]`，之后 `PUT l5_logs-2026.08.31`（不带任何 mapping）→ 自动套用模板的 settings/mappings/dynamic_templates。**仅对之后创建的索引生效** | 本机实测 | 高 | 2026-08-31 |
| **date 字段拒收中文实测**：写入 `"昨天"` → `document_parsing_exception`，`failed to parse date field [昨天] with format [strict_date_optional_time||epoch_millis]` | 本机实测 | 高 | 2026-08-31 |
| **BM25 公式可手算复现，误差极小**（实测）：搜索 `l7_news` 的 `content` 字段查「苹果」，doc1（freq=4, dl=23, avgdl=17.5, n=4, N=6）：`idf = log(1+(6-4+0.5)/(4+0.5)) = 0.441833`，`tf = 4/(4+1.2×(1-0.75+0.75×23/17.5)) = 0.729547`，`score = 2.2 × 0.441833 × 0.729547 = 0.709143`；**ES explain 返回 0.7091，完全一致** | 本机实测 | 高 | 2026-08-31 |
| **explain 里的 `boost = 2.2` 是 k1+1 常量，不是用户设的字段权重**（实测）：未写任何 boost 时 explain 显示 `boost → 2.2`；写 `title^3` 后变成 `6.6`（= 2.2 × 3）。**字段权重是乘在这个 2.2 上的** | 本机实测 | 高 | 2026-08-31 |
| **`dl` 是字段总词项数（含重复），不是去重词数**（实测）：doc1 的 content **去重后 18 个词**，但 explain 里 `dl = 23`。6 篇文档 content 总词项数分别为 23/12/18/18/22/12，合计 105，`105/6 = 17.5` = explain 的 avgdl | 本机实测 | 高 | 2026-08-31 |
| **每个字段独立统计 IDF**（实测）：同一批文档，title 字段里「苹果」出现在 3 篇（idf=0.6931），content 字段里出现在 4 篇（idf=0.4418）。所以短字段天然占便宜——既因长度归一化，也因 IDF | 本机实测 | 高 | 2026-08-31 |
| **`log1p` 用 log10 且 factor 在 log 之前乘**（实测纠错）：`field_value_factor` 配 `modifier: log1p, factor: 0.5` 时，explain 原文为 `log1p(doc['views'].value * factor=0.5)`。正确公式 = `BM25基础分 × log10(1 + views × factor)`。用自然对数会算错（doc6：ln 得 5.9605，log10 得 2.5886，实测 2.5886323）。四篇文档交叉验证误差 ~1e-7 | 本机实测 | 高 | 2026-08-31 |
| **ES 9.5.1 不能对 `_id` 排序**（实测报错）：`{"sort":[{"views":"desc"},{"_id":"asc"}]}` → `illegal_argument_exception: Fielddata access on the _id field is disallowed, you can re-enable it by updating the dynamic cluster setting: indices.id_field_data.enabled`。**改用 `_doc` 做决胜键** | 本机实测 | 高 | 2026-08-31 |
| **`from + size` 上限 10000 的实测报错原文**：`illegal_argument_exception: Result window is too large, from + size must be less than or equal to: [10000] but was [10010]. See the scroll api for a more efficient way to request large data sets. This limit can be set by changing the [index.max_result_window] index level setting` | 本机实测 | 高 | 2026-08-31 |
| **`sort` 后 `_score` 变 null**（实测）：加 `"sort":[{"views":{"order":"desc"}}]` 后，返回文档的 `_score` 字段全部为 `null`——指定了排序后 ES 不再计算相关性 | 本机实测 | 高 | 2026-08-31 |
| **IK 不认识「雷军」，切成「雷」+「军」**（实测）：`_analyze` 显示 `ik_smart` 把「雷军」切成 `雷 | 军` 两个词，导致搜「雷」也能召回该文档。多词查询分数相加（1.5226 + 1.5226 = 3.0453）。真人名词需加自定义词典（课 5 提过 `IKAnalyzer.cfg.xml`） | 本机实测 | 高 | 2026-08-31 |
| **`boost_mode: multiply` 只放大不颠覆排序**（实测）：`function_score` 按 views 加权后，doc5（views=20000 最高）因 BM25 基础分仅 0.3998，加权后仍是最后一名。想让热度主导排序须用 `replace` | 本机实测 | 高 | 2026-08-31 |
| **PIT 与 scroll 实测可用**：`POST /l7_news/_pit?keep_alive=1m` 开 PIT → `/_search`（URL 不带索引名）查询 → `DELETE /_pit` 关闭返回 `{"succeeded":true,"num_freed":1}`；scroll 第 1 批 `['1','2']`、第 2 批 `['3','4']`，清理返回 `{"succeeded":true,"num_freed":1}` | 本机实测 | 高 | 2026-08-31 |
| **fuzziness 容错实测**：搜 `iPhone` → 1 条（1.365）；拼错成 `Iphane` 不加容错 → 0 条；加 `fuzziness:auto` → 1 条（1.1375） | 本机实测 | 高 | 2026-08-31 |
| **存在：高亮默认做 HTML 转义**（`encoder: default`），改成 `html` 会引入 XSS 风险（原文含用户输入时） | 官方文档 | 高 | 2026-08-31 |
| **索引名必须全小写**（实测踩坑）：`PUT l5_noDyn` → `invalid_index_name_exception: Invalid index name [l5_noDyn], must be lowercase` | 本机实测 | 高 | 2026-08-31 |
| **match vs term 决定性对照实测**（索引 l6_shop，name=text+IK）：`match「苹果手机」`→**6 条**；`term「苹果手机」`→**0 条**；`term「苹果」`→**3 条**；`term name.raw` 整句→**1 条**；`term brand「Apple」`→3 条；`term brand「apple」`→**0 条**（keyword 不归一化，大小写敏感） | 本机实测 | 高 | 2026-08-31 |
| **match 分数 = 各词项得分之和**（实测拆解）：`term「苹果」→iPhone 15 Pro 得 0.9894`，`term「手机」→同文档得 0.7262`，`match「苹果手机」→同文档得 1.7156`（= 0.9894 + 0.7262）。印证官方 more-matches-is-better：各 must/should 子句分数相加得总分 | 本机实测 + 官方文档 | 高 | 2026-08-31 |
| **filter 不影响打分的决定性证据**（实测三组对照）：搜「手机」条件下——[A] brand 放 `must` → **1.6706**；[B] brand 放 `filter` → **0.7262**；[C] 完全不加品牌条件 → **0.7262**。[B] 与 [C] 完全一致，证明 filter 只筛选不打分。另一组 `filter(stock>=20)` 交叉验证：四条命中文档分数逐一未变 | 本机实测 | 高 | 2026-08-31 |
| **minimum_should_match 默认值陷阱实测**：bool 中同时有 `must`+`should` 时默认值为 **0**（should 只加分不筛选，实测命中 4 条含非旗舰商品）；显式设 `minimum_should_match:1` 后 → **2 条**（非旗舰被筛掉）。**关键：分数不变（都是 1.9135），变的是命中数量**。官方：有 should 且无 must/filter 时默认 1，否则默认 0 | 本机实测 + 官方文档 | 高 | 2026-08-31 |
| **filter 缓存在小数据量下测不出**（诚实结论）：8 条文档反复执行相同 filter 查询，`_nodes/stats/indices/query_cache` 的 `hit_count`/`miss_count`/`cache_size` **始终为 0**；`took` 稳定在 1-2ms 无差异。**不能在讲义中宣称 filter 更快，只能说"可缓存，生产规模才有意义"** | 本机实测 | 高 | 2026-08-31 |
| **match_phrase 与 IK 的交互实测**：`match_phrase「苹果手机」`→**0 条**（IK 切成「苹果」「手机」两词，而原文"苹果 iPhone 15 Pro 手机"中两词不相邻）；`match_phrase「iPhone 15」`→1 条（相邻且顺序一致，score=3.7542）；加 `slop:3` 后「苹果 手机」→1 条（score=0.6672） | 本机实测 | 高 | 2026-08-31 |
| **profile API 在 9.5.1 的写法**：写成 URL 参数 `?profile=true` → `illegal_argument_exception: request [/_search] contains unrecognized parameter: [profile]`。**正确写法是放在请求体里** `"profile":true`。但本机 9.5.1 返回的 `profile.shards` 为空数组（量级太小无明细），故本课未用 profile 做性能论证 | 本机实测 | 中 | 2026-08-31 |
| **exists 与 multi_match 实测**：`exists:brand`→8 条（max_score 恒为 1.0）；`exists:missing_field`→0 条；`multi_match「苹果」`跨 name+brand→3 条（AirPods 1.071 / iPhone 0.9894 / MacBook 0.8054） | 本机实测 | 高 | 2026-08-31 |
| **sort 会覆盖相关性排序**：一旦指定 `"sort"`，返回的 `_score` 变为 **null**（实测），因为已明确排序规则，ES 不再计算相关性分数 | 本机实测 | 高 | 2026-08-31 |
| **聚合必须用 keyword 的决定性实测**（同一 brand 字段两种 mapping）：`l8_text_demo`（brand=text+fielddata）→ `['为','华','小','果','米','苹']` **6 个碎桶**；`l8_orders_v2`（brand=keyword）→ `['华为','小米','苹果']` **3 个正确桶**。IK 把「苹果」切成「苹」「果」，聚合按词项分组而非按完整值 | 本机实测 | 高 | 2026-08-31 |
| **text 聚合报错原文**（9.5.1，索引 l8_text_demo）：`Fielddata is disabled on [brand] in [l8_text_demo]. Text fields are not optimised for operations that require per-document field data like aggregations and sorting... Alternatively, set fielddata=true... Note that this can use significant memory` | 本机实测 | 高 | 2026-08-31 |
| **ES 9.5.1 的 `cumulative_sum` 父聚合必须是指方图**（实测报错）：挂在 `terms` 桶下 → `Validation Failed: 1: cumulative_sum aggregation [累计] must have a histogram, date_histogram or auto_date_histogram as parent`。原因：terms 是名义类别无顺序，累计无意义 | 本机实测 | 高 | 2026-08-31 |
| **`bucket_script` 不能放顶层**（实测报错）：放在 `aggs` 顶层引用桶内指标 → `Validation Failed: 1: No aggregation found for path [销售额]`。它必须挂在**桶内部**与所引指标**同级**，`buckets_path` 相对父桶解析 | 本机实测 | 高 | 2026-08-31 |
| **ES 9.5.1 的 ES\|QL 不支持中文列名**（实测报错）：`STATS 订单数 = COUNT(*) BY brand` → `parsing_exception: line 1:27: token recognition error at: '订'`。但**中文值完全正常**（`BY brand` 返回苹果/华为/小米），字符串字面量也正常（`WHERE brand == "苹果"`）。**别名用英文、值用中文** | 本机实测 | 高 | 2026-08-31 |
| **ES\|QL 报错位置按字符计数**（实测互证）：同一错误在 `l8_orders_v2` 报 `line 1:27`、在 `l8_orders` 报 `line 1:24`，**差 3 恰为两索引名长度差** | 本机实测 | 中 | 2026-08-31 |
| **ES\|QL 与 DSL 结果完全一致**（实测互证）：`STATS ... BY brand` 与 `terms`+`sum` 子聚合，两者均返回苹果 80390 / 华为 64179 / 小米 45488，均价 8849.0 / 6497.625 / 4236.5；`BUCKET(sale_date, 1 week)` 与 `date_histogram` 均返回同样的 5 个周桶 | 本机实测 | 高 | 2026-08-31 |
| **聚合 `avg` 按文档算而非按值去重**（手算验证）：苹果 8 单价格 [4799, 5999, 7999, 8999, 8999, 9999, 10999, 12999]，含两个同为 8999 的文档仍按 2 条计，`70792 / 8 = 8849.0` 与 ES 返回值完全一致 | 本机实测 | 高 | 2026-08-31 |
| **array 字段 terms 聚合计数之和 ≠ 文档数**（实测）：24 条订单的 `tags` 字段 TOP5 计数之和为 **22**，因为单条文档多标签时每个标签独立计数，属正常现象非 bug | 本机实测 | 中 | 2026-08-31 |
| **`terms` 聚合默认只返回 Top 10 桶**（实测）：设 `size:2` 时返回「华为 8、小米 8」，而 `sum_other_doc_count = 8`（苹果那 8 条被吞）。该字段 >0 即漏桶信号 | 本机实测 | 高 | 2026-08-31 |
| **9.x 已移除 `node.max_local_storage_nodes`**（实测报错）：`unknown setting [node.max_local_storage_nodes] please check that any required plugins are installed, or check the breaking changes documentation for removed settings`。**单机多节点集群不需要这个配置**，直接指定不同端口即可 | 本机实测 | 高 | 2026-08-31 |
| **单机建多节点集群的完整可行路径**（实测）：robocopy 复制 ES 目录（**不可用 `cp -al` 硬链接**——keystore 会因共享 inode 拒绝覆盖，报 `will not overwrite keystore ... changing the file owner, exit code 78`）；各节点须**删掉 `config/elasticsearch.keystore`**（否则残留 SSL 配置报 `invalid configuration for xpack.security.transport.ssl`）；`path.data`/`path.logs` 用 **Windows 风格路径**（bat 由 cmd 解析，写 `/mnt/d/...` 会被拼成 `D:\mnt\d\...`）；学习环境建议 `xpack.security.enabled: false` 省去证书密码 | 本机实测 | 高 | 2026-08-31 |
| **多分片聚合误差真实复现**（1245 条 / 1200+ 基数 / 3 分片，`l9_trap2`）：`doc_count_error_upper_bound = 3`，**3 次执行稳定复现**；`sum_other_doc_count = 1193`；各分片 415 条。`shard_size` 25→3、50→3、100→3、**500→0**、1000→0。**单分片对照组（同数据 1245 条）该值恒为 0** —— 课 8 伏笔的正面回答：单分片无跨分片合并，不存在漏报 | 本机实测 | 高 | 2026-08-31 |
| **低基数下多分片聚合误差为 0**（实测反例）：3 个品牌 / 30 条 / 3 分片，`doc_count_error_upper_bound = 0`。因为默认 `shard_size`（约 `size×1.5+10`）远大于实际基数，每个分片都能报全。**要看到误差必须把基数撑破 shard_size 窗口** | 本机实测 | 高 | 2026-08-31 |
| **路由公式可控且可复现**（实测）：`routing=user1` 连续写入的文档**全部落同一分片**（实测全在分片 1，其余两个分片 0 条）。用途：相关数据同分片 → 查询只打一个分片；代价：数据倾斜 | 本机实测 | 高 | 2026-08-31 |
| **自然分布天然不均衡**（实测）：3 分片写入 12 条 → **6/2/4**；后续持续写入 → 8/8/4。**哈希分布小样本偏斜是常态**，分片落点还会随节点重启重新均衡 | 本机实测 | 高 | 2026-08-31 |
| **分片数不可改的报错原文（9.5.1）**：`Can't update non dynamic setting(s) [[index.number_of_shards]] for open indices [...]. The setting(s) [[index.number_of_shards]] cannot be modified on an index once it is created. You will need to create a new index with the desired setting(s) and reindex your data` | 本机实测 | 高 | 2026-08-31 |
| **副本数可动态改**（实测）：`number_of_replicas` 0→1→2，分片总数 3→6→9，全程集群保持 green | 本机实测 | 高 | 2026-08-31 |
| **故障转移的决定性对照**（实测，停 node-3）：**有副本索引（rep=1）→ yellow 且数据零丢失**（`l9_orders` 22 条全在、写入照常 `successful shards=2/2`）；**0 副本索引 → red**（`l9_hi`/`l9_trap2`/`l9_agg`/`l9_trap`/`l9_nrt`/`l9_rep0`/`l9_audit_nrt` 共 7 个，`unassigned_primary_shards = 7`）。**red = 有数据真读不到；yellow = 数据都在只是副本无处安放** | 本机实测 | 高 | 2026-08-31 |
| **节点恢复后集群自动 green**（实测）：重启 node-3，无需人工干预，全部索引自动回 green、`unassigned_shards` 归零 | 本机实测 | 高 | 2026-08-31 |
| **写入响应 `_shards.total` = 主分片1 + 副本数**（实测）：1 副本索引写入返回 `{"total":2,"successful":2,"failed":0}` | 本机实测 | 高 | 2026-08-31 |
| **近实时实测（3 节点集群）**：写入后立刻 `_count` = **0**；等 2 秒后 = 1；`?refresh=true` 写入后立刻 = 2。与单节点结论一致 | 本机实测 | 高 | 2026-08-31 |
| **translog 存在性实测**：`_stats?level=shards` 返回 `translog.operations` 与 `translog.size_in_bytes`（l9_orders 实测 2 条 / 520 bytes） | 本机实测 | 中 | 2026-08-31 |
| **yellow 的精确特征（不丢数据）**：把有副本索引的 `number_of_replicas` 设为 3（超过节点数），集群转 yellow，`unassigned_shards=3` 但 **`unassigned_primary_shards=0`**；`_count` 前后完全一致。`_cluster/allocation/explain` 显示决策器 `same_shard` 逐节点拒绝（"a copy of this shard is already allocated to this node"）——即课 9「主副本永不同节点」铁律在起作用 | 本机实测 | 高 | 2026-08-31 |
| **节点数据永久销毁 = 0 副本分片永久丢失**（决定性实验）：真杀 node-3 进程后**移走其整个 data 目录**（模拟硬盘报废），空盘重启后 `unassigned_primary_shards` 仍为 7。数据核对：`l9_hi` 300→**197**、`l9_trap2` 1245→**830**、`l9_trap` 63→**42**、`l9_agg` 30→**18**、`l9_nrt` 2→**0**；而 1 副本的 `l9_orders` **22 条零丢失**。查询丢失分片报 `no_shard_available_action_exception`。**结论：副本不是性能优化，是数据的第二份存在** | 本机实测 | 高 | 2026-08-31 |
| **ES 官方对 red 的建议原文**（`_cluster/allocation/explain`）：`reason: NODE_LEFT` + `last_allocation_status: no_valid_shard_copy`；allocate_explanation 原文含 **"If no such node is available, restore this index from a recent snapshot"** —— 课 10 遗留悬念直通课 11 快照恢复 | 本机实测 | 高 | 2026-08-31 |
| **`allocate_stale_primary` 在无旧副本时也会失败**（实测报错原文）：`illegal_argument_exception: No data for shard [0] of index [l9_agg] found on any node`。此时只剩 `allocate_empty_primary`（返回 `acknowledged:true`，集群转 green 但**该分片数据永久清空且不留痕迹**） | 本机实测 | 高 | 2026-08-31 |
| **反复故障转移会让分片"静默消失"**：课 10 实验中恢复 node-3 的 data 备份后，5 个 UNASSIGNED 分片**数据依然找不回来**——逐一核对三节点磁盘的 `data/indices/<UUID>/<shard>` 目录，确认这 5 个分片编号在**任何节点磁盘上都不存在**（它们在课 9 更早的实验中就已丢失，只是当时被重新分配成空分片掩盖）。**教训：每次故障转移后必须核对 `docs.count`，只看颜色会漏** | 本机实测 | 高 | 2026-08-31 |
| **法定人数防护实证（脑裂）**：3 节点集群逐个停到只剩 1 个节点（1/3 < 法定人数 2）时，`_cluster/health` 返回 **HTTP 503**（而非 red），少数派拒绝服务。反之停掉主节点 node-1（剩 2 个 ≥ 2）时，**node-2 自动接任主节点（`*`）且写入正常**（`result:created`, `_shards.successful:2/2`） | 本机实测 | 高 | 2026-08-31 |
| **磁盘水位线默认值（9.5.1）**：`low=85%` / `high=90%` / `flood_stage=95%`，并各带 `max_headroom`（low 200GB / high 150GB / flood 100GB），实际生效规则为百分比与剩余空间**取更严格者**（大盘百分比会失真）。`flood_stage` 触发后索引被设 `read_only_allow_delete`，**清磁盘后须手动解锁**：`{"index.blocks.read_only_allow_delete": null}`（值为 null 而非 false） | 本机实测 | 高 | 2026-08-31 |
| **分片数不是越多越好（预热后严谨测量）**：相同 3000 条数据，1/3/50 分片的聚合延迟为 **2–2.5 / 2–3 / 5 ms**（各跑 20 次、两轮交叉验证），50 分片约慢 **2 倍**；索引存储 **77.3 / 88.9 / 359.9 kb**，50 分片存储开销约 **4 倍**。⚠️ **测量教训：直接跑 5 次取平均会被冷启动污染**（首轮实测高达 54–65ms），曾得出"50 片慢 3.6 倍"的错误结论，**必须预热后测量** | 本机实测 | 高 | 2026-08-31 |
| **routing 会造成严重数据倾斜**（实测）：用 `R0`/`R1`/`R2` 三个 routing 值写入，3 分片分布为 **500 / 1000 / 0**（分片 2 一条都没有）。用 `_search_shards?routing=xxx` 可精确探测落点（实测 `R0`→分片0、`R1`→分片1、`RC`→分片2） | 本机实测 | 高 | 2026-08-31 |
| **本机未安装 Git Bash**（重要环境约束）：`C:\Program Files\Git\bin\bash.exe` 等三个常见路径均不存在；PATH 中的 `bash.exe` 是 `C:\Windows\system32\bash.exe`（WSL 启动器），调用无响应。**课 3 定的"Git Bash 写法"在本机无法执行** | 本机实测 | 高 | 2026-08-31 |
| **Windows curl 的 JSON 传参四种写法实测**：`-d '{"key":1}'`（Git Bash 风格）在 PowerShell/CMD **均失败**；`-d "{`"key`":1}"`（PS 反引号转义）**失败**；`-d "{\"key\":1}"`（CMD 反斜杠转义）**仅 CMD 成功、PowerShell 失败**；`--data-binary @文件.json` **两者均成功**。报错原文：`was expecting double-quote to start field name`（curl 吃掉双引号）。**PowerShell 下 JSON 一律用文件传参** | 本机实测 | 高 | 2026-08-31 |
| **Ingest Pipeline 鏀圭殑鏄?_source 鏈韩**锛堝疄娴嬶級锛氬啓鍏?{brand:apple, price_str:8999}锛岀粡 pipeline锛坲ppercase + convert + remove + script锛夊悗鍙栧洖 {brand:APPLE, price:8999, price_level:high}锛宲rice_str 鍦?_source 涓?*褰诲簳涓嶅瓨鍦?* | 鏈満瀹炴祴 | 楂?| 2026-08-31 |
| **_update_by_query 榛樿閲嶈窇 default_pipeline**锛堝疄娴嬫姤閿欏師鏂囷級锛氱粦瀹?default_pipeline 鐨勭储寮曟墽琛?_update_by_query 鈫?total:2, updated:0锛宖ailures 鎶?illegal_argument_exception: field [price_str] not present as part of path [price_str]锛堣瀛楁宸茶 pipeline 鐨?remove 鍒犳帀锛夈€傚姞 ?pipeline=_none 鍚?updated:2 | 鏈満瀹炴祴 | 楂?| 2026-08-31 |
| **grok 涓嶅尮閰嶄細鎷掓敹鏁寸瘒鏂囨。**锛堝疄娴嬶級锛欳OMBINEDAPACHELOG 瑕佹眰鏃ュ織鍚?referrer/agent锛岀己瀛楁鏃舵姤 Provided Grok expressions do not match field value锛涙敼鐢?COMMONAPACHELOG 鍚庤В鏋愬嚭 clientip/verb/request/response/bytes 绛?8 瀛楁 | 鏈満瀹炴祴 | 楂?| 2026-08-31 |
| **on_failure 姝讳俊绱㈠紩**锛堝疄娴嬶級锛歡rok 鐨?on_failure 涓?set _index=failed-logs锛岃鑼冩棩蹇楁甯歌В鏋愯繘鍘熺储寮曪紝鑴忔暟鎹**鑷姩鏀硅矾鐢?*鍒?failed-logs 涓斿啓鏈?grok_error 瀛楁锛屽師鏂囦繚鐣?| 鏈満瀹炴祴 | 楂?| 2026-08-31 |
| **reindex 鍙敼鍒嗙墖鏁?*锛堝疄娴嬶級锛氭簮 l11_shop锛? 涓?0 鍓級鈫?鐩爣 l11_shop_v2锛?*3 涓?1 鍓?*锛夛紝created:3 鏁版嵁瀹屾暣锛屽厬鐜拌 9銆屽垎鐗囨暟涓嶅彲鏀归櫎闈?reindex銆?| 鏈満瀹炴祴 | 楂?| 2026-08-31 |
| **path.repo 蹇呴』閲嶅惎鎵嶇敓鏁?*锛堝疄娴嬫姤閿欏師鏂囷級锛歽ml 宸插啓鍏ヤ絾鏈噸鍚椂娉ㄥ唽浠撳簱 鈫?repository_exception: [l11_repo] location [l11_backup] doesnt match any of the locations specified by path.repo because this setting is empty锛涢噸鍚?3 鑺傜偣鍚庢敞鍐屾垚鍔燂紝_verify 杩斿洖 3 鑺傜偣鍏ㄩ儴閫氳繃 | 鏈満瀹炴祴 | 楂?| 2026-08-31 |
| **蹇収鎭㈠鍚庢暟鎹笌鍒嗙墖閰嶇疆鍘熸牱杩樺師**锛堝疄娴嬶級锛氬垹闄?l11_shop_v2(3 鏉? 涓?l9_orders(23 鏉? 鈫?浠?snap_1 鎭㈠ 鈫?鏉℃暟**涓€鏉′笉宸?*锛? 涓?1 鍓厤缃師鏍峰甫鍥炲綊锛沖source 涓?migrated_from锛坮eindex 鎵撴爣锛夈€乽pdated:true锛坲pdate_by_query 鎵撴爣锛夈€乥rand 澶у啓锛坧ipeline锛変笁灞傜棔杩瑰叏鍦?| 鏈満瀹炴祴 | 楂?| 2026-08-31 |
| **蹇収鏄閲忕殑锛屽垹鏃у揩鐓т笉鎹熷潖鏂板揩鐓?*锛堝疄娴嬶級锛氬垹鎺夋渶鏃?snap_1 鍚庝粨搴?106.7鈫?3.3 KB锛堜粎閲婃斁 23.4 KB锛岃鏄庢鏂囦欢琚悗缁揩鐓у鐢級锛涙鏃朵粠 snap_4 鎭㈠浠嶅緱 24 鏉″畬鏁存暟鎹€?*姣忎釜蹇収閫昏緫瀹屾暣锛岀墿鐞嗗叡浜笉鍙樻** | 鏈満瀹炴祴 | 楂?| 2026-08-31 |
| **璺ㄩ泦缇ゆ仮澶嶅彲琛?*锛堝疄娴嬶級锛?201 闆嗙兢锛坙9-cluster锛夊垱寤虹殑 snap_4锛屽湪 9200 闆嗙兢娉ㄥ唽鍚屽悕浠撳簱鍚?*鍙洿鎺ヨ鍙栧苟鎭㈠**锛?4 鏉°€? 涓?1 鍓€佽繛 _id锛圥j1oV6ABHncISD6JEHB5锛夐兘鍘熸牱淇濈暀銆傜敤 rename_pattern/rename_replacement 鎭㈠涓烘柊绱㈠紩鍚嶉伩鍏嶈鐩?| 鏈満瀹炴祴 | 楂?| 2026-08-31 |
| **SLM 鑷姩鍛藉悕涓庡畾鏃舵墽琛?*锛堝疄娴嬶級锛氱瓥鐣?name 妯℃澘 <l11-daily-snap-{now/d}> + schedule: 0 */30 * * * ?锛屾墜鍔?_execute 杩斿洖 l11-daily-snap-2026.08.31-b78gvwsxrz6tkf3una6eaw锛涗笖**鏈共棰勬儏鍐典笅浜?10:59:59 鑷姩鍙堟墽琛屼竴娆?*锛堟暣鍗婄偣锛夛紝璇佹槑瀹氭椂璋冨害鐪熷湪宸ヤ綔 | 鏈満瀹炴祴 | 楂?| 2026-08-31 |
| **url 鍙浠撳簱鍙涓嶅彲鍐?*锛堝疄娴嬶級锛氭敞鍐?type:url 鎸囧悜 file:/// 鍚庡彲鍒楀嚭鍏ㄩ儴蹇収锛涘啓鍏ユ椂鎶?illegal_argument_exception: [l11_readonly] cannot create snapshot in a readonly repository | 鏈満瀹炴祴 | 楂?| 2026-08-31 |
| **9.5.1 鍐呯疆浠撳簱绫诲瀷鎺㈡祴**锛堝疄娴嬶級锛歠s / url / s3 / gcs / azure / source **鍧囧凡璇嗗埆**锛堝悇鑷姤缂哄皯 bucket 绛夊弬鏁板嵆璇佹槑绫诲瀷瀛樺湪锛夛紱**hdfs 涓嶆敮鎸?*锛屾姤 repository type [hdfs] does not exist | 鏈満瀹炴祴 | 楂?| 2026-08-31 |
| **鍏冩暟鎹崯鍧忓彲鐢?_dangling 鏁戝洖**锛堝疄娴嬩簨鏁呭缃級锛氬崟鑺傜偣闆嗙兢鍥犲巻鍙查厤缃薄鏌?+ 璇惎鍔ㄥ鑷?CorruptStateException: the index containing the cluster metadata ... has been changed by an external force锛涙竻绌?data/_state 鍚庨噸鍚垚鍔燂紝GET /_dangling 鍒楀嚭 19 涓鍎跨储寮曪紝閫愪釜 POST /_dangling/<uuid>?accept_data_loss=true 鎭㈠锛屾枃妗ｆ暟涓庢。妗堝畬鍏ㄤ竴鑷达紙l6_shop 8 / l7_news 6 / l8_orders 24 / l8_orders_v2 24 / l8_text_demo 24 / news_ik 5锛?| 鏈満瀹炴祴 | 楂?| 2026-08-31 |
| **elastic 瀵嗙爜閲嶇疆闇€缁曡繃 TTY**锛堝疄娴嬶級锛歟lasticsearch-reset-password -u elastic -i 涓?-a 鍧囨姤 unable to read from standard input; is standard input open and a tty attached?锛汸owerShell 绠￠亾鍠?y 浼氬洜 BOM 鎶?Did not understand answer锛?*姝ｇ‘鍋氭硶鏄敤 cmd 鍘熺敓閲嶅畾鍚?*锛坈md.exe /c 璋冪敤 bat 骞剁敤 < 鍠傛棤 BOM 鏂囦欢锛?| 鏈満瀹炴祴 | 楂?| 2026-08-31 |

## 认证备考映射

> 目标③（应试认证）贯穿全课：每个知识点在进度表标注对应认证域。考纲要点见 `05-认证与选型/lessons/lesson-14-...` 备考指南一节。

**关键时间提醒**：官方公告 Elastic Certified Engineer 考纲将于 **2026-09-01 从 8.15 升级到 9.3**。本课基于 **9.5.x**，与升级后考纲（9.3）同代，备考方向一致。
