# Elasticsearch 课程评审清单

> 强制检查点：每批内容生成后，先在本文件找到对应未勾选条目，完成双视角评审（A：技术事实核查；B：零基础教学体验）且 P0 清零后，才能勾选并交付。未勾选 = 评审未完成，禁止进入下一批。
> 每课交付必查项（评审之外的结构性检查）：讲义结尾必须包含「🚀 下一批接力提示词」（可复制续学 prompt）与「🧭 课程导航」两段。此项在 PromQL L1、ZooKeeper L1 两次首次交付时均被遗漏、由用户指出后补齐。
> 评审结论完整摘要记入 [00-学习档案.md](00-学习档案.md) 的「评审记录」表，本清单只管勾选状态与极简摘要。

## 待评审清单

### 大纲

- [x] 大纲评审（Phase 1）— P0=0 通过（2026-08-28，主 agent 内联 pedagogy + learner；P2×1 已采纳：课14 补「知识体系收束」闭环全课主线，知识点 41→42）

### 阶段 1 · 为什么需要 ES

- [x] 阶段 1 · 课1《为什么数据库搞不定搜索》— A 事实核查 + B 教学体验，P0=0（10 项必查全通过，2026-08-28）
- [x] 阶段 1 · 课2《ES 是谁、凭什么》— A 事实核查 + B 教学体验，P0=0（10 项必查全通过，2026-08-28）

### 阶段 2 · 核心原理与上手

- [x] 阶段 2 · 课3《把 ES 跑起来》— A 事实核查 + B 教学体验，P0=0（10 项必查全通过，2026-08-31；命令全部本机实测，非抄文档）
- [x] 阶段 2 · 课4《倒排索引——快到离谱的秘密》— A 事实核查 + B 教学体验，P0=0（10 项必查全通过，2026-08-31；含 IK 9.5.1 插件真装实测，历史起源已联网核实并标注分歧）
- [x] 阶段 2 · 课5《映射：给数据定规矩》— A 事实核查 + B 教学体验，P0=0（10 项必查全通过，2026-08-31；16 组命令本机实测，第四幕端到端跑通；阶段 2 收官）

### 阶段 3 · 查询与聚合

- [x] 阶段 3 · 课6《Query DSL：问问题的语言》— A 事实核查 + B 教学体验，P0=0（10 项必查全通过，2026-08-31；20+ 组命令本机实测，新建 l6_shop 数据集）
- [x] 阶段 3 · 课7《为什么这条排在前面》— A 事实核查 + B 教学体验，P0=0（10 项必查全通过，2026-08-31；40+ 组命令本机实测，新建 l7_news 数据集）
- [x] 阶段 3 · 课8《聚合：不做搜索，做统计》— A 事实核查 + B 教学体验，P0=0（10 项必查全通过，2026-08-31；30+ 组命令本机实测，新建 l8_orders / l8_orders_v2 / l8_text_demo 各 24 条订单；双 agent 交叉评审发现并修正 3 处问题：ESQL 报错列号、退款率精度、text 聚合演示不可复现；阶段 3 收官）

### 阶段 4 · 分布式与工程实践

- [x] 阶段 4 · 课9《分片：ES 分布式的基石》— A 事实核查 + B 教学体验，P0=0（10 项必查全通过，2026-08-31；**新建 3 节点学习集群真跑分布式**，40+ 组命令实测；双 agent 交叉评审发现并修正 3 处：分片分布表为快照需注明会变、routing 实验条数依赖执行次数、red 归因原为猜测已用实测对照表取代——证实有副本索引转 yellow、0 副本索引转 red）
- [x] 阶段 4 · 课10《集群健康与排障》— A 事实核查 + B 教学体验，P0=2 均已修（10 项必查全通过，2026-08-31；**真实销毁 node-3 数据盘验证永久丢失**，40+ 组命令实测；双 agent 交叉评审发现并修正：① A 视角抓到分片延迟数据被冷启动污染（原 3.8/3.4/12.2ms 实为冷启动首轮 54-65ms 拉高，预热后重测为 2-2.5/2-3/5ms，结论从"慢 3.6 倍"修正为"慢约 2 倍"）、存储 24kb 系建索引未写数据时读取、已改为 359.9kb ② B 视角抓到**本机未装 Git Bash**（PATH 中 bash.exe 为 WSL 启动器），课 3 定的 Git Bash 写法无法执行，且 Windows curl 在 PowerShell 下仅有 `--data-binary @文件` 可靠，已把全部 21 处内联 JSON 命令改为双写法并新增环境说明段）
- [x] 阶段 4 · 课11《数据管道与备份》— A 事实核查 + B 教学体验，P0=1 已修（10 项必查全通过，2026-08-31；**跨集群快照恢复真跑**，40+ 组命令实测；双 agent 交叉评审发现并修正：① B 视角（零基础读者）抓到 P0——清理阶段误删 l11_shop 导致讲义 2.1/2.2/2.4 三节执行链断裂，逐字复制报 no such index，已重建索引并在 2.1 节补「前置依赖」提示 ② A 视角抓到 P1——仓库大小 83.3 KB 是时点值（SLM 后续又生成快照，复核时 93.5 KB），已改为看增量比例；另发现只读仓库被误删致报错不可复现，已重建重验。**核心实测**：_update_by_query 默认重跑 default_pipeline 致 updated:0、加 ?pipeline=_none 后 updated:2；reindex 把 1 主 0 副改成 3 主 1 副；快照删除后恢复一条不差且三层加工痕迹全在；删 snap_1 后 snap_4 仍完整恢复（增量+引用计数）；跨集群恢复成功（9201→9200，24 条、连 _id 原样）；SLM 定时真执行；url 只读仓库可读不可写；hdfs 仓库类型不支持）
- [x] 阶段 4 · 课12《接入真实项目》— A 事实核查 + B 教学体验，P0=0（10 项必查全通过，2026-09-01；**真装官方 JS 客户端 9.5.1 跑通全链路**，7 组脚本实测；双 agent 交叉评审当批修复 3 处 P1：① 脚本示例 await 写在 .js 顶层会报 SyntaxError→加 async main() 包装并补 .mjs 方案 ② curl 单引号仅 PowerShell 可用（本机无 Git Bash）→补 CMD/PowerShell 双写法 ③ 三种同步模式依赖 MySQL 而本机无 MySQL→显式说明为何不可实操并点明可练的两个机制。**核心实测**：客户端请求头 compatible-with=9；compatible-with=8/9 成功、**=7 报 400**（`Accept version must be either version 9 or 8, but found 7`）；bulk 部分失败（errors=true 但成功项已落库）；`_id` 幂等（_version 2→3→4、总数恒为 3）；显式列三节点则均摊 2/2/2、**sniffOnStart 不生效**；深分页根因藏在 root_cause（`Result window is too large... [10] but was [25]`）；search_after 连翻 4 页不受限。**环境限制**：本机仅 Node.js v22.14.0 可用，Python 为 Store 存根、Java/Go/.NET 均未装）

- [x] 阶段 4 · 课15《索引管理与生命周期策略》— A 事实核查 + B 教学体验，P0=0（2026-09-01；**补用户指出的缺口**：原大纲无索引管理专题。5 组实测脚本，4 个知识点：索引模板体系、别名、ILM 生命周期、索引运维。**核心实测**：组件模板 + 索引模板 `priority` 合并覆盖；`_simulate_index` 预览；**别名可承载索引级 _settings 操作**（单索引穿透、多索引广播，两索引 refresh_interval 均变 17s），但 `DELETE /别名` 被拒（`matches an alias, specify the corresponding concrete indices instead`）——**此条推翻编写前的预期，不复核就会把错误结论写进讲义**；`is_write_index` 让写入只落一个索引、搜索仍跨全部；ILM 五阶段秒级流转（**`indices.lifecycle.poll_interval` 是集群级、不是索引级**，必须走 `_cluster/settings`，写在索引模板里报 `unknown setting`）；forcemerge 段数 8→1；shrink 三硬条件（同节点 + 只读 + 因数，4→3 报 `must be a multiple of [3]`）。**踩坑**：直接对具体索引 rollover 报 `rollover target is a [concrete index] but one of [alias,data_stream] was expected`；分片分散时需先 `index.routing.allocation.require._name` 集中再 shrink。**环境已复原**：poll_interval 回到默认 10m，全部 l15 临时索引已清理）

### 收尾产物（Phase 3 / Phase 5）

> ⚠️ **2026-09-01 补记**：Phase 1 未给这两项收尾产物占位，属流程漏洞——未占位的产物在断点续学时不会被发现，导致场景解法库与索引管理长期缺失。**今后任何 Phase 产物必须在 Phase 1 就占位。**

- [x] 结课实战项目（Phase 3）— A 事实核查 + B 教学体验 + 专项维度 A1-A6，**P0=2 均已修**（2026-09-02；`projects/商品搜索服务/`，13 个源文件 / 4 份文档。**复杂度四门槛**：①跨阶段 **5/5** 阶段、22 知识点逐个回指课时 ②非功能约束 **5 项**（错误处理/性能/可维护性/安全/成本）③真权衡决策 **5 个**五段式 ④多文件四层目录。**A6 可运行性已实测**：`npm run demo` 从零退出码 0、7 步全过；`--with-dirty` / `--to=v1` 回滚 / `ES_PROFILE=secure` 三条分支均实测。**评审修复 4 处**：① **P0（脚本）**——验收清单引用了 `q.txt` / `ik.json` 两个**根本不存在**的文件，读者照抄必卡死（与课 15 教训完全同类）→ 改为不依赖外部文件的验证方式，并在项目内提供真实的 `data/keyword.txt` ② **P0（文档）**——`--brand=苹果` 这类中文参数在 PowerShell 5.1 下必失真 → 新增 `--demo-brand` 内置开关，C3 改用数字筛选验证 filter 不打分 ③ **P1**——`npm run switch -- --to=v1` 在 Windows PS 5.1 下**参数被丢**（实测三种写法只有直接 node 调用有效），已修 README / 验收清单 / 脚本提示 / demo.js 四处 ④ **P1**——`ES_PROFILE=secure npm run security` 是 shell 语法、PowerShell 不认，已改为 `$env:ES_PROFILE="secure"` 并给出双平台写法。**本轮真实踩坑（已写入代码注释与文档）**：① 模板 `index_patterns` 写成连字符 `cap_products-*` 而索引名用下划线 → 模板一个都匹配不上、索引照样建成功且 mappings 全空、**不报错** ② `reindex-switch.js` import `schema.js` 触发其模块级 `main()`，切索引会顺带重跑初始化 → 抽 `es/cli.js` 的 `runIfEntry()` ③ 客户端 `simulateIndexTemplate` 走 `_simulate_index/{索引名}`，`simulateTemplate` 才是 `_simulate/{模板名}`，**名字与直觉相反** ④ 数据流接口字段是 `indices` 不是 `backing_indices`；判断 ILM 是否挂载看 `ilm_policy`（返回对象里根本没有 `managed_by`）⑤ `cat.indices` 不加 `bytes:'b'` 时 `store.size` 是 "7.9kb" 字符串，直接 Number() 得 NaN ⑥ ILM 策略必须挂在模板 `settings.index.lifecycle.name` 上，否则数据流是 Unmanaged ⑦ ESM 入口判断不能拼 `file://${argv[1]}`（Windows 反斜杠导致永不相等，表现为"跑了但一个字不输出"））
- [x] 实战经验 / 排障速查手册 / 场景解法库（Phase 5 三份一次生成）— A 事实核查 + B 结构合规 + 专项维度 B1-B11 / C1-C7，**P0=0 P1=0 P2=0**（2026-09-01；三态分工：08 学习态讲原理、09 使用态只给动作、10 设计态给多解法权衡。**08**：6 适用边界 + 6 反模式 + 7 条故障模式（五段式：症状→根因→排查路径→修复→预防）+ 上线 Checklist + 2 个真实案例复盘，证据标注本机实测 5 处 / 官方文档 3 处。**09**：13 个条目 E1-E13 + 症状索引表 + 三档紧急度 + QRH 五特征齐备 + 回指经验层 8 处。**10**：8 个场景，每场景 4-6 个解法（均 ≥3）、均含「代价」列与不适用边界、`<details>` 16 组配对先想后看。**评审修复 5 处**：① P0 误报——提醒正文里的「不要用 grep」被当成可执行命令，改为只扫描代码块；② P1 误报——条件-动作表正则 `\| 定位 \|` 与实际表头不符，改为匹配「步骤/动作/预期」；③ P1 豁免——10 为设计态不给可执行命令，已在文档显式声明后按定位豁免 curl 检查；④ **P2 真缺陷（脚本）**——代码块提取正则的语言白名单漏了 `mermaid`，导致围栏配对错位、08 的 `--data-binary` 所在块根本未被扫描，改为通用 `[a-z]*` 后归零；⑤ **真缺口（文档）**——09 未显式声明「只给动作不给原理」，已补）

### 阶段 5 · 生产与选型

- [x] 阶段 5 · 课13《三大主战场》— A 事实核查 + B 教学体验，P0=0（10 项必查全通过，2026-09-01；三大战场各跑通一遍，9 组实测；双 agent 交叉评审当批修复 3 处 P1：① PowerShell 续行写法不一致→统一反引号 ② `semantic_text` 提了未落地→补选型对比+本机实测（能建索引、写入即报 `non-compliant for [inference]`，建时不报错写时才炸）③ 字段级/文档级安全未给本机预期→补三级粒度表。**核心实测**：rollover 后新写入进 000002、旧索引封存 3 条；搜 data stream 跨后备索引得 2 条 vs 单索引 1 条；关键词搜「宠物」仅 1 条 vs **向量 kNN 3 条且「狗」0.9992 排在「猫」0.9985 前**；维度不匹配根因 `different number of dimensions [2] than [3]`；RBAC 读 24 条 ✅、写/删/越权**均 403**（报错列出所需权限名）、无凭据与错密码**均 401**。**环境**：两集群 license 均 basic。**时效**：认证考纲 2026-09-01 升级至 9.3）
- [x] 阶段 5 · 课14《该不该用 ES》— A 事实核查 + B 教学体验，P0=0（10 项必查全通过，2026-09-01；**全课收官**，4 组选型实测 + 考纲逐条探测；双 agent 交叉评审当批修复 3 处 P1：① 缺五幕结构→已重构 ② 缺「接力提示词」段→已补且给备考/实战两个方向 ③ 缺「课程导航」段→已补；B 视角 P2×1 已修：只有脚本引用无可复制命令→补 4 组命令并逐字验证通过。**核心实测**：分片 1/3/50 的 p95=16.58/16.48/**35.90** ms（慢 2.2 倍）、存储 77.3/88.9/**361.9** kb（4.7 倍）、段 1 vs 50，p50 几乎无差（代价在长尾）；**多分片聚合失真**：单分片 error=0 且每桶精确 100，50 分片 error=**82**（BRAND-0 95、BRAND-1 90）；**乐观锁只到单文档**（过期 seq_no→HTTP 409 version_conflict_engine_exception）；**无跨表关联**（ES\|QL JOIN 报 parsing_exception）；**近实时五连测**（写后立刻搜 0 条→1.2s 后 1 条→refresh=true 可见→GET 实时 vs SEARCH 近实时→refresh_interval=-1 搜不到）。**考纲探测**：Architecture/ES\|QL/kNN/ILM/别名/pipeline/async search/快照/RBAC/IK 全可用；`semantic_text` ❌（basic license，建时不报错写时才炸）、Streams ❌；已移除的 runtime fields 本机仍能跑。**踩坑**：kNN 字段名写错**静默返回 0 条不报错**；runtime field 偶发 script_exception 重试即可）

## 每课必查项（结构性检查，评审之外逐项过）

| # | 检查项 | 说明 |
|---|--------|------|
| 1 | **结尾两段齐全** | ①「🚀 下一批接力提示词」②「🧭 课程导航」。PromQL L1、ZooKeeper L1 曾两次遗漏 |
| 2 | **五幕结构完整** | 场景引入 → 认知冲突 → 层层揭示 → 实操验证 → 体系收束 |
| 3 | **全程中文** | 不含中英混杂表述（如"kicks in"），术语首次出现给中文解释 |
| 4 | **零基础友好** | 新术语逐个通俗化，配类比，不假设前置知识 |
| 5 | **实操可运行** | 命令适配 Windows 11；`curl` 用 `curl.exe`；本机为 ES 9.5.1 原生 zip（非 Docker）。**⚠️ 2026-08-31 实测修订：本机 Windows PowerShell 5.1 的 `Invoke-RestMethod` 访问 ES 9.5.1 HTTPS 端点失败**（无 `-SkipCertificateCheck` 参数，强制 TLS 1.2/1.3 亦报"基础连接已经关闭"），根因未定位。全课程统一用 `curl.exe`（CMD / PowerShell / Git Bash 通用），示例按 **Git Bash 写法**（单引号不转义 + `\` 续行），不得混用 CMD 的 `^` 续行与转义引号 |
| 6 | **版本号准确** | 与档案「事实核查记录」一致，不臆造 |
| 7 | **跨课引用准确** | 引用其他课时须核实该课确实讲了此内容（Kafka 课 10 曾出现回扣失真 P1） |
| 8 | **Mermaid 兼容性** | 边标签不用反引号语法（低于 Mermaid 10.3 渲染失败） |
| 9 | **SVG 浅色主题** | 深色背景 SVG 曾系统性违规，全部改用浅色主题 |
| 10 | **无孤立节点** | 图中不得有未连线的孤立节点（Kafka 课 7 曾出现） |
| 11 | **端到端实跑（2026-09-01 新增，优先级最高）** | 讲义里每条命令必须**按出现顺序真跑一遍**。课 15 交付后复跑才发现三个 P0：引用的 6 个 `l15-*.json` 文件根本不存在、正文说"reindex 过去"却没给命令（照做会切到空索引、数据全丢）、ILM 配法跑不通（三种连环报错）。**格式评审全过 ≠ 讲义能跑通**——脚本只查格式，查不出命令跑不跑得通。凡讲义引用数据文件（如 `@xxx.json`），必须确认文件真实存在 |

> ⚠️ **第 11 条是课 15 之后新增的**。它防的是一类此前完全没被覆盖的缺陷：讲义格式漂亮、链接全通、评审 P0=0，但学员照着敲就是跑不通。

## 环境相关必查

- 实操命令不得依赖 Docker（本机未安装）
- 统一用 `curl.exe`（`C:\Windows\system32\curl.exe`，本机 PowerShell 5.1 无 pwsh 7，实测 `Invoke-RestMethod` 连不上 ES 9.5.1 HTTPS）
- **⚠️ 2026-08-31 实测重大修订：本机未安装 Git Bash**——`C:\Program Files\Git\bin\bash.exe` 等三个常见路径均不存在，PATH 中的 `bash.exe` 是 `C:\Windows\system32\bash.exe`（WSL 启动器，调用无响应）。**课 3 定的「按 Git Bash 写法给示例」在本机无法执行，后续课程不得再默认该写法**
- **Windows curl 传 JSON 的正确方式**（实测四种写法）：`--data-binary @文件.json` 在 PowerShell 与 CMD **均可靠**，是唯一推荐写法；`-d "{\"key\":1}"`（CMD 反斜杠转义）**仅 CMD 可用、PowerShell 失败**；`-d '{"key":1}'`（Git Bash 风格）与 `-d "{`"key`":1}"`（PS 反引号转义）**两种终端均失败**。报错原文 `was expecting double-quote to start field name`
- **涉及 JSON 的命令一律给「文件写法为主 + CMD 转义写法为辅」**，并在课上说明原因
- 过滤命令**不要写 `| grep`**（本机无 grep），PowerShell 用 `| Select-String`
- ES 默认 HTTPS：请求需 `-k`（跳过证书校验）；**认真校验必须加 `--ssl-revoke-best-effort`**，否则 Windows curl（Schannel 后端）报 `(60) revocation status is unknown`
- **首次启动必须直接在终端里跑，不要重定向输出**——实测无 TTY 时 ES 会跳过密码生成（日志原文：`we cannot determine if there is a terminal attached`）
- 密码没拿到用 `bin\elasticsearch-reset-password -u elastic -i`（交互）；脚本化用 `-i -b` + 管道喂密码
- 单节点学习环境建索引**显式指定 `"number_of_replicas": 0`**，否则自动创建的索引默认 1 副本 → 集群 yellow
- **⚠️ 2026-08-31 重大环境变更：9200 单节点集群密码已重置为 `9PvhcGNNc86uFZb_ePAN`**（原 `ESlearn2026` 失效）。原因：课 9 建 3 节点集群时把 `elasticsearch-9.5.1/config/elasticsearch.yml` 整体改写成 node-3 配置（`cluster.name: l9-cluster` / `http.port: 9203` / `path.data` 指向 node-3），导致 9200 无法启动；课 11 重写配置后重建元数据，密码随之丢失并重置。**该 yml 已修复为单节点配置并加上 `path.repo`**，课 12 起按新密码访问
- **元数据损坏的救回路径**（2026-08-31 实测）：`CorruptStateException` 时先全量备份 `data` → 清空 `data/_state` → 重启 → `GET /_dangling` 列出孤儿索引 → `POST /_dangling/<uuid>?accept_data_loss=true` 逐个恢复。**元数据坏了不等于数据坏了**
- **重置 elastic 密码须绕过 TTY**：`-i`（交互）与 `-a`（自动）都报 `unable to read from standard input; is standard input open and a tty attached?`；PowerShell 管道喂 `y` 会因 BOM 报 `Did not understand answer`；**用 cmd 原生重定向** `cmd.exe /c "bat -u elastic -a < 无BOM文件"` 才成功
- **PowerShell 5.1 读取含中文路径/内容的 .ps1 脚本会被 GBK 误读**：脚本内避免直接写中文路径，改用通配符定位（`Get-ChildItem -Filter "00-*.md"`）

## 评审记录（勾选后追加极简摘要）

| 日期 | 评审对象 | 评审方式 | P0 数 | 意见摘要 |
|------|----------|----------|-------|----------|
| 2026-08-28 | 大纲评审 | 主 agent 内联（pedagogy + learner） | 0 | 5 阶段 14 课 42 知识点依赖链无缺口；P2×1（课14 补知识体系收束）已采纳 |
| 2026-08-28 | 课1 批次 | 主 agent 内联（A 事实核查 + B 教学体验） | 0 | A：LIKE 前导通配符致 B+ 树失效（type=ALL/key=NULL）、MySQL 内置解析器不认中文（官方文档原文）、ngram 机械切字、分区表不支持 FULLTEXT 均准确。B：菜市场/索引页类比到位、结尾两段齐全、全程中文、Mermaid 边标签无反引号。10 项必查全通过 |
| 2026-08-28 | 课2 批次 | 主 agent 内联（A 事实核查 + B 教学体验） | 0 | A：ES 起源时间线（1999 Lucene→2004 Compass→2010-02-08 ES 0.1.0→2012 Elastic 成立→2018-10-05 上市 ESTC）、许可证 7.11 起改 SSPL+Elastic License、2021-04 AWS fork OpenSearch、2024 追加 AGPLv3、Type 7.0 废弃、refresh_interval 默认 1s 均准确。B：三大误解先行打碎、决策清单实用、明确本课聚焦 ES 本身（Logstash/Kibana 仅必要处提及）。10 项必查全通过 |
| 2026-08-31 | 课3 批次 | 主 agent 内联（A 事实核查 + B 教学体验），**命令全部本机实测** | 0 | A：P0×1 已修——知识点 2 六处 curl 示例混用 CMD 的 `^` 续行与转义引号，与第四幕 Git Bash 写法冲突，复制必失败，已统一为 Git Bash 写法；P1×2（mermaid 内 `certs\http.p12` 等反斜杠有渲染风险 + 节点角色拆分指向课 10 归属不准→改"阶段 4"）；P2×2（`docs.deleted=4` 的算术推导与实测对不上→改定性描述；Kibana 接入令牌缺解释→补三件套表格）。B：L1-L6 完整，无 P0；P1×1 已采纳（第 6 步近实时演示存在手速随机性，补"数字一样就快速连敲几遍"提示）；P2×1（速查卡补"重设密码"高频操作）。10 项必查全通过 |
| 2026-08-31 | 课4 批次 | 主 agent 内联（A + B），**含 IK 插件真装实测 + 起源联网核查** | 0 | A：P0×0。修 2 处准确性（第一幕"5 条新闻"与实测时序不符→改 4 条；`termvectors` 补"从文档视角反读倒排索引"的说明，避免被误当成倒排索引本身）。P1×1 已采纳（段 segment 与分片 shard 未做区分，学生易混→加对照说明并点明分片属课 9）。P2×1 已采纳（装 IK 的重启步骤不完整→补齐 cd 与启动命令）。B：L1-L6 完整无 P0——「搜人民命中 4 条、3 条是垃圾」的冲击证据充分，两个认知冲突在第三幕均有回答。**命令风格与课 3 已统一**（自动校验：`^` 续行 0 处、转义引号 0 处）。10 项必查全通过 |
|
| 2026-08-31 | 课6 批次 | 主 agent 内联（A + B），**20+ 组命令本机实测 + 新建 l6_shop 8 条数据集** | 0 | A：P1×2 已修——① **SVG「手机」词项命中文档列表写错**（原写 doc6，实测 doc6 是"华为 MateBook 笔记本电脑"不含"手机"，应为 doc1,doc2,doc3,doc8，并集 6 条才与 match 结果自洽）② **速查表列了 `exists`/`multi_match` 却未实测**（违反"引用须有实测支撑"），已补测并写入结果。P2×1（SVG「0.99+0.72」为估算→改为实测精确值 0.9894+0.7262=1.7156）。B：L1-L6 完整无 P0——开场「term 0 条 vs match 6 条」反差强烈，第四幕第 4 步三组对照实验（1.6706/0.7262/对照组）是本课最硬的证据。**主动标注 filter 缓存在 8 条数据量级下 `query_cache.hit_count` 恒为 0、测不出收益**，未夸大性能。10 项必查全通过（五幕 5、知识点 3、结尾 2、`^` 续行 0、转义引号 0、Mermaid 反引号 0） |
| 2026-08-31 | 课5 批次 | 主 agent 内联（A + B），**16 组命令本机实测 + 第四幕端到端跑通** | 0 | A：P1×2 已修——① **开场报错索引名与实测不符**（写 `l5_shop` 的 `name`，实测是 `l5_shop_bad` 的 `brand`），已按实测原文改并补建该索引的实测命令；② **「19.9 存成 19」原属想当然**，补测后确认属实且证据升级（`docvalue_fields` 返回 `[19]`、`term:19` 命中 1 条、`term:19.9` 命中 0 条、`_source` 仍 19.9），已写入第一幕并据此把「三种代价」扩为**四种**。P2×1（`fielddata=true` 是官方备选但紧随 `significant memory` 警告，易误导新手→加显式提醒"正确做法是改用 keyword"）。B：L1-L6 完整无 P0；开场的"搜不到/存不对"四连问有代入感；**`_source` 原件 vs 索引副本**的区分已提炼为贯穿全课的原理句。自动校验：`^` 续行 0、`\"` 转义 0、Mermaid 边标签反引号 0、结尾两段齐全。10 项必查全通过 |

> ⚠️ 当前评审方式为「主 agent 内联」（course-reviewer 子 agent 未创建，独立性受限）。**评审脚本本身也会出错**——本轮 5 项报警中 4 项是脚本缺陷、仅 1 项是文档真缺口，故评审结论须先判定真伪再动手改，不可盲改文档。
| 2026-09-01 | 课15《索引管理与生命周期策略》 | 主 agent 内联（A 事实核查 + B 教学体验），**5 组脚本本机实测** | 0 | **补用户指出的内容缺口**（原大纲无索引管理专题）。A：14 组事实核查全通过——组件模板 priority 合并、`_simulate_index`、别名四玩法、ILM 五阶段秒级流转、forcemerge 8→1、shrink 三条件均有实测支撑。**关键纠错**：别名可承载索引级 `_settings`（多索引广播，两索引均变 17s），推翻「别名不是索引所以应报错」的预设；`DELETE /别名` 确实被拒。B：P0×2 已修——① 跨课链接指向错误（课 14 实际在阶段 5 目录）且 `projects/` 目录不存在 ② Mermaid 虚线 `-.管.->` 未被检查脚本识别致误报孤立节点，已改标准实线。P1×1 为脚本正则 `9\.[0-8]\.` 误判版本号，已改为与本机 9.5.1 精确比对 |
| 2026-09-01 | 08/09/10 三份收尾产物（Phase 5） | 主 agent 内联 + 专项维度脚本 B1-B11 / C1-C7 | 0 | 三态分工清晰（08 学习态 / 09 使用态 / 10 设计态）。**修复 5 处**：① P0 误报——提醒正文「不要用 grep」被当成可执行命令，脚本改为只扫描代码块 ② P1 误报——条件-动作表正则与文档实际表头不符 ③ P1 豁免——10 为设计态，显式声明「不给可执行命令」后按定位豁免 curl 检查 ④ **P2 真缺陷（脚本）**——代码块正则语言白名单漏 `mermaid` 致围栏配对错位，08 的 `--data-binary` 块根本没被扫到，改通用 `[a-z]*` 后归零 ⑤ **真缺口（文档）**——09 未显式声明「只给动作不给原理」，已补 |
| 2026-09-01 | 课15 讲义端到端复跑（交付后复核） | `20-verify-l15-json.js` + `26-verify-l15-ilm.js` 按讲义 7 步实跑 | 0 | **发现 3 个 P0 级讲义缺陷，评审脚本全都没查出来**（脚本只查格式，不查"命令跑不跑得通"）：① **讲义引用的 6 个 `l15-*.json` 文件根本不存在**，学员照抄必失败 → 已全部补齐 ② **第 4 步正文说"reindex 过去"却只给了切换命令**，照做会切到空索引、数据全丢 → 已补 reindex 命令与警告 ③ **第 5 步 ILM 配法跑不通**，三种连环报错（别名不带 `is_write_index` / 模板写了 `template.aliases` / 对具体索引 rollover），正确写法是"模板不放 aliases + 建索引时显式 `is_write_index`"，已用 `24/25/26` 号脚本取证并重写，实测 rollover 成功生成 `-000004`。另：`_ilm/explain` 刚建好时**没有 `phase` 字段**，讲义原话"输出里会有 phase/action/step/age"是错的，已改为双状态实测输出。**教训：格式评审过了 ≠ 讲义能跑通，交付前必须按讲义步骤实跑一遍** |
| 2026-09-01 | 09 排障手册补 E14（rollover 不滚动） | 主 agent 内联 + 专项维度脚本重跑 | 0 | 端到端复跑挖出的高频坑此前手册未覆盖，已补 E14（三种报错原文 + 正确配法三条件 + `_ilm/explain` 无 phase 的陷阱），并加入症状索引表。三份产物重跑后 P0=0 P1=0 P2=0 |
| 2026-09-02 | 课程手册汇总（Phase 4） | 主 agent 内联 + 链接脚本校验 | 0 | `final-课程手册.md`：5 阶段 15 课（每课一图总结 + 核心结论 + 全文链接）+ 十个结论 + 四层排查法 + 实战与排障索引 + 知识点总索引。**校验**：相对链接 54/54 全部存在、0 断链；课时覆盖 15/15；每课一图总结 15 个（11 mermaid + 4 复用既有 SVG）。另补 `assets/learning-path-overview.svg`（ES 目录原本缺这张图，其他主题均有） |
| 2026-09-02 | 结课实战项目（Phase 3） | 主 agent 内联（A 事实核查 + B 教学体验）+ 专项维度 A1-A6 | 0 | 见上方「收尾产物」条目。P0×2 均已修（引用不存在的文件 / 中文 CLI 参数失真）；P1×2 均已修（npm 传参丢失 / `ES_PROFILE` 写法）。A6 可运行性**实测通过**：从零 `npm run demo` 退出码 0、7 步全过，三条分支路径（dirty / 回滚 / secure）均实测 |
