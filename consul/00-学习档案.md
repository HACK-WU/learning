# Consul 学习档案

> 本档案是断点续学的唯一依据：画像、进度、大纲调整、评审记录全部在此。
> 最近更新：2026-09-21（**新增子教程「py-consul 使用」7 课大纲**：Python 客户端专项，兑现主线场景 01 的提及级缺口；全课程收口：final-课程手册.md 生成 + 知识点进度表阶段 3/4 补齐 + 02-课程目录运维专项进度行修正）

## 学习者画像

| 项目 | 值 | 备注 |
|------|-----|------|
| 主题 | Consul（服务注册/发现与配置选型） | 完整体系，课程制 |
| 当前水平 | 入门 | 听说过或用过皮毛，正常节奏 |
| 学习目标 | **决策参考** | 对比 Consul / etcd / ZooKeeper / Nacos / Eureka，产出选型决策清单 |
| 输出格式 | Markdown + Mermaid/SVG | 不使用 HTML |
| 模式 | 课程制 | 4 阶段 12 课 36 知识点（2026-09-17 增补课 8 ACL 与安全模型），分批教学 |
| **角色视角**（2026-09-20 多角色判定） | **主线 = 架构师 / 开发视角做选型**；**次角色 = 运维 / SRE** | 命中三信号：① 官方 Docs 分角色组织（Develop / Operate）；② 现实中分岗位；③ 删掉运维内容主线仍完整。次角色收敛为 **[子教程 · 运维专项](./子教程/运维专项/overview.md)**（8 课，按需学习、不进主线进度）。主线对运维知识只做**提及级**带过（课 11 运维五问已加指向入口） |
| **第二条子教程**（2026-09-21 新增） | **[子教程 · py-consul 使用](./子教程/py-consul使用/overview.md)**（7 课，含环境准备课） | 主线场景 01 只出现一行 `consul.Consul(...)`，Python 客户端调用属"提及级未兑现"。与运维专项**视角正交**：运维专项 = 服务端/集群怎么养，本教程 = 客户端/应用代码怎么调。同样按需学习、不进主线进度 |

## 目标拆解（决策参考）

学完本课程应能回答：

1. Consul 到底是什么、能干什么、内部机制是否可靠？
2. 它与 etcd / ZooKeeper / Nacos / Eureka 相比各有什么牌？
3. 许可证（BUSL 1.1）、运维成本、退出成本如何影响决策？
4. 我的具体场景该不该用 Consul？用它的哪部分？不用的话换什么？

## 故事主线

**故事线：一位架构师的选型签字之路**

- **主角**：架构师小林，公司微服务化，要为"服务寻址 + 配置"选定底座
- **冲突**：候选五位（Consul、etcd、ZooKeeper、Nacos、Eureka），资料各执一词；选错迁移成本极高；2023 年后 Consul 还换了许可证，决策更复杂
- **转折**：每阶段回答一层疑问——先认识本尊（它有什么本事）→ 拆开引擎（机制可靠性）→ 竞品同台（横向对比）→ 签字画押（决策框架）
- **收束**：能回答"我的场景该不该用 Consul、用它的哪部分、不用换什么"

## 知识点级进度表

> 状态：⬜ 未开始 | ✅ 已完成 | ⏸️ 跳过

### 阶段 1：认识 Consul（候选人登场）

| 课 | 知识点 | 状态 | 完成日期 |
|----|--------|------|----------|
| 课 1 为什么需要服务注册与发现 | 服务寻址难题：从硬编码 IP 到动态世界 | ✅ | 2026-08-28 |
| 课 1 | 服务发现的两种模式：客户端 vs 服务端 | ✅ | 2026-08-28 |
| 课 1 | 注册中心的核心职责与 CAP 初印象 | ✅ | 2026-08-28 |
| 课 2 Consul 是什么与能力全景 | 诞生与定位（HashiCorp，2014） | ✅ | 2026-08-28 |
| 课 2 | 能力全景图：六大能力 | ✅ | 2026-08-28 |
| 课 2 | 架构角色：Server/Client/DC | ✅ | 2026-08-28 |
| 课 2 | 版本与许可证现状（2.x / BUSL） | ✅ | 2026-08-28 |
| 课 3 五分钟跑起来看一眼 | dev 模式启动与 UI | ✅ | 2026-08-28 |
| 课 3 | 注册与查询服务 | ✅ | 2026-08-28 |
| 课 3 | 健康检查与状态流转 | ✅ | 2026-08-28 |

### 阶段 2：核心能力拆解（拆开引擎看成色）

| 课 | 知识点 | 状态 | 完成日期 |
|----|--------|------|----------|
| 课 4 服务发现与健康检查机制 | catalog 与查询接口 | ✅ | 2026-08-28 |
| 课 4 | 健康检查类型与语义 | ✅ | 2026-08-28 |
| 课 4 | 阻塞查询与 watch | ✅ | 2026-08-28 |
| 课 5 Raft 与 Gossip 一致性成色 | Raft 写路径与 quorum | ✅ | 2026-08-28 |
| 课 5 | Gossip 两层池（LAN/WAN） | ✅ | 2026-08-28 |
| 课 5 | 三种读模式（default/consistent/stale） | ✅ | 2026-08-28 |
| 课 6 KV 存储与配置管理 | KV API 与核心操作 | ✅ | 2026-08-28 |
| 课 6 | KV 当配置中心：用法与局限 | ✅ | 2026-08-28 |
| 课 6 | Consul Template 与集成生态 | ✅ | 2026-08-28 |
| 课 7 多数据中心与服务网格 | 多数据中心联邦 | ✅ | 2026-08-28 |
| 课 7 | Connect 服务网格（mTLS/intentions） | ✅ | 2026-08-28 |
| 课 7 | 网格能力的成色评估 | ✅ | 2026-08-28 |
| 课 8 ACL 与安全模型 | ACL 三层授权模型（token/policy/rule） | ✅ | 2026-09-17 |
| 课 8 | 默认策略与匿名 token（非零权限） | ✅ | 2026-09-17 |
| 课 8 | ACL 与 gossip 加密、TLS 的边界 | ✅ | 2026-09-17 |

### 阶段 3：横向对比（候选人同台竞技）

| 课 | 知识点 | 状态 | 完成日期 |
|----|--------|------|----------|
| 课 9 四大竞品逐个看 | ZooKeeper | ✅ | 2026-09-17 |
| 课 9 | etcd | ✅ | 2026-09-17 |
| 课 9 | Nacos | ✅ | 2026-09-17 |
| 课 9 | Eureka | ✅ | 2026-09-17 |
| 课 10 多维对比矩阵 | 一致性与可用性对比 | ✅ | 2026-09-17 |
| 课 10 | 功能与接口对比 | ✅ | 2026-09-17 |
| 课 10 | 生态与集成对比 | ✅ | 2026-09-17 |
| 课 10 | 性能与规模参考 | ✅ | 2026-09-17 |

### 阶段 4：决策落地（签字画押）

| 课 | 知识点 | 状态 | 完成日期 |
|----|--------|------|----------|
| 课 11 许可证成本与风险 | BUSL 许可证深度解读 | ✅ | 2026-09-17 |
| 课 11 | 运维成本评估 | ✅ | 2026-09-17 |
| 课 11 | 迁移与退出成本 | ✅ | 2026-09-17 |
| 课 12 选型决策框架与场景结论 | 决策维度清单 | ✅ | 2026-09-17 |
| 课 12 | 典型场景结论 | ✅ | 2026-09-17 |
| 课 12 | 决策清单与落地路线 | ✅ | 2026-09-17 |

## 大纲调整记录

| 日期 | 调整内容 | 原因 |
|------|----------|------|
| 2026-08-27 | 初始大纲：4 阶段 11 课 33 知识点 | 开课 |
| 2026-09-17 | 阶段 2 增补课 8《ACL 与安全模型》（3 知识点），原课 8–11 顺延为课 9–12；大纲变为 4 阶段 12 课 36 知识点 | 以官方 Docs 网页索引比对发现 secure 分区 48 条为全站最大分区之一却零覆盖，且课 7 已 3 次引用 ACL 未讲。用户授权立项 |
| 2026-09-20 | **新增子教程「运维专项」（8 课）**——不进主线进度、不改主线结构 | 多角色判定命中三信号（官方分角色 / 现实分岗位 / 删次角色主线仍完整）。主线课 11 把运维当**成本项**列了「运维五问」但没教怎么做，正是规范所说"提及级"形态。子教程逐条兑现五问（课 2/4/5/6/7），另补部署（课 1）、性能容量（课 3）、多机房与 K8s（课 8）。本轮只落 overview 大纲，**课文件进入学习时按 Phase 2 分批生成** |
| 2026-09-21 | **新增子教程「py-consul 使用」（7 课，含环境准备课）**——不进主线进度、不改主线结构；与运维专项视角正交（服务端怎么养 vs 客户端怎么调） | 主线仅[场景 01](./场景解法库/场景-01-多实例选主与防重跑.md) 出现一行 `c = consul.Consul(host="127.0.0.1", port=8500)`，属典型“提及级”；而“Python 怎么操作 Consul”是开发岗高频内容，既无主线覆盖又无子教程规划（按推演规则属 🔴）。子教程兑现：选型与连接（课 1）/ KV 读写（课 2）/ 阻塞查询与热更新（课 3）/ 注册与健康检查（课 4）/ 服务发现（课 5）/ Session 与锁（课 6）/ ACL·TLS·生产化（课 7）。**写前事实核查**：`py-consul` 1.7.1（Criteo 维护，2025-11-24）为唯一活跃客户端，`python-consul` 2018 停更、`python-consul2` 2022 停更。本轮只落 overview 大纲，课文件进入学习时按 Phase 2 分批生成；装包属改环境动作，已抛决策点待授权，本轮未执行 |
| 2026-09-23 | **课 7 ACL、TLS 与生产化**（[讲义](./子教程/py-consul使用/lessons/lesson-07-ACL、TLS与生产化.md)）**子教程收口** | **核心实测**：①🔴 **异常家族 7 个存在、5 个导出**——`consul.BadRequest`/`consul.ClientError` 顶层 `hasattr` 为 **False**（`__init__.py` 只导出 5 个），但定义在 `consul.exceptions` 且**被库内部用来抛异常**，须 `from consul.exceptions import`；②⚠️ **401 被映射为 `ACLDisabled`**（源码 `callback.py` 实测），真实语义常是 token 无效/过期，**会误导排查方向**；404 默认 `allow_404=True` **返回 None 不抛异常**；③token 优先级四组实测复核课 1 结论一致：**方法参数 > 环境变量 > 构造参数**（环境和构造都有→`env-token`）；④🔴 **`acl.policy` 仅 create/list/read**——`hasattr` 的 update/delete **均 False**，且 **delete 也未实现**，「删了重建」同样走不通，**唯一路径是直调 HTTP**；⑤🔴 **`acl.role` 子对象不存在**（`hasattr(c.acl,'role')` False），role 只能经 `token.create(roles_id=)` 间接引用；⑥`requests` 异常（`ConnectionError`/`SSLError`）**不是** `ConsulException` 子类（实测 issubclass False），只 except ConsulException 会穿透；⑦`verify`/`cert` 实测**逐请求透传给 requests**（get/put/post/delete 四方法均带），`verify=False` 生效证据为 `WRONG_VERSION_NUMBER`（用 https 连 http 端口）；⑧`import consul.aio` **直接 ModuleNotFoundError**（aiohttp 未安装），异步路径当前不可用；⑨连接复用实测 5 次请求 ESTABLISHED 仅 +1；`http.close()` 后再调用**仍成功**，close 不等于销毁。**⚠️ 未实测**：ACL 全部写操作（本机 dev 模式未启用 ACL，`acl.policy.list()` 抛 `ACLDisabled`；启用需重启 agent 加配置，属环境改动未擅自执行）；`consul.aio` 超时行为；`CONSUL_HTTP_SSL` 与构造 scheme 同存时的优先级（源码注释称环境变量优先，但源码实际为 `if not os.getenv(...) and prs.scheme`，实测构造 http 生效，**注释与实测存在出入，已标注为未厘清边界**）；真实 TLS 证书握手（无证书环境） |
| 2026-09-23 | **课 6 Session 与分布式锁** 交付（[讲义](./子教程/py-consul使用/lessons/lesson-06-Session与分布式锁.md)） | **核心实测**：①🔴 **锁入口确认在 `kv.put`**：`hasattr(session,'acquire')` 与 `hasattr(session,'release')` **实测均为 False**，`Session` 只有 create/destroy/renew/info/list/node 六个方法；加锁 `kv.put(k,v,acquire=sid)`、解锁 `kv.put(k,v,release=sid)`；②`acquire`/`release` **失败返回 False 不抛异常**（不检查返回值会静默误判为抢到锁）；**非持有者 release 返回 False** 且不影响原锁；③🔴 **release 后 Value 保留、Session 清空** —— 判断有无锁**必须看 `Session` 字段**，不能看键是否存在；④🔴 **实测推翻直觉：TTL 按 2 倍失效**（ttl=10 逐秒采样 30 次，**t=20.0s 才失效**；停止续期后同样 20s），根因为 Consul 源码 `session_ttl.go` 的 `ttl = ttl * structs.SessionTTLMultiplier`，官方文档称 TTL 是下界、上界可延迟，故**最坏切换时间 = 2×TTL + lock_delay**；⑤`ttl` 越界（9 / 86401）**客户端 assert 先拦截**，请求根本不发出，异常信息为空；`LockDelay` 返回**纳秒**（15000000000）；⑥续期有效（ttl=10s 每 3s renew，18s 内持续持有），`renew` 源码 `allow_404=False`，session 不存在会**抛异常**而非返回 None；⑦`lock_delay` 防抖实测：A 的 session 已失效后 B 仍抢不到（返回 False）；⑧选主实测三实例**只有一个 True**；session 失效会改 KV index（4821→4823），故阻塞查询能立即感知掉主；⑨🔴 **`kv.put` 的 `connections_timeout` 传了 TypeError**（`HTTPClient.put` 不 `**kwargs`），与课 3 同步客户端无超时是同一问题。**⚠️ 未实测**：`lock_delay=0` 的实际行为（文档称存在服务端覆盖 quirk）；`checks` 参数；端到端完整切换时间（2×TTL+lock_delay 为分项推算） |
| 2026-09-23 | **课 5 服务发现与调用** 交付（[讲义](./子教程/py-consul使用/lessons/lesson-05-服务发现与调用.md)） | **核心实测**：①`catalog` 是注册表、**不过滤不健康**（实测 3 实例含 critical 全返回），`health` 是健康视图——做服务发现一律用 `health.service`；两者返回结构**完全不同**（catalog 23 字段平铺 vs health 仅 Checks/Node/Service 三键）；②🔴 **`passing` 是三态不是二态**：不传与 `passing=False` **完全等价**（源码 if passing 真值判断），只有 True 才过滤；**warning 不算 passing**（实测被过滤）；`Checks[0]` 是节点级 `serfHealth`，服务自身检查在后面；③🔴 **实测推翻 overview 两处记载**：`health.service` 传 `consistency` 或 `filter_expr` **直接 TypeError**——`Health._service` 签名里根本没有这两个参数，只有 `Catalog._service` 有 `consistency`；需要 stale/filter 须走 HTTP API 直连（实测 `?stale` 返回 3 条、`filter=Service.Port == 8301` 精确命中；⚠️ 字段名是 `Service.Port`，写 `ServicePort` 报 invalid selector）；④`tag` 多值是 **AND 语义**（`tag=['v1','v2']` 实测返回空，非 OR）；⑤`Address` 回退 `Node.Address` 生效（未填 address 的实例正确回落到 127.0.0.1），接住课 4 的坑；⑥查不到服务返回 `(index, [])` 空列表**不报错**；health/catalog **均支持 index+wait 阻塞查询**（实测 3.02s 后返回，与课 3 语义一致）；⑦🔴 **时序坑**：HTTP 检查注册后需等约一个 interval 才转 passing（interval=10s 逐秒采样，t=1~8s 均 critical，**t=9s 才 passing**）——注册完立刻查会得到空列表，极易误判为代码错；⑧DNS 接口（8600）实测可用（本机无 dig/nslookup/host/drill，改用 Python 原生 UDP 查询，响应含 10.5.0.1）。**⚠️ 未实测**：`node_meta` 仅验证参数可传（dev 模式无节点级 meta）；`near` 单节点无法验证 RTT 排序效果 |
| 2026-09-22 | **课 4 服务注册与健康检查** 交付（[讲义](./子教程/py-consul使用/lessons/lesson-04-服务注册与健康检查.md)） | **核心实测**：①`register` 真实签名核实（`service_id` 才是唯一标识；`script`/`interval`/`ttl`/`http`/`timeout` 直接挂 register 已废弃，统一走 `check=`）；**不填 `address` 存空串**（`"Address": ""`），消费方须回退 `Node.Address`；②🔴 **实测推翻 overview 记载：`Check` 类没有 `grpc` 构造器**——`hasattr(consul.Check,'grpc')` 为 False，仅 http/tcp/ttl/docker/script 五个（`_compat` 为内部函数）；gRPC 检查须手写 dict 传 `check=`（实测注册成功）；`Check.ttl(ttl)` 源码只返回 `{"ttl": ttl}`，不接 interval/deregister；③TTL 检查**注册即 critical**，须 `ttl_pass` 才 passing；三档上报 pass/warn/fail 实测对应 passing/warning/critical；**TTL 即死线**——ttl=3s 逐秒采样，恰好第 3 秒转 critical，上报间隔应取 ttl 一半；④多检查走 `extra_checks`，**检查 ID 带序号**（`service:x:1/:2/:3`，单检查无后缀）；⑤`replace_existing_checks` 默认 False，改检查方式须显式置 True；主动 `deregister` 后 agent 与 catalog 均立即清空（实测残留 0）。**⚠️ 额外发现（重跑污染）**：同一 `service_id` 重复注册时 Consul **保留上一次的 Address**，会掩盖“不填 address 即空串”的结论——验证脚本须先 deregister 同名服务；全新 service_id 连测 3 次 + HTTP API 直连均返回 `"Address": ""`，结论可靠 |
| 2026-09-22 | **课 3 阻塞查询与配置热更新** 交付（[讲义](./子教程/py-consul使用/lessons/lesson-03-阻塞查询与配置热更新.md)） | **核心实测**：①**阻塞查询是 `index` + `wait` 的合取**——缺 index 时 wait 被静默忽略（实测 `wait="10s"` 仅耗时 0.003s 即返回，退化成轮询，不报错最难发现）；齐备则阻塞（wait=5s→5.18s），值变更时被唤醒（wait=30s 在 2.00s 返回），响应头 `X-Consul-Query-Backend: blocking-query` 佐证；②`wait` **必须带 Go duration 单位**（`3s`/`1m`），裸 `5`、`"abc"`、`"10seconds"` 均 400；**服务端上限约 10 分钟**——socket 直连实测 20m→601s、11m→611s、10m30s→617s（含随机减量防惊群）；③`Timeout` **是 `ConsulException` 子类但属正常控制流**，须单独 catch 后 continue，不可与真错误混同；键被删返回 `data=None` 非异常；④`recurse`+阻塞可监听整棵配置树（任一子键变更即唤醒），`health.service` 同样支持阻塞以监听实例上下线。**🔴 实测发现 py-consul 1.7.1 第二处缺陷**：**同步客户端 `connections_timeout` 是死参数**——签名有之，但传下去撞 `TypeError: HTTPClient.get() got an unexpected keyword argument`（源码同步 `HTTPClient.get` 仅 4 参、无 `**kwargs`、无 timeout，全模块 grep 不到超时设置 ⇒ **同步 HTTP 请求永不超时**）；**兜底推荐改用短 wait**（wait 是服务端保证的返回时限）。⚠️ 文中 session 补丁方案为思路演示、**未完整实测**，已标注 |
| 2026-09-22 | **课 2 KV 读写与配置中心用法** 交付（[讲义](./子教程/py-consul使用/lessons/lesson-02-KV读写与配置中心用法.md)） | **核心实测**：①**两层 None 语义**——`data is None`＝键不存在，`data["Value"] is None`＝值为空（实测 `put(k,"")` 会建键且读回 Value 为 None），只判第一层会在 UI 清空配置时炸 `AttributeError`；②**Value 是 bytes**，`put(key, 8080)` 读回 `b'8080'`（类型丢失），复杂值须 JSON；③**返回值即结果**——CAS 失败**返回 False 不抛异常**（不检查＝误判成功）、`delete` 不存在也返回 True（判不了存在性）；④`recurse=True` 返回 **list**、空前缀返回 **None**（非 `[]`）；键与“目录”可同名共存（平铺键空间实证）。**🔴 实测发现 py-consul 1.7.1 真实缺陷**：写入**非 ASCII 值会被截断**——`"你好，Consul"`（9 字符 / **15 字节**）服务端只存下 **9 字节**（`你好，`），截断长度＝字符数而非 UTF-8 字节数，系按字符数算 Content-Length 所致；**绕过：先 `.encode("utf-8")`**。⚠️ 诚实标注：写入中文后偶发 `405 method ConsulGET/PUT not allowed` 与 `400`，**未能稳定复现**（等待 2 秒自愈，agent 日志无错），根因未确认，已标注不入结论性断言 |
| 2026-09-22 | **课 1 环境准备与客户端选型** 交付（子教程 py-consul 首课，[讲义](./子教程/py-consul使用/lessons/lesson-01-环境准备与客户端选型.md)） | 装包（py-consul 1.7.1）属改环境动作，经用户授权后执行。**核心实测**：①**版本落差悬案了结**——README 声明支持 1.20–1.22，实测在 **Consul 2.0.2 上完全可用**（`agent.self()` 返回 Version=2.0.2、KV 读写往返成功、13 个子 API 对象全部实例化），根因是 HTTP API 向后兼容；②**推翻 overview 一条源码推断**——token 优先级实测为「方法参数 > **环境变量** > 构造参数」（原写“构造参数赢环境变量”，系把 `getenv(key, default)` 语义理解反了，四组对照实测更正，已同步回 overview 两处）；`Consul()` 无 `timeout` 构造参数此条与推断一致；③**新发现（生产最危险的漏网）**——连不上时抛 `requests.ConnectionError`，**不是 `ConsulException` 子类**，只 catch 后者会漏掉全部网络故障；④`Consul` 确为上下文管理器（`__enter__`/`__exit__` 实测存在）；`CONSUL_HTTP_ADDR` 仅在 host 与 port 均未显式传入时生效。课 1 内附更正记录表 |
| 2026-09-21 | **全课程收口**（三处历史遗留修复） | ① **生成 [final-课程手册.md](./final-课程手册.md)**——阶段 4 overview「阶段收束」明确"决策清单将汇总进 final-课程手册.md"，该文件此前从未生成（02-课程目录「汇总」节长期为纯文本）。手册含全课程地图 + 12 课速览 + 三条元结论 + 运维 8 课实测 + 实战五篇 + **末尾可勾选决策清单**（一票否决 → 三页纸 → 四维打分卡 → 回指检查）；② **知识点进度表阶段 3/4 由 ⬜ 全部补为 ✅**（14 个知识点，2026-09-17 交付但漏回写本表，与 02-课程目录不一致）；③ **02-课程目录运维专项进度行修正**（原写「课 4、5、7、8 待生成」，实际 8/8 已交付）。校验：手册 52 条链接 + 全目录相对链接 **0 断链**，15 条内容断言全过 |

## 评审记录

> 评审方式：主 agent 内联（子 agent 未创建，独立性受限）。可创建独立 course-reviewer 子 agent 获得更独立的评审视角。
> 每课讲义评审按用户长期规则执行双视角交叉评审：A 技术事实核查 + B 零基础教学体验。

| 日期 | 评审节点 | 评审方式 | 意见摘要 | P0 | 结论 |
|------|----------|----------|----------|-----|------|
| 2026-08-27 | 大纲评审 | 主 agent 内联（pedagogy 视角） | 阶段递进合理（认知→机制→对比→决策）；前置依赖无缺口；决策目标适配良好。P1 两条已采纳：①课 3 标注"轻量体验，可选跳过"；②课 10 性能对比须标注数据来源，无出处不写数字 | 0 | ✅ 通过 |
| 2026-08-28 | 课 1 批次内容评审 | 主 agent 内联（pedagogy + learner 双视角，独立性受限） | pedagogy：五幕完整、六要素齐、骨架关键点 9/9 覆盖、CAP 历史已联网核实（Brewer 2000 / Gilbert & Lynch 2002 / 2012 澄清）。learner L1-L6 逐项过：L5 指出双模式对比图可能被误读为"两个注册中心"，裁决采纳，图下补说明；L2 "多数派"一词未解释，裁决留作课 5 伏笔（P2）。P1 一条：知识点 2 示例为纸面推演，概念课可接受，第四幕微实验补足实操维度 | 0 | ✅ 通过 |
| 2026-08-28 | 课 2 批次内容评审 | 主 agent 内联（pedagogy + learner 双视角，独立性受限） | pedagogy：五幕完整、六要素齐、骨架关键点 12/12 覆盖（评审中补入 Go 单二进制关键点）；事实核查 8 项全部联网核实（2014-04-17 发布、Connect 1.2 于 2018-06-26、BUSL 2023-08-10、1.17.0 首个 BUSL 版/1.16.4 末版 MPL、IBM 2024-04-24 宣布/2025-02-27 交割、2.0.0 于 2026-05、三条支持线 EOL、API/SDK 仍 MPL）。learner L1-L6 逐项过：L5① 知识点 3 拓扑图双向边语法错误会导致渲染失败（P0 级）已修复；L5② "1.16.4 晚于 1.17.0 发布"反直觉细节补时间线彩蛋；L2 MPL 首次出现补一句话解释；L5③ gossip 仅在图出现、正文补流言协议直觉句。修复后复审通过 | 0 | ✅ 通过 |
| 2026-08-28 | 课 3 批次内容评审 | 主 agent 内联（pedagogy + learner 双视角，独立性受限） | pedagogy：五幕完整、六要素齐、骨架关键点 9/9 覆盖。事实核查方式为**本机全流程实测**（winget 安装 2.0.2、dev agent 启动日志、注册与三视图查询、健康状态全周期流转、DNS A/SRV/NXDOMAIN 全部为真实输出）；实测发现本机 Windows 11 nslookup 不支持自定义端口（53 端口监听器实验实锤查询落点），讲义以 -dns-port=53 适配并如实记录排查过程。learner L1-L6 逐项过：L2 修复三处（NXDOMAIN 首现补注释、"回环地址"补 loopback 注释、HTTP 检查状态码语义标注"文档规定+本机实测两端"）；L5 修复三处（UI"两个红点"等未实测视觉细节弱化为状态描述、KP3 节点失联清理机制不再错误指向课 4、"服务未启动态"DNS 行为标注为 critical 态等价）。修复后复审通过 | 0 | ✅ 通过 |
| 2026-08-28 | 课 4 批次内容评审 | 主 agent 内联（pedagogy + learner 双视角，独立性受限） | pedagogy：五幕完整、六要素齐、骨架关键点 9/9 覆盖（评审中补入 Near 最近实例路由关键点）。事实核查双轨：机制类全部本机实测（TTL 全生命周期含 1m+15s 自动注销时间线与 agent 日志原文、prepared query 创建/执行/DNS、maintenance 全周期、阻塞查询对照+实验组并抓到"唤醒返回旧快照"竞态、watch 正宗 handler 触发 5 秒链路）；文档类联网核实 5 项（检查类型全集十类、DCSA 最短 1m+回收器 30 秒一扫、blocking wait 默认 5m 上限 10m 且返回不保证变更、NearestN 基于 WAN gossip RTT、watch 无子命令打印即终止为本版帮助文档明文）。learner L1-L6 逐项过：P0 一处（KP2 状态图中文状态名+引号有渲染失败风险，课 2 同类教训，改 ASCII 状态 ID）；L2 修复四处（反熵、幂等、goroutine 改通俗语、"排水"补解释）；L5 修复四处（"本质是长轮询"改为 API 语义表述以兼容 streaming 演进、DCSA 命名"实测确认"降为证据边界内表述、"立即重读"按真实时间线改写且论证更强、DCSA 建议表述按来源强度软化）。修复后复审通过 | 0 | ✅ 通过 |
| 2026-08-28 | 课 5 批次内容评审 | 主 agent 内联（pedagogy + learner 双视角，独立性受限） | pedagogy：五幕完整、六要素齐、骨架关键点 9/9 覆盖。事实核查双轨：机制类全部本机实测（单机 3 节点 Raft 集群端口组错开方案并踩中 Windows WAL LogStore 兼容坑回退 boltdb、首次选举 3.3 秒、写路径 X-Consul-Index==ModifyIndex 对应、Commit Index 异步追平、杀 leader 三层节奏 +3.6s suspect/+7.2s failed/+11s 新 leader/+19.7s autopilot 移除、quorum 丢失矩阵 default/consistent 读与写全 500 而 stale 200、回归节点 Voter=false 经 10 秒稳定期转正、读模式延迟微基准 stale<default<consistent、双 DC WAN 联邦与网络坐标、优雅退出 vs 硬杀对照）；文档类联网核实 5 项（端口表、一致性模式语义、autopilot 机制、SWIM/gossip 参数、raft_multiplier 与官方容错表）。learner L1-L6 逐项过：修复七处（WAL 兼容坑结论从"都会撞上"降为"大概率撞上"并补两个独立环境复现证据与缩写展开、SWIM 首现补"分布式'谁还活着'探测算法"白话、选举超时来源标注拆分为文档+Raft 标准做法、间接探测数 3 标注 memberlist 库默认值、"线性一致"补白话定义、复现清单杀 leader 步骤前补"先查当前 leader 是谁"消除 node1 假设、candidate 首现补"选举过渡态"注释）。修复后复审通过 | 0 | ✅ 通过 |
| 2026-08-28 | 课 6 批次内容评审 | 主 agent 内联（pedagogy + learner 双视角，独立性受限） | pedagogy：五幕完整、六要素齐、骨架关键点 9/9 覆盖。事实核查双轨：机制类全部本机实测（dev agent 上 CRUD 与 base64、前缀查询三件套与"web 与 web/api-gateway 并存"平铺键空间实证、CAS 矩阵、分布式锁全流程含持锁会话销毁自动释放与 LockDelay 15 秒冷静期实测、/v1/txn 两键原子与 cas 条件失败 409 整批回滚、阻塞查询热更新 52ms 闭环且抓到"用 ModifyIndex 当 index 秒回旧值"翻车现场、无版本历史 404、单值上限 600KB→413、consul kv CLI 族含 export、consul-template v0.42.1 单次/守护渲染与 63ms 全自动链路及幂等语义三场景钉死（同值重写 ModifyIndex 动了也不触发）、模板函数 key 缺失阻塞与 keyOrDefault 兜底、envconsul v0.14.0 环境变量注入）；文档类联网核实 6 项（namespaces 与 audit logging 企业版边界、KV 值上限 512KB、事务 64 操作上限与单 DC 限制、consul-template 命令两种形式及 sh 依赖、K8s catalog sync 机制与同步类型、Nomad 1.19 集成增强）。learner L1-L6 逐项过：P0 一处（初稿断言"envconsul 不监听变更"与补测相反——实测守护模式 KV 变更后约 1 秒重启子进程换新环境变量，已按实测改写核心原理、误区 3、速查卡与收束表四处）；图修复两处（锁状态图 HeldB 幽灵状态与自相矛盾冷静期边重画为 FREE/HELD/COOLDOWN+note、flowchart 中文 subgraph ID 改 ASCII——课 2/4 渲染教训）；L2 修复五处（CAS 全称白话、session"带生命期的占用凭证"、base64 白话、12-factor 注释、k8s-sync 伪节点说明）；L5 修复三处（幂等表述补三场景实测、key/keyOrDefault 补实测证据、KP2 边界删未验证的 agent 缓存细节）。修复后复审通过 | 0 | ✅ 通过 |
| 2026-08-28 | 课 7 批次内容评审 | 主 agent 内联（pedagogy + learner 双视角，独立性受限） | pedagogy：五幕完整、六要素齐、骨架关键点 9/9 覆盖（含多活 vs 灾备的需求匹配差异）。事实核查双轨：机制类全部本机实测（dev agent Connect 线：sidecar 自动注册 Kind=connect-proxy 动态端口 21000/21001、内置 CA EC-256 根证书 10 年与 UUID trust domain、SPIFFE ID 全格式、leaf 证书 3 天有效期与双 EKU、无证书 curl exit 52、持有效证书连接拿 HTTP 200、deny 意图下 authz DENY 日志+TLS alert bad_certificate 三方互证、默认意图 Allowed、**幽灵意图与授权视图冻结全程取证**（意图删除后 11 分钟内 authorize 端点仍返回已删意图、新意图 60 秒观察窗不生效、sidecar 重启不恢复）；多 DC 线复用课 5 四节点集群：跨 DC catalog/health 转发查询、dc1 本地空、KV 同 key 双值（shanghai/shenzhen）、dc2 失联 dc1 本地读写正常+跨 DC 500/9ms+WAN gossip alive 滞后、dc2 重启恢复、微基准本 DC 0.85ms vs 跨 DC 0.99ms、prepared query NearestN failover 3ms 自动路由 dc2）；文档类联网核实 7 项（内置代理官方"三连否定"定位原文、默认意图策略回退 ACL default_policy 与生产 default deny 建议、意图 agent/proxy 本地缓存与 xDS 非即时传播、WAN 联邦 vs cluster peering 官方对比表与"不能同时用"、mesh gateway 两种联邦模式、Envoy 兼容矩阵三层绑定与 2.0=1.37.2/2.0.1=1.38.2 及"老版本不加新 Envoy"策略原文、consul-dataplane 定位与 Istio/Linkerd/Consul 三方定位共识含第三方性能数字来源标注）。learner L1-L6 逐项过：P0 两处（幽灵意图时间线"60 分钟"实为 11 分钟、"新意图从未生效"过强断言改"60 秒观察窗内未生效"）；P1 两处（导航链接跨目录路径错误修正为 ../../3-横向对比/...、"重新签发证书"实为 agent 返回缓存同张证书）；L2 三处（leaf 证书白话、xDS 白话、hub-and-spoke 白话）；L5 一处（删无来源的"Envoy 秒级推送"数字，标注为文档级结论）。修复后复审通过 | 0 | ✅ 通过 |
| 2026-08-28 | **课 6 勘误**（Phase 3 实战项目备课触发的专项复核） | 主 agent 内联（双源一致性核查） | 触发：Phase 3 项目备课实测发现"同值重写不推进 index"（前缀 index 136→136），与课 6 讲义第 168 行"同值重写 ModifyIndex 动了"的附带断言冲突。双重复验：① 官方 CLI `consul kv put` 写相同值后 `ModifyIndex` 保持 201 不变，写新值才跳 203；② Python 客户端复验 `X-Consul-Index` 保持 197 不变。**结论：课 6 原附带断言错误，已在课 6 正文以「勘误与加强」区块修正**（核心结论"渲染结果不变则不触发命令"不受影响，反而因服务端本就不产生变更事件而更强）。本条记为勘误而非修订，保留历史可追溯 | 0 | ✅ 已勘误 |
| 2026-08-28 | **综合实战项目**（Phase 3） | 主 agent 内联（pedagogy + learner 双视角，**叠用专项维度 A1-A6**，独立性受限） | 项目：服务注册发现与配置中心（6 文件多文件工程，Python 3.11 纯标准库零依赖）。**全部代码真跑验证通过**：六步演示（注册 2 实例→发现调用→故障摘除→配置热更新 1023ms→领导者选举→集群观测）+ 真实服务端到端（改 KV 不重启即生效，greeting 值从 `Hot-reloaded value!` 变 `Hot-reloaded OK!`）。**A1 复杂度四门槛**：① 跨 ≥3 阶段 ❌ **未达标**（仅覆盖阶段 1–2，因课程只完成 2/4 阶段）——已在 README 顶部显式声明而非粉饰；② 非功能约束 ✅ 3 项（错误处理/可维护性/降级可用性，均有落实位置）；③ 真权衡决策 ✅ 3 个（TTL vs HTTP 检查、阻塞查询 vs 轮询 vs consul-template、Consul 会话锁 vs 外部协调 vs DB 锁，每个含翻转条件）；④ 规模 ✅ 6 文件模块化。**A2 知识点地图** 11 行逐条回指课时；**A3 反例可跑性** ✅（轮询版配置监听能正常启动并更新，4 条差异逐条说清后果）；**A4 代码可执行性** ✅（标注 Python 3.9+、仅标准库、附真跑输出）。learner 视角：项目定位说明置于 README 最顶部避免误解为达标版；附"后续升级方向"3 条待阶段 3/4 完成后扩展。P1 一处已修（`config_center.wait_update` 签名残留未使用的 timeout 参数，实为按 wait 内部计算，避免误导读者）。修复后回归重跑六步全绿 | 0 | ✅ 通过（门槛①除外，已声明） |
| 2026-08-28 | **实战经验 + 排障速查手册**（Phase 5，两份一次生成） | 主 agent 内联（pedagogy + learner 双视角，**叠用专项维度 B1-B8**，独立性受限） | **B1 同源生成** ✅ 两份共用同一批失败模式（7 条故障模式 ↔ 9 条手册条目），手册每条回指经验层编号。**B2 证据纪律**：优先本机实测（阻塞查询超时、同值重写 index 不推进 136→136、LockDelay 15 秒 t=0/5/11 秒 False 至 17 秒 True、TTL 心跳停止 8 秒摘除、幽灵意图 11 分钟）；文档级走联网核查并标注来源（HashiCorp 官方错误清单、官方参考架构、Netdata 排障指南）。**B3 宁缺毋滥** ✅ 手册收录 9 条，如实写"已收录 9 条"，未硬凑。**B4 不确定项不入手册** ✅ 无 `⏳ 置信度：低` 项混入手册。**B5 事故案例** 3 个均联网核实（Roblox 2021 官方博客全站中断 73 小时、Stripe 官方博客早期 Raft 震荡与磁盘 I/O 根因、爱奇艺多 DC 抖动触发 OOM——已标注为第三方转载页面，细节以原始出处为准）。**B6 手册五特征** ✅ 索引表/可观测症状/先止血后定位/条件-动作分支/升级出口（含转 `debug` 出口）。**B7 时点标注** ✅ 两份顶部均标注"生成于课程 2/4 阶段完成时，未涵盖阶段 3/4 内容"。learner 视角：手册每条"一眼识别"用报错原文或可观测现象，不用"服务很慢"这类模糊描述；经验层"适用边界"6 条 + 反模式 5 条放在最前（"不该用"比"怎么用"更值钱）。关联产出：本次触发课 6 勘误一条（见上） | 0 | ✅ 通过 |
| 2026-09-17 | **课程目录与 web-index 比对审核**（内容覆盖专项） | 主 agent 内联（以官方 Docs 网页索引为基线逐区比对 + 脚本统计正文关键词实际出现次数） | 比对 31 分区 / 562 条官方文档索引与 11 篇课文件，查出：目录登记缺口 3 处（web-index 入口、应用实战判定记录、10-场景解法库），内容覆盖缺口 8 项（ACL/加密/备份恢复/监控告警 等 4 项零覆盖，升级兼容/K8s 落地/网关/灰度 等 4 项浅覆盖）。已覆盖扎实：concept、automate、east-west、troubleshoot、error-messages、release-notes。本次仅落登记类修改，大纲结构未动 | 0 | ✅ 通过（结论见下方内容缺口台账） |
| 2026-09-17 | **课 8 ACL 与安全模型**（阶段 2 增补课，含独立复审） | 主 agent 内联（A 技术事实核查 + B 零基础教学体验 双视角，独立性受限）+ 独立复审 | **A 视角**：重启 dev agent 逐字复跑，核查 8 类事实——ACL 三条静默失败断言（读单键 403 / 列服务 200 {} / 列 keys 200 [] / 递归前缀 404）全部复现；权限矩阵 8 条全过（读 app/ 200、写 app/ 403 key:write、读 other/ 403、列服务 200、注册服务 403 service:write、列 token 403 acl:read、递归读 200、匿名递归 404）；匿名读 intention 200 [] 而只读 token 见真实数据；内置 CA 有效期 2026→2036（10 年）取自 NotAfter 字段；bootstrap 二次执行报 403 ACL bootstrap no longer allowed 原文一致；TrustDomain 与日志 SPIFFE URI 前缀一致；匿名 token SecretID = "anonymous" 证实小测答案 D；/v1/acl/create 404 证 2.x 无 legacy ACL。**A 视角抓出 3 项**：【P0】DNS 结论不可归因——授权 service:read + node:read 后 HTTP 已能查到服务，但 nslookup 仍失败，原生报文探测收 
code=1（FORMERR，报文格式错误）而非 NXDOMAIN(3)，另起关闭 ACL 对照实例同样收不到正常应答，**判定为本机报文/环境问题而非 ACL**，验证 7 整段改写为"现象已观测、归因未确定"并给出 Linux dig 复验方法；【P1】"四类资源前缀"与表格 7 类自相矛盾；【P1】"HTTP 明文、RPC 明文"过度——实测 Internal RPC 已启用 TLS 仅 Verify Incoming: false。**B 视角**：五幕完整、六要素齐、课尾三段式齐（小测 4 题含多选附解析）、第四幕自包含；抓出 5 项并修复——intention 首现无零基础解释（补"允许谁访问谁"白话）、cl-svc-web.json / cl-intention.json / cl-token-create.json / cl-policy-readonly.json 四个 payload 未给内容（均已补齐，policy JSON 另附"HCL 规则作为字符串塞进请求体"说明）。修复后复审通过 | 0 | ✅ 通过 |

| 2026-09-17 | **09 排障手册二次扩容**（吸收实战篇后的结论回写） | 主 agent 内联（文档一致性 + 代码引用核查） | 新增症状 13/14/15 三条，**先回读手册原文再动手**，沿用既有体例（一眼识别 → 30 秒判断法 → 步骤表 → 预防 → 原理）。**代码引用核查**：`config_center.py` 404 分支与裸路径探针注释、`main.py` 第 4 步 `while` 循环重试、`stage34_ext.py` 的 `validate_acl_rules()` 三处均 grep 确认存在，未凭记忆写引用。**写后校验** 10 条断言：9 PASS；1 FAIL 经回读确认为**误报**（命中更新说明中对旧计数"已收录 9 条"的有意引述，非残留）。**顺带修正**文末"已收录 9 条"滞后计数 → 15 条（上一轮加 10/11/12 时未同步）。三条均标证据来源；症状 15 含 `_fetch_current_index()` 实测无效已撤回的诚实记录。判定 P0=0 | 0 | ✅ 通过 |
| 2026-09-20 | **子教程 · 运维专项 overview（大纲）** | 主 agent 内联：**① 教学效果推演（rehearsal）→ ② 双视角评审（pedagogy + learner）**，两步顺序执行 | **推演**：运维岗位高频场景 12 个（日常 5 / 故障 4 / 决策 3）+ 理论理解问题 5 个，判定 11 项深覆盖、1 项浅覆盖、0 项零覆盖。浅覆盖项 **F4「升级后异常怎么回滚」**原只到"回滚预案"级别 → **已修订**课 7 知识点 3 为「升级流程与**可执行的回滚**」，并明确回滚边界（快照恢复是全量覆盖，故回滚不是无代价）+ 回指课 5；增量重推后闭合。**pedagogy 6 维**：准确性 / 完整性 / 递进 / 格式 / 一致性 / 主线 均无 P0；与主线课号、08 故障模式、09 症状、场景解法库编号的互引经回读核实无张冠李戴。**learner 8 维**：L1–L8 逐项过无 P0；L7 注明课文件入口要素（本质 / 处境 / 全局图 / 地图）待生成时按 Phase 2 同口径落地。**结论：推演通过（未实测），双视角 P0=0**，无双模式冲突。主线课 11 已加提及级入口 | 0 | ✅ 通过 |
| 2026-09-18 | **应用实战篇课内接缝补齐 + 全量链接校验**（本轮） | 主 agent 内联（pedagogy + learner 双视角，独立性受限）+ **脚本真值校验**（逐条解析相对路径做存在性检查，不靠推理） | **缺口来源**：三份实战篇正文 2026-09-17 已落盘实测，但与课程主线的接缝未建立——grep `应用实战\|练一练\|practices/` 于 `stages/` 下仅命中课 12 第 576 行一条过时陈述，课 5/7/8 零入口。**本轮四处改动**：①新建 `应用实战/INDEX.md` 一键跳转索引（不搬运已落盘正文，只做统一入口，避免打断 02-课程目录/00-学习档案/09 手册的多处既有引用）；②课 5/7/8 第四幕末各补 4.2 入口块；③三课文末导航各补「🎯 练一练」行；④课 12「三类待办仍未完成」改写为已完成。**P0 附带发现（既有缺陷，非本轮引入）**：为校验新增链接做全量扫描时发现阶段 3/4 四课（课 9–12）共 **22 条跨目录相对链接全部断链**——`lessons/` 到课程根是**三级**，原文误写为两级 `../../`（课 7 评审曾修过同类问题，但阶段 3/4 是 2026-09-17 一次性落盘，未过校验）。修复后复验：115 条相对链接 → **0 条断链**。**脚本误伤与还原**：批量替换把 3 条本就正确的跨阶段兄弟链接（指向 `stages/` 下兄弟阶段，两级才对）一并改坏，复验时抓出并还原——教训：批量替换前须区分"指向根"与"指向兄弟阶段"两类目标。**learner 视角**：入口块刻意用"老周追问一句…命令清单答不了"的句式交代"为什么还要另开一篇"，避免读者把 4.2 当成重复链接；三篇各在入口处给一句"最硬的结论"作为跳过去的理由。判定 P0=0 | 0 | ✅ 通过 |

## 外部文档索引

> 一次性快照，链接大面积失效时整站重跑重建（脚本与中间地图见仓库 `temp/`）。

| 项目 | 值 |
|------|-----|
| 站点 | developer.hashicorp.com/consul/docs |
| 索引位置 | [web-index/hashicorp-consul/index.md](./web-index/hashicorp-consul/index.md) |
| 生成日期 | 2026-09-10 |
| 范围（scope） | /consul/docs |
| 条目数 | 562（31 个分区） |
| 用法 | 只索引不镜像：按「我要…」列定位条目，再用 web_fetch 打开对应 URL（带锚点直达）取正文 |

**最大的几个分区**（备课与内容覆盖比对时优先看）：`secure` 48 条、`deploy` 46 条、`release-notes` 39 条、`reference-misc` 35 条、`north-south` 28 条、`automate` 28 条、`register` 26 条。

**高频直达**：[Agent 基础](https://developer.hashicorp.com/consul/docs/fundamentals/agent)｜[Raft 共识](https://developer.hashicorp.com/consul/docs/concept/consensus)｜[Gossip](https://developer.hashicorp.com/consul/docs/concept/gossip)｜[三种一致性模式](https://developer.hashicorp.com/consul/docs/concept/consistency)｜[Agent 配置全量参数](https://developer.hashicorp.com/consul/docs/reference/agent/configuration-file)｜[健康检查字段参考](https://developer.hashicorp.com/consul/docs/reference/service/health-check)｜[版本兼容矩阵](https://developer.hashicorp.com/consul/docs/upgrade/compatibility)｜[端口清单](https://developer.hashicorp.com/consul/docs/reference/architecture/ports)｜[备份与恢复](https://developer.hashicorp.com/consul/docs/manage/disaster-recovery/backup-restore)｜[企业版许可证](https://developer.hashicorp.com/consul/docs/enterprise/license)｜[dev 模式安装](https://developer.hashicorp.com/consul/docs/fundamentals/install/dev)

## 内容缺口台账

> 来源：2026-09-17 以 web-index 为基线的覆盖比对。学习目标为**决策参考**，故判定标准是"选型评审会上的高频提问能否在课程中找到答案"，而非"官方文档是否全讲"。
> 状态：⬜ 待补（未立项）｜⏳ 已定去向待写｜✅ 已补

| # | 缺口 | 对应分区（条数） | 现状证据 | 建议去向 | 状态 |
|---|------|------------------|----------|----------|------|
| 1 | ACL 体系与生产安全模型 | secure（48） | 无系统讲解；课 7 引用 11 次、课 3/6 各 1–2 次，均为顺带提及 | ✅ **2026-09-17 已补：阶段 2 增补课 8《ACL 与安全模型》，正文已落盘并实测** | ✅ |
| 2 | gossip/TLS 两层加密与证书轮转 | secure/encryption | 全文 0 次 | ✅ **2026-09-17 已补：并入课 8 知识点 4「三件独立的事」** | ✅ |
| 3 | 备份恢复与灾备 | manage/disaster-recovery | "备份/快照/灾备"全部课文件 0 次 | ✅ **2026-09-17 已登记：并入课 11 骨架知识点 2，含官方链接与评估视角** | ✅ |
| 4 | 监控告警与遥测指标 | monitor（11）+ observe（13） | "监控/遥测"全文 0 次 | ✅ **2026-09-17 已登记：并入课 11 骨架知识点 2，含指标三步核验铁律引用** | ✅ |
| 5 | 升级路径与兼容性矩阵 | upgrade（21） | 仅课 2 提 LTS 5 次，无升级路径与兼容矩阵 | ✅ **2026-09-17 已登记：并入课 11 骨架知识点 2，含官方兼容矩阵链接** | ✅ |
| 6 | K8s 落地（Helm / consul-k8s / CRD） | deploy/k8s（约 25） | "Helm" 0 次；阶段 4 学习重点列了"k8s 原生"场景结论 | ✅ **2026-09-17 已登记：并入课 12 骨架知识点 2 作为场景结论前置铺垫** | ✅ |
| 7 | 北向网关（API / Ingress / Terminating） | north-south（28） | 课 7 提 gateway 3 次未展开；课 6 的 28 次 "gateway" 是 KV 示例服务名 `web/api-gateway`，与网关无关 | ✅ **2026-09-21 二次复核（推翻前判）：已立项并完成**。前判"维持不单开课"的唯一硬理由是「本机未装 Envoy」，**该理由已失效**——复核发现 Docker daemon 可用（v29.4.1），Envoy 1.37.6 走容器化即可，宿主零污染。新建 [实战E-北向网关与真实数据面](./practices/实战E-北向网关与真实数据面/README.md)（3 张 SVG，23 条实测证据）：**网关→sidecar→后端真实数据面打通**，50/50 实测 V1:16/V2:14、权重热更新 90/10 免重启；四条硬约束（Kind / grpc-ca-file / Host 头 / setsid）。⚠️ 遗留：网关场景 **intention deny 写入 200 但未拦截**，根因未确认，已在正文+INDEX+手册三处标注 | ✅ |
| 8 | 灰度发布与故障转移（discovery chain） | manage-traffic（14） | 课 7 有 prepared query failover，discovery chain / 灰度未讲 | ✅ **2026-09-21 已补**：①课 7 新增插播节「灰度发布怎么发」（discovery chain 控制面/数据面分工、三配置条目组合、三条硬约束、网关边界）；②新建 [实战D-灰度发布与流量切分](./practices/实战D-灰度发布与流量切分/README.md)（3 张 SVG，17 条实测证据）；③**顺带修复课 9 事实错误**——原写"灰度发布 ❌ 无"与课 12「ServiceSplitter 是灰度基础」矛盾，实测证明 Consul 有灰度能力，已拆为配置维度（❌）/ 服务流量维度（✅）两行 | ✅ |
| 9 | 应用实战篇 | — | 阶段 1–2 共 8 课已完成，目录下无任何实战篇或判定记录 | ✅ **2026-09-17 全部完成**：判定 8 课（配套 3 / 并入现有项目 4 / 不配 1）+ **三份实战篇正文全部落盘并实测**——[C 课8-ACL](./practices/实战C-ACL生产权限模型/README.md)（21 项权限矩阵，发现 `operator` 资源不带 label 的静默失效坑）、[A 课5-读模式](./practices/实战A-读模式实测/README.md)（三节点集群，stale 稳定落后 1 版，leader 强杀后 9.5s 窗口）、[B 课7-Connect](./practices/实战B-Connect最小闭环/README.md)（**无需 Envoy 跑通 mTLS 数据面**，intention 82ms 生效） | ✅ |
| 10 | 场景解法库（原 10-场景解法库.md） | — | Phase 5 第三份产物从未生成，评审清单占位标题也只写了两份 | ✅ **2026-09-17 首版**→✅ **2026-09-18 升级为目录形态**：[场景解法库/INDEX.md](./场景解法库/INDEX.md)（INDEX + 8 场景文件 + **32 张**逐解法机制图）。旧版问题：生成于 **2/4 阶段**、10 场景仅覆盖阶段 1–2、单解法三段式、无机制图、无替代路线、无先想后看。新版：经典设计题 4（选主 / 热更新 / mTLS / ACL）+ 规模压力题 4（异构寻址 / 多 DC / 规模膨胀 / K8s 与许可证），每场景 ≥4 解法并逐一详解、效果列按题定制、逐解法机制图标失效点、含 ≥1 条非 Consul 栈替代路线、挂课 N 知识点、标不适用边界。**消化旧版三类待补缺口**：竞品选型 / 许可证 / K8s 落地全部并入场景 8；旧版「想跨机房同步配置」这类无解法谱系的"做不到"题并入场景 6 作边界说明。旧单文件保留为跳转存根 | ✅ | 
| 11 | 阶段 3/4 完成后重做 08/09 与实战项目 | — | 08/09 顶部已标注"生成于 2/4 阶段"；实战项目标注"跨阶段门槛未达标" | ✅ **2026-09-17 已全部完成并二次升级**：①课 9–12 四课正文（32553/30950/32623/29651 字节）；②08 重做（故障模式 10 条）；③09 扩容（症状 12 条，**同日二次扩容至 15 条**，见台账 #12）；④实战项目升级至跨 **4** 阶段——`stage34_ext.py` 可插拔后端抽象 + 上线前自检，演示六步→**十步**全通。⑤**吸收三份实战篇**：服务发现改 stale 读、递归读 404 权限归因、`validate_acl_rules()` 规则校验、网格绕过自检。**顺带修复两个既有缺陷**：`load_all()` 空前缀 404 崩溃；`main.py` 第 4 步单次等待导致首次运行热更新失效（改循环重试，实测 ~1000ms 恢复） | ✅ |
| 12 | 实战篇吸收后的三条排障结论回写 09 | — | 吸收三份实战篇时实测踩出 3 个坑（递归读越权返回 404 伪装成空配置、`operator` 类资源写成 `_prefix` 静默失效、空前缀导致阻塞查询 2ms 秒回），已写入长期记忆但**尚未进 09 手册** | ✅ **2026-09-17 已回写**：[09-排障速查手册](./09-排障速查手册.md) 新增**症状 13（递归读越权伪装成空配置，🔴）**、**症状 14（`operator` 等资源写成 `_prefix` 静默失效，🟡）**、**症状 15（空前缀导致阻塞查询秒回、启动窗口热更新失效，🟡）**，手册 12→**15 条**；症状 4 补指向症状 15 的交叉引用与"循环重试"预防项；顺带修正文末"已收录 9 条"的滞后计数（上一轮加了 10/11/12 未同步）。三条均标证据来源；症状 15 含 `_fetch_current_index()` 实测无效**已撤回**的诚实记录。写后 10 条断言校验（9 PASS / 1 误报已回读确认），并核验手册引用的三处代码位置全部属实 | ✅ |
| 13 | 应用实战篇与课程主线的接缝（课内入口 + 一键索引） | — | 三份实战篇正文已落盘实测，但课 5/7/8 正文无 4.2 入口块、文末无「练一练」导航行，`应用实战/INDEX.md` 不存在；02-课程目录/00-学习档案虽已登记正文，读者无法从课内走到实战篇 | ✅ **2026-09-18 已补**：新建 [应用实战/INDEX.md](./应用实战/INDEX.md) 一键跳转索引；课 5/7/8 第四幕各补 4.2 入口块、文末导航各补「🎯 练一练」行；课 12「三类待办仍未完成」过时陈述已改写 | ✅ |
| 14 | 阶段 3/4 四课的跨目录链接层级系统性错误 | — | 2026-09-18 全量链接校验发现：课 9–12 共 **22 条**指向课程根的链接误写为两级 `../../`（`lessons/` 到根实为三级）全部断链——四课于 2026-09-17 一次性落盘，未过链接校验 | ✅ **2026-09-18 已修复**：批量校正为 `../../../`，并还原 3 条本就正确的跨阶段兄弟链接（指向 `stages/` 下兄弟阶段，两级才对）。复验：115 条相对链接 **0 条断链** | ✅ |
| 16 | **12 课全部缺「一眼全局图」**（按 review-dimensions L7 属 P0） | — | 2026-09-18 核对 topic-teach 发现：规范将「一眼全局图」列为**固定 SVG 场景**（入本阶段 `assets/`，第三幕首、知识点 1 之前）。全 12 课 0 张——此前误判为"课程用 mermaid 是既定设计"，实际混淆了**概览层 SVG** 与**课级入口 SVG** 两类：概览层（学习路径总览 / 阶段 overview）确为 SVG，但课级入口图一直没画 | ✅ **2026-09-18 已补齐**：阶段 1 三张 / 阶段 2 五张 / 阶段 3 两张 / 阶段 4 两张，命名 `lesson-{NN}-global-{主题}.svg`，入各阶段 `assets/`；每课第三幕首插入图 + 一句读图指引。校验：12 张 XML 全合法、**零术语硬约束 0 违规**（32 词全量术语表扫描）、图片引用 12 条 0 断链、全目录 238 条链接 0 断链 | ✅ |
| 15 | 三份实战篇不合规于新版「应用实战」规范（结构非渐进演进 + 零图） | — | 2026-09-18 核对 topic-teach（09-18 09:31 更新版）发现：三篇写成了**五幕实测报告**，缺「全貌一句话 / ①基础实现 / ②综合实现 / 🎯会用标志」四项结构要素；**9 张分步设计图一张没有**；A/B 两篇篇幅达对应课的 110%/120%，远超「≤1/3」软锚点 | ✅ **2026-09-18 已按方案二就地改造**：三篇重写为渐进演进结构（基础 → 它的问题 → 综合 → 边界），补 `🎯 会用标志`；新建 9 张 SVG 分步设计图（kebab-case，入各篇 `assets/`，每张带一句读图指引）；篇幅压至 **28% / 31% / 26%** 全部达标。**实测数据零丢失**——以「📎 实测证据」表 + 可复现脚本回指保留。校验：42 条链接 0 断链、9 图 XML 全合法、JSON 片段与真实文件逐项比对一致。**如实记录两处冲突**：①quorum 丢失时 stale 返 404，与官方文档「无 quorum 仍可响应」不符；②intention 82ms 生效，未复现课 7 的「11 分钟幽灵意图」 | ✅ |

| 17 | **运维视角内容缺系统承载**（多角色判定的次角色） | deploy（46）/ manage / monitor（11）/ observe（13）/ upgrade（21） | 主线只在课 11「运维账」里把运维当**成本项**列了「运维五问」（谁负责 quorum 监控 / 证书轮换 / 验证恢复 / 核验指标 / 跟随升级），**教"怎么做"的内容全课程 0 处**；08/09 只覆盖"崩了怎么查"，场景解法库只覆盖"新需求怎么设计"，**"平时怎么养住它"无人讲** | ✅ **2026-09-20 已立项为子教程**：[子教程/运维专项/overview.md](./子教程/运维专项/overview.md)（8 课：部署 / day-2 健康 / 性能容量 / 证书密钥 / 备份恢复 / 监控告警 / 升级迁移 / 多机房与 K8s）。按规范**子教程不做 Phase 3/5、不配应用实战与源码解析**；本轮仅落 overview，**课文件进入学习时按 Phase 2 分批生成** | ✅ 大纲已落盘 |

| 21 | **子教程课 6 监控指标与告警（双重命名 → 告警静默失效）** | deploy | 课 2 只发现"指标恒 0"未给根因；主线课 11 缺口 #4「谁核验过告警指标的语义」未兑现 | ✅ **2026-09-20 已交付**：[lesson-06-监控指标与告警](./子教程/运维专项/lessons/lesson-06-监控指标与告警.md)（含一眼全局图 1 张）。**根因实测**：Consul 2.x 的 Prometheus 端点里**同时存在两套命名**——无前缀的 `consul_autopilot_healthy` / `consul_server_isLeader` / `consul_raft_last_index` **恒为 0（空壳）**，真实值在**带主机名前缀**的同名指标里（`consul_VWYPGWU_PC5_autopilot_healthy = 1`，前缀 = hostname 横杠换下划线）。**实测规模**：两轮 528 条中 268 条恒 0（51%）、610 条中 281 条恒 0（46%）。**核心教学点（修正了常见认知）**：恒 0 指标配告警**不是"不工作"，而是两种相反的错误**——写 `== 0` / `< 1` 会**永久误报**（健康时也触发，导致告警疲劳后被静音），写 `> 0` / `== 1` 会**静默失效**（真故障也不响，**最危险**）。故障注入实测：停掉 leader 后无前缀指标**从头到尾都是 0**，带前缀的 `autopilot_healthy` 从 1 降到 0、`members_servers` 从 3 降到 2。**三步核验在 Consul 上的适配**：HELP 行**仅重复指标名**（不像 JMX Exporter 带 attribute=Count/Value），拿不到语义 → 第 1 步（值域）与第 3 步（连续采样）更关键，三步不能省成两步。**可用指标实测**：`server_isLeader` 求和=1（可信）、`members_servers`=3、`runtime_num_goroutines` 116~197；FSM 落后差值**本机恒为 0**（无压力），阈值 100 已标注为**占位值非实测结论**。**终验 9 组断言全过**。评审修正 2 处：恒 0 占比由 51% 改为 46%~51% 区间；第二幕"JSON 落后于 CLI"补注终验时两者完全一致（均为 26），明确**只有 Prometheus 无前缀名有问题** | ✅ |

| 20 | **子教程课 3 性能容量调优（两条 413 硬边界 + 42 倍吞吐差）** | deploy | 课 2 只解决"看健不健康"，性能容量未交付；业务反馈"变慢"时缺判断依据 | ✅ **2026-09-20 已交付**：[lesson-03-性能、容量与调优](./子教程/运维专项/lessons/lesson-03-性能、容量与调优.md)（含一眼全局图 1 张）。**核心实测**：①**两条硬边界**——单条 value 512KB 过/513KB 拒，事务总大小 320KB 过/384KB 拒（**按总字节非条数**，20×8KB 与 40×8KB 都过、60×8KB 拒），超限回 **413 是主动拒绝，重试无效**；②**吞吐差 42~48 倍**——串行 94~111、并发 675~757、批量事务 4559~4684 条/秒（两轮独立复现），根因是每条串行写都要等一次 fsync+复制确认；③**stale 陈旧窗口约 0.07s**（follower stale index 比 leader 少 1），而**三种读模式延迟差在噪声内**（多轮极差 0.34~1.03ms，复验轮次 stale 反而比 default 慢）；④fsync 单次约 0.11ms（WSL 虚拟盘，不可外推生产）；⑤判瓶颈看 `raft.last_index` 与 `raft.applied_index` 差值。**评审修正 3 处**：①讲义原断言"stale 快 0.6ms"，复验发现该差值**落在噪声内且顺序不稳定**，已改为"收益在噪声内"并全文同步（含 SVG）；②脚本 `awk '$3'` 取 State 错误（**应为 `$4`**，课 2 是对的），导致 follower 取空；③`pkill -f 'consul agent'` **会杀掉自身 shell**（命令行含该字符串），改用 `pgrep -x consul`。**终验 6 组断言全过** | ✅ |

| 19 | **子教程课 2 集群健康 day-2（指标不可信 + 多数派判定）** | deploy | 课 1 只解决"搭起来"，day-2 的"怎么看健不健康"未交付；且课 1 结尾已发现无 quorum 失败形态不唯一，需系统解法 | ✅ **2026-09-20 已交付**：[lesson-02-集群健康与day-2运维](./子教程/运维专项/lessons/lesson-02-集群健康与day-2运维.md)（含一眼全局图 1 张）。**核心实测**：①**指标恒 0 陷阱**——API `Healthy=true` 而 `consul_autopilot_healthy` 恒 0，连 leader 自己的 `consul_server_isLeader` 也 0（三步核验确认指标未产出有效值，非集群问题）；②`left`(leave，立即交权) vs `failed`(强杀，autopilot 约 30s 清理)；③**扩容时间线**——新节点约 25s 才提升为 voter，**4 台时 FailureTolerance 仍=1**（实证课 1"4 台=3 台容错"），5 台才=2；④leader 震荡恢复 **7~9 秒**（三轮独立实测）；⑤**脑裂边界**——多数派(2/3) HTTP=200 可写，少数派(1/3) 被拒（500 或 000，形态不唯一）。**评审修正 2 处**：①脚本 probe 端口写死 8501 致 leader 节点误报 000（脚本 bug 非集群问题，按铁律先核验后修正）；②少数派失败形态原只写 500，实测还有 000，已补。**终验 6 组断言全过** | ✅ |

| 18 | **子教程课 1 生产部署（本课必须真起集群）** | deploy | 子教程 2026-09-20 立项时只落 overview，8 课课文件未生成；课 1 讲「集群搭建」，纸面推演无法交付 | ✅ **2026-09-20 已交付**：[lesson-01-生产部署与集群搭建](./子教程/运维专项/lessons/lesson-01-生产部署与集群搭建.md)（含一眼全局图 1 张）。**环境**：Windows 上 server 模式实测起不来（Raft WAL `sync ...Access is denied`，换路径/换盘/管理员权限均无效，dev 模式可起）→ 经用户授权改走 **WSL Ubuntu 24.04 + Consul 2.0.2**，3 节点真集群。**实测结论**：quorum 坏 1 台可写（含停 leader，会重选）、坏 2 台不可用且**剩余进程仍存活**；无 quorum 失败形态**不唯一**（空响应 HTTP 000 / `No cluster leader` / `Raft leader not found` / `connection refused`），**空响应最常见**——故验收脚本必须校验 HTTP 状态码而非 body。**评审修正 3 处 P0**：①4b 报错原写死单一文本（实测四种形态）；②4a 原 `sleep 6` 探测得"不可用"（实为 leader 未选出，改 8s 后两种情形均可写）；③3 条主线链接层级少一级。终验 8 项断言全过 | ✅ |

**已覆盖扎实**：concept（Raft/Gossip/一致性，课 5）、automate（KV/Session/watch/consul-template，课 6）、east-west（federation vs peering，课 7）、troubleshoot + error-messages（09 手册 **15 条**，其中症状 10/13/14 构成 ACL 静默失败专题）、release-notes（版本核查来源）。
