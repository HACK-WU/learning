global:
  resolve_timeout: 5m

route:
  receiver: webhook
  group_by: ['alertname']
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 1h

receivers:
  - name: webhook
    webhook_configs:
      - url: "http://l8-webhook:8080/webhook"
        send_resolved: true
