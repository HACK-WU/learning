global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/rules-record-only.yml

scrape_configs:
  - job_name: agent-self
    static_configs:
      - targets: ["localhost:9090"]

remote_write:
  - url: "http://l7-backend:9090/api/v1/write"
