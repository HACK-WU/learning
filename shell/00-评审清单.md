# Bash 编程 评审清单

> 强制检查点：每批内容生成后，先在本文件找到对应未勾选条目，完成双 agent 评审且 P0 清零后，才能勾选并交付。
> **未勾选 = 评审未完成，禁止进入下一批。**

## 待评审清单

- [x] 大纲评审（Phase 1）— pedagogy 视角
- [x] 阶段 1·课 1《一行命令的生命周期》 — pedagogy + learner 双视角（2026-09-03，主 agent 内联，P0×0 / P1×3 已修）
- [x] 阶段 1·课 2《展开全家桶》 — pedagogy + learner 双视角（2026-09-03，主 agent 内联，P0×0 / P1×3 已修，25 项复核全通过）
- [x] 阶段 1·课 3《退出码与条件》 — pedagogy + learner 双视角（2026-09-04，主 agent 内联，P0×0 / P1×3 已修，112 项复核全通过）
- [x] 阶段 2·课 4《变量属性与作用域》 — pedagogy + learner 双视角（2026-09-04，主 agent 内联，P0×0 / P1×3 已修，66 项复核全通过）
- [x] 阶段 2·课 5《数组与映射》 — pedagogy + learner 双视角（2026-09-04，主 agent 内联，P0×0 / P1×2 已修，75 项复核 + 学员视角照抄验证全通过）
- [x] 阶段 2·课 6《函数与调用约定》 — pedagogy + learner 双视角（2026-09-04，主 agent 内联，P0×0 / P1×3 已修，62 项复核 + 41 代码块照抄验证全通过）
- [x] 阶段 2·课 7《重定向与文件描述符》 — pedagogy + learner 双视角（2026-09-04，主 agent 内联，P0×0 / P1×3 已修，64 项复核 + 44 代码块照抄验证全通过）
- [x] 阶段 3·课 8《子shell与执行上下文》 — pedagogy + learner 双视角（2026-09-04，主 agent 内联，P0×0 / P1×4 已修，50 项复核 + 51 代码块照抄验证全通过）
- [x] 阶段 3·课 9《信号trap与清理》 — pedagogy + learner 双视角（2026-09-04，主 agent 内联，P0×0 / P1×1 已修，50 项复核 + 61 代码块照抄验证全通过）
- [x] 阶段 3·课 10《与文本工具的协作》 — pedagogy + learner 双视角（2026-09-04，主 agent 内联，P0×0 / P1×0，22 项复核 + 45 代码块照抄验证全通过）
- [x] 阶段 4·课 11《严格模式与错误处理》 — pedagogy + learner 双视角（2026-09-04，主 agent 内联，P0×0 / P1×0，70 项复核 + 86 代码块照抄验证全通过）
- [x] 阶段 4·课 12《可观测与测试》 — pedagogy + learner 双视角（2026-09-04，主 agent 内联，P0×0 / P1×0，30 条断言复核 + 41 个 bash 块照抄验证全通过）
- [x] 阶段 4·课 13《安全性能与选型》 — pedagogy + learner 双视角（2026-09-04，主 agent 内联，P0×0 / P1×0，30 条断言复核 + 35 个 bash 块照抄验证全通过）
- [x] **结课实战项目**（Phase 3）— pedagogy + learner 双视角 + **复杂度四门槛核查**（2026-09-04，主 agent 内联，P0×0 / P1×0，57 断言全通过 + 复杂度四门槛核查全通过）
- [x] **课程手册**（Phase 4）— pedagogy + learner 双视角 + **开工前资产体检**（2026-09-04，主 agent 内联，P0×0 / P1×0，24 文件 126 链接零断链 + 内部锚点全命中）
- [x] **实战经验 / 排障速查手册 / 场景解法库**（Phase 5）— pedagogy + learner 双视角 + **证据纪律核查**（2026-09-04，主 agent 内联，P0×0 / P1×0，27 文件 164 链接零断链 + 锚点全命中 + 16 组本机实测对照）
- [x] **工作区清理**（Phase 6）— 删除前核验引用关系（2026-09-04，17 项残留已删，零死链 + 结课项目 57 断言全通过；`.c8work`/`.c13work` 当时经核验为可复跑资产，改判保留；**后在 Phase 8 经用户定夺删除**，见下一条）
- [x] **产物仓库卫生检查**（Phase 7）— 按 topic-teach SSOT 五步执行（2026-09-04）：`git status --porcelain -- "shell/"` 列出 69 文件逐层下钻；**本轮零新增 `.gitignore` 规则**（运行时残留扫描全空：无 `.log`/`.tmp`/`__pycache__`/`.venv`/`node_modules`/`.DS_Store`）；校验 `git check-ignore` 对全部 69 文件**零命中**（教学产物无一被吞）；`.gitignore` 现存 148 行未提交改动经核实**全部来自 grafana 主题**，与本课程无关。⚠️ 存疑两项经用户定夺后已清理（见下一条）
- [x] **存疑项清理**（Phase 8）— 删除前核验引用关系 + 备份校验（2026-09-04）：`.c8work/`（1 文件）+ `.c13work/`（25 文件 136K）共 26 文件已删，69→43 文件，全仓 `.*work` 目录**零命中**。核验要点：**零外部功能引用**（grep 只命中档案自身记录文字；lesson-08 中 12 处 `/tmp/c8work` 系**示例命令输出文本**，属教学内容故保留）；内容已并入正式讲义（p1–p6 共 1637 行，提取 p6.md 的 10 个二/三级标题逐一 `grep -F`，**10/10 命中** lesson-13）。删除前备份至 `_shell-hidden-backup-20260904.tar.gz`（38K，28 条目 = 2 目录 + 26 文件，抽样 `verify.py`/`p6.md`/`add_tail.py` diff 全 OK）。8 处失效档案引用已同步修正。详见[学习档案 Phase 8 存疑项清理](./00-学习档案.md#phase-8-存疑项清理2026-09-04-完成)

## 评审记录（勾选后追加结论摘要）

| 日期 | 评审对象 | 评审方式 | P0 数 | 意见摘要 |
|------|----------|----------|-------|----------|
| 2026-09-03 | 大纲评审（4 阶段 13 课 39 知识点） | 主 agent 内联（pedagogy，子 agent 未创建） | 0 | P1×1：课 7 协进程偏冷门 → 降为拓展阅读 |
| 2026-09-04 | 课 3《退出码与条件》 | 主 agent 内联（pedagogy + learner，子 agent 未创建） | 0 | P1×3 已修：性能数字改为 5 次取范围（0.015-0.016s vs 6.9-7.7s，约 370 倍）、`nocasematch` 补全局副作用警告、`robust_backup` 补依赖说明。另补 3 处边界（`case` 无匹配退出码 0、`set -e` 下 `PIPESTATUS` 可用、`for` 改循环变量无效）。复核 112 项全通过 |
| 2026-09-04 | 课 4《变量属性与作用域》 | 主 agent 内联（pedagogy + learner，子 agent 未创建） | 0 | P1×3 已修：①「父看不见子 local」判定条件写错（调用期间子**能**看见父），改为返回后判定并补对照实验；② nameref 撞名须**同名**才触发，改用 `val`/`val` 精确复现（warning×3 + 值未改）；③ 补入 `declare -x` 导数组**静默失败**（父 `-ax`、子拿空、零报错）。另补 2 处边界（`local` 无法遮蔽 `-r` 全局、nameref 链式穿透）。复核 66 项全通过 |
| 2026-09-04 | 课 5《数组与映射》 | 主 agent 内联（pedagogy + learner，子 agent 未创建） | 0 | P1×2 已修：① 小测第 8 题参考解直接 `mapfile` 读含注释文件，与第四幕过滤逻辑矛盾，已补「mapfile 纯读入不过滤」警示 + 过滤代码（实测 4→3 项）；② 复核脚本 5 项失败均为脚本 bug（切片多元素未包字符串、`set -u` 报错未被捕获），非讲义错误，已修正后 10 项全通过。另实测确认 3 处边界：`${arr[@]:-2}` 无空格退化为默认值语法、`set -u` 下未声明数组取长度报错但空数组安全、`((关联数组[缺键]++))` 报错而索引数组当 0。复核 75 项全通过，学员视角照抄验证全部可跑 |
| 2026-09-04 | 课 6《函数与调用约定》 | 主 agent 内联（pedagogy + learner，子 agent 未创建） | 0 | P1×3 已修：① 复核脚本 8 项失败均为脚本自身 bug（A 组 `$( (...) )` 包裹导致 `$?` 被当字面串未执行、G 组 `deep` 内误用 `$2` 致递归无第二参数而永不超限），非讲义错误，修正后 62 项全通过；② 知识点 2 两段示例调用未定义的 `real_deploy`，学员照抄报 `command not found`，已补定义与预期输出；③ 代码块内误嵌围栏（"输出："被包进 ` ```bash `）致围栏错乱，已移出。另实测修正 1 处表述精度：nameref 遮蔽**不局限递归**，四组对照证明触发条件是「被调用方 `local` 与 nameref 目标同名」，两层嵌套同样遮蔽（outer `_a` 保持 100）。补 3 处边界：`return` 无参返回上条命令退出码（非 0）、`shift` 超限返回 1 且不改变参数、无限递归退出码 139 且 stderr 零输出。学员视角照抄验证：41 块语法全通过、32 个可独立执行块运行通过 |
| 2026-09-04 | 课 8《子shell与执行上下文》 | 主 agent 内联（pedagogy + learner，子 agent 未创建） | 0 | P1×4 已修：① 小测第 10 题引用未定义的 `servers.txt`/`ping_lib.sh`/`cleanup.sh`（照抄报 127）——**第三次踩同类坑**，已补造文件前置块并让改后版本自带依赖；同时把 `ping` 换成 `grep` 模拟可达性以摆脱网络依赖 ② `bash -i` 块在非 tty 下挂起（timeout 124），改为 `text` 不可执行 + 给替代写法 ③ 小测第 9 题并发片段引用未定义 `deploy_one`，改为自包含版本 ④ 26 个输出块缺语言标记，统一补 `text`。**另实测纠正 1 处讲义硬错误**：`return` 在非 source 顶层报错后退出码是 **2 不是 0**（且脚本继续执行），已修正正文/输出块/误区表。另实测确认 5 处关键边界：`set -e` 对裸 `wait` **生效**（rc=5，后续不执行）而对 `if wait` 失效、子 shell **继承** trap 但退出不触发（EXIT 只在最外层触发一次）而独立进程完全不继承、`lastpipe` 在 `set -m` 下静默失效（`c=0`）、`readonly` 不进子进程但子 shell 可见、`wait -n` 无任务时返回 127。复核 50 项全通过，照抄验证 51 块（49 正常 + 2 个已标注的故意失败块） |
| 2026-09-04 | 课 7《重定向与文件描述符》 | 主 agent 内联（pedagogy + learner，子 agent 未创建） | 0 | P1×3 已修：① 第一幕及第四幕共 3 处代码块调用未定义的 `deploy.sh`，学员照抄报 127，已在第一幕补构造代码（heredoc + chmod）；② `> >(tee proc.log) 2>&1` 是已验证会重复输出的反例，已标注"不要照抄"并在照抄验证中排除；③ `unbuffer` 本机未安装（属 expect 包），已改为推荐 `stdbuf`（coreutils 自带）+ `script -qec` 并标注。另实测确认 5 处关键边界：`&>` 在 dash 下把 `&` 当后台符（e.log 0 字节、退出码仍 0）、关闭 fd1 后 echo 报 Bad file descriptor 退出码 1、`<>` 读后写入是追加非覆盖、`PIPESTATUS` 被下一条命令重置（长度 3→1，须立刻存变量）、here-string 不做词拆分（`<<< $VAR` 得到完整 "hello world"）。复核 64 项全通过，学员视角照抄验证 44 块语法+执行双通过 |
| 2026-09-04 | 课 9《信号trap与清理》 | 主 agent 内联（pedagogy + learner，子 agent 未创建） | 0 | P1×1 已修：第四幕问题版脚本「先建临时目录再发现拿不到锁」，失败时也留下新烂摊子——已在讲义中明确点出这是问题版的设计缺陷（作为教学对照保留）。**另实测纠正 4 处**：① 等外部命令的脚本收 SIGINT 退出码是 **0 不是 130**（纯计算脚本才是 130），已配 A8/A9 对照实验；② 信号被**推迟**到前台命令返回后才处理（T+0.5 发 INT，T+3.0 才触发，sleep 跑满 3004ms），故 Ctrl-C 无法立刻中断 scp/tar；③ 「进程死了锁自动释放」的反例——`kill -9` 只杀 bash，**孤儿子进程继承 fd 继续持锁**，清掉孤儿才释放；④ `kill -- -$$` 两种结果都错：脚本 `$$`(2306199) ≠ `PGID`(2305967=父 PID)，`-$$` 组不存在（清理失效）、用真 PGID 会误杀调用者。**新增安全警告**：后续一律用「kids 数组显式记录 PID + cleanup 逐个 kill」；并澄清 `jobs -pr` 在 trap 中**可用**。**新增环境认知**：WSL 下 `/proc/<pid>/status` 的 SigIgn 读不到（pid 命名空间受限），取证信号处置须改用 `trap -p` 或行为观测。复核 50 项全通过（A/B/C/D/E/F/G/H/I/J/K 十一组），照抄验证 61 块全通过 |
| 2026-09-04 | 课 10《与文本工具的协作》 | 主 agent 内联（pedagogy + learner） | 0 | P1×0。过程中修掉 1 处讲义内部不一致：知识点 1 示例引用 `app-0000.log`（4 位）而数据生成脚本产出 `app-00000.log`（5 位），改为知识点内自带样例生成块（sample/app.log），学员照抄即可运行。另为架构示意代码块补注「/opt/bin/*.py 为虚构路径，不可直接运行」。复核 22/22 全通过（A–L 组）：fork 525μs、NUL=9 / 换行=10 / for=16、-exec `\;`=9 进程 vs `+`=1 进程、分批 3 行汇总 40063、循环版慢 16 倍。照抄验证 45 块通过（1 块架构示意已标注）。全仓链接零断链 |
| 2026-09-04 | 课 11《严格模式与错误处理》 | 主 agent 内联（pedagogy + learner） | 0 | P1×0。**骨架三处描述被实测推翻并已更正**：① 算术上下文 `(( ))` 不是「失效」而是「误伤」（`i=0; ((i++))` 返回 0 导致误杀，修复方向完全相反）；② `local x=$(cmd)` 吞码仅对「一行写完」成立，分开写则正确捕获；③ `-u` 空数组报错是 bash 4.3 及更早的问题，本机 5.2.21 已修复。**照抄验证修复 4 处自包含缺陷**：`$log`/`$pattern` 未定义、`die`/`usage` 未定义、case 片段单独成块、依赖外部 `/tmp/c11_g1.sh`。复核 70 条断言（A–M 十三组），其中 J1 为判定式引号嵌套误报、数据本身正确 |
| 2026-09-04 | 课 12《可观测与测试》 | 主 agent 内联（pedagogy + learner） | 0 | P0×0 / P1×0。**两处描述被实测推翻并已更正**：① `PS4` 不做 printf 格式化（`PS4='%(%H:%M:%S)T '` 原样输出字面量）；② main 守卫 `&&` 写法在 `set -e` + source 下返回 1 杀死调用方（应改用 `if` 块）。**讲义自身一处真缺陷已修**：日志函数参数错位导致 `[时间] [消息]`（级别标签丢失），改为级别名内置于包装函数。**照抄验证 41/41 通过**——4 个「片段演示」块（main 守卫三种写法、bats `run` 示意、小测题面）原为 `bash` 标记，单独复制必然报错，已改 `text` 并加「这是要放进脚本里的片段」说明。复核 30 条断言（A–K 十一组）全通过：echo/printf 可移植性、printf 格式串陷阱、PS4 层级与 XTRACEFD 分流、日志分级与 stderr 分离、SC2181 `$?` 污染、SC2312 三场景、main 守卫四种写法、`run` 三种写法、stub 计数被子 shell 吞 |
| 2026-09-04 | 课 13《安全性能与选型》 | 主 agent 内联（pedagogy + learner） | 0 | P0×0 / P1×0（4 条 P2 为第四幕长代码块，已加「阅读提示」导航注释）。**照抄验证 35/35 通过**——初测 19 个失败，全部为「片段代码」（裸 `local`/`return`、调用未 source 的函数）或「依赖前序块数据」（`app.log`/`data.json`/`logs/` 不存在），已逐一改为 `text` 标记或补自包含前摇，**与课 8/11/12 同类问题同源（第四次踩「片段块标 bash」的坑）**。**三处常识/骨架描述被实测推翻或修正**：① 去掉 `cat` 的性能收益被严重高估——实测仅 **12%**（338ms vs 302ms），因为大头是 `wc` 自身 200 次 fork；去掉 `cat` 的真正理由是**避免管道开子 shell**（课 11），不是性能；② `[ ]` vs `[[ ]]` 差异 **1.2× 可忽略**（7ms vs 6ms，两者都是内置），`[[ ]]` 的价值是安全与功能不是速度；③ `--` 不是万能的——`tar -czf -- out.tar.gz dir/` **构建失败**，`-f` 后必须紧跟归档名，**带参数的选项内部顺序优先于 `--`**。**另抓到两个真 bug**：`local tag=$1 out="app-$tag.tar.gz"` 在 `set -u` 报 unbound variable，且外层有同名变量时**静默用错值**（app-GLOBAL 而非 app-v2）；`local tag=${1:-latest}` 的 `:-` 会把空串也替换成默认值，**掩盖空参数**（拦截矩阵漏了空 tag）。复核 30 条断言（A–J 十组）全通过：eval 泄露 vs 参数数组免疫、`%q` 转义与还原、glob 注入三文件、fork 倍数方向（expr 283× / basename 141×）、read 的 `-r`/`IFS=`/末行丢失三元、白名单正则 7 边界、JSON 判据（本机无 jq）、dash Bad substitution、退出码污染（rc=0 vs rc=3）、local 同行引用两种后果。⚠️ **性能数字已标注平台偏差**：WSL2 fork 跨 VM 边界，代价远高于原生 Linux（通常 50–150×），讲义明确写「倍数会变，结论不变」。全仓 86 条本地链接零断链 |
| 2026-09-04 | 结课实战项目 `release-audit`（Phase 3） | 主 agent 内联（pedagogy + learner） | 0 | P0×0 / P1×0。交付 1053 行主程序（1 主程序 + 3 库 + 31 函数）+ 969 行测试，57 条断言全通过，复杂度四门槛全达标。**五处真 bug 被实测抓出并修复**：① `ra_mktemp_dir` 用 stdout 返回值，调用方必须命令替换 → 子 shell 吞掉数组登记 → 临时目录泄漏（改 nameref 出参，`BASH_SUBSHELL` 1→2 为铁证）；② nameref 循环引用——函数内 `local d` 与调用方传来的 `d` 同名，bash 报 `circular name reference`，赋值静默失效（改用 `_ra_tmp_`/`_ra_nref_` 前缀隔离，系课 6 陷阱的**第三变体**：撞的不是 nameref 变量名，而是函数内局部量与 nameref 指向的名字同名）；③ 库用 `declare -r` 却无 include guard，重复 source 在 `set -e` 下被 `declare: readonly variable` 报错中断，后续变量全未定义（加 guard）；④ 清单文件的 `version=2.1.0` 元数据行被当成必需文件名，clean 样例误报 BLOCK（补 `key=value` 跳过）；⑤ 测试运行器把用例文件的 source 放在函数内，`declare` 变量变局部（改子 shell 顶层 source）。**另纠正两处取证方法错误，均实测推翻我最初假设**：`set +o` 在函数/命令替换上下文里读 `errexit` 显示未开启（须用 `$SHELLOPTS`）；日志标签是 `%-5s` 补齐的 `[INFO ]` 带尾空格，用 `[INFO]` 永远匹配不上。运行时验证：10/100/400 文件下外部命令调用恒为 5 次（常量级，零循环内 fork）。评审过程本身抓出 1 处**脚本误报**（评审脚本自匹配自身的硬编码模式串，连踩两次），已按「误报不等于真缺陷、先分辨再修」处理 |
| 2026-09-03 | 大纲复审（骨架落盘后） | 主 agent 内联（pedagogy，子 agent 未创建） | 0 | P0×2 已修：① 课 8 补「作业控制与并发」 ② 课 12 补 `printf` vs `echo`。知识点总数 39 → 40 |

## 每批交付前强制项（质量门禁）

> 依据本仓库既有约定与历史教训固化，逐批执行，不得以"内容简单"为由跳过。

- [ ] **四向逐字一致性校验**：讲义 bash 代码块 ↔ 脚本文件、讲义输出块 ↔ 运行时 stdout 逐字一致（含行数核对）
- [ ] **围栏与结构校验**：代码围栏全闭合、`<details>` 配对、题号连续、占位符清零
- [ ] **进阶适配校验**：本批是否出现"把进阶者当零基础教"的段落（如解释 `cd`、解释管道是什么）→ 有则删
- [ ] **档案四处回写**：`00-学习档案.md` 进度表 + `00-评审清单.md` 勾选 + `stages/N-*/overview.md` 标 ✅ + **`02-课程目录.md` 与 `01-学习路径总览.md` 索引同步**
- [ ] **链接可达性检查**：本批新增链接（含索引与上一课/下一课）逐条可打开
