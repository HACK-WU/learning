# 第 2 课：基础类型标注与推导

> 所属阶段：阶段 1《类型思维启蒙》｜ 水平：零基础 TS
> 本课知识点：原始类型标注与类型推导、数组、元组与只读、函数类型标注
> 故事情节：主角开始给项目加类型标注，发现**大部分地方什么都不用写**——TS 已经知道了
> ✅ 状态：⬜ 待编写 ｜ 实操环境：Node.js v22.14.0 + TypeScript 7.0.2

## 🎯 本课目标

- 为变量、数组、元组、函数写出正确的类型标注，并判断某处**该写注解还是靠推导**
- 说清 `readonly` 与 `as const` 的差别，以及"可变与只读"之间的赋值方向
- 区分 `void` / `never` / `undefined` 与可选参数，为任意函数写出完整签名

## 📌 知识点导航

| # | 知识点 | 关键点 | 状态 |
|---|--------|--------|------|
| 1 | 原始类型标注与类型推导 | 原始类型注解写法 / 推导何时生效 / 必须显式标注的场合 / 注解 vs 推导的取舍 | ⬜ |
| 2 | 数组、元组与只读 | `T[]` 与 `Array<T>` / 元组的定长与位置类型 / `readonly` 与 `as const` / 可变与只读的赋值方向 | ⬜ |
| 3 | 函数类型标注 | 参数与返回值 / 可选参数与默认参数 / 剩余参数 / 函数类型表达式 / `void` 与 `never` | ⬜ |

## 📦 前置依赖（JS 概念）

| 用到的 JS 概念 | 掌握要求 | 回补 |
|---------------|---------|------|
| 7 种原始类型与 `typeof` | 需理解（本课直接建在其上） | [JS 课 1 变量与类型](../javascript-core/stages/1-值与作用域/lessons/lesson-01-变量与类型.md) ✅ 已学 |
| 函数的参数 / 默认值 / 剩余参数 | 会用即可 | 本节课内一句话带过 |
| 数组的常用方法 | 会用即可 | — |

---

<!-- 以下正文由 Phase 2 按五幕叙事骨架填充 -->

## 第一幕：起源与场景引入

（待填充）

## 第二幕：认知冲突

（待填充）

## 第三幕：层层揭示

（待填充）

## 第四幕：实操验证

（待填充）

## 第五幕：体系收束

（待填充）
