# JavaScript 语言核心（ES6+）评审清单

> **强制检查点**：每批内容生成后，先在本文件找到对应未勾选条目，完成双视角评审（教学法视角 + 学习者视角）且 **P0 清零**后，才能勾选并交付。
> **未勾选 = 评审未完成，禁止进入下一批。**

## 待评审清单

- [x] 大纲评审（Phase 1）— P0=0 通过（2026-08-31）
- [x] 阶段 1 · 课 1《变量与类型》 — P0=0 通过（2026-08-31）
- [x] 阶段 1 · 课 2《值的复制与比较》 — P0=0 通过（2026-08-31）
- [x] 阶段 1 · 课 3《作用域与闭包》 — P0=0 通过（2026-08-31，阶段 1 收官）
- [x] 阶段 2 · 课 4《函数是一等公民》 — pedagogy + learner 双视角，P0=0 通过（2026-09-02）
- [x] 阶段 2 · 课 5《this 到底指向谁》 — pedagogy + learner 双视角，P0=0 通过（2026-09-02）
- [x] 阶段 2 · 课 6《原型与类》 — pedagogy + learner 双视角，P0=0 通过（2026-09-02，阶段 2 收官）
- [x] 阶段 3 · 课 7《事件循环》 — pedagogy + learner 双视角，P0=0 通过（2026-09-02）
- [x] 阶段 3 · 课 8《Promise 与 async/await》 — pedagogy + learner 双视角，P0=0 通过（2026-09-03）
- [x] 阶段 3 · 课 9《现代语法与内置数据结构》 — pedagogy + learner 双视角，P0=0 通过（2026-09-03，阶段 3 收官）
- [x] 阶段 4 · 课 10《模块化》 — pedagogy + learner 双视角，P0=0 通过（2026-09-03）
- [x] 阶段 4 · 课 11《错误处理与调试》 — pedagogy + learner 双视角，P0=0 通过（2026-09-03）
- [x] 阶段 4 · 课 12《内存·性能与选型收束》 — pedagogy + learner 双视角，P0=0 通过（2026-09-03，全部 12 课收官）
- [x] **结课实战项目**（Phase 3）— pedagogy + learner 双视角，P0=0 通过（2026-09-03，《异步任务编排器》，复杂度四门槛均达标）
- [x] **实战经验 / 排障速查手册 / 场景解法库**（Phase 5）— pedagogy + learner 双视角 + **证据纪律核查**，P0=0 通过（2026-09-04，全部流程收官）

## 评审记录（勾选后追加结论摘要）

| 日期 | 评审对象 | 评审方式 | P0 数 | 意见摘要 |
|------|----------|----------|-------|----------|
| 2026-08-31 | 大纲评审 | 主 agent 内联（pedagogy + learner） | 0 | P1×3 全部采纳（课 7 微任务前置 / 课程边界声明 / 课 6 逆向讲法），详见学习档案 |
| 2026-08-31 | 课 1 批次 | 主 agent 内联（pedagogy + learner） | 0 | P1×4 全部采纳（回调输出顺序说明 / 10 天传闻降级 / 困惑 N 显式回扣 / slice 魔数注释） |
| 2026-08-31 | 课 2 批次 | 主 agent 内联（pedagogy + learner） | 0 | P1×5 全部采纳（横评表表述 / 循环引用示例去重 / 困惑 N 回扣 / @@toPrimitive 白话 / 课 1 导航顺序统一） |
| 2026-08-31 | 课 3 批次 | 主 agent 内联（pedagogy + learner） | 0 | P1×4 全部采纳（Scheme 起源精确化 + 补核实 / 三处困惑 N 回扣 / 补"循环体内改 let i"实测 / 修正一处预判错误） |
| 2026-09-02 | 课 4 批次 | 主 agent 内联（pedagogy + learner） | 0 | **P0×1 已修**：原文 NFE 写 `typeof me` 报 ReferenceError，实测为 `'undefined'`（`typeof` 对未声明标识符安全），已改 `me(1)` 并补说明；P1×1 采纳（`arguments` 联动表后补工程结论句）。两视角无冲突 |
| 2026-09-02 | 课 5 批次 | 主 agent 内联（pedagogy + learner） | 0 | **P0×1 已修**：判定流程图把「是否箭头函数」放在**最后一问**，会导致 `obj.arrowFn()` 被误判成隐式绑定 → 已提到**第一问**并加警示段；P1×1 采纳（`call`/`apply` 修复行改写为「立即执行陷阱」）。小测 Q2/Q5 答案经实测复核一致。两视角无冲突 |
| 2026-09-02 | 课 6 批次 | 主 agent 内联（pedagogy + learner） | 0 | 无 P0。落盘前由实测推翻并修正两处：①「组合继承父构造两次」初稿测成 1 次（计数器在建立原型链之后才清零），已重写为「建链 1 次 + 每实例 1 次 = 2 次」并补「原型上留冗余 name」证据；② 论证「class 方法不可枚举」时用了一个已被演示污染的 prototype（返回 `['study']`），已换干净类重测得 `[]` 并补 `getOwnPropertyNames`。P1×1 采纳（类比补三处失效边界）。两视角无冲突 |
| 2026-09-02 | 课 7 批次 | 主 agent 内联（pedagogy + learner） | 0 | 无 P0。P1×1 采纳：`setImmediate` 与 `setTimeout(0)` 的顶层不定序，已用「实测 4 次：3 次 setTimeout 先 / 1 次 setImmediate 先」+「I/O 回调内 4/4 定序」给出可复现证据，并给「别依赖顺序」的实用结论。微任务仅借 `Promise.then` 外壳（遵循大纲 P1 约定）。两视角无冲突 |
| 2026-09-03 | 课 8 批次 | 主 agent 内联（pedagogy + learner） | 0 | **P0×1 已修**：原稿称「CommonJS（普通 `.js`）顶层 `await` 会 SyntaxError」，实测相反——Node v22.14.0 对无 package.json 的 `.js` 做语法检测，含顶层 await 即按 ESM 加载、能跑通；真正报 SyntaxError 的是 `.cjs`。已改为 `.mjs` / `.js` / `.cjs` 三行实测对照表并加警示段。**教训：环境相关的断言必须实测，不能凭常识书写。** P1×1 采纳（`all` 失败时其他请求未被取消、只是结果被丢弃 → 写入类比失效边界与误区）。两视角无冲突 |
| 2026-09-03 | 课 9 批次 | 主 agent 内联（pedagogy + learner） | 0 | 无 P0。**探针阶段抓到一处反向结论**（浅拷贝示例标注「defaults 被改了」，实测 `retries` 仍为 3 说明未被改——因 `merged.nested` 指向 `userOpts.nested` 而非 `defaults.nested`），已改用「只展开 defaults」的正确用例重测后写入正文，未污染最终产物。P1×1 采纳（`for...in` 会带上自定义属性 → 在对照实测中突出）。另补充：实测确认本机 Node v22.14 已支持 ES2025 的 Set 原生方法，已写入正文。两视角无冲突 |
| 2026-09-03 | 结课实战项目（Phase 3） | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | 0 | **P0×2 已修**：① 大纲要求知识点地图须含「文件链接」，初版只写了课号没给链接 → 已把 12 课全部补成可点击链接；② 覆盖率统计写「12 课中的 11 课」，与实际不符 → 改为「4 阶段 / 12 课全部涉及」并如实标注课 2 关联最弱（不硬凑）。**P1×1 采纳**（补「这个项目在整门课里的位置」章节，给出全局定位与下一步）。**复杂度四门槛逐条核对均达标**：门槛 1 跨 **4** 个阶段（要求 ≥3）；门槛 2 含 **4** 项非功能约束（性能/错误处理/内存/可维护性，要求 ≥2）；门槛 3 有 **3** 个真权衡决策点（要求 ≥2，逐个验证「换个人可能选另一个」均成立）；门槛 4 为 **6 源码模块 + 2 示例 + 1 测试**的多文件工程。**专项检查项**：反例确实「看起来能跑」（实测 exit 0，7 条问题均在小规模测试中隐形）；代码真能跑（**24 条断言 24 通过 / 0 失败**，Node v22.14.0 实测）。**落盘前由链接校验抓到一个真 bug**：README 里 24 条课程链接多写了一层 `../../../`（应为 `../../`），**全部失效** —— 已修正并经校验确认 `checked 25 links; missing 0`。两视角无冲突 |
| 2026-09-03 | 课 12 批次 | 主 agent 内联（pedagogy + learner） | 0 | 无 P0。**探针阶段修正 1 处实验设计**：初版 Map vs WeakMap 对照把 WeakMap 本身做成了局部变量（函数返回后不可达），导致两组内存数字**毫无差别**，无法支撑结论 → 重做为「两个容器都常驻、只丢弃键」，得到教科书级对照（156.9 → 95.9 MB，差值 61.0 MB）。**另由实测 + MDN 抓到一条比原计划更重要的结论**：`FinalizationRegistry` 的清理回调**规范不保证被调用**（MDN 原文 "is not required to call cleanup callbacks"，"or not at all"），初版第 ⑤ 个实验因此反复失败，已改为把该限制本身写入正文并给出「确定性清理要用 try...finally」的工程结论。P1×3 采纳（引用计数模拟补函数作用域使注释自洽 / 补「✅ 困惑 3 已解」闭合故事弧 / OOM 退出码 134 的措辞区分 Unix 惯例与 Windows 实测）。**KP3 按要求走事实核查并标注时点（2026-09）**，全部取自官方一手来源；其中 Bun 与 Deno 官网基准**互相矛盾**的事实被保留并作为核心教学素材。两视角无冲突 |
| 2026-09-03 | 课 11 批次 | 主 agent 内联（pedagogy + learner） | 0 | **探针阶段推翻 3 条预设**（均已在落盘前修正，未污染产物）：①「`console.log` 会触发 getter 副作用」→ 实测 Node 打印 `[Getter]` 而**不调用**它（`_n` 保持 0）；②「`process.exit()` 会截断 stdout 日志」→ 实测文件重定向与管道**都完整**（20000 行无截断），Node 对该坑不成立；③「自定义错误类必须写 `Error.captureStackTrace`」→ 实测 V8 已自动隐藏 `extends Error` 的构造帧，写不写栈顶一致（该 API 有效但用武之地在「辅助函数里藏中间帧」）。**另发现一个比原计划更有价值的真实 bug 并写入正文**：`new Promise` **接不住**其 executor 之外的异步回调里抛的错，且 Promise 会**永远 pending**（`await` 它则静默挂死）。P1×4 采纳（三个坑编号理顺为 1/2/3，深度截断并入坑 1 / 补「✅ 困惑 3 已解」闭合故事弧 / 第一幕代码标为示意并指向第四幕可运行版 / 小测 Q3 收窄到实测可支撑的范围）。**顺带修复课 10 遗留**：阶段 4 概览仍写「ESM 循环依赖不报错却拿到 undefined」，已按课 10 实测结论改为三种表现。两视角无冲突 |
| 2026-09-03 | 课 10 批次 | 主 agent 内联（pedagogy + learner） | 0 | **纠正大纲一处不准确预设**：骨架与阶段概览均写「ESM 循环依赖**不报错**却拿到 `undefined`」，实测推翻——`export const` + 顶层同步访问会抛 **`ReferenceError: Cannot access 'a' before initialization`**（TDZ）；只有 `var`（提升为 undefined，不进 TDZ）与「延迟访问」另两种表现。已按三种情况准确呈现，并写入误区与小测。**知识点名称未变，属内容精化，非结构性调整**，大纲不需改动。另补测 CJS↔ESM 互操作边界（命名导入能否识别取决于 CJS 三种写法），为决策参考表提供实测依据。两视角无冲突 |

| 2026-09-04 | 实战经验 / 排障速查手册 / 场景解法库（Phase 5） | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | 0 | **P0×1 已修**：`09` 的索引表与交叉引用使用了 Markdown 锚点链接，而**中文标题的锚点在不同渲染器（GitHub / VS Code / GitLab）下生成规则不一致**——排障手册的核心价值就是快速跳转，锚点失效等于毁掉它 → 全部改为「条目 N」纯文字编号引用；`08` 速览表与 `10` 的同款锚点一并清除。**P1×3 全部采纳**：① `08` 补「故障模式速览表」（按「踩中概率 × 后果严重度」排序，不与正文编号冲突）② 补 FM2 / FM9 缺失的知识点回指，FM7 标注「正则属课外补充」并说明为何仍收录 ③ `unref` 术语白话化（目标读者是入门水平）。**证据纪律核查（Phase 5 从严）**：10 条故障模式的「症状—根因—修复」全部经 Node v22.14.0 实测复现，或可溯源至官方一手来源；`util.promisify` / `Error.cause` / `AbortSignal.timeout` / `--cpu-prof` / `import/no-cycle` 五项写入前未实测或核实，已补测补核（前四项实测通过，第五项联网核实存在）。**探针阶段抓到一条真 bug**：`AbortSignal.timeout()` 的内部定时器是 **unref 的**，无其它活跃句柄时 **abort 不触发、进程静默退出（exit 0）**，而同条件 `setTimeout` 正常触发 → 直接推翻并修正了产物中「加超时」建议的普适性，已补入适用边界表、FM4 与手册条目 4。**另证伪一个猜测**：「没保存 signal 引用会被 GC 掉」不成立（强制 `gc()` 后仍触发），真因是 unref 而非 GC。**链接校验**：66 条本地链接，`missing 0`，锚点链接 0。两视角无冲突 |

> 评审结论的完整摘要记入 [`00-学习档案.md`](00-学习档案.md) 的「评审记录」表；本清单以勾选状态为准。
