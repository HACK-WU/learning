# JavaScript 语言核心（ES6+）学习档案

## 学习者画像

| 项目 | 值 | 来源 |
|------|-----|------|
| 主题 | JavaScript 语言核心（ES6+） | 用户指定 |
| 当前水平 | 入门（写过一些 JS / jQuery，用过皮毛） | 用户指定 |
| 学习目标 | 理解概念 + 动手实操 + 决策参考（三选多选） | 用户指定 |
| 输出格式 | Markdown（主载体，Mermaid + SVG 图表） | 默认值 |
| 模式 | 课程制（完整体系，4 阶段 / 12 课 / 36 知识点） | 自动判定 |
| 产物目录 | `d:/projects/learning/frontend/javascript-core/`（topic-slug = `javascript-core`） | 用户指定 |

## 实操环境（本机实测 · 2026-08-31）

| 项 | 值 |
|----|-----|
| 运行时 | **Node.js v22.14.0**（`C:\nvm4w\nodejs\node.exe`，由 nvm4w 管理） |
| 包管理器 | npm 10.9.2 |
| Shell | Windows PowerShell |
| 浏览器侧 | Chrome / Edge DevTools（课 11 调试、课 12 Memory 面板会用到） |

> 目标含「动手实操」，所有可运行示例默认以 **Node.js v22.14.0** 为准（`node xxx.js` / `node -e "..."`），
> 涉及浏览器行为的示例另行标注。若后续发现命令与本机不符，就地更新本表并告知。

## 三大目标如何落地

| 目标 | 落地方式 |
|------|----------|
| ① 理解概念 | 每课按「场景引入 → 认知冲突 → 层层揭示 → 实操验证 → 体系收束」五幕展开；核心概念配类比并指出类比失效边界 |
| ② 动手实操 | 每课第四幕给可在 Node v22.14.0 直接跑的示例代码 + 预期输出，并回扣第一幕场景 |
| ③ 决策参考 | 课 6（继承写法取舍）、课 10（CommonJS vs ESM）、课 12（JS vs TS、运行时与工具链选型）三处给对比 + 决策清单 |

## 知识点级进度表

| 阶段 | 课 | 知识点 | 状态 | 完成日期 | 小测成绩 |
|------|-----|--------|------|----------|----------|
| 1 | 课 1 | var·let·const 与 TDZ | ✅ 已完成 | 2026-08-31 | - |
| 1 | 课 1 | 数据类型全景 | ✅ 已完成 | 2026-08-31 | - |
| 1 | 课 1 | 类型检测的四种方式 | ✅ 已完成 | 2026-08-31 | - |
| 1 | 课 2 | 原始值 vs 引用值 | ✅ 已完成 | 2026-08-31 | - |
| 1 | 课 2 | 深浅拷贝 | ✅ 已完成 | 2026-08-31 | - |
| 1 | 课 2 | == vs === 与隐式转换 | ✅ 已完成 | 2026-08-31 | - |
| 1 | 课 3 | 词法作用域与作用域链 | ✅ 已完成 | 2026-08-31 | - |
| 1 | 课 3 | 变量提升与块级作用域 | ✅ 已完成 | 2026-08-31 | - |
| 1 | 课 3 | 闭包 | ✅ 已完成 | 2026-08-31 | - |
| 2 | 课 4 | 函数的四种定义方式与差异 | ✅ 已完成 | 2026-09-02 | - |
| 2 | 课 4 | 参数机制 | ✅ 已完成 | 2026-09-02 | - |
| 2 | 课 4 | 高阶函数与回调 | ✅ 已完成 | 2026-09-02 | - |
| 2 | 课 5 | this 的四种绑定规则 | ✅ 已完成 | 2026-09-02 | - |
| 2 | 课 5 | 箭头函数的 this | ✅ 已完成 | 2026-09-02 | - |
| 2 | 课 5 | 手写 call·apply·bind 与 new | ✅ 已完成 | 2026-09-02 | - |
| 2 | 课 6 | 原型与原型链 | ✅ 已完成 | 2026-09-02 | - |
| 2 | 课 6 | class 语法与本质 | ✅ 已完成 | 2026-09-02 | - |
| 2 | 课 6 | 继承的五种写法与取舍 | ✅ 已完成 | 2026-09-02 | - |
| 3 | 课 7 | 单线程与调用栈 | ✅ 已完成 | 2026-09-02 | - |
| 3 | 课 7 | 宏任务与微任务 | ✅ 已完成 | 2026-09-02 | - |
| 3 | 课 7 | 事件循环全流程 | ✅ 已完成 | 2026-09-02 | - |
| 3 | 课 8 | Promise 状态机 | ✅ 已完成 | 2026-09-03 | - |
| 3 | 课 8 | 错误处理与组合方法 | ✅ 已完成 | 2026-09-03 | - |
| 3 | 课 8 | async/await | ✅ 已完成 | 2026-09-03 | - |
| 3 | 课 9 | 解构·展开·模板字符串 | ✅ 已完成 | 2026-09-03 | - |
| 3 | 课 9 | Map·Set·WeakMap·WeakSet | ✅ 已完成 | 2026-09-03 | - |
| 3 | 课 9 | 迭代器·生成器·可选链 | ✅ 已完成 | 2026-09-03 | - |
| 4 | 课 10 | CommonJS 与 ESM | ✅ 已完成 | 2026-09-03 | - |
| 4 | 课 10 | import·export 全语法 | ✅ 已完成 | 2026-09-03 | - |
| 4 | 课 10 | 循环依赖与动态导入 | ✅ 已完成 | 2026-09-03 | - |
| 4 | 课 11 | Error 体系与 throw | ✅ 已完成 | 2026-09-03 | - |
| 4 | 课 11 | try·catch·finally 与异步错误 | ✅ 已完成 | 2026-09-03 | - |
| 4 | 课 11 | 调试工具链与 Source Map | ✅ 已完成 | 2026-09-03 | - |
| 4 | 课 12 | 垃圾回收机制 | ✅ 已完成 | 2026-09-03 | - |
| 4 | 课 12 | 常见内存泄漏与排查 | ✅ 已完成 | 2026-09-03 | - |
| 4 | 课 12 | 选型收束：JS vs TS、运行时与工具链 | ✅ 已完成 | 2026-09-03 | - |

## 评审记录

> 评审方式：`course-reviewer` 子 agent **尚未创建**，当前为「主 agent 内联（pedagogy + learner 双视角）」，独立性受限。
> 如需更独立的评审视角，可按 `create-sub-agent` 规范创建 `course-reviewer` 子 agent。

| 日期 | 评审对象 | 评审方式 | 意见摘要 | 处置 |
|------|----------|----------|----------|------|
| 2026-08-31 | 多阶段大纲 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | P0×0 / P1×3 | 三条 P1 全部采纳（见下） |
| 2026-08-31 | 课 1《变量与类型》 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | P0×0 / P1×4 | 两视角无冲突，P1 全部采纳：① 第四幕补"回调为何最后输出"（并埋课 7 伏笔）② "10 天写 JS"传闻降级为括号内补充 + 置信度标注 ③ 知识点 1/2 末尾加"✅ 困惑 N 已解"闭合故事弧 ④ `slice(8,-1)` 补魔数注释 |
| 2026-08-31 | 课 1 事实核查 | 实测 + 权威源核查 | — | 全部示例代码经 Node v22.14.0 实跑；JS 起源（Wikipedia，引 Netscape/Sun 1995-12-04 新闻稿）、`typeof null` 成因（MDN，引 2ality）核查通过；"10 天写出 JS"未获权威源证实，已标 ⏳ 置信度：低 |
| 2026-08-31 | 课 2《值的复制与比较》 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | P0×0 / P1×5 | 两视角无冲突，P1 全部采纳：① 横评表"浅拷贝含函数 ✅"改"—（不递归，原样带过去）"② 循环引用示例改直白写法并去掉与第四幕的重复 ③ KP1/2/3 末尾补"✅ 困惑 N 已解"（与课 1 体例一致）④ `@@toPrimitive` 补白话解释 ⑤ 课 1 导航区块顺序统一为「上一课/下一课/返回目录」 |
| 2026-08-31 | 课 2 事实核查 | 实测 + 权威源核查 | — | 全部示例代码经 Node v22.14.0 实跑；`structuredClone` 经 **Node.js 官方 API 文档**（Added in v17.0.0，属 WHATWG HTML `structuredClone`）+ **MDN**（浏览器 Baseline 自 2022-03 广泛可用）双源核实；`[] == ![]` 五步推演逐步实测验证；无未证实内容进入正文 |
| 2026-08-31 | 课 3《作用域与闭包》 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | P0×0 / P1×4 | 两视角无冲突，P1 全部采纳：① 起源精确到 Scheme「1975 年 / MIT / Steele 与 Sussman / 第一个采用词法作用域的 Lisp 方言」并补联网核实 ② KP1/2/3 末尾补"✅ 困惑 N 已解"③ 补"循环体内改 `let i`"的实测示例（证明每轮复制）④ 修正一处预判错误：循环体内改 `i` 会改变轮次，改用 `i < 30` 的干净示例 |
| 2026-08-31 | 课 3 事实核查 | 实测 + 权威源核查 | — | 全部示例代码经 Node v22.14.0 实跑；Scheme 起源经**维基百科 Scheme 词条**核实（1975 / MIT / Steele 与 Sussman / 首个采用词法作用域的 Lisp 方言）；JS 与 Scheme 渊源沿用课 1 已核实结论；闭包内存实验用 `node --expose-gc` 实测（4.6 → 4.2 → 19.4 → 4.2 MB） |
| 2026-08-31 | 阶段 1 完成 | — | — | 阶段 1《值与作用域》3 课 9 知识点全部完成；大纲无结构性调整，接力提示词已指向阶段 2 课 4 |
| 2026-09-02 | 课 4《函数是一等公民》 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | P0×0（1 条已在落盘前修正）/ P1×1 | P0：原文 NFE 处写 `typeof me` 报 ReferenceError，实测为 `'undefined'`（`typeof` 对未声明标识符安全）→ 已改为 `me(1)` 并补一段说明（与课 1 已讲特性一致）。P1：「arguments 联动」四情况表后补工程结论句（用剩余参数取代 arguments）。两视角无冲突 |
| 2026-09-02 | 课 4 事实核查 | 实测 + 权威源核查 | — | 全部示例代码经 Node v22.14.0 实跑（含 `map(parseInt)`、`arguments` 四种联动、稀疏数组调用次数、异步错误崩溃退出 exit code 1）；「一等公民」术语起源经 **Wikipedia First-class function 词条**核实（Strachey 1960 年代中期提出，1967 讲义《Fundamental Concepts in Programming Languages》以 ALGOL 过程为 second class 反例；2000 年正式发表）；Lisp 动态作用域与 funarg problem 同源核实；JS 与 Scheme 渊源沿用课 1/课 3 已核实结论 |
| 2026-09-02 | 课 5《this 到底指向谁》 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | P0×0（1 条已在落盘前修正）/ P1×1 | P0：判定流程图把「是否箭头函数」放在最后一问，`obj.arrowFn()` 会被误判成隐式绑定 → 已提到第一问并加警示段。P1：「修复手段」表中 `call`/`apply` 一行改写为「立即执行陷阱」（`setTimeout(fn.call(o), ms)` 传的是结果不是函数）。两视角无冲突 |
| 2026-09-02 | 课 5 事实核查 | 实测 + 权威源核查 | — | 全部示例代码经 Node v22.14.0 实跑（四条规则、隐式丢失四写法、`call(null)`→globalThis、`bind` 不可二次覆盖、`new` 覆盖 `bind`、手写三件套与原生行为一致、`new` 返回值规则）；小测 Q2/Q5 答案单独实测复核一致。**`this` 的动态绑定设计动机未检索到权威来源（Wikipedia / MDN 均未记载）→ 已在正文标注 ⏳ 置信度：低，只陈述结果不作断言**；"1995 年网景要求语法像 Java"沿用课 1 已核实结论 |
| 2026-09-02 | 课 6《原型与类》 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | P0×0 / P1×1 | 无 P0。**落盘前由实测推翻并修正两处**：①「组合继承父构造两次」初稿测成 1 次（计数器在 `Child.prototype = new Parent()` 之后才清零），已重写为「建链 1 次 + 每实例 1 次 = 2 次」，并补上真正缺陷证据「原型上躺着一份多余的 name」；② 论证「class 方法不可枚举」时用了一个已被前面演示污染的 prototype（返回 `['study']`），已换干净类重测得 `[]`，并补 `getOwnPropertyNames` 证明方法确实在原型上。P1×1 采纳（类比的「共享工具间 vs 家族族谱」补三处失效边界）。本课采用大纲约定的**逆向讲法**。两视角无冲突 |
| 2026-09-02 | 课 6 事实核查 | 实测 + 权威源核查 | — | 全部示例代码经 Node v22.14.0 实跑（三角关系四等式、读/写不对称、共享引用值、class 五条证据、`#x` 私有字段、五种继承逐项实测、`super` 报错原文）；**Self 语言起源经 Wikipedia Self 词条核实**（David Ungar 与 Randall Smith，1986 年设计于施乐 PARC，1987 年首个可用编译器；JS 列入其 Influenced 名单）；**ES6 class 是原型继承的语法糖**经 Wikipedia 原型编程词条核实（引 MDN Classes）；「写操作只遮蔽不改原型」经 Wikipedia 原型编程词条背书；`#x` 类外访问为**解析期 SyntaxError**（`try/catch` 抓不到）经独立文件实测确认 |
| 2026-09-02 | 阶段 2 完成 | — | — | 阶段 2《函数与对象》3 课 9 知识点全部完成（课 4 / 课 5 / 课 6）；大纲无结构性调整，接力提示词已指向阶段 3 课 7 |
| 2026-09-02 | 课 7《事件循环》 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | P0×0 / P1×1 | 无 P0。P1×1 采纳：`setImmediate` 与 `setTimeout(0)` 的顶层不定序，已用实测 4 次（3 次 setTimeout 先 / 1 次 setImmediate 先）+ I/O 回调内 4/4 定序给出可复现证据，并给「别依赖顺序」的结论。微任务仅借 `Promise.then` 外壳，机制留课 8（遵循大纲 P1 约定）。两视角无冲突 |
| 2026-09-02 | 课 7 事实核查 | 实测 + 权威源核查 | — | 全部示例代码经 Node v22.14.0 实跑（栈溢出 12549 层、0ms 定时器被 200ms 循环拖到 200ms、两级队列、微任务连锁清空、经典题 1 3 5 4 2、`process.nextTick` 优先、`setImmediate` 顶层不定序 / I/O 回调内定序）；**嵌套定时器 4ms 下限经 WHATWG HTML Living Standard §8.7 Timers 原文核实**（"If nestingLevel is greater than 5, and timeout is less than 4, then set timeout to 4"），并标注该规则是浏览器规则、Node 实测不遵循（本机稳定 ~15ms，疑为 Windows 定时器粒度，仅报实测值不妄断）；「为什么单线程」已标注为社区公认动机解释、非规范条文 |
| 2026-09-03 | 课 8《Promise 与 async/await》 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | P0×0（1 条已在落盘前修正）/ P1×1 | P0：原稿称「CommonJS（普通 `.js`）顶层 `await` 会 SyntaxError」，**实测相反**——Node v22.14.0 对无 package.json 的 `.js` 做模块语法检测，含顶层 `await` 即按 ESM 加载、能跑通；真正抛 `SyntaxError` 的是 `.cjs`（已核实目录树中无 package.json）。已改为三行实测对照表 + 警示段。**教训记录：环境相关的断言必须实测，不能凭常识书写。** P1×1 采纳（`all` 失败时其他请求并未被取消、只是结果被丢弃 → 写入类比失效边界与常见误区）。两视角无冲突 |
| 2026-09-03 | 课 8 事实核查 | 实测 + 权威源核查 | — | 全部示例代码经 Node v22.14.0 实跑（三态不可逆、then 返回新 Promise、值穿透、错误冒泡、第二参数 vs catch、四个组合器、`async` 返回值恒为 Promise、`await` 不阻塞 start→A→end→B、串行 535ms vs 并发 109ms、forEach 无效、try+await 能抓 / 未 await 抓不到、unhandledRejection 使进程崩溃退出码 1）；**Promise 起源经 Wikipedia「Futures and promises」词条核实**（1976 Friedman & Wise 提出 promise 术语、1977 Baker & Hewitt 提出 future、1988 Liskov & Shrira 论文；JS 的 Promise 在 ES2015、`async`/`await` 在 ES2017；`async/await` 语法流行于 .NET 4.5（2010 宣布 / 2012 发布），受 F# 异步工作流（2007）启发）。⚠️ **该词条导语与 History 章节对「谁首创 promise 一词」说法不一致**（导语归 Friedman & Wise 1976，History 归 Liskov & Shrira），已在正文显式标注，按「概念 1976 提出、1988 在分布式系统语境确立」的稳妥口径陈述 |
| 2026-09-03 | 课 9《现代语法与内置数据结构》 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | P0×0 / P1×1 | 无 P0。**探针阶段抓到一处反向结论**：浅拷贝示例标注「defaults 被改了」，实测 `defaults.nested.retries` 仍为 3（未被改）—— 根因是 `merged.nested` 指向 `userOpts.nested` 而非 `defaults.nested`。已改用「只展开 defaults」的正确用例重测（改嵌套 → 99 被改；改顶层 → 不受影响）后写入正文，未污染最终产物。P1×1 采纳（`for...in` 会带上自定义属性 → 对照实测中突出）。另补充实测确认本机已支持 ES2025 的 Set 原生方法。两视角无冲突 |
| 2026-09-03 | 课 9 事实核查 | 实测 + 权威源核查 | — | 全部示例代码经 Node v22.14.0 实跑（解构各类、函数参数解构、浅拷贝三层证据、模板字符串与标签模板、Map vs Object 键类型、Map 保序与迭代、Set 去重与集合运算、WeakMap 无 size/不可遍历/键必须对象、Map 强引用 vs WeakMap 弱引用（`--expose-gc`）、Symbol.iterator 手动 next、for...of vs for...in、生成器暂停恢复与无限序列、`??` vs `\|\|` 六值对照表）；**版本时间线经 Wikipedia「ECMAScript version history」核实**：ES5 于 2009 年 12 月发布、ES6/ES2015 于 2015 年 6 月定稿（**ES4 因复杂度分歧胎死腹中，最后草案停在 2003 年 6 月**，遗留特性并入 Harmony 计划）；**对象展开 `...` 属 ES2018（ES9）而非 ES6**；**可选链 `?.` 与空值合并 `??` 属 ES2020（ES11）**；ES2025 新增 Set 原生方法并已在本机实测可用 |
| 2026-09-03 | 阶段 3 完成 | — | — | 阶段 3《异步与现代语法》3 课 9 知识点全部完成（课 7 / 课 8 / 课 9）；大纲无结构性调整，接力提示词已指向阶段 4 课 10 |
| 2026-09-03 | 课 10《模块化》 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | P0×0 | **纠正大纲一处不准确预设**：骨架与阶段概览写「ESM 循环依赖**不报错**却拿到 `undefined`」，实测推翻——`export const` + 顶层同步访问抛 **`ReferenceError: Cannot access 'a' before initialization`**（TDZ，回扣课 1）；`export var` 才是 `undefined`；延迟访问（放函数里）完全正常。已按三种情况准确呈现，写入正文、误区与小测 Q2。**知识点名称未变，属内容精化，非结构性调整**。另补测 CJS↔ESM 互操作边界，为决策参考表提供实测依据。本课承载的「决策参考」专项（CJS vs ESM 选型 + 翻转条件）已落地。两视角无冲突 |
| 2026-09-04 | Phase 4 课程手册汇总 | — | — | 生成 `final-课程手册.md`：手册开头放阶段总览 + 4 阶段 Mermaid 依赖图；12 课各配「一句话定位 + 3 知识点一句话 + 关键 SVG + 正文链接」；收录实战项目（需求 + 覆盖知识点地图 + 3 决策点摘要，**不抄源码**，链接回 README）；追加「决策清单」专项（6 条：值语义/相等比较/继承写法/模块化/TS/运行时）；末尾「🚀 接下来可以做什么」指向 Phase 5。**汇总完整性检查通过**：12 课全部纳入、阶段总览在开头、实战项目已收录、决策清单已加。更新 `01-学习路径总览.md`（进度指向 Phase 4 完成）与 `02-课程目录.md`（「汇总手册」条目改链接） |
| 2026-09-03 | 结课实战项目（Phase 3） | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | P0×0（2 条已修）/ P1×1 | 项目：**《异步任务编排器》**（`projects/异步任务编排器/`），零依赖、Node v22.14.0 实测、**24 条断言 24 通过 / 0 失败**。**P0×2 已修**：① 知识点地图缺「文件链接」（大纲要求）→ 12 课全部补成可点击链接；② 覆盖率统计「11 课」与实际不符 → 改为「12 课全部涉及」并如实标注课 2 关联最弱（不硬凑知识点）。**P1×1 采纳**（补全局定位章节）。**复杂度四门槛均达标**：跨 4 阶段 / 4 项非功能约束 / **3 个真权衡决策点**（并发机制三选一、超时真取消 vs 假取消、缓存 LRU vs WeakMap）/ 6 源码模块的多文件工程。**专项检查**：反例「看起来能跑」成立（实测 exit 0，7 条问题均在小规模测试中隐形）；代码真能跑（24/24）。**落盘前由链接校验抓到真 bug**：README 中 24 条课程链接多写一层 `../../../`，**全部失效**，已修正并校验 `25 links; missing 0`。**另修两处自造 bug**：① 演示脚本场景 3 的两个子场景共用计数器导致「真取消」误报为 1（实为上一段残留）→ 改为分段计数；② LRU 测试的期望值写错（误以为 a 该被保住）→ 重推 LRU 语义后修正。**教训：文档里的命令与链接必须实跑/实校，不能靠推算。** 两视角无冲突 |
| 2026-09-03 | **全部 12 课完成** | — | — | 4 阶段 / 12 课 / 36 知识点**全部讲完**（阶段 1《值与作用域》课 1–3、阶段 2《函数与对象》课 4–6、阶段 3《异步与现代语法》课 7–9、阶段 4《工程化与运行时》课 10–12）。大纲无结构性调整，全部评审 P0=0。按流程可进入 **Phase 3 综合实战项目** |
| 2026-09-03 | 课 12《内存·性能与选型收束》 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | P0×0 / P1×3 | 无 P0。**探针阶段修正 1 处实验设计错误**：初版 Map vs WeakMap 对照把 WeakMap 做成局部变量（返回后不可达），两组内存**毫无差别**、无法支撑结论 → 重做为「容器常驻 + 只丢弃键」，得到 156.9 → 95.9 MB（差值 61.0 MB）的干净对照。**另由实测 + MDN 抓到一条比原计划更重要的结论**：`FinalizationRegistry` 清理回调**规范不保证被调用**（MDN 原文 "is not required to call cleanup callbacks"、"or not at all"），初版第 ⑤ 实验因此反复失败 → 已把该限制本身写入正文，并给出「确定性清理必须用 `try...finally` / 显式 `close()`」的工程结论。P1×3 全部采纳（引用计数模拟补函数作用域 / 补「✅ 困惑 3 已解」闭合 / OOM 退出码 134 区分 Unix 惯例与 Windows 实测）。**KP3 承载的「决策参考」专项已落地**（JS vs TS 四条件判断清单 + 翻转点、运行时取舍清单、厂商基准三步甄别法）。两视角无冲突 |
| 2026-09-03 | 课 11《错误处理与调试》 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | P0×0 / P1×4 | 无 P0。**探针阶段由实测推翻并修正 3 条预设**：①「`console.log` 触发 getter 副作用」→ 实测 Node 打印 `[Getter]` 而**不调用**它；②「`process.exit()` 截断 stdout 日志」→ 实测文件与管道**均完整**（20000 行），Node 侧不成立；③「自定义错误类必须写 `Error.captureStackTrace`」→ 实测 V8 已自动隐藏 `extends Error` 构造帧。另**捕捉到一个比原计划更有价值的真实 bug 并写入正文**：`new Promise` 接不住 executor 之外异步回调里抛的错，且 Promise **永远 pending**。P1×4 全部采纳（坑编号理顺、补「✅ 困惑 3 已解」、第一幕代码标示意、小测 Q3 收窄到实测可支撑范围）。**顺带修复课 10 遗留**：阶段 4 概览中「ESM 循环依赖不报错却拿到 undefined」的旧预设已按课 10 实测改为三种表现。两视角无冲突 |
| 2026-09-03 | 课 12 事实核查 | 实测 + 权威源核查（**标注时点 2026-09**） | — | 全部示例代码经 **Node v22.14.0（`--expose-gc`）** 实跑：引用计数算法模拟、`FinalizationRegistry` 观察回收（普通对象 + **循环引用 A/B 双回收**，这是「V8 用可达性」的直接证据）、V8 堆分区实测（`new_space` 2MB / `old_space` 3.8MB / `code_space` / `trusted_space` / `large_object_space`）、`process.memoryUsage()` 五字段辨析、**OOM 实测**（`--max-old-space-size=64` → 日志中可见 `Mark-Compact` 与 `scavenge`，`FATAL ERROR: Reached heap limit`，退出码 134）、四种典型泄漏的复现与修复（意外全局 4.2→19.5→4.3 MB + 严格模式 `ReferenceError`；定时器 4.3→19.6→4.3 MB；闭包 4.3→19.6 MB，修复版**压根不涨**；Map vs WeakMap 156.9→95.9 MB，**差值 61.0 MB**）、`v8.writeHeapSnapshot()` 实测产出 88.17 MB 的 `.heapsnapshot`（确认与 DevTools 同格式）。**权威源**：GC 起源经 **Harvard CS252《Origins of Garbage Collection》** 核实（**John McCarthy 1959 年为 LISP 发明，算法即 mark-and-sweep，判据即可达性、用深度优先搜索**）；`FinalizationRegistry` 的「回调不保证被调用」经 **MDN** 原文核实（"is not required to call cleanup callbacks"、"not at all"、不可用于关键逻辑）；**Node.js 发布节奏变更经 nodejs.org 官方公告核实**（2026-03-10 发布：自 27.x 起改为每年一个大版本、4 月发布 10 月进 LTS、**每个版本都成 LTS**、Node 26 是旧模式最后一条线）；**TypeScript 7.0 经 typescriptlang.org + 微软官方博客核实**（2026-07-08 发布，Go 原生移植，官方实测 8–12x，vscode 125.7s→10.6s；**关键限制：7.0 不提供程序化 API**，typescript-eslint / webpack loaders / Vue / Svelte 等仍需 TS 6.0）；**Deno 现状经 deno.com 核实**（2.9.6，默认拒绝的安全模型、13 个内置工具）；**Bun 现状经 bun.sh 核实**（1.4.0，2026-08 发布，核心已从 Zig 重写为 Rust，JavaScriptCore，四合一）；**Vite 现状经 vite.dev 核实**（构建由 Rolldown 驱动，80k+ stars，8000 万+ 周下载）。⚠️ **关键发现并写入正文**：**Bun 官网与 Deno 官网在同一时点公布的吞吐基准互相矛盾**（Bun 官网 Express 吞吐 Bun 48,243 > Node 25,181 > Deno 19,243；Deno 官网 Realworld 吞吐 Deno 72,400 > Bun 68,200 > Node 44,000）——两边均为厂商自发布基准且都把自己排第一，已作为核心教学素材并给出「只采信对手之间一致的结论」的方法。⏳ **未实测项已显式标注**：DevTools Memory 面板具体操作（Comparison / Retainers 视图）、脱离 DOM 的引用残留（浏览器场景）；**webpack / Rollup / esbuild / SWC / Rspack / Turbopack 未逐一核查版本，正文不给版本号，避免过时数字** |
| 2026-09-03 | 课 11 事实核查 | 实测 + 权威源核查 | — | 全部示例代码经 Node v22.14.0 实跑（8 种内置错误类型、`stack` 属性描述符、`stackTraceLimit`/`prepareStackTrace`、`cause` 三层链、`AggregateError`、跨 realm `instanceof` 失效、`captureStackTrace` 五组对照、`throw` 五种原始值、`finally` 四条覆盖与执行顺序、`try` 抓不到异步（exit 1）、Promise 三种捕获、全局兜底（exit 1→0）、错误边界三层分工、`console` 三坑含性能 220/243ms 与时序 10→113→484ms、`debugger` no-op、`--inspect-brk` + CDP 端点、**手工构造 Source Map 并被 Node 真实消费**（`demo.min.js:1:40` → `demo.src.js:2:21`））。**权威源**：ES3（1999-12）引入 try/catch 经 **Wikipedia「ECMAScript version history」第 3 版原文**核实；`Error.cause` 属 **ES2022**（Wikipedia 第 13 版 + **MDN「Error: cause」**核实其属性为 writable/enumerable:no/configurable:yes）；**Source Map 起源经 ECMA-426 标准原文核实**（v1 Joseph Schorr / Closure Inspector；v2 John Lenz 2010；v3 John Lenz + Nick Fitzgerald，2011-02-11，基于 Pavel Podivilov 建议改用 Base64 VLQ，比 v2 减 50%；2023–2024 标准化为 ECMA-426，第 1 版 2024-12）；浏览器侧 `unhandledrejection` 经 **MDN** 核实（`PromiseRejectionEvent`、`reason`/`promise` 字段、cancelable、`preventDefault()`、跨域不触发）。⏳ **未实测项已显式标注**：浏览器 DevTools 的惰性求值（社区记载）、Babel/TS 降级导致 `instanceof` 失效（背景陈述） |
| 2026-09-03 | 课 10 事实核查 | 实测（临时模块实验室） | — | 建临时目录 `_tmp-l10-demo/` 实测 17 项 + 互操作 4 项 + 命名导出探测 4 项，全部跑通后删除：值拷贝（0→0）vs 实时绑定（0→2）、只读绑定 TypeError、模块单例、CJS 条件/变量路径 require 可行 vs ESM 静态 import 写在块里报 `SyntaxError: Unexpected token '{'`、循环依赖三种表现、CJS 循环依赖先后两种结果（`{a:'A'}` vs `{}`）、动态 import()（ESM 与 CJS 两侧）、顶层 await（ESM 可用 / CJS `SyntaxError`）、`.mjs`/`.cjs`/`type` 三条判定规则、ESM 命名导入 CJS 取决于写法（`exports.x=` 可 / `module.exports={}` 与先建对象再赋值不可 / 默认导入永远安全）、`import.meta` 与 `__dirname` 差异。**ES2015 引入 ES modules 经 Wikipedia「ECMAScript version history」核实**；⏳ CommonJS / AMD 的诞生年份与推动者未逐一检索一手来源，故只作背景陈述并标注置信度，不作精确断言 |

| 2026-09-04 | Phase 5 实战经验 / 排障速查手册 / 场景解法库 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | P0×1（已修）/ P1×3 | 三份一次生成：`08-实战经验.md`（10 条实测故障模式五段式 + 适用边界与反模式 + 落地 Checklist + Cloudflare 2019-07-02 事故复盘）、`09-排障速查手册.md`（12 条 QRH 式条目）、`10-场景解法库.md`（7 道开放设计题）。**P0×1 已修**：`09` 全篇锚点链接在中文标题下跨渲染器不一致，已全改纯编号引用（详见评审清单）。**P1×3 采纳**（速览表 / 补知识点回指 / unref 白话化）。**探针阶段抓到真 bug**：`AbortSignal.timeout()` 内部定时器是 unref 的，无其它句柄时 abort 不触发、进程静默退出 —— 已修正「加超时」建议的适用范围。**证据纪律**：全部故障模式经 Node v22.14.0 实测；`util.promisify` / `Error.cause` / `AbortSignal.timeout` / `--cpu-prof` 补实测，`import/no-cycle` 联网核实。**链接校验 66 条 missing 0**。两视角无冲突 |
| 2026-09-04 | **全部流程收官** | — | — | 4 阶段 / 12 课 / 36 知识点 + Phase 3 实战项目 + Phase 4 课程手册 + **Phase 5 三件套**全部完成，大纲全程无结构性调整，全部评审 P0=0。下一步可选：知识点对齐（Phase 6）/ 深入新方向（DOM / TypeScript / Node 后端 / 前端框架） |

**大纲评审 P1 明细（已全部在落盘产物中落实）**：

1. 【教学逻辑】课 7「事件循环」必然要举微任务例子，而 Promise 在课 8 才讲 → 处置：课 7 只借 `Promise.then` 的**外壳**说明"它是微任务"，机制留到课 8 展开；阶段 3 概览中显式标注此约定。
2. 【完整性】目标含"动手实操"但未含 DOM / 浏览器 API → 处置：在 `01-学习路径总览.md` 显式声明**课程边界**（聚焦语言核心，DOM/BOM、前端框架、Node 后端 API 不在范围内），避免学员误以为漏了。
3. 【困惑点】课 6「原型与类」是入门者最大劝退点，且与课 5（this）连着上，难度叠加 → 处置：课 6 采用**先用 `class` 写代码、再拆开看原型**的逆向讲法；阶段 2 概览标注"本课是全程最难，允许分两次学"。

## 大纲调整记录

| 日期 | 调整内容 | 原因 |
|------|----------|------|
| 2026-08-31 | 初版大纲落盘：4 阶段 / 12 课 / 36 知识点 | Phase 1 备课，大纲评审 P0=0 通过 |

> 大纲是**活文档**，随学习进度 / 目标变更 / 重点变更随时调整，每次调整在此追加一行。
> 结构性调整（增删阶段 / 课 / 知识点）须重新过大纲评审，并同步增删 `00-评审清单.md` 的占位条目。

---

**断点续学规则**：读本表进度找「当前阶段第一个非『已完成』的知识点」继续；状态流转为 `⬜ 未开始 → 🔄 进行中 → ✅ 已完成`。
续学前先看「大纲调整记录」与「评审记录」，并读 [`00-评审清单.md`](00-评审清单.md) 检查是否有**产物已存在但清单未勾选**的条目（= 漏评审），有则先补评审。
