# Redis 学习档案

## 学习者画像

| 项目 | 值 | 来源 |
|------|-----|------|
| 主题 | Redis 系统学习 | 用户指定 |
| 当前水平 | 零基础 | 用户指定（沿用 ES 课程画像） |
| 学习目标 | 理解概念 + 动手实操 + 应试认证 + 决策参考（四项全要） | 用户指定（沿用 ES 课程画像） |
| 输出格式 | Markdown（主载体，Mermaid + SVG 图表） | 默认值 |
| 模式 | 课程制（4 阶段 / 9 课） | 自动判定，用户已确认 |
| 实操环境 | WSL Ubuntu 24.04 + Redis 8 官方源 | 用户选定方案 A（升级 Redis 8） |
| 产物目录 | `redis/` | 沿用 learning 仓库既有课程命名规范 |

### 本机环境约束（备课实测，2026-08-31）

| 项 | 值 |
|----|-----|
| WSL | Ubuntu 24.04.4 LTS（noble），Windows 侧无 Docker、无 Redis |
| 系统自带 Redis | 7.0.15（apt 源最高即此版本，7.0 线已 EOL） |
| 官方源连通性 | `packages.redis.io` 与 `download.redis.io` 均 HTTP 200 |
| 编译工具链 | gcc 13.3.0、GNU Make 4.3 可用 |

> 命令执行方式：PowerShell 下用 `$env:WSLENV=""; & "C:\Windows\System32\bash.exe" -c "..."` 调用 WSL，Windows 盘挂载在 `/mnt/d` 下。

## 知识点级进度表

| 阶段 | 课 | 知识点 | 状态 | 完成日期 | 小测成绩 |
|------|-----|--------|------|----------|----------|
| 1 | 课 1 Redis 是什么 | Redis 的诞生动机 | ✅ 已完成 | 2026-08-31 | - |
| 1 | 课 1 Redis 是什么 | 内存数据结构服务器 | ✅ 已完成 | 2026-08-31 | - |
| 1 | 课 1 Redis 是什么 | Redis 不是什么 | ✅ 已完成 | 2026-08-31 | - |
| 1 | 课 2 跑起来第一个 Redis | 安装与启动 | ✅ 已完成 | 2026-08-31 | - |
| 1 | 课 2 跑起来第一个 Redis | 五种基础类型与通用命令 | ✅ 已完成 | 2026-08-31 | - |
| 1 | 课 2 跑起来第一个 Redis | key 设计与过期 | ✅ 已完成 | 2026-08-31 | - |
| 2 | 课 3 List 与 Hash | List 双向操作与阻塞弹出 | ✅ 已完成 | 2026-08-31 | - |
| 2 | 课 3 List 与 Hash | List 当消息队列的三个硬伤 | ✅ 已完成 | 2026-08-31 | - |
| 2 | 课 3 List 与 Hash | Hash 存对象 vs String 存 JSON | ✅ 已完成 | 2026-08-31 | - |
| 2 | 课 4 Set、ZSet 与特殊类型 | Set 交并差与去重 | ✅ 已完成 | 2026-09-01 | - |
| 2 | 课 4 Set、ZSet 与特殊类型 | ZSet 跳表 + 哈希表双结构 | ✅ 已完成 | 2026-09-01 | - |
| 2 | 课 4 Set、ZSet 与特殊类型 | Bitmap / HyperLogLog / Geo | ✅ 已完成 | 2026-09-01 | - |
| 2 | 补充课 Stream 与 Pub/Sub | Stream 基础读写（XADD/XRANGE/XREAD BLOCK） | ✅ 已完成 | 2026-09-17 | - |
| 2 | 补充课 Stream 与 Pub/Sub | 消费者组与 PEL / XACK / XCLAIM | ✅ 已完成 | 2026-09-17 | - |
| 2 | 补充课 Stream 与 Pub/Sub | Pub/Sub 与「List/Stream/Pub-Sub」三者选型 | ✅ 已完成 | 2026-09-17 | - |
| 3 | 课 5 RDB 与 AOF 持久化 | RDB fork 与写时复制 | ✅ 已完成 | 2026-09-01 | - |
| 3 | 课 5 RDB 与 AOF 持久化 | AOF 写后日志与刷盘策略 | ✅ 已完成 | 2026-09-01 | - |
| 3 | 课 5 RDB 与 AOF 持久化 | 持久化选型决策 | ✅ 已完成 | 2026-09-01 | - |
| 3 | 课 6 主从复制与哨兵 | 全量与增量复制 | ✅ 已完成 | 2026-09-01 | - |
| 3 | 课 6 主从复制与哨兵 | 哨兵故障转移 | ✅ 已完成 | 2026-09-01 | - |
| 3 | 课 6 主从复制与哨兵 | 哨兵解决不了的丢数据 | ✅ 已完成 | 2026-09-01 | - |
| 4 | 课 7 分片与集群 | 哈希槽与 CRC16 | ✅ 已完成 | 2026-09-01 | - |
| 4 | 课 7 分片与集群 | 集群伸缩与重定向 | ✅ 已完成 | 2026-09-01 | - |
| 4 | 课 7 分片与集群 | 集群下的多 key 与 Lua 限制 | ✅ 已完成 | 2026-09-01 | - |
| 4 | 课 8 缓存设计 | 穿透 / 击穿 / 雪崩 | ✅ 已完成 | 2026-09-01 | - |
| 4 | 课 8 缓存设计 | 缓存与数据库一致性 | ✅ 已完成 | 2026-09-01 | - |
| 4 | 课 8 缓存设计 | 内存淘汰与过期策略 | ✅ 已完成 | 2026-09-01 | - |
| 4 | 课 9 生产实践与选型 | 性能诊断 | ✅ 已完成 | 2026-09-01 | - |
| 4 | 课 9 生产实践与选型 | 安全与运维基线 | ✅ 已完成 | 2026-09-01 | - |
| 4 | 课 9 生产实践与选型 | 生态与选型 | ✅ 已完成 | 2026-09-01 | - |

## 评审记录

> course-reviewer 每次评审的结论在此追加一行。评审方式：`use_agent 委派`（子 agent 已创建）或 `主 agent 内联`（子 agent 未创建）。批次内容评审为双模式（教学法视角 + 学习者视角），冲突时主 agent 裁决并标注。

| 日期 | 评审对象 | 评审方式 | 意见摘要 | 处置 |
|------|----------|----------|----------|------|
| 2026-08-31 | 多阶段大纲 | 主 agent 内联（pedagogy，子 agent 未创建） | P0×0；P1×1（阶段 1 原计划 5 知识点偏满，且缺 key 设计） | 采纳 P1：阶段 1 调为 6 知识点，课 2 补「key 设计与过期」 |
| 2026-08-31 | 阶段1·课1《Redis 是什么》批次 | 主 agent 内联（pedagogy + learner） | pedagogy：P0×0 / P1×1（实操用 6399 端口与正文 6379 未说明差异）；learner：P0×0 / P1×1（讲"单线程"时未区分命令执行线程与后台线程，零基础易卡住） | 两条 P1 均采纳并修订：补充 6399 端口说明、补充单线程与后台线程边界；修订后复审 P0=0 |
| 2026-08-31 | 阶段1·课2《跑起来第一个 Redis》批次 | 主 agent 内联（pedagogy + learner） | pedagogy：P0×0 / P1×2（KEYS 缺第二危险"响应撑爆内存"且 rename-command 已 deprecated 未提 ACL；第四幕命令行启动与"生产用配置文件"表面冲突）；learner：P0×0 / P1×1（SCAN 未给完整游标循环，易误以为跑一次就遍历完） | 三条 P1 均采纳并修订：补 KEYS 第二危险 + ACL 替代方案、补 SCAN 循环到游标 0 及 `--scan` 用法、补第四幕一致性说明；同步更新 Q1 小测解析；修订后复审 P0=0 |
| 2026-08-31 | 阶段2·课3《List 与 Hash》批次 | 主 agent 内联（pedagogy + learner） | pedagogy：P0×0 / P1×1（含未核实的 Redis 7.4 Hash 字段级 TTL 说法）；learner：P0×0 / P1×1（"LINDEX 是 O(N)"与"实测仅 4.6ms"并列，零基础易困惑到底慢不慢） | 两条 P1 均采纳并修订：删除未核实的字段级 TTL 说法；把 LINDEX 拆成理论复杂度/实际常数/真实代价三层讲透，并同步常见误区第 4 条；修订后复审 P0=0 |
| 2026-09-01 | 阶段2·课4《Set、ZSet 与特殊类型》批次 | 主 agent 内联（pedagogy + learner） | pedagogy：P0×1（2.5 节 ZSet 500 万与 List 100 万规模不同却直接对比每元素开销，属事实错误）/ P1×1（1.1 节 SADD 返回值含自相矛盾的思考过程残留）；learner：P0×0 / P1×1（Bitmap 稀疏陷阱数值在正文 14.00 MB、决策树 12.5 MB、实验注释 12.5 MB 三处不一致） | 三条均采纳并修订：2.5 节改为同规模（100 万）对比并补充规模梯度观察；1.1 节改为清晰的返回值说明；Bitmap 数值全文统一为实测 14.00 MB（理论 11.9 MB 作补充）；同步修正 Q4/Q5 相关选项；修订后复审 P0=0 |
| 2026-09-01 | 阶段3·课5《RDB 与 AOF 持久化》批次 | 主 agent 内联（pedagogy + learner） | pedagogy：P0×2（1.1 节用空数据集的 7.50ms 支撑"SAVE 全程阻塞"论断，数字无意义且会误导；困境二"父子进程各持一份"与后文"只复制被修改的页"自相矛盾）/ P1×2（Q3 选项 A"实测零丢失"未标注 WSL 环境限制；实验 3 用相对路径 grep 未说明工作目录）；learner：P1×1（实验 1 用了不存在的 awk `replicate` 函数，照抄会报错） | 五条均采纳并修订：1.1 节改为定性说明阻塞范围并标注不要用空数据集测速；困境二改为准确描述并指引到第三幕实测；Q3 选项 A 与解析补充环境限制说明；实验 3 补充 cd 与 `config get dir`；实验 1 的手写代码改为可用的 Python 生成并指引直接跑脚本；修订后复审 P0=0 |
| 2026-09-01 | 阶段3·课6《主从复制与哨兵》批次 | 主 agent 内联（pedagogy + learner） | pedagogy：P0×2（困境二"5 个从库同时上线就是 5 次 fork"与 1.3 节"共享一次 fork"自相矛盾；2.6 节对比表格右侧单元格为空、格式破损）/ P1×2（1.5 节对 `REPLICAOF NO ONE` 失败原因解释不准确，实测 Redis 8 下主库 replid 未变，失效的是从库侧缓存状态；1.5 节引用"1.2 节 backlog 数据"章节号错误，实际在第二幕冲突二）；learner：P0×0 / P1×0 | 四条均采纳并修订：困境二区分"同时上线共享 fork"与"陆续上线各自 fork"并补充触发频率对比；2.6 节改为散文式取舍说明并标注文档默认值 30000；1.5 节改用实测日志 `Discarding previously cached master state.` 佐证并补充 replid 实测发现；1.5 节章节引用更正为第二幕冲突二；修订后复审 P0=0 |
| 2026-09-01 | 阶段4·课7《分片与集群》批次 | 主 agent 内联（pedagogy + learner） | pedagogy：P0×2（3.6 节规范表"避免脚本内硬编码 key、即使同槽也要声明"与 3.4 节"同槽即允许"自相矛盾；Q2 单选题 B/D 两个选项都正确，用"修正说明"打补丁等于把错题留正文）/ P1×2（2.6 节代码 `f[8:]` 与注释"第 9 个字段"不一致，且未处理单槽无连字符的情况；困境一"单机 Redis 内存上限 512 GB"未核实且与后文"买到 1 TB 机器"自相矛盾）；learner：P0×0 / P1×0 | 四条均采纳并修订：3.6 节改为"numkeys=0 导致脚本无路由依据"这一准确表述，并补充同槽允许的说明框；Q2 改为反向提问（问"不在同一槽"）使答案唯一，原知识点移入解析；2.6 节代码修正索引注释并补充单槽 `elif tok.isdigit()` 分支与迁移态标记跳过；困境一删除未核实的 512 GB 数字改为定性描述；修订后复审 P0=0 |

| 2026-09-01 | 阶段4·课8《缓存设计》批次 | 主 agent 内联（pedagogy + learner） | pedagogy：P0×1（3.5 节 samples 数据呈现 samples=1 存活 1827 却未解释，易被误读为"LRU 更准"）/ P1×1（3.3 节策略横评表未标注 volatile-* 组只给冷 key 设 TTL，表格会被误读成策略排名）；learner：P0×0 / P1×1（3.2 节定期删除实测"5 秒只回收 15 个"缺量化解释，零基础易误判为异常） | 三条均采纳并修订：3.5 节补淘汰数/写入数对照实验，证实 samples=1 是"淘汰太慢导致写入提前失败"而非精度更高，并补充"调太小会拖垮写入吞吐"的结论；3.3 节在表前加"读表前必看"、表后加"这张表不能直接用来排名"两处警示；3.2 节补充 0.2 个/次的抽样期望验算，说明 15 个与之同数量级；另主动修正 Q3 题干（原"秒杀库存选互斥锁"与冲突三"秒杀慎用互斥锁"自相矛盾，改为考察选型判据本身）；修订后复审 P0=0 |

| 2026-09-02 | 结课实战项目《电商大促数据层》（Phase 3） | 主 agent 内联（pedagogy + learner + A1-A6 复杂度门槛） | pedagogy：P0×1（知识点地图第 20 项「生态与选型」回指到决策 5，而决策 5 是 ACL 不是生态选型，回指错误）/ P1×1（目录说明称「5 个权衡点」实际有 6 个）；learner：P0×0 / P1×1（运行方式只给了 WSL 内写法，未给 Windows PowerShell 侧的调用方式，本机为 Windows 环境照抄无法执行）；门槛核查：跨 4 阶段 ≥3 ✅、非功能约束 4 项 ≥2 ✅、真有争议的独立决策 6 个 ≥2 ✅、多文件工程 ✅ | 三条均采纳并修订：第 20 项回指改为决策 6 并补写完整的 Redis 8 / Valkey / 云托管对比决策；目录说明改为 6 个；README 运行方式补充 PowerShell 下 `$env:WSLENV=""; & "C:\Windows\System32\bash.exe" -c "..."` 的写法并附三种常见报错的排查指引；修订后复审 P0=0 |
| 2026-09-02 | 实战经验 / 排障速查手册 / 场景解法库（Phase 5） | 主 agent 内联（pedagogy + learner + B1-B8 证据纪律 + C1-C7 场景解法库） | pedagogy：P0×2（`08` 量化判据把课 9 实测的「10 万条订单命中 9990（9.99%）」误写成「1 万条、有效命中率趋近于 0」，与原文直接冲突；`10` 场景 3 断言编码转换「内存跳变接近翻倍」无实测支撑）/ P1×1（`08` 案例引用的 `tsight.io` 链接实测 403 不可达）；learner：P0×1（端到端复现时 `rebuild.sh` 结尾固定 `sleep 3` 导致复制显示 down，学习者会误判环境搭建失败）/ P1×2（`09` 症状 7 只说「用管理员账号」却没给 default 已关闭时的具体路径；`08` 的「TTL 跨度 115 秒」无复现入口）；证据纪律 B1-B8：9 条故障模式均可溯源至本机实测或官方文档，无编造；C1-C7：6 个场景三要素齐全、details 折叠 12 组配对正确 | 五条均采纳并修订：量化判据改为「命中率 10% 却要传输 100% 数据」；本机补测编码转换实为 **128→129 成员时 1721→11821 字节、跳变 6.87 倍**，三份文档统一更正；移除 403 来源只留可达来源；`rebuild.sh` 第 8 步改为最多 20 秒轮询（实测需 6 秒，已验证输出「等待 6 秒后就绪」且 offset 对齐）；`09` 补 default 关闭后的两条执行路径；`08` 删除无复现路径的 115 秒改为引用主程序第二幕输出；另完成许可证核查回写（7.2.4 为最后 BSD 版、8.0 于 2025-05-01 加入 AGPLv3、8.0 于 2026-12-01 停止支持）；修订后复审 P0=0 |

| 2026-09-02 | 课程手册汇总 `final-课程手册.md`（Phase 4） | 主 agent 内联（pedagogy + learner + 汇总完整性核查） | **P0×2**：① 汇总过程中 `replace_in_file` 用「### 关键数字」做锚点，该标题在 9 课中重复出现，导致补入的持久化内容被**误插到全部 9 课**（文件从 848 行膨胀到 1432 行，课 1 讲"Redis 是什么"却出现"持久化选型决策树"）——已用按课归属判定的脚本精确删除 7 处误插区块并归位课 5/课 6，备份留存 /tmp，修复后逐课复查结构正确；② 手册与 README 均把「提速约 110 倍」写作确定值，但该项目提速倍数**每次运行都不同**（本机三次实测 110 / 129 / 167 倍，因为是"20ms 模拟延迟 ÷ 0.1ms 量级耗时"的比值，负载微波动即被放大），属证据纪律问题——已改为标注区间与波动原因，并指出稳定指标是"缓存侧单次约 0.12 ms"。**P1×2**：课 5 的持久化选型决策树、课 6 的防丢数据三手段对比、课 9 的选型决策树是各课唯一的结构化决策图示，初版遗漏，已全部补入（阶段目标"能配持久化策略""能给出决策条件"失去抓手）；课 4 补入 Phase 5 实测订正的编码转换 6.87 倍数据 | 四条均采纳并修订。另完成汇总完整性核查：9 课全纳入 ✅、4 阶段全纳入 ✅、实战项目含需求+知识点地图+决策摘要 ✅、学习目标四项全要故补「考点速记」（25 条高频考点 + 13 组易混点对照）与「决策清单」（7 条不该用信号 + 9 条选型条件 + 12 项上线配置基线）✅、31 个相对链接目标全部可达 ✅、16 个关键数字与课时原文一致 ✅。最终规模 944 行 / 56 KB |

| 2026-09-02 | **卫生检查与清理**（Phase 4 收尾） | 主 agent 执行（非评审对象，为环境合规核查） | **结论：`.gitignore` 合规，无规则被删**。表面 `git diff` 显示 83 行被删、似整文件重写，实为**换行符变更**（HEAD 为 LF、工作区为 CRLF，Git 逐行判改）；按「去注释/空行后的规则集合」比对，HEAD 的 42 条规则**零丢失**，工作区 47 条（净增 5 条：package-lock.json×2、animation-engine 的 mp4/gif/png 帧序列×3），符合「只追加不删规则」。8 类关键残留（`__pycache__/`、`*.rdb`、`*.aof`、`appendonlydir/`、`nodes-*.conf` 等）全覆盖；12 个教学产物经 `git check-ignore` 逐一验证**均未被误忽略** | 已执行清理：删除 8 个 `__pycache__` 字节码（`redis/playground/` 2 个 + `projects/电商大促数据层/实现/` 6 个）；停掉 7201/7202/7203 三个实验实例（7202/7203 用 `SHUTDOWN NOSAVE`；**7201 因 ACL 关闭 default 且 appuser 按最小权限未获 SHUTDOWN，改用 SIGTERM 优雅退出**，PID 由端口反查并经三重校验：进程存活 / cmdline 含 7201 / cmdline 不含 6379）；删除 `/tmp/manual-backup-*.md` 备份。**6379 上的两个进程一律保留**（识别为非孤儿：`/usr/bin/redis-server` 系统自带 23h、`/opt/redis-stack` 已运行 1 天 4h，且 celery-django 课程大量依赖 6379 上的 redis-stack 做 BF/search 实测）。复扫确认 redis 目录零残留、6379 ping=PONG 数据量 0 未受影响 |

> ⚠️ 独立性提示：`course-reviewer` 子 agent 尚未创建，本轮评审为**主 agent 内联**（独立性受限）。可创建独立 `course-reviewer` 子 agent 获得更独立的评审视角。

| 2026-09-17 | **结构对齐轮**（10 课 × topic-teach 规范） | 主 agent 内联（规范符合性核查，非内容重评审） | **范围**：按最新 topic-teach 规范对已完结 10 课做全量结构对齐（用户显式授权，规范默认"旧课不回溯"）。**已交付**：入口四要素（一句话本质 / 处境对照 / 一眼全局图 / 本课地图）10 课全覆盖；承接句 30 条；第三幕图配额 10 课 × 4 张；术语双轨（行话锚定 28 / 官方链接 29 / 四列对照表 8）；53 条官方 URL 全量 HEAD 验证 BAD=0（修正 2 条坏链）。**新增产物**：`应用实战/`（4 篇 + 8 张分步图）、`10-场景解法库/`（目录化 6 篇）。**审查抓出并已修**：① 实战 03/05 用裸 `except Exception: pass`（违反评审必查项 #28），改为显式列 `(TaskError, RedisError, ValueError)`；② `应用实战/INDEX.md` 与 4 篇头部回指课的路径多跳一级（`../../stages/` → `../stages/`），8 条死链已修；③ 场景解法库原单文件缺「关键代码」「非 Redis 替代路线」两条硬要求，拆分时补齐。**状态**：补充课 / 应用实战 / 场景解法库三项在 `00-评审清单.md` 已占位，**待下一轮独立评审**（P0 未清零前不得勾选） | 待评审：三项占位条目未勾选，链接校验与档案回写已完成 |
| 2026-09-17 | **独立复审**（结构对齐轮交付后自动触发） | 主 agent 独立核查（篇幅比例 / 要素齐备 / 孤儿文件 / 代码可照抄性） | **自评未抓出的缺陷 ×2，均已修复**：① 场景 04 的 Python 调用写 `LUA_SECKILL = """..."""` 占位符，与上方 Lua 段脱节，读者照抄会跑空——已把完整脚本内联进 Python 块，并补初始化与三种返回值示例；② 场景解法库 6 篇 + 应用实战 4 篇的示例代码直接使用 `r` / `db_query` 却从未定义，照抄必然 `NameError`——10 个文件统一补「代码约定」说明（违反既有讲义规范"读者照抄能跑通吗"）。另核验：4 篇实战篇幅 0.10~0.18（限 ≤ 对应课 1/3）、会用标志 4/4、分步图 8 张全部被引用（孤儿 0）、10 张全局图 XML 合法且零术语 | 两条均已修复；收口复跑链接 245/BAD=0、AST 8/BAD=0、场景六条硬要求 6/6 齐备，无回归。**本轮再次印证：自评不可信，独立复审必须保留** |

| 2026-09-17 | **场景解法库升级**（8 篇 + 7 张 SVG） | 主 agent 内联（topic-teach C1-C12 + pedagogy + learner；`course-reviewer` 尚未创建，独立性受限） | P0=0。8/8 场景保留“先想后看”；34 个解法逐一补齐思路、代码/命令、代价、边界、课程挂钩与来源（场景 07 含 6 个，其余每篇至少 4 个）；经典设计题/规模压力题各 4 篇；外链 37/37 HTTP 200、相对链接 0 断链、SVG XML 全部通过、固定凭据 0 | 已采纳：canonical 统一为 `场景解法库/`，旧入口仅跳转；明确补充 MySQL ↔ Redis 双写、Outbox、Binlog/CDC 与关键读绕过缓存；未重写 `08`/`09` |

| 2026-09-17 | **场景解法库优化轮**（8 篇 + 8 张全场景 SVG + 34 张逐解法机制图） | 主 agent 内联（topic-teach C1-C12 + pedagogy + learner；`course-reviewer` 尚未创建，独立性受限） | **P0=0**，自查发现的 6 处规范偏离已当轮修复：① **C3 折叠方向反了**——原「🔒 先自己想」被折进 `<details>`，而解法一览与展开解法却平铺，机制失效；改为「问题常显、答案折叠」（先想常显；提示与展开解法各折一层）。② **C9 缺按题定制的效果对比列**——原「解法一览」只有核心动作/适合/代价，没有效果维度；8 篇各定一个维度（01=DB 回源 QPS 与生效速度、02=内存削减与扩展上限、03=TopN 与个人排名实时性、04=不超卖保证与峰值吞吐、05=误操作爆炸半径与运营自助度、06=引入成本与退出难度、07=旧值窗口与可恢复性、08=丢失风险与重放能力）。③ **C10 替代路线原为散文一段**——改为「替代路线 / 适合什么情况 / 与 Redis 解法的关键差异 / 代价」四列表。④ **C6 互引无锚点**——8 篇各补「先看 / 崩了怎么办」两条，分别锚到 `08-实战经验.md` 的真实故障模式标题（1/3/5/6/9 与「什么时候不该用 Redis」）与 `09` 症状，8 条锚点经脚本校验全部命中真实标题。⑤ **C12 逐解法机制图为 0**——补到 34 张（每个解法一张，红色虚线框标失效点 + 一句读图指引），并补上场景 06 缺失的选型决策 SVG（`scene-06-selection-decision.svg`，原 8 篇只有 7 张图）；全场景图引用由链接式改为**图片语法**（原写法不渲染）。⑥ **自造数字**——删除 02 解法 A「砍掉 20%~50% 常驻内存」这一无证据支撑的区间，改为「按 `MEMORY USAGE` 实测确认」。另补：7 篇含 Python 片段的文件统一加「代码约定」说明（占位函数不自备照抄会 `NameError`；08 篇纯命令无需）。**核查**：details 配对 16/16、mermaid 34/34、8 张 SVG XML 全部可解析、相对链接 0 断链、外链 37 条可达（`dev.mysql.com` 两条 GET 403 属站点反爬，HEAD 200，页面存在）、Python 代码块 AST 全通过、无固定凭据 | 已落盘并回写 `00-评审清单.md`；未改动 `08` / `09` 正文 |

## 大纲调整记录

> 大纲是活文档，每次调整在此追加一行（与 `01-学习路径总览.md` / 阶段 `overview.md` 同步更新；结构性调整须重新过 course-reviewer 评审）。

| 日期 | 调整内容 | 原因 |
|------|----------|------|
| 2026-08-31 | 阶段 1 的课 2 由「五种基础类型与通用命令」1 个知识点拆为「安装与启动 / 五种基础类型与通用命令 / key 设计与过期」3 个知识点 | 大纲评审 P1：零基础需要环境搭建环节，且 key 设计与 `KEYS` 危险命令是高频生产事故点，单课 5 知识点偏满 |
| 2026-08-31 | 阶段 3 标题定为「持久化与高可用」，阶段 4 定为「分布式与生产实践」 | 使故事主线四阶段各对应一个清晰冲突：慢 → 结构 → 失忆 → 不够用 |
| 2026-09-17 | 阶段 2 新增《Stream 与 Pub/Sub》补充课（3 知识点），全课由 9 课变 10 课 | 原大纲缺 Stream 与 Pub/Sub，属生产高频能力；补课后「List / Stream / Pub-Sub」三者选型闭环 |
| 2026-09-17 | 新增 `应用实战/`（INDEX + 03/04/05/08 四篇）与 `10-场景解法库/`（目录化，INDEX + 6 篇）两个产物目录 | 按最新 topic-teach 规范补齐「拿去用」与「场景解法库目录形态」两条；场景解法库由单文件拆为目录并补齐「关键代码」「非 Redis 替代路线」 |
| 2026-09-17 | **场景解法库升级收口**：canonical `场景解法库/`（INDEX + 8 篇 + 7 张 SVG） | 保留 6 篇既有题并新增缓存一致性、可靠消息；按 C1-C12 补齐逐解法展开、课程挂钩、来源、边界、替代路线与结构图；旧 `10-场景解法库.md` 仅作兼容入口；内联 pedagogy + learner 复审 P0=0（`course-reviewer` 尚未创建，独立性受限） |
