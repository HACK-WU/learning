# RabbitMQ 系统学习 · 学习档案

> 断点续学唯一依据。续学前先看本文件的「知识点级进度表」「大纲调整记录」「评审记录」，并配合 `00-评审清单.md` 检查是否有漏评审的批次。

## 学习者画像

| 项目 | 值 | 来源 |
|------|-----|------|
| 主题 | RabbitMQ 系统学习（从动机到生产落地） | 用户指定 |
| 当前水平 | 零基础（未系统接触过消息队列） | 用户指定 |
| 学习目标 | 综合：理解概念（主）+ 动手实操 + 决策参考 | 用户指定「全都要」 |
| 实操语言 | Python + pika，命令行用 rabbitmqctl / rabbitmqadmin / rabbitmq-diagnostics | 用户指定 |
| 输出格式 | Markdown（主载体，Mermaid + SVG 图表） | 默认值 |
| 模式 | 课程制（主题为完整体系，4 阶段 12 课） | 自动判定，用户已确认 |

> 目标说明：以「建立完整心智模型」为主干，每课带可运行实操，最后给出「该不该用 RabbitMQ」的决策清单。

## 知识点级进度表

| 阶段 | 课 | 知识点 | 状态 | 完成日期 | 小测成绩 |
|------|-----|--------|------|----------|----------|
| 1 | 课 1 | 同步调用的四个困境 | ✅ 已完成 | 2026-08-31 | 3/3 |
| 1 | 课 1 | 消息队列的本质 | ✅ 已完成 | 2026-08-31 | 3/3 |
| 1 | 课 1 | 引入 MQ 的代价 | ✅ 已完成 | 2026-08-31 | 3/3 |
| 1 | 课 2 | 起源与 AMQP 协议 | ✅ 已完成 | 2026-08-31 | - |
| 1 | 课 2 | 核心角色与 AMQP 模型 | ✅ 已完成 | 2026-08-31 | - |
| 1 | 课 2 | 定位与直觉对比 | ✅ 已完成 | 2026-08-31 | - |
| 2 | 课 3 | Docker 起服务与端口 | ✅ 已完成 | 2026-08-31 | - |
| 2 | 课 3 | Management UI 巡礼 | ✅ 已完成 | 2026-08-31 | - |
| 2 | 课 3 | Python 发第一条消息 | ✅ 已完成 | 2026-08-31 | - |
| 2 | 课 3 | CLI 常用命令 | ✅ 已完成 | 2026-08-31 | - |
| 2 | 课 4 | 交换机为什么存在 | ✅ 已完成 | 2026-08-31 | - |
| 2 | 课 4 | fanout 与 direct | ✅ 已完成 | 2026-08-31 | - |
| 2 | 课 4 | topic 与 headers、默认交换机 | ✅ 已完成 | 2026-08-31 | - |
| 2 | 课 5 | 队列的属性 | ✅ 已完成 | 2026-08-31 | - |
| 2 | 课 5 | 消息的属性 | ✅ 已完成 | 2026-08-31 | - |
| 2 | 课 5 | 队列类型三分 | ✅ 已完成 | 2026-08-31 | - |
| 3 | 课 6 | 消费者确认 | ✅ 已完成 | 2026-08-31 | - |
| 3 | 课 6 | 发布者确认 | ✅ 已完成 | 2026-08-31 | - |
| 3 | 课 6 | 预取与公平分发 | ✅ 已完成 | 2026-08-31 | - |
| 3 | 课 7 | 三层持久化 | ✅ 已完成 | 2026-08-31 | - |
| 3 | 课 7 | 持久化的真实程度 | ✅ 已完成 | 2026-08-31 | - |
| 3 | 课 7 | TTL 与死信队列 | ✅ 已完成 | 2026-08-31 | - |
| 3 | 课 8 | 三种交付语义 | ✅ 已完成 | 2026-08-31 | - |
| 3 | 课 8 | 重复来源与幂等设计 | ✅ 已完成 | 2026-08-31 | - |
| 3 | 课 8 | 顺序性 | ✅ 已完成 | 2026-08-31 | - |
| 4 | 课 9 | 连接与信道管理 | ⬜ 未开始 | - | - |
| 4 | 课 9 | 生产者模板 | ⬜ 未开始 | - | - |
| 4 | 课 9 | 消费者模板 | ⬜ 未开始 | - | - |
| 4 | 课 10 | 延迟消息与延迟重试 | ⬜ 未开始 | - | - |
| 4 | 课 10 | 优先级队列 | ⬜ 未开始 | - | - |
| 4 | 课 10 | 流控与资源水位 | ⬜ 未开始 | - | - |
| 4 | 课 11 | 集群基础 | ⬜ 未开始 | - | - |
| 4 | 课 11 | 复制型队列 | ⬜ 未开始 | - | - |
| 4 | 课 11 | 故障与网络分区 | ⬜ 未开始 | - | - |
| 4 | 课 12 | 典型消息模式 | ⬜ 未开始 | - | - |
| 4 | 课 12 | 选型对比 | ⬜ 未开始 | - | - |
| 4 | 课 12 | 生产落地清单 | ⬜ 未开始 | - | - |

## 评审记录

> 评审方式：course-reviewer 子 agent 尚未创建，本轮走「主 agent 内联（双视角）」——教学法视角与学习者视角各一轮，独立性受限。建议后续按 create-sub-agent 规范创建独立 course-reviewer 子 agent，可获得更独立的评审视角。

| 日期 | 评审对象 | 评审方式 | 意见摘要 | 处置 |
|------|----------|----------|----------|------|
| 2026-08-31 | 阶段 3·课 8《交付语义与幂等》 | 主 agent 内联（pedagogy + learner） | P0×1 已修、P1×2 已修：**P0（自评复检发现，跨文件一致性）** 知识点 3「保序方案」引用的 prefetch 吞吐数据（p=1 → 3152、p=50 → 13339 条/秒、提升 4.2 倍）来自 `l8-ordering.py`，该脚本多线程共享计数锁造成**测量污染**；交叉验证脚本 `l8-order-crosscheck.py` 改用「各线程独立计数 + barrier 统一起跑」干净重测得 p=1 → 3716、p=10 → 20363、p=50 → 31721、p=100 → 26501 条/秒（提升约 **8.5 倍**）。已修正讲义正文、速查卡、小测 Q5 答案与 SVG **共 4 处**，并在正文加「数据说明」块交代弃用原因与判断依据，新增守护项 **F7** 防止污染数据回流（实测 7/7 通过）。pedagogy：P1×1（多消费者吞吐测出反常识的「4 并发慢 13 倍」，须交代测量局限、明确**不给具体倍数**、改为引用课 6 单变量曲线）+ P1×1（丢失放大效应是本节最有价值的发现，须给出机制解释与「积压越多损失越大」推论）；learner：P1×1（requeue 破坏顺序取决于队列是否有积压，单消费者**并非**绝对保序，须做「无积压插回原位 vs 有积压挤到队尾」两场景对照）。新建 `l8-guard-facts.py` 守护 7 条硬事实（F1–F7），全部通过 | 全部采纳：修正 P0 数据污染 4 处并加 F7 守护；反常识结论如实交代并给出排查方法论；三种保序方案按「严格保序 / 折中 / 分队列推荐」三档给出。**阶段 3 收官** |
| 2026-08-31 | 阶段 3·课 7《持久化与死信》 | 主 agent 内联（pedagogy + learner） | P0×1 / P1×3，全部已修：**P0（learner 视角发现，自评复检确认）** 初稿第二幕写「三层全开后重启，消息没了」、第一幕写「队列和交换机都不见了」，与**本课自己的实测直接矛盾**——实测 A 组三层全开消息存活（1→1），且 4.3 上队列 `durable=False` 根本建不了（课 3/课 5 的 541）、transient 交换机实测存活。属编造现象且造成学习路径自相矛盾。已重写两幕：第一幕改为「容器完好、只有内容消失」；第二幕改为两个**真实发生**的冲突（① 只设队列 durable、漏 delivery_mode → 队列在消息没了，实测 D 组 1→0；② 交换机 durable=False 重启后仍在 → 引出 Khepri 版本差异）。同步修正第四幕回扣表述与剧透项。pedagogy：P1×1（知识点 2 连做五版实验均测出零丢失，须交代「零丢失真实但测量覆盖了窗口」，保留失败过程作为方法教学）+ P1×1（transient 交换机存活与官方冲突，须标注置信度与版本差异、给出「仍建议一律 durable=True」的可执行建议）；learner：P1×1（须建守护脚本覆盖本课三条硬事实）。另新建 `l7-guard-facts.py` 守护三条事实（transient 交换机存活 / delivery_mode 计数 / TTL 惰性过期），验证脚本 4 项断言全通过 | 全部采纳：修正 P0 叙事矛盾（第一幕/第二幕/第四幕三处）；实验失败过程（v1–v5）完整保留入讲义；transient 交换机结论加置信度标注与版本差异说明；新建守护脚本并纳入验证清单 |
| 2026-08-31 | 阶段 3·课 6《确认机制与预取》 | 主 agent 内联（pedagogy + learner） | P0×3 / P1×2 + P2×1，全部已修：**P0-1（learner 视角发现）** 第四幕消费者骨架用 `x-death` 头的 count 给 `nack(requeue=True)` 重试计数——实测证明 requeue=True **根本不写 x-death**（连续三次 headers 均为 `{}`），计数恒为 0，恰好造出讲义自己警告的无限重试死循环。已实测复现并改为自维护 `x-retry-count` + 重发，加 `l6-xdeath-check.py` 守护，同时补进误区表第 11 条与速查卡。**P0-2（实测发现）** prefetch 对 `auto_ack=True` **完全无效**（设 prefetch_count=1 发 10 条仍被一次推空），因 auto_ack 下不存在未确认态；此为课 4 伏笔的正式兑现，已作为知识点 1/3 的核心纠正与误区第 1 条。**P0-3（实测发现，与官方说法冲突）** pika confirm 实测比事务批量更慢，经 RTT 基准三角验证（每条耗时÷RTT≈1.0）+ pika 源码确认（`_flush_output(..., is_ready)` 逐条同步等待），定性为**客户端适配器实现特征**而非协议性质，并在讲义中显式区分二者、标注异步验证未跑通的置信度，避免学员误学成"事务更好"。pedagogy：P1×1（confirm 性能反直觉结论须给证据链而非结论）+ P2×1（SVG 应整合三知识点实测数据）；learner：P1×1（不公平分发初稿写死"慢消费者独占"，实测两轮结果相反，应表述为"谁先连上谁独占"并同步修正 Q4 答案与验证脚本断言） | 全部采纳：修正 P0×3 并加守护脚本；讲义与 SVG 同步；验证脚本扩为 18 项（新增 x-death 2 项），18/18 通过 |
| 2026-08-31 | 阶段 2·课 5《队列与消息的属性》 | 主 agent 内联（pedagogy + learner） | P0×1 / P1×2 + P2×1：**P0（learner 视角发现）** 初稿第一幕/第二幕写「裸 `queue_declare(queue='order_queue')` 能跑通、只是重启后消息丢」，但**课 3 已教过这行代码在 4.3 上直接报 541 并断开连接**——初稿会把课 3 踩过的坑当成能跑的示例代码教给学员，属事实性错误且造成学习路径自相矛盾。已实测复现（541 + 连接关闭）并重写两幕：改为以「课 3 已踩 541 → 加了 durable → 但消息仍丢」推进，新冲突聚焦「容器保住了、内容怎么办」，并加 `l5-check-541.py` 守护该事实。pedagogy：P1×1（内存数据不严谨——初稿用首次观测的 34,620~68,548 B 与「2.5~5 倍」，干净重测后应为 39,492→47,412 B、「2.8~3.4 倍」）+ P2×1（SVG 引用路径误写 `./assets/`，lesson 在 lessons/ 子目录应为 `../assets/`，2 处已修）；learner 另提 P1×1（下一课链接目录名写错，实为 `3-可靠性与投递语义`，已修） | 全部采纳：修正 P0 叙事错误并加守护脚本；内存数据干净重测后同步更新讲义 5 处 + SVG 3 处；修正 SVG 路径与课 6 链接；另修正 stream 内存描述（空队列 34,704 B 反而不占优，优势只在海量消息时）。**阶段 2 收官** |
| 2026-08-31 | 阶段 2·课 4《交换机与路由》 | 主 agent 内联（pedagogy + learner） | P0×1 / P1×2 + P2×1：**P0（复检发现）** 清理环境时实测撞出 `rabbitmqctl delete_exchange` **命令根本不存在**（报 `Command 'delete_exchange' not found. Did you mean 'delete_queue'?`），而讲义初稿在速查卡、踩坑清单、误区表 3 处均写入了它——属于会被学习者直接复制执行的错误命令，已全文更正为 HTTP API 与 `rabbitmqadmin delete exchange --name`（须用 `--name` 选项）两种实测可用写法。pedagogy：P1×1（知识点 1 缺「常见误区」「一句话记住」两项必备要素，已补齐）+ P2×1（prefetch 默认值缺实测证据，已补 `rabbitmqctl environment` 输出）；learner：P1×1（轮询分发隐藏"消费者先就绪"前提，已改为"先翻车→找根因→修正"叙事） | 全部采纳：修正 P0 命令错误并同步 3 处；补齐知识点 1 两项要素；轮询分发改为"先翻车（0/6）→ 找根因（prefetch 无上限）→ 修正（3/3）"叙事并附实测输出；补 `#` 匹配零个词、`#` 等价 fanout、AE 优先于 mandatory 三项实测发现；新增 Q5（AE×mandatory 优先级）、Q6（pika 退回时机） |
| 2026-08-31 | 阶段 2·课 3《起 RabbitMQ 与发第一条消息》 | 主 agent 内联（pedagogy + learner） | P0×0 / P1×2 + P2×1：pedagogy 指出「guest 仅限本机」与实测矛盾（Docker 镜像 `loopback_users.guest = false`），属安全类错误必须修正；learner 指出本课一次性抛 4 个坑，零基础读者在知识点 3 前无消化锚点，且缺 durable 与消息持久化的边界预告；pedagogy 另指出 `check_local_alarms` 已废弃仍被推荐 | 全部采纳：重写 guest 小节为三组对照实验并加安全提醒、知识点 1 末尾加"本课四个坑预告"导航、速查卡与误区表同步；`check_local_alarms` 加废弃说明与替代命令 |
| 2026-08-31 | 阶段 1·课 2《RabbitMQ 是什么与起源定位》 | 主 agent 内联（pedagogy + learner） | P0×0 / P1×2 + P2×1：pedagogy 指出 mermaid 里 AMQP 0-9-1「2008 定稿」、1.0「2011 OASIS」两处年份**未联网核实**（起源类事实是硬约束）；learner 指出「Channel 不是线程安全」只给结论未给机制，零基础读者记不住 | 全部采纳：删除两处未核实的年份断言；补充「AMQP 帧按信道号交织、响应对不上号」的机制解释；知识点 1 补官方文档小节 |
| 2026-08-31 | 阶段 1·课 1《为什么需要消息队列》 | 主 agent 内联（pedagogy + learner） | P0×0 / P1×2 + P2×1：learner 指出 broker 术语未通俗化（零基础会卡）、第一幕起源史缺置信度标注；pedagogy 指出知识点级官方文档链接缺失 | 全部采纳：补 broker 术语白话解释、起源句加置信度标注、知识点 2 补官方文档小节，另加课末速查卡 |
| 2026-08-31 | 多阶段大纲（4 阶段 12 课） | 主 agent 内联（pedagogy） | P0×0 / P1×3：① 零基础 + 动手实操目标下，Python 首条消息要到第 9 课太晚 ② 队列类型（classic/quorum/stream）在阶段 4 才讲，前置缺口 ③ 课 11 若按老资料讲「镜像队列 + 分区策略」会教过时知识（4.0 移除镜像、4.3 移除 Mnesia 分区策略） | 全部采纳：课 3 增「Python 发第一条消息」知识点；课 5 增「队列类型三分」知识点；课 11 知识点按 4.x 事实重写 |

## 大纲调整记录

| 日期 | 调整内容 | 原因 |
|------|----------|------|
| 2026-08-31 | 阶段 3 收官：课 8《交付语义与幂等》三个知识点全部完成并落盘（约 1110 行 + 1 张 SVG + 7 项事实守护脚本 + 6 道小测）；讲义与阶段概览、课程目录、评审清单已同步 | 正常推进；下一课为阶段 4 课 9《Python 客户端工程实践》 |
| 2026-08-31 | 课 7《持久化与死信》三个知识点全部完成并落盘（约 900 行 + 1 张 SVG + 4 项断言验证脚本 + 3 条事实守护脚本）；讲义与阶段概览、课程目录、评审清单已同步 | 正常推进；下一课为阶段 3 课 8《交付语义与幂等》 |
| 2026-08-31 | 初始化大纲：4 阶段 / 12 课 / 37 个知识点 | 首次备课 |
| 2026-08-31 | 阶段 2 收官：课 5《队列与消息的属性》三个知识点全部完成并落盘（874 行 + 1 张 SVG + 30 项实测验证脚本）；讲义与阶段概览、课程目录、评审清单已同步 | 正常推进；下一阶段为阶段 3《可靠性与投递语义》课 6 |

## 事实核查记录

> 起源、版本号等时效敏感内容，均在下述时点联网核查过；正文对应位置标注 `（核查于 2026-08）`。

| 日期 | 核查内容 | 结论 | 来源 |
|------|----------|------|------|
| 2026-08-31 | **auto_ack 崩溃的丢失放大效应（本课最重要发现）** | 投递 100 条、业务回调**只执行 1 条**就崩溃 → 队列剩余 **0 条**（丢失 100 条）。丢失条数远超已处理条数，多出的 99 条死在 pika 接收缓冲区里，业务代码根本没见过。**机制**：auto_ack=True 且 prefetch 无上限时 broker 一次性推空队列，推送瞬间即确认删除；pika 缓存在接收缓冲区、回调逐条慢速执行。**推论：队列积压越多，一次崩溃损失越大。**对照：手动 ack + prefetch=1 → 丢失 0/100 条 | 实测（l8-atmost-magnitude.py、l8-guard-facts.py F1） |
| 2026-08-31 | **prefetch 对 auto_ack 无效（课 6 结论复核）** | 加 `prefetch_count=1` 后，同样「回调执行 1 条崩溃」依然丢失 100/100 条，与不加完全一致。原因：prefetch 约束「未确认消息数」，auto_ack 下投递即确认，永不存在未确认态，限制无处施加 | 实测（l8-atmost-magnitude.py、l8-guard-facts.py F2） |
| 2026-08-31 | **★生产侧重复：broker 不打任何标记（最危险）** | 连续发布两条同业务单号消息 → broker 视为两条独立消息，消费时 `redelivered` 序列为 **[False, False]**。对照：消费侧重复（崩溃重投 / nack requeue=True）序列为 [False, True, True]。**结论：`redelivered` 只能识别消费侧重复，生产侧重复必须靠业务单号（幂等键）识别** | 实测（l8-idempotent.py、l8-guard-facts.py F4） |
| 2026-08-31 | **幂等方案实测效果** | 发布 3 条同单号消息：不做幂等 → 业务执行 3 次（重复扣款 2 次）；幂等键 + 去重表（内存 set 模拟）→ 业务执行 1 次，挡住 2 次重复 | 实测（l8-idempotent.py） |
| 2026-08-31 | **requeue 破坏顺序的条件（单消费者并非绝对保序）** | 关键变量是**队列是否有积压**：场景 A（prefetch=1，边消费边重投，无积压）→ msg-03 被 requeue 后立即又被同一消费者拿到，**插回原位、顺序侥幸保持**；场景 B（prefetch=8，客户端已有积压）→ msg-04~08 早已推入缓冲区，msg-03 重投只能排到后面，**被挤到第 8 位、顺序破坏**。**推论：prefetch 越大 / 积压越多，失败重投对顺序破坏越严重** | 实测（l8-order-probe.py、l8-guard-facts.py F5） |
| 2026-08-31 | **单消费者 prefetch 吞吐曲线（P0 数据污染修正）** | ⚠️ `l8-ordering.py` 因**多线程共享计数锁**造成测量污染，测出 p=1 → 3152、p=50 → 13339 条/秒（偏低）。`l8-order-crosscheck.py` 改用**各线程独立计数 + barrier 统一起跑**干净重测（600 条）：p=1 → **3716.4**、p=10 → **20363.2**、p=50 → **31721.4**（峰值）、p=100 → **26501.2** 条/秒，提升约 **8.5 倍**（非污染值的 4.2 倍）。判断依据：干净值与课 6 prefetch 曲线（20 → 37938）量级一致。已修正讲义 4 处并加守护项 F7 | 实测（l8-order-crosscheck.py、l8-guard-facts.py F7） |
| 2026-08-31 | **多消费者吞吐：反常识结论的测量局限（方法论）** | pika 多线程实测「4 消费者 1986 条/秒 < 1 消费者 27217 条/秒」（慢 13 倍），与常识相反。排查：改用**多进程**绕开 GIL 重测 → 1/2/4 进程吞吐几乎相同（5629 / 5683 / 5651），但**各进程消息数均分**（157/161/158/124）证明并发分发确实生效；真因是 600 条消息仅耗时 0.11 秒，**总耗时被进程启动 + 连接建立的固定开销主导**，未跑到吞吐瓶颈。**结论：本课不给出「多消费者快 N 倍」的具体倍数**，只给出保序 vs 并发的定性取舍。**方法论：测出反常识结果时先怀疑测量工具，换独立方法交叉验证** | 实测（l8-order-probe.py、l8-order-crosscheck.py） |
| 2026-08-31 | **验证持久化必须用真实强制宕机（方法学 P0）** | 两次方法错误实录：① `docker restart` = 优雅关机（SIGTERM），broker 退出前把元数据全部落盘，**连 durable=False 的交换机都活过重启** → 三组配置全存活，得出与生产相反的结论；② 容器内 `pkill -9 -f beam.smp` 无效——**broker 在容器内是 PID 1，Linux 不允许 kill -9 杀 PID 1**，进程从未中断（uptime 连续 94 秒）→ 三组「全存活」是假象。**正确做法**：`docker kill -s KILL <ct>` + `docker start`。**硬校验指标**：容器 StartedAt 变化 + uptime 归零重算（实测 125s → 12s）| 实测（l7-run-persist3.sh、l7-probe-kill.sh） |
| 2026-08-31 | **4.3 上 transient（durable=False）交换机活过真实宕机** | 与官方文档「transient exchanges do not survive restart」冲突。最小化干净复现（只声明交换机，无队列/绑定/消息）：`l7.mini.transient(durable=False)` 在 `docker kill -s KILL` 后**依然存在**，HTTP API 复核 durable=False 属性明确、绑定完好、可正常路由。**根因**：`rabbitmqctl status` 显示 4.3 使用 **Khepri** 作为元数据存储（Raft 复制日志），所有元数据变更都进 Raft 日志并落盘，不再区分 durable 与否。官方 Exchanges 文档伏笔句：「Further in the 4.x series, support for transient (non-durable) entities will be removed when Khepri becomes the only supported metadata store」。⚠️ 置信度：4.3.5 实测 + 官方伏笔句共同支撑，**不同版本行为可能不同**（3.x 上 transient 确会消失）。教学建议不变：**一律设 durable=True** | 实测（l7-probe-khepri.sh、l7-probe-ex-survive.sh）+ 官方 Exchanges 文档 |
| 2026-08-31 | **三层持久化缺层后果（真实宕机对照）** | A 组三层全开 → 交换机在 / 队列在 / 消息 1 条（**存活**）；D 组交换机+队列持久化但消息 `delivery_mode=1` → 交换机在 / 队列在 / **消息 0 条（丢失）**。证明「队列持久化 ≠ 消息持久化」。补充精确计数：`delivery_mode=2` → messages=1 / messages_persistent=1；`delivery_mode=1` → messages=1 / messages_persistent=**0** | 实测（l7-persist.py、l7-verify-persist.py） |
| 2026-08-31 | **classic vs quorum 的 fsync 差异（官方定性 + 本机实测）** | 官方定性（How messages are stored / confirms.md）：**classic 队列在发送 publisher confirm 前不执行 fsync**，「even durable messages that a publisher received a confirmation for, can technically be lost if the server crashes」；**quorum 队列确认前已 fsync 到多数派**，「if the publisher received a confirmation, this means the message had already been written to disk and fsync-ed on the quorum of nodes」。本机实测：① 落盘延迟——发布 3000 条持久化消息，`messages_persistent` 在 **100ms 内追平** messages（3000/3000，未落盘=0），印证官方「内存停留毫秒级、绝对不超过 200ms」；② 吞吐代价——500 条 classic 0.245s / quorum 0.903s，**quorum 约为 classic 的 3.7 倍耗时** | 官方文档 + 实测（l7-verify-persist.py、l7-persist-lag.py） |
| 2026-08-31 | **fsync 丢失窗口：测不出 ≠ 不存在（五版实验失败实录）** | 连做五版均测出零丢失，每版失败原因不同：v1 发布完才 kill（窗口已过）；v2 逐条 confirm 节奏太慢（给足落盘时间，ack 10541 / 存活 10541）；v3 客户端记账滞后 → **出现负数 -88**；v4 队列统计异步滞后 → **负数 -159**；v5 先 SIGSTOP 暂停发布再读权威值 → 零丢失（12298/12298）。**最终结论：零丢失是真实的，但不能证明「持久化绝对安全」**——为使测量准确而等待统计追平的 3 秒，恰好覆盖了 200ms 的落盘窗口，**测量行为本身消除了被测现象**。教学上须如此表述：官方明确定性 classic 存在 fsync 窗口，本机因落盘太快未能稳定复现，「没测出来」不等于「不存在」；需强保证则选 quorum（协议层面保证）| 实测（l7-fsync-window*.sh、l7-fsync-window5.sh） |
| 2026-08-31 | **TTL 是惰性过期（四组对照实测）** | 只有消息**到达队首**时才被真正移除。① 单条 1s TTL（自身即队首）→ 1.8s 后深度 0（正常消失）；② A(10s) 在前 B(1s) 在后 → 3.3s 后深度**仍为 2**（B 已过期但被 A 阻塞）；③ 用 `basic.get` 取走队首 A → B 一到队首**立即消失**（深度 0）；④ B(1s) 在前 A(10s) 在后 → 1.8s 后深度 1（正常）。**推论**：同一队列混用不同 TTL 会让短 TTL 完全失效，应按延迟时长分队列 | 实测（l7-ttl-lazy.py） |
| 2026-08-31 | **两种 TTL 取较小值 + 三种死信来源** | TTL：队列级 `x-message-ttl` 与消息级 `expiration` 同时存在时**取较小值**——队列 3s + 消息 10s → 按 3s 过期（实测 4.5s 后深度 0）。死信三来源实测（死信队列累计）：`rejected`（nack requeue=False）→ +1；`expired`（TTL 到期）→ +1；`maxlen`（`x-max-length=2` 发 4 条）→ 源队列保留 2 条、**2 条进死信**。另：`expiration` 必须是**字符串**，写整数会被拒绝 | 实测（l7-ttl-dlx.py） |
| 2026-08-31 | **x-death 头结构与延迟重试闭环** | 死信化时写入 `x-death`（数组，每经历一次死信追加一条），字段含 queue / reason / count / time / exchange / routing-keys。实测延迟重试链路（业务队列 → DLX → 等待队列 TTL=3s → 回默认交换机 → 重回业务队列）**完整闭环**：t=0.3s 进入等待队列 → t=4.3s 重新出现在业务队列 → 再次消费成功并 ack，此时 `x-death` 含两条记录 `[{expired, l7.retry.wait}, {rejected, l7.work}]`（完整「病历」）。**与课 6 呼应**：`requeue=True` 不写 x-death（课 6 已证），故不能用它给 requeue 重试计数 | 实测（l7-ttl-dlx.py） |
| 2026-08-31 | **队列统计的取数方式（踩坑）** | Management API 在本环境取不到 `messages_persistent`：① 单队列接口 `/api/queues/{vhost}/{name}` 默认返回**不含** messages/messages_persistent（实测只有 name/durable/type/state/arguments）；② 加 `?columns=messages,messages_persistent` **单队列接口不支持**，返回 0；③ 列表接口 `/api/queues/{vhost}?columns=...` 能返回 messages 但 persistent 缺失。**可靠来源**：`rabbitmqctl list_queues name messages messages_persistent`（实测输出 `l7.col.probe\t2\t1`）| 实测（l7-probe-fields.sh、l7-probe-columns.sh） |
| 2026-08-31 | **x-death 头的真实触发条件（P0 实测）** | **`nack(requeue=True)` 完全不写 `x-death`**——连续三次 requeue=True，headers 始终是 `{}`、`x-death=None`。`x-death` 只在消息被**死信化**时才产生（requeue=False / TTL 过期 / 超 max-length）；实测 requeue=False 后死信队列才出现 `x-death=[{'count':1,'reason':'rejected',...}]`。**讲义第四幕初稿用 x-death 给 requeue=True 重试计数是错的**（计数恒为 0 → 无限重试死循环），已改为自维护 `x-retry-count` 并重发，加 `l6-xdeath-check.py` 守护 | 实测（playground/l6-xdeath-check.py） |
| 2026-08-31 | **prefetch 对 auto_ack 无效（P0 实测）** | 设 `prefetch_count=1` + `auto_ack=True` 发 10 条 → **一次全推走、ready 归零**。原因：prefetch 约束"未确认消息数"，而 auto_ack 下消息投递即确认，永不存在未确认态，限制无处施加。对照：auto_ack=False + prefetch=1 → ready=9/unacked=1；不设 prefetch → 10 条全部进入 unacked。**本课最重要的一条纠正** | 实测（playground/l6-qos-probe.py） |
| 2026-08-31 | **pika confirm 是"假异步"（P0 实测）** | 官方称 confirm 异步、事务降吞吐约 250 倍，本机实测却相反（500 条：无确认 10ms / confirm 120ms / 事务批量 25ms）。三角验证：测得 1 次 RTT=0.366ms，confirm 每条耗时 ÷ RTT = **0.99/0.78/0.67 ≈ 1.0**，证明每条 publish 都在等一次往返。读 pika 源码确认：`BlockingChannel.basic_publish` 在 confirm 模式下执行 `_flush_output(..., is_ready)` 即逐条同步等待。并发实验佐证瓶颈是等待而非 broker（单连接 3865 → 4 连接 4550 条/秒）。**结论：慢的是 pika 同步适配器用法，不是协议机制**。⏳ SelectConnection 本机 ioloop 卡住未跑通，"异步很快"为间接证据 | 实测（l6-perf.py、l6-perf2.py、l6-pika-confirm-check.py、l6-confirm-frames3.py） |
| 2026-08-31 | **公平分发：不设 prefetch 是"抢跑"非"能者多劳"** | 两消费者（快 5ms / 慢 150ms）跑 40 条：不设 prefetch 时**一方独占全部**，跑两轮结果不同（一轮 0:40、一轮 40:0），**与速度无关，取决于谁先连上**（线程调度随机，验证脚本已兼容两种极端）。prefetch=1 → 38:2（19:1，反映速度差）；prefetch=10 → 30:10（3:1）。吞吐随预取值提升：1→4164、20→37938、100→45360 条/秒，无限预取 45440（超过 100 无增益） | 实测（playground/l6-prefetch.py） |
| 2026-08-31 | 当前稳定版本系列 | 4.3 系列，4.3.5 发布于 2026-08-17；4.3.0 发布于 2026-04-23；4.2 系列社区支持已于 2026-07-31 结束；4.3 社区支持至 2026-11-30 | rabbitmq.com/release-information |
| 2026-08-31 | classic queue 镜像 | 自 2021 年弃用，4.0 起完全移除；4.x 的复制型队列为 quorum queue 与 streams，classic queue 变为非复制类型 | rabbitmq.com/ha.html、4.0 新特性文档 |
| 2026-08-31 | 元数据存储与分区策略 | Khepri 在 4.2.0 成为新建集群默认元数据存储，4.3 起为唯一受支持元数据存储；Mnesia 及 pause_minority / autoheal 等分区处理策略已移除 | RabbitMQ 4.3 发布说明、Tanzu RabbitMQ 4.3 发布说明 |
| 2026-08-31 | 起源与归属 | AMQP 由 John O'Hara 在摩根大通牵头提出（2003 年前后），联合 Cisco/IONA/Red Hat/iMatix 成立工作组；2007 年 Alexis Richardson 与 Matthias Radestock 创办 Rabbit Technologies 并发布 RabbitMQ（Erlang/OTP 实现）；2010 年被 SpringSource（VMware）收购，后经 Pivotal、VMware，2023 年随 VMware 归入 Broadcom | InfoQ 演讲记录、公开技术史料（多源一致，置信度：中高） |
| 2026-08-31 | 默认端口 | 5672 = AMQP 0-9-1/1.0 明文；5671 = AMQPS；15672 = 管理界面/HTTP API（HTTP），15671 = HTTPS；25672 = Erlang 分布式（AMQP 端口 + 20000）；5552/5551 = Stream 协议明文/TLS；4369 = epmd 节点发现；CLI 工具用 35672-35682 | rabbitmq.com/docs/networking（官方 4.3 文档） |
| 2026-08-31 | AMQP 1.0 状态 | 自 4.0 起成为核心协议，**始终启用**（插件变为空操作）；但 AMQP 0-9-1 仍是主流客户端默认协议，二者是**两套并行协议**而非升级关系 | rabbitmq.org.cn/docs/whats-new（4.0 新特性，官方镜像） |
| 2026-08-31 | 开源协议 | 核心 broker 采用 **Mozilla Public License 2.0**；另有 Broadcom 商业版 Tanzu RabbitMQ（24/7 支持、更长生命周期） | rabbitmq.com 首页 |
| 2026-08-31 | **transient_nonexcl_queues 弃用推进** | 2021-08 起弃用（permitted_by_default）；**4.3.0（2026-04-23）起推进为 denied_by_default**，非持久化 + 非排他队列组合默认被拒，报 541 INTERNAL_ERROR。官方推荐改用 durable 队列 / exclusive 队列 / 带 TTL 的 durable 队列；开关 `deprecated_features.permit.transient_nonexcl_queues = true` 仅为迁移过渡，未来大版本彻底移除。**实测复现报错并验证 durable=True 解法有效** | 4.3.0 release notes、官方 Queues 文档、弃用特性清单页、GitHub commit 4223e36 |
| 2026-08-31 | **官方 Docker 镜像的 guest loopback 限制** | **与常见说法相反**：官方镜像内置 `/etc/rabbitmq/conf.d/10-defaults.conf` 含 `loopback_users.guest = false`，并自注"DEFAULT SETTINGS ARE NOT MEANT TO BE TAKEN STRAIGHT INTO PRODUCTION"。实测干净容器 `loopback_users=[]`，从容器外用 guest 连 5673 与调 HTTP API **均成功**。"guest 只能本机登录"适用于**非容器化安装**。另实测：设置 `RABBITMQ_DEFAULT_USER` 后 guest 用户被删除 | 实测（容器配置文件直读）+ 官方 access-control 文档交叉印证 |
| 2026-08-31 | **rabbitmqadmin 新版语法** | 镜像自带版本 **2.34.0**，改为子命令结构，**不再支持 `list queues name messages` 这类列名参数**（报 unexpected argument）；凭据用 `RABBITMQADMIN_USERNAME/PASSWORD` 环境变量，否则 `Not_Authorized`。常用参数 `--non-interactive` / `-V` / `--table-style` / `--page-size` | 实测 `rabbitmqadmin --help` 与各子命令实测 |
| 2026-08-31 | **check_local_alarms 已废弃为空操作** | 实测执行会打印 `This command is DEPRECATED and is a no-op. It will be removed in a future version.`，不再返回告警信息。替代：用 `rabbitmq-diagnostics status` 查看 | 实测输出 |
| 2026-08-31 | **没有 `rabbitmqctl delete_exchange` 命令** | 实测 `rabbitmqctl delete_exchange t_direct` 报 `Command 'delete_exchange' not found. Did you mean 'delete_queue'?`。`rabbitmqctl` 只提供 `delete_queue`/`delete_user`/`delete_vhost`，**删除交换机无对应子命令**。实测可用两种删法：① HTTP API `curl -u U:P -X DELETE /api/exchanges/%2F/<name>`（返回 204）；② `rabbitmqadmin delete exchange --name <name> --non-interactive`（**必须用 `--name` 选项**，写成 `name=xxx` 报 `unexpected argument`）。**讲义初稿曾写入此错误命令，已全文更正**（速查卡、踩坑清单、误区表同步） | 实测 |
| 2026-08-31 | **三种队列类型的空队列内存占用（干净测量）** | 各类型建 2 个空队列、间隔 15 秒两次取样：**classic 13,984 B**（稳定）；**quorum 39,492 B → 47,412 B**（**空队列也在持续上涨**，Raft 日志与状态机开销）；**stream 34,704 B**（稳定）。结论修正两点：① quorum 倍率按初测"2.5~5 倍"不准，实为 **2.8~3.4 倍**且随时间增长；② 旧稿称 stream 内存最小（9~35 KB）**不成立**——空队列时 stream(34,704 B) 反而比 classic(13,984 B) 大约 2.5 倍，stream 的内存优势只在**海量消息**场景（消息存段文件、不常驻内存）才体现。讲义与 SVG 已同步更正 | 实测（playground/l5-mem.sh） |
| 2026-08-31 | **裸 `queue_declare(queue='x')` 在 4.3 上的行为（P0 复现）** | 实测确认**直接报 541 并断开整个连接**，与课 3 已教内容一致。课 5 初稿第一幕/第二幕曾写成"裸声明能跑通、只是重启后消息丢"，**属事实错误**（会把课 3 已踩的坑当成能跑的代码教给学员）。已重写为：4.3 上裸声明根本建不了队列 → 本课前提是"队列已 durable"，新冲突改为"容器保住了、内容怎么办"。已加 `l5-check-541.py` 守护该事实 | 实测（playground/l5-check-541.py） |
| 2026-08-31 | **队列属性与 4.3 限制（逐项实测）** | ① transient 非独占（`durable=False`+`exclusive=False`）报 **541 且关闭整个连接**（非信道级 406）；② `exclusive` 是**连接级**独占——跨连接访问报 **405 RESOURCE_LOCKED**，同连接的另一信道 passive 声明**成功**；③ `auto-delete` 触发条件是"**曾有过消费者**+最后一个断开"——从未消费的 auto-delete 队列**保留**（实测 `l5_ad_never 存在? True`）；④ 属性不可改，改类型/改 durable 均报 `inequivalent arg`；⑤ `rabbitmqadmin declare queue` **无 `--exclusive` 选项**，排它队列只能用客户端库创建 | 实测（playground/l5-queue-props2.py、l5-autodelete.py、l5-qtype3.sh） |
| 2026-08-31 | **持久化双开关（重启实测）** | `docker restart` 前后对照：**队列 durable + `delivery_mode=2` → 重启后消息存活**（1→1，内容完整读出）；**队列 durable + `delivery_mode=1` → 重启后消息丢失**（1→0）。证明"队列持久化 ≠ 消息持久化"。另实测：**不设 properties 时 `delivery_mode` 回传 `None`**（字段未设置），**不等于 1**——老教程"默认非持久化"的说法在 4.3 上不成立 | 实测（playground/l5-restart.sh、l5-restart-verify.py、l5-msg-props.py） |
| 2026-08-31 | **消息大小限制的精确行为** | 环境 `max_message_size = 16,777,216 B`（16 MiB）。实测三点：① `basic_publish` **不立刻抛异常**（AMQP 异步）；② 超限消息**从未入队**（队列深度 = 0）；③ 以 **channel 级 406** 返回（`message size 16777217 is larger than configured max size 16777216`），**连接仍存活**（与 541 的连接级错误不同）。5 MiB 消息正常收发（取出 5,242,880 字节） | 实测（playground/l5-msg-size3.py） |
| 2026-08-31 | **stream 队列的限制与回放（原生协议实测）** | 启用 `rabbitmq_stream` 插件后：① **回放成立**——用 rstream 连读三轮，每轮都能从头读到全部 3 条消息，证明**消费后不删除**；② 不支持 `x-message-ttl` / `x-max-length`（报 `invalid arg ... of queue type rabbit_stream_queue`）；③ 不支持 auto-delete（报 `invalid property 'auto-delete'`）；④ **不支持 AMQP `basic.get`**（报 `NOT_IMPLEMENTED - basic.get not supported by stream queues`），须用 stream 协议 5552 + rstream 客户端 | 实测（playground/l5-stream-final.py + sidecar 容器） |
| 2026-08-31 | **stream 实操环境限制** | 课 3 建的 `rabbitmq-learn` 容器**未映射 5552 端口**，且**容器内无 Python/curl**。两种绕行实测可行：① 用 `--network container:rabbitmq-learn` 的 sidecar 容器共享网络命名空间；② 宿主机装 rstream 需建 venv（PEP 668 禁止全局 pip install）。另注意 rstream API：`Producer.send()`（非 `publish`）、`ConsumerOffsetSpecification(offset_type=OffsetType.FIRST)`（非 `OffsetSpecification.first`）、**不加 decoder 才能正确读到 bytes**（加 `amqp_decoder` 会报 `'NoneType' object is not callable`） | 实测 |
| 2026-08-31 | **队列 arguments 四个参数实测** | `x-max-length=3` 发 5 条 → 剩 `m3/m4/m5`（丢最老的）；`x-message-ttl=1500` → 0.5 秒后在、2.5 秒后清空；`x-max-priority=10` 发 `low-1(p1),low-2(p1),high-1(p9)` → 取出顺序 `high-1,low-1,low-2`；`x-expires=2000` → 3.5 秒后队列报 404 消失。支持度：**quorum ✅ TTL/长度限制；stream ❌ 两者** | 实测（playground/l5-args.py） |
| 2026-08-31 | **默认队列类型** | 未配置 `default_queue_type` 时，实测不指定 `--type` 声明队列 → 得到 **classic**；`rabbitmqadmin declare queue` 帮助文本显示 `[default: classic]`。**注意**：AWS 等部分云厂商的 RabbitMQ 4.x 已把默认改为 quorum（其文档明示），但**开源版实测仍是 classic**，教学以本机实测为准 | 实测 + 云厂商文档交叉印证 |
| 2026-08-31 | **消息属性校验与安全** | 以 `learn` 用户连接却在消息里写 `user_id='admin'` → 报 `406 PRECONDITION_FAILED - user_id property set to 'admin' but authenticated user was 'learn'`。**user_id 会被 broker 校验，不能冒充**。另实测 13 个属性（含 headers 三个自定义键值）发送后可原样读回 | 实测（playground/l5-msg-props.py） |
| 2026-08-31 | topic 通配符行为 | 实测 8 条 routing key 对 `*.orange.*` 与 `lazy.#` 两个绑定：`*.orange.*` 收 3 条、`lazy.#` 收 4 条、`lazy.orange.elephant` **同时命中两者**、`orange`（单词）与 `quick.brown.fox` **均不命中被丢弃**。另实测：`#` 可匹配**零个**词（裸 `lazy` 命中 `lazy.#`）；binding key `#` 效果**等同 fanout**（多词/单词消息均全收） | 实测（playground/l4-probe.sh、l4-verify.sh） |
| 2026-08-31 | headers 的 x-match 默认值 | **不写 `x-match` 时按 `all`（AND）处理**：实测绑定 `{'format':'pdf','type':'report'}`（无 x-match），发 `{'format':'pdf','type':'log'}` **未收到**，发 `{'format':'pdf','type':'report'}` 收到。另实测 headers 交换机**完全忽略 routing key**（填 `TOTALLY_IGNORED` 照样投递） | 实测 |
| 2026-08-31 | **默认交换机的限制** | 实测手动 `queue_bind(exchange='', ...)` 报 `(403, 'ACCESS_REFUSED - operation not permitted on the default exchange')`——**不可显式绑定/解绑/删除**。队列创建时自动以队列名为 binding key 绑定（`list_bindings` 显示 source 为空、key=队列名） | 实测 |
| 2026-08-31 | **AE 与 mandatory 的优先级** | 同时配置时：**AE 优先**——消息转入 AE 兜底队列，**mandatory 不触发退回**。即"能转发出去就不算不可路由" | 实测 |
| 2026-08-31 | **default_consumer_prefetch 默认值** | `rabbitmqctl environment` 显示 `{default_consumer_prefetch,{false,0}}`，即**未启用限制（无上限）**。后果：队列已堆积消息时先启动的消费者会抢空全部消息（实测 6 条全被 C2 拿走、C1 得 0 条）；设 `basic_qos(prefetch_count=1)` 或先启消费者可得到 3/3 均分 | 实测 |
| 2026-08-31 | 交换机属性不可改 | 实测重复声明不同类型报 `(406, "PRECONDITION_FAILED - inequivalent arg 'type' ... received 'fanout' but current is 'direct'")`；改 `durable` 导致 `Channel is closed`。与队列的属性不可改属同一错误家族 | 实测 |
| 2026-08-31 | 交换机不存储消息 | `rabbitmqctl list_exchanges name messages` 报 `Info key(s) messages are not supported`——**交换机没有消息数这一列**，可作为"不存消息"的判别依据（对比 `list_queues` 支持 `messages`） | 实测 |
| 2026-08-31 | pika mandatory 退回的触发时机 | pika `BlockingChannel` 上 `add_on_return_callback` **不在 `basic_publish` 返回时立即触发**，需等下一次网络往返（`connection.sleep()` 或补一次同步调用）。实测补交互后收到 `code=312 NO_ROUTE` | 实测 |
