# OpenTelemetry 学习档案

> 本档案是断点续学的唯一依据。每次学习批次完成后必须回写本文件的「进度表」「评审记录」「事实核查」「大纲调整记录」四张表。

---

## 一、学习者画像

| 项 | 值 | 备注 |
|---|---|---|
| **主题** | OpenTelemetry 系统学习 | 可观测性标准（Traces / Metrics / Logs / Profiles） |
| **当前水平** | 入门 | 默认画像，2026-09-02 建档时用户未另行指定 |
| **学习目标** | ① 理解概念 ② 动手实操 ③ 决策参考 | 无对应认证考试要求（OTCA 认证存在但用户未提），故不含「应试认证」 |
| **输出格式** | Markdown（主载体）+ Mermaid（默认图）+ SVG（复杂图 / 路径图） | 不使用 HTML |
| **实操环境** | Windows 11 本机 + WSL Ubuntu 24.04（Docker 29.4.1、Python 3.12.3、uv 可用） | 与 redis / rabbitmq / surrealdb 课程同环境；**node / fnm / go / java 均未安装** |

**画像说明**：本画像为默认推断值，依据 `D:/projects/learning` 仓库既有课程（elasticsearch / redis / influxdb / surrealdb）的画像惯例设定。若实际水平或目标不同，请直接说明，我会据此调整各课的深浅与实操比重，并记录在下方的「大纲调整记录」。

**⚠️ 环境约束（建档时实测，影响后续实操设计）**：

- WSL Ubuntu 24.04 内**无 Node.js / Go / Java 运行时**。故本课程的实操语言定为 **Python**（uv 可用，Python 3.12.3），Collector 与后端（Jaeger / Prometheus）走 **Docker 容器**。
- 若后续需要演示 Go / Node 插桩，须先征询用户是否安装对应工具链（按运行环境约定第 3 条硬纪律，不擅自安装）。

---

## 二、模式与进度总览

| 项 | 值 |
|---|---|
| **模式** | 课程制（4 阶段 / 12 课 / 42 知识点） |
| **故事主线** | 一次 502 的四小时（详见 `01-学习路径总览.md`） |
| **建档日期** | 2026-09-02 |
| **当前阶段** | 阶段 4《生产落地》（课 10、课 11、课 12 已完成，12/12 课完结） |
| **已完成知识点** | **42 / 42**（41 课内知识点 + 结课实战项目 1 项） |
| **进度** | ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓ 100%（最近更新：2026-09-04，`final-课程手册.md` 完成；课 1-课 12 全部讲完 + 结课实战项目完成，全课程收官） |

---

## 三、知识点级进度表

> 状态标记：`⬜ 未开始` / `🟡 学习中` / `✅ 已完成` / `⏸ 跳过`

### 阶段 1：三个控制台的四小时（"为什么需要 OTel"）

| # | 知识点 | 所属课 | 状态 | 完成日期 |
|---|--------|--------|------|----------|
| 1.1 | 三个控制台的四小时：可观测性碎片化的真实代价 | 课1 | ✅ | 2026-09-02 |
| 1.2 | 可观测性的三根支柱与它们的割裂 | 课1 | ✅ | 2026-09-02 |
| 1.3 | OTel 是什么：一句话定义与四大组件 | 课2 | ✅ | 2026-09-02 |
| 1.4 | OTel 不是什么：划清五条边界 | 课2 | ✅ | 2026-09-02 |
| 1.5 | 起源：OpenTracing + OpenCensus 的合并 | 课2 | ✅ | 2026-09-02 |
| 1.6 | 生态现状与版本基线 | 课2 | ✅ | 2026-09-02 |
| 2.1 | 环境搭建：uv 虚拟环境 + Docker 后端 | 课3 | ✅ | 2026-09-02 |
| 2.2 | 第一个 Trace：从零到 Jaeger 上的一条链路 | 课3 | ✅ | 2026-09-02 |
| 2.3 | Resource：这条数据是谁发的 | 课3 | ✅ | 2026-09-02 |

> **注**：上表课号与大纲一致——课1 = 《一次 502 的四小时》，课2 = 《OTel 是什么与它不是什么》，课3 = 《跑起来：第一个 Trace》。编号「2.x」属于课 3，是建档时沿用的分段记法（1.x 属课 1、课 2，2.x 属课 3），**不是**编号错误，后续回写请按此对齐。

### 阶段 2：一次请求的完整旅程（"Traces"）

| # | 知识点 | 所属课 | 状态 | 完成日期 |
|---|--------|--------|------|----------|
| 3.1 | Span：链路的最小单元 | 课4 | ✅ | 2026-09-02 |
| 3.2 | Trace 与 Span 上下文：trace_id / span_id / trace flags | 课4 | ✅ | 2026-09-02 |
| 3.3 | 上下文传播：W3C Trace Context 与 traceparent | 课4 | ✅ | 2026-09-02 |
| 3.4 | Baggage：随请求携带的键值对 | 课4 | ✅ | 2026-09-02 |
| 4.1 | 手动插桩：Tracer API 与 Span 生命周期 | 课5 | ✅ | 2026-09-03 |
| 4.2 | 自动插桩：零代码覆盖框架与库 | 课5 | ✅ | 2026-09-03 |
| 4.3 | 语义插桩与 Span 事件、状态、异常 | 课5 | ✅ | 2026-09-03 |
| 5.1 | 为什么必须采样：数据量的物理约束 | 课6 | ✅ | 2026-09-03 |
| 5.2 | 头部采样：简单但有偏 | 课6 | ✅ | 2026-09-03 |
| 5.3 | 尾部采样：完整但有状态 | 课6 | ✅ | 2026-09-03 |

### 阶段 3：指标与日志（"另外两个信号"）

| # | 知识点 | 所属课 | 状态 | 完成日期 |
|---|--------|--------|------|----------|
| 6.1 | 指标模型：Instruments 全家桶 | 课7 | ✅ | 2026-09-03 |
| 6.2 | 六种 Instruments 与聚合视角 | 课7 | ✅ | 2026-09-03 |
| 6.3 | Exponential Histogram：P99 精度问题 | 课7 | ✅ | 2026-09-03 |
| 6.4 | Exemplar：从指标跳回链路 | 课7 | ✅ | 2026-09-03 |
| 7.1 | 日志桥接 API：不替换你的日志库 | 课8 | ✅ | 2026-09-03 |
| 7.2 | 日志关联：`trace_id` 自动注入 | 课8 | ✅ | 2026-09-03 |
| 7.3 | 日志采集的两条路径 | 课8 | ✅ | 2026-09-03 |
| 8.1 | 语义约定是什么：可观测性的通用词汇表 | 课9 | ✅ | 2026-09-03 |
| 8.2 | HTTP 属性的弃用与迁移 | 课9 | ✅ | 2026-09-03 |
| 8.3 | 稳定性等级：Stable / Beta / Alpha 怎么读 | 课9 | ✅ | 2026-09-03 |
| 8.4 | 自定义属性与 Cardinality 爆炸 | 课9 | ✅ | 2026-09-03 |

### 阶段 4：生产落地（"敢不敢上"）

| # | 知识点 | 所属课 | 状态 | 完成日期 |
|---|--------|--------|------|----------|
| 9.1 | Collector 的角色：可观测性的 nginx | 课10 | ✅ | 2026-09-03 |
| 9.2 | 管道三件套：Receiver / Processor / Exporter | 课10 | ✅ | 2026-09-03 |
| 9.3 | 用 Processor 做脱敏、过滤与富化 | 课10 | ✅ | 2026-09-03 |
| 9.4 | Collector 的稳定性等级与发行版选择 | 课10 | ✅ | 2026-09-03 |
| 10.1 | 部署拓扑：Agent / Gateway / 混合 | 课11 | ✅ | 2026-09-03 |
| 10.2 | 成本治理：数据量与 Cardinality | 课11 | ✅ | 2026-09-03 |
| 10.3 | Kubernetes 与 Operator 注入 | 课11 | ✅ | 2026-09-03 |
| 11.1 | 第四信号 Profiles：它补上了什么 | 课12 | ✅ | 2026-09-04 |
| 11.2 | 横向对比：Prometheus / Jaeger / 商业 APM | 课12 | ✅ | 2026-09-04 |
| 11.3 | 迁移路径：存量系统怎么上 OTel | 课12 | ✅ | 2026-09-04 |
| 11.4 | 收束：四小时能缩短到多久 | 课12 | ✅ | 2026-09-04 |

**合计**：42 个知识点（阶段 1：9 / 阶段 2：10 / 阶段 3：11 / 阶段 4：12）

---

## 四、评审记录

> 每个批次生成后必须经**双视角评审**（教学法视角 pedagogy + 学习者视角 learner），P0 清零后才允许进入下一批。

| 日期 | 批次 / 产物 | 评审方式 | 视角 | P0 | P1 | P2 | 意见摘要 |
|------|------------|----------|------|----|----|----|----------|
| 2026-09-02 | 多阶段大纲（4 阶段 / 12 课 / 42 知识点 + 故事主线） | 主 agent 内联（course-reviewer 子 agent 未创建，独立性受限） | pedagogy | 0 | 2 | 2 | 见下方「大纲评审详情」 |
| 2026-09-02 | **课 1《一次 502 的四小时》**（知识点 1.1 碎片化的真实代价、1.2 三根支柱与它们的割裂，共 2 知识点） | **主 agent 内联**（course-reviewer 子 agent 未创建，独立性受限；按技能要求跑**教学法视角 5 维度 + 学习者视角 L1-L6 两轮独立评审**） | **P0×0 / P1×2 / P2×3，全部当批修复。** **【第一轮 · 教学法视角】** 教学逻辑：本课定位「先立靶子」——1.1 建立痛感（四类成本）、1.2 拆开三根支柱并给出根因（缺 trace_id），依赖链无缺口，且认知阶梯为感知层→概念层，无倒序。准确性：全部事实性内容取自当轮联网核查——三根支柱源自 Peter Bourgon 2017-02《Metrics, tracing, and logging》Venn 图（原始目的是共享词汇而非架构蓝图）、2018-08《Observability signals》提出 über-system 设想、Metrics 起源 RRDtool 1999→Graphite 2008→Prometheus 2012、Logs 起源 Splunk 2003→Elasticsearch 2010、Traces 起源 Dapper 2010→Zipkin 2012→Jaeger 2017、MELT 占 39.5% 与 NETSCOUT 2026 对 950 人调查的 17%→11% / 14.6%→28.7% 数据、40-60% 事故时间用于收集信息、人工调查占 MTTR 60-80%、告警到首个假设 30-45 分钟且切换 6-8 个工具——均已在事实核查表登记来源与置信度。完整性：两个知识点六要素（一句话定义/直觉建立/核心原理/示例演示/常见误区/一句话记住）齐全；五幕结构完整。格式规范：五幕 / Mermaid / 🐞误区 / 一图总结 / 📌练习 / 📍全局定位 / 🔗下一步 / ⚠️别急着下结论 / 本课小结 / 🚀接力提示词 / 🧭课程导航 / 交付状态，十二项齐全；Mermaid 配色沿用浅底深字（蓝 #eff6ff/#2563eb、红 #fef2f2/#dc2626、绿 #ecfdf5/#059669、黄 #fefce8/#ca8a04）。一致性：与阶段 1 overview 的知识点表、故事主线、认知阶梯完全对齐。**P1×1**——时间拆解表各行合计 165 分钟，但事故跨度（21:14→23:53）只有 159 分钟，且正文写「两个小时三十六分钟」与课名「四小时」不自洽 → 已重构为可核对的**时钟区间表**（21:09 起算，各行首尾相接，合计 164 分钟，与课名「接近四小时」自洽），并同步修正四类成本表的有效时间占比（11%→6%）、Mermaid 图、三句话带走、演练 B 结论共 5 处。**P2×1**——课名含「四小时」但正文未解释这个数字怎么来的 → 已补「164 分钟 + 事后复盘 ≈ 四小时」的口径说明，并点出「当一类事故有了专属称呼，说明它不是意外，是常态」。**【第二轮 · 学习者视角】** L1 场景吸引力：周五晚 21:09 大促期间 502、客服群先炸、按真实时钟推进的时间线，代入感强，无问题。L2 由浅入深：先感受痛（第一幕）→ 再追问为什么（第二幕）→ 最后拆原理（第三幕），无问题。L3 认知阶梯：1.1 感知层 → 1.2 概念层，无倒序，无问题。L4 故事弧：五幕完整；第二幕「这不是人的问题」与第五幕「⚠️别急着下结论」形成呼应，且为课 2 埋下「OTel 不能存数据也不能画图」的钩子，无问题。L5 困惑点：**P1×1**——演练 B 声称「从 2 小时 36 分压到 30 分钟以内」，但跨团队协调的 115 分钟里大部分与工具无关（等人上线、走变更流程），该结论会**过度承诺 OTel 的收益** → 已改为更保守的「164 分钟压到 40 分钟以内」，并补 📌 诚实说明：**OTel 能砍掉的是「找数据」的时间，砍不掉「等人」的时间**，且该区分将在课 12 结算收益时单独核算。**P2×2**——①知识点 1.2 的示例演示写了「注意加粗的那个共同字段」，但代码块内 JSON 无法加粗 → 已改为代码块内注释 `// ← 钥匙` 引导；②三种镜头的类比（航拍/地面特写/跟拍）与前面「三位医生」的类比存在**类比过载**（同一课内两套独立比喻体系）→ 已接受并保留（二者服务于不同知识点：医生类比服务于 1.1「信息无法合并」，镜头类比服务于 1.2「各答一问」，分工明确），但已在两处各自紧扣对应知识点，避免读者混淆。L6 全局定位：第五幕含 📍 全局定位（回扣课 2/3/8/12）+ 🔗 下一步，无问题。**裁决**：两视角唯一潜在冲突在于——教学法视角要求「结论要有力」，学习者视角要求「不要过度承诺」；主 agent 采纳**学习者视角的保守口径**并在正文保留诚实说明（与 influxdb 课程「假设值须三重标注」的既有规范一致） | 通过，落盘 |
| 2026-09-02 | **课 2《OTel 是什么与它不是什么》**（知识点 1.3 四大组件、1.4 五条边界、1.5 起源与合并、1.6 生态与版本基线，共 4 知识点） | **主 agent 内联**（course-reviewer 子 agent 未创建，独立性受限；按技能要求跑**教学法视角 5 维度 + 学习者视角 L1-L6 两轮独立评审**） | **P0×0（修复后）/ P1×2 / P2×2，全部当批修复。** 教学法视角：先立后破结构到位（1.3 是什么 → 1.4 立刻不是什么），第一幕「周一晨会」承接课 1 的 164 分钟，第二幕三个冲突反问拆掉错误期待；准确性方面全部事实取自当轮联网核查 + 本机实测（CNCF 2019-05-07 接纳 / 2026-05-11 TOC 通过毕业 / 2026-05-21 明尼阿波利斯 Observability Summit 宣布、OpenTracing 2022-01 归档、OpenCensus 2023-07 归档、Collector v0.107.0 移除 OpenCensus bridge、12,000+ 贡献者 CNCF 公告口径 vs 27,247 LFX 口径、项目速度 CNCF 第 2、规范 1.59.0 / 语义约定 1.43.0 / Collector v1.64.0-v0.158.0 / Python SDK 1.44.0 / JS sdk-node 0.222.0 / JS api 1.9.1，后四项为 PyPI 与 npm registry 实时实测）；完整性四知识点六要素齐全、五幕完整；格式十二项齐全，Mermaid×4 配色沿用浅底深字。**【P0 级不一致，已修复】** 原「本机环境提示」称"Windows 有 Node、无 Python，课 3 优先用 Node"，与课 3 骨架已实测结论（WSL Ubuntu 有 Python 3.12.3 + uv + Docker，无 Node/Go；课 3 定为 Python + Docker）**直接冲突** → 实测双环境后改写为对照表，并同步修正接力提示词。**【P0 级实测错误，已修复】** 演练 D 原写"未配置 SDK 时返回 NoOpTracerProvider"——**这是错的**；WSL 实跑验证实际返回 **ProxyTracerProvider**（内部 `_real_tracer_provider` 为 None，span `trace_id` 为 0、`is_recording()` 为 False），许多教程沿用 NoOp 说法，照抄会给出错误诊断方法 → 已按实测重写，并确立 **`trace_id == 0` 为最可靠判据**（比 provider 类名更稳），同步修正知识点 1.3 中同一处表述。**P1×1** 演练 D 原"如果你有 Python 环境"表述含糊，已改写为 WSL 下可直接执行的 uv 命令链并升级为推荐。**P2×1** 版本号表补标"PyPI / npm 实时实测，不是抄文档"。学习者视角 L1-L6 无阻塞项；L5 抓出的 NoOp 错误是本轮最重要产出。**裁决：两视角无实质冲突。本轮两次"跑一遍而不是抄一遍"分别发现环境冲突与 NoOp 错误，均已当批修复并登记事实核查** | 通过，落盘 |
| 2026-09-02 | **课 3《跑起来：第一个 Trace》**（知识点 2.1 环境搭建、2.2 第一个 Trace、2.3 Resource，共 3 知识点；**本课程首个实操课**） | **主 agent 内联**（course-reviewer 子 agent 未创建，独立性受限；按技能要求跑**教学法视角 5 维度 + 学习者视角 L1-L6 两轮独立评审**） | **P0×0（修复后）/ P1×3 / P2×2，全部当批修复。** 教学法视角：五幕为「我知道了，然后呢」→「代码跑通但后端空空」→「拆三段链路」→「四步实操」→「Resource 收束」，冲突幕选择"沉默的失败"作为认知冲突，精准命中初学者第一痛点；准确性方面全部数据来自当轮 **WSL 实跑**（Jaeger v2.20.0 / 内部 OTel Collector v0.155.0 / Python 3.12.13 / uv 0.11.6 / Docker 29.4.1 / OTel SDK 1.44.0），骨架 P0 硬约束「所有命令必须实测」已满足。**【P0 级实测结论，本课最重要产出】** `force_flush()` 在 **五种错误导出配置下均返回 `True`**，但数据全部静默丢弃 → 确立**唯一可信判据为后端 `GET /api/services`**，并以"每个配置独立 service.name"的实验设计确证（fault-F1/F2/F3/F5 从未在后端注册，仅 fault-F4 到达）。**【P1 级实验设计缺陷，已修复】** 首轮实验让五种配置共用同一服务名 `faultlab`，后端显示 2 条导致误判"F4 之外也有成功" → 改为 `fault-F1`…`fault-F5` 独立命名后结论才确凿，且该教训已写入正文提醒学员。**【P2 级，已修复】** 正文原未解释 Jaeger 自身为何出现在服务列表 → 补充"Jaeger v2 基于 Collector 构建，会给自己发遥测（实测 9 条自监控 trace）"。**【P2 级，已修复】** `uv run` 命令补充说明：实测无需 `VIRTUAL_ENV` 或 `--no-project`，uv 自动选中 `lab03/.venv`。**P1 级补充**：正文开头补双环境对照表，并说明 WSL 系统 Python 3.12.3 与 uv 实际使用的 CPython 3.12.13 之差异，避免学员困惑。**P1 级补充**：把课 2 遗留的 `ProxyTracerProvider` 发现落到本课实操场景——新增误区五「在 `set_tracer_provider()` 之前就 `get_tracer()`」，实测 `trace_id=0` 且**不报错**，并揭示反直觉细节：**同一个早期 tracer 对象在 provider 设置之后复用，数据能补回来**（代理延迟绑定所致）。学习者视角 L1-L6 无阻塞项；L5 抓出的"flush_ok 撒谎"是本轮最高价值发现，直接决定了全课的排错方法论。**裁决：两视角无实质冲突。本轮延续课 2 的「跑一遍而不是抄一遍」原则，三次"险些写错"均由实测拦下（fault 计数、uv 命令形态、tracer 顺序），全部当批修复** | 通过，落盘 |
| 2026-09-02 | **课 4《Span 与上下文传播》**（知识点 3.1 Span、3.2 Trace 与 Span 上下文、3.3 上下文传播与 traceparent、3.4 Baggage，共 4 知识点；**首个跨进程实操课**） | **主 agent 内联**（course-reviewer 子 agent 未创建，独立性受限；按技能要求跑**教学法视角 5 维度 + 学习者视角 L1-L6 两轮独立评审**） | **P0×1（已修复）/ P1×3 / P2×2，全部当批修复。** 教学法视角：五幕为「课 3 那条链路不止一个框」→「明明同一次请求却断成两条」→「Span 字段 → 上下文结构 → traceparent 拆解」→「手工解析 + 故意断链再修好」→「Baggage 定位与代价」，冲突幕以"断链"为认知冲突，精准命中阶段 2 预设高频困惑点第 1 条。**【P0 级事实错误，已修复】** 第二幕汇总表最初**混用了两个不同实验的数据**：第 ① 行取自有 bug 的 Flask 实验（`78814db1`），其余四行取自修复后的最小实验；更严重的是四个断链行原引用**上游 root 的 trace_id** 而非下游新建的那个，反而**掩盖了断链证据**。已用最小实验权威数据重填（下游 trace_id 分别为 `9772cc4f`/`4f5d18ca`/`fd93883c`/`ee0d7335`，与上游全不相同）。**【P1 级，已修复】** 补齐"下游另起炉灶"的对照说明，让 ① 与 ②-⑤ 的差异一眼可见。**【P1 级补充】** 把两处实验踩坑写入正文：①Flask+requests 因自动插桩覆盖手工 header 导致四种断链**全部失真**，须改用未插桩 `http.client`；②`tracer` 在 Flask 路由外获取会导致**连正常场景也断链**，须先 `attach(extract(...))` 再取 tracer——与课 3 误区五同源。**【P1 级补充】** 补 `extract` 产出 `NonRecordingSpan` + `is_remote=True` 的实测说明，讲清"远程父跨度"与跨服务边界的绘制依据。**【P2 级，已修复】** 练习 1/2/3/4 补实测参考答案（含 `09` 陷阱：`sampled=True` 但数值≠01）。**【P2 级，已修复】** 统一正文中 trace_id 引用，消除跨实验混用。学习者视角 L1-L6 无阻塞项；L5 抓出的"`trace_flags` 实测为 `03` 而非 `01`"是本轮最高价值发现——老教程的 `if flags == "01"` 在 1.44.0 上会**全部误判未采样**。准确性方面：`traceparent` 格式已按骨架 P0 硬约束**核对 W3C 官方规范原文**（55 字符 / 全小写 / 全零非法 / `ff` 禁止 / tracestate 32 项上限 / 最左为最近更新），断链五种场景全部实测复现并经后端统计确认（`linked=1 / broken=4`）。**裁决：两视角无实质冲突。本轮延续「跑一遍而不是抄一遍」原则，四处"险些写错"均由实测或交叉核对拦下（表格数据混用、断链证据被掩盖、tracer 时机、flags 位运算）** | 通过，落盘 |
| 2026-09-03 | **课 9《语义约定：命名的战争》**（知识点 8.1 语义约定、8.2 属性弃用与迁移、8.3 稳定性等级、8.4 Cardinality，共 4 知识点；**阶段 3 收官课**） | **主 agent 内联**（course-reviewer 子 agent 未创建，独立性受限；跑**教学法视角 5 维度 + 学习者视角 L1-L6 两轮独立评审**） | **P0×0 / P1×1 / P2×1，全部当批修复。** 五幕为「两个团队同一个东西两个名字」→「照着老教程写的 `http.method` 被告知弃用了」→「79 个域 → 分层组织 → 七级成熟度 → 规范稳定≠实现稳定」→「扫描 → 改写 → Collector 层迁移」→「Cardinality 红线收束」，冲突幕以"弃用了但数据明明正常"为认知冲突，精准命中"弃用 ≠ 不能用"这个核心误解。**【本课最重要产出：骨架的"五级生命周期"是过时说法，官方现为七级，且 `stability` 字段只有五个取值】** 核对规范原文 `maturity-levels.md`（Status: Stable）与 `versioning-and-stability.md`（Status: Stable）确认：现行七级为 Development/Alpha/Beta/Release Candidate/Stable/Deprecated/Unmaintained，且 **`Experimental` 已于 2023 年更名 `Development`**（原文第 90 行）。骨架所写 Draft/Experimental/Stable/Deprecated/Removed 五级的三处偏差：①Draft 现称 Development；②**缺 Release Candidate 级**；③Removed 不是等级，对应的是 Unmaintained（无活跃维护者，6 个月后可转 Deprecated）。更关键的发现：扫描官方仓库 `model/` 全目录共 **2772 处** `stability:` 字段，取值只有五个——**development 2262 / stable 260 / release_candidate 231 / experimental 14 / alpha 5**；**没有 beta 取值，且 `deprecated` 是独立字段不是 stability 值**（弃用属性的 `stability` 会被重置为 `development`）。已在正文开篇用对照表正面修正骨架表述，并给出三个可操作结论：stable 仅占 9.4% 是稀缺资源、release_candidate（231 处）是被忽略的"准稳定"级、development 占 81.6% 意味着多数属性仍可变更。**【第二产出：弃用属性三档开关实测，修正"弃用了就不能用"的误解】** Flask 插桩在 5 档 `OTEL_SEMCONV_STABILITY_OPT_IN` 下的真实 span 属性（InMemorySpanExporter 抓取，子进程隔离）：**default 档出 13 个属性、其中 11 个是旧名**（数据完全正常，只是官方不再保证）；**http 档出 10 个、全是新名**；**http/dup 档出 22 个（13 旧 + 9 新）**；**database / database/dup 档对 HTTP 属性毫无影响**——证明三档开关是**分域独立**的，`http` 管不了 `database`。迁移对照表**逐条核对官方源码**（`model/http/deprecated/registry-deprecated.yaml` 14 条 + `model/db/deprecated/registry-deprecated.yaml` 30 条），而非凭记忆或二手博客。**【第三产出：Cardinality 实测 15 → 150,000，倍数恒等于维度基数】** 真实 SDK Counter 聚合：5 路由 × 3 状态码 = **15 条序列**；加 10000 个 `user.id` 后 = **150,000 条**，放大 **10,000 倍**——**倍数正好等于加进去的维度基数**（加基数为 N 的维度，序列数与成本都乘 N），这是本课最重要的一条算术。5 维推演：15 → 150,000 → 600,000 → **1.2 亿**。**【P1 级数字错误，已修复】** 成本换算表原写"一年 73 MiB、倍数 ~8,000×"，按 2 字节/样本、15s 采样、365 天重算实为 **60 MiB / 10,000×**，已修正并补上"倍数恒等于维度基数"的判据。**【P2 级补充】** 事实核查表补"本机 semconv 0.65b0 对应 schema 1.43.0、官方 main 已 v1.44.0，**本机落后 1 个版本**"的实测条目，进一步强化"规范稳定 ≠ 实现稳定 ≠ 插桩稳定"这条全课程纪律（本课第三次回应：课 7 `create_gauge`、课 8 Python Logs SDK、本课 semconv 0.65b0 仍是 beta）。学习者视角 L1-L6 无阻塞项；L5 抓出的"`http.route` vs `url.path`"是最高价值发现——**`http.route` 是路由模板（低基数，可放指标），`url.path` 是真实路径（高基数，放指标即爆炸）**，官方 registry 对 `http.route` 的要求原文即为 *"the route attribute should have low-cardinality and the URI path can NOT substitute it"*。**裁决：两视角无实质冲突，通过落盘。本轮延续"跑一遍而不是抄一遍"原则，两处"险些写错"均由重算与源码核对拦下（成本倍数、稳定性等级数量）** | 通过，落盘 |
| 2026-09-03 | **课 8《日志桥接与信号关联》**（知识点 7.1 日志桥接 API、7.2 日志关联、7.3 采集两条路径，共 3 知识点；**三根支柱第一次真正打通**） | **主 agent 内联**（course-reviewer 子 agent 未创建，独立性受限；跑**教学法视角 5 维度 + 学习者视角 L1-L6 两轮独立评审**） | **P0×1（已修复）/ P1×2 / P2×1，全部当批修复。** 五幕为「链路定位到服务，然后呢」→「你以为日志已稳定，其实差得远」→「Bridge API 在哪 → 关联怎么实现 → 两条路怎么选」→「五步实操」→「三根支柱打通」。准确性方面全部数据来自当轮 **WSL 实跑**（八组实验 A/B/C1-C12/D/E/F/G）。**【本课最重要产出：采样切不断日志关联，只切得断 exemplar】** 三组子进程隔离实测（200 次请求）：`always_on` 200/200 采样、**200 条日志全带 trace_id**；`ratio_10pct` 25/200、**200 条全带 trace_id**；**`always_off` 0/200 采样、日志仍 200 条 100% 带 trace_id，而 exemplar 归零**。根因：日志关联判据是"span context 是否有效"，exemplar 判据是"SAMPLED 位"；`always_off` 是"创建 span 但不置 SAMPLED 位"，故 trace_id 有效而 exemplar 不生成。**这修正了流传很广的误解「降采样会让日志关联失效」**，并给出工程结论：降采样省钱不会丢日志与请求的对应关系，**但那个 trace_id 在后端点不开**（课 6「真相与成本的权衡」在日志侧的投影）。**【第二产出：三件被混为一谈的事】** ①日志能发出去吗？**能**（Collector 实测 `log records: 3`）②后端收不收？**Jaeger 不收**（`/v1/logs` → **404 Unimplemented**，实测 `n_spans=1 / n_logs=0`）③trace_id 挂上了吗？**挂上了**（实测 `TraceId (hex)` 与 span 完全一致）。**② 的失败被误认为 ①③ 失败，是第二幕全部困惑的根源**；这也是本课程**第六个静默失败**，形态最新——**导出器返回成功、Collector 收到，数据在后端被静默丢弃**。**【第三产出：Python Logs SDK 的 Development 状态有具体技术表现】** `import opentelemetry.sdk.logs` → **`ModuleNotFoundError`**；`opentelemetry/sdk/` 目录下 `metrics`/`trace` 是公开包而 **`_logs` 带下划线**；`sdk._logs` 仅暴露 12 个公开名。**【P0 已修复】** 汇总表列入两条内容相同的「日志发给 Collector = 后端能查到」（误区 1 与 11）→ 删除重复项并连续重编号为 20 条。**【P1】** 课 6 的 Collector **只有 traces pipeline**，对 `/v1/logs` 直接回 404；本课新建 `otelcol-lab08`（34317/34318）加 logs pipeline 后 404 消失；另建 `otelcol-lab08dbg`（35318）用于看完整字段。**【P1】** 路径 B 实测：**异常堆栈 4 行全部解析失败**（不带 trace_id 前缀），须配 `multiline`；filelog receiver / Fluent Bit 已标注**本机无镜像未实测**。**【P2】** 三处导入路径坑见评审清单第 9 批。**【本轮最长排查，单独记录】** exemplar 计数恒为 0 的排查耗时 **11 轮**（C1-C11）：前八轮做"加法"（逐项对齐课 7 脚本的配置差异）全部失败；**C9 改为"复制课 7 脚本做减法"后定位到"只留 D1 仍是 1"，C10 逐项关闭五个变量仍得 1，证明与配置无关**；**C11 四模式实测定位根因：`InMemoryMetricReader` 的 exemplar 是一次性的**——`get_metrics_data()` 触发 collect 后 reservoir 清空，而 `force_flush()` 内部也触发一次 collect，所以「flush 后再读」恒为 0。实测四模式：`immediate=1` / `after_force_flush=0` / `after_flush_and_sleep=0` / 读两次 `1 然后 0`。**正确姿势：record 后立刻读，读一次，绝不 flush 后读。三条方法论：①复制原脚本做减法远优于重写脚本做加法；②所有变量排除后结论仍不变时，怀疑观测方式本身；③课 7 结论本身没错，错的是本轮的测量方式。** 这是「测量工具自己骗人」的第七次出现。学习者视角 L1-L6 无阻塞项。**裁决：两视角无实质冲突，通过落盘** | 通过，落盘 |
| 2026-09-03 | **课 7《指标模型与六种 Instruments》**（知识点 6.1 指标模型、6.2 六种 Instruments、6.3 Exponential Histogram、6.4 Exemplar，共 4 知识点；**阶段 3 首课，指标信号首次系统展开**） | **主 agent 内联**（course-reviewer 子 agent 未创建，独立性受限；跑**教学法视角 5 维度 + 学习者视角 L1-L6 两轮独立评审**） | **P0×1（已修复）/ P1×2 / P2×1，全部当批修复。** 五幕为「平均 200ms 骗了你」→「六种 Instrument 选错数据就废」→「模型分层 → 六种选择 → 分桶精度」→「五步实操」→「Exemplar 打通指标与链路」，冲突幕以"购物车该用哪个 Instrument"为认知冲突，精准命中阶段 3 overview 预判的高频困惑点第 1 条。准确性方面全部数据来自当轮 **WSL 实跑**（八组实验 B1/B2/C/D/E1/E3/E4-E5/F + Prometheus 端到端），骨架三条 P0 硬约束全部满足。**【本课最重要产出：指标永不采样，exemplar 跟着采样走】** 10000 次请求、trace 采样率 0.1% 实测：被采样 span 仅 **11 条**（0.11%），Histogram 的 count 仍是 **10000**（全量，指标永不采样），exemplar 仅 **1** 条。这回答了课 6 埋下的伏笔，并把「采样率怎么定」从纯成本问题转化为「要多少条可跳转链接」的问题，给出反推公式与 100/1000 QPS 两组下界。**【第二产出：P99 精度量化】** 5 场景 × 10000 样本、真实 SDK 聚合（非手写分桶）：显式桶最坏 **−50.0%**（E 场景 20s 被砍成 10s，且方向是**低估**——监控上看起来还行，最危险）、指数桶最坏 **−2.2%**；自适应证据：窄区间场景 scale 自动从 3 升到 **8**，E 场景仅 2 个非空桶说明桶按需分配不预分配。**【第三产出：六种 Instrument 的 Prometheus 出口实测】** `UpDownCounter` 出口是 **`gauge`** 而非 counter（反直觉，因 Prometheus counter 语义必须单调）；加不加 `_total` 取决于「能否减少」而非「是否异步」；Histogram 一个变 **18 条**序列，成本比 **18:1**。**【P0 级内部矛盾，已修复】** 第一幕示例用线性插值算「9900 快+100 慢」得 P99=**209.9ms**，而第三幕实验脚本用 nearest-rank 得 **20000ms**——同一分布两个答案，而正文却断言 P99=20000。修正：①示例改为 9850+150（此时两种口径均得 20000）；②1.2 新增「两种分位数口径对照表」并解释 `k=(n−1)×q=9899.01` 的插值权重仅 0.01；③实验表上方加口径声明，并点明这个"不对等"恰是真实世界（你拿桶估的分位数与拿原始数据算的本就不是一个算法）。**【P1 级数据错误，已修复】** 指数桶自适应证据表「非空桶数」五格全错（108/92/87/45/76），**重跑 `l7_c_exp.py` 后按实测修正为 83/98/45/108/2**——这是"凭记忆填表"的典型失误，已固化为「凡表格数字必须回查脚本输出」。**【P1 级补充】** Python SDK 提供规范外的第 7 个同步 `create_gauge`（LastValue 聚合，set 10→20→5 得 5），Go/Java/.NET 各有其扩展；已写入 2.7 并落到「规范稳定 ≠ 实现稳定」这条全课程纪律上。**【P2 级补充】** 三处导入路径坑：`InstrumentSelector` 在 SDK 1.44.0 **不存在**（`view` 模块只导出 8 个名字，须直接把 Instrument 类传给 `instrument_type`）；`Observation` 在 API 层 `opentelemetry.metrics` 不在 SDK 层；`PrometheusMetricReader` 在 `opentelemetry.exporter.prometheus` 不在 `sdk.metrics.export`。学习者视角 L1-L6 无阻塞项；L5 抓出的**"指标说有问题、trace 却找不到"**是跨课呼应（课 6 练习 4 已指出这是采样链路的诊断信号，本课给出根因：指标全量而 trace 采样）。**裁决：两视角无实质冲突。本轮延续「跑一遍而不是抄一遍」原则，三处"险些写错"均由重跑脚本或跨节核对拦下（分位数口径、非空桶数、口径声明）；另首次出现"表格数字凭记忆填写"的失误类型** | 通过，落盘 |
| 2026-09-03 | **课 5《手动与自动插桩》**（知识点 4.1 手动插桩、4.2 自动插桩、4.3 语义插桩与事件状态异常，共 3 知识点；**首个插桩方式对比课**） | **主 agent 内联**（course-reviewer 子 agent 未创建，独立性受限；按技能要求跑**教学法视角 5 维度 + 学习者视角 L1-L6 两轮独立评审**） | **P0×0 / P1×2 / P2×1，全部当批修复。** 教学法视角：五幕为「只有 HTTP Span 的链路」→「加了自动插桩还是查不出问题」→「手动生命周期 / 覆盖范围 / 事件与状态」→「六步实操」→「两种插桩完整对照」，冲突幕以"945ms 黑洞"为认知冲突，承接课 4 的断链主题并推进到"链路有了但没用"。准确性方面全部数据来自当轮 **WSL 实跑**（六轮实验：Span 生命周期 / 三模式对比 / 属性名新旧 / sqlite3 覆盖矩阵 / CLI 端到端 / 混合插桩），骨架 P0 硬约束「自动插桩覆盖范围须写实测结论」已满足。**【本课最重要产出：两个静默零数据陷阱】** ① 缺 `opentelemetry-distro` 时，`opentelemetry-instrument` **只加载插桩库、不配置 SDK 导出器**，实测 provider 仍为 `ProxyTracerProvider`、`trace_id=0`、`is_recording=False`，数据全部丢弃且**无任何报错**——装上后 provider 变真实 `TracerProvider`、resource 多出 `telemetry.auto.version=0.65b0`（自动插桩指纹）；② `OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318` **不会**让协议自动切成 HTTP，OTLP 默认 gRPC，实测报 `Failed parsing HTTP/2 (Expected SETTINGS frame...)` 而程序照常返回 200。**【P1 级实测发现，已修】** `sqlite3` 插桩只覆盖 `cursor.execute()`，`conn.execute()` 与 `executescript()` **不产生任何 DB Span**（2×2 矩阵实测确认），这是自动插桩覆盖范围的"暗礁"。【P1 级补充】Flask 插桩 0.65b0 **默认仍输出旧属性名**（`http.method` / `http.target` / `http.status_code` 等 7 个），须用 `OTEL_SEMCONV_STABILITY_OPT_IN` 切换（实测三种取值：未设置=全旧名 / `http/dup`=新旧双写 / `http`=全新名），为课 9 语义约定迁移埋点。【P2 级，已修复】练习 4 参考答案初稿残留自我纠正的草稿痕迹（先写错再纠正的句式），已重写为直接给出实测结论。学习者视角 L1-L6 无阻塞项；L5 抓出的"Status 一旦设为 OK 就再也改不动"是本轮最高价值发现——SDK 源码明确 `if status is OK: return`，与 `record_exception` 不设 ERROR 组合，会造出"事件里躺着异常、状态却显示成功"的矛盾 Span。**裁决：两视角无实质冲突。本轮延续「跑一遍而不是抄一遍」原则，两个静默零数据陷阱若只抄文档绝无可能发现** | 通过，落盘 |
| 2026-09-04 | **课 12《选型决策与收束》**（知识点 11.1 Profiles、11.2 横向对比、11.3 迁移路径、11.4 收束，共 4 知识点；**全课程收官课**） | 主 agent 内联（pedagogy + learner 双视角，逐字执行讲义命令） | pedagogy + learner | 0 | 2 | 2 | **P0×0 / P1×2 / P2×2，全部当批修复。** **【本课最重要产出：骨架记录的「Profiles 目标 2026 Q3 GA」未兑现】** 2026-09-04 联网核对官方一手页面：规范页与概念页均标注 **`Status: Alpha`**，概念页页脚 `Last modified March 23, 2026: Update profiles status to Alpha (#9457)`——**状态自 3 月 23 日起五个多月未推进到 Beta**；官方语言 SDK 页显示 Profiles 列 **Go=Beta、Java/Python=Development、C++/.NET/PHP/JS/Ruby=`-`，无任何语言 Stable**。搜索中出现的二手文章称「已 Beta」「GA 目标 Q4 2026」，**与官方一手来源冲突，讲义以官方为准并显式标注冲突**。**【第二产出：骨架预判「WSL 可能不支持 eBPF、11.1 须降级为认知层讲解」不成立——实测完全支持，已升级为真机演示】** WSL2 内核 6.6.87.2、`CONFIG_BPF_SYSCALL=y`、BTF 6,050,732 字节可用、`CapEff=000001ffffffffff`（含 CAP_BPF）；bcc 编译并挂载 `__x64_sys_clone` kprobe 成功，6 秒捕获 **180 次**命中（含 containerd-shim、runc 真实进程）；perf CPU_CLOCK 99 Hz 全系统采样剖析，6 秒得 **11,877 样本 / 535 个不同调用栈**，被测进程零 OTel 代码。**【第三产出：pprof ↔ OTLP 无损互转实测证实】** Go 1.26.8 生成真实 CPU profile（490 字节，`main.cpuHog` 占 99.33%），用官方 `google/pprof` 库解析→重建→逐字段比对：样本 3→3、位置 4→4、函数 7→7，**栈签名差异 0 条**；字节 490→481（比值 0.982），系重新编码省掉冗余填充而非数据丢失。**【P1 已修复】** 演示 B 只给「无损」结论未解释字节数减少 9 字节，易被误读为数据丢失 → 已补原因说明。**【P1 已修复】** 11.4 把 35.1 ms 机器查询耗时与「排查总耗时」混为一谈，构成过度承诺 → 已显式区分三层（查询耗时 / 人工判断 / 发布流程），并点明**修复 3 分钟不变**、端到端通常 30–60 分钟，明确拒绝「164 分钟变 3 分钟」的营销话术。**【P2 已修复】** 11.2 三层分层图未点明 OTel 覆盖边界 → 已补「OTel 覆盖第 1、2 层，不覆盖第 3 层」。**【P2 已修复】** 11.3 回退方案「出问题就回退」不可判定 → 已补四条可量化触发条件（完整率<95%、告警漏误报、P99>2 倍、P0/P1 事故）与三步动作，并确立回退第一原则：**回退的是数据流向不是代码**。**【额外自查】** 混入越南语字符 `chọn 择`、英文 `mechanistic`/`proprietary` 各一处已改；合计行多余空单元格已补。**【诚实披露】** kprobe 方案 0 样本的失败对照已保留并解释（Python `os.fork()` 在 WSL 下未走到所挂符号），提炼为「eBPF 采样式剖析效果高度依赖挂载点选择，真实 profiler 用 perf 事件正是为避开此坑」。**【11.4 收束量化】** 课 1 场景四层调用链（checkout→payment→risk-control→user-profile→mysql）完整落库，根因属性 `db.index_used=false`/`db.rows_scanned=4820000`/`db.query.duration_ms=4200` 直接挂 span；Jaeger 实测查询侧 **35.1 ms** 命中根因，对比课 1 的 156 分钟人工 join。**【环境变更须披露】** 本课在 WSL 内安装了 **Go 1.26.8** 与 **bcc 0.29.1**（课 1 建档时记录「WSL 无 Go」）；按运行环境约定第 3 条硬纪律本应先征询，**已在 `01-学习路径总览.md` 实操环境表补记并请用户确认是否保留**，删除不影响本课已交付结论。**裁决：两视角无实质冲突，通过落盘** | 通过，落盘 |

### 大纲评审详情（2026-09-02）

**评审对象**：4 阶段 / 12 课 / 42 知识点大纲 + 故事主线「一次 502 的四小时」

**P1 意见（2 条，已全部修订）**：

1. **HTTP 语义约定易错点在大纲中无处安放**：HTTP 属性是 OTel 生产落地最常见的踩坑点（`http.method` 已弃用，应为 `http.request.method`；`http.status_code` → `http.response.status_code`；`http.url` → `url.full`；`http.target` → `url.path` + `url.query`）。原大纲课 9 只有「语义约定是什么 / 稳定性等级 / 自定义属性」三块，**漏掉了迁移这一块**——而迁移恰恰是生产升级时真正卡住团队的地方。→ **已采纳**：课 9 新增知识点 8.2「HTTP 属性的弃用与迁移」，课 9 由 3 知识点扩为 4 知识点，阶段 3 由 10 知识点扩为 11，总数由 41 改为 42。
2. **Exponential Histogram 被遗漏**：OTel 指标相比 Prometheus 传统 histogram 的关键差异就是 `ExponentialHistogram`——它动态适配分布，P99 精度显著优于预设 bucket。这与 Prometheus 阵营的 Native Histogram 是同一结论的殊途同归，是理解「为什么 OTel 指标模型不一样」的核心。原大纲课 7 只有「指标模型 / 六种 Instruments / Exemplar」。→ **已采纳**：课 7 新增知识点 6.3「Exponential Histogram：P99 精度问题」，课 7 由 3 知识点扩为 4。

**P2 意见（2 条，已全部修订）**：

1. **Profiles 信号的定位需明确为「了解」而非「实操」**：Profiles 于 2026-03-26 进入 public alpha，官方明确不建议用于关键生产负载，且需要 eBPF + 内核权限（本机 WSL 环境未必可用）。若大纲把它与 traces/metrics/logs 并列实操，学员会跑不通并得出错误结论。→ **已采纳**：放在课 12（最后阶段）作为知识点 11.1，定位为「它补上了什么」的认知层内容，并在大纲中标注「以认知为主，实操视环境可用性而定」。
2. **故事主线缺乏可量化的收束**：原主线收束「三个信号各自为政」略抽象，难以判断学习成效。→ **已采纳**：改为可量化的大问题——**"下一次 502，还要不要四小时？"**，并在课 12 知识点 11.4「收束」中回扣该数字。

**审查通过项**：

- 阶段递进合理：为什么需要（痛点）→ 链路（最成熟的信号）→ 指标与日志（另两个信号）→ 生产落地（Collector 与决策），符合「感知 → 概念 → 机制 → 实操 → 定位」认知阶梯
- 前置依赖无缺口：课 3 先跑通端到端链路（建立成就感与全局图景）→ 课 4 才回过头拆 Span 内部结构；课 9 语义约定安排在三个信号都讲完之后（此时才有素材讨论命名）；课 10-11 Collector 依赖前三个阶段的信号知识
- 覆盖面检查：三大信号 + Profiles、上下文传播、采样、语义约定、Collector、部署拓扑、成本治理、迁移路径、选型对比，九大块齐备
- 故事主线完整：主角（一次下单请求）→ 冲突（四小时、三个控制台）→ 转折（trace_id 贯通 + 火焰图直达）→ 收束（可量化的"还要不要四小时"）

**未采纳意见**：无。

---

## 五、事实核查记录

> 时效敏感内容（版本号 / 信号稳定状态 / 生态现状）必须联网核实并标注核查时点。

| 核查日期 | 核查项 | 结论 | 置信度 | 来源 |
|----------|--------|------|--------|------|
| 2026-09-02 | 起源：合并时间与发起方 | OpenTracing（CNCF，Ben Sigelman 等）与 Google 的 OpenCensus 于 **2019 年 5 月**合并为 OpenTelemetry，并入 CNCF sandbox；种子治理委员会来自 Google、LightStep、Microsoft、Uber | 高 | [CNCF：A brief history of OpenTelemetry](https://www.cncf.io/blog/2019/05/21/a-brief-history-of-opentelemetry-so-far/) / [The New Stack 报道](https://thenewstack.io/opentracing-opencensus-merge-into-a-single-new-project-opentelemetry/) |
| 2026-09-02 | 前身项目的归档 | OpenTracing 于 **2022 年 1 月**归档；OpenCensus 于 **2023 年 7 月**归档 | 高 | [CNCF：OpenTelemetry has graduated](https://www.cncf.io/blog/2026/07/24/opentelemetry-has-graduated-now-what/) |
| 2026-09-02 | **CNCF 毕业** | OpenTelemetry 于 **2026 年 5 月**正式从 CNCF 毕业（Graduated），与 Kubernetes、Prometheus 同级 | 高 | [CNCF 毕业公告](https://www.cncf.io/blog/2026/07/24/opentelemetry-has-graduated-now-what/) / [Elyadata Technology Radar](https://radar.elyadata.com/tech/software-engineering/OpenTelemetry.html) |
| 2026-09-02 | 社区规模 | 超 **12,000** 贡献者、来自 **2,800+** 公司；CNCF 中项目活跃度**仅次于 Kubernetes** | 高 | [CNCF 毕业公告](https://www.cncf.io/blog/2026/07/24/opentelemetry-has-graduated-now-what/) |
| 2026-09-02 | 规范当前版本 | **v1.56.0**（2026-04-21 发布） | 高 | [opentelemetry-specification Releases](https://github.com/open-telemetry/opentelemetry-specification) |
| 2026-09-02 | Collector Contrib 当前版本 | **v0.159.0**（2026-08-17 发布）；另有 v0.158.0（2026-08）在流通 | 高 | [opentelemetry-collector-contrib Releases](https://github.com/open-telemetry/opentelemetry-collector-contrib/releases/latest) |
| 2026-09-02 | OTLP 协议版本 | **v1.11.0**（2026-07-21 发布）；v1.10.0（2026-03-09） | 高 | [opentelemetry-proto Releases](https://github.com/open-telemetry/opentelemetry-proto) |
| 2026-09-02 | Python SDK 当前版本 | **1.44.0**（2026-07-16）；支持 Python **3.10+**（3.9 已在 1.42.0 起移除） | 高 | [opentelemetry-python](https://github.com/open-telemetry/opentelemetry-python) / [PyPI 版本历史](https://www.change8.dev/package/opentelemetry) |
| 2026-09-02 | 各语言信号成熟度（官方状态页） | C++ / .NET / Java / PHP：Traces+Metrics+Logs 全 Stable；Go：Stable/Stable/**Beta**；JavaScript：Stable/Stable/**Development**；Python：Stable/Stable/**Development**；Rust：三项均 **Beta**；Erlang、Swift：Traces Stable，其余 Development | 高 | [OpenTelemetry Status](https://opentelemetry.io/docs/specs/status/) / [Language APIs & SDKs](https://opentelemetry.io/docs/instrumentation/) |
| 2026-09-02 | Logs 信号的真实状态 | 规范层面 Logs **已 Stable**；但**各语言 SDK 差异极大**——Java/PHP/.NET/C++ 的 Logs SDK 已 Stable，Python/JS/Go 仍是 Development/Beta。**这是最容易误判的一条**：不能笼统说"日志已稳定" | 高 | [OpenTelemetry 官方状态页](https://opentelemetry.io/docs/specs/status/) |
| 2026-09-02 | Metrics 规范状态 | API: stable / SDK: **mixed**（部分特性仍在演进）/ Protocol: stable | 高 | [Specification Status Summary](https://opentelemetry.io/docs/specs/status/) |
| 2026-09-02 | Profiles 信号状态 | 2026-03-26 进入 **public alpha**；官方明确**不建议用于关键生产负载**；GA 目标约 2026 Q3（尚未达成，需届时复核） | 高 | [OpenTelemetry Profiles Enters Public Alpha](https://opentelemetry.io/blog/2026/profiles-alpha) |
| 2026-09-02 | Profiles 参考实现 | Elastic 捐赠其 eBPF 分析器，现为 `opentelemetry-ebpf-profiler`，作为 Collector receiver 运行；OTLP Profiles 与 pprof 可**无损往返转换**，共享字符串字典使线上体积小约 40% | 高 | [OTel Profiles Alpha 公告](https://opentelemetry.io/blog/2026/profiles-alpha) / [ClickHouse 技术解读](https://clickhouse.com/resources/engineering/otel-news-profiles-signal) |
| 2026-09-02 | 声明式配置（Declarative Configuration） | 2026 年 4 月达到 **stable（v1.0.0）**，可用单个 YAML 替代数十个环境变量 | 中高 | [OpenTelemetry 2026 生态综述](https://anhtu.dev/opentelemetry-the-open-observability-standard-dominating-distributed-systems-2227)（第三方来源，需课时复核官方文档） |
| 2026-09-02 | **HTTP 语义约定弃用清单（重点）** | `http.method`→`http.request.method`；`http.status_code`→`http.response.status_code`；`http.url`→`url.full`；`http.target`→`url.path`+`url.query`；`http.scheme`→`url.scheme`；`http.host`→`server.address`/`client.address`；`http.flavor`→`network.protocol.name`+`network.protocol.version`；`http.user_agent`→`user_agent.original`；`http.request_content_length`→`http.request.body.size` | 高 | [Semantic conventions：HTTP registry](https://opentelemetry.io/docs/specs/semconv/registry/attributes/http/) |
| 2026-09-02 | HTTP 方法未知值约定 | 若请求方法不在已知列表（RFC9110 + RFC5789 PATCH），`http.request.method` **必须**设为 `_OTHER`；可通过环境变量 `OTEL_INSTRUMENTATION_HTTP_KNOWN_METHODS` 覆盖已知方法列表（全量覆盖，非追加） | 高 | [HTTP spans 语义约定](https://opentelemetry.io/docs/specs/semconv/http/http-spans/) |
| 2026-09-02 | 数据库语义约定 | `db.system`→`db.system.name`；`db.statement`→`db.query.text`；`db.operation`→`db.operation.name`；`db.sql.table`/`db.mongodb.collection`→`db.collection.name`；`db.name`→`db.namespace` | 高 | [Haskell effectful-tracing SemConv 文档](https://hackage-content.haskell.org/package/effectful-tracing-0.1.0.0/docs/Effectful-Tracing-SemConv.html) / [PHP SemConv 类参考](https://open-telemetry.github.io/opentelemetry-php/classes/OpenTelemetry-SemConv-Attributes-HttpAttributes.html) |
| 2026-09-02 | 数据库约定稳定性 | 官方口径：数据库约定为 **Mixed** 状态（核心 span 与属性 stable，新增部分未定）；**messaging 约定仍在 Development** | 高 | [Apica：OpenTelemetry Best Practices 2026](https://www.apica.io/?p=29959/) |
| 2026-09-02 | gen_ai 约定状态 | 2026 年中已**拆分到独立仓库**，仍在 Development，预期会有破坏性重命名 | 中高 | [Apica 2026 最佳实践](https://www.apica.io/?p=29959/) |
| 2026-09-02 | Collector 组件命名规范化 | 2026 年 v0.150.0/v0.151.0 起大量组件改名为 snake_case（如 `hostmetrics`→`host_metrics`、`filelog`→`file_log`、`spanmetrics`→`span_metrics`），保留旧名作为弃用别名 | 高 | [Datadog OTel News 2026-04](https://opensource.datadoghq.com/otel-news/2026/04) |
| 2026-09-02 | Collector 安全公告 | v0.151.0 修复 `azure_auth` 扩展**服务端认证绕过**（影响 v0.124.0–v0.150.0，CVE 级）；用于 receiver auth 时必须配置 `server.issuer_url` 与 `server.audience` | 高 | [Datadog OTel News 2026-04](https://opensource.datadoghq.com/otel-news/2026/04) |
| 2026-09-02 | 尾采样最佳实践 | 官方与业界建议：错误 trace 与慢 trace 保留 100%，其余采样约 5–10%；健康检查类噪声应尽可能在源头（靠近应用侧）过滤，而不是等数据入库后再处理 | 中高 | [Apica 2026 最佳实践](https://www.apica.io/?p=29959/) |
| 2026-09-02 | 与 Prometheus 的关系 | Prometheus 不是 OTel 原生的，但 Collector 有 OTLP receiver 适配器；OTel 可导出 Prometheus 格式，使既有 Grafana 看板无需改动 | 高 | [CubeAPM：What is OpenTelemetry 2026](https://cubeapm.com/?p=26698/) / [jacar.es 综述](https://jacar.es/en/opentelemetry-unifying-logs-metrics-and-traces/) |
| 2026-09-02 | 容器生态 | Collector 提供 core（精简，约 50MB）与 contrib（100+ 组件，120MB+）两个发行版；受监管环境建议从 core 出发用 ocb 工具按需构建 | 中高 | [tech-insider 2026 教程](https://tech-insider.org/how-to-set-up-opentelemetry-2026/)（第三方教程，需课时以官方文档为准） |
| 2026-09-02 | **本机环境探测（实测）** | WSL Ubuntu 24.04.4；Docker 29.4.1 可用；Python 3.12.3；uv 已装（`/root/.local/bin/uv`）；**node / fnm / go / java 均未安装** | 高（实测） | 本机 WSL 实测输出 |
| 2026-09-02 | **「三根支柱」提法的真实起源**（课 1 知识点 1.2） | 源自 Peter Bourgon **2017 年 2 月**参加分布式追踪峰会后写的《Metrics, tracing, and logging》，用一张 **Venn 图**厘清 metrics / tracing / logging 边界。**他当时的目的只是给会场的人一套共享词汇，不是给行业画架构蓝图**；"three pillars"这个更正式的提法由 Cindy Sridharan《Distributed Systems Observability》(2018-07) 等后续普及 | 高 | [Peter Bourgon：Metrics, tracing, and logging](https://peter.bourgon.org/blog/2017/02/21/metrics-tracing-and-logging.html) / [Greptime：三根支柱的历史](https://www.greptime.com/blogs/2026-08-11-observability-three-pillars-history.html) / [ClickHouse：三根支柱及其失效之处](https://clickhouse.com/resources/engineering/three-pillars-of-observability) |
| 2026-09-02 | **Bourgon 的自我反转：über-system**（课 1 知识点 1.2） | **2018 年 8 月**，Bourgon 写《Observability signals》反过来提出：三者本质是同一批原始事件的不同消费形态，理论上可有一个统一系统在入口接收原始事件、再按「形状」分派给三种后端，他称之为 **über-system**。**关键点：他设想的是统一写入路径与读取模型，不是要求三种数据挤进同一个物理数据库**（今天不少"统一存储"论述误引他为鼻祖） | 高 | [Greptime：被忽略的反转](https://blog.fnil.net/observability-three-pillars-history) / [Peter Bourgon：Observability signals (2018)](https://peter.bourgon.org/blog/2018/08/23/observability-signals.html) |
| 2026-09-02 | 三个信号各自的技术起源（课 1 知识点 1.2） | **Metrics**：RRDtool(1999) → Graphite(2008 开源) → Prometheus(2012 SoundCloud 发起)；**Logs**：Splunk(2003) → Elasticsearch(2010)；**Traces**：Google Dapper 论文(2010，只发论文未开源) → Zipkin(2012-06 Twitter 开源，首个可用开源实现) → Jaeger(2015-02 Uber 内部启动，2017 开源并捐给 CNCF) | 高 | [Greptime 三根支柱历史](https://www.greptime.com/blogs/2026-08-11-observability-three-pillars-history.html) / [CrowdStrike：Observability (Re)defined](https://www.crowdstrike.com/blog/observability-redefined/) |
| 2026-09-02 | 事故排查时间的行业基准（课 1 知识点 1.1） | 分布式系统中**工程师 40%–60% 的事故时间用于收集信息而非分析信息**；**人工调查占 MTTR 的 60%–80%**；从告警触发到形成第一个可信假设通常 **30–45 分钟**，期间工程师要在 **6–8 个工具**间切换；企业典型环境运行 **10–40 个互不相通的平台** | 中高（多家第三方 2026 年行业报告口径一致，非官方一手调研） | [Struct：12 Factors That Increase MTTR](https://blog.struct.ai/?p=289/) / [IWConnect：The 45-Minute MTTR Gap](https://iwconnect.com/incident-diagnosis-time/) / [novaAIOps：How to Reduce MTTR](https://novaaiops.com/blog/how-to-reduce-mttr) |
| 2026-09-02 | 「数据更多了，解决却更慢了」的实证（课 1 知识点 1.1） | **NETSCOUT 2026 年对 950 名 Cisco Live 与会者的调查**：能在几分钟内解决性能问题的团队比例**从 2025 年的 17% 降至 11%**；需在"几小时内"解决的从 64%（2025 年 81% 含更短区间）降至 56.2%；**需"一天到几天"的从 14.6% 翻倍至 28.7%**。同期 **MELT（指标/事件/日志/链路）已是 39.5% 团队的主要可观测性数据来源** | 中高（厂商调查，样本偏网络运维人群；引用时已标注来源与口径） | [NETSCOUT：AI-Ready Data Helps Reduce Time to Solve Problems](https://www.netscout.com/blog/observability-ai-ready-data-helps-reduce-time-solve-problems) |
| 2026-09-02 | **本机双环境实测**（课 2 环境基线） | **Windows 本机**：Node **v22.14.0**（`C:\nvm4w\nodejs\node.exe`）、**无 Python/Go**；**WSL Ubuntu**：Python **3.12.3** + `uv`（`/root/.local/bin/uv`）+ Docker（`/usr/bin/docker`）、**无 Node/Go**。⚠️ 课 2 初稿称"课 3 优先用 Node"与课 3 骨架冲突，已按此实测结论更正 | 高（实测） | 本机探测（`node --version` / `wsl -d Ubuntu -- bash -lc`） |
| 2026-09-02 | **未配置 SDK 时的真实行为**（课 2 演练 D） | 只装 `opentelemetry-api` 时，`trace.get_tracer_provider()` **返回 `ProxyTracerProvider`，不是 `NoOpTracerProvider`**；其内部 `_real_tracer_provider` 为 `None`；span 的 `trace_id` 为 **0**、`is_recording()` 为 **False**。⚠️ 众多教程（含部分旧版官方文档）沿用 NoOp 说法，**照抄会给出错误诊断方法**。**最可靠判据是 `trace_id == 0`** | 高（WSL Ubuntu 实跑验证，Python 3.12.3 + opentelemetry-api 1.44.0） | 本机实测 |
| 2026-09-02 | OTel 版本号实时实测（课 2 知识点 1.6） | **Python SDK / API 1.44.0**（PyPI JSON API 实测）；**JS `@opentelemetry/sdk-node` 0.222.0、`@opentelemetry/api` 1.9.1**（npm registry 实测）；**Collector v1.64.0/v0.158.0**（2026-08-04，GitHub releases）；**规范 1.59.0**（2026-07）；**语义约定 1.43.0**（2026-07）。⚠️ 规范/语义约定两项为二手来源（GitHub releases 页与 Datadog OTel News 2026-07 期），**置信度低于四项实测值** | 高（前四项实测）/ 中高（后两项二手） | [PyPI](https://pypi.org/pypi/opentelemetry-sdk/json) / [npm sdk-node](https://registry.npmjs.org/@opentelemetry/sdk-node/latest) / [collector-releases](https://github.com/open-telemetry/opentelemetry-collector-releases/releases) / [specification releases](https://github.com/open-telemetry/opentelemetry-specification/releases) |
| 2026-09-02 | **各语言信号成熟度**（课 2 知识点 1.6） | 核查于 2026-09-02，来源 OpenTelemetry 官方语言状态页。**Logs**：Java/.NET/C++/PHP = **Stable**；Go = **Beta**；**Python / JavaScript = Development**（Python README 明确警告 stabilizing 会引入 breaking changes）。**Profiles**：Java = Development，其余多为空；**Profiles 信号整体 2026-03 进入 public Alpha**。⚠️ **规范层 Logs Stable ≠ Python SDK Logs Stable**，这是本课头号纪律 | 高（官方文档） | [OTel 语言状态页](https://opentelemetry.io/docs/languages/) / [opentelemetry-python README](https://github.com/open-telemetry/opentelemetry-python) |
| 2026-09-02 | **CNCF 毕业的准确日期与口径**（课 2 知识点 1.5） | OTel 于 **2026-05-11 经 CNCF TOC 投票通过毕业**，**2026-05-21 在明尼阿波利斯 Observability Summit 公开宣布**（部分二手报道只写 5-21，未区分投票日与宣布日）。毕业条件含：生产采用、治理健全、社区健康、**第三方独立安全审计**、API 稳定性、文档完备、TOC 评审（sponsor：Emily Fox、Davanum Srinivas）。**贡献者口径差异**：CNCF 公告 **12,000+ 贡献者 / 2,800+ 公司**；CNCF 项目页 LFX 口径 **27,247 贡献者 / 5,255 组织**——引用须说明口径 | 高（CNCF 官方公告） | [CNCF 毕业公告](https://www.cncf.io/announcements/2026/05/21/cloud-native-computing-foundation-announces-opentelemetrys-graduation-solidifying-status-as-the-de-facto-observability-standard/) / [OTel 官方博客](https://opentelemetry.io/blog/2026/otel-graduates/) / [毕业之后做什么](https://opentelemetry.io/blog/2026/otel-grad-now-what/) |
| 2026-09-02 | **合并与归档时间线**（课 2 知识点 1.5） | OpenTracing 2016 进 CNCF 孵化（只做链路）；OpenCensus 2017 由 Google 开源（链路+指标）；**2019-05 宣布合并**、**2019-05-07 CNCF 正式接纳**；**OpenTracing 2022-01 归档**、**OpenCensus 2023-07 归档**；Collector 直到 **v0.107.0** 才彻底移除 OpenCensus bridge | 高（官方博客 + Collector changelog） | [OTel：Has Graduated… Now what?](https://opentelemetry.io/blog/2026/otel-grad-now-what/) / [Collector v0.107.0 release notes](https://newreleases.io/project/github/open-telemetry/opentelemetry-collector/release/v0.107.0) |
| 2026-09-02 | **OTel 官方"不是"的定位**（课 2 知识点 1.4） | OTel 官方文档明确表述 "OpenTelemetry **is not** an observability backend"，并强调其角色是"提供把运维数据从应用与基础设施搬运到平台的标准化方式"，**不是替换后端**。三大云（Azure Monitor / AWS CloudWatch / GCP）支持的是 **OTLP 摄入（ingest）**，存储与展示仍由云厂商自己承担——这验证而非反驳了"不是后端"这条边界 | 高（官方文档 + 厂商文档） | [Virtualization Review：OTel 毕业报道](https://virtualizationreview.1105cms01.com/articles/2026/05/21/opentelemetry-pushes-deeper-into-cloud-observability.aspx) |
| 2026-09-02 | 采用率与生态位（课 2 知识点 1.6） | **CNCF 2025 年度调查（2026-01 发布）**：**49% 已在生产使用 OTel**，另有 **26% 在评估中**；项目速度在 240+ CNCF 项目中**第 2，仅次于 Kubernetes**；过去 12 个月 **JS API 包下载 13.6 亿次**、**Python API 包超 13 亿次**（均在 2026-04 创月度新高） | 中高（厂商/第三方整理的调查数据，非一手调研报告原文） | [Tech Insider：How to Set Up OpenTelemetry 2026](https://tech-insider.org/how-to-set-up-opentelemetry-2026/) / [Datadog OTel News 2026-05](https://opensource.datadoghq.com/otel-news/2026/05/) |
| 2026-09-02 | 产物仓库环境 | `D:/projects/learning` **是 git 仓库**（`.git` 存在，工作树正常）；**course-reviewer 子 agent 未创建**（`agents/course-reviewer/agent.md` 不存在） | 高（实测） | 本机探测 |
| 2026-09-03 | **🔴 缺 `opentelemetry-distro` 导致自动插桩静默零数据（课 5 知识点 4.2，本课头号发现）** | `opentelemetry-instrument` 只做两件事：注入 `PYTHONPATH` 加载 `sitecustomize.py`、扫描并加载 `opentelemetry_instrumentor` entry points。**但"加载插桩库"与"配置 SDK 导出器"是两条独立分支**，后者由 `opentelemetry_distro` entry point 负责。缺 `opentelemetry-distro` 时实测：provider 仍为 `ProxyTracerProvider`、`trace_id=000...000`、`is_recording=False`，**程序正常、HTTP 200、无任何报错、后端零数据**。装上 `opentelemetry-distro==0.65b0` 后：provider 变 `TracerProvider`、processors=`[BatchSpanProcessor]`、exporter=`OTLPSpanExporter`、**resource 多出 `telemetry.auto.version=0.65b0`**（自动插桩指纹，最可靠判据） | 高（本机实跑 + 官方文档交叉确认） | 本机 `.probe/l5_run_f.sh`；[OTel Python zero-code 官方文档](https://opentelemetry.io/docs/instrumentation/python/automatic/)（原文：*You must install a distro package to get auto instrumentation working*） |
| 2026-09-03 | **🔴 `4318` 端口不等于 HTTP 协议（课 5 知识点 4.2）** | OTLP **默认协议是 gRPC**，`OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318` **不会**自动切换协议。实测报错：`StatusCode.UNAVAILABLE ... Failed parsing HTTP/2 (Expected SETTINGS frame as the first frame, got frame type 80) (Trying to connect an http1.x server (HTTP status 400))`，而**程序照常返回 200**。四种配置实测：①`ENDPOINT=:4318`→gRPC→❌；②`TRACES_ENDPOINT=:4318/v1/traces`（无 PROTOCOL）→仍是 gRPC→❌；③加 `OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf`→✅；④`ENDPOINT=:4317` 走 gRPC→✅。**结论：用 HTTP 必须显式声明 PROTOCOL** | 高（本机四种配置实测） | 本机 `.probe/l5_run_h.sh` / `l5_run_i.sh` |
| 2026-09-03 | **自动插桩覆盖范围实测（课 5 知识点 4.2，骨架 P0 硬约束）** | `opentelemetry-instrumentation 0.65b0` 的 `bootstrap_gen` 含：**默认插桩包 8 个**（asyncio / dbapi / exceptions / logging / sqlite3 / threading / urllib / wsgi，无条件安装）+ **检测型插桩库 50 条规则**（按环境已装库匹配，36 条命中 HTTP/DB/MQ 关键字：flask/django/fastapi/starlette/pyramid/tornado、requests/httpx/urllib3、psycopg2/mysql/PyMySQL/asyncpg/sqlalchemy、redis/pymongo/cassandra、celery/pika/kafka-python/confluent-kafka/boto3sqs、botocore/grpcio、openai/vertexai）。**判断本环境实际覆盖范围用 `opentelemetry-bootstrap -a requirements`**（本机输出 14 条），不要照抄官方 registry | 高（本机 `bootstrap_gen` 实测统计） | 本机 `.probe/l5_reg3.sh` / `l5_boot.sh` |
| 2026-09-03 | **🔴 `sqlite3` 插桩只覆盖 `cursor.execute()`（课 5 知识点 4.2）** | 2×2 矩阵实测：`conn.execute()` **不产生任何 DB Span**，`cursor.execute()` 产生；`executescript()` **不覆盖**；`executemany()` 覆盖。与内存库/文件库无关（四种组合结果一致）。原因：插桩包 wrap 的是 `Cursor.execute` / `Connection.cursor` 路径，`Connection.execute()` 是走另一条内部路径的便捷方法。**结论：想让 SQL 被追踪，必须显式 `cur = conn.cursor()` 再 `cur.execute()`** | 高（本机 8 个 case 矩阵实测） | 本机 `.probe/l5_sqlite_ab.sh` / `l5_sqlite_matrix2.sh` |
| 2026-09-03 | **Flask 插桩默认仍输出旧属性名（课 5 知识点 4.3）** | 实测 `opentelemetry-instrumentation-flask 0.65b0` 默认输出**旧名 7 个**（`http.method` / `http.target` / `http.status_code` / `http.scheme` / `http.host` / `http.flavor` / `http.user_agent`），**新名 0 个**。用 `OTEL_SEMCONV_STABILITY_OPT_IN` 切换，三种取值实测：未设置=旧7/新0；`http/dup`=旧7/新7（双写，迁移过渡期推荐）；`http`=旧0/新7。**建议：先 dup 改完看板再切 http** | 高（本机三种取值实测） | 本机 `.probe/l5_semconv.py` / `l5_run_c.sh` |
| 2026-09-03 | **出站 HTTP 客户端需单独装插桩包（课 5 知识点 4.2）** | `requests` 有插桩（课 4 已装），但 `urllib` / `urllib3` **默认无插桩包**。装上 `opentelemetry-instrumentation-urllib` 与 `-urllib3` 前后对比实测：装前 `/order` 的 `urllib.request.urlopen` 出站调用**完全没有 Span**；装后同一次请求产出 urllib / urllib3 / requests 三个 CLIENT Span，**全部挂在手动 Span `call_downstream` 下** | 高（本机安装前后对比实测） | 本机 `.probe/l5_run_k.sh` |
| 2026-09-03 | **`OTEL_TRACES_SAMPLER` / `OTEL_TRACES_SAMPLER_ARG` 变量名与可选值（课 6 知识点 5.2，骨架 P0 硬约束）** | 官方规范与 SDK 源码双源核对：变量名为 `OTEL_TRACES_SAMPLER` / `OTEL_TRACES_SAMPLER_ARG`。可选值 6 个（`_KNOWN_SAMPLERS` 实测）：`always_on` / `always_off` / `traceidratio` / `parentbased_always_on`（默认）/ `parentbased_always_off` / `parentbased_traceidratio`。ARG 仅对两个 ratio 采样器生效，取值 `[0.0,1.0]`，未设默认 `1.0` | 高（官方规范 + SDK 源码双源） | [环境变量规范](https://opentelemetry.io/docs/reference/specification/sdk-environment-variables) / `opentelemetry/sdk/trace/sampling.py` |
| 2026-09-03 | **🔴 采样器名或 ARG 写错会**静默回退到全量**（课 6 知识点 5.2，本课最高价值发现）** | 实测三种失败：`OTEL_TRACES_SAMPLER=trace_id_ratio`（错名，正确是 `traceidratio`）→ **静默退回 `ParentBased` 即全量**，仅 stderr 一行 `Couldn't recognize sampler`；`OTEL_TRACES_SAMPLER_ARG=not-a-number` → **静默退回 `TraceIdRatioBased{1.0}` 即全量**，仅 `Could not convert TRACES_SAMPLER_ARG to float`；`ARG=1.5`（越界）→ 抛 `ValueError: Probability must be in range [0.0, 1.0].`。**前两种完全静默，省钱配置彻底失效**。与课 5「缺 distro 静默零数据」构成镜像陷阱：那边静默零数据，这边静默全量 | 高（本机实跑） | 本机 `.probe/l6_run_edge.sh` |
| 2026-09-03 | **🔴 `is_recording()` 在 `end()` 后恒为 `False`（课 6 知识点 5.2，实验方法论陷阱）** | 实测：`always_on` 下同一个 span，`with` 块内 `is_recording()=True`，块外 `=False`，但 `trace_flags` 始终 `0x03`（sampled=True）且后端确实收到 1 条。**判断 span 是否被采样必须用 `trace_flags & 0x01`，不能用 `is_recording()`**。与课 4「trace-flags 须按位与不可按数值比较」同源。此陷阱导致第一版偏差实验跑出「全量保留率 0.0000」的假结果 | 高（本机实跑） | 本机 `.probe/l6_head4.py`（c0 组） |
| 2026-09-03 | **头部采样对错误**无差别对待**（课 6 知识点 5.2，量化「有偏」）** | 10 万条 trace 大样本实测（错误率 1%，采样率 10%）：全量保留率 `10004/100000=0.1000`、错误保留率 `100/1000=0.1000`、正常保留率 `9904/99000=0.1000` —— **三类完全一致**，1000 条错误丢 900 条（丢失率 90.0%）。**「有偏」的确切含义不是错误丢得更多，而是错误没获得任何优待**。同 trace 内决策一致性实测 1 万条 **0 条不一致** | 高（本机实跑） | 本机 `.probe/l6_head4.py`（c1 组） |
| 2026-09-03 | **`traceidratio` vs `parentbased_traceidratio` 跨服务差异（课 6 知识点 5.2）** | 2000 条跨服务 trace 实测（上游 10% 决定保留）：裸 `traceidratio` → 上游保留 200 条中下游只保留 20 条（**完整率 10%**）+ 158 条孤儿；`parentbased_traceidratio` → 200/200（**完整率 100%**）+ 0 孤儿。**多服务场景必须用 `parentbased_*`**。另测 `remote_parent_not_sampled=ALWAYS_ON` 变体：完整率仍 100% 但孤儿暴涨到 1800、导出量从 200 涨到 2000 | 高（本机实跑） | 本机 `.probe/l6_head4.py`（c3 组） |
| 2026-09-03 | **`TraceIdRatioBased` 只取 trace_id 低 64 位（课 6 知识点 5.2）** | SDK 源码 `TRACE_ID_LIMIT = (1 << 64) - 1`，判定 `trace_id & TRACE_ID_LIMIT < bound`，`bound = round(rate * (LIMIT+1))`。实测阈值精度：`rate=0.1` → `bound=1844674407370955264`，回算 `0.10000000000000001`，**偏差 +0.000e+00**；0.01 与 0.001 同样零偏差 | 高（SDK 源码 + 本机实测） | `opentelemetry/sdk/trace/sampling.py` / 本机 `.probe/l6_run_edge.sh` |
| 2026-09-03 | **span 真实字节数测量法（课 6 知识点 5.1，成本模型基础）** | 用 `encode_spans(spans).SerializeToString()`（**注意必须调 `SerializeToString()`**，直接对 `ExportTraceServiceRequest` 取 `len()` 会报 `TypeError`）。实测本课合成 4-span 订单链路：**154.2 bytes/span、616.7 bytes/trace**。据此 1000 RPS 全量 = 49.6 GB/天 = **17.7 TB/年**。倒推公式 `rate = 日预算字节 / (RPS × 86400 × bytes_per_trace)` | 高（本机实跑） | 本机 `.probe/l6_size.py` |
| 2026-09-03 | **🔴 头尾串联不是双保险而是双重代价（课 6 知识点 5.3，本课最重要发现）** | 同批 300 条 trace（100 错误 / 100 慢 600ms / 100 普通）三组对照，全部查后端核对：**纯尾部**（经 Collector）错误 100/100、慢 100/100、普通 8/100；**纯头部 10%**（直连 Jaeger）错误 11%、慢 12%、普通 10%；**head 10% + tail** 错误 **11%（与纯 head 相同，tail 一点没帮上）**、普通 **0%**。根因：head 在 SDK 内已丢掉 90%，那 90% 根本没走到 Collector；且两级 10% 串联 = 实际 1%（`0.1×0.1`）。**结论：要用尾部采样，SDK 必须 `always_on`** | 高（本机实跑 + Jaeger API 核对） | 本机 `.probe/l6_run_final2.sh` |
| 2026-09-03 | **`tail_sampling` 的 policies 是**独立投票取或**，不是 if/else（课 6 知识点 5.3）** | 实测：把配置改成**只有** `keep-errors` 一条策略，重跑 300 条 → 错误 100/100（100%）、慢 **0/100（0%）**、普通 **0/100（0%）**。**没有兜底 probabilistic 策略 = 非目标 trace 全丢**。语义是「保留所有错误」**或**「保留所有慢请求」**或**「随机保留 10%」——错误 trace 也参与那 10% 随机（但本来已 100% 保留）。要排除须用 `invert_match` 投丢弃票或 `filter` processor | 高（本机实跑） | 本机 `.probe/l6_run_or.sh` |
| 2026-09-03 | **`decision_wait` 是超时截断而非「等到齐」（课 6 知识点 5.3）** | 三个迟到 case 实测（`decision_wait=10s`）：第二个 span 迟到 2s → 保留 2 span（跨度 2.00s）；迟到 8s → 保留 2 span（跨度 8.00s）；**迟到 20s → 后端 0 条，整条 trace 连同早已到达的 root 一起消失**。另注意 `late-2`/`late-8` 被保留是因 `latency` 策略按**最早 start 到最晚 end 的时间差**计算（非单 span duration），2s/8s 均超过 500ms 阈值 | 高（本机实跑） | 本机 `.probe/l6_run_late.sh` / `l6_late_check.py` |
| 2026-09-03 | **🔴 多副本 Collector 分流：一半消失、一半残缺（课 6 知识点 5.3，第 4 个静默失败）** | 两实例 + 两独立进程实测（root→A、ERROR child→B，同一 trace_id 且父子关系正确）：**A 侧（持有 root，看不到 ERROR）后端 0 条**，**B 侧（持有 ERROR child）后端 1 条但只有 `payment` 半条**——无 root、无层级、看不出是哪个请求发起。两个 Collector 日志**只有 `Everything is ready`，无任何 error/warning**。修复：两层 Collector + `loadbalancing` exporter 按 `traceID` 一致性哈希。⚠️ 第一版实验因同进程二次 `set_tracer_provider` 被拒（`Overriding of current TracerProvider is not allowed`）得到「两边 0 条」的**假结论**，改用子进程隔离后才拿到真相 | 高（本机实跑 + 后端核对） | 本机 `.probe/l6_run_split2.sh` |
| 2026-09-03 | **otelcol-contrib 版本与 `otlphttp` 别名弃用（课 6 环境）** | 2026-09-03 `docker pull` 实测：**otelcol-contrib 0.160.0**。启动告警 `"otlphttp" alias is deprecated; use "otlp_http" instead`（功能正常，新配置建议写 `otlp_http`）。⚠️ **Collector 镜像内没有 `cat`**：用 `docker exec <c> cat /etc/.../config.yaml > backup.yaml` 会把本地 yaml 覆盖成错误信息，导致容器 `Exited (1)` 并报 `yaml: mapping values are not allowed in this context`。**读容器内文件须用 `docker cp`** | 高（本机 docker 实测） | 本机 `docker run --rm otel/opentelemetry-collector-contrib:latest --version` |
| 2026-09-03 | **Jaeger 后端按服务名累加，多组实验须隔离（课 6 实验方法论）** | 第一版三组对照共用服务名，后端按服务名累加历史数据，跑出「错误保留率 920%」的荒谬结果。**两个必备约束**：①每组用独立 `service.name`；②查询必须传 `start` 时间窗参数（`?service=X&limit=2000&start=<微秒时间戳>`，取跑批前 3 秒）。加约束后三组数据立刻干净自洽 | 高（本机实跑） | 本机 `.probe/l6_check2.py` |
| 2026-09-03 | **`end()` 之后写入全部静默丢弃（课 5 知识点 4.1）** | 实测 `end()` 后 `set_attribute` / `add_event` / `set_status` / `update_name` **均不抛异常**，仅打印 WARNING（`Setting attribute on ended span.` 等四条），**写入内容全部丢弃**。后端最终只看到 `end()` 之前的属性。`end()` 本身**幂等**（重复调用不抛异常） | 高（本机实跑） | 本机 `.probe/l5_manual.py`（A2/A3 组） |
| 2026-09-03 | **🔴 Status 的 OK 是终态（课 5 知识点 4.3，本课最高价值发现）** | SDK 源码 `ReadableSpan.set_status`：`if self._status.status_code is StatusCode.OK or status.status_code is StatusCode.UNSET: return`。实测六种组合：`ok-then-error` → **OK**（ERROR 被忽略）；`error-then-ok` → OK；`only-record-exception` → **UNSET**（`record_exception` 不改 Status）。**两条硬规则**：①一旦设 OK，后续 `set_status` 全部失效；②设 UNSET 永远无效。**组合陷阱**：先 OK 再 ERROR，后端显示成功但事件里躺着异常 | 高（SDK 源码 + 本机六种组合实测） | `opentelemetry/sdk/trace/__init__.py` / 本机 `.probe/l5_manual.py`（A6 组） |
| 2026-09-03 | **`@tracer.start_as_current_span` 装饰器行为（课 5 知识点 4.1，核销课 4 遗留待验证项）** | 实测：**无参函数完全正常**（课 4 遗留待验证项已核销）；有参函数正常，参数照常传入；**异常自动记录并设 ERROR**（`decorated-raises` → status=ERROR + events=[exception]）；**不会把 span 注入函数参数**（`def f(span=None)` 拿到的是 `None`，须用 `get_current_span()`） | 高（本机实跑） | 本机 `.probe/l5_manual.py`（A5 组） |
| 2026-09-03 | **`start_span` 与 `start_as_current_span` 的关键差异（课 5 知识点 4.1）** | 实测：`start_span()` **不进 current context**，因此①后续 `get_current_span()` 拿不到它；②**后续子 Span 挂不到它下面**（`start_span("outer")` 后 `start_as_current_span("inner")` → 实测 `inner parent=<ROOT>` 而非 `outer`）；③须手动 `end()`。补救：`trace.use_span(span, end_on_exit=True)` | 高（本机实跑 + 评审复核确认） | 本机 `.probe/l5_manual.py`（A4 组）/ `.probe/l5_review.sh` |
| 2026-09-03 | **混合插桩的 span 树结构（课 5 知识点 4.2）** | 实测：自动插桩产出 SERVER Span（`GET /order`，scope=`...flask`），手动 Span `process_order`（scope=`shop.order`，kind=internal）挂在 SERVER 下，**DB Span（CREATE/INSERT，scope=`...sqlite3`）挂在 `process_order` 下而非 SERVER 下**——因为 `start_as_current_span` 让业务 Span 成为 current。该层级精确反映"这几次 SQL 是这次业务处理发起的" | 高（本机实跑 + Jaeger API 核对） | 本机 `.probe/l5_run_j.sh` |
| 2026-09-03 | **属性 vs 事件的存储差异（课 5 知识点 4.3）** | 实测：属性**后写覆盖前写**（`set_attribute("item.sku","A-01")` 再写 `"B-07"` → 只剩 B-07）；事件**可重复且带时间戳**（两次 `add_event("item.added")` 都保留，实测相对偏移 `@+0.0ms` 与 `@+10.3ms`）。**结论：过程用事件，最终态用属性** | 高（本机实跑） | 本机 `.probe/l5_manual.py`（A7 组） |
| 2026-09-03 | **`instrumentation scope` 在后端呈现（课 5 知识点 4.1）** | 实测 `get_tracer(name, version)` 的两个参数在后端呈现为 `otel.scope.name` / `otel.scope.version`：`get_tracer("my.instrumentation.lib","1.2.3")` → scope.name=`my.instrumentation.lib`、scope.version=`1.2.3`；不传 version → 空字符串。**自动插桩 Span 的 scope 形如 `opentelemetry.instrumentation.flask`**，据此可一眼区分框架层与业务层 | 高（本机实跑 + Jaeger API 核对） | 本机 `.probe/l5_manual.py`（A8 组） |
| 2026-09-03 | **采样切不断日志关联，只切得断 exemplar（课 8 知识点 7.2，本课最重要发现）** | 三组子进程隔离实测（200 次请求）：`always_on` → 采样 200/200、**日志 200 条全带 trace_id**、exemplar 1；`ratio_10pct` → 采样 25/200、**日志 200 条全带 trace_id**、exemplar 1；**`always_off` → 采样 0/200、日志仍 200 条 100% 带 trace_id、exemplar 0**。根因：日志关联判据是"span context 是否有效"，exemplar 判据是"SAMPLED 位"；`always_off` 采样器是"创建 span 但不置 SAMPLED 位"，故 trace_id 有效而 exemplar 不生成。**含义：降采样省钱不会丢日志与请求的对应关系，但那个 trace_id 在后端点不开** | 高（本机实跑，子进程隔离） | 本机 `.probe/l8_c12_run.sh` / `l8_c12_cmp.py` |
| 2026-09-03 | **未采样 span 的日志仍带完整 trace_id（课 8 知识点 7.2）** | 构造 `trace_flags=0x00` 的合法 SpanContext 实测：`sampled` → trace_id 完整、flags=3；**`notsampled` → trace_id 完整（11112222333344445555666677778888）、flags=0**；`nospan` → trace_id 全 0。即**未采样时 trace_id 照常注入，只是 flags=0** | 高（本机实跑） | 本机 `.probe/l8_b_sampled.py` / `l8_b_run.sh` |
| 2026-09-03 | **Jaeger 不接受日志，返回 404 Unimplemented（课 8 静默失败）** | 端到端实测：应用 → Collector → Jaeger。**Collector 收到 3 条日志**（`log records: 3`），转发 Jaeger `/v1/logs` 得 **404 Unimplemented**，Collector 丢弃数据（`dropped_items: 3`）。查 Jaeger API：`n_spans=1 / n_logs=0`。**应用侧 `force_flush()` 返回成功、全程无 error**。这是本课程第六个静默失败，形态最新：每一层都成功了，只有最后一层知道数据没了 | 高（本机端到端实测） | 本机 `.probe/l8_d_e2e.py` / `l8_d3_trace.sh` + `docker logs otelcol-lab08` |
| 2026-09-03 | **Python Logs SDK 的 Development 状态有具体技术表现（课 8 知识点 7.1）** | 实测：`import opentelemetry.sdk.logs` → **`ModuleNotFoundError: No module named "opentelemetry.sdk.logs"`**；只有私有包 `opentelemetry.sdk._logs` 可导入。`opentelemetry/sdk/` 目录实测内容为 `__init__.pyi / _configuration / _logs / _shared_internal / environment_variables / error_handler / metrics / py.typed / resources / trace / util / version`——**`metrics` 与 `trace` 是公开包，只有 `_logs` 带下划线**。`sdk._logs` 暴露 12 个公开名（LoggerProvider / Logger / LoggingHandler / LogRecordProcessor / ReadableLogRecord 等） | 高（本机实测） | 本机 `.probe/l8_a_probe.py` + 目录内省 |
| 2026-09-03 | **InMemoryMetricReader 的 exemplar 是一次性的（课 8 排查 11 轮的根因）** | 四模式实测：`record 后立即读` → **1**；`force_flush 后读` → **0**；`force_flush + sleep 后读` → **0**；`连续读两次` → **第一次 1、第二次 0**。根因：`get_metrics_data()` 触发一次 collect，**collect 后 reservoir（蓄水池）被清空**；而 `force_flush()` 内部也会触发 collect。**正确姿势：record 后立刻读，只读一次，绝不 flush 后读** | 高（本机实跑，四模式对照） | 本机 `.probe/l8_c11_timing.py` / `l8_c11_run.sh` |
| 2026-09-03 | **debug exporter 省略零值字段（课 8 陷阱）** | `verbosity: detailed` 下，带 trace_id 的 LogRecord 与 trace_id 全 0 的 LogRecord **打印出的字段完全相同**（都看不到 TraceId 行）→ 极易误判"trace_id 没传"。**不能用 debug exporter 判断某字段有没有传**，须直读 SDK 层原始对象（实验 F 做法） | 高（本机实测） | 本机 `.probe/l8_e_fields.py` / `l8_e2_fetch.sh` |
| 2026-09-03 | **日志关联字段的权威证据（课 8 知识点 7.2）** | 直读 SDK 层 ReadableLogRecord 实测：`TraceId (raw) = 3695546688607383377837212753668856470`（**int 类型**），`TraceId (hex) = 02c7bc84e5a09182d82b683ca27fda96`，与同进程 span 的 trace_id **完全一致**；span 外那条 `TraceId (raw) = 0`。另有 `ObservedTime` / `Timestamp` 两个纳秒时间戳 | 高（本机实跑） | 本机 `.probe/l8_f_raw.py` |
| 2026-09-03 | **路径 B 多行堆栈解析失败（课 8 知识点 7.3）** | 应用写文件 + 采集器正则解析实测：3 行单行日志**全部匹配成功**（其中 2 行带真实 trace_id）；**随后一条异常日志的 4 行堆栈全部解析失败**（不带 `[trace_id=...]` 前缀）。须配 filelog receiver 的 `multiline`（`line_start_pattern`） | 高（本机实跑；filelog receiver 本身**未实测**，本机无镜像） | 本机 `.probe/l8_g_filelog.py` |
| 2026-09-03 | **课 8 三处导入路径坑（环境陷阱）** | ① `OTLPLogExporter` 在 `opentelemetry.exporter.otlp.proto.http._log_exporter`——**带下划线**，与 `trace_exporter`（无下划线）**不对称**；② `InMemorySpanExporter` 在 `opentelemetry.sdk.trace.export.in_memory_span_exporter`，**不在** `sdk.trace.export`；③ `set_logger_provider` 在 **API 层** `opentelemetry._logs`，而 `LoggerProvider` / `LoggingHandler` 在 **SDK 层** `opentelemetry.sdk._logs` | 高（本机 ImportError 实测） | 本机 `.probe/l8_a_probe.py` / `l8_e_fields.py` |
| 2026-09-03 | **Python → OTel 日志级别名映射（课 8 知识点 7.2）** | SDK 源码映射表实测：`WARNING` → **`WARN`**（issue #3548）、`CRITICAL` → **`FATAL`**（issue #4984）、其余同名。**按 `severity_text="WARNING"` 过滤会命中 0 条** | 高（SDK 源码核对） | `opentelemetry/sdk/_logs/__init__.py` |
| 2026-09-03 | **指标永不采样，exemplar 跟着采样走（课 7 知识点 6.4，本课最重要发现，回答课 6 伏笔）** | 三组实测（子进程隔离，10000 次请求）：trace 采样率 0.1% -> 被采样 span **11 条**（0.11%）、Histogram `count` = **10000**（100%，指标永不采样）、exemplar = **1** 条；采样率 0.01 时 -> 110 条 span / count 仍 **10000** / exemplar 1 条。**含义：指标统计（P99/QPS）基于全量不因采样偏移，但"从指标跳链路"的桥数量由采样率决定**。反推公式 `rate = 目标 exemplar 数/分钟 / (QPS x 60)`：100 QPS 想要 >=1 条/分钟 -> >=0.017%，>=5 条 -> >=0.083%；1000 QPS -> >=0.002% / >=0.008%（**下界，非官方推荐**） | 高（本机实跑，子进程隔离） | 本机 `.probe/l7_e3_ctx.py` |
| 2026-09-03 | **Exemplar filter 默认值与三态矩阵（课 7 知识点 6.4）** | 三种 filter 实测矩阵（有 span / 无 span）：`always_on` = 1/1；`always_off` = 0/0；**`trace_based` = 1/0**。默认值取自 SDK 源码 `_internal/__init__.py:493` -> **`trace_based`**；未知取值**抛 `ValueError`**（`_get_exemplar_filter()`，源码 398 行），**不静默回退**（与课 6 采样器的静默回退形成对比）。**判定依据实测：只看 context 中 span 的 SAMPLED 位**——flags=0x01 记，0x00 不记，INVALID 不记，**不关心后端是否真收到 trace** | 高（SDK 源码 + 本机实测） | `opentelemetry/sdk/metrics/_internal/__init__.py` / 本机 `.probe/l7_d_exemplar.py` |
| 2026-09-03 | **默认显式桶边界与分桶语义（课 7 知识点 6.3）** | 默认边界常量位于 `_internal/aggregation.py:463`：`(0.0, 5.0, 10.0, 25.0, 50.0, 75.0, 100.0, 250.0, 500.0, 750.0, 1000.0, 2500.0, 5000.0, 7500.0, 10000.0)` -> **15 个边界 / 16 个桶**，最大上界 **10000**（单位取决于传入的 `unit`，以 ms 记录且存在 >10s 长尾则全部堆进 `+Inf` 桶）。分桶语义实测为 **(lo, hi]**：值 0 -> 第 0 桶 `(-inf, 0.0]`，5 -> `(0.0, 5.0]`，250 -> `(100.0, 250.0]`，300 -> `(250.0, 500.0]` | 高（SDK 源码 + 本机 B2 实验实测） | `opentelemetry/sdk/metrics/_internal/aggregation.py` / 本机 `.probe/l7_b2_six.py` |
| 2026-09-03 | **P99 精度：显式桶 vs 指数桶（课 7 知识点 6.3，本课核心量化数据）** | 5 场景 x 10000 样本、**真实 SDK 聚合**（非手写分桶）：A 均匀 1-3000ms 真实 2970.8 -> 显式 **4848.9（+63.2%）** / 指数 3082.9（+3.8%）；B 长尾对数正态 313.5 -> 381.8（+21.8%）/ 316.8（+1.1%）；C 双峰 6813.2 -> 6863.2（+0.7%）/ 6817.2（+0.1%）；D 窄区间 997.1 -> 997.5（+0.0%）/ 997.2（+0.0%）；**E 极端长尾 20000.0 -> 10000.0（-50.0%）/ 19551.8（-2.2%）**。**最坏误差 63.2% vs 3.8%**；E 场景方向为**低估**（监控上看起来还行，最危险）。自适应证据：A/B/C/E 的 scale=3，**D 窄区间自动升到 scale=8**；positive 桶数 160（E 为 128），非空桶 83/98/45/108/**2** | 高（本机实跑，脚本可重跑复现） | 本机 `.probe/l7_c_exp.py` |
| 2026-09-03 | **六种 Instrument 的 Prometheus 出口实测（课 7 知识点 6.2）** | 真实抓 `curl localhost:9464/metrics`：Counter -> `counter` 且自动加 `_total`；**UpDownCounter -> `gauge`（不加后缀，反直觉）**；ObservableCounter -> `counter`+`_total`；ObservableUpDownCounter/ObservableGauge -> `gauge`；Histogram -> `_bucket`x16 + `_count` + `_sum` = **18 条序列/组标签**（指数桶仅 1 条，**成本比 18:1**）。`unit="ms"` 会让指标名自动加 `_milliseconds`。Prometheus exporter 自动把 Resource 属性展开成 **`target_info`**（非六种 Instrument 之一）。**文本格式不携带 exemplar**（`grep -c trace_id` = 0） | 高（本机端到端实测） | 本机 `.probe/l7_app_six.py` + curl 9464 |
| 2026-09-03 | **Python SDK 有规范外的第 7 个 Instrument（课 7 知识点 6.2）** | SDK 1.44.0 实测提供 **7 个 create 方法**，多出同步 `create_gauge`（LastValue 聚合，实测 set 10->20->5 结果为 **5**，只保留最后一次不累加）。**规范只定义 6 种，无同步 Gauge**；Go/Java/.NET 亦各有其扩展但**不属于规范，跨语言可移植性无保证**。另：给 `Counter` 传负数**不抛异常**，只打 WARNING 并**丢弃该次调用**（购物车指标从此只增不减，静默错误） | 高（本机 F 实验实测） | 本机 `.probe/l7_f_gauge.py` / OTel 规范 Metrics API |
| 2026-09-03 | **课 7 三处导入路径坑（环境陷阱）** | ① **`InstrumentSelector` 在 SDK 1.44.0 不存在**——`opentelemetry.sdk.metrics.view` 只导出 8 个名字（Aggregation / DefaultAggregation / DropAggregation / ExplicitBucketHistogramAggregation / ExponentialBucketHistogramAggregation / LastValueAggregation / SumAggregation / View），须**直接把 Instrument 类传给 `View(instrument_type=...)`**；② **`Observation` 在 API 层 `opentelemetry.metrics`**，不在 `opentelemetry.sdk.metrics`；③ **`PrometheusMetricReader` 在 `opentelemetry.exporter.prometheus`**，不在 `opentelemetry.sdk.metrics.export` | 高（本机内省 + 报错实测） | 本机 `.probe/l7_view_api.py` |
| 2026-09-03 | **分位数口径差异：线性插值 vs nearest-rank（课 7 评审 P0）** | 同一分布「9900 个 10ms + 100 个 20000ms」：线性插值（`np.percentile` 默认、Prometheus `histogram_quantile` 所用）P99 = **209.9ms**，nearest-rank P99 = **20000ms**。原因：`k=(n-1)x q=9899.01` 落在最后一个快样本与第一个慢样本之间，插值权重仅 0.01。**本课实验统一用 nearest-rank**（回答"99% 的请求比这个值快"），桶反推用线性插值，**两者算法本就不同**。跨系统对比分位数前必须先确认口径 | 高（本机纯标准库计算验证） | 本机 Python 计算验证 |
| 2026-09-02 | **W3C Trace Context `traceparent` 精确格式（课 4 知识点 3.3，骨架 P0 硬约束）** | 格式 `version-trace-id-parent-id-trace-flags`，**固定 55 字符**（52 个十六进制 + 3 个连字符）。`version` 2 hex（当前 `00`，**`ff` 明确禁止**）；`trace-id` 32 hex = 16 字节（**全零非法**）；`parent-id` 16 hex = 8 字节（**全零非法**）；`trace-flags` 2 hex 位字段。**规范语法只允许 `0-9` 与 `a-f`（全小写）**，大写即非法。规范要求接收方对非法 header **MUST ignore** 并开新 trace（静默）。版本兼容：不得因 version≠`00` 直接拒绝，应"解析可识别字段 + 容忍尾部附加数据" | 高（W3C 官方规范原文） | [W3C Trace Context](https://www.w3.org/TR/trace-context/) |
| 2026-09-02 | **🔴 `trace-flags` 实测为 `03` 而非 `01`（课 4 知识点 3.2，本课最重要发现）** | OTel Python SDK 1.44.0 源码 `TraceFlags`：`DEFAULT=0x00`、`SAMPLED=0x01`、**`RANDOM_TRACE_ID=0x02`**。实测默认 `trace_flags=0x03 = 00000011`（`SAMPLED|RANDOM_TRACE_ID`）。bit1 来自 **W3C Trace Context Level 2**，标示 trace_id 为随机生成（至少右 7 字节均匀分布）。**结论：判断采样必须 `flags & 0x01`，老教程的 `if flags == "01"` 会全部误判**。实测 `09`=00001001 时 `sampled=True` 但数值≠01，印证规范"位字段不可按数值比较"的警告 | 高（SDK 源码 + 本机实跑） | `opentelemetry.trace.span.TraceFlags` 源码 / [W3C Level 2](https://www.w3.org/TR/trace-context-2/#trace-flags) |
| 2026-09-02 | **跨进程传播五种场景实测（课 4 知识点 3.3，骨架要求"断链须实测复现"）** | 用**未插桩** `http.client`（避免自动插桩覆盖手工 header）实测：①`inject` 正常 → 下游 `trace_id` 与上游一致、`parent_span_id` 有值（连通）；②忘记注入 → 下游 `parent=null`；③trace-id 全零 → 静默忽略；④大写十六进制 → 静默忽略；⑤长度不足 → 静默忽略。**后四种下游均生成全新 trace_id，且全程无异常、无日志、HTTP 200**。后端统计：`min-client total=5 linked(>1span)=1 broken(=1span)=4` | 高（本机实跑 + Jaeger API 核对） | 本机 `.probe/l4_min_server.py` / `l4_min_client.py` |
| 2026-09-02 | **Span 字段实测（课 4 知识点 3.1）** | Kind 五种取值（INTERNAL/SERVER/CLIENT/PRODUCER/CONSUMER）实测均通过，**默认为 INTERNAL**。Status 三态实测：默认 `UNSET`（非 `OK`）、`OK`、`ERROR`。**`record_exception()` 生成名为 `exception` 的事件并自动填充四个属性**：`exception.type` / `exception.message` / `exception.stacktrace` / `exception.escaped`；**但不会自动设置 Status=ERROR**，须另行 `set_status` | 高（本机实跑） | 本机 `.probe/l4_span_fields.py` |
| 2026-09-02 | **`extract` 产出 `NonRecordingSpan` 且 `is_remote=True`（课 4 知识点 3.2）** | 实测 `extract(headers)` 返回的 Context 含 `NonRecordingSpan(SpanContext(..., is_remote=True))`。`NonRecordingSpan` 是只读影子，不记录、不导出，仅代表上游 span 身份；`is_remote=True` 标记父 span 来自**另一进程**，是后端绘制跨服务边界的依据 | 高（本机实跑） | 本机 `.probe/l4_diag.py` |
| 2026-09-02 | **异步 / 线程池断链（课 4 第四幕第 5 步）** | 实测 `threading.Thread` **不会**自动传递 OTel Context：子线程内 `get_current_span()` 的 `trace_id` 为**全零**、`is_valid=False`。显式 `attach(get_current())` 传入后恢复正常。**规则：`threading.Thread` 与 `executor.submit()` 不自动传递；`asyncio` 任务因基于 `contextvars` 会自动继承** | 高（本机实跑） | 本机 `.probe/l4_async_flags.py` |
| 2026-09-02 | **Baggage 传播与"不自动进 span"（课 4 知识点 3.4）** | 实测 `inject` 会同时写入 `traceparent` 与 `baggage` 两个独立 header（`baggage: user.tier=gold,tenant.id=acme`）。**关键：Baggage 默认不会自动成为下游 span 属性**，须显式 `baggage.get_all()` 后 `set_attribute`。注意 `baggage.get_all()` 返回值不可直接 JSON 序列化，须 `dict()` 转换 | 高（本机实跑） | 本机 `.probe/l4_baggage.py` |
| 2026-09-02 | **自动插桩会覆盖手工 header（课 4 实验踩坑）** | 先用 Flask + `requests` 做断链实验，**四种断链场景全部连上**（失真）。原因：`RequestsInstrumentor` 自动注入的 `traceparent` **覆盖了**手工设置的 header。须改用未插桩 `http.client` 才能让非法 header 真正生效。**副作用结论：自动插桩确实能兜住大部分传播问题** | 高（本机实跑对比） | 本机 `.probe/l4_propagation.py`（Flask 版）vs `l4_min_*.py`（未插桩版） |
| 2026-09-02 | **tracer 获取时机导致断链（课 4 实验踩坑）** | 在 Flask 路由**外**执行 `tracer = trace.get_tracer(...)`，即使"正常注入"场景也断链（`parent_span=None`）。正解：**先 `attach(extract(headers))`，再获取 tracer，再创建 span**。与课 3 误区五（`set_tracer_provider` 先于 `get_tracer`）同源：OTel 对象获取时机不当会拿到空上下文 | 高（本机实跑，最小复现验证） | 本机 `.probe/l4_diag.py` / `l4_min_server.py` |
| 2026-09-02 | **`tracestate` 规则（课 4 知识点 3.3）** | 格式为逗号分隔 `key=value` 列表，**顺序有意义：最左侧为最近更新过的条目**；厂商更新自己条目时须**移到最前**（便于后端从尾部截断）。**条目上限 32 个**。OTel 使用 `ot` 作为 key（一致性概率采样用 `ot` 下的 `th` 子键传递采样阈值） | 高（W3C 规范 + 多方二次源交叉） | [W3C Trace Context](https://www.w3.org/TR/trace-context/) |
| 2026-09-02 | **Jaeger 镜像名与版本（课 3 知识点 2.1）** | **镜像名已变更**：v1 时代 `jaegertracing/all-in-one` → v2 时代 **`jaegertracing/jaeger`**。**Jaeger v1 已于 2025-12-31 EOL**，v2 构建在 OTel Collector 之上、原生接收 OTLP，Jaeger agent 与原生协议已移除。本机 `docker pull` + `--version` 实测：**v2.20.0**（git-commit d65f9516，build-date 2026-07-20），日志显示内部基于 **OTel Collector v0.155.0**。⚠️ 2026 年发布的教程仍大量沿用 `all-in-one:1.xx` 旧名 | 高（官方 README + 本机 docker 实测） | [Jaeger GitHub README](https://github.com/jaegertracing/jaeger) / 本机 `docker run --rm jaegertracing/jaeger:latest --version` |
| 2026-09-02 | **Jaeger v2 端口与 OTLP 端点（课 3 知识点 2.2）** | 实测容器暴露并监听 **4317（OTLP gRPC）/ 4318（OTLP HTTP）/ 16686（UI）**，UI 返回 HTTP 200。日志确认 `otlpreceiver@v0.155.0` 分别起 GRPC `[::]:4317` 与 HTTP `[::]:4318`。**HTTP 导出器必须带 `/v1/traces` 路径**，漏写返回 404 | 高（docker ps + 容器日志实测） | 本机 `docker logs jaeger-lab03` |
| 2026-09-02 | **🔴 `force_flush()` 返回值不可信（课 3 第二幕，本课最重要发现）** | 五种错误导出配置（gRPC→4318 / HTTP→4317 / HTTP 漏路径 / HTTP 全路径 / 死端口 9999）下，`force_flush()` **全部返回 `True`**，但经"每个配置独立 service.name"（fault-F1…F5）验证，**仅 fault-F4（正确配置）的数据到达后端**，其余四种从未在 `GET /api/services` 中注册。终端虽打印 `UNAVAILABLE` / `BadStatusLine` / `404` / `Connection refused`，但**程序不抛异常、退出码为 0**。**唯一可信判据：查后端 API** | 高（本机实跑 + Jaeger API 逐个核对） | 本机 `.probe/l3_faults2.py` + `curl http://localhost:16686/api/services` |
| 2026-09-02 | **OTel Python 依赖版本（课 3 知识点 2.1）** | `uv pip install` 实测：`opentelemetry-api` / `-sdk` / `-exporter-otlp-proto-grpc` / `-exporter-otlp-proto-http` / `-exporter-otlp-proto-common` / `opentelemetry-proto` **均为 1.44.0**；`opentelemetry-semantic-conventions==0.65b0`；`grpcio==1.83.1`、`protobuf==7.36.1`、`requests==2.34.2` | 高（uv 实时安装实测） | 本机 `~/otel-course/lab03` |
| 2026-09-02 | **uv 虚拟环境行为（课 3 知识点 2.1）** | 系统 Python 为 **3.12.3**，但 `uv venv --python 3.12` 实际创建的是 **CPython 3.12.13**（uv 自动下载托管版本），二者均为 3.12 不影响命令。`uv run python` **无需** `VIRTUAL_ENV` 或 `--no-project` 即自动选中本目录 `.venv`（实测 `sys.executable` = `lab03/.venv/bin/python3`） | 高（实测） | 本机 `uv run python -c "import sys; print(sys.version, sys.executable)"` |
| 2026-09-02 | **Resource 默认值与优先级（课 3 知识点 2.3）** | 未配置时默认 Resource 含 `telemetry.sdk.language/name/version`、`service.instance.id`，**`service.name` 为 `unknown_service`**。**优先级实测：代码显式属性 > 环境变量（`OTEL_SERVICE_NAME` / `OTEL_RESOURCE_ATTRIBUTES`）> SDK 默认值**（`OTEL_SERVICE_NAME=env-service` + 代码 `code-wins` → 结果为 `code-wins`）。**`A.merge(B)` 是 B 覆盖 A**（实测 `base.merge(over)` → `override`）。**`service.name: ""` 被兜底成 `unknown_service`** | 高（本机实跑） | 本机 `.probe/l3_resource.py` |
| 2026-09-02 | **未设 service.name 的后端呈现（课 3 知识点 2.3）** | 未设 `service.name` 上报的数据，在 Jaeger API 中服务名即为 **`unknown_service`**（实测 `/api/traces?service=unknown_service` 可查到）。多个未设名的服务会在后端**挤在同一个 `unknown_service` 名下**，失去分辨能力——这是 `service.name` 必须设的实证 | 高（本机实跑 + API 核对） | 本机 `.probe/l3_otlp.py` + Jaeger API |
| 2026-09-02 | **tracer 创建顺序与 ProxyTracerProvider（课 3 误区五，承接课 2）** | 在 `set_tracer_provider()` **之前** `get_tracer()` 并创建 span：实测 `is_recording()=False`、`trace_id=0`，**不抛异常、不报错**（静默丢弃）。**反直觉细节：同一个早期 tracer 对象，在 provider 设置之后复用，能正常记录**（实测拿到真实 `trace_id`）——这是 `ProxyTracerProvider` **延迟绑定**所致，印证课 2 结论（非旧教程所称 `NoOpTracerProvider`） | 高（本机实跑） | 本机 `.probe/l3_order.py` |
| 2026-09-02 | **Jaeger 自监控数据（课 3 第四幕）** | 实测 `GET /api/services` 返回含 `jaeger` 服务，**trace 计数为 9**——Jaeger v2 基于 OTel Collector 构建，**会给自己发遥测数据**。学员在 UI 下拉框看到 `jaeger` 属正常现象，非数据异常 | 高（本机 API 实测） | 本机 `curl http://localhost:16686/api/services` |
| 2026-09-03 | **成熟度等级数量（骨架表述纠错）** | **七级**，非骨架所写五级。Development / Alpha / Beta / Release Candidate / Stable / Deprecated / Unmaintained | 高 | 规范原文 `specification/maturity-levels.md`（Status: Stable） |
| 2026-09-03 | `Experimental` 已更名 | 是，2023 年更名 `Development`。原文第 90 行："Development status was previously called Experimental" | 高 | `specification/versioning-and-stability.md` 第 90 行 |
| 2026-09-03 | semconv YAML 的 `stability` 取值 | **5 个**：development 2262 / stable 260 / release_candidate 231 / experimental 14 / alpha 5（共 2772 处） | 高 | 本机扫描官方仓库 `model/` 全目录实测统计 |
| 2026-09-03 | `deprecated` 是否为 stability 取值 | **否**。独立字段；且弃用属性的 `stability` 被重置为 `development` | 高 | `model/http/deprecated/registry-deprecated.yaml` 源码核对 |
| 2026-09-03 | HTTP 域弃用属性数 | **14 个** | 高 | `model/http/deprecated/registry-deprecated.yaml` 逐条清点 |
| 2026-09-03 | DB 域弃用属性数 | **30 个** | 高 | `model/db/deprecated/registry-deprecated.yaml` |
| 2026-09-03 | `http.target` 替代关系 | → `url.path` + `url.query`，`reason: obsoleted`，**一对多拆分** | 高 | 官方 YAML + 本机实验 D 实测 |
| 2026-09-03 | `db.system` → `db.system.name` 版本 | **v1.30.0** | 高 | `schemas/1.34.0` 文件的 1.30.0 段落 |
| 2026-09-03 | `db.statement` → `db.query.text` 版本 | **v1.25.0** | 高 | `schemas/1.34.0` 文件的 1.25.0 段落 |
| 2026-09-03 | `http.*` 六个属性重命名版本 | **v1.21.0** | 高 | `schemas/1.21.0` 文件 |
| 2026-09-03 | 本机 semconv 版本与 schema | **0.65b0**（beta），对应 schema **1.43.0** | 高 | 本机 `importlib.metadata` 实测 |
| 2026-09-03 | 官方 semconv 最新版本 | **v1.44.0** | 高 | 官方 CHANGELOG.md 最新条目 |
| 2026-09-03 | **本机落后官方版本数** | **1 个版本**（1.43.0 vs v1.44.0） | 高 | 两者对比 |
| 2026-09-03 | semconv 域数量 | **79 个** | 高 | 扫描官方仓库 `model/` 目录实测 |
| 2026-09-03 | 默认档 Flask span 属性数 | **13 个**，其中 11 个是已弃用旧名 | 高 | 本机实验 B 实测（InMemorySpanExporter） |
| 2026-09-03 | `http` 档属性数 | **10 个**，全是新名 | 高 | 本机实验 B 实测 |
| 2026-09-03 | `http/dup` 档属性数 | **22 个**（13 旧 + 9 新） | 高 | 本机实验 B 实测 |
| 2026-09-03 | `database` 档对 HTTP 属性的影响 | **无**。HTTP 属性仍输出旧名，证明三档开关分域独立 | 高 | 本机实验 B 五档对照实测 |
| 2026-09-03 | Cardinality 低基数序列数 | **15**（5 路由 × 3 状态码） | 高 | 本机实验 C 实测（真实 SDK Counter 聚合） |
| 2026-09-03 | Cardinality 高基数序列数 | **150,000**（加 10000 个 `user.id`） | 高 | 本机实验 C 实测 |
| 2026-09-03 | **Cardinality 放大倍数** | **10,000 倍**，等于加进去的维度基数 | 高 | 本机实验 C 实测 + 重算验证 |
| 2026-09-03 | 5 维推演序列数 | 15 → 150,000 → 600,000 → **120,000,000** | 中 | 实验 C 乘法推演（基数假设已标注） |
| 2026-09-03 | 存储成本估算 | 低基数 0.16 MiB/天 → 高基数 **1,647.9 MiB/天**；一年 60 MiB → 587 GiB | 中 | 实验 C 按 15s 采样、2 字节/样本估算，**数量级推演非精确报价** |
| 2026-09-03 | 弃用属性扫描检出率 | 10 个属性中 **8 个**弃用（80%） | 高 | 本机实验 D 实测 |
| 2026-09-03 | 1:N 拆分能否用 schema processor | **不能**，schema 文件结构仅支持 1:1 rename | 高 | `schemas/*.yaml` 仅 `rename_attributes.attribute_map` |
| 2026-09-03 | K8s 指标在 v1.44.0 仍有改名 | 是，3 个 memory.paging.faults 指标改名 | 高 | 官方 CHANGELOG v1.44.0 breaking changes |
| 2026-09-03 | 生产环境新旧名共存实例 | `http.url` 1,830 次与 `url.full` 23,984 次共存同一数据集 | 中 | Honeycomb 工程师分享（匿名生产数据），**第三方引述非本机实测** |
| 2026-09-03 | Collector transform 改写配置 | 本课仅预告，**未实测**；正式落地在课 10 的 9.3 | — | — |
| 2026-09-04 | **Profiles 稳定状态（复核，推翻骨架预判）** | 官方规范页与概念页均 **Alpha**（非骨架所记「目标 Q3 GA」）；页脚变更记录 `#9457` 停留 2026-03-23；各语言 SDK **无一 Stable**（Go=Beta、Java/Python=Development、其余 `-`） | 高（官方一手页面） | [Profiles 规范](https://opentelemetry.io/docs/specs/otel/profiles/) / [Profiles concepts](https://opentelemetry.io/docs/concepts/signals/profiles/) / [Language APIs & SDKs](https://opentelemetry.io/api/) |
| 2026-09-04 | **Profiles 生产建议** | 官方原文：`should not be used for critical production workloads` | 高 | [Profiling SIG Alpha 公告](https://opentelemetry.io/blog/2026/profiles-alpha) |
| 2026-09-04 | **WSL eBPF 可用性（实测）** | **可用**：内核 6.6.87.2-microsoft-standard-WSL2、`CONFIG_BPF_SYSCALL=y`、BTF 6,050,732 字节、`CapEff` 含 CAP_BPF；bcc 挂载 kprobe 成功、perf 采样剖析成功 | 高（本机实测） | 本机 WSL 实测输出 |

**待核查（进入对应课时时补做）**：

- ✅ **已核销（2026-09-03 课 5）**：自动插桩覆盖范围已实测——`bootstrap_gen` 含默认插桩包 8 个 + 检测型规则 50 条；本机 `opentelemetry-bootstrap -a requirements` 输出 14 条；另发现两个覆盖范围暗礁（`conn.execute()` 不覆盖、`urllib`/`urllib3` 需单独装包）
- 声明式配置 `file_format` 字段的当前值（"0.3" 还是 "0.4"），须以官方文档为准
- Collector 的准确最新版本号与 core/contrib 镜像 tag（课时以 `docker pull` 实测为准）
- Profiles 是否已达 GA（原目标 2026 Q3，课 12 时须复核）
- 本机 WSL 是否支持 eBPF（决定课 12 Profiles 能否实操）

**课 4 已核销的待核查项**（2026-09-02 完成，从上方清单移除）：

- ✅ W3C Trace Context `traceparent` 精确格式 → 已核对官方规范原文：55 字符 / 全小写 / 全零非法 / `ff` 禁止；`tracestate` 上限 32 项、最左为最近更新
- ✅ 断链演示实测复现 → 五种场景全部复现，后端统计 `linked=1 / broken=4`

**课 5 已核销的待核查项**（2026-09-03 完成，从上方清单移除）：

- ✅ 自动插桩实际覆盖范围 → 已实测：`bootstrap_gen` 默认插桩包 8 个 + 检测型规则 50 条；`opentelemetry-bootstrap -a requirements` 是本环境真相
- ✅ `@tracer.start_as_current_span` 装饰器在无参函数上的行为 → 已实测：完全正常；异常自动记录并设 ERROR；不注入 span 参数
- ✅ Python 自动插桩命令 → 已实测：必须装 `opentelemetry-distro`，且 HTTP 导出须显式 `OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf`

**课 6 已核销的待核查项**（2026-09-03 完成，从上方清单移除）：

- ✅ 采样环境变量准确名称（`OTEL_TRACES_SAMPLER` / `OTEL_TRACES_SAMPLER_ARG`，优先级最高）→ 已双源核对（官方规范 + SDK 源码 `_KNOWN_SAMPLERS`）：6 个可选值、ARG 取值 `[0.0,1.0]` 默认 `1.0`；并实测发现**错名/错 ARG 会静默回退到全量**
- ✅ Collector 的准确版本号与镜像 tag → 已 `docker pull` 实测：**otelcol-contrib 0.160.0**（2026-09-03）
- ✅ 尾部采样是否需要起独立 Collector → 已评估并实施：起了 `otelcol-lab06` / `otelcol-lab06b` 两实例 + `otel-lab06-net` 网络，最小 `tail_sampling` 配置已跑通并实测三组对照
- ✅ `tail_sampling` 策略名 → 已核对 contrib README 并在本机 0.160.0 实跑验证（`status_code` / `latency` / `probabilistic` / `string_attribute` / `always_sample` 等 13 种）

---

## 六、大纲调整记录

| 日期 | 调整内容 | 原因 |
|------|----------|------|
| 2026-09-02 | 建档：4 阶段 / 12 课 / 42 知识点 | 首次规划 |
| 2026-09-02 | 课 7 由 3 知识点扩为 4（新增 6.3 Exponential Histogram）；课 9 由 3 知识点扩为 4（新增 8.2 HTTP 属性的弃用与迁移）；总数 41 → 42 | 大纲评审 P1×2 修订 |
| 2026-09-02 | Profiles（课 12 知识点 11.1）定位调整为「认知为主，实操视环境可用性而定」 | 大纲评审 P2×1：Profiles 处于 alpha 且需 eBPF 内核权限，本机 WSL 未必可用 |

---

## 七、断点续学信息

| 项 | 值 |
|---|---|
| **已完成** | 课 1–课 12 **全部 12 课讲完**，共 41 个知识点（2026-09-04，41/42）；阶段 1（4/4）、阶段 2（7/7）、阶段 3（11/11）、阶段 4（12/12）**全部完成**。**结课综合实战项目已于 2026-09-04 完成**（`projects/capstone/`，跨 4 阶段整合），**进度 42/42，全课程收官**。**`final-课程手册.md`、`08-实战经验.md`、`09-排障速查手册.md`、`10-场景解法库.md` 均已 2026-09-04 完成。收尾产物全部交付，全课程收官**（均为非知识点的产出文档） |
| **下一批** | **无——全课程收官**（42/42 知识点 + 5 类产出全部完成）。**可复用资产**：`08-实战经验.md` 已把 10 个故障模式按**五段式**（症状 → 根因 → 排查路径 → 修复 → 预防）结构化完成，09 可直接压缩为「症状 → 30 秒内可执行的检查命令 → 判据」的 QRH 表格，每条回链到 08 的对应故障模式，无须重新扫描讲义 |
| **接力提示词** | 见 `02-课程目录.md` 末尾，或 `final-课程手册.md` 末尾「🚀 接力提示词」 |
| **待办** | **42/42 已完成，全课程收官**；`final-课程手册.md`、`08-实战经验.md`、`09-排障速查手册.md`、`10-场景解法库.md` 均已完成（2026-09-04）。**收尾产物全部交付，全课程收官**。实战项目已交付要点：①**慢 SQL / N+1 / 连接池三种故障均实测定位成功**，根因属性（`db.index_used=false`、`db.rows_scanned=4820000`、`pool_exhausted=true`）直接挂在 span 上；②**收束量化：找信息 156 分钟 → 2.3 分钟（约 68 倍）**，但**修复 3 分钟不变**，端到端仍 30–60 分钟，拒绝「164 分钟变 3 分钟」的包装；③**N+1 是唯一会躲过常规排查的**——单次查询都不慢（23 ms），只有 Self Time 聚合能看见（×200 累计 2518.8 ms，占端到端 96%）；④eBPF 零插桩剖析实测可用（15,840 样本），但**只到进程级**，无法符号化到函数名，且 Profiles 仍 Alpha，不应放入关键定位链路 | |
| **环境基线** | 实测于 2026-09-03：**Windows** = Node v22.14.0，无 Python/Go；**WSL Ubuntu** = Docker 29.4.1 + `uv` 0.11.6 + Python 3.12.13（venv CPython），无 Node/Go。**已建资产**：`~/otel-course/lab03/.venv`（OTel SDK **1.44.0**，instrumentation **0.65b0**，flask **3.1.3**，opentelemetry-distro **0.65b0**）；容器 `jaeger-lab03`（Jaeger **v2.20.0**，16686/4317/4318）；**课 6 新增**容器 `otelcol-lab06` / `otelcol-lab06b`（**otelcol-contrib 0.160.0**，14317/14318 与 24317/24318）+ 网络 `otel-lab06-net`（Jaeger 已接入，原端口映射不受影响）。⚠️ **三个环境陷阱**：①自动插桩 HTTP 导出须显式 `OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf`；②**Collector 镜像内无 `cat`**，读容器文件须用 `docker cp`，用 `docker exec cat > file` 会覆盖本地 yaml 并致容器退出；③Jaeger 后端按服务名累加，多组实验须用独立 `service.name` 且查询带 `start` 时间窗。课 7 沿用 **WSL + Python + Docker** |
| **课 7 新增资产（2026-09-03）** | 容器 **`prom-lab07`**（Prometheus **v2.53.0**，端口 **9099**，启动参数 `--enable-feature=native-histograms`，接入网络 `otel-lab06-net`）；后台进程 **`l7_app_six.py`**（暴露 **9464** 供 Prometheus 抓取）。WARNING：**v2.53.0 不是 v3.x，Native Histograms 的完整链路本机未实测**，v3.8.0 才是 Stable 实现。WARNING：**实验脚本实际位于仓库根 `D:/projects/learning/.probe/`**（非 `opentelemetry/.probe/`），WSL 内路径 `/mnt/d/projects/learning/.probe/`。**课 7 新增三条陷阱**：(1)`InstrumentSelector` 在 SDK 1.44.0 **不存在**，`view` 模块只导出 8 个名字，须直接把 Instrument 类传给 `View(instrument_type=...)`；(2)`Observation` 在 **API 层** `opentelemetry.metrics`，`PrometheusMetricReader` 在 `opentelemetry.exporter.prometheus`，二者都不在 `sdk.metrics` 下；(3)**Prometheus 文本格式不携带 exemplar**（`grep -c trace_id` = 0），验证 exemplar 必须走 OTLP/protobuf |
| **课 8 新增资产（2026-09-03）** | 容器 **`otelcol-lab08`**（collector-contrib，端口 **34317/34318**，配置含 **logs pipeline**，转发 Jaeger 会 404）、**`otelcol-lab08dbg`**（端口 **35318**，只输出 debug 用于看完整字段）。WARNING：**Jaeger 不接受日志**——对 `/v1/logs` 返回 404 Unimplemented，故课 8 的日志端到端只能验证到 Collector 层（实测 `log records: 3`），**无法在 Jaeger 里看到日志**；若要完整验证需 Loki / ES / 商业 APM，**本机无对应镜像**。**课 8 新增四条陷阱**：(1) 公开的 `opentelemetry.sdk.logs` **不存在**，只有私有 `sdk._logs`；(2) `OTLPLogExporter` 在 `...proto.http._log_exporter`（**带下划线**，与 `trace_exporter` 不对称），`InMemorySpanExporter` 在 `sdk.trace.export.in_memory_span_exporter`；(3) **debug exporter 省略零值字段**，不能用来判断某字段有没有传；(4) **`InMemoryMetricReader` 的 exemplar 一次性**——`get_metrics_data()` 触发 collect 后 reservoir 清空，`force_flush()` 也会消费，故须 record 后立刻读且只读一次 |
| **课 9 新增资产（2026-09-03）** | 官方 semconv 源码包已下载至 WSL **`/root/semconv/`**（main 分支，CHANGELOG 最新条目 v1.44.0），可直接 `cat model/http/deprecated/registry-deprecated.yaml` 查弃用清单、`cat schemas/1.21.0` 查各版本迁移定义（**这是本课逐条核对的权威依据，后续课程沿用**）。实验脚本：`l9_a_semconv_ver.py`（版本探测）、`l9_b_semconv_optin.py` + `l9_b_run.sh`（5 档开关实测）、`l9_c_cardinality.py`（基数爆炸）、`l9_d_migrate.py`（扫描+改写）；**2026-09-03 讲义交付后已全部改造为可复用工具**，详见下一条与 [09-实验脚本可复用清单.md](./assets/09-实验脚本可复用清单.md)。**课 9 新增两条陷阱**：(1) **`OTEL_SEMCONV_STABILITY_OPT_IN` 是分域独立的**——`http` 只切 HTTP 域，`database` 只切数据库域，设一个不影响另一个（实验 B 实测：设 `database` 后 HTTP 属性仍全是旧名）；(2) **弃用属性照常工作且不打任何 warning**，只有在升级插桩库后发现看板数据少一半时才会暴露，是静默失败在命名层的形态。⚠️ **课 10 待办**：本课 4.3 的 Collector transform 配置（`l9-semconv-migrate.yaml`）仅预告未实测，课 10 的 9.3（Processor）是其正式落地处 |
| **课 9 脚本工具化（2026-09-03，讲义交付后追加）** | 课 9 的 5 个实验脚本已从「课内一次性脚本」改造为**可独立复用工具**，产出清单文档 [09-实验脚本可复用清单.md](./assets/09-实验脚本可复用清单.md)。**改造要点**：(1) `l9_a_semconv_ver.py` 修掉两处**失效探测**——原版用 `Resource.schema_url`（**恒为空串**）判断版本、用 `dir(attributes)` 找 `*Attributes` 类（新版已改为模块常量，**恒返回 0**），两者输出空值却不报错；现改为读 `opentelemetry.semconv.schemas.Schemas` 常量 + `pkgutil.iter_modules` 数大写常量（实测：21 个 schema 版本、105 条属性常量 / 16 域）；(2) `l9_d_migrate.py` 从「只能扫写死的内置示例字符串」改为支持 `scan <目录>` 扫真实代码，按 rename / split / conditional / removed 四类统计迁移工作量，支持 `--json` 导出与 `文件:行号` 定位，并**自排除本脚本**（映射表本身含全部旧属性名，不排除会全量误报）；(3) `l9_c_cardinality.py` 参数化（`--routes/--status/--users/--extra/--interval/--bytes/--skip-real`）；(4) `l9_b_run.sh` 支持指定档位与自动定位脚本目录；(5) `l9_scan_dep.py` 补 `--all` / `--base` 与官方源码下载说明。**新发现两条硬约束**：① **`OTEL_SEMCONV_STABILITY_OPT_IN` 必须子进程隔离跑**——`trace.set_tracer_provider()` 全局只允许成功设置一次，单进程内循环切换会**静默失败**（只打印一行 `Overriding of current TracerProvider is not allowed`、**不抛异常**），第二档起抓到 **0 个 span**，产出「某档位不产出属性」的假结论，实测 `default n=12 → http n=0 → http/dup n=0`；但环境变量本身是**运行时读取**（建 span 时读），**不是** import 时锁存，故「设好变量再 import」只是多余保险，真正必需的是子进程隔离；② `l9_scan_dep.py` 用正则猜替代名**会漏检**——官方源码检出 HTTP 域 **13 条**，人工核对 YAML 得 **14 条**，漏的正是 `http.target`（官方写 `reason: obsoleted`，不是 `Replaced by` 句式），故权威做法仍是 `cat model/<域>/deprecated/registry-deprecated.yaml` 查 `renamed_to`。⚠️ **数据口径修正**：B 脚本 `default` 档属性数讲义记 **13**、本次实测 **12**，差异因 `http.route` 在 404 span 上不存在，本次统计的是两个 span 属性的**并集去重**；属性集合本身与讲义一致，不影响结论。**验证**：A / C / D / scan_dep 退出码均 0，C 与 D 数据与讲义完全一致（C：15→150,000、10,000×、587.4 GiB/年；D 演示模式 8/10 弃用） |
| **课 10 新增资产（2026-09-03）** | **9 个 Collector 容器**：`otelcol-lab10`（36317/36318，最小管道）、`otelcol-lab10-e2`（36327，脱敏+过滤）、`otelcol-lab10-e3`（36337，语义迁移）、`otelcol-lab10-e4a`/`e4b`（36347/36357，**顺序敏感对照**）、`otelcol-lab10-e5-jaeger`/`e5-file`（36367/36377，换后端）、`otelcol-lab10-e6`（36387，富化）、`otelcol-lab10-e7`（36397/13133/1777，memory_limiter+health_check，限内存 512m），全部接入 `otel-lab06-net`。**实验脚本 28 个**（`l10_*`），清单见 [10-实验脚本可复用清单.md](./assets/10-实验脚本可复用清单.md)。**本课最大成果：课 9 遗留的 `l9-semconv-migrate.yaml` 正式落地并实测通过**，包括课 9 只留注释未实现的 **1:N 拆分**（`http.target` → `url.path` + `url.query`），实测用 OTTL **`Split()`** 实现；并验证了 `where` 守卫的**幂等性**（无 `http.url` 的 span 不会产生 `url.full`）。**实测踩坑六条**：①**`attributes` processor 在 0.160.0 做不了重命名**——`new_key` 被拒（`has invalid keys: new_key`），枚举确认只有 insert/update/upsert/delete/hash/extract/convert 七个 action，**无 rename/move/copy**，且 **delete/hash 不接受 value 字段**；这**印证了课 9 的 4.2 结论**（重命名归 transform）；②**OTTL 写法反直觉**——`Split()` 可用而 `split()` 小写不存在（**大小写敏感**），**没有 `matches` 操作符**（正则条件判断须用 `IsMatch()`），`ExtractPatterns().p` 点号取值语法错误；③**`debug` exporter 的 verbosity 只有 `basic`/`normal`/`detailed` 三个合法值**（`minimal`/`info`/`debug` 均被拒）；④**file exporter 不能输出到 `/mnt/d`**（Windows 9P 不支持 chmod，报 `operation not permitted`），也**不能用 `/root` 下目录**（容器以非 root 运行 + `/root` 700，报 `permission denied`），须用 `/var/tmp/l10out` 这类 WSL 原生 ext4 目录；⑤**`memory_limiter` 在容器里必须用百分比**——写死 `limit_mib` 绝对值读的是**宿主机内存**而非容器限额（实测：512MiB 容器 + 80% → 正确算出 `limit_mib=409`）；⑥**组件命名正处于改名期**（v0.150.0/0.151.0 大批改 snake_case），实测确认**新旧名都可用**（`filelog`/`file_log`、`hostmetrics`/`host_metrics`、`kubeletstats`/`kubelet_stats` 均通过；`hostmetrics` 的报错信息里直接显示被规范化为 `host_metrics`），exporter 侧 `otlp`/`otlp_grpc`/`otlp_http` 三种写法也都能用。**稳定性等级（权威取法）**：`docker run --rm <img> components` 比查文档可靠，反映实际运行版本；须**按 kind 分区解析**——`prometheus` 在 receivers 与 exporters 段同名，我用 awk 首次提取时把 `filter` 的 Alpha 误读成 Beta，改用 Python 分区解析后修正。实测本机 0.160.0：`otlp` receiver 三信号全 **Stable**、`transform` 三信号 **Beta**、**`filter` 三信号 Alpha**、`debug` exporter 三信号 **Alpha**、`batch`/`memory_limiter` 均 Beta。**纠正骨架一处事实错误**：骨架卡片原文写「v0.151.0 的 `azure_auth` 认证绕过」，**把修复版本说成了受影响版本**——正确表述是 **CVE-2026-42602 受影响 v0.124.0–v0.150.0，修复于 v0.151.0**（CVSS 8.1 High，[GHSA-pjv4-3c63-699f](https://github.com/open-telemetry/opentelemetry-collector-contrib/security/advisories/GHSA-pjv4-3c63-699f)）；照骨架写法会让人「避开 0.151.0」，实际恰好相反。本机 0.160.0 > 0.151.0，**已含修复**。**顺序敏感实证**：`e4a`（filter→transform→attributes）脱敏生效；`e4b`（attributes→transform）**`url.query=page=1` 漏过脱敏**——脱敏执行时该属性尚未被 transform 创建。**这是「配置里写了脱敏、实际却漏了」的典型。** **未验证项**：`k8sattributes` 的实际富化效果（本机无 K8s，仅通过 validate），已在讲义标注。**验证**：7 个实验全部实测，配置全部 `validate` rc=0，并用反例（不存在的 processor / OTTL 语法错误）确认 validate 确实会报错；全仓库链接检查 22 文件 / 128 链接零断链 |
| **课 12 新增资产（2026-09-04）** | **WSL 新增**：Go **1.26.8**（为 pprof 实验 `apt-get install golang-go` 装 1.22.2，`go mod tidy` 时自动升级至 1.26.8）、bcc **0.29.1**（`bpfcc-tools`）、`linux-tools-common`。**新建容器**：`l12-final`（collector-contrib **0.133.0**，**不映射宿主端口，用容器 IP 172.17.0.11 直连**）。**实验脚本 18 个**（`l12_*`），清单见 [12-实验脚本可复用清单.md](./assets/12-实验脚本可复用清单.md)。**本课最大成果：推翻骨架两处预判**——①Profiles「目标 2026 Q3 GA」**未兑现**，2026-09-04 复核官方规范页与概念页均为 `Status: Alpha`，状态变更记录停留在 2026-03-23（`#9457`），各语言 SDK 无一 Stable；②「WSL 可能不支持 eBPF、11.1 须降级为认知层」**不成立**，实测完整支持。**实测结论**：eBPF perf CPU_CLOCK 99 Hz 采样 6 秒得 **11,877 样本 / 535 栈**；pprof 无损往返 **栈签名差异 0 条**（490→481 字节，比值 0.982）；课 1 四层调用链落库、Jaeger 查询侧 **35.1 ms** 命中根因。**⚠️ 环境基线变更（须披露）**：课 1 建档时记录「WSL 无 Go」，**课 12 为 pprof 实验安装了 Go**——按运行环境约定第 3 条硬纪律，安装前本应征询，此处补记并请用户确认是否保留（保留则后续可做 Go 插桩演示，删除则不影响已交付讲义）。**新增八条陷阱**：(1) PowerShell 会抢管道，须把管道放进 `.sh` 内（课 10 已记，本课再踩）；(2) **Windows 无 Python**，一律走 `wsl.exe -d Ubuntu -- bash /mnt/d/...`，且脚本内路径是 `/mnt/d/` 不是 `D:/`；(3) **孤儿 docker-proxy 占住端口**——容器已 `rm` 但端口仍 LISTEN，新建容器应改用容器 IP 直连；(4) **`bpf()` 返回 EINVAL(22) 不代表不支持**，那是参数错误，EPERM(1)/EACCES(13) 才是权限问题；(5) bcc 常量须 `from bcc import BPF, PerfType, PerfSWConfig`，`BPF.PerfType_SOFTWARE` 不存在；(6) **`bpf_perf_event_data` 是前向声明**，函数签名须用 `struct pt_regs *ctx`；(7) Go `go mod tidy` 会**自动下载升级工具链**（1.22.2→1.26.8），首次较慢属正常；(8) pprof 库字段是 **`Function` 不是 `Functions`**。**未验证项**：OTLP Profiles 的**真实 Collector 收发**（本机 0.133.0 无 profiles pipeline，且该信号仍 Alpha），讲义中的无损互转是用**官方 pprof 库**验证格式层面的往返，非端到端 OTLP Profiles 管道，已在讲义标注 |
| **结课综合实战项目（2026-09-04）** | **42/42，全课程收官**。产物 [projects/capstone/README.md](./projects/capstone/README.md)（11,092 字符，12 节） + [REVIEW.md](./projects/capstone/REVIEW.md)（双视角评审）+ 11 个源码/配置文件。**新建容器**：`capstone-gateway`（172.24.0.21，宿主 46317/46318/48889）、`capstone-agent`（172.24.0.22，**无宿主映射**）、`capstone-prom`（Prometheus v2.53.0，宿主 **46999**），均接入 `otel-lab06-net`。**4 个服务进程**：checkout 5060 / payment 5061 / risk-control 5062 / user-profile 5063（Flask + OTel 自动插桩，走 agent→gateway 两层）。**实验脚本 10 个**（`p1`/`p2`/`p5`~`p11_*`）。**实测结论**：①三种故障定位成功——慢 SQL（Self Time 4225 ms、`db.rows_scanned=4820000`/`index_used=false`）、N+1（×200 累计 2518.8 ms，占端到端 96%）、连接池（502 + `pool_exhausted=true`）；②**收束量化**：找信息 156 分钟 → 2.3 分钟（约 68 倍），**修复 3 分钟不变**；③eBPF 零插桩剖析 8 秒得 **15,840 样本**（20 核下 python3 占 5.0% ≈ 吃满一核）。**新增六条陷阱**：(1) **自动插桩与手工初始化冲突**——`opentelemetry-instrument` 会先建好 Provider，代码里再 `set_tracer_provider()` **不抛异常**、只打印 `Overriding of current TracerProvider is not allowed` 然后忽略，导致代码里配的 resource 全部丢失；修法是先 `isinstance(trace.get_tracer_provider(), SDKTracerProvider)` 探测，已存在则复用。(2) **`resource` processor 的 `upsert` 不插入**——只在键已存在时覆盖，键不存在时静默不生效，无条件写入必须用 `insert`（实测 `deployment.environment.name` 用 upsert 后 Jaeger 显示为 null）。(3) **OTel→Prometheus 指标名追加单位后缀**——`payment_amount`(CNY)→`payment_amount_CNY`、`user_profile_query_duration_ms`(ms)→`..._milliseconds`，按原名查会误判「指标丢了」。(4) **指标默认 60 秒导出一次**（`OTEL_METRIC_EXPORT_INTERVAL`），验证时等 10 秒查不到，演示环境应设 5000。(5) **`--service_version` 不是合法 CLI 参数**——只有 `--service_name` 与 `--resource_attributes`。(6) **`opentelemetry.sdk._logs` 没有 `get_logger_provider()`**，且顶层 `opentelemetry.logs` 在 1.44.0 下不存在，复用模式下拿不到 CLI 建的 logger 句柄。**方法论产出（可迁移）**：N+1 类问题会躲过常规排查——看「最慢单个 span」会漏判（每次都只有 23 ms），看「同名 span 累计 duration」会被父子重叠的入口 span 霸榜（等于没说），只有 **Self Time 聚合**（自身 duration − 子 span 占用）能看见。**未验证项**：Profiles 端到端（无 pipeline）、日志未进 Jaeger（404 Unimplemented，无 Loki/ES）、eBPF 只到进程级（无法符号化）、Native Histograms（Prom v2.53.0）、人工判断耗时为估算非秒表实测 |

---

## 八、与其他课程的关联

本仓库已有课程中与 OpenTelemetry 可对照的：

| 课程 | 目录 | 对照点 |
|------|------|--------|
| Prometheus / PromQL | `promql/` | 指标模型对比（OTel Instruments vs PromQL 四种类型）；Exemplar 与 PromQL 查询的结合 |
| VictoriaMetrics | `victoriametrics/` | 时序后端的选型与 OTel metrics 的落地路径 |
| InfluxDB | `influxdb/` | 另一类时序存储，与 OTel metrics 的写入协议差异 |
| Elasticsearch | `elasticsearch/` | 日志方案对比（ELK vs OTel logs + Loki）；全文检索与 trace 检索的定位差异 |
| Redis / RabbitMQ / Kafka | `redis/` `rabbitmq/` `kafka/` | 这些组件本身是 OTel 自动插桩的重点对象（课 5 会回扣） |
| ZooKeeper / Consul | `zookeeper/` `consul/` | 服务发现与健康检查，与 OTel 的 K8s 属性富化形成对照 |
| SurrealDB | `surrealdb/` | 其 3.1.0 起内置统一 OpenTelemetry，是多模型数据库的观测实践 |

---

## 九、本课程的交付约定（沿用仓库既有规范）

每课交付后必须回写**四处**档案，缺一不可：

1. `00-学习档案.md`（进度表本课各行 + 评审记录 + 事实核查 + 断点信息）
2. `00-评审清单.md`（本课勾选 + 评审记录表追加）
3. 对应阶段 `stages/N-xxx/overview.md`（本课标 ✅、阶段状态、核心结论）
4. `02-课程目录.md` 与 `01-学习路径总览.md`（索引链接与进度条）

课文件末尾必含两段：

- 「🚀 下一批接力提示词」：一段可直接复制发给 AI 的续学 prompt（含学习档案路径 + 刚学完的阶段/课名/知识点列表 + 请按大纲继续讲解下一课及课名）
- 「🧭 课程导航」：上一课 / 下一课 / 返回目录三个链接（返回目录的层级在各阶段 overview 中为 `../../02-课程目录.md`，**注意不是 `../`**）

每课交付前必须完成 **pedagogy + learner 双视角评审**，P0 清零后才可勾选清单条目；评审结论要对用户可见。
