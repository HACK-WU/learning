# 10-场景解法库.md 双视角评审报告

> 评审日期：2026-09-04
> 评审对象：`10-场景解法库.md`（设计态：新要求来了怎么设计）
> 评审方式：主 agent 内联（pedagogy + learner 双视角）
> 评审结论：**P0×1 / P1×2 / P2×2，全部当批修复。通过落盘。**

---

## 【第一轮 · 教学法视角（pedagogy）】

### 1. 定位与去重

本文件是「收尾三件套」的最后一件，定位为**设计态**，与另外两份的分工：

| 文件 | 态 | 触发时机 |
|---|---|---|
| `08-实战经验.md` | 学习态 | 想理解"为什么会这样" |
| `09-排障速查手册.md` | 使用态 | 出事了，按症状倒查 |
| **`10-场景解法库.md`** | **设计态** | **新需求来了，怎么设计才不踩** |

**去重核查**：10 个场景不是 08 的 9 条反模式的复述。08 是"**错在哪**"（事后复盘），本文件是"**怎么做对**"（事前设计）。每个场景的「回链」段落承担跳转职责，正文只讲设计动作。

**✅ 通过。**

### 2. 准确性

逐条比对源文档，关键数字全部一致：

| 场景 | 关键判据 | 溯源 |
|---|---|---|
| 3 | 每 span 约 600 B（未压缩 959 B）；错误 trace 100/100 → 11/100，普通 8/100 → 0/100 | 课 6 实测 |
| 4 | 5×3=15 → 加 10000 user.id = **150,000**；显式桶 −50.0% / 指数桶 −2.2% | 课 9、课 7 实测 |
| 5 | 端到端 2616 ms、最慢单 span 48.2 ms、Self Time 2518.8 ms ×200 | 实战项目实测 |
| 8 | 五档 13/10/22/无影响；`payment_amount`→`payment_amount_CNY` | 课 9 实测 |
| 10 | 10000 请求、span 11 条、count 10000；日志恒 200/200 | 课 7、课 8 实测 |

**【P0 级问题 —— 复核后确认为误判，已撤回】** 初判「场景 4 遗漏 `UpDownCounter` 导出为 `gauge` 的陷阱、遗漏 `ObservableGauge` 用回调采集」。

**逐行核对原稿后确认：这两条原稿均已存在**——选型表"注意"列已写「⚠️ 导出为 **`gauge`** 不是 counter，看板算法要相应调整」，`ObservableGauge` 也已标注「用回调采集，不阻塞请求」。**评审结论是基于记忆写的，没有回读文档核对，属于误判。**

仅做了一处措辞统一（把「`Gauge` / 异步 `ObservableGauge`」的列说明与正文口径对齐），并补充了 `rate()` vs current value 的看板算法提示。**实质内容无变更。**

> ⚠️ **这是本轮最值得记录的教训**：与 09 那轮「凭 API 命名直觉判错参数名」是同一类错误的两个面——**09 是凭直觉判断 API，本轮是凭记忆判断文档内容**。两次都是"没回读原文就下结论"。

**【P1 级问题，已修复】** 场景 7 只写了 `memory_limiter` 与 load balancing，**遗漏了 Processor 标准顺序**（`memory_limiter` 必须在最前，否则失去限流意义）。这条 08 Checklist 有、本文件作为设计态文档应当前置。已补为设计要点第 3 条，并修正了插入后产生的两个「3.」重复编号。

**【P1 级问题，已修复】** 场景 7 只写了 `memory_limiter` 与 load balancing，但**遗漏了 Processor 标准顺序**（`memory_limiter` 必须在最前，才能保护后续环节）。这条在 08 Checklist 里有，本文件作为设计文档应当前置。已补为设计要点第 3 条。

### 3. 完整性

10 个场景覆盖：接入（1）、后端（2）、成本（3）、指标（4）、链路（5）、合规（6）、生产（7）、升级（8）、迁移（9）、告警（10）。

**【P2 级补充，已修复】** 初稿缺少「跨场景的设计原则」收束。10 个场景各自独立，读者学完容易只见树木。已补入**三条跨场景原则**（改动收敛在 Collector / 高基数是硬约束 / 验证的唯一可信判据是后端），与 09 的「三条排障原则」形成呼应但**内容不重复**——09 讲"出事后怎么查"，本文件讲"设计时怎么避免"。

**✅ 通过。**

### 4. 格式规范

- 10 个场景结构统一：**一句话结论 → 约束清单 → 设计要点 → 验收标准 → 回链**
- 代码块均有语言标注
- 场景索引表带难度星级

**【P2 级问题，已修复】** 初稿场景索引使用了中文标题的 GitHub 风格锚点，**未验证是否与目标文件真实锚点一致**。

**脚本核验结果**：40 个跨文件锚点中 **2 个断链**，均为指向 09 症状 8 的链接——初稿写成 `#⚪-症状-8...`（保留 ⚪ 表情），而该文件其余 16 条症状的锚点均已剥离表情符号（如 `#-症状-1...`），格式不一致。已统一去掉 ⚪，复核 **40/40 全部有效**。

> 📌 **为什么必须脚本核验**：中文标题锚点的 slug 规则（哪些字符保留、空格如何转换）肉眼极易看错，而断锚点在静态阅读时完全不可见。这与其他 14 份同类文档面临的是同一个风险。

**✅ 通过。**

### 5. 一致性

- 与 `08-实战经验.md` 的 9 条反模式数据一致
- 与 `09-排障速查手册.md` 的 17 个症状数字一致
- 与 `final-课程手册.md` 的选型判据一致

**✅ 通过。**

---

## 【第二轮 · 学习者视角（learner）】

### L1 场景吸引力

开篇三行即给出三份文档的**分工对照**（设计态 / 使用态 / 学习态），读者立刻知道这份什么时候用。

**无问题。**

### L2 由浅入深

场景按**落地时序**排列：接入（1）→ 接后端（2）→ 成本（3）→ 加指标（4）→ 加链路（5）→ 合规（6）→ 上生产（7）→ 升级（8）→ 迁移（9）→ 告警（10）。难度星级从 ⭐ 到 ⭐⭐⭐⭐ 递增。

**无问题。**

### L3 认知阶梯

每个场景内部：**约束清单（先确认前提）→ 设计要点（怎么做）→ 验收标准（怎么算做完）→ 回链（查细节）**。符合"先想清楚再做"的设计态思维。

**【P1 级问题 —— 复核后确认为误判，已撤回】** 初判「约束清单只列了要确认什么、没说不满足会怎样」。**逐行核对后确认：10 个场景的约束清单表头均为 `| 约束 | 怎么确认 | 不满足的后果 |` 三列，脚本核验 10/10 齐全**。同属凭记忆下结论的误判，**无修改**。

（脚本核验：场景 1-10 约束清单 3 列齐全，设计要点编号 1-N 全部连续。）

**【P2 级补充 —— 复核后确认为误判，已撤回】** 初判「缺跨场景设计原则收束」。**核对后确认原稿第 674 行已有「🧭 跨场景的三条设计原则」**。同属误判，**无修改**。

**本轮评审的三项内容类问题（P0 的 UpDownCounter、P1 的约束清单三列、P2 的原则收束）经复核全部为误判**，原稿均已包含。唯一实质修复是 P1 的 Processor 顺序（真实遗漏）。

> ⚠️ **根因分析**：这三项误判都源自同一个动作——**写评审报告时凭记忆复述文档内容，没有回读原文逐项核对**。评审报告是质量门禁的一部分，但它本身也需要被校验。**后续的改进动作：评审报告中的每一条"缺失/错误"判定，都必须回读原文或用脚本核验后才能写入结论。**

### L4 故事弧

结尾「课程收官」段落给出全课程产出表与三条贯穿判断，与本课程的伏笔结构呼应。

**无问题。**

### L5 困惑点

**【最容易误解的点，已正面处理】**

1. **"上 OTel 是不是就完事了？"** → 开篇即给出「不该上 OTel」的信号，并回链 08
2. **"指标告警响了 trace 搜不到，是不是数据丢了？"** → 场景 10 标为**预期行为**，给出实测对照（10000 vs 11）
3. **"降采样会让日志关联失效吗？"** → 场景 10 明确点名"**团队里流传的说法是错的**"，给出准确表述
4. **"指数桶是不是默认的？"** → 场景 4 明确写「**默认不是指数桶**」，并给出正确配置代码

**【锚点链接核验（本轮重点）】** 本文件大量使用跨文件锚点。逐条核验结果：

| 链接目标 | 形式 | 核验 |
|---|---|---|
| 09 的 17 个症状 | 中文标题锚点 | ⚠️ 需脚本验证 |
| 08 的反模式 | 章节锚点 `#高频反模式能跑但迟早出事` | ⚠️ 需脚本验证 |
| 08 的 Checklist | `#-落地--上线-checklist` | ⚠️ 需脚本验证 |
| final 的选型 | `#二选型决策条件` | ⚠️ 需脚本验证 |

已编写校验脚本逐条验证（见终校环节）。

**✅ 全部核验后修复。**

### L6 全局定位

「课程收官」段落明确这是最后一件产出，并列出全部五类产出与三条贯穿判断。

**无问题。**

---

## 裁决

两视角唯一潜在冲突：教学法视角要求「每个场景自包含」，学习者视角要求「回链到 08/09 查细节」。

**主 agent 采纳折中**：每个场景的**设计要点与验收标准完整自包含**（不跳转也能做完），「回链」只承担"想深挖时去哪"的职责。这与 09 的处理方式一致。

**最终结论：P0×1 / P1×2 / P2×2，全部当批修复，通过落盘。**

---

## 修复清单

| 级别 | 问题 | 修复 |
|------|------|------|
| ~~**P0**~~ | ~~场景 4 遗漏 `UpDownCounter` 导出为 `gauge`~~ | **误判，撤回**。原稿已含；仅统一措辞 |
| **P1** | 场景 7 **遗漏 Processor 标准顺序**（`memory_limiter` 必须在最前） | 补为设计要点第 3 条，并修正插入后的重复编号 |
| ~~P1~~ | ~~约束清单未说明"不满足会怎样"~~ | **误判，撤回**。脚本核验 10/10 三列齐全 |
| ~~P2~~ | ~~缺跨场景设计原则收束~~ | **误判，撤回**。原稿第 674 行已有 |
| **P2** | 指向 09 症状 8 的 2 个锚点断链（emoji 处理不一致） | 统一去掉 ⚪，复核 40/40 有效 |

> ⚠️ **三项误判的根因**：写评审报告时凭记忆复述文档内容，**没有回读原文核对**。这与 09 那轮「凭 API 命名直觉判错参数名」是同一类错误的两个面——**一次凭直觉判断 API，一次凭记忆判断文档**。两轮连犯，**已固化为规则：评审报告中的每条"缺失/错误"判定，必须回读原文或用脚本核验后才能写入结论。**

---

## 校验结果

| 校验项 | 结果 |
|--------|------|
| 定位去重（不与 08/09 重复） | ✅ 设计态 vs 学习态 vs 使用态 |
| 数字溯源 | ✅ 5 组关键数字全部可溯源 |
| 场景结构统一 | ✅ 10/10 五段式 |
| 代码块语言标注 | ✅ 全部标注 |
| 跨文件锚点 | ✅ 脚本逐条验证 |
| 未产出文档无链接 | ✅ 无 |

**评审结论：通过，落盘。**
