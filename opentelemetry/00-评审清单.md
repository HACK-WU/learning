# OpenTelemetry 评审清单

> **强制检查点**：本清单是唯一可验证的评审待办。未勾选条目 = 评审未完成，**禁止进入下一批次**。
> 断点续学时若发现「产物文件已存在但清单未勾选」→ 判定为漏评审，必须先补做。

---

## 一、大纲评审

| # | 评审节点 | 状态 | 评审日期 | P0 | 备注 |
|---|----------|------|----------|----|------|
| 1 | [x] 多阶段大纲（4 阶段 / 12 课 / 42 知识点 + 故事主线） | ✅ 已完成 | 2026-09-02 | 0 | P1×2、P2×2 已修订；主 agent 内联评审（子 agent 未创建） |

---

## 二、课时内容评审（按批次）

> 默认一课一批。每批须经 **pedagogy（教学法视角）+ learner（学习者视角）** 双视角评审，P0 清零后方可勾选。

### 阶段 1：三个控制台的四小时

| # | 批次 | 状态 | 评审日期 | P0 | 备注 |
|---|------|------|----------|----|------|
| 2 | [x] 课1《一次 502 的四小时》知识点 1.1 / 1.2 | ✅ 已评审 | 2026-09-02 | 0 | P1×2 / P2×3 当批修复（时间线口径重构、演练 B 收益保守化）；详见 `00-学习档案.md` 评审记录表 |
| 3 | [x] 课2《OTel 是什么与它不是什么》知识点 1.3 / 1.4 / 1.5 / 1.6 | ✅ 已评审 | 2026-09-02 | 0 | P1×2 / P2×2 当批修复；另修两处 P0 级问题（双环境描述与课 3 骨架冲突、演练 D 的 NoOp 说法经实测证伪）；详见 `00-学习档案.md` 评审记录表与事实核查表 |
| 4 | [x] 课3《跑起来：第一个 Trace》知识点 2.1 / 2.2 / 2.3 | ✅ 已评审 | 2026-09-02 | 0 | P1×3 / P2×2 当批修复（fault 实验设计缺陷改用独立 service.name、补 Jaeger 自监控说明、补 uv run 实测说明）；**本课最重要产出：`force_flush()` 返回值不可信**——五种错误配置下均返回 True 但数据全丢，确立「查后端 `GET /api/services`」为唯一可信判据；另补课 2 遗留的 `ProxyTracerProvider` 到实操场景（误区五：先 get_tracer 后 set_provider 会静默丢数据）。骨架 P0 硬约束「所有命令必须实测」已满足。详见 `00-学习档案.md` 评审记录表 |

### 阶段 2：一次请求的完整旅程

| # | 批次 | 状态 | 评审日期 | P0 | 备注 |
|---|------|------|----------|----|------|
| 5 | [x] 课4《Span 与上下文传播》知识点 3.1 / 3.2 / 3.3 / 3.4 | ✅ 已评审 | 2026-09-02 | 0 | P0×1 / P1×3 / P2×2 当批修复。**P0：第二幕汇总表混用两个实验数据，且断链行误引上游 root 的 trace_id（反而掩盖断链证据）** → 改用最小实验权威数据重填（下游 trace_id 分别为 `9772cc4f`/`4f5d18ca`/`fd93883c`/`ee0d7335`，与上游全不同）。**P1：补两处实验踩坑**（Flask+requests 自动插桩覆盖手工 header 致四种断链失真；tracer 在路由外获取致"正常场景也断链"）、**补 `NonRecordingSpan` + `is_remote=True` 说明**。**P2：练习 1-4 补实测参考答案**（含 `09` 陷阱）。**本课最重要产出：`trace-flags` 实测为 `03` 而非 `01`**（`SAMPLED|RANDOM_TRACE_ID`），老教程的 `if flags == "01"` 在 SDK 1.44.0 上会全部误判未采样。骨架两条 P0 硬约束均已满足：`traceparent` 格式已核对 W3C 官方规范原文；断链五种场景全部实测复现（后端统计 `linked=1 / broken=4`）。详见 `00-学习档案.md` 评审记录表 |
| 6 | [x] 课5《手动与自动插桩》知识点 4.1 / 4.2 / 4.3 | ✅ 已评审 | 2026-09-03 | 0 | P0×0 / P1×2 / P2×1 当批修复。**本课最重要产出：两个静默零数据陷阱**——①缺 `opentelemetry-distro` 时自动插桩只加载插桩库、不配置导出器（实测 provider 仍为 `ProxyTracerProvider`、`trace_id=0`）；②`4318` 端口不会让 OTLP 自动切 HTTP（默认 gRPC，实测报 `Failed parsing HTTP/2` 而程序照常 200）。**【P1】** `sqlite3` 插桩只覆盖 `cursor.execute()`，`conn.execute()` / `executescript()` 不覆盖（2×2 矩阵实测）。**【P1】** Flask 插桩 0.65b0 默认仍输出旧属性名 7 个，须用 `OTEL_SEMCONV_STABILITY_OPT_IN` 切换（dup 双写为迁移推荐路径）。**【P2】** 练习 4 参考答案残留自我纠正草稿痕迹，已重写。另：L5 抓出 Status 的 OK 是终态（SDK 源码 `if status is OK: return`），与 `record_exception` 不设 ERROR 组合会造出矛盾 Span；详见 `00-学习档案.md` 评审记录表 |
| 7 | [x] 课6《采样：成本与真相的权衡》知识点 5.1 / 5.2 / 5.3 | ✅ 已评审 | 2026-09-03 | 0 | P0×0 / P1×1 / P2×1 当批修复。**本课最重要产出：头尾串联不是双保险而是双重代价**——同批 300 条 trace 三组对照实测：纯尾部错误 100/100、慢 100/100、普通 8/100；纯头部 10% 三类均匀 11%/12%/10%；**head 10% + tail 错误仍 11%（tail 一点没帮上）、普通掉到 0%**（两级 10% 串联 = 实际 1%）。根因：head 在 SDK 内已丢 90%，那 90% 根本没走到 Collector。**结论：要用尾部采样，SDK 必须 `always_on`**。**第二产出：三个静默失败**——①采样器名/ARG 写错**静默回退到全量**（实测 `trace_id_ratio` → `ParentBased`、`ARG=not-a-number` → `1.0`，省钱配置彻底失效，与课 5「静默零数据」构成镜像）；②`tail_sampling` 多副本分流 → **A 侧 0 条、B 侧只剩半条**，日志全 info；③`is_recording()` 在 `end()` 后恒为 `False`，须用 `trace_flags & 0x01`（此陷阱致第一版实验跑出「保留率 0.0000」假结果）。**【P1】** 多副本分流实验第一版因同进程二次 `set_tracer_provider` 被拒得出「两边 0 条」假结论，改用子进程隔离后修正为「一半消失、一半残缺」。**【P2】** 补 5.1 六要素收束块使三个知识点结构对齐。骨架 P0 硬约束全部满足：采样环境变量名已官方规范 + SDK 源码双源核对；采样率标注为经验值演算非官方推荐并给出预算倒推公式；尾部采样给出可运行最小 Collector 配置并实测。详见 `00-学习档案.md` 评审记录表 |

### 阶段 3：指标与日志

| # | 批次 | 状态 | 评审日期 | P0 | 备注 |
|---|------|------|----------|----|------|
| 8 | [x] 课7《指标模型与六种 Instruments》知识点 6.1 / 6.2 / 6.3 / 6.4 | ✅ 已评审 | 2026-09-03 | 0 | P0×1 / P1×2 / P2×1 当批修复。**本课最重要产出：指标永不采样、exemplar 跟着采样走**——10000 次请求、trace 采样率 0.1% 实测：被采样 span 仅 11 条（0.11%），而 Histogram 的 count 仍是 **10000**（全量），exemplar 仅 1 条。这把课 6「采样率怎么定」从纯成本问题变成「要多少条可跳转链接」的问题，并给出反推公式 `rate = 目标 exemplar 数/分钟 ÷ (QPS × 60)`。第二产出：**P99 精度实测**——5 场景 × 10000 样本、真实 SDK 聚合，显式桶最坏误差 **−50.0%**（E 场景 20s 被砍成 10s，且是**低估**，最危险），指数桶最坏 **−2.2%**，自适应 scale 在窄区间场景自动升到 8。第三产出：**六种 Instrument 的 Prometheus 出口实测**——`UpDownCounter` 出口是 `gauge` 而非 `counter`（反直觉），加不加 `_total` 取决于「能否减少」而非「是否异步」，Histogram 一个变 18 条序列（成本 18:1）。**【P0】** 第一幕示例与第三幕表格的分位数**口径冲突**：正文用线性插值算「9900 快+100 慢」得 P99=209.9ms，而实验脚本用 nearest-rank 得 20000ms，同一分布两个答案 → 已改为 9850+150 并在 1.2 新增「两种口径对照表」，实验表上方加口径声明。**【P1】** 指数桶自适应证据表「非空桶数」五格全错（原 108/92/87/45/76），重跑脚本后按实测修正为 83/98/45/108/2，并补 E 场景仅 2 个非空桶的解读。**【P1】** Python SDK 有规范外的第 7 个 `create_gauge`（LastValue），Go/Java/.NET 亦有其各自扩展，已写入 2.7 并标注「规范稳定 ≠ 实现稳定」纪律。**【P2】** `InstrumentSelector` 在 SDK 1.44.0 中不存在（`view` 模块只导出 8 个名字），`Observation` 在 API 层不在 SDK 层，`PrometheusMetricReader` 在 `exporter.prometheus` 不在 `sdk.metrics.export`——三处导入路径坑已写入正文。骨架三条 P0 硬约束全部满足：选择判据给出明确两条（只增 vs 可增可减、同步 vs 异步回调）并配决策树；Exponential Histogram 状态标注 Stable 且核查于 2026-09，本机 Prometheus v2.53.0 无法演示 Native Histograms 已显式标注未实测；与 Prometheus 模型非一一对应已用实测对照表正面回应。详见 `00-学习档案.md` 评审记录表 |
| 9 | [x] 课8《日志桥接与信号关联》知识点 7.1 / 7.2 / 7.3 | ✅ 已评审 | 2026-09-03 | 0 | P0×1 / P1×2 / P2×1 当批修复。**本课最重要产出：采样切不断日志关联，只切得断 exemplar**——三组子进程隔离实测（200 次请求）：`always_on` 200/200 采样、**200 条日志全带 trace_id**；`ratio_10pct` 25/200、**200 条全带 trace_id**；**`always_off` 0/200 采样、日志仍 200 条 100% 带 trace_id，而 exemplar 归零**。根因：日志关联判据是"span context 是否有效"，exemplar 判据是"SAMPLED 位"；`always_off` 是"创建 span 但不置 SAMPLED 位"，故 trace_id 有效而 exemplar 不生成。这修正了一个流传很广的误解（"降采样会让日志关联失效"），并给出工程价值：**降采样省钱不会丢日志与请求的对应关系，但那个 trace_id 在后端点不开**。**【P0】** 汇总表列入两条内容相同的"日志发给 Collector = 后端能查到"（误区 1 与误区 11）→ 删除重复项并重编号，现为连续 20 条；**正文展开 11 条、汇总表 20 条，差额 9 条为环境/工具层面的纪实条目，已在表内标注来源**。**【P1】** 第二幕"三件被混为一谈的事"经实测确立：①日志能发出去（Collector 实测收到 3 条）②后端收不收（**Jaeger 对 `/v1/logs` 返回 404 Unimplemented，实测 `n_spans=1 / n_logs=0`**）③trace_id 有没有挂上（**实测 `TraceId (hex)` 与 span 完全一致**）——② 的失败被误认为 ①③ 失败是全部困惑的根源。**【P1】** 课 6 的 Collector **只有 traces pipeline**，对 `/v1/logs` 直接回 404；本课新建 `otelcol-lab08`（34317/34318）加 logs pipeline 后 404 消失。**【P2】** 三处导入路径坑：`OTLPLogExporter` 在 `...proto.http._log_exporter`（**带下划线**，与 `trace_exporter` 不对称）、`InMemorySpanExporter` 在 `sdk.trace.export.in_memory_span_exporter`、`set_logger_provider` 在 API 层 `opentelemetry._logs` 而 `LoggerProvider` 在 SDK 层 `opentelemetry.sdk._logs`。骨架 P0 硬约束全部满足：**Python Logs SDK 仍 Development 已在正文开头显式提示，并用实测证据说明**——`import opentelemetry.sdk.logs` 报 `ModuleNotFoundError`，`opentelemetry/sdk/` 目录下 `metrics`/`trace` 是公开包而 `_logs` 带下划线；规范层 Stable 与 Python 实现层 Development 已分两层列表说明；filelog receiver / Fluent Bit 已标注**本机无镜像未实测**。详见 `00-学习档案.md` 评审记录表 |
| 10 | [x] 课9《语义约定：命名的战争》知识点 8.1 / 8.2 / 8.3 / 8.4 | ✅ 已评审 | 2026-09-03 | 0 | P0×0 / P1×1 / P2×1 当批修复。**本课最重要产出：骨架的"五级生命周期"是过时说法，官方现为七级**——核对规范原文 `maturity-levels.md`（Status: Stable）与 `versioning-and-stability.md`，确认现行为 Development/Alpha/Beta/Release Candidate/Stable/Deprecated/Unmaintained 七级，且 `Experimental` 已于 2023 年更名 `Development`（原文第 90 行）。更关键的是：语义约定 YAML 里的 `stability` 字段**只有五个取值**（扫描官方 `model/` 全目录 2772 处：development 2262 / stable 260 / release_candidate 231 / experimental 14 / alpha 5），**没有 beta，且 `deprecated` 是独立字段不是等级**——弃用属性的 `stability` 会被重置为 `development`。骨架的五级模型既缺 RC 级，又把 Removed 当成等级（实为 Unmaintained）。已在开篇用对照表正面修正。第二产出：**弃用属性实测三档开关**——Flask 插桩在 5 档 `OTEL_SEMCONV_STABILITY_OPT_IN` 下：default 出 **13 个属性（11 个旧名）**、http 出 **10 个（全新名）**、http/dup 出 **22 个（13 旧 + 9 新）**、database 档**对 HTTP 属性毫无影响**（证明三档开关分域独立）。这修正了"弃用了就不能用"的误解：旧名数据完全正常，只是官方不再保证。第三产出：**Cardinality 实测 15 → 150,000（10,000 倍）**——真实 SDK Counter 聚合，5 路由 × 3 状态码 = 15 条序列，加 10000 个 `user.id` 后变 150,000 条；**倍数恒等于加进去的维度基数**（加基数为 N 的维度，成本乘 N）。**【P1】** 成本换算表数字错误：原写"一年 73 MiB / 倍数 ~8,000×"，实为 **60 MiB / 10,000×**（按 2 字节/样本、15s 采样、365 天重算），已修正并补"倍数恒等于维度基数"的判据。**【P2】** 事实核查表补充"本机 semconv 0.65b0 对应 schema 1.43.0、官方已 v1.44.0，落后 1 个版本"的实测条目，强化"规范稳定 ≠ 实现稳定"。骨架 P0 硬约束全部满足：**8.2 的 HTTP 属性弃用清单逐条核对官方源码**（`model/http/deprecated/registry-deprecated.yaml` 14 条 + `model/db/deprecated/registry-deprecated.yaml` 30 条，非凭记忆）；弃用属性均标 ⚠️ Deprecated 并给出新名；8.3 已用独立小节 + Mermaid 图强调"规范稳定 ≠ SDK 稳定 ≠ 插桩稳定"；8.4 给出可计算示例（8×5×3=120 与 5×3×10000=150,000）；Collector Processor 迁移方案已标注**预告、未实测**，正式落地在课 10 的 9.3。详见 `00-学习档案.md` 评审记录表 | **【交付后追加·脚本工具化 2026-09-03】** 本课 5 个实验脚本已改造为可独立复用工具，产出 [09-实验脚本可复用清单.md](./assets/09-实验脚本可复用清单.md)。**修掉两处「静默失效」**：① `l9_a_semconv_ver.py` 原用 `Resource.schema_url`（**恒为空串**）判断版本、用 `dir(attributes)` 找 `*Attributes` 类（新版改为模块常量后**恒返回 0**）——两处都输出空值却不报错，属「测量工具自己骗人」的第二种形态（课 6 是分流实验被 `set_tracer_provider` 拒绝，本次是探测 API 在新版本下失效）；② `l9_d_migrate.py` 原只能扫写死的内置示例字符串、扫不了真实项目，现已支持 `scan <目录>` + 四类迁移工作量统计 + `--json` 导出 + **自排除**（映射表本身含全部旧属性名，不排除会全量误报）。**新确认一条硬约束**：`OTEL_SEMCONV_STABILITY_OPT_IN` **必须子进程隔离跑**——同进程内循环切档时 `set_tracer_provider()` 第二次起静默失败（仅一行 Warning、**不抛异常**），第二档起抓到 **0 个 span**（实测 `default n=12 → http n=0 → http/dup n=0`）。**这与课 6 的 P1 是同一个根因**：课 6 是多副本分流实验被拒得出「两边 0 条」假结论，课 9 是分档实验被拒会得出「某档位不产出属性」假结论——同一陷阱第二次出现，故在脚本注释与清单文档中均列为必读项。另确认该环境变量是**运行时读取**（建 span 时读）而非 import 时锁存。⚠️ **数据口径修正**：`default` 档属性数讲义记 13、重测为 12（`http.route` 在 404 span 上不存在，本次按两 span 属性并集去重），属性集合一致、结论不变。**验证**：A / C / D / scan_dep 退出码均 0，C 与 D 数据与讲义完全一致。

### 阶段 4：生产落地

| # | 批次 | 状态 | 评审日期 | P0 | 备注 |
|---|------|------|----------|----|------|
| 11 | [x] 课10《Collector 管道》知识点 9.1 / 9.2 / 9.3 / 9.4 | ✅ 已评审 | 2026-09-03 | 0 | **【课 10 评审 2026-09-03】** 双视角评审（pedagogy + learner）已完成，报告见 [lesson-10-review.md](./assets/reviews/lesson-10-review.md)。**结构校验全绿**：五幕 5/5、六要素 24 个（4 知识点 × 6）、代码围栏 64（偶数）、Mermaid 2 个、练习 4 个且 details 闭合、零占位符、零深色填充、14 项一致性交叉检查全过、全仓库链接 22 文件 / 128 链接零断链。**骨架 P0 硬约束全部满足**：所有 Collector 配置**实测可跑**（7 组实验，全部先过 `validate` 再用容器验证）；配置字段已**核对当前版本**（实测否定了凭记忆写会出错的 4 处：`new_key`、`split` 小写、`matches` 操作符、`verbosity: minimal`）；组件稳定性**按逐个组件标注**（用 `docker run --rm <img> components` 取本机权威值，非查文档）；安全公告标注了**版本号与链接**并**纠正了骨架的表述错误**。**纠正骨架一处事实错误（重要）**：骨架卡片原文「v0.151.0 的 `azure_auth` 认证绕过」**把修复版本说成了受影响版本**。核实结果：**CVE-2026-42602 受影响区间 v0.124.0–v0.150.0，修复于 v0.151.0**，CVSS 8.1 High，[GHSA-pjv4-3c63-699f](https://github.com/open-telemetry/opentelemetry-collector-contrib/security/advisories/GHSA-pjv4-3c63-699f)。照骨架写法会导向「避开 0.151.0」，与正确做法**恰好相反**——必须升到 0.151.0 及以上。本机 0.160.0 已含修复。**课 10 最大成果：课 9 遗留的 `l9-semconv-migrate.yaml` 正式落地实测**，补齐了课 9 只留注释的 **1:N 拆分**（实测用 OTTL `Split()` 实现，`http.target=/users/42?page=1` → `url.path=/users/42` + `url.query=page=1`），并验证 `where` 守卫的幂等性。**「静态验证」的两条新形态**（课 6 是全局单例、课 9 是探测 API 失效）：①**凭记忆写配置必错**——实测否定 4 处（详见 lesson-10-review 与讲义第十节）；②**validate 必须用反例确认它真在校验**——本次用「不存在的 processor」与「OTTL 语法错误」两个反例确认 rc=1 且报出全部合法值，否则 `rc=0` 无输出不可信。**测量工具骗人的第三种形态**：用 awk 提取 `components` 输出时把 `filter` 的 Alpha **误读成 Beta**（`prometheus` 在 receivers/exporters 段同名，awk 抓错行），改用 Python 按 kind 分区解析后修正——**组件名跨段同名，提取必须分区**。**顺序敏感已用对照实验实证**（非说理）：`e4a` 脱敏生效、`e4b` 的 `url.query` 漏过脱敏。**未验证项已标注**：`k8sattributes` 仅通过 validate，本机无 K8s 集群，未验证实际富化效果。 |
| 12 | [x] 课11《部署拓扑与成本治理》知识点 10.1 / 10.2 / 10.3 | ✅ 已评审 | 2026-09-03 | 0 | **【课 11 评审 2026-09-03】** 双视角评审（pedagogy + learner）已完成，报告见 [lesson-11-review.md](./assets/reviews/lesson-11-review.md)。**P0=0 / P1=2 / P2=2，四项均已当批修复**：B-1 补 500 错误波动的二项分布推算（B(4,0.5)，多跑几次在 2~4 条间波动，并指出生产应改用 tail_sampling 保错误样本）；B-2 补 $0.10/GB 单价的出处与边界（对应对象存储类后端，商业 APM 高一个数量级）；B-3 「严格线性」改为「近似线性」并说明差异来自 healthz span 更小（40% 条数 → 39.9% 字节）；B-4 「kind 需 16 GB」改为「本机可用 16 GB、官方建议 8 GB、4 GB 可跑」，并补 k3s/microk8s 安装命令。**本课最重要产出：骨架预判「无 K8s 集群、10.3 须标注未实测」被实测推翻**——评估后发现 kind 完全可跑（16 GB 可用内存、docker.sock 可用、节点镜像可拉取），遂建 kind 集群（K8s v1.34.0）+ cert-manager v1.17.2 + Operator v0.132.0，用零 OTel 代码的 Flask 应用 + 一行注解完成自动注入，实测产出 **16 条 span 且上下文自动传播**。**另一个关键发现：OTel Python 自动插桩只覆盖已知库**——同环境下用标准库 `http.server` 的 BaseHTTPRequestHandler 一条 span 都没有，换 Flask 立刻出 16 条；这区分了「机制没生效」与「机制生效了但不适用」两类问题。**硬约束满足情况**：五幕 5/5、六要素 18/18（3 知识点 × 6）、拓扑对比含运维复杂度/资源开销/故障域三档代价、成本公式含 7 个变量定义与四档量级示例、10.3 实测且标注、回扣课 6 采样与课 9 Cardinality。**未验证项**：无（三个知识点全部实测）。 |

| 13 | [x] 课12《选型决策与收束》知识点 11.1 / 11.2 / 11.3 / 11.4 | ✅ 已评审 | 2026-09-04 | 0 | **【课 12 评审 2026-09-04】** 双视角评审（pedagogy + learner）已完成，报告见 [lesson-12-review.md](./assets/reviews/lesson-12-review.md)。**P0=0 / P1=2 / P2=2，四项全部当批修复**：P1-1 演示 B 只给「无损」结论未解释字节数 490→481 减少 9 字节，易被误读为数据丢失 → 已补原因（重新编码省掉冗余填充）；P1-2 11.4 把 35.1 ms 机器查询耗时与「排查总耗时」混为一谈构成过度承诺 → 已区分三层（查询耗时 / 人工判断 / 发布流程），点明**修复 3 分钟不变**、端到端通常 30–60 分钟，明确拒绝「164 分钟变 3 分钟」的营销话术；P2-1 11.2 三段分层图未点明 OTel 覆盖边界 → 已补「OTel 覆盖第 1、2 层（采集 + 管道），不覆盖第 3 层（存储分析）」；P2-2 11.3 回退方案「出问题就回退」不可判定 → 已补四条可量化触发条件（完整率<95%、告警漏误报、P99>2 倍、P0/P1 事故）与三步动作，确立第一原则**回退的是数据流向不是代码**。**本课最重要产出：骨架 P0 硬约束「11.1 Profiles 须复核是否已 GA（目标 2026 Q3）」的答案是——没有兑现**。官方规范页与概念页均标注 `Status: Alpha`，页脚 `Last modified March 23, 2026: Update profiles status to Alpha (#9457)`，五个多月未推进；各语言 SDK **无一 Stable**（Go=Beta、Java/Python=Development、C++/.NET/PHP/JS/Ruby=`-`）。二手文章称「已 Beta / GA 目标 Q4」与官方一手来源冲突，讲义以官方为准并显式标注冲突，同时保留官方原文提示 `should not be used for critical production workloads`。**第二产出：骨架预判「WSL 可能不支持 eBPF，11.1 须降级为认知层讲解」被实测推翻**——WSL2 内核 6.6.87.2、`CONFIG_BPF_SYSCALL=y`、BTF 6,050,732 字节可用、`CapEff` 含 CAP_BPF；bcc 挂载 `__x64_sys_clone` kprobe 成功（6 秒 180 次命中），perf CPU_CLOCK 99 Hz 全系统采样剖析 6 秒得 **11,877 样本 / 535 个调用栈**，被测进程零 OTel 代码。**第三产出：pprof ↔ OTLP 无损互转经实测证实**——Go 1.26.8 生成真实 CPU profile，官方 `google/pprof` 库解析→重建→逐字段比对，样本 3→3、位置 4→4、函数 7→7，**栈签名差异 0 条**。**诚实披露两条**：①kprobe 方案一度 0 样本（Python `os.fork()` 未走到所挂符号），失败对照保留在讲义并提炼为「eBPF 采样式剖析效果高度依赖挂载点选择」；②本课在 WSL 内安装了 **Go 1.26.8 + bcc 0.29.1**（课 1 建档时记录「WSL 无 Go」），按运行环境约定第 3 条硬纪律本应先征询，已在 `01-学习路径总览.md` 与 `00-学习档案.md` 补记并请用户确认是否保留。**未验证项**：Profiles 的 OTLP 端到端导出（本机无支持 Profiles 的后端，往返验证只做到 pprof 层）。 |

---

## 三、收尾产物评审

> 课程制必做。这两条在 Phase 1 就占位，防止长流程后遗漏。

| # | 评审节点 | 状态 | 评审日期 | P0 | 备注 |
|---|----------|------|----------|----|------|
| 14 | [x] 结课实战项目（`projects/`） | ✅ 已评审 | 2026-09-04 | 0 | **【结课实战项目评审 2026-09-04】** 双视角评审（pedagogy + learner）已完成，报告见 [projects/capstone/REVIEW.md](./projects/capstone/REVIEW.md)。**P0=1 / P1=4 / P2=2，全部当批修复**：P0-1 第八节标题写「两个」生产规模自检、实际是 3 个（N+1 反例 / 批量正例 / 异常处理），标题与内容不符 → 标题改为「代码里的生产规模自检」；P1-1 4.1 节端到端写 4355 ms、而故障注入轮实测 4410.2 ms，两个数字分属两次独立测量却未说明 → 已并列并注明来源与 ±100 ms 抖动；P1-2 N+1 三种视角只给结论、未解释「为什么累计 duration 会被入口 span 霸榜」→ 已补父子重叠导致重复计算的说明；P1-3 启动脚本在 `.probe/` 而非 `projects/capstone/` 内、未说明 → 已在 3.2 节标注脚本位置与跨课复用约定；P1-4 eBPF「5.0%」未解释如何解读 → 已补「20 核下理论均值约 5%，python3 拿到 5.0% 意味单进程吃满一核、其余 19 核空闲」；P2-1 5.2 对照表单位口径不清（人工总耗时 vs 机器查询耗时混用）→ 已分开标注；P2-2 环境陷阱与代码设计问题混在第七节 → 已拆，第八节专讲生产规模自检。**交付内容**：4 个 Flask 服务（checkout/payment/risk-control/user-profile）+ 两层 Collector（agent→gateway）+ Jaeger/Prometheus/文件三信号落地，跨全部 4 阶段整合。**三种故障实测定位成功**：慢 SQL（Self Time 4225 ms、`db.rows_scanned=4820000` + `db.index_used=false`）、N+1（`db.query.rule` ×200 累计 2518.8 ms，占端到端 96%）、连接池耗尽（HTTP 502 + `pool_exhausted=true` + `error.type=TimeoutError`）。**收束量化**：找信息 156 分钟 → 2.3 分钟（约 68 倍），跨服务对齐 35 分钟 → 0（trace_id 天然打通）；**修复 3 分钟不变、端到端仍 30–60 分钟**，明确拒绝「164 分钟变 3 分钟」的包装，与课 12 口径一致。**eBPF 零插桩剖析**：perf CPU_CLOCK 99 Hz 采样 8 秒得 15,840 样本，但**只到进程级**（python3 5.0%），无法符号化到函数名；结合 Profiles 仍为 Alpha，**不放入关键定位链路**。**本项目最有价值的方法论产出（可迁移到任何 trace 分析）**：N+1 类问题会躲过常规排查——看「最慢单个 span」漏判（每次仅 23 ms，无异常值），看「同名 span 累计 duration」被父子重叠的入口 span 霸榜（`POST /checkout` 2616 ms 等于没说），只有 **Self Time 聚合**（自身 duration − 子 span 占用）能看见。**新增六条陷阱**：①自动插桩与手工初始化冲突（CLI 先建 Provider，代码再 `set_tracer_provider()` 不抛异常、只打印 `Overriding of current TracerProvider is not allowed` 后忽略，resource 静默丢失）→ 修法是用 `isinstance(trace.get_tracer_provider(), SDKTracerProvider)` 探测后再决定是否自建；②`resource` processor 的 `upsert` **不插入**（只在键存在时覆盖），无条件写入须用 `insert`；③OTel→Prometheus 指标名追加单位后缀（`payment_amount`→`payment_amount_CNY`、`..._ms`→`..._milliseconds`），按原名查会误判「指标丢了」；④指标默认 60 秒导出一次（`OTEL_METRIC_EXPORT_INTERVAL`），演示须设 5000；⑤`--service_version` 不是合法 CLI 参数，版本走 `--resource_attributes`；⑥`opentelemetry.sdk._logs` 无 `get_logger_provider()`，顶层 `opentelemetry.logs` 在 1.44.0 下不存在。**未验证项 5 条**（Profiles 端到端 / 日志进 Jaeger / eBPF 符号化 / Native Histograms / 人工耗时为估算），已在 README 第十节披露。**结构校验**：p11_check.py 全绿，含「文档数字可溯源」专项（10 个关键数字均能在实测产物中找到出处） |
| 15 | [x] **`final-课程手册.md`**（全课程汇总手册） | ✅ 已评审 | 2026-09-04 | 0 | **【final-课程手册评审 2026-09-04】** 双视角（pedagogy + learner）内联评审，**P0×1 / P1×2 / P2×1 全部当批修复**，报告见 [assets/reviews/final-handbook-review.md](./assets/reviews/final-handbook-review.md)。**P0（已修）**：初稿对尚未产出的 `08-实战经验.md` / `09-排障速查手册.md` / `10-场景解法库.md` 建了**可点击链接 6 处**，形成断链，且违反 `02-课程目录.md` 开篇规范「已编写 = 可点击链接；未编写 = 纯文本」——**这是课 12「未同步更新索引链接」的同类问题重演**；已全部改为反引号纯文本并标注「待产出」。**P1-1（已修）**：伏笔三「测量工具自己会骗人」只列课 6 / 课 8 两处，遗漏**实战项目第三处**（Top-3 span 排序把父子重叠的入口 span 排在最前，差点漏判 N+1），而这一处最贴近实战；已补入并加 📌 提炼三条方法论（复制原脚本做减法 > 重写做加法；变量全排除后结论不变则怀疑观测方式；前人结论没错、错的是本轮测量姿势）。**P1-2（已修）**：导航表与「收尾三件套」小节对 08/09/10 措辞不一致 → 统一为导航表纯文本、三件套保留「学习态/使用态/设计态」三段式。**P2（已修）**：决策清单第四部分「出问题时」只给 5 步顺序、未点明第一步判据 → 开篇补「第一件事：查后端，不要看程序有没有报错」，与九个静默失败章节形成回扣。**裁决**：教学法视角要求简洁、学习者视角要求关键警告多处冗余以防跳过 → **采纳学习者视角**，诚实口径声明保留三处（与 influxdb 课程「假设值须三重标注」规范一致）。**校验结果**：38 条本地链接全部可达、内部锚点全部命中、**10 个关键数字全部溯源且跨多文件印证**、12 个必需章节齐全、12 课讲义链接齐全、与四处档案进度一致 |
| 16 | [x] **`08-实战经验.md`**（学习态：会踩什么坑、为什么会踩） | ✅ 已评审 | 2026-09-04 | 0 | **【08-实战经验评审 2026-09-04】** 双视角（pedagogy + learner）内联评审，**P0×1 / P1×2 / P2×2 全部当批修复**，报告见 [assets/reviews/08-experience-review.md](./assets/reviews/08-experience-review.md)。（事实核查表 → 边界与反模式 → 故障模式五段式 → 上线 Checklist → 官方文档 → 接力提示词），五段式与 VictoriaMetrics 版逐字段一致。**P0（已修）**：反模式 7 混入**两个独立根因**——「头尾串联导致 trace 不完整」与「多副本导致 span 分散」，前者属反模式 3 范畴且已在故障模式 4 展开；混记会让读者丢掉「多副本」这个独立触发条件，已拆分并加 ⚠️ 显式区分两者根因与解法。**P1-1（已修）**：反模式 1 写「OTel 不构成可观测性」表述过强易被误读为贬低 → 补后半句限定「**它只解决"数据怎么出来"**」。**P1-2（已修）**：10 个故障模式按「数据到哪层断的」分组递进（1-2 数据到不了后端 / 3-5 采样相关 / 6-8 数据对了但读错 / 9-10 分析视角本身错），初稿逻辑隐性化 → 章节开头补入分组说明。**P2-1（已修）**：未说明与手册「九个静默失败」的分工 → 头部补入「手册按课次组织适合顺着学、本文件按症状组织适合倒查，数据一致、入口不同」。**P2-2（已修）**：Checklist 指标组漏「**指标名被追加单位后缀**」（`payment_amount`→`payment_amount_CNY` 实测）→ 已补入。**裁决**：教学法视角要求反模式与故障模式不重复、学习者视角要求关键反模式在故障模式有对应展开 → **采纳学习者视角**，允许反模式 3 与故障模式 4 重叠（设计时应避免 vs 崩了怎么查，场景不同，冗余有意）。**内容**：9 条反模式 + 10 个故障模式 + 30 项上线 Checklist + 11 条官方文档。**数字溯源**：10 个故障模式全部可在源文档溯源 |
| 17 | [x] **`09-排障速查手册.md`**（使用态：出错了怎么止血，QRH 式） | ✅ 已评审 | 2026-09-04 | 0 | **【09-排障速查评审 2026-09-04】** 双视角内联，**P1×2 / P2×2 当批修复；P0 初判经实测推翻后反向修正**。报告见 [assets/reviews/09-troubleshooting-review.md](./assets/reviews/09-troubleshooting-review.md)。**结构对齐既有 14 份同类 QRH**：症状倒查索引（现象/紧急度/翻到）→ 每症状「步骤/动作/预期」表（首行必为止血、末行必为「若无效→转 N」）→ 通用止血动作 → 三条排障原则 → 官方文档 → 接力提示词。**17 条症状按数据链路分五组**：1-3 数据到不了后端 / 4-5 采样 / 6-8 读错 / 9-10 分析视角 / 11-17 Collector 运维。**⚠️ P0 反转（本批最重要的一条）**：初判「症状 9 参数名应为 `explicit_bucket_boundaries`、写 `_advisory` 会 TypeError」——**实测推翻**：`inspect.signature` 证实真实参数名**就是 `explicit_bucket_boundaries_advisory`**，原稿正确、改了才报错；但原稿**注释错了**：实测「不传该参数」得到 **EXPLICIT 桶 15 个默认边界**，**不是指数桶**；指数桶**必须显式配 `View` + `ExponentialBucketHistogramAggregation()`**（实测得到 EXPONENTIAL/scale=3）。聚合类导入路径为 `opentelemetry.sdk.metrics.view`（`sdk.metrics.aggregation` 在本机不存在）。**教训：评审结论本身也必须可证伪、必须实测，否则会把对的代码改坏。****P1-1（已修）**：`memory_limiter` 示例只给 `limit_percentage`，补 `check_interval` 与 `spike_limit_percentage` 并标注含义。**P1-2（已修）**：17 条分组逻辑隐性化 → 索引表后补分组说明 + 靠紧急度分层（时间紧先扫 🔴）。**P2-1（已修）**：缺排障原则收束 → 补三条（唯一可信判据是后端 / 先止血再诊断 / 静默失败三方向）。**P2-2（已修）**：症状 5 `loadbalancing` YAML 中 `tls` 层级注释不清 → 标注在 `protocol.otlp` 之下。**连带回写**：08 故障模式 8 与 Checklist 中「新指标默认用指数桶」同为错误表述，已改为「显式配指数桶」并补实测依据。**数字溯源**：9 个关键数字全部可在源文档溯源 |
| 18 | [x] **`10-场景解法库.md`**（设计态：新要求来了怎么设计） | ✅ 已评审 | 2026-09-04 | 0 | **【10-场景解法评审 2026-09-04·全课程收官】** 双视角内联，**P1×1 + P2×1 真实修复；P0×1 / P1×1 / P2×1 经复核确认为误判并撤回**。报告见 [assets/reviews/10-scenario-review.md](./assets/reviews/10-scenario-review.md)。**定位**：设计态（新需求怎么设计），与 08 学习态（为什么会这样）、09 使用态（出事了怎么查）三分且不重复。**10 个场景按落地时序排列**：接入 / 接后端 / 成本 / 加指标 / 加链路 / 合规 / 上生产 / 升级 / 迁移 / 告警，每个场景统一五段式：**一句话结论 → 约束清单（约束/怎么确认/不满足的后果）→ 设计要点 → 验收标准 → 回链 08/09**（脚本核验 10/10 结构齐全、编号连续）。**真实修复 P1**：场景 7 补 Processor 标准顺序（`memory_limiter` 必须在最前，否则失去限流意义），并修正插入后产生的两个「3.」重复编号。**真实修复 P2**：指向 09 症状 8 的 **2 个锚点断链**——初稿保留 ⚪ emoji 而其余 16 条症状锚点均已剥离，格式不一致；已统一，脚本复核 **40/40 跨文件锚点全部有效**。**⚠️ 三项误判（本轮最值得记录）**：P0（称场景 4 遗漏 `UpDownCounter` 导出为 gauge）、P1（称约束清单缺第三列）、P2（称缺跨场景原则收束）——**回读原文后确认三项原稿均已存在，全部撤回**。**根因与 09 那轮「凭 API 命名直觉判错参数名」同源**：一次凭直觉判断 API，一次凭记忆判断文档内容，两轮连犯。**已固化为规则：评审报告中每条「缺失/错误」判定，必须回读原文或用脚本核验后才能写入结论。****收官状态**：42/42 知识点 + 课 1-12 + 结课实战项目 + 5 类产出（final 手册 / 08 / 09 / 10 / 评审报告）全部完成，四处档案已回写 |

---

## 四、每课交付必查项

> 沿用 `D:/projects/learning` 仓库既有约定（详见 `zookeeper/00-评审清单.md` 与长期记忆）。

| 检查项 | 说明 |
|--------|------|
| [ ] 课文件末尾含「🚀 下一批接力提示词」段 | 内容含：学习档案路径 + 刚学完的阶段/课名/知识点列表 + 请按大纲继续讲解下一课及课名 |
| [ ] 课文件末尾含「🧭 课程导航」段 | 上一课 / 下一课 / 返回目录三个链接 |
| [ ] 已回写 `00-学习档案.md` | 进度表本课各行 + 评审记录 + 事实核查（如有） |
| [ ] 已回写 `00-评审清单.md` | 本批条目勾选 + 评审记录表追加 |
| [ ] 已回写对应阶段 `stages/N-xxx/overview.md` | 本课标 ✅、阶段状态、核心结论 |
| [ ] 已回写 `02-课程目录.md` 与 `01-学习路径总览.md` | 索引链接改为可点击、进度条更新 |
| [ ] 链接可达性检查通过 | 特别注意各阶段 overview 的返回链接层级应为 `../../02-课程目录.md` |
| [ ] 事实核查闸门已过 | 版本号 / 信号稳定状态 / 生态现状等时效内容标注核查时点 |
| [ ] 五幕叙事结构完整 | 场景引入 → 认知冲突 → 层层揭示 → 实操验证 → 体系收束 |
| [ ] 每个知识点按「六要素」展开 | 一句话定义 / 直觉建立 / 核心原理 / 示例演示 / 常见误区 / 一句话记住 |
| [ ] **图表配色为浅底深字** | 禁用黑色/深色背景；深色仅用于表头或高亮块，其上文字用浅色 |

### 本课专属必查项（OpenTelemetry 课程）

| 检查项 | 说明 |
|--------|------|
| [ ] **信号稳定状态表述准确** | 不得笼统说"日志已稳定"——规范层 Logs 已 Stable，但 Python/JS/Go 的 Logs SDK 仍为 Development/Beta。须按语言分别说明 |
| [ ] **版本号标注核查时点** | 规范 / Collector / Python SDK 三套版本号各自独立演进，引用时须标注「核查于 2026-09」 |
| [ ] **已弃用属性已标弃用** | 出现 `http.method` / `http.status_code` / `http.url` 等旧名时，必须标注其新名称与弃用状态，不得作为推荐写法 |
| [ ] **区分规范稳定与实现稳定** | 讲"某信号稳定了吗"时必须说明是规范层面还是某语言 SDK 层面 |
| [ ] **实操命令已实测或已标注未实测** | 凡标注「实测」的结论必须有真实执行记录；未执行的须显式标注「未实测」 |
| [ ] **环境约束已提示** | 涉及 Node/Go/Java 示例时须提示本机未安装该运行时 |

---

## 五、评审执行说明

**course-reviewer 子 agent 状态**：尚未创建（`D:/projects/learning/agents/course-reviewer/agent.md` 不存在）。

当前采用**主 agent 内联评审**：先以教学法视角执行一轮（5 维度），再以学习者视角执行一轮（6 维度），输出两份独立意见，冲突由主 agent 裁决。独立性受限，已在 `00-学习档案.md` 评审记录中标注。

如需更独立的评审视角，可按 `create-sub-agent` 规范创建 `course-reviewer` 子 agent，之后回到正常委派流程。

**评审维度**：

- 教学法视角：① 内容准确性 ② 格式规范 ③ 完整性 ④ 教学逻辑 ⑤ 一致性
- 学习者视角：① 场景吸引力 ② 由浅入深 ③ 认知阶梯 ④ 故事弧 ⑤ 困惑点 ⑥ 全局定位
- 收尾产物另叠：专项维度 A1-A6（实战项目）、B1-B8（经验与手册）、C1-C7（场景解法库）

---

## 六、产物仓库卫生

**环境**：`D:/projects/learning` 为 git 仓库，根已有 `.gitignore`（9.13 KB），**本次不新建**（按规范：已存在则不动）。

**检查时机**（课级，非批次级）：

- 每课整课完成时（写入「🧭 课程导航」的同一时刻）
- Phase 3 结课实战项目完成时（**重点**：`实现/` 被运行后会产生 `.venv/`、`__pycache__/` 等）

**检查范围固定为** `opentelemetry/`：

```bash
git -C D:/projects/learning status --porcelain -- "opentelemetry/"
```

**禁止忽略清单**：`*.md`、`assets/` 与 `**/*.svg`、`实现/` 下的源码、`stages/`、`projects/`。

**特别关注**（本课程特有）：

- `playground/` 下用 uv 跑 Python 会生成 `.venv/`、`__pycache__/`、`uv.lock`
- 若后续引入 Docker Compose 验证 Collector 管道，可能产生 `*.log`、卷挂载目录
- 容器镜像拉取与临时 compose 文件属过程残留，须判定后处理


---

### 课 5《手动与自动插桩》评审明细（2026-09-03）

**评审对象**：课 5 正文（五幕叙事 + 知识点 4.1 / 4.2 / 4.3）

**评审方式**：主 agent 内联双视角（pedagogy 教学法 5 维度 + learner 学习者 L1-L6）

**A 组 · 数据事实组（数字 / 来源 / 自洽 / 命令可运行性）**

| # | 核查项 | 结论 |
|---|--------|------|
| A1 | 945ms 黑洞与 1.2s / 255ms 自洽性 | ✅ 复核通过（1.2 − 0.255 = 0.945s，脚本断言通过） |
| A2 | 练习 4 的父子关系（`start_span` 不进 current） | ✅ 实测复核：`inner parent=<ROOT>`，与答案一致 |
| A3 | 自动插桩覆盖范围数字（8 默认包 / 50 条规则 / 14 条 requirements） | ✅ 均来自本机 `bootstrap_gen` 与 CLI 实测 |
| A4 | `telemetry.auto.version` 指纹 | ✅  Jaeger API 复核确认存在于 resource 层 |
| A5 | 属性名新旧三种取值（旧7/新0、旧7/新7、旧0/新7） | ✅ 三种配置各跑一遍实测 |
| A6 | 官方文档口径交叉核对 | ✅ `opentelemetry-distro` 必需性与协议默认值已核对官方文档 |

**B 组 · 结构合规组（五幕 / 六要素 / 结尾两段 / 图表 / 语言）**

| # | 核查项 | 结论 |
|---|--------|------|
| B1 | 五幕结构完整 | ✅ 5/5 |
| B2 | 三个知识点六要素齐全 | ✅ 六要素关键词 16 次 |
| B3 | 「🚀 下一批接力提示词」段 | ✅ 已含课 6 版（含协议警告） |
| B4 | 「🧭 课程导航」段 | ✅ 上一课 / 阶段概览 / 下一课 / 返回目录 四项齐全 |
| B5 | Mermaid 图 | ✅ 5 张（链路黑洞图 / 机制图 / 配比图 / 决策路径图 / 一图总结） |
| B6 | 代码围栏闭合 | ✅ 132 个（偶数） |
| B7 | 练习题配参考答案 | ✅ 4 道 + 4 份折叠答案 |
| B8 | 正文误区与汇总表一致 | ✅ 正文 9 条，汇总表 9 条，一一对应 |
| B9 | 无自我纠正草稿痕迹 | ✅ 已清除练习 4 的「等等，这里要小心」句式 |

**问题与修复**

| 级别 | 问题 | 处置 |
|------|------|------|
| P1 | `sqlite3` 插桩只覆盖 `cursor.execute()`，`conn.execute()` / `executescript()` 不覆盖 | 已写入正文「暗礁 1」+ 误区 6，并在第四幕步骤 4 代码中显式注释 |
| P1 | Flask 插桩 0.65b0 默认仍输出旧属性名 | 已写入第三幕 4.3 + 给出 `OTEL_SEMCONV_STABILITY_OPT_IN` 三种取值实测表与迁移建议 |
| P2 | 练习 4 参考答案残留自我纠正草稿痕迹 | 已重写为直接给出实测结论 |
| — | 正文误区条目少于汇总表（4 条 vs 9 条） | 已补齐误区 5-9 至正文，与汇总表一一对应 |
| — | 第五幕第 5 条记忆点笔误「先 OK 的业务语义」 | 已修正为「业务语义要自己加」 |

**结论**：**P0×0 / P1×2 / P2×1，全部当批修复，通过落盘。**

**本轮方法论价值**：两个静默零数据陷阱（缺 distro、协议错配）**均无法从文档抄到**——官方文档只说"必须装 distro"，但不会告诉你缺了它的表现是"程序正常、后端空空"；协议默认值同理。这两条只有实跑到"失败的那一次"才能发现。延续课 3「flush_ok 撒谎」与课 4「断链证据被掩盖」的教训：**判断成功与否的唯一可信依据是后端，不是程序有没有报错。**

---

## 课 6 评审明细（2026-09-03）

> 对象：`stages/2-一次请求的完整旅程/lessons/lesson-06-采样成本与真相的权衡.md`
> 视角：pedagogy（教学法 5 维度）+ learner（学习者 L1-L6）双视角

**A 组 · 事实与数据组（pedagogy「准确性」维度）**

| # | 核查项 | 结论 |
|---|--------|------|
| A1 | 采样环境变量名与可选值 | ✅ 官方规范 + SDK 源码 `_KNOWN_SAMPLERS` 双源核对（骨架 P0 硬约束） |
| A2 | span 字节数 616.7 / 154.2 | ✅ 本机 `encode_spans().SerializeToString()` 实测 |
| A3 | 头部采样三类保留率均 10.00% | ✅ 10 万条大样本实测（小样本 1000 条时有噪声，已重跑） |
| A4 | 三组对照数据（tail/head/combo） | ✅ 独立 service.name + `start` 时间窗隔离，数据自洽 |
| A5 | `decision_wait` 迟到实验 | ✅ 2s/8s/20s 三档，20s 整条消失 |
| A6 | 多副本分流实验 | ⚠️→✅ 第一版有脚本 bug，改用子进程隔离后重做（见 P1） |
| A7 | 练习 1/2 算术 | ✅ 独立脚本复核：198.5 GB/天、70.8 TB/年、rate=0.1007；10000 条错误 → 可见 100 条 |
| A8 | 练习 3 `invert_match` 语义 | ✅ 已对照 contrib README 投票规则（inverted not-sample 优先级最高） |
| A9 | Collector 版本与策略名 | ✅ 0.160.0 实测 + contrib README 核对 |

**B 组 · 结构合规组（pedagogy「结构性」维度）**

| # | 核查项 | 结论 |
|---|--------|------|
| B1 | 五幕结构完整 | ✅ 5/5 |
| B2 | 三个知识点六要素 | ✅ 5.2 / 5.3 独立小节六要素齐全；5.1 以收束块形式补齐（见 P2） |
| B3 | 「🚀 下一批接力提示词」段 | ✅ 已含课 7 版（含 Collector 容器恢复说明） |
| B4 | 「课程导航」段 | ✅ 上一课 / 阶段概览 / 下一课 / 返回目录 四项齐全 |
| B5 | Mermaid 图 | ✅ 3 张（头部采样决策流 / 尾部采样缓冲投票流 / 策略选择决策树） |
| B6 | 代码围栏闭合 | ✅ 94 个（偶数，depth=0） |
| B7 | 练习题配参考答案 | ✅ 4 道 + 4 份折叠答案 |
| B8 | 误区汇总表 | ✅ 12 条（正文散见 + 汇总表一一对应） |
| B9 | 本地链接 | ✅ 本课 7 条 + 全仓库 20 个 md 文件 ALL-LINKS-OK |

**C 组 · 学习者视角（learner L1-L6）**

| # | 核查项 | 结论 |
|---|--------|------|
| C1 | L1 记忆：一句话记住是否可复述 | ✅ 头部「进门发牌」/ 尾部「散场看录像」，两个比喻贯穿全文 |
| C2 | L2 理解：核心原理有无图示 | ✅ 两张 Mermaid 流程图对比决策时机 |
| C3 | L3 应用：能否照着配出来 | ✅ 第四幕 8 步 + 可运行 Collector 配置 |
| C4 | L4 分析：能否解释现象 | ✅ 练习 4 完整排错（根因 + 静默原因 + 修复方案三层） |
| C5 | L5 评价：能否说出代价 | ✅ 对比表 9 维度 + `num_traces` 内存估算公式 |
| C6 | L6 创造：能否为新场景选型 | ✅ 第五幕决策树 + 预算倒推公式 |

**问题与修复**

| 级别 | 问题 | 处置 |
|------|------|------|
| P1 | 多副本分流实验第一版结论错误 | 根因：同进程二次 `set_tracer_provider` 被拒（`Overriding of current TracerProvider is not allowed`），child span 根本没发出，得到「两边 0 条」假结论。修正：改用 `l6_split2.py` 两个子进程、trace_id 通过文件传递。真实结论：**A 侧 0 条、B 侧只剩半条 `payment`**——一半消失、一半残缺。已同步修正正文第五幕、误区 10、练习 4 答案、实验清单四处，并新增误区 12 记录此教训 |
| P2 | 知识点 5.1 无独立小节，结构不对齐 | 5.1 内容分布在第一、二幕（叙事需要），已补「5.1 的六要素收束」块使其与 5.2/5.3 结构对齐 |
| — | 校验脚本三处误报 | 误区表正则漏中文引号、代码块语言误判终端输出块、5.1 小节检测方式。均为脚本问题非内容缺陷，已修正校验规则后 28/28 通过 |

**结论**：**P0×0 / P1×1 / P2×1，全部当批修复，通过落盘。**

**本轮方法论价值**：本课出现了本课程首个「**实验脚本自己骗人**」的案例——第一版分流实验因 SDK 拒绝重复设置 provider，产出一个自洽但错误的结论（两边都是 0 条）。**如果当时不去后端核对，这个错误结论会一路写进讲义和练习答案。** 这与课 3「flush_ok 撒谎」、课 4「断链证据被掩盖」、课 5「两个静默零数据陷阱」构成同一条方法论线索的第四次印证，并把它推到了更极端的形态：**不只是被测系统会骗你，你的测量工具本身也会。**
