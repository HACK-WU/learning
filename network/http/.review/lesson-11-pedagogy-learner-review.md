# 第 11 课《HTTP/2 与多路复用》双视角评审记录

> 评审日期：2026-09-16
> 评审方式：主 agent 内联完成 pedagogy + learner 两轮检查；`course-reviewer` 子 agent 尚未创建，因此独立性受限（同一主体负责生成、检查与裁决）。
> 学习者视角证据性质：基于 `network/http/00-学习档案.md` 中“入门 / 理解概念 + 动手实操 + 决策参考”画像的学习者代入推理，非真实用户测试。

## 1. 评审对象与事实证据

| 对象 | 核验结果 | 证据位置 |
|---|---|---|
| 课文结构 | 五幕齐全；3 个知识点各有六要素、承接说明、行话对照、官方文档 | `lesson-11-HTTP2与多路复用.md:24-617` |
| 官方文档闸门 | RFC 9113、RFC 7541、RFC 9218、RFC 7301、MDN HTTP/2、Chrome Server Push 均在课首与知识点落链；写前校准表明确记录 4 个易混点 | `lesson-11-HTTP2与多路复用.md:5,13-20,196-202,423-431`；官方路由来自 `web-index/httpwg/topics/core-specs.md:13-16` |
| 核心机制 | 二进制帧头、流编号、HTTP/TCP 两层 HoL、HPACK 表与 Server Push 当前状态均与官方规范抽查一致 | RFC 9113 §2/§3/§4/§5/§5.2/§8.3/§8.4；RFC 7541 §2/§2.3/§6.2.3 |
| 真实实验 | Python 标准库实验通过；输出包含 24 字节连接前言、帧头解析、stream 3 DATA 先于 stream 1；curl 实测 `2` / `1.1` 对照 | `lesson-11-http2-framing-lab.py`；课文 `:452-500` |
| 图表 | 1 张零术语课级全局 SVG、每个知识点至少 1 张 SVG、1 张课末总结 SVG；XML 解析通过，无脚本/动画 | `stages/4-协议演进/assets/http2-*.svg`、`lesson-11-summary.svg` |
| 应用实战判定 | 不配独立应用实战，理由写入 4.1：真实迁移需要证书、ALPN、代理/CDN、浏览器矩阵和指标，硬凑会误导；4.1 已形成机制闭环 | `lesson-11-HTTP2与多路复用.md:435-505` |

## 2. Pedagogy 视角

| 维度 | 检查结论 | 处置 |
|---|---|---|
| 教学逻辑 | 先从 HTTP/1.1 多连接的场景进入，再按“帧/流/消息 → 多路复用与残留 HoL → HPACK/推送/优先级”递进；11.2、11.3 均有 `🧭 第 N/3 步` 承接 | 通过 |
| 内容准确性 | RFC 9113 的 9 字节帧头、stream 0、奇偶流 ID、窗口控制、TCP HoL 未被解决；RFC 7541 的静态/动态表与 never-indexed；Server Push“规范仍有、浏览器退潮”均对齐官方 | 通过；时间敏感内容标 `核查于 2026-09` |
| 完整性 | 阶段概览承诺的 11.1/11.2/11.3 全部兑现；HTTPS 与 `h2` 的 ALPN 关系、`h2c` 过时路径、优先级旧信令弃用均有落点 | 通过 |
| 格式规范 | 课首零术语全局 SVG + 读图指引 + 人话路线表；每个知识点先类比、再边界、再机制；每张图均有读图句；课末有误区、过时说法、总结图、命令速查、测验和接力词 | 通过 |
| 实操适配 | 实验使用 Python 3 标准库，无第三方依赖，不伪装成完整 HPACK 实现；curl 对照验证当前客户端的 h2/h1.1 协商 | 通过 |
| 应用实战判定 | 纯协议机制课不硬塞生产迁移篇；理由具体，且没有把“未配实战”留成隐性缺口 | 通过；无 P1 |
| 术语双轨 | “小包/订单/传送带”均回指帧/流；`binary framing`、`stream`、ALPN、HoL、flow control、HPACK、Server Push 讲透后都有标准叫法与遇到位置 | 通过 |
| 事实与敏感信息 | 历史年份和当前浏览器状态带核查月份；示例使用 `example.test`、`example.com` 和 `<TOKEN>` 占位；扫描未发现真实凭据、真实 IP、私有 URL | 通过 |

### Pedagogy 结论

P0：0；P1：0；P2：0。课文可进入交付，已同步勾选 `00-评审清单.md` 的课 11 条目。

## 3. Learner 视角（L1–L8）

> 代入角色：会写调接口代码、会用浏览器/curl，但说不清报文结构和连接机制的入门学习者。

| 维度 | 学习者走读记录（含具体位置） | 判定 |
|---|---|---|
| L1 场景吸引力 | 第 1 幕先给首页 HTML/CSS/JS/字体/图片和“37 个资源竞争 6 条连接”的日志（`lesson-11-HTTP2与多路复用.md:28-36`），我能理解问题且不需要先懂 HTTP/2；起源只占 1 段并标核查月份（`:26`），没有压过主线 | 通过；正面发现：日志让“为什么要多路复用”变成具体排队问题 |
| L2 由浅入深 | 先用整车/小包建立直觉（`:40,96-100`），再看帧头字段（`:104-125`），随后进入三层 HoL（`:223-237`），最后才读 HPACK 表状态和浏览器生态（`:315-390`）；难度逐步上升，没有第一幕突然甩 RFC 术语 | 通过 |
| L3 认知阶梯平滑度 | 首次读到“帧/流/消息”时紧跟订单类比与类比边界（`:90-100`）；首次读到多路复用时先讲传送带（`:211-219`）；HPACK/推送先讲菜单/配菜（`:296-312`）；术语之后都有标准行话锚定 | 通过；没有发现“术语先于直觉”的 P1 |
| L4 故事弧完整性 | 场景提出“想少开路又不想重新排长队”（`:38`），第二幕把“二进制是否自动变快 / HoL 是否全消失 / HPACK 是否加密 / 推送是否必快”拆开（`:48-59`），4.1 用帧和 curl 回扣，结尾指向 HTTP/3（`:502-513`） | 通过；未配应用实战的理由与知识点依赖匹配，不是缺练习而硬凑 |
| L5 困惑点 | 最大易混点是“HTTP/2 = HTTPS”，课文用 `https → TLS → ALPN → h2/http1.1` 图和 `h2c` 边界拆开（`:141-157`）；第二个易混点是“HTTP 层 HoL vs TCP 层 HoL”，用三层表和帧序列重复确认（`:231-237,265-277`）；顿悟点是 `[3] stream 3 DATA` 先完成，同时脚本最后仍保留 TCP 丢包边界（`:463-472`） | 通过；困惑被定位并在后文消解 |
| L6 全局定位清晰度 | 第五幕明确把课 10 → 本课 → 课 12 串起来（`:512-513`）：1.1 的多连接补丁、2 的帧/流/HPACK、3 的 QUIC；最后的复习口诀（`:534-540`）和接力提示（`:607-613`）让我知道接下来学什么、为什么 | 通过 |
| L7 入口可读性 | 一句话本质是零术语的“整份货 → 小包交错”（`:40`）；处境对照有 37/6 的可观测锚点并说明 TCP 丢包例外（`:42`）；全局图不出现帧/流/HPACK/TCP（`:65-71`）；地图只写 3 步任务，没有塞机制（`:73-81`） | 通过；入口可独立理解 |
| L8 节奏与密度 | 课内交替使用日志、对照表、SVG、Mermaid、HTTP/2 帧序列、真实实验输出、命令卡和 4 道小测；`11.1` 帧头表（`:110-119`）、`11.2` 三层表（`:231-235`）、`11.3` 静态/动态表（`:331-341`）都把可表格化信息做成表，没有连续文字墙 | 通过；正面发现：真实 curl 版本对照把“协商”从抽象概念变成可观察字段 |

### Learner 结论

L1–L8：均无 P0；无需要阻断交付的 P1。结论仅代表学习者代入推理，非真实用户测试。

## 4. 双视角裁决与交付结论

| 项目 | 结论 |
|---|---|
| 双视角冲突 | 无冲突。pedagogy 判断“承诺完整、机制准确”；learner 判断“入口可读、递进平滑、HoL 边界可定位”。 |
| 主 agent 裁决 | 因无冲突，不需要在两份意见之间取舍；保留“未配应用实战”的明确判定，避免把真实迁移简化成开关教程。 |
| P0/P1 | P0=0，P1=0，P2=0。 |
| 可交付状态 | 通过；已回写学习档案、评审清单、课程目录、阶段概览和仓库 `AGENTS.md`。 |

## 5. 验证命令摘要

```text
python3 network/http/stages/4-协议演进/labs/lesson-11-http2-framing-lab.py  -> PASS
curl --http2 https://example.com/  -> http_version=2
curl --http1.1 https://example.com/ -> http_version=1.1
SVG XML parse (5 files)             -> PASS
local Markdown link targets         -> missing=[]
official URLs (6 endpoints)         -> HTTP 200/302, all reachable
sensitive pattern scan              -> no hit
```
