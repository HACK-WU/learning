# 阶段 4·课 10 双视角评审记录

> 评审日期：2026-09-16
> 评审对象：[lesson-10-HTTP1.1与协议奠基.md](../stages/4-协议演进/lessons/lesson-10-HTTP1.1与协议奠基.md)
> 评审方式：主 agent 内联执行 `pedagogy` + `learner` 两个视角；`course-reviewer` 子 agent 尚未创建，因此独立性受限。学习者视角结论基于学习者代入推理，非真实用户测试。

## 评审证据

| 检查项 | 结果 |
|---|---|
| 写前官方文档闸门 | ✅ 课首第 5 行有核查留痕；写前从 `network/http/web-index/` 定位并核对 MDN Evolution of HTTP、Connection management in HTTP/1.x、Host、Transfer-Encoding，RFC 9110、RFC 9112、RFC 1945、RFC 2068、RFC 2616。 |
| 核心机制抽查 | ✅ RFC 9112 §9.3 的持久连接默认行为、§3.2 的 `Host` 要求、§6.3 的消息长度优先级、§7.1 的 Transfer-Encoding/chunked 与 RFC 9110 的语义分工均与正文一致。 |
| 历史事实抽查 | ✅ HTTP/0.9 一行协议、HTTP/1.0 的表达能力、RFC 1945（1996-11）、RFC 2068（1997-01）、RFC 2616（1999-06）、RFC 7230–7235（2014-06）、RFC 9110/9111/9112（2022）的时间线均有官方来源和月份级核查留痕。 |
| 实测纪律 | ✅ 原始 socket 实验最终真实跑通：同一连接两个请求、两个 `Host` 对应两个虚拟站点、chunked 正文还原、缺少 `Host` 返回 400；随后 `py_compile` 通过。首轮一次性发送两个请求曾暴露读取器未保留多余响应字节的问题，已修复后再评审。 |
| SVG 约束 | ✅ 5 张新增课级 SVG 均通过 XML 解析；无 `<script>`、`<animate>`、`<set>` 或 `javascript:`；全局图不把正式协议术语作为读者入口。 |
| Markdown 相对链接 | ✅ 正文中的 7 个本地链接全部解析到存在文件；从 `lessons/` 回课程根使用 `../../../02-课程目录.md`。 |
| 敏感信息扫描 | ✅ 未发现凭据、私钥、真实内网拓扑或非公开 URL；`example.com`、`alpha.test`、`beta.test` 等均为教学占位符。 |

## 教学法视角（pedagogy）

| 维度 | 具体检查与证据 | 结论 |
|---|---|---|
| 教学逻辑 | 第一幕先提出“重复建连、共用 IP、未知长度”的三个问题；第三幕按版本动因（10.1）→三件基础能力（10.2）→性能补丁与下一代天花板（10.3）递进；第四幕将三项能力放到同一实验中。 | ✅ 前置与后置顺序正确，粒度适中。 |
| 内容准确性 | 10.1 第 114–127 行核对版本史与现行 RFC 分工；10.2 第 227–289 行核对持久连接、`Host`、chunked 与消息边界；10.3 第 403–420 行明确域名分片在 HTTP/2 中的边界，不把“失效”绝对化为“所有场景禁止”。 | ✅ P0×0；未发现核心机制与官方文档冲突。 |
| 完整性 | 对照阶段 4 概览和课 10 骨架逐项覆盖：10.1 三版本动因与长寿原因、10.2 持久连接/Host/chunked、10.3 合并/内联/精灵/域名分片与 HTTP/2 重新评估。每个知识点均含定义、类比及边界、核心原理、示例、误区、记忆句和官方文档。 | ✅ 无遗漏。 |
| 格式规范 | 第 73–77 行是一眼全局 SVG；第 79–85 行是表格式 `📌 知识点导航`；10.1/10.2/10.3 各有对应 SVG 和读图指引；三处均有行话对照；课末含误区、小测、速查卡、接力提示和课程导航。 | ✅ 模板要素齐全。 |
| 一致性 | 与课 4 的持久连接/队头阻塞、课 8 的资源加载优化、课 9 的代理/CDN 与 `Host` 路由边界衔接；阶段 4 概览已同步修正为“HTTP/1.x 直接遗产，HTTP/2/3 换用帧与伪首部表达”。 | ✅ 未发现跨课冲突。 |

## 学习者视角（learner）

> 以下判断以入门、会用浏览器/curl、但说不清协议演进和报文边界的学习者为起点；每项均指向具体教学段落。结论基于学习者代入推理，非真实用户测试。

| 维度 | 具体阅读位置与体验判断 | 结论 |
|---|---|---|
| L1 场景吸引力 | 第一幕第 24–48 行用小航接手老系统、三个域名共用一个 IP、动态导出未知长度和一份示意日志制造问题；问题直接对应三个知识点，不是脱离主题的故事装饰。 | ✅ 场景可理解、可观察，并且先于定义出现。 |
| L2 由浅入深 | 第 24–68 行先从现象与误解开始；第 73–85 行用零术语全局图和三步地图；10.1 先讲历史，10.2 再讲报文规则，10.3 最后讲工程补丁；第 476–509 行以实验收束。 | ✅ 认知深度逐层增加，没有从历史直接跳到复杂实现。 |
| L3 认知阶梯平滑度 | 第一幕把“虚拟主机、chunked、持久连接”等问题先转成“正确站点、东西大小、门开着”的生活语言；10.1 第 98–106 行先给窗口类比；10.2 第 209–217 行先给门/门牌/分批送货类比；10.3 第 360–371 行先给购物清单绑大包类比。 | ✅ 正式术语都在直觉铺垫之后出现，且每个类比后说明失效边界。 |
| L4 故事弧完整性 | 第一幕提出三个现象，第二幕把错误直觉显性化，第三幕按三步回答，第四幕逐行解释真实输出，第五幕第 519–523 行把结论接到 HTTP/2。第三幕的 10.1/10.2/10.3 均有“第 N/3 步｜承接”。 | ✅ 五幕闭环，实操回扣场景。 |
| L5 学习者困惑点 | 主要难点在 10.2 第 227–236 行“持久连接仍需要消息边界”、第 256–263 行 `Host` 与 TLS SNI 的时间顺序、以及第 280–289 行 Transfer-Encoding 与 Content-Encoding 的区别；这些位置分别配有共同问题图、编号步骤和反例。顿悟点在第 316 行：“复用连接之前，先把边界讲清楚”，把三个看似独立的特性统一起来。 | ✅ 难点可定位且有承接，无 P0。 |
| L6 全局定位清晰度 | 第 515–523 行的一图总结明确“版本动因 → 1.1 三件遗产 → 并发天花板 → HTTP/2”；同时回指阶段 3 并交代课 11/12 的问题链。 | ✅ 能理解本课在整门 HTTP 课程中的位置。 |
| L7 入口可读性 | 第一幕第 46–48 行给出一句话本质与处境对照；第三幕第 73–77 行给出无正式协议术语的一眼全局图和读图指引；第 79–85 行用路线表说明三步目标，没有提前塞机制；入口图与第五幕总结图职责不同。 | ✅ 入口四要素齐全，适合入门者进入正文。 |
| L8 节奏与密度 | 版本时间线 SVG、三列特性 SVG、代价对比 SVG、原始报文、虚拟主机树、chunked 字节形态、真实实验输出、可复现性事故、小测交替出现；第 493–509 行尤其把“代码运行→输出→逐行解释→修复经验”连起来。 | ✅ 有真实实验和反例亮点，没有全课定义墙。 |

## 分级结论

| 级别 | 数量 | 处理 |
|---|---:|---|
| P0 | 0 | 通过交付闸门。 |
| P1 | 0 | 阶段概览中的“Host/chunked 今天每次都在用”和“旧补丁全部失效”绝对化表述已在写后评审中修正；正文无遗留 P1。 |
| P2 | 0 | 暂不追加范围外扩展。 |

## 最终裁决

**通过。** 双视角均无 P0，课 10 的正文、实验、图表、官方文档留痕和接力信息满足交付条件。独立 `course-reviewer` 尚未创建，本记录不宣称具备独立评审证据。
