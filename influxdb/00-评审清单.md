# InfluxDB 课程评审清单

> 强制检查点：每批内容生成后，先在本文件找到对应未勾选条目，完成双视角评审（A：技术事实核查；B：零基础教学体验）且 P0 清零后，才能勾选并交付。未勾选 = 评审未完成，禁止进入下一批。
> 每课交付必查项（评审之外的结构性检查）：讲义结尾必须包含「🚀 下一批接力提示词」（可复制续学 prompt）与「🧭 课程导航」两段。
> 评审结论完整摘要记入 [00-学习档案.md](00-学习档案.md) 的「评审记录」表，本清单只管勾选状态与极简摘要。

## 本课程的额外必查项

InfluxDB 3.x 迭代快、术语与版本敏感，评审时除常规项外必须核查：

1. **版本号与时间点**：凡出现版本号、`latest` 标签、catalog 升级等带时间语义的断言，必须有官方文档出处，并核对 [学习档案 · 事实核查记录](00-学习档案.md) 中已核条目
2. **术语一致性**：measurement / table 两个术语并存，全文须说明二者关系，不得混用而不加注
3. **概念防混淆**：凡出现「向量」二字，必须判明是 vectorized execution（向量化执行）还是向量数据库，并在语境中消歧
4. **可运行性**：Line Protocol、SQL、CLI、Python 代码须为可照抄运行的真实语法；`1` 是 float、`1i` 是 integer 这类精度陷阱必须显式标注

## 待评审清单

- [x] 大纲评审（阶段划分与依赖链）— P0=0 通过（2026-08-31，主 agent 内联 pedagogy）
- [x] **阶段 1 · 批次 1**（课1《为什么需要时序数据库》+ 课2《InfluxDB 是什么》，共 6 知识点）— 双视角评审 P0=0（P1×1、P2×4 已当批修复，2026-08-31）。均按五幕结构成文，含 📚官方文档 + 📋速查卡
- [x] **阶段 2 · 批次 1**（课3《环境搭建与第一次写入》，3 知识点）— 双视角评审 P0=0（P1×1、P2×2 已当批修复，2026-08-31）。P1：查询输出为构造非实测 → 改「三条判断标准」+ ⏳ 标注。P2：docker exec 前缀模式未解释 → 第四幕开头补统一说明。同步回补 L2 两处（72 小时约束、LVC/DVC 出处）
- [x] **阶段 2 · 批次 2**（课4《Line Protocol 与写入基本功》，3 知识点）— 双视角评审 P0=0（P1×1、P2×1 已当批修复，2026-08-31）。P1：auto 精度检测风险描述过度（"猜错就是几十年误差"）→ 经换算验证三阈值对齐 2128 年，改写三处并新增误区 4b。P2：时间戳范围来自第三方 → 加 ⏳ 置信度标注
- [x] 阶段 2 · 课5《Python 客户端与 CLI 工具》— 双视角评审 P0=0（P1×1、P2×2 已当批修复，2026-08-31）。**本机 venv 实装客户端 + inspect 核验**，订正档案 3 条（版本 0.20.0→0.21.0、新增默认 V2 端点与 no_sync 互斥、influx3 仅 query 子命令）。P1：实验 E 回调时序缺陷 → 补 sleep + 误区第 11 条。含阶段 2 出口检查清单 10 项
- [x] 阶段 3 · 课6《数据模型：table、tag、field、timestamp》— 双视角评审 P0=0（P1×1、P2×1 已当批修复，2026-08-31）。官方一手核实 7 条（**Core 库 5 / 表 2000 / 列 500** 硬限制、主键=null tag 不进主键、保留键三类后果、转义不对称、大小写三套规则）。P1：实验 A 的 `information_schema` 出自第三方 → 补 ⏳ 标注 + 两条降级路径。P2：实验 E 输出按实跑修正。含**本机实跑验证的命名自检脚本**
- [x] 阶段 3 · 课7《Schema 设计与基数陷阱》— 双视角评审 P0=0（P1×2、P2×1 已当批修复，2026-08-31）。官方一手核实：基数定义与**依赖 tag 不增加基数**（Glossary 原文 3×2=6、加 firstname 仍为 6）、**Core 页面全文 0 次提及索引**（本机抓取核实）vs Clustered 明文「不索引 tag 值」、宽/稀疏 schema 三类后果、`SHOW SERIES CARDINALITY` 类元查询**很可能不被 3.x 支持**、**首次写入决定物理列顺序且不可改**。P1：Core「未提及」与 Clustered「明文不索引」证据强度混用 → 补证据强度说明框；「既然不索引为何还要用 tag」缺结论先行 → 补结论块。含**本机实跑的基数估算器**（4 场景真实输出，可接 CI）
- [x] 阶段 3 · 课8《SQL 查询：从 SELECT 到窗口函数》— 双视角评审 P0=0（P1×2、P2×2 已当批修复，2026-08-31）。官方一手核实：`date_bin` 三参数与 **origin 默认 Unix epoch**、**gapfill 不支持 months/years/century**（date_bin 支持）、**gapfill requires time bounds in WHERE**、`interpolate`/`locf` **参数必须含聚合函数**、窗口函数四要素、**3.x 无 `increase()`/`NON_NEGATIVE_DIFFERENCE()`**（官方给 GREATEST+LAG+CTE 三段式替代）。P1：实验 A 代码块为片段而输出含实验 1-4（照抄跑不出）→ 补全为一一对应的完整脚本；gapfill 三选一缺前置结论 → 补"温度在变/状态不变/计数从零"。P2×2：实验 2「前两者相同」易误导 → 补"整除巧合"说明；sample-data 链接已核实 HTTP 200。含**本机实跑的 date_bin 模拟器**（4 场景，实验 1 输出与官方例子 18:18:18→18:15 完全一致）
- [x] 阶段 3 · 课9《InfluxQL 与 Flux：遗产与迁移》— 双视角评审 P0=0（P1×1、P2×2 已当批修复，2026-08-31）。官方一手核实（支持矩阵由脚本从 HTML 勾选图标逐条提取）：**3.x 仅 SQL 与 InfluxQL 两种语言，Flux 已移出清单**（官方出路 Flux to SQL converter beta）；InfluxQL **"rearchitected / ongoing"**；**基数类元查询 "likely not supported"**（理由：基数不再是瓶颈）；**`NON_NEGATIVE_DIFFERENCE()` 在 InfluxQL 标 ✅**（修正 L8 的语境问题）；v1→v3 模型映射；两端点仅路径不同。**P1**：实验 A 代码块为片段却声明"完整脚本见课件"（打印逻辑未实写，与 L8 同类问题）→ 补全为一一对应完整脚本。**P2×2**：实验 C 的 shell 函数与其注释自相矛盾 → 删除矛盾句；L10 课名为推测 → 核实后改为大纲真实标题《存储引擎：WAL、Parquet 与压实》。含**本机实跑的 InfluxQL↔SQL 对照模拟器**（4 场景，对照 2 证明带 `tz()` 的按天分组直译会错位 8 小时）
- [x] 阶段 4 · 课10《存储引擎：WAL、Parquet 与压实》— 双视角评审 P0=0（P1×2、P2×1 已当批修复，2026-08-31）。官方一手核实：**Core 确实没有 compactor**，三处证据（①storage engine 页直接否定句 *"InfluxDB 3 Core does not include it"*；②Core 三个核心页面搜不到 compact 一词；③`gen1-duration` 把 compactor 明确归属 Enterprise）。**量化后果：每 10 分钟一个文件 → 90 天 ≈ 12,960 个 → 永不合并**，官方 `query-file-limit` 的存在正是该约束的体现。写入五站参数（WAL 每 1 秒 / 可查缓冲最多 900 文件 = 15 分钟 / Parquet 每 10 分钟且留最近 5 分钟在内存 / 持久化的是最老数据）、`no_sync=false` 为默认值（改 true 即拿持久性换延迟）、WAL tail *"is durable"*、`--object-store` 六取值、优雅停机三动作与 `node-id` 唯一性、Core 节点状态仅 running/stopped。**P1×2**：①对照 5 的 `google`/`azure` 两行数据元组缺元素导致列错位，把两个**持久化**存储错打成「重启即丢」→ 修正脚本并重跑；②讲义代码块为手写精简版（缺对照 1/2/4 的官方原文打印），与声称的「严格一一对应」不符（**与 L9 同类问题第二次出现**）→ 补全为完整版。**P2×1**：「每天 144 个 Parquet 文件/表」中「每表」为未经官方确认的推论 → 弱化为不含量纲的表述。含**本机实跑的写入路径与持久性模拟器**（6 场景）
- [x] 阶段 4 · 课11《向量化执行：列存为什么快》— 双视角评审 P0=0（P1×1、P2×1 已当批修复，2026-09-01）。官方一手核实：**「72 小时」实为 432 个文件限制**（代码无任何时间判断，72h = 432×10min 派生值）；**超限是直接报错非变慢**（四条副作用 + 官方建议保持默认）；**`gen1-duration` 调小反而缩小范围**（1m→7.2h，10m 已是最少档）；**LVC 四条**（`count` 上限 10 / InfluxQL 不支持 `last_cache()` / 停止即清空且不自动回填 / 引擎不怕基数但 LVC 怕）；**`file-cache-recency` 默认 5h**；**DataFusion 默认 8192 行/批**。**P1×1**：实验 A「5.72 倍」易被误读为 SIMD 加速比 → 诚实说明前置到实验 A 最开头。**P2×1**：EXPLAIN 示例实为官方博客缩写版 → 补 ⏳ 标注。含**本机实跑的列存模拟器**（4 对照，端到端 8.62 倍）与**分区裁剪/432 模拟器**（4 对照，7 天即超限）
- [x] 阶段 4 · 课12《写入与查询性能调优》— 双视角评审 P0=0（P1×2、P2×3 已当批修复，2026-09-01）。官方一手核实：**批量写入双阈值 10,000 行或 10 MB**（四档行长实测**全部行数先到**，阈值分界 1048 字节/行）；**gzip 官方口径 "up to a 5x speed improvement"**（速度非压缩比）；**`--max-http-request-size` 默认 10mb 与 10MB 批量阈值撞车**（官方未明示压缩前/后，取保守策略）；**客户端 `WriteOptions` 默认 `batch_size=1000`**（比官方推荐保守 10 倍）；**`SELECT *` 官方门槛 1000+ 列 vs Core 上限 500 列 → 在 Core 上改它基本不变快**；**`system.queries` 字段与两条限制**（重启即丢 / 默认 1000 条且跨库驱逐）；**`--max-concurrent-queries` 可运行时调整**。P1×2：①「根本建不出明显收益的表」过绝对 → 补「500 列可能有可感知差距但远未达官方量级」；②「10MB 阈值基本用不上」过绝对 → 补超宽表/超长 string field 例外与「算平均行长」判据。P2×3：gzip 三方式补出处；`WriteOptions` 补 SKU 来源标注；实验 D 的 `${i:0:1}` 改为 100 点稳定写法。含**本机实跑的批量写入权衡模拟器**（5 组对照）与**慢查询诊断决策树模拟器**（5 场景 + 七步倒查法），以及**阶段 4 出口检查清单 10 项**
- [x] 阶段 5 · 课13《部署形态与容量规划》 —— 双视角内联评审 P0=0（P1×1、P2×2 当批修复）。核心：**432 文件 × 10min = 3 天，超限是报错不是慢**；**官方给新生产负载的默认答案是 Enterprise 不是 Core**（Core 定位为 edge / non-critical）；**原生 v3 API 与处理引擎 Core 也有，只用于排除 Cloud Serverless**；**内存三默认相加 90%**（4GB 机器只剩 0.4GB）；**升级 3.10+ 前必须备份 catalog（v2→v3 单向不可逆，且要先确认版本选路径）**。含**本机实跑的容量规划模拟器**（6 组对照）与**部署形态选型决策树模拟器**（6 场景 + 五条选型口诀）
- [x] 阶段 5 · 课14《降采样、保留策略与成本》— 双视角评审 **P0=1、P1=1、P2=2（均已当批修复，2026-09-01）**。官方一手核实：Core 保留期**查询时过滤 + 后台 retention-check-interval（30m）真删**；单位 `mo`=30 天 / `y`=365 天（非日历）、**不支持 m/s**、最短 1h、**`0`（0d/0h）= 立刻全删**（与 1.x/2.x 的 0d=永久相反）；保留期**数据库级**（表级为 Enterprise 3.2+）→ **每层分辨率必须独立建库**（falko 博客三库：mqtt 90d / power_1h 1y / power_1d 10y）；Core **无 compactor**、冷层=对象存储 Parquet（比内存/SSD 省 90%+）、**第三方引擎可直读 parquet**；downsampler 插件两模式（scheduled 必填 window / HTTP 回填 batch_size 30d）、11 种 calculations、三元数据列（record_count/time_from/time_to）、**同窗口重跑不重复**（measurement+tag set+timestamp 覆写语义）；删除四阶段（软删→重命名→24h 宽限期→硬删）。**文档冲突两处已双面呈现**：①data-retention 页称 immutable，但 v3.2.0 release notes 新增 `update database --retention-period`（#26520）+ GitHub 测试 `test_update_db_retention_period` 佐证 → 判为**文档滞后于实现**，讲义双面呈现并标 ⏳ 需 `--help` 自查，规划仍按不可改；②3.2 博客「72-hour grace period」vs config-options `--delete-grace-period` 默认 **24h** → 以 config-options 为准（`--hard-delete-default-duration` 已废弃）。**P0×1**：速查卡把第 3 层「every:6h → 1,460 文件」标为 ✅，实测 **1,460 > 432 上限应为 ❌**；且第 1 层「1,008（靠短保留期规避）」含糊 → 已重写整张表并补 Core/Enterprise 两种落法。**P1×1**：正文成本模型表只讲「省 95.9%」，未点破**「秒级 7 天」在 Core 上本身超限（1,008 > 432）**——该经典数字来自有 compactor 的环境 → 已补三形态对照表（Enterprise 配 7 天全可查 / Core 须压到 3 天 / Core 强配 7 天则第 4~7 天只能存不能查）。**P2×2**：①Q3 解析未点明 D 项双重错误（Core 无 `ALTER DATABASE` 语句 + 默认应按不可改规划）→ 已补；②新增 Q1 把 P1 的「7 天超限」发现固化为考题，并重排 Q1-Q5 题号。含**本机实跑的分层保留与降采样成本模拟器**（6 对照，核心结论：**降采样层能否查取决于调度周期而非精度**，90 天需 ≥5h 取 6h）、**Core 保留期硬边界检查器**（5 场景 + 10 项建库清单）。新增 12 条事实核查
- [x] 阶段 5 · 课15《处理引擎：Python 插件与触发器》— 双视角评审 **P0=5、P1=0、P2=0（均已当批修复，2026-09-01）**。官方一手核实：**WAL 触发器绑 WAL flush 事件、默认每秒 1 次 = 一天 86,400 次**（与数据量无关，是 `every:1d` 的 8.6 万倍）；**`plugin-dir=""` 空字符串不禁用处理引擎**（须 unset/注释）；**所有插件共享同一 Python 进程**（官方原文，插件性能预算从写入侧扣，回扣 L13）；**取消信号是 `KeyboardInterrupt`（`BaseException` 子类）**，`except Exception` 吞不掉，且 `QueryError` 无法 import（`except QueryError` 会 `NameError`）；**`gh:` 非 GitHub 专用且每次触发器启动都重新拉取**（供应链风险）；**Notifier 是发送端**，五个检测器 `required_plugins` 均为 Notification sender → **告警必须两件套**；**Timestream 不允许自定义插件**；**缓存重启即清 + 不设 TTL 永不过期**。**P0×5 同源**：实验 A 对照 4 循环结束后 `rows` 已是**第 21 秒**值却打印成「第 20 秒」→ 讲义 7 处统一引用了错误的「20 秒 1 亿行」，且**课后小测 Q3 答案「14 秒」也错**（正确 **15 秒**，第 n 秒 = `100×2ⁿ⁻¹`，第 1 秒未翻倍）。修复：脚本加 `flushed_at_20` 改印两行（20 秒 **52,428,800** / 21 秒 **104,857,600**），重跑后**整体重生成**源码块与输出块，全文统一 7 处口径，Q3 答案改 15 秒并补「易错点」。**文档冲突三处已双面呈现**：①`row["host"]`（API reference，扁平 dict）vs `row["tags"]["host"]`（官方博客，嵌套）→ 采信前者，后者会 `KeyError` 且被 `--error-behavior log` 吞掉；②`process_request` vs `process_http_request` → 采信前者；③官方插件 11 个（Core 文档页）vs 23 个（Plugin Directory）→ 两者皆官方口径，未裁决。含**本机实跑的触发器频次与资源占用模拟器**（5 组对照）与**插件代码静态检查器**（六条铁律 CI 门禁，AST 扫描，有 P0 则 exit 1）。新增 15 条事实核查
- [x] 阶段 5 · 课16《生态集成：Telegraf、Grafana 与自监控》— 双视角评审 **P0=2、P1=1、P2=2（均已当批修复，2026-09-02，阶段 5 收官）**。官方一手核实：**`outputs.influxdb_v3` 自 Telegraf v1.38.0 引入**；**多 URL = 故障转移（每 flush 随机挑一个），非双写非负载均衡**；**`database_tag` 目标库数 = 该 tag 去重值个数 → 撞 Core 5 库上限**；**`sync=false` 即 L10 的 `no_sync=true` 换名**；**`influxdb_v2` 写 Core 时 `organization` 必须空串**；**Grafana Product 选 InfluxDB Enterprise 3.x**（官方原话 *"currently, no Core menu option"*）；**SQL 走 Flight SQL(gRPC) 必须 HTTP/2，InfluxQL 走 HTTP/1.1 不受限**；**`/health`/`/ping`/`/metrics` 默认需认证，且 `/ping` 必须 GET（HEAD 返回 404）**；**`system_metrics` 插件采主机非数据库**（依赖 psutil）；遥测默认每小时上传、可关闭。**P0×2**：①**实验 B 输出块与实跑不一致**——「对照 3」写 5s 单面板 **432,000**，实跑为 **86,400**（差 5 倍，口径混用了「每面板 5 query」与官方算例的「每面板 1 query」）→ 脚本改整除、脚本内显式标注口径、同步知识点 2 与速查卡 5，全文校验旧值清零；②**修复脚本 marker 重复导致讲义段落被误删**（锚点在实验 A/B 各出现一次，替换越界）→ 精确重建。**P1×1**：实验 B 初版只贴对照 1、其余三组仅给核心表 → 已改为**完整 stdout 逐字回贴**（161 行）。**P2×2**：①第五幕 L17 预告与阶段 6 overview（SSOT）候选名单不一致 → 已按 SSOT 修正；②实验 C 第 3 步未体现 `/ping` 的 GET 约束 → 已补 `curl -I` 反例。**四向逐字一致性校验通过**（针对 L15 五个 P0 的根因增设）：讲义 python 块 ↔ 脚本文件（356/245 行）、讲义 text 块 ↔ 运行时 stdout（102/161 行）全部逐字一致；围栏 36 全闭合、details 21/21、题号 1-5 连续。含**本机实跑的 Telegraf 配置体检器**（14 条规则，有 P0/P1 则 exit 1，纯标准库 `tomllib`）与**自监控覆盖率与查询成本模拟器**（4 组对照）。新增 13 条事实核查
- [x] 阶段 6 · 课17《横向对比：五款候选》— 双视角评审 **P0=2、P1=1、P2=2（均已当批修复，2026-09-02，阶段 6 开篇）**。官方一手核实：**Prometheus 官方 FAQ 说拉模式只是 *\"slightly better\"* 且 *\"should not be considered a major point\"***；**Prometheus 官方说本地存储不集群不复制、*\"not meant as durable long-term storage\"*，默认 15d、1-2 字节/样本**；**VictoriaMetrics 官网 20x/10x/70x/7x 全是厂商自述且带 `up to`**；**ClickHouse 官方说 *\"Partitioning does not speed up queries\"***（剪枝靠 ORDER BY 稀疏索引，按客户 ID 分区触发 `Too Many Parts` 默认 3000，ORDER BY 建后不可改）；**TimescaleDB 连续聚合压缩仅 61%**（bytea 存部分聚合）；**VM 官方自述定位 *\"long-term remote storage for Prometheus\"***，支持 line protocol，**Remote Write 2.0 不支持**，MetricsQL 无 JOIN。**P0×2（两个都是「方法论懂了、手上做错」）**：①实验 A 给 VM 压缩打满分 5 分、理由却写「⚠️ 厂商自述，慎引」→ **嘴上说慎引、手上打满分**，已降 4 分并把自检写进脚本输出；②实验 B 把「Core 只能查 3 天」当性能宣称审，判成「⚠️ 不能裸写数字」——**而它是全课最硬的排雷依据** → 根因是审计器缺分类维度，新增 **Q0 三类分治**（benchmark 六问全审 / spec 只审来源与版本 / misread 一律判死），脚本升级 v2（10→11 条，可写从 0 条变 3 条）。**P1×1**：Telegraf 插件数官方自相矛盾（文档页 **400+** vs GitHub README **over 300**）→ 全文 8 处统一「300+（保守口径）」+ 新增冲突记录第 4 条 + 官方文档表补 2 条来源。**P2×2**：①实验 B 解读段引用旧结论（10 条 / 0 条可写）→ 同步 v2；②重跑脚本只替换 text 块未同步 python 块（校验器抓出）→ 补 `replace_py_block`。**四向逐字一致性校验通过**：讲义 python 块 ↔ 脚本文件（459/391 行）、讲义 text 块 ↔ 运行时 stdout（153/163 行）全部逐字一致。**官方文档冲突 4 条双面呈现**（TimescaleDB 压缩率 / VM 倍数 / Prometheus 长期存储两句话 / Telegraf 插件数）。含**本机实跑的五款候选选型决策打分器**（4 场景 × 6 维度，先排雷再打分，**四个场景四个不同冠军**）与**基准数字口径审计器 v2**（11 条宣称 × Q0 分类 + 六问三档）。新增 7 条事实核查
- [x] 阶段 6 · 课18《迁移指南：从 1.x/2.x 到 3.x》— 双视角内联评审 **P0=0、P1=5、P2=5（均已当批修复，2026-09-02）**。官方一手核实：**术语映射官方用词 `is synonymous with`**（bucket=database、measurement=table）；**InfluxQL DBRP 倒灌命名 `db/rp`**；**`precision=m/h` 仅 v1 端点支持**且**默认精度 v1=纳秒 vs v3=auto**（换端点不改代码=换解释方式）；**导出必须 `-lponly`**（否则整批失败）；**Flux 无直接兼容层**（官方 *"regrettably cannot offer"*），出路 Explorer 1.9 converter（beta，AI 生成须复核，且 Flux `map()` 保留所有列而 SQL 只选写的列）；**插件系统是 CQ/Tasks/Kapacitor/Telegraf 的 natural successor**；**3.10 catalog v2→v3 单向迁移**（备份路径按 3.4.0 分界，旧目录是 leftovers 非有效回滚源，官方称恢复备份是 the only way）。**P1×5（全部当批修复）**：①**静默项口径混淆**——保留期专项把「语义反转」与「幅度偏差」都标同一个「静默」，后果差一个量级 → 新增 `kind` 字段分 `reverse`/`drift` 两类，案例 5→7（补降采样库 432 超限、合规库 3y 两个同构场景），并标明清单「2 条静默」与专项「4 条静默」**口径分开不合并**；②**版本升级在七阶段里无处安放** → 补独立升级路径（四步，不可逆点在第 ④ 步启动 3.10，比数据迁移 S7 靠前得多）；③**3.11.0→3.11.0 案例自相矛盾** → 改 3.10.0→3.11.0，并给决策器补 ≥3.10 分支；④**磁盘开销未估** → 补估算表（1.2 倍预留），点明行协议无压缩、导出体积大于 TSM 内部存储；⑤**双写时长无判据** → 补算法（下限=对账周期 1-3 天，上限=可查窗口，Core 仅 3 天 → **双写窗口实际只有 1-3 天**）。**P2×5**：报错原文缺失 / M05 organization 边界 / 事实一缺检查动作 / CQ 被移除无出处 / 解读段未同步新内容。**四向逐字一致性校验通过**：python 块 ↔ 脚本（**500/471 行**）、text 块 ↔ stdout（**266/212 行**）逐字一致；围栏 44 全闭合、details 5/5、题号 1-5 连续、占位符清零、链接 10 个 0 死链。含**本机实跑的三代差异清单检查器**（17 条 × 三档 + 保留期专项 7 案例两类静默 + 翻译器自检）与**迁移演练与回滚规划器**（七阶段可逆性矩阵 + catalog 备份路径决策 + 回填批次规划 + 磁盘估算）。新增 16 条事实核查
- [x] 阶段 6 · 课19《场景演练与选型决策》 —— 2026-09-02 交付。双视角内联评审 P0=2 / P1=3 / P2=4，全部当批修复。实验 A（决策树引擎 607 行 / 245 行输出）与实验 B（TCO 规划器 515 行 / 282 行输出）本机实跑、逐字回贴；四向一致性校验通过（607/515 源码、245/282 输出）；本地链接 7 个 0 死链。

- [x] **结课实战项目**（Phase 3 · `projects/时序数据平台落地/`）— 2026-09-02 交付。双视角内联评审 **P0=2 / P1=3 / P2=3，全部当批修复**。跨 **4 个阶段**（3/4/5/6，门槛 ≥3）；5 个真权衡决策点；反例 `bad_example.py` 实跑 rc=0 而正确版 rc=1（**能跑通 ≠ 检查到位**）；四份产物齐备（README 118 行 / 设计决策 141 行 / 反例对照 102 行 / 验收清单 65 行）+ 7 个实现文件（1,016 行）。**P0-1**：迁移与保留期检查无条件全量告警（不管实际写的是 `90d` 还是 `0d`）→ 一个用来教 reverse/drift 分类的模块自己不做分类判断，**与 L17「标注慎引却打满分」、L19「批评想托管却推荐自托管」同类，是该错误的第三次出现** → 改为命中检测（命中 reverse=P0 / drift=P1，未命中降 INFO 保留检查项）。**P0-2**：drift 严重度按绝对天数判（`diff >= 1`）导致 `1y` 差 0.2 天（0.07%）在有合规要求时判 P0，而 `3mo` 差 1.3 天（1.45%）判 P1 —— **严重的排在轻微的后面** → 改按相对偏差 ≥ 0.3% 判。**P1×3**：①硬编码 `0.012` / `240.0` 散在业务模块，而设计决策 3 声称常量集中在 config（文档说一套代码做一套）②AST 校验抓出 `engine.py` 在讲「约束要集中管理」的文案里自己硬编码 `432` 做运算 ③TCO 分项之和 ≠ 展示总数（写入费未列出）。**P2×3**：保留期字段缺失无法检测 / README 汇总数字与实跑不一致 / 退出码语义未文档化。**本机实跑**：`main.py` rc=1（491 行输出，三场景 P0=21 P1=21 P2=7 INFO=40）、`main.py iot` rc=1、`main.py nope` rc=3、`bad_example.py` rc=0；链接 21 个 0 死链；知识点地图 16 个回指链接 0 死链
- [x] **实战经验 / 排障速查手册 / 场景解法库**（Phase 5 · `08-` / `09-` / `10-` 三份一次生成）
- [x] **课程手册汇总**（Phase 4 · `final-课程手册.md`）

## 评审记录（勾选后追加极简摘要）

| 日期 | 评审对象 | 评审方式 | P0 数 | 意见摘要 |
|------|----------|----------|-------|----------|
| 2026-09-02 | 阶段 6 · 课19（3 知识点，**全书收官**） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮） | 2 | **P0=2 / P1=3 / P2=4，全部当批修复。****P0-1：约束归属错误** —— 实验 B 把 Core 的 432 文件上限无条件套用到全部五个 SKU，对 IoT 场景（60min × 365 天 = 8,760 文件）输出「最小调度周期需 ≥ 1216.7 分钟」（≈20 小时）的荒谬结论。根因：**把 A 的约束当成整个产品的约束** → 新增 `SKU_FILE_LIMIT` 按 SKU 分列（Core 硬约束 vs 其他 SKU 有 compactor 免检），结论变为「只有选 Core 才需要改周期」。**P0-2：TCO「最省方案」与实验 A 的 BLOCK 结论互相打架** —— 成本排序不能推翻可行性结论→ TCO 对比前加口径对齐声明（本表只比成本，不比可行性）。**P1-1：实验 A 的 `want_managed` 只用于排雷、未进打分逻辑**，导致「想托管 + 1 人运维」的 hybrid 场景仍被推荐自托管 Enterprise，**而讲义正文正在批评这件事**（与 L17「标注慎引却打满分」同类错误）→ 想托管而选自托管直接判 1 分，并新增**约束冲突检测**（步骤 1.5）让脚本自己报出「约束之间打架」（hybrid / IoT 两场景触发：想托管 + 原生 v3 API + 处理引擎，无便宜解）。**P1-2**：讲义未同步冲突检测结论。**P1-3**：速查卡 1 未含冲突检测（已扩为六步）。**P2×4**：脚本行数标注过时 / 实验 A 流程描述缺步骤 / 误区表未含冲突类（补第 25、26 条）/ 场景四说明与脚本行为不一致。**四向逐字一致性校验通过**：python 块 ↔ 脚本文件（**607 / 515 行**）、text 块 ↔ 运行时 stdout（**245 / 282 行**）逐字一致；围栏 44 全闭合、details 5/5、题号 1-5 连续、占位符清零、本地链接 7 个 0 死链。**官方文档冲突 3 条双面呈现**：①Enterprise 库表上限（官方博客 100+/10k+ vs 文档页未给数字 → 取博客下限并标 ⚠️）②Docker `latest` 日期官方自给多个 → **不引用日期，只采用行动建议**③Serverless 是否有长程查询 → 保守按「有 compactor」处理并标 ⚠️。**本批最有价值产出**：①**约束是有归属的** —— 432 上限是 **Core 的属性**不是 InfluxDB 的属性，此为「约束归属」检查法（**把 A 的约束套到 B 上，和把 A 的优点算到 B 头上，是同一类错误**）；②**选型是两次决策不是一次** —— 「我们选了 InfluxDB」没有信息量，五个 SKU 硬约束差异极大，四个场景三个把 Core 挡在门外，且 **Serverless 不是 Core 的托管版**（无原生 v3 API、无处理引擎）；③**打分器会输出冠军，但不会告诉你约束本身有问题** —— 新增约束冲突检测区分「排雷」与「冲突」；④**成本排序会被授权费翻转** —— Enterprise 授权费不公开（❓ 不估算），IoT 场景运维人力（0.5 FTE = $7,500/月）是主机费（$240/月）的 **31 倍**。**学习者视角**：L1「评审会拍板『那就 InfluxDB 吧』，第三周报表跑不出来」一句话揭示「选了产品 ≠ 选了 SKU」；L2/L3 无倒序；L4 五幕完整，第二幕四个反直觉事实覆盖三知识点；L5 困惑点全覆盖（**26 条误区** + 5 题小测 + 5 张速查卡 + 六步选型法）；L6 含阶段 6 进度 + 全书进度 19/19 + 收官动作。**本机实跑**：选型决策树引擎（六步流程，245 行输出）、TCO 与容量规划器（容量估算 + 按 SKU 校验 + 三方案成本 + 清单生成，282 行输出） |
| 2026-09-02 | 阶段 6 · 课18（3 知识点） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮） | 0 | **P0=0 / P1=5 / P2=5，全部当批修复。** **【P1×5】** ①**静默项口径混淆**——保留期专项把「语义反转（`0d` 想永久结果全删）」与「幅度偏差（`3mo` 想 91.3 天只留 90 天）」都标成同一个「静默」，而这两类后果差一个量级 → 给 `RetentionCase` 新增 `kind`（`reverse`/`drift`）分两类，案例 5→7 个（补「降采样库 432 超限」「合规库 3y 换算」两个**同构场景**，让迁移清单能照抄排查顺序而不必每次重新推理），收束段分别统计并**明确清单的「2 条静默」与专项的「4 条静默」口径分开、不合并**。②**版本升级在七阶段里无处安放**——「已装 3.9.5 直接升 3.10」不是数据迁移，读者却会拿七阶段去套 → 对照 3 补**独立升级路径**（四步，不可逆点在第 ④ 步启动 3.10 就已出现，**比数据迁移的 S7 靠前得多**），速查卡 4 同步补该分支。③**3.11.0→3.11.0 案例自相矛盾**（原地升自己）→ 改 3.10.0→3.11.0 真实场景，并给 `pick_catalog_backup` 补 **≥3.10 分支**（catalog 已是 v3，备 `{prefix}/catalog/`，但**备它也不能退回 3.9.x**）。④**磁盘开销未估**——只说「分批导出」未提体积 → 补估算表（1.2 倍预留），点明**行协议是文本格式无压缩，导出体积通常大于 TSM 内部存储**，磁盘满会中断导出且半途文件最易出问题。⑤**双写时长无判据**——只说「双写一段时间」，未给结束条件 → 收束段补算法：**下限 = 覆盖对账周期（1-3 天），上限 = 可查窗口（Core = 3 天）** → **Core 上双写窗口实际只有 1-3 天，很窄，S5 对账脚本必须提前写好**；同步写入速查卡 4、误区第 22 条、落地视角第 11 条。**【P2×5】** 报错原文缺失 / M05 的 `organization` 缺边界（限定为客户端库场景，Telegraf 侧为 L16 另一口径）/ 事实一缺检查动作（补「建库后立刻 show databases 确认不是 0d」）/ 「CQ 被移除」无出处（补 L9 口径 + ⏳ 实测建议）/ 实验解读段未同步新内容。**【四向逐字一致性校验通过】** python 块 ↔ 脚本文件（**500/471 行**）、text 块 ↔ 运行时 stdout（**266/212 行**）逐字一致；围栏 44 全闭合、details 5/5、题号 1-5 连续、占位符清零、链接 10 个 0 死链。**【官方文档冲突 3 条双面呈现】** ①v3 精度写法全拼 vs 缩写 → 取全拼 ②Docker latest 日期官方自给两个（2026-02-03 / 2026-09-15，镜像站 2026-04-07）→ **不引用日期，只采用行动建议** ③v2 端点是否支持 m/h（官方 Core 矩阵不支持 vs AWS 集成页含 m,h）→ 以官方 Core 页为准。**【本批最有价值产出】** ①**「静默」必须再分两类**：`reverse`（方向反了，灾难级）vs `drift`（幅度偏了，合规级）——混为一谈会让合规问题挤占对灾难问题的注意力 ②**不可逆点不止一个**：数据迁移 S1-S5 随时可退 / S6 收窄 / S7 关死，而 3.9.x→3.10 在第 ④ 步就已不可逆 —— **拿迁移的节奏去套升级是本课最容易犯的结构性错误** ③**双写窗口可以算出来**：上限由可查窗口反推，得出「Core 上双写只有 1-3 天」——写太久最早的数据反而查不到，双写失去对照意义。**【学习者视角】** L1 三条消息对应三个不同性质的坑（432 硬约束 / 能存≠能查 / 静默反转），结构清晰；L2/L3 无倒序；L4 五幕完整，第二幕四个反直觉事实覆盖三知识点；L5 困惑点全覆盖（**23 条误区** + 5 题小测 + 6 张速查卡）；L6 含阶段 6 进度 + 全书进度 + L19 收官预告。**本机实跑**：三代差异清单检查器（17 条 × 三档 + 保留期专项 7 案例 + 翻译器自检，266 行输出）、迁移演练与回滚规划器（七阶段可逆性 + catalog 备份决策 + 回填批次 + 磁盘估算，212 行输出） |
| 2026-09-02 | 阶段 6 · 批次 1（课17，3 知识点，阶段开篇） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮） | 2 | **P0×2（均已当批修复，两个都是「方法论懂了、手上做错」）**：①**实验 A 自评发现**——给 VictoriaMetrics 压缩维度打满分 5 分，理由却写「⚠️ 厂商自述……自述基准，慎引」，**嘴上说慎引、手上打满分**，正是本课要警告的错 → 已降为 4 分，并把自检写进脚本输出收束段。②**实验 B 分类缺陷**——把「InfluxDB Core 可查窗口 3 天」当**性能宣称**去审，结论是「⚠️ 厂商自述，不能裸写数字」，**而它恰恰是全课最硬的排雷依据**。根因：审计器缺 `claim_kind` 维度，拿审性能宣称的问法（Q1 厂商自述 / Q4 配置披露 / Q5 峰值稳态）去审**官方硬约束**，必然误伤 → 新增 **Q0 分类**（benchmark 性能宣称六问全审 / spec 官方规格只审来源与版本 / misread 理解错误一律判死），脚本升级 v2，宣称清单 10→11 条，可写结论从「0 条」变「3 条（全为官方规格类）」。**教训：先分类再审问；审计器自己也该被审计。** **P1×1**：Telegraf 插件数官方自相矛盾（官方 Telegraf 文档页与产品页 **400+ plugins** vs 官方 GitHub README v1.39 **over 300 plugins**）→ 按本课冲突规范处置（双面呈现 + 取保守值），全文 8 处统一「300+（保守口径）」，新增**冲突记录第 4 条**，官方文档表补 2 条一手来源，并作为第 10 条 spec 类宣称纳入审计器（判 ⚠️ 而非 ✅，因官方自己给了两个口径）。**P2×2**：①实验 B 解读段仍引用 v1 旧结论（「10 条宣称，0 条可以直接写」「第 10 条」）→ 全部同步为 v2（11 条 / 3 条可写 / 第 11 条 misread）；②重跑脚本只替换 text 输出块、未同步 python 源码块，导致源码与脚本文件不一致（**校验器当场抓出**）→ `_l17_repaste.py` 补 `replace_py_block`。**四向逐字一致性校验通过**：讲义 python 块 ↔ 脚本文件（**459 / 391 行**）、讲义 text 块 ↔ 运行时 stdout（**153 / 163 行**）全部逐字一致；围栏 54 全闭合、details 5/5、题号 1-5 连续、占位符清零。**教学法视角**：四个反直觉事实全部是**官方自己说的**——①Prometheus FAQ *\"pulling is slightly better than pushing, but it should not be considered a major point\"*；②Prometheus storage 页 *\"not meant as durable long-term storage\"*（不集群不复制，默认 15d）；③VM 官网 20x/70x/7x 全是 `up to` + 厂商自述；④ClickHouse *\"Partitioning does not speed up queries (in contrast to the ORDER BY expression)\"*。**学习者视角**：L1「选型会上三个问题把你问住（层次 / 已有资产 / 所以推荐哪个）」代入感强，且三个问题正好对应本课三条主线；L2/L3 无倒序（层次差→推拉模式→共存组合→六维权衡）；L4 五幕完整，**第二幕四个反直觉事实覆盖三知识点**；L5 困惑点全覆盖（**17 条误区** + 5 题小测 + 8 张速查卡）；L6 含阶段 6 进度 + 全书进度 + L18 预告。**本批最有价值产出**：①**层次差**——InfluxDB 是发动机、Prometheus 是整车，拿组件比整体是选型文档最常见也最致命的错误；②**没有一款赢下全部场景**——实验 A 四个场景四个不同冠军（VM / VM / TimescaleDB / ClickHouse），且**先排雷再打分**比打分本身重要（场景 1 里 Core 不是「分数低」而是「**查不到**」）；③**Q0 分类**——数字要先分「性能宣称 / 官方规格 / 理解错误」三类再审问。**本机实跑**：五款候选选型决策打分器（4 场景 × 6 维度）、基准数字口径审计器 v2（11 条 × Q0 分类 + 六问三档，✅3 / ⚠️4 / ❌4） |
| 2026-09-02 | 阶段 5 · 批次 4（课16，3 知识点，阶段收官） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮） | 2 | **P0×2（均已当批修复）**：①**实验 B 输出块与实跑输出不一致**——讲义「对照 3」写 5s 单面板 **432,000**，实跑为 **86,400**（差 5 倍）。根因是**口径混用**：讲义表格按「每面板 5 query」计算，却直接引用了官方博客「一个 10s 面板 ≈ 259,200 次/月 ≈ $31」的算例，而**该算例是每面板 1 个 query 的口径**（86,400÷10×30 = 259,200）→ 已把脚本改为整除、在脚本内显式标注官方口径、同步知识点 2 与速查卡 5 两处表格，并全文校验旧值清零。**教训：引用官方数字前必须先确认对方的口径单位，否则会得出相差 5 倍的账单**。②**修复脚本 marker 重复导致讲义段落被误删**——`_l16_fix.py` 用「**真实输出**（本机 Python 3.11.15 实跑，逐字回贴）：」作锚点，该句在实验 A/B 中各出现一次，`index` 定位到第一次出现处而结束标记落在实验 B，替换区间越界，误删了实验 A 输出块与实验 B 的标题/引导/源码块 → 用 `_l16_fix2.py` 按文件实际状态精确重建。**教训：批量替换讲义时，锚点必须在全文唯一，或改用「起止双锚点 + 区间长度校验」。** **P1×1**：实验 B 初版违反「实验输出必须逐字回贴」规范（只贴对照 1，其余三组仅给核心表格）→ 已改为**完整 stdout 逐字回贴**（161 行）。**P2×2**：①第五幕 L17 预告写「Prometheus / TimescaleDB / QuestDB」与阶段 6 overview（SSOT）的「InfluxDB 3 / TimescaleDB / VictoriaMetrics / ClickHouse / Prometheus」不一致 → 已按 SSOT 修正；②实验 C 第 3 步未体现 `/ping` 的 GET 约束 → 已补 `curl -I` 反例对照。**四向逐字一致性校验（针对 L15 五个 P0 的根因增设）**：编制 `_l16_verify.py` 比对**讲义 python 块 ↔ 脚本文件**（356 / 245 行，逐字一致）与**讲义 text 块 ↔ 运行时 stdout**（102 / 161 行，逐字一致）；围栏 36 个全闭合、details 21/21 配对、题号 1-5 连续、占位符清零。**建议固化为后续每课的交付前强制步骤。** **教学法视角**：十个官方核实项全部取自一手页面——`outputs.influxdb_v3` 版本门槛 v1.38.0、多 URL 故障转移语义、`database_tag` 路由基数、`sync`/`content_encoding`/`timeout` 默认值、`influxdb_v2` 的 `organization` 空串要求、Grafana Product 选 Enterprise（官方原话）、Flight SQL 需 HTTP/2、三个端点默认需认证且 `/ping` 必须 GET、`system_metrics` 采主机（psutil 依赖）、遥测每小时上传。**官方文档冲突 3 条已双面呈现未裁决**：①Flight SQL 端口 8181（官方 Core 文档）vs 8082（社区 docker-compose / IOx 形态）→ 以 Core 官方 8181 为准；②`system_metrics` 定位 → 采信主机指标；③Grafana 无 Core 菜单项 → 采信官方原话。**学习者视角**：L1「运维三问（数据从哪来 / 给谁看 / 谁监控监控者）」承接 L13-L15 搭出的「一台能跑的 InfluxDB」，三问指向明确；L2/L3 无倒序依赖；L4 五幕完整，**第二幕四个反直觉事实覆盖三知识点**（Grafana 要选 Enterprise / SQL 需 HTTP/2 / 端点默认需认证 / 自监控只有 3 天）；L5 困惑点全覆盖（**16 条误区** + 5 题小测 + **10 张速查卡** + 上线前检查清单 12 项）；L6 含**阶段 5 完整闭环图** + 阶段/全书进度 + 阶段 6 预告。**本批最有价值产出**：①**「SQL 面板连不上、InfluxQL 面板正常」是排障第一分叉点**——非对称现象直接指向 HTTP/2，可跳过 token/端口的无效排查；②**自监控数据自己也只受 3 天可查窗口**（432÷144=3），且**文件数只跟墙钟有关、跟数据量无关**（与 L15 的 WAL 触发器一天 86,400 次同构）→ 「自监控留 90 天」= **存得下但查不到**，且**砍掉一半指标一个文件都不会少**；③**引用官方「10s 面板 ≈ $31/月」必须对齐口径**（单 query vs 5 query 差 5 倍）。**本机实跑**：Telegraf 配置体检器（14 条规则，反面样例 P0=3 P1=3 P2=4 INFO=1 退出码 1；正面样例无 P0/P1/P2）、自监控覆盖率与查询成本模拟器（4 组对照，5s 抓取 = 1,582 MB/天但只能查 3 天） |
| 2026-09-01 | 阶段 5 · 批次 3（课15，3 知识点） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮） | 5 | **P0×5（同源，全部当批修复）**：实验 A 对照 4 的 `for n in range(1,21)` 循环结束后 `rows` 已是**第 21 秒**的值，却被 `print(f"→ 第 20 秒：...")` 打印 → **讲义 7 处**（第二幕事实二、三条红线表、一图总结 G1、三句话收束第 2 句、速查卡 4、实验 A 小结、课后小测 Q3）统一引用了错误的「20 秒 1 亿行」；且**课后小测 Q3 的答案「14 秒」也错**（正确 **15 秒**：第 n 秒 = `100×2ⁿ⁻¹`，第 1 秒尚未翻倍，翻倍从第 2 秒开始）。修复：脚本新增 `flushed_at_20` 记录第 20 秒真值，改印两行（20 秒 **52,428,800** / 21 秒 **104,857,600**）；重跑后**整体重生成**讲义中的源码块与输出块（避免手工改漏）；再全文统一 7 处口径；Q3 答案改 15 秒并补「易错点：别把第 1 秒算成翻倍」。**根因归为 L13 同类问题的第三次出现**（讲义与实跑产物不同步）→ 本课改用**脚本化重生成 + 逐字一致性校验**（源码块 vs 脚本文件、输出块 vs 运行时 stdout 四向比对全部通过：A 源码 278 行 / 输出 149 行，B 源码 397 行 / 输出 69 行）。**教学法视角**：八个官方核实项全部取自一手页面原文（WAL flush 每秒、`plugin-dir=""` 不禁用、共享 Python 进程、`KeyboardInterrupt` 取消信号、`gh:` 每次重拉、Notifier 依赖表、Timestream 限制、缓存语义）。**学习者视角**：L1「运维三问（降采样谁跑 / 告警怎么做 / 脏数据怎么办）」承接 L14 三层保留方案，三问指向同一答案；L2/L3 无倒序；L4 五幕完整，第二幕四个反直觉事实覆盖三知识点；L5 困惑点全覆盖（16 条误区 + 5 题小测 + 8 张速查卡 + 上线前检查清单 13 项）；L6 含阶段 5 进度树 + 全书进度 + 三个「第一次」定位。**本批最有价值产出**：**「写回被监听的表 = 20 秒破 5,000 万行/秒且零报错」是处理引擎唯一的静默自毁路径**（数据库视角一切正常，插件在正确无误地执行一个错误的逻辑），已固化为红线一、速查卡、Q3 考题与 CI 门禁（实验 B 的 P0-1） |
| 2026-09-01 | 阶段 5 · 批次 1（课13，3 知识点，阶段开篇） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮） | 0 | **P1×1**：讲义实验 B 输出块与实跑输出不同步（脚本第 3 轮修正后加「≈ 3 天」，讲义仍为旧文案）→ 已同步脚本源码块与两处输出块，正则校验 OLD=0 / NEW=3。**P2×1**：正文「写入速率 vs 存储量」表缺实跑输出中的 500,000 点/秒行 → 已补齐。**P2×2**：「按负载内存建议 60-70% / 80-90% / 70%」被写成三个参数通用 → 官方该区间只针对 `--exec-mem-pool-size`，已标注参数归属。**关键核实**：Enterprise 六组件扩展策略与 GC 不应水平扩展、五种 `--mode` 与 compact 全集群唯一、Serverless 四维度单价与「查询按次计费」、Timestream 多引擎 SKU、内存三默认 90%、IO 线程默认 2 且每并发写入者占 1、catalog 3.10 单向迁移与两条备份路径、Core 无内建 backup 命令、`GET /ready` 200/503、`debug catalog` 离线检查、官方无 Core 硬件基线（诚实说明）。**本机实跑**：容量规划模拟器六组对照（含「能存 ≠ 能查」分家点与量级锚点 10 万点/秒 × 30 天 ≈ 1 TB）、部署形态选型决策树模拟器六场景 + 五条选型口诀。新增 14 条事实核查 |
| 2026-09-01 | 阶段 4 · 批次 2（课12，3 知识点，阶段收官） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮） | 0 | **教学法视角**：八个官方核实项全部取自一手页面——批量写入双阈值（Core optimize-writes 原文 "10,000 lines of line protocol or 10 MBs, whichever threshold is met first"）、gzip 口径（"up to a 5x speed improvement"，速度非压缩比）、`--max-http-request-size` 默认 10mb（Core config-options）、`WriteOptions` 默认 `batch_size=1000`（官方客户端库页，属 Cloud Serverless SKU）、`SELECT *` 门槛（official optimize-queries 原文 10 列 minimal / 1000+ 列 slower）、`system.queries` 字段与官方示例输出（Core HTTP API 页）、`--max-concurrent-queries` 运行时可调（Core config-options 原文 `POST /api/v3/configure/query_concurrency_limit`）、tag 排序的 Core/Clustered SKU 差异。P1×2：①第二幕「在 Core 上建不出能靠改 `SELECT *` 获明显收益的表」表述过绝对，易被读成「改 `SELECT *` 完全无收益」→ 补充「500 列时可能已有可感知差距，只是远未达官方量级」，使论断从二元变为连续谱；②「10MB 阈值基本用不上」过绝对 → 补充超宽表/超长 string field 的例外与「算平均行长」判据。P2×3：①gzip 三种启用方式未标出处 → 补「官方 optimize-writes 页三处示例（Telegraf / 客户端库 / InfluxDB API）」；②`WriteOptions` 默认值出自 Cloud Serverless SKU 文档页而非 Core 页 → 补 SKU 来源标注（与 L7「Core 未提及 vs Clustered 明文」同类问题，证据强度需分开标注）；③实验 D 的 shell `${i:0:1}` 取值不稳定且会产生重复值（违反「可照抄运行」必查项）→ 改为 100 个点的稳定写法并同步 B/C 组文件名。**学习者视角**：L1「周一早上两通电话（写入从 8 万/秒掉到 6 千 + 大屏 8 秒）」真实具体，且开场即点明两者是同一门课（固定开销 / 文件数），代入感强；L2/L3 无倒序（快递整车→固定开销与边际递减→双阈值与 gzip→batch_size/flush_interval 两旋钮→两组模拟器实跑→落地视角）；L4 五幕完整，**第二幕三个反直觉事实与三知识点一一对应**（边际递减→事实一 / SELECT * 门槛→事实二 / plan>exec 与四类不受控瓶颈→事实三），第一幕两通电话在第五幕闭环；L5 困惑点全覆盖（14 条误区 + 4 题小测 + 速查卡，含「批量越大越好」「SELECT * 是元凶」「tag 过滤走索引」「system.queries 能查全部历史」）；L6 含**阶段 4 三课闭环四行对照表** + **阶段 4 出口检查清单 10 项** + 阶段 5 预告。**额外产出（已回写档案 6 条）**：**`SELECT *` 官方门槛 1000+ 列 vs Core 上限 500 列的证伪**、**10 MB 批量阈值与 10 MB HTTP 上限撞车**、**四档行长全部行数先到（阈值分界 1048 字节/行）**、**gzip 是 speed improvement 而非压缩比**、**`system.queries` 字段清单与两条限制**、**`--max-concurrent-queries` 可运行时调整**。**本机实跑**：批量写入权衡模拟器五组对照（含边际收益递减位置与 flush_interval 两难）、慢查询诊断决策树模拟器五场景 + 七步倒查法 || 2026-08-31 | 阶段 4 · 批次 1（课10，3 知识点，阶段开篇） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮） | 0 | **教学法视角**：关键数字全部取自官方页（WAL 每 1 秒 / 900 文件=15 分钟 / 10 分钟持久化 / 留 5 分钟 / `no_sync` 默认 false / 六取值 / `node-id` 唯一 / 优雅停机三动作）；「Core 无 compactor」有三处独立官方证据，非推测。P1×2：①实验 A 对照 5 脚本 `google`/`azure` 两行数据元组缺元素 → **列错位，把持久化存储错打成「重启即丢」** → 修正脚本并重跑验证；②讲义代码块为手写精简版（省略对照 1/2/4 的官方原文打印与解读），与「严格一一对应」的声明不符 —— **此为 L9 已修过的同类问题第二次出现**，需在后续课程中固化为检查项。P2×1：「每天 144 个 Parquet 文件**/表**」中「每表」为未经官方确认的推论 → 弱化。**学习者视角**：L1 用运维「90 天查询 40 秒」开场，落到「不是数据量大，是要打开的文件多」；L2/L3 无倒序（快递中转站→五站详解→no_sync/WAL tail→压实证据链与量化→对象存储→模拟器实跑）；L4 五幕完整、三处反直觉事实（写≠可查 / `no_sync` 默认安全档 / 小文件只增不减）与第二幕对应；L5 困惑点全覆盖（14 条误区 + 4 题小测，含「数据在 WAL ≠ 不安全」「持久化最老的、留最新 5 分钟」「`gen1` 调小反而更糟」）；L6 含阶段 4 进度 + **L11「向量」伏笔闭环** + L12 钩子。**额外产出（已回写档案）**：**Core 无 compactor 的三处官方证据与量化后果**、**写入五站默认参数**、**`no_sync` 默认值与语义**、**WAL tail 是持久的**、**`--object-store` 六取值**、**优雅停机与 node-id 唯一性**、**Core 节点状态仅两种**。**本机实跑**：写入路径与持久性模拟器六场景（已修正并重跑验证） |
| 2026-08-31 | 阶段 3 · 批次 4（课9，3 知识点，阶段收官） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮） | 0 | **教学法视角**：支持矩阵由脚本从官方 HTML 勾选图标（`Checkmark_New`）逐条提取，非推测。核实项：3.x **只有 SQL 与 InfluxQL 两种语言**（Flux 已移出清单，官方出路 Flux to SQL converter beta）、InfluxQL **"rearchitected / ongoing"**、**基数类元查询 "likely not supported"**（官方给理由：基数不再是瓶颈）、**`NON_NEGATIVE_DIFFERENCE()` 在 InfluxQL 标 ✅**、v1→v3 模型映射、两端点仅路径不同、`SHOW TAG VALUES` 须带 FROM。P1×1：实验 A 代码块仅给片段却声明「完整脚本见课件实验 A」，四个打印逻辑未实写（与 L8 同类问题）→ 补全为与输出严格一一对应的完整脚本。P2×2：①实验 C 的 shell 函数 `Q()` 与「把 `$1` 换成具体语句」注释自相矛盾 → 删除矛盾句并补兼容性说明；②L10 课名为推测 → 核实大纲后改为真实标题《存储引擎：WAL、Parquet 与压实》。**学习者视角**：L1 用「同事打脸 L8 结论」开场，比凭空讲兼容层更有代入感，且**示范了技术判断应如何被证据推翻**；L2/L3 无倒序（老房子改造→数据模型映射+支持矩阵→三陷阱机制→模拟器实跑→阶段收官）；L4 五幕完整、第二幕三事实与三知识点对应（重建中 / 永久封死 / Flux 无位置）；L5 困惑点全覆盖（14 条误区 + 4 题小测，含「SQL 没有 ≠ 3.x 没有」「`fill(previous)` ≠ `LAG`」「`SHOW DATABASES` 反直觉不支持」）；L6 含阶段 3 收官标记 + L10 钩子 + L11 悬念。**额外产出（已回写档案）**：**`NON_NEGATIVE_DIFFERENCE()` 在 InfluxQL 受支持**（修正 L8 语境）、**3.x 双语言格局与 Flux 定位**、**基数类查询 likely not supported 的官方理由**、**两端点仅路径不同（灰度迁移成本低）**、**`SHOW TAG VALUES` 稳定性警告**。**本机实跑**：InfluxQL↔SQL 语义对照模拟器四场景，对照 2 证明带 `tz()` 的按天分组直译会错位 8 小时 |
| 2026-08-31 | 阶段 3 · 批次 3（课8，3 知识点） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮） | 0 | **教学法视角**：`date_bin` 三参数与 origin 默认 Unix epoch、gapfill 单位差异（不支持 months/years/century）、gapfill requires time bounds、`interpolate`/`locf` 参数必须含聚合函数、窗口函数四要素、`LAG` 三参数、**3.x 无 `increase()`/`NON_NEGATIVE_DIFFERENCE()`**（官方 GREATEST+LAG+CTE 三段式替代）——全部取自 Core 官方页面原文。P1×2：①实验 A 讲义代码块是片段但实跑输出含实验 1-4，学员照抄跑不出 → 补全为与输出严格一一对应的完整脚本；②gapfill 三选一（interpolate/locf/COALESCE）缺前置结论 → 补「温度在变、状态不变、计数从零开始」。P2×2：①实验 2「前两者相同」易让学员误以为 origin 不重要 → 补「1 天能整除 1 天是巧合，90 分钟/7 小时立刻分家」；②sample-data 链接为推测 URL → 已核实 HTTP 200 有效。**学习者视角**：L1「日报要 504 行你只给了 3 行」+「凌晨 2 点归到前一天」比 L7 更贴近日常；L2/L3 无倒序（面条切段→三参数与单位表→origin 与 UTC+8 机制→模拟器实跑→全局定位）；L4 五幕完整、第二幕三事实与三知识点一一对应（无小时列 / origin 默认 / gapfill 才补桶）；L5 困惑点全覆盖（15 条误区 + 4 题小测，含「LAG 是不是前一小时」「gapfill 能不能填 0」「SELECT * 代价」「GROUP BY 限制」）；L6 含进度条 + L9 钩子（increase() 正是迁移最常撞的墙）+ L11 悬念。**额外产出（已回写档案）**：**UTC+8 按天分桶默认错位 8 小时**（默认 origin 让「一天」从北京时间 08:00 开始，修法 `1970-01-01T16:00:00Z` 或 `date_bin_wallclock`）、**LAG 是行偏移非时间偏移**（精确偏移须自连接）、**gapfill 与 date_bin 单位支持不同**、**计数器重置三段式**。**本机实跑**：date_bin 分桶模拟器四场景，实验 1 输出 `18:18:18 → 18:15` 与官方例子逐字一致 |
| 2026-08-31 | 阶段 3 · 批次 2（课7，3 知识点） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮） | 0 | **教学法视角**：基数定义/依赖 tag/宽与稀疏 schema/列顺序/`SHOW SERIES CARDINALITY` 不受支持——全部取自官方原文，「Core 全文 0 次提及索引」经本机抓取官方页面正文核实。P1×2：①Core 版「未提及索引」（推断）与 Clustered 版「明文不索引 tag 值」（直接证据）证据强度混用 → 补证据强度说明框分开标注；②「既然 tag 不被索引为何还要用 tag」长论证缺结论先行，读者会卡住 → 补「先给结论再讲理由」块（判据没变，变的是理由与限制）。P2×1：实验 A 脚本内 `growing` 变量重复赋值（循环变量与列表同名）→ 改名 `unbounded`。**学习者视角**：L1「只加一个 trace_id 慢十几倍、数据量一行没变」与 L6「加引号断一截」同构递进；L2/L3 无倒序（衣柜格子→三定义→乘法与点密度→四实验→全局定位）；L4 五幕完整、第二幕三事实与三知识点一一对应；L5 困惑点全覆盖（13 条误区 + 4 题小测，含「需要按某字段查是否必须做 tag」「依赖 tag」「DISTINCT 要不要时间范围」）；L6 含进度条 + L8 钩子 + L11 悬念（Parquet min/max 如何做分区裁剪）。**额外产出（已回写档案）**：Core 页面**全文 0 次提及索引**（`index` 与「索引」各 0 次）、Clustered 明文「不索引 tag 值或 field 值」、**基数类元查询很可能不被 3.x 引擎支持**、**首次写入决定物理列顺序且不可更改**、依赖 tag 不参与乘法。**本机实跑**：四场景基数估算器（3,000→3 亿 series 对照，点密度 1000→0.01），输出已逐字核对并回贴讲义 |
| 2026-08-31 | 阶段 3 · 批次 1（课6，3 知识点，阶段首课） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮） | 0 | **教学法视角**：四要素/主键/schema-on-write/保留键/转义/大小写/Core 硬限制全部取自官方文档原文。P1×1：实验 A 的 `information_schema.columns` 出自第三方教程非官方页面 → 补 ⏳ 置信度「中」+ 两条降级路径（InfluxQL `SHOW TAG KEYS` / 客户端 `.schema`）。P2×1：实验 E 输出与实跑不符（`_internal` 实为两条警告）→ 按实跑修正。**学习者视角**：L1「加了个引号面板断一截」与 L4 的 42 vs 42i 同构递进；L2/L3 无倒序；L4 五幕完整、三事实对应三知识点；L5 困惑点全覆盖（13 条误区 + 4 题小测）；L6 含进度条 + L7 基数钩子。**额外产出（已回写档案 7 条）**：Core 硬限制**库 5 / 表 2000 / 列 500**、主键规则与 null tag 塌缩、保留键三类后果不同（`time` 拒绝 vs `_field`/`_measurement` 静默丢弃）、转义不对称（只有 string field value 转义引号）、大小写三套规则、tag/field 同名在 3.x 是拒绝（1.x 是改名） |
| 2026-08-31 | 阶段 2 · 批次 3（课5，3 知识点，阶段收官） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮） | 0 | **教学法视角**：API 签名/默认值/枚举全部取自本机 venv 实装后 `inspect` 实测，非推测；host 解析缺陷取自安装包源码并经 pyarrow 复现 `ArrowInvalid`。P1×1：实验 E「flush 后读计数」回调在后台线程存在时序缺陷 → 补 `sleep(2)` + 告警 + 误区第 11 条。P2×2：实验 A 缺 python3.11 路径提示（已补）；示例代码残留 `if False else None` 调试写法且 WriteType 未导入（已改为可照抄版本）。**学习者视角**：L1「照抄官方示例 host 却崩、报错冒出未写过的 :443」真实可复现；L2/L3 无倒序（快递员类比→双协议→源码→五实验→落地视角）；L4 五幕完整、第二幕三事实与三知识点对应；L5 十个困惑点全部覆盖（11 条误区 + 速查卡）；L6 含进度条 + L6 钩子 + 阶段 3 预告。**额外产出（已回写档案事实核查表 5 条）**：版本订正 0.20.0→**0.21.0**、新增**默认 V2 端点 + no_sync 互斥**、新增 **influx3 仅 query 子命令**、新增 **Core Flight 与 HTTP 共用 8181（8082 属社区版）** |
| 2026-08-31 | 大纲评审 | 主 agent 内联（pedagogy） | 0 | 6 阶段 19 课依赖链无缺口（问题→实操→模型→引擎→生产→决策）；工作落地导向与"尽量全面"要求匹配；P2：阶段 2 上手篇置于阶段 3 之前（先有手感再学 schema 设计，已采纳） |
| 2026-08-31 | 阶段 2 · 批次 2（课4，3 知识点） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮） | 0 | **教学法视角**：line protocol 四要素/转义表/引号规则/五种类型/类型冲突错误消息/HTTP API 端点与两类响应/no_sync/gzip multi-member/cluster-uuid/三端点选择——全部取自官方文档原文。P2×1：时间戳有效范围来自第三方（阿里云）→ 加 ⏳ 标注。**学习者视角**：L1「42 vs 42i」类型冲突开场真实高频；L2 由浅入深；L3 无倒序；L4 五幕完整、第二幕「三层坑」与三知识点一一对应；L5 **P1×1**＝初稿称 auto「猜错就是几十年误差」——**换算验证为错**（5e9 秒/5e12 毫秒/5e15 微秒三者对应同一时刻 2128-06-11），已改写三处并新增误区 4b 破除该误解；L6 全局定位含指向 L12 的钩子。两视角无冲突 |
| 2026-08-31 | 阶段 2 · 批次 1（课3，3 知识点） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮） | 0 | **教学法视角**：命令/参数/token/端口/72h 限制/Flux 不支持/LVC·DVC/环境变量三 shell 写法/原生 python 依赖/快速启动模式——全部取自官方文档原文，已逐条入档案事实核查表。P1×1：第四幕「预期输出」为构造非实测（编写环境无 Docker）→ 改「判断成功三条标准」+ ⏳ 标注，档案记置信度「中」待验证。**学习者视角**：开场「拉到 v2 镜像」叠加 2026-09-15 latest 变更真实事件，代入感强；无倒序；五幕完整且 L2「Core 不含 compactor」伏笔在 72h 限制处兑现并亲手验证。P2×1：docker exec 前缀反复出现未解释 → 已补统一说明。**额外收获（已回补 L2）**：核实到 Core 限 72 小时、<10ms/~30ms 出自 LVC/DVC 两个需主动配置的缓存 |
| 2026-08-31 | 阶段 1 · 批次 1（课1+课2，6 知识点） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮） | 0 | **教学法视角**：依赖链无缺口；版本时间线/FDAP/Core 无 compactor/性能口径均沿用已核实条目；九项结构件齐全（五幕/Mermaid/🐞误区/一图总结/课后小测/📚官方文档/📋速查卡/🚀接力/🧭导航）；SVG 仅用于路径图（固定场景）、浅底深字合规。P2×2：①知识点缺「一句话定义」（六要素之一，3 处补齐）②五项优化中「③追加写路径」在三笔账无对应项 → 补 📌 说明它治的是写入吞吐这一独立维度。**学习者视角**：L1 场景真实具体（900GB/延迟 6h/DELETE 4h）；两课由浅入深无倒序；五幕完整且首尾钩子呼应；两课均有全局定位与下一步。P1×1：L2 无 SQL 示例 → 补 2.x Flux vs 3.x SQL 同需求对照表（DATE_BIN 取自官方首页示例）。P2×1：字典编码/RLE 只列术语 → 四项各补一句人话。两视角无冲突，全部采纳 |

> ⚠️ 当前评审方式为「主 agent 内联」（course-reviewer 子 agent 未创建，独立性受限）。可创建独立 course-reviewer 子 agent 获得更独立的评审视角。
