# InfluxDB 课程学习档案

> 主题：InfluxDB 3 系统学习（工作落地导向）
> 创建日期：2026-08-31
> 模式：课程制 · 6 阶段 / 19 课 / 57 知识点

## 学习者画像

| 项目 | 值 | 来源 |
|------|-----|------|
| 主题 | InfluxDB 3 系统学习 | 用户指定（经概念澄清） |
| **当前水平** | **零基础**（把 InfluxDB 误认为向量数据库，说明尚未建立时序数据库概念） | 用户提问推断 |
| **学习目标** | **动手实操 + 决策参考**（用户原话"工作需要，尽量全面"——既要有可照做的命令，也要有能向团队汇报的选型依据） | 用户指定 |
| **输出格式** | Markdown + Mermaid/SVG（技能默认）；每课带「🎯 落地视角小结」 | topic-teach 默认 |
| 关联背景 | 已学 promql（Prometheus 查询语言，2/12 课）、kafka（5/10 课）、zookeeper（已完成 12/12） | 仓库档案 |
| 术语起点 | ⚠️ 开课前的概念纠正：InfluxDB **不是**向量数据库，是时序数据库（TSDB）。用户最初以「向量数据库」指代，来源可能是 InfluxDB 3 的 *vectorized execution*（向量化执行，列存 + SIMD 查询加速技术），与存 embedding 做 ANN 检索的向量数据库（Milvus/Qdrant/pgvector）是完全不同的东西 | 2026-08-31 澄清 |
| 环境偏好 | **Docker 为主线 + 原生安装备选**。用户未指定，主 agent 决策（理由：官方镜像成熟、起服务最快、贴近生产容器化部署；保留原生路径覆盖非容器场景） | 2026-08-31 决策（用户以「好，继续」推进，未另行指定） |

## 课程故事主线

> 按 topic-teach 规范设计：找主角 → 找冲突 → 找转折 → 找收束

**🎭 主角：时间**

在 MySQL 里，时间只是一个普通的 `DATETIME` 字段，和其他字段挤在同一行里；在 InfluxDB 里，时间是**组织一切的一号维度**——整个数据库的存储、压缩、分区、查询都围绕它设计。本课程讲的就是"时间成为一等公民"之后发生的故事。

**⚔️ 冲突：行式牢笼 vs 时间洪流**

高频、只追加、按时间范围聚合的数据流，撞上了为"随机读写单条记录"优化的关系型数据库。结果是三笔账同时到期：**空间放大**（标签每点重存）× **删除昂贵**（DELETE 逐行 + undo 膨胀）× **聚合缓慢**（整行扫描 + GROUP BY）。加机器救不了，因为这是数据组织方式的不匹配，不是算力不足。

**🔄 转折（每个阶段是故事的一个章节，解决前一章遗留的问题）**

| 章节 | 阶段 | 关键转折 | 主角的状态变化 |
|------|------|----------|----------------|
| 第一章 | 1 · 问题与定位 | 认冲突：三笔账算清楚，InfluxDB 登场 | 听说过 → **知道为什么需要它** |
| 第二章 | 2 · 上手篇 | 装起来：亲手跑通写入与查询 | 知道为什么 → **能操作它** |
| 第三章 | 3 · 数据模型与查询 | 学会设计：schema 与 SQL 写对 | 能操作 → **能用对它** |
| 第四章 | 4 · 存储引擎与性能 | 探原理：为什么快、慢在哪、边界在哪 | 能用对 → **知道它的边界** |
| 第五章 | 5 · 生产落地 | 上生产：部署、降本、接生态 | 知道边界 → **能扛生产** |
| 第六章 | 6 · 对比与决策 | 做抉择：横向对比、迁移、选型 | 能扛生产 → **能向团队交代** |

**🎬 收束：能回答一个大问题**

> "我们公司该不该用 InfluxDB？用哪个 SKU？怎么落地？成本多少？迁移代价多大？"

终点产物：一份能向团队汇报的 **《时序数据库选型与落地方案》**（第 19 课产出）。

**情节落地**：L1 的"MySQL 被写崩的那个晚上"是故事的引爆点；L2 揭示 InfluxDB 经历过两次推倒重来（1.x→2.x→3.x），回答"为什么教程版本这么乱"；阶段 2 让手感建立起来；阶段 4 的"向量化执行"一课回扣开课时的概念澄清（用户最初的"向量数据库"误解在此彻底闭环）。

## 知识点级进度表

| 阶段 | 课 | 知识点 | 状态 | 完成日期 | 小测成绩 |
|------|-----|--------|------|----------|----------|
| 1 | L1 | 时序数据的四个特征 | ✅ 已完成 | 2026-08-31 | - |
| 1 | L1 | MySQL/PostgreSQL 为什么扛不住 | ✅ 已完成 | 2026-08-31 | - |
| 1 | L1 | TSDB 的五项专属优化 | ✅ 已完成 | 2026-08-31 | - |
| 1 | L2 | 一句话定位与能力地图 | ✅ 已完成 | 2026-08-31 | - |
| 1 | L2 | 三代演进：TSM/Flux → FDAP | ✅ 已完成 | 2026-08-31 | - |
| 1 | L2 | 三个 SKU 与生态位 | ✅ 已完成 | 2026-08-31 | - |
| 2 | L3 | 安装方式与启动参数 | ✅ 已完成 | 2026-08-31 | - |
| 2 | L3 | 第一次写入与查询 | ✅ 已完成 | 2026-08-31 | - |
| 2 | L3 | 目录结构与配置文件 | ✅ 已完成 | 2026-08-31 | - |
| 2 | L4 | Line Protocol 语法全解 | ✅ 已完成 | 2026-08-31 | - |
| 2 | L4 | 数据类型与精度陷阱 | ✅ 已完成 | 2026-08-31 | - |
| 2 | L4 | HTTP API 与批量写入 | ✅ 已完成 | 2026-08-31 | - |
| 2 | L5 | Python 客户端 influxdb3-python | ✅ 已完成 | 2026-08-31 | - |
| 2 | L5 | influx3 CLI | ✅ 已完成 | 2026-08-31 | - |
| 2 | L5 | Flight SQL 客户端 | ✅ 已完成 | 2026-08-31 | - |
| 3 | L6 | table/tag/field/timestamp 四要素 | ✅ 已完成 | 2026-08-31 | - |
| 3 | L6 | schema-on-write 与类型冲突 | ✅ 已完成 | 2026-08-31 | - |
| 3 | L6 | 命名限制与特殊字符 | ✅ 已完成 | 2026-08-31 | - |
| 3 | L7 | 基数（cardinality）本质 | ✅ 已完成 | 2026-08-31 | - |
| 3 | L7 | tag vs field 的抉择 | ✅ 已完成 | 2026-08-31 | - |
| 3 | L7 | schema 设计反模式 | ✅ 已完成 | 2026-08-31 | - |
| 3 | L8 | DataFusion SQL 基础查询 | ✅ 已完成 | 2026-08-31 | - |
| 3 | L8 | `DATE_BIN` 与时间分组 | ✅ 已完成 | 2026-08-31 | - |
| 3 | L8 | 聚合、窗口函数与 CTE | ✅ 已完成 | 2026-08-31 | - |
| 3 | L9 | InfluxQL 兼容层 | ✅ 已完成 | 2026-08-31 | - |
| 3 | L9 | Flux 为何被弃 | ✅ 已完成 | 2026-08-31 | - |
| 3 | L9 | SQL 迁移对照表 | ✅ 已完成 | 2026-08-31 | - |
| 4 | L10 | 写入路径：WAL 与内存缓冲 | ✅ 已完成 | 2026-08-31 | - |
| 4 | L10 | Parquet 持久化与压实 | ✅ 已完成 | 2026-08-31 | - |
| 4 | L10 | 对象存储与无盘架构 | ✅ 已完成 | 2026-08-31 | - |
| 4 | L11 | 列存与向量化执行 | ✅ 已完成 | 2026-09-01 | - |
| 4 | L11 | 谓词下推与分区裁剪 | ✅ 已完成 | 2026-09-01 | - |
| 4 | L11 | last-value 查询为何快 | ✅ 已完成 | 2026-09-01 | - |
| 4 | L12 | 批量写入与压缩 | ✅ 已完成 | 2026-09-01 | - |
| 4 | L12 | 查询性能调优 | ✅ 已完成 | 2026-09-01 | - |
| 4 | L12 | 慢查询诊断 | ✅ 已完成 | 2026-09-01 | - |
| 5 | L13 | Core/Enterprise/Cloud 选型 | ✅ 已完成 | 2026-09-01 | - |
| 5 | L13 | 容量规划与硬件 | ✅ 已完成 | 2026-09-01 | - |
| 5 | L13 | 高可用与备份 | ✅ 已完成 | 2026-09-01 | - |
| 5 | L14 | 保留策略与删除 | ✅ 已完成 | 2026-09-01 | - |
| 5 | L14 | 降采样与成本模型 | ✅ 已完成 | 2026-09-01 | - |
| 5 | L14 | 冷数据分层 | ✅ 已完成 | 2026-09-01 | - |
| 5 | L15 | 处理引擎架构 | ✅ 已完成 | 2026-09-01 | - |
| 5 | L15 | Python 插件与触发器 | ✅ 已完成 | 2026-09-01 | - |
| 5 | L15 | 内置插件与告警 | ✅ 已完成 | 2026-09-01 | - |
| 5 | L16 | Telegraf 采集生态 | ✅ 已完成 | 2026-09-02 | - |
| 5 | L16 | Grafana 可视化 | ✅ 已完成 | 2026-09-02 | - |
| 5 | L16 | 监控 InfluxDB 自身 | ✅ 已完成 | 2026-09-02 | - |
| 6 | L17 | 五款候选对比 | ✅ 已完成 | 2026-09-02 | - |
| 6 | L17 | 与 Prometheus 的关系 | ✅ 已完成 | 2026-09-02 | - |
| 6 | L17 | 关键维度对比表 | ✅ 已完成 | 2026-09-02 | - |
| 6 | L18 | 1.x/2.x → 3.x 差异清单 | ✅ 已完成 | 2026-09-02 | - |
| 6 | L18 | 迁移路径与工具 | ✅ 已完成 | 2026-09-02 | - |
| 6 | L18 | 版本升级注意事项 | ✅ 已完成 | 2026-09-02 | - |
| 6 | L19 | 选型决策树 | ✅ 已完成 | 2026-09-02 | 讲义已交付 |
| 6 | L19 | 三个场景演练 | ✅ 已完成 | 2026-09-02 | 讲义已交付 |
| 6 | L19 | 落地检查清单 | ✅ 已完成 | 2026-09-02 | 讲义已交付 |

## 评审记录

> 评审方式：`主 agent 内联`（A：技术事实核查；B：零基础教学体验）。

| 日期 | 评审对象 | 评审方式 | 意见摘要 | 处置 |
|------|----------|----------|----------|------|
| 2026-09-02 | **Phase 4 课程手册汇总**（`final-课程手册.md`，含实战项目收录） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮） | **P0=0 / P1=3 / P2=1，全部当批修复。** **【产物】** `final-课程手册.md` **1634 行**：封面导言 + 怎么读这份手册（六种状态路由表）+ 故事主线（六阶段转折表）+ **全局速查表（10 个硬数字，带「归属」列）** + 六阶段 × 19 课（每课含 本课目标 / 一图总结 / 🎯落地视角小结 / 讲义链接）+ 综合实战项目章节 + **决策清单**（学习目标=决策参考，规范要求的附加内容）+ 结课提示词。**【汇总方式】** 未手工重写，而是**从 19 课程序化抽取**「本课目标 + 一图总结 + 落地视角小结」拼装 —— 避免双源漂移（手写摘要必然与讲义脱节），并保证 19 课的图与小结**与讲义逐字一致**。**【P1-1 · 两课缺「本课目标」】** L18 / L19 讲义头部用的是「本课知识点」而非「本课目标」章节，自动抽取落空 → 校验器报「缺目标」。修复：**不回改讲义**（讲义结构一致，无必要），而是在生成脚本里加 `GOAL_OVERRIDE` 按知识点手工补写这两课的目标。**教训：抽取式汇总的健壮性取决于源文件的结构一致性，少数派结构必须显式兜底，不能靠正则是万能的假设。**【P1-2 · 数字口径不一致】** 手册决策清单与解法库场景 4 写「TCO 差 21 倍」，而 L19 原文口径是「运维人力是主机费的 **31 倍**」（$7,500/月 ÷ $240/月）—— 两个数字含义不同（一个是 TCO 差额倍率，一个是人力对主机的倍率），混用会让读者对不上账。修复：解法库场景 4 与手册决策清单**统一改为 31 倍口径**，并在手册里补出算式（0.5 FTE × $15,000/月 = $7,500/月），让倍率可被读者自己验算。**教训：汇总手册是「二次引用」的重灾区，倍率类数字必须回原文核对算式，不能凭印象取用。****【P1-3 · 实战项目章节缺「项目需求」】** Phase 4 规范要求实战项目章节含「项目需求 + 覆盖知识点地图 + 设计决策摘要 + 链接回 README」四项，初版只写了后三项 —— 需求被压缩进一句话表格行，读者看不出输入/输出是什么。修复：新增「### 项目需求」小节，显式列出输入（负载描述字段）、输出（四类结论）、三个预设负载各埋的坑与跑出来的结果。**【P2×1】** 手册「全局速查」表初版只有数字与含义，未标**归属 SKU** —— 而本课程最核心的方法论就是「约束是有归属的」。修复：新增「归属」列，并在表下加警示（把 Core 的约束套到别的 SKU 会得出荒谬结论，L19 实验 B 初版就踩过）。**【校验器实跑 10 项全绿】** ①19 课全部纳入且各有章节；②一图总结 19/19、落地小结 19/19、mermaid 图 19 个全部保留；③路径总览的故事主线/六阶段/全局速查均在开头，6 个阶段 overview 链接齐全；④实战项目四项内容齐全，**未误抄源码**（只做索引与决策摘要，已用源码关键字反查确认）；⑤决策清单四问齐全（该不该用/用哪个 SKU/成本怎么算/迁移代价）；⑥结课提示词齐全；⑦本地链接与 SVG 引用 53 个 **0 死链**；⑧内部锚点 6 个 0 失效；⑨标题层级无跳级；⑩代码块围栏 38 个成对闭合。**【本批最有价值产出】** ①**汇总手册必须程序化抽取，不能手写摘要** —— 手写必然双源漂移，而抽取能保证图与小结与讲义逐字一致；②**倍率类数字是二次引用的重灾区** —— 「21 倍」与「31 倍」都看起来对，只有回原文核对算式才能发现口径不同；③**「归属」列比数字本身更重要** —— 速查表里的 432 / 72h / 库5表2000列500 全是 Core 的属性，不标归属就等于在手册里重复本课程反复批评的那个错误 | 通过，落盘 |
| 2026-09-02 | **Phase 5 实战经验 / 排障速查手册 / 场景解法库**（`08-` / `09-` / `10-` 三份一次生成） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮 + 专项 B1-B8，场景解法库另叠 C1-C7） | **P0=2 / P1=2 / P2=2，全部当批修复。** **【产物】** 三份共 870 行：`08-实战经验.md`（284 行，学习态）、`09-排障速查手册.md`（220 行，使用态）、`10-场景解法库.md`（366 行，设计态）。**【P0-1 · 回指链断裂】** 手册索引有 9 条症状，但经验层只定义了 8 个故障模式 —— 症状 9（认证与端点 401/404）的「原理」栏写着「经验层未单列故障模式」，**读者按编号翻过去会扑空**。此为「先写手册后补经验层」的典型后果：手册是压缩产物，编号一致性只能靠校验器保证。修复：经验层补故障模式 9（401 语义 / 404 语义 / v3 原生端点 `/api/v3/write_lp` vs 客户端默认 V2 端点 / host 必须带 scheme，回扣 [阶段 2 · L3](stages/2-上手篇/lessons/lesson-03-环境搭建与第一次写入.md)），并修正引言的「已收录 8 条」→ 9 条。修复后校验器确认 **故障模式 1-9 与手册回指双向一致，0 悬空**。**【P0-2 · 引用官方条文却导向相反结论】** 故障模式 7（压缩失效）引用官方《模式设计建议》「标签值基数不会影响数据库的整体性能」来论证基数的危害 —— **读者照官方原文理解，会得出「基数完全无害」并与本条结论直接冲突**。根因：官方这句话针对的是 **1.x/2.x 的 TSM 索引爆炸痛点**（高基数会让索引与内存直接崩），3.x 换引擎后确实解决；但**点密度影响的是压缩率**，是另一条链路。修复：把易混点从「一句话夹带」升级为独立的 ⚠️ 辨析块，明确「3.x 上基数高不慢，但基数高占地方」，并在溯源栏标注**该官方表述不可外推为『基数对存储无影响』**；场景解法库场景 2 同步改写并回指经验层。**【P1×2】** ①三份产物只在各自文件末尾单向列出另两份的链接，解法库缺「什么时候该翻哪份」的入口说明 → 解法库顶部补三态导航（出事→手册 / 学原理→经验 / 做设计→解法库），并给出推荐读法「先按场景做设计决策，再按『做错会踩的坑』的编号回经验层补原理」；②经验层三件套导航表缺该读法建议 → 已补。**【P2×2】** 索引表紧急度分布未统计（🔴5/🟡5/⚪1）；解法库各场景「做错会踩的坑」密度不均 → 保留现状（每个场景均已挂钩故障模式编号，不强求条数）。**【专项维度 B1-B8 自查】** B1 证据纪律 ✅（9 条故障模式全部标注官方来源 + 核查时点 2026-09，432 报错原文引自第三方在 Core 3.11.0 的实测复现并注明「是验证非生产事故」）；B2 宁缺毋滥 ✅（手册如实写「已收录 9 条」；**真实事故案例章节主动留空并说明原因** —— 检索未找到公开可查证的 InfluxDB 3 生产事故 postmortem，按证据纪律「虚构事故比没有更糟」不虚构）；B3 三态不混 ✅（经验层讲原理、手册只给动作、解法库给多方案权衡，手册每条「原理」只写编号不写锚点）；B4 症状可观测 ✅（每条「一眼识别」均为报错原文或可观测现象，无「服务很慢」这类无效描述）；B5 先止血后定位 ✅（每条动作表首行均为「止血」，症状 2 明确「抢时间：物理删除是异步的」）；B6 分支判定 ✅（400 vs 503 在症状 5 显式分叉，401 vs 404 在症状 9 显式分叉）；B7 有升级出口 ✅（每条均有「若无效 → 转 debug」或转具体条目）；B8 手册从经验层同源压缩 ✅（9 条症状 ↔ 9 个故障模式一一对应，非独立重写）。**【场景解法库 C1-C7 自查】** C1 多解法 ✅（6 场景解法数 5/5/4/4/6/4，均 ≥3）；C2 先想后看 ✅（每场景 2 个 `<details>`：💡 提示 + 📖 展开解法）；C3 知识点挂钩 ✅（每场景均回指具体课时，覆盖阶段 1-6）；C4 递进路径 ✅（每场景有「推荐路径」，按「先便宜后贵」排序，如场景 1：确认 SKU → 降采样 → 算聚合表文件数 → 才轮到换 Enterprise）；C5 不适用边界 ✅（每场景均有「什么情况下此方案不适用」）；C6 做错会踩的坑 ✅（每场景均回指经验层故障模式编号，形成设计态→学习态闭环）；C7 场景来源真实 ✅（6 场景全部来自课程已教内容的真实延伸：432 超限 / 基数与压缩 / 迁移静默反转 / 五款选型 / 写入峰值 / 处理引擎边界）。**【校验器实跑】** 编号一致性：故障模式 1-9 ↔ 手册回指 1-9，0 悬空；手册五特征：索引 11 条 ↔ 正文 9 条一一对应、紧急度一致、每条均含「一眼识别 / 止血 / 若无效 / 预防 / 原理」；解法库四硬要求：6 场景 × (多解法 ≥3 + 2 个 details + 推荐路径 + 知识点挂钩 + 不适用边界 + 做错会踩的坑) 全部达标；三份产物本地链接 58 个 0 死链。**【本批最有价值产出】** ①**手册的编号是契约，不是装饰** —— 先写手册再补经验层，编号断链只在校验器跑过后才暴露，这是继 Phase 3「文档里的原则不会自动变成代码」之后同一教训的又一次验证（本次靠脚本比对而非人工核对抓出）；②**引用官方原文不等于结论成立** —— 官方「高基数不影响性能」在它的语境里为真，但被搬到「存储成本」语境下会导向相反结论，引用官方条文时必须连它的**语境**一起引用；③**宁可留空也不虚构** —— 真实事故案例章节因找不到可查证的 postmortem 主动留空，比编一个听起来合理的故事更符合本课程的证据纪律 | 通过，落盘 |
| 2026-09-02 | **结课实战项目《时序数据平台落地》**（Phase 3，跨阶段 3/4/5/6） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮 + 项目专项 A1-A6） | **P0=2 / P1=3 / P2=3，全部当批修复。** **【产物】** `projects/时序数据平台落地/`：四份文档（README 118 / 设计决策 141 / 反例对照 102 / 验收清单 65 行）+ 7 个实现文件共 1,016 行（config 192 / decision 240 / ops 278 / engine 157 / main 137 / schema_design 132 / tco 112 / bad_example 73）。**跨 4 个阶段**（3/4/5/6，门槛 ≥3），知识点地图 16 条回指讲义、0 死链。**复杂度四门槛全达标**：跨阶段 ✅ / 非功能约束 4 项 ✅ / 真权衡决策 5 个 ✅ / 多文件工程 ✅。**【P0-1 · 无条件告警 vs 命中检测】** 迁移检查无条件输出 4 条、保留期检查无条件输出 2 条，**不管实际写的是 `90d` 还是 `0d`** —— 一个用来**教 reverse/drift 分类**的模块，自己却不对输入做分类判断。**嘴上讲分类、手上无差别告警**，与 L17「标注慎引却打满分」、L19「批评想托管却推荐自托管」是**同一类错误的第三次出现**。修复：`MIGRATION_TRAPS` 加命中判断函数（命中 reverse=P0 / drift=P1，未命中降 INFO 并注明「什么情况下会命中」，保留教学价值但不占 P0 名额）；`check_retention` 改为 `parse_retention` 解析实际写法再分支。**【P0-2 · 判据选绝对量还是相对量】** drift 严重度按绝对天数判（`diff >= 1`）导致 `1y` 差 0.2 天（相对 **0.07%**）在有合规要求时判 **P0**，而 `3mo` 差 1.3 天（相对 **1.45%**）却判 **P1** —— **严重的排在轻微的后面**。修复：改按**相对偏差 ≥ 0.3%** 判，输出里同时给出绝对天数与百分比，并写明判据本身。**【P1×3】** ①设计决策 3 声称「常量集中在 config.py」，但 `0.012`（查询单价）与 `240.0`（主机费）硬编码在业务模块 —— **文档说一套、代码做一套** → 全部改为 config 常量；②**AST 校验抓出** `engine.py` 在打印「⭐ 432 是 query-file-limit 的默认值，3 天是它的派生值」时，**计算派生天数的表达式里写死了 `432` 和 `10`** —— 一个正在讲「约束要集中管理」的模块自己却在硬编码（正则搜索会被 docstring 误报，遍历 `ast.BinOp` 子节点才抓到）→ 改用 `limit` 与 `CORE_MAX_QUERY_DAYS`；③TCO 分项之和 ≠ 展示总数（写入费 $37,079 未列出，读者看到 $2,538 + $1,500 ≠ $41,116）→ 改为遍历所有非零分项并加 `assert` 自校验，同时标出「写入费占比 90%」。**【P2×3】** 保留期字段缺失导致无法检测 / README 汇总数字与实跑不一致（空格格式）/ 退出码语义未文档化。**【教学法视角 A1-A6 专项】** A1 跨阶段整合 ✅（4 阶段，16 条回指）；A2 非功能约束 ✅（正确性·约束归属 / 可维护性·开闭 / 可审计·来源追溯 / 诚实·不编造数字）；A3 真权衡决策 ✅（5 个，每个含候选对比+选择+代价+翻转条件，决策 4 单列排雷顺序这一「顺序即正确性」项）；A4 规模 ✅（7 文件 1,016 行，非单文件脚本）；A5 可运行性 ✅（本机 Python 3.12.3 实跑，纯标准库零依赖）；A6 反例质量 ✅（`bad_example.py` 能跑通且初看更简洁，8 条逐项对比）。**【学习者视角 L1-L6】** L1 场景吸引力：三份预设负载都是真实岗位会遇到的（IoT 遥测 / K8s 监控 / 需 JOIN 的业务指标），且各埋了一个真实世界的坑；L2/L3 无倒序（先排雷再体检，约束归属决定下游该报什么）；L4 结构完整（两阶段流水线 + 分级汇总 + 退出码）；L5 困惑点全覆盖（验收清单四层 20 项 + 反例对照 9 条 + 设计决策 5 个）；L6 全局定位：README 末节把项目接回全课程故事主线（主角=时间，收束=能回答那个大问题）。**【本机实跑】** `main.py` rc=1（491 行，三场景 **P0=21 P1=21 P2=7 INFO=40**）、`main.py iot` rc=1、`main.py nope` rc=3、`bad_example.py` rc=0；AST 硬编码校验 0 处违规；文档内链接 21 个 0 死链；知识点地图回指 16 个 0 死链。**【本批最有价值产出】** ①**反例 rc=0 而正确版 rc=1** —— 一个「成功跑完」的脚本可能什么都没查出来；反例报出的 5 条里有 3 条是「写进去就会报错、自己能发现」的，而唯一致命的 `0d` 反转一条都没报（**它看起来确实查出了 5 个问题**，比明显漏检更隐蔽）；②**成本差 21 倍**（$366 vs $7,780）的根因是漏算运维人力，而运维人力是主机费的 31 倍；③**文档里的原则不会自动变成代码** —— 本项目三次自相矛盾（无条件告警、常量散落、讲约束却硬编码）都是「原则写在文档里但代码没跟上」，最终靠**校验器**（退出码 + AST 遍历 + 链接检查）而非自觉抓出来，这是本项目对「怎么防止自相矛盾」给出的答案 | 通过，落盘 |
| 2026-09-02 | **阶段 6 · 课19《场景演练与选型决策》**（3 知识点，**全书收官**） | **主 agent 内联**（教学法 5 维度 + 学习者 L1-L6 两轮独立评审） | P0-1：实验 B 把 Core 的 432 文件上限无条件套用到全部五个 SKU，输出「最小调度周期需 ≥ 1216.7 分钟」（≈20 小时）的荒谬结论 —— **约束归属错误**。修复：新增 `SKU_FILE_LIMIT` 按 SKU 分列判定，Core 硬约束 vs 其他 SKU 有 compactor 免检。P0-2：TCO「最省方案」与实验 A 的 BLOCK 结论互相打架（成本排序不能推翻可行性结论）。修复：TCO 对比前加口径对齐声明。P1-1：实验 A 的 `want_managed` 字段只用于排雷、未进打分逻辑，导致「想托管 + 1 人运维」的 hybrid 场景仍被推荐自托管 Enterprise —— **而讲义正文正在批评这件事**（与 L17「标注慎引却打满分」同类）。修复：想托管而选自托管直接判 1 分，并新增**约束冲突检测**（步骤 1.5），让脚本自己报出「约束之间打架」，hybrid 与 IoT 两场景触发。P1-2：讲义未同步新的冲突检测结论。P1-3：速查卡 1 未含冲突检测步骤（已扩为六步）。P2×4：脚本行数标注过时 / 实验 A 流程描述缺步骤 / 误区表未含冲突类（已补第 25、26 条）/ 场景四说明与脚本行为不一致。**【备课：官方一手核实】** ①**五个 SKU 定位**（which-influxdb-3 页）：Core = *"free, open-source, single-node release"*、Enterprise = *"For new production workloads"*、Serverless = *"No native v3 write API"* + *"No Processing Engine"* —— **Serverless 不是 Core 的托管版，两者能力集合不同**（Core 有这两项，Serverless 有 compactor）②**Serverless 四个官方单价**：写入 $0.0025/MB、查询 $0.012/100 次、存储 $0.002/GB-hour、出流量 $0.09/GB；免费层写入 5MB/5min、查询 300MB/5min、保留 30 天、2 库③**三种许可证**（license 页）：Trial 256 核/30 天/**不可商用**、At-Home **2 核**/永不/单节点/**不可商用**、Commercial 按合同；**按集群不按机器**，批次 8/16/32/64/128 核④**Core→Enterprise 无需搬数据但 Downgrading is not supported**（单向门）⑤**Enterprise 100+ 库 / 10k+ 表**（⚠️ 官方博客口径，非文档页，保守取下限）⑥**Enterprise 商业授权费不公开**（官方仅 Contact Sales）→ 以 ❓ 标注，未做估算。**【本批最有价值产出】** ①**约束是有归属的** —— 432 文件上限是 **Core 的属性**不是 InfluxDB 的属性；初版套用到全部 SKU 得出「降采样必须每 20 小时跑一次」的荒谬结论，此为「约束归属」检查法：**把 A 的约束套到 B 上，和把 A 的优点算到 B 头上，是同一类错误**②**选型是两次决策不是一次** —— 「我们选了 InfluxDB」没有信息量，五个 SKU 硬约束差异极大，四个场景三个把 Core 挡在门外③**打分器会输出冠军，但不会告诉你约束本身有问题** —— 新增约束冲突检测，区分「某 SKU 不满足约束」（排雷）与「约束之间打架」（冲突），后者只能回需求侧改④**成本排序会被授权费翻转** —— IoT 场景运维人力（0.5 FTE）是主机费的 **31 倍**。**【四向逐字一致性校验通过】** 讲义 python 块 ↔ 脚本文件（**607 / 515 行**）、讲义 text 块 ↔ 运行时 stdout（**245 / 282 行**）逐字一致；围栏 44 全闭合、details 5/5、题号 1-5 连续、占位符清零、九项结构件与五幕齐备、本地链接 7 个 0 死链。**【官方文档冲突 3 条，双面呈现】** ①Enterprise 库表上限（官方博客 100+/10k+ vs 文档页未给数字 → 取博客下限并标 ⚠️）②Docker `latest` 变更日期官方自给多个（2026-02-03 / 2026-09-15 / 中文站 2026-04-07）→ **不引用日期，只采用行动建议** ③Serverless 是否有长程查询 → 保守按「有 compactor」处理并标 ⚠️ 量级判断。**【学习者视角】** L1「技术评审会拍板说了句『那就 InfluxDB 吧』，第三周报表跑不出来」——一句话揭示「选了产品不等于选了 SKU」，代入感强；L2/L3 无倒序（两次决策 → 五个 SKU → 三个场景 → 检查清单）；L4 五幕完整，**第二幕四个反直觉事实覆盖三知识点**；L5 困惑点全覆盖（**26 条误区** + 5 题小测 + 5 张速查卡 + 六步选型法）；L6 含阶段 6 进度 + 全书进度 19/19 + 全书收官动作。 | 通过，落盘 |
| 2026-09-02 | **阶段 6 · 课18《迁移指南：从 1.x/2.x 到 3.x》**（3 知识点） | **主 agent 内联**（教学法 5 维度 + 学习者 L1-L6 两轮独立评审） | **P0×0 / P1×5 / P2×5，全部当批修复。** **【备课：官方一手核实】** ①**术语映射官方原话是 `is synonymous with`**（schema-design 页）：v2 **bucket = 3.x database**、v1/v2 **measurement = 3.x table** —— 是「同一个东西换名字」不是「相似概念」 ②**InfluxQL 的 DBRP 倒灌命名约定**（Core create database 页）：rp 从数据模型消失后又从命名规范回归，用 InfluxQL 查询的库名必须写成 **`database_name/retention_policy_name`** ③**官方 write-data 页精度矩阵**：`precision=m/h` **只有 v1 `/write` 端点支持**，v2/v3 均不支持；且**默认值从「纳秒」变为「auto（按量级猜）」**，换端点不改代码 = 换了解释方式 ④**导出必须 `-lponly`**，官方原文 *"export line protocol **without InfluxQL DDL or DML**"*，不带它导出的 CREATE 语句会让**整批**写入失败 ⑤**Flux 官方明确无兼容层**，GA 博客原话 *"we currently **regrettably cannot offer a direct compatibility layer**"*，出路是 Explorer 1.9 的 Flux to SQL converter（**beta**，官方原话 *"AI-generated, so its output can vary. **Review the converted SQL before running**"*），且官方自标行为差异：Flux `map()` **保留所有列**，转出的 SQL 只选写的列 ⑥**GA 博客原话**：插件系统是 *"the natural successor"* of **Continuous Queries / Tasks / Kapacitor / Telegraf** ⑦**3.10.0 release notes 原文**：*"Upgrading to InfluxDB 3.10 is a **one-way migration**"*、*"Restoring these objects is **the only way** to roll back to 3.9.x"*，备份路径按 **3.4.0 分界**（≥3.4.0 备 `catalog/v2/logs/` + `catalog/v2/snapshot`；<3.4.0 备 `catalogs/` + `_catalog_checkpoint`），且官方明说旧目录 *"may still be present as **leftovers**... **aren't a valid rollback source**"* ⑧**Docker latest 变更**：官方全站横幅提醒 `latest` 将指向 InfluxDB 3 Core，建议用固定版本标签。**【P1×5，全部当批修复】** ①**静默项口径混淆**（实验中）：保留期专项把「语义反转（`0d` 想永久结果全删）」与「幅度偏差（`3mo` 想 91.3 天只留 90 天）」都标成同一个「静默」，后果差一个量级 → 给 `RetentionCase` 新增 `kind` 字段（`reverse` / `drift`）分两类，专项案例 5→7 个（补「降采样库 432 超限」与「合规库 3y 换算」两个**同构场景**），收束段分别统计并**标明清单的「2 条静默」与专项的「4 条静默」口径分开、不合并**。②**版本升级在七阶段里无处安放**（实验中）：「已装 3.9.5 直接升 3.10」不是数据迁移，却会被读者拿七阶段去套 → 对照 3 末尾补**独立升级路径**（四步，不可逆点在第 ④ 步启动 3.10 就已出现，**比数据迁移的 S7 靠前得多**）；速查卡 4 同步补该分支。③**3.11.0→3.11.0 案例自相矛盾**（实验中）：原地升自己，无意义 → 改为 3.10.0→3.11.0 的真实升级场景，并给 `pick_catalog_backup` 补 **≥3.10 分支**（catalog 已是 v3，备份 `{prefix}/catalog/`，但**备它也不能退回 3.9.x**）。④**磁盘开销未估**（实验中）：只说「分批导出」，未提导出体积 → 补**磁盘开销估算表**（按 1.2 倍预留），并点明**行协议是文本格式无压缩，导出体积通常大于 TSM 内部存储**，磁盘满会导致导出中断。⑤**双写时长无判据**（实验中）：只说「双写一段时间」，未给结束条件 → 收束段补算法：**下限 = 覆盖对账周期（1-3 天），上限 = 可查窗口（Core = 3 天）**，故 **Core 上双写窗口实际只有 1-3 天，很窄，S5 对账脚本必须提前写好**；同步写入速查卡 4、误区表第 22 条、落地视角第 11 条。**【P2×5】** ①第一条报错消息未给原文 → 补 `` `Query exceeded the limit of 432 parquet files` `` ②M05 的 `organization` 留空串缺边界 → 限定为「客户端库」场景（Telegraf 侧为 L16 已核实的另一口径）③事实一缺「检查动作」→ 补「建库后立刻 `influxdb3 show databases` 确认不是 `0d`」④「CQ/Task 被移除」无出处 → 补 L9 已核实口径 + ⏳ 实测建议（`SHOW CONTINUOUS QUERIES` 验）⑤实验 A/B 解读段未同步新内容 → 补两类静默区分、7 案例、版本升级分支、磁盘估算、双写窗口。**【四向逐字一致性校验通过】** 讲义 python 块 ↔ 脚本文件（**500 / 471 行**）、讲义 text 块 ↔ 运行时 stdout（**266 / 212 行**）全部逐字一致；围栏 44 全闭合、details 5/5、题号 1-5 连续、占位符清零、九项结构件与五幕齐备、本地链接 10 个 0 死链。**【官方文档冲突 3 条，双面呈现】** ①v3 端点精度写法：官方 Core/v3-write-lp 页**全拼**（`second`/`millisecond`/…）vs AWS Timestream 集成页**缩写**（`s`/`ms`/`us`/`ns`）→ 取保守口径写全拼 ②Docker `latest` 变更日期：官方自身给 **2026-02-03** vs **2026-09-15**（中文镜像站 **2026-04-07**）→ **不引用具体日期**，只采用行动建议（固定版本标签）③v2 端点是否支持 m/h：官方 Core 矩阵**不支持** vs AWS 集成页参数表含 `m,h` → 以官方 Core 页为准。**【本批最有价值产出】** ①**「静默」必须再分两类** —— `reverse`（方向反了，灾难级：想永久结果全删、降采样库 432 超限变成「存得下但查不到」）vs `drift`（幅度偏了，合规级：`3mo`=90 天 vs 91.3 天、`3y`=1095 天 vs 1096 天）。混为一谈会让合规问题挤占对灾难问题的注意力 ②**不可逆点不止一个，且版本升级的不可逆点比数据迁移靠前得多** —— 数据迁移 S1-S5 随时可退、S6 收窄、S7 关死；而 3.9.x→3.10 在第 ④ 步（启动 3.10）就已不可逆。**用迁移的节奏去套升级，是本课最容易犯的结构性错误** ③**双写窗口可以被算出来而不是拍脑袋** —— 上限由可查窗口反推（Core = 3 天），得出「Core 上只有 1-3 天」这一反直觉结论：双写写太久，最早的数据反而查不到了，双写失去对照意义。**【学习者视角】** L1「迁移跑完了，一条数据都没少」+ 三小时后两条追命消息，三条消息恰好对应三个不同性质的坑（432 硬约束 / 3 天窗口能存≠能查 / `0d` 静默反转），代入感强且结构清晰；L2/L3 无倒序（四个反直觉事实→术语与端点→迁移路径→升级注意事项→两个模拟器实跑）；L4 五幕完整，**第二幕四个反直觉事实覆盖三知识点**；L5 困惑点全覆盖（**23 条误区** + 5 题小测 + 6 张速查卡）；L6 含阶段 6 进度 + 全书进度 + L19 收官预告。 | 通过，落盘 |
| 2026-09-02 | **阶段 6 · 课17《横向对比：五款候选》**（3 知识点，**阶段 6 开篇**） | **主 agent 内联**（教学法 5 维度 + 学习者 L1-L6 两轮独立评审） | **P0×2 / P1×1 / P2×2，全部当批修复。** **【备课：官方一手核实】** ①**Prometheus 官方 FAQ 原文**：*"pulling is slightly better than pushing, but it should not be considered a major point"*——拉模式只是**略好、非决定性因素**（几乎所有中文介绍都把它吹成革命性设计）②**Prometheus 官方 storage 页**：本地存储**不集群不复制**，*"not meant as durable long-term storage"*，默认保留 **15d**，**1-2 字节/样本** ③**VictoriaMetrics 官网 20x/10x/70x/7x 全部是厂商自述**，且全部带 `up to`（上界）④**ClickHouse 官方 MergeTree 页**：*"Partitioning does not speed up queries (in contrast to the ORDER BY expression)"*——剪枝靠 ORDER BY 的稀疏索引（`index_granularity` 默认 8192），分区只用于生命周期管理；按客户 ID 分区会触发 `Too Many Parts`（默认 3000）⑤**TimescaleDB hypercore 列存 >90% / up to 98%**，但**连续聚合只有 61%**（官方博客实测，因用 bytea 存部分聚合）⑥**VM 官方自述定位**：*"long-term remote storage for Prometheus"*，支持 InfluxDB line protocol，**Remote Write 2.0 不支持** ⑦**Telegraf 插件数官方自相矛盾**：文档页/产品页 **400+** vs GitHub README（v1.39）**over 300** → 保守取 300+。**【P0×2 —— 两个都是「方法论懂了、手上做错」】** ①**实验 A 自评发现**：给 VictoriaMetrics 压缩维度打满分 5 分，理由却写「⚠️ 厂商自述，慎引」——**嘴上说慎引、手上打满分**，正是本课要警告的错误 → 已降为 4 分，并把自检写进脚本输出。②**实验 B 分类缺陷**：把「InfluxDB Core 可查窗口 3 天」当成性能宣称去审，结论是「⚠️ 厂商自述不能裸写数字」——**而它恰恰是全课最硬的排雷依据**。根因是审计器缺 `claim_kind` 分类维度，拿审性能宣称的问法（Q1 厂商自述 / Q4 配置披露 / Q5 峰值稳态）去审官方硬约束，必然误伤 → 新增 **Q0 分类**（benchmark 性能宣称六问全审 / spec 官方规格只审来源与版本 / misread 理解错误一律判死），脚本升级 v2，宣称清单 10→11 条，可写结论从「0 条」变为「3 条（全为官方规格类）」。**【P1×1 / P2×2】** ①P1：Telegraf 插件数口径冲突，全文 8 处统一为「300+（保守口径）」并新增冲突记录第 4 条 + 官方文档表补 2 条一手来源。②P2：实验 B 解读段引用旧结论（10 条 / 0 条可写 / 第 10 条）→ 全部同步为 v2 新结论。③P2：重跑脚本只替换 text 输出块、未同步 python 源码块，导致源码与脚本不一致（校验器抓出）→ `_l17_repaste.py` 补 `replace_py_block`。**【四向逐字一致性校验】** 讲义 python 块 ↔ 脚本文件（**459 / 391 行**）、讲义 text 块 ↔ 运行时 stdout（**153 / 163 行**）全部逐字一致；围栏 54 全闭合、details 5/5、题号 1-5 连续、占位符清零。**【官方文档冲突 4 条，双面呈现】** ①TimescaleDB 压缩率（up to 98% vs >90%）②VM 性能倍数（无第三方复现，不采用）③Prometheus 长期存储（"not meant as durable" vs "possible to retain years"，判为**持久性 vs 可行性**两件事）④**Telegraf 插件数（400+ vs over 300）**。**【本批最有价值产出】** ①**层次差**：InfluxDB 是发动机、Prometheus 是整车，拿组件比整体是选型文档最常见也最致命的错误 ②**先排雷再打分**：实验 A 四个场景四个不同冠军（VM / VM / TimescaleDB / ClickHouse），**没有一款赢下全部场景**；且场景 1 里 InfluxDB Core 不是「分数低」而是「查不到」③**Q0 分类**：数字要先分「性能宣称 / 官方规格 / 理解错误」三类再审问，否则会把最硬的约束误伤成软数字——**审计器自己也该被审计**。**【学习者视角】** L1「选型会上三个问题把你问住」代入感强；L2/L3 无倒序（层次差→推拉模式→共存组合→六维权衡）；L4 五幕完整，**第二幕四个反直觉事实覆盖三知识点**；L5 困惑点全覆盖（**17 条误区** + 5 题小测 + 8 张速查卡）；L6 含阶段 6 进度 + 全书进度 + L18 预告。 | 通过，落盘 |
| 2026-09-02 | **阶段 5 · 课16《生态集成：Telegraf、Grafana 与自监控》**（3 知识点，**阶段 5 收官**） | **主 agent 内联**（教学法 5 维度 + 学习者 L1-L6 两轮独立评审） | **P0×2 / P1×1 / P2×2，全部当批修复。** **【备课：官方一手核实】** ①**`outputs.influxdb_v3` 自 Telegraf v1.38.0 引入**，走 `/api/v3/write_lp`；版本不够时配置写对也起不来且不直说 ②**多 URL 语义 = 每个 flush 周期随机挑一个写入，失败才换下一个 → 故障转移，非双写、非负载均衡**（要双写须写两个 output 块）③**`database_tag` 目标库数量 = 该 tag 的去重值个数**，撞 Core **5 库上限**（回扣 L6）④**`sync=false` 即 L10 的 `no_sync=true` 换名**；`content_encoding` 默认 `gzip`；`timeout` 默认 `5s` ⑤**`influxdb_v2` 插件写 Core 时 `organization` 必须留空串** ⑥**Grafana Product 选 InfluxDB Enterprise 3.x**（官方原话 *"currently, no Core menu option"*）⑦**SQL 走 Flight SQL(gRPC) 必须 HTTP/2，InfluxQL 走 HTTP/1.1 无此要求** ⑧**`/health`、`/api/v1/health`、`/ping`、`/metrics` 默认都需要认证**；**`/ping` 必须用 GET，HEAD 返回 404** ⑨**`system_metrics` 官方插件采的是主机**（依赖 psutil），名字里的 system 指 OS ⑩**遥测默认每小时上传一次**，`--disable-telemetry-upload` 关闭。**【P0×2】** ①**实验 B 输出块与实跑输出不一致**：讲义「对照 3」写 5s 单面板 **432,000**，实跑为 **86,400**（差 5 倍，因口径混用了「每面板 5 query」与官方算例的「每面板 1 query」）→ 已修正脚本（`SEC_PER_DAY // sec` 整除）、在脚本内显式标注官方算例口径、并同步知识点 2 与速查卡 5 两处表格，全文校验旧值清零。②**修复脚本 marker 重复导致讲义段落被误删**：`_l16_fix.py` 用的锚点「**真实输出**（本机 Python 3.11.15 实跑，逐字回贴）：」在实验 A/B 中各出现一次，替换区间越界，误删了实验 A 输出块与实验 B 标题/引导/源码块 → 已用 `_l16_fix2.py` 精确重建。**【P1×1 / P2×2】** ①P1：实验 B 初版只贴了对照 1、其余三组只给核心表格，违反「实验输出必须逐字回贴」规范 → 已改为**完整 stdout 逐字回贴**（161 行）。②P2：第五幕 L17 预告写「Prometheus / TimescaleDB / QuestDB」，与阶段 6 overview（SSOT）的「InfluxDB 3 / TimescaleDB / VictoriaMetrics / ClickHouse / Prometheus」不一致 → 已按 SSOT 修正。③P2：实验 C 第 3 步未体现 `/ping` 的 GET 约束 → 已补 `curl -I` 反例对照。**【四向逐字一致性校验】** 针对 L15 五个 P0 的根因（源码块手工粘贴未校验），本课编制 `_l16_verify.py` 做四向比对：**讲义 python 块 ↔ 脚本文件（356 / 245 行，逐字一致）**、**讲义 text 块 ↔ 运行时 stdout（102 / 161 行，逐字一致）**，围栏 36 个全闭合、details 21/21 配对、题号 1-5 连续、占位符清零。**【官方文档冲突 3 条，双面呈现未裁决】** ①Grafana/Flight SQL 端口：官方 Core 文档给 **8181** vs 社区 docker-compose 的 **8082**（开源社区版/IOx 形态）→ 以 Core 官方 8181 为准 ②`system_metrics` 定位（主机指标 vs 易被误读为数据库内部指标）→ 采信主机指标，有 psutil 依赖与 measurement 清单为证 ③Grafana Product 下拉无 Core 选项 → 采信官方原话。**【本批最有价值产出】** ①**「SQL 面板连不上、InfluxQL 面板正常」是排障第一分叉点**——非对称现象直接指向 HTTP/2，可跳过 token/端口的无效排查 ②**自监控数据自己也只受 3 天可查窗口**（432÷144=3），且**文件数只跟墙钟有关、跟数据量无关**（与 L15 的 WAL 触发器一天 86,400 次同构）→ 故「自监控留 90 天」= **存得下但查不到**，且**砍掉一半指标一个文件都不会少** ③**引用官方「10s 面板 ≈ $31/月」必须对齐口径**（那是单 query，本表是 5 query，差 5 倍）。**【学习者视角】** L1「运维三问（数据从哪来 / 给谁看 / 谁监控监控者）」承接 L13-L15 的「一台能跑的 InfluxDB」，指向明确；L2/L3 无倒序依赖；L4 五幕完整，**第二幕四个反直觉事实覆盖三知识点**（Grafana 要选 Enterprise / SQL 需 HTTP/2 / 端点默认需认证 / 自监控只有 3 天）；L5 困惑点全覆盖（**16 条误区** + 5 题小测 + **10 张速查卡** + 上线前检查清单 12 项）；L6 含阶段 5 完整闭环图 + 阶段/全书进度 + 阶段 6 预告。 | 通过，落盘 |
| 2026-09-01 | **阶段 5 · 课15《处理引擎：Python 插件与触发器》**（3 知识点） | **主 agent 内联**（教学法 5 维度 + 学习者 L1-L6 两轮独立评审） | **P0×5 / P1×0 / P2×0，全部当批修复（P0 全部源于同一个 off-by-one）。** **【备课：官方一手核实】** ①**WAL 触发器绑的是 WAL flush 事件，默认每秒 1 次，与数据量无关** → 一天 **86,400 次**，是 `every:1d` 的 **8.6 万倍**（官方原文 *"when the database flushes data to the Write-Ahead Log (WAL) (by default, every second)"*，回扣 L10）。②**`plugin-dir=""` 空字符串不禁用处理引擎**——config-options 原文 *"does not disable... You must comment out, remove, or unset"*。③**所有插件共享同一 Python 进程**（官方：*"runs all plugins in the same Python process. Changes made by one plugin can affect other plugins"*）→ 插件性能预算要从写入侧扣（回扣 L13 四功能竞争）。④**取消信号是 `KeyboardInterrupt`（`BaseException` 子类）**，`except Exception` 吞不掉；`QueryError` 无法 import，`except QueryError` 会 `NameError`，须 `except Exception` + `type(err).__name__`。⑤**`gh:` 前缀非 GitHub 专用**（接受任意 HTTP/HTTPS URL），且**每次触发器启动都重新拉取**（服务重启 / 重新 enable 均会拉）→ 供应链风险，查 `system.plugin_files` 的 `last_modified` 验代码版本。⑥**Notifier 是 HTTP 触发器的发送端**，仓库依赖表显示五个检测器（Threshold Deadman / MAD / ADTK / State Change / Forecast Error Evaluator）的 `required_plugins` 均为 `Notification sender` → **装告警必须两件套**。⑦**Amazon Timestream 不允许自定义插件**，只能跑认证插件（影响形态选型，回扣 L13）。⑧缓存**重启即清**、**生产不设 TTL 永不过期**。 **【P0×5 —— 同一根因：递归写回算错一位】** 实验 A 对照 4 的循环在 `range(1,21)` 结束后 `rows` 已是**第 21 秒**的值，却被打印成「第 20 秒」，导致**讲义 7 处**（第二幕事实二、红线表、一图总结、三句话收束、速查卡 4、实验小结、课后小测 Q3）统一引用了「20 秒 1 亿行」这一错误数字，且**课后小测 Q3 的答案「14 秒」也错**（正确为 **15 秒**：第 n 秒 = `100×2ⁿ⁻¹`，第 1 秒尚未翻倍）。修复：脚本新增 `flushed_at_20` 记录第 20 秒真值并改印两行（20 秒 **52,428,800** / 21 秒 **104,857,600**）；重跑后**整体重生成**讲义中的源码块与输出块；再全文统一 7 处口径；Q3 答案改为 15 秒并补「易错点：别把第 1 秒算成翻倍」。**【官方文档冲突，双面呈现未裁决】** ①`table_batch["rows"]` 每行结构：API reference 为**扁平 dict**（`row["host"]`）vs 官方博客《Preventing Alert Storms》用嵌套 `row["tags"]["host"]` → 采信 API reference；照博客写会 `KeyError`，且被默认 `--error-behavior log` 吞掉，表现为「插件没报错但就是不告警」；②HTTP 入口名 `process_request`（API reference）vs `process_http_request`（仓库 README/DeepWiki）→ 采信 API reference；③官方插件数 **11 个**（Core 文档页）vs **23 个**（官网 Plugin Directory）→ 两者皆官方口径，更新节奏不同。 **【本批最有价值产出】** **「WAL 触发器一天 86,400 次」** 与 **「写回被监听表 20 秒破 5,000 万行/秒且零报错」** —— 后者是处理引擎唯一的**静默自毁**路径（数据库视角一切正常，插件在正确无误地执行一个错误的逻辑），已固化为红线一、速查卡、Q3 考题与 CI 门禁（实验 B 的 P0-1）。**【学习者视角】** L1「运维三问（降采样谁跑 / 告警怎么做 / 脏数据怎么办）」承接 L14 三层保留方案，三问指向同一答案；L2/L3 无倒序依赖；L4 五幕完整，**第二幕四个反直觉事实覆盖三知识点**（WAL 频率 / 递归爆炸 / 微批处理 / gh: 重拉）；L5 困惑点全覆盖（**16 条误区** + 5 题小测 + **8 张速查卡** + 上线前检查清单 13 项）；L6 含阶段 5 进度树 + 全书进度 + 三个「第一次」定位。 | 通过，落盘 |
| 2026-09-01 | **阶段 5 · 课14《降采样、保留策略与成本》**（3 知识点） | **主 agent 内联**（教学法 5 维度 + 学习者 L1-L6 两轮独立评审） | **P0×1 / P1×1 / P2×2，全部当批修复。** **【备课：官方一手核实】** ①**Core 保留期是「查询时过滤 + 后台 retention-check-interval（默认 30m）真删」——「查不到」与「腾出空间」是两个时刻** ②单位 **`mo`=30 天 / `y`=365 天是固定换算非日历**（`3mo`=90 天 vs 3 个自然月 91.3 天，**少留 1.3 天**）、**不支持 m/s**、最短实际 1h ③**`0`（0d/0h）= 立刻全删**，与 1.x/2.x 的 `0d`=永久保留**完全相反**（静默语义反转，迁移高危）④保留期**数据库级**（表级为 Enterprise 3.2+）→ **每层分辨率必须独立建库**（官方 falko 博客三库：mqtt 90d / power_1h 1y / power_1d 10y）⑤**Core 无 compactor**、冷层即对象存储 Parquet（比内存/SSD 省 90%+）、**第三方引擎可直读 parquet 绕过 432 限制** ⑥downsampler 插件两模式（scheduled 必填 window / HTTP 回填 batch_size 30d）、11 种 calculations、**三元数据列 record_count/time_from/time_to**（判断聚合可信度的关键）、**同窗口重跑不重复**（measurement+tag set+timestamp 覆写语义）⑦删除四阶段（软删→重命名 `<table>-<timestamp>`→24h 宽限期仍可查→硬删）。**【两处官方文档冲突，已双面呈现】** ①data-retention 页称 **immutable**，但 v3.2.0 release notes 新增 `update database --retention-period`（#26520）+ GitHub `tests/cli/db_retention.rs` 有 `test_update_db_retention_period` → 判为**文档滞后于实现**，讲义双面呈现 + ⏳ `--help` 自查路径，规划仍按不可改；②3.2 博客「default 72-hour grace period」vs config-options `--delete-grace-period` 默认 **24h** → **以 config-options 为准**（另核实 `--hard-delete-default-duration` 已废弃，从未生效）。**P0×1**：速查卡把第 3 层「every:6h → 1,460 文件」标为 ✅，**实测 1,460 > 432 应为 ❌**；且第 1 层「1,008（靠短保留期规避）」含糊 → 已重写整张表。**P1×1**：正文成本模型表只讲「省 95.9%」，未点破**「秒级 7 天」在 Core 上本身超限（144×7=1,008 > 432）**——该经典数字来自**有 compactor 的环境** → 已补三形态对照表（Enterprise 配 7 天全可查 / Core 须压到 3 天 / Core 强配 7 天则第 4~7 天只能存不能查）。**P2×2**：①小测解析未点明 D 项双重错误（Core 无 `ALTER DATABASE` 语句 + 默认应按不可改规划）→ 已补；②新增 Q1 把上述 P1 发现固化为考题并**重排 Q1-Q5 题号**（首次替换造成题号错位，已二次校正并验证连续）。**【学习者视角】** L1「领导追问成本三连」承接 L13 选型方案，代入感强；L2/L3 无倒序（删分区→成本模型→冷层）；L4 五幕完整，**第二幕四个反直觉事实与三知识点对应**；L5 困惑点全覆盖（15 条误区 + 5 题小测 + 7 张速查卡）。**本批最有价值产出**：**「降采样层能否查取决于调度周期而非降采样精度」**——gen1 按时间分桶，每次调度只落 1 个桶 → 文件数=每天调度次数；反推 90 天可查需 **调度周期 ≥5h（取 6h）**，故「分钟级 90 天配 every:1h」= 2,160 文件**照样报错，白降** | 通过，落盘 |
| 2026-09-01 | **阶段 5 · 课13《部署形态与容量规划》**（3 知识点，**阶段 5 开篇**） | **主 agent 内联**（教学法 5 维度 + 学习者 L1-L6 两轮独立评审） | **P0×0 / P1×1 / P2×2，全部当批修复。** **【准确性（A 视角）】** ①**P1-1（讲义与实跑不同步，L10/L12 同类问题第三次出现）**：实验 B 输出块与实测脚本输出不一致（脚本第 3 轮修正后加「≈ 3 天」，讲义仍为旧文案）→ 已同步脚本源码块 + 两处输出块，并以正则校验 `OLD=0 / NEW=3`。②**P2-1**：正文「写入速率 vs 存储量」表缺实跑输出中的 **500,000 点/秒**行（14.1 TB / 5.7 TB / 2.8 TB）→ 已补齐，与实验 A 逐行一致。③**P2-2**：「官方调优页给了按负载的建议：写密集 60-70%、查询密集 80-90%、混合 70%」被写成三个参数通用 → 官方该区间**只针对 `--exec-mem-pool-size`**，另两个参数无同款区间表 → 已改为标注参数归属，并保留「`force-snapshot` 默认 50% 在纯查询节点偏高」的推论。**【完整性】** 九项结构件齐全（一图总结 / 误区速查 15 条 / 官方文档 10 条 / 速查卡 6 张 / 课后小测 4 题 / 接力提示词 / 课程导航 / 全局定位 / 落地视角 6 条），五幕完整。**【教学逻辑】** 第二幕三个反直觉事实（432 文件 = 报错不是慢 / 官方默认答案是 Enterprise 不是 Core / 内存默认已占 90%）均有一手官方原文支撑；第一幕「领导三问」与第五幕「本课回答了前两问半」形成闭环，缺口明确指向 L14（成本）与 L15/L16（告警与监控）。**【一致性】** 与 L10（Core 无 compactor，三处官方证据）、L11（432 超限报错已核实）、L12（每点字节 60-150、文件数为慢查询根因）全部对齐无冲突。**【学习者视角（B 视角）】** L1「官网有五个形态，我不知道该下哪个」——悬念真实，正是本课要解决的；L2/L3 无倒序依赖；L4 五个类比均标注失效边界（开车/租车队：Core 与 Enterprise 引擎功能有差异，非配置差异；搬家：磁盘满会拒绝写入且内存不足会 OOM；配钥匙：catalog 迁移单向不可逆）；L5 三个知识点各配实验，A/B 本机实跑、C 标 ⏳ 并给判断标准；L6 小测 Q4 多选题专门考「幂等 ≠ 可逆」，是本课最易混的概念。 |
| 2026-09-01 | **阶段 4 · 课12《写入与查询性能调优》**（3 知识点，**阶段 4 收官**） | **主 agent 内联**（教学法 5 维度 + 学习者 L1-L6 两轮独立评审） | **P0×0 / P1×2 / P2×3，全部当批修复。** **【备课：官方一手核实】** ①**官方批量写入双阈值 10,000 行或 10 MB，先到为准**（Core optimize-writes 页原文）；经实验 A 对照 4 硬算术验证 **四档行长（60/120/300/1000 字节）全部是行数先到**，只有每行 **> 1048 字节**才轮到体积阈值 ②**gzip 官方口径是 "up to a 5x speed improvement"（速度提升，非压缩比）** ③**`--max-http-request-size` 默认 10mb，与 10 MB 批量阈值恰好撞车**，官方未说明批量阈值是压缩前/后 → 定为保守策略（始终启用 gzip）④**客户端 `WriteOptions` 默认 `batch_size=1000`，比官方推荐的 10,000 保守一个数量级**（属取舍非矛盾）⑤**`SELECT *` 官方门槛是 1000+ 列**，而 **Core 每表上限 500 列** → 得出本课最反直觉结论：**在 Core 上改 `SELECT *` 基本不会让查询变快** ⑥**`system.queries` 字段与示例输出**取自 Core HTTP API 页官方示例（`plan_duration`/`execute_duration`/`permit_duration`/`parquet_files`/`end2end_duration`/`max_memory`），并核实两条限制（**重启即丢**、**`--query-log-size` 默认 1000 条**且跨库互相驱逐）⑦**`--max-concurrent-queries` 可运行时通过 `POST /api/v3/configure/query_concurrency_limit` 调整**（档案此前未记，属 3.10 新增项）⑧**Core 页 tag 排序建议是「按查询优先级」，Clustered 页是「按 key 字典序」，两者不一致** → 已标注 SKU 差异。**P1×2**：①第二幕「根本建不出…明显收益的表」表述过绝对，易被读成「改 `SELECT *` 完全无收益」→ 补充「500 列时可能已有可感知差距，只是远未达官方量级」；②「10MB 阈值基本用不上」过于绝对 → 补充超宽表/超长 string field 的例外与「算平均行长」判据。**P2×3**：①gzip 三种启用方式未标出处 → 补「官方 optimize-writes 页三处示例」；②`WriteOptions` 默认值出自 **Cloud Serverless SKU** 文档页 → 补 SKU 来源标注；③实验 D 的 shell `${i:0:1}` 取值不稳定且会产生重复值 → 改为 100 个点的稳定写法并同步 B/C 组。**【学习者视角】** L1 用「周一早上两通电话（写入变慢 + 大屏 8 秒）」开场，且点明两者是同一门课（固定开销 / 文件数），代入感强。L2/L3 无倒序（快递整车→固定开销与边际递减→双阈值与 gzip→两个旋钮→模拟器实跑→落地视角）。L4 五幕完整，**第二幕三个反直觉事实与三知识点一一对应**（边际递减→事实一 / SELECT * 门槛→事实二 / plan>exec 与四类不受控瓶颈→事实三）。L5 困惑点全覆盖（14 条误区 + 4 题小测 + 速查卡）。L6 含阶段进度树 + **三课闭环四行对照表** + **阶段 4 出口检查清单 10 项** + 阶段 5 预告。**裁决**：两视角无冲突。**本批最有价值产出**：①把「改 `SELECT *` 能让查询变快」这一流传最广的调优建议**证伪其在 Core 上成立不了**（官方门槛 1000+ 列 vs Core 上限 500 列），并据此重排了调优优先级表 ②核实到 **10 MB 批量阈值与 10 MB HTTP 请求上限恰好相等**这一此前未发现的撞车点 ③给出可复用的**七步倒查法**与「plan > exec = 文件数太多」这一诊断指纹 | 通过，落盘 |
| 2026-09-01 | **阶段 4 · 课11《向量化执行：列存为什么快》**（3 知识点，**开课「向量数据库」误解的最终闭环**） | **主 agent 内联**（course-reviewer 子 agent 未创建，独立性受限；教学法 5 维度 + 学习者 L1-L6 两轮独立评审） | **P0×0 / P1×1 / P2×1，全部当批修复。** **【备课：官方一手核实】** ①**「72 小时」实为 432 个文件限制**——官方 config-options 原文 *"With the default 432 setting and the default gen1-duration setting of 10 minutes, queries can access up to a 72 hours of data"*，代码里**无任何时间判断**，72 小时是 432×10min 的**派生值**（可解释社区「查 13 天不报错」：数据稀疏未凑够 432 个文件）②**gen1-duration 调小反而缩小范围**（1m→432 文件只覆盖 7.2 小时，10m 已是文件最少档）③**超限是直接报错非变慢**（第三方在 Core 3.11.0 实测复现错误原文，官方列四条副作用且建议保持默认）④**LVC 四条官方事实**：count 上限 10、InfluxQL **不支持** last_cache()、**服务器停止即清空且不自动回填**（仅新写入填充）、*"the storage engine is not limited by cardinality, **it does affect the LVC**"*（引擎不怕基数但 LVC 怕）⑤ile-cache-recency 默认 **5h**（原 parquet-mem-cache-query-path-duration）⑥DataFusion RecordBatch 默认 **8192 行/批**（向量化的「向量」长度）。**P1×1**：实验 A 的「5.72 倍」易被误读为「向量化 = 快 5.72 倍」→ 诚实说明从对照 2 之后**前置到实验 A 最开头**，明确「Python 层测的是循环开销，非 SIMD 加速比，真正 SIMD 在 Rust 内核里测不到」。**P2×1**：EXPLAIN 示例标注为「官方真实结构」，实为官方博客**缩写版** → 补 ⏳ 标注「字段名为示意，勿逐字比对」。**【学习者视角】** L1：5 万信号大屏每 5 秒刷新的 *scan-for-latest* 痛点真实。L2：列存→裁剪→LVC 递进（读得快→读得少→不用读），无倒序。L3：Excel 类比（感知）→SIMD 原理（机制）→EXPLAIN（实操）→落地视角（定位），无倒序。L4：五幕完整，**第二幕三个反直觉事实与三知识点一一对应**（列存→事实一 / 432→事实二 / LVC→事实三），第一幕悬念在第五幕闭环。L5：11 条误区全覆盖。L6：含阶段进度树 + L10/L11 四行对照表 + L12 预告。**裁决**：两视角无冲突。**本批最有价值产出**：把「72 小时」这个流传甚广的说法**还原为 432 个文件**，并据此推出运维可感知的边界——**Core 实际上查不了 1 周以上的数据（7 天 = 1,008 文件 > 432），而且是直接报错不是慢**；同时明确了**「列存省 I/O、SIMD 省 CPU、列存是 SIMD 的前提」**这一易被混为一谈的关系 | 通过，落盘 |
| 2026-08-31 | **阶段 3 · 批次 1**（L6 数据模型：table/tag/field/timestamp，3 知识点，阶段 3 首课） | 主 agent 内联（教学法 5 维度 + 学习者 L1-L6 两轮） | **P0×0 / P1×1 / P2×1，全部当批修复。** 备课核实：四要素与主键、tag 值仅字符串、null tag 不进主键、schema-on-write 与 tag 列不可变、tag/field 同名列冲突、类型冲突错误消息、保留键（`time` 拒绝 / `_field`·`_measurement` 静默丢弃 / `_` 与 `iox_` 前缀）、转义规则（**string field value 才转义引号反斜杠**）、大小写三套规则、Core 硬限制（库 5 / 表 2000 / 列 500）——全部取自官方文档原文，新增 7 条事实核查。**P1×1**：实验 A 的 `information_schema.columns` 查询出自第三方教程而非官方页面 → 已补 ⏳ 置信度「中」标注 + 两条已核实降级路径（InfluxQL `SHOW TAG KEYS` / 客户端 `.schema`），并点明实验真正目的是「亲眼确认 tag 是 Dictionary 编码」。**P2×1**：实验 E 输出与实跑不符（`_internal` 实际触发**两条**警告：下划线开头 + 未以字母数字开头，课件原写一条）→ 已按本机实跑结果修正。学习者视角：L1「新采集器加了个引号，面板断了一截」+「从没执行过 CREATE TABLE，哪来的列类型」——悬念真实且与 L4 的 42 vs 42i 同构递进（L4 问「为什么这次写不进去」，L6 答「schema 什么时候定下的」）；L2/L3 无倒序；L4 五幕完整、第二幕三事实对应三知识点；L5 困惑点全覆盖（13 条误区 + 4 题小测 + 速查卡 + 可跑自检脚本）；L6 含进度条 + L7 基数钩子。两视角无冲突。**本批最有价值产出**：①实验 E 命名自检脚本经本机 Python 3.11 实跑验证（编写环境无 Docker，该实验不依赖数据库即可跑通，可接 CI）②核实到 Core 只有 5 个数据库额度这一硬约束，写入落地视角第 5 条（分库策略不能按实例/按天/按客户划分） | 通过，落盘 |
| 2026-08-31 | **阶段 2 · 批次 3**（L5 Python 客户端与 CLI 工具，3 知识点，阶段 2 收官课） | **主 agent 内联**（教学法 5 维度 + 学习者 L1-L6 两轮） | **P0×0 / P1×1 / P2×2，全部当批修复。** **【备课：本机实跑核验】** 编写环境无 Docker（`docker: command not found`），但本机有 `python3.11.exe`（uv 管理，直接 pip 触发 PEP 668 拦截）→ 改用 venv 实装 `influxdb3-python` 并 `inspect` 真实 API，据以订正档案 3 条记录：①版本号档案记 0.20.0，**实测最新 0.21.0** ②档案未记 **`use_v2_api` 默认 `True`**（客户端默认写 `/api/v2/write` 而非 L4 学的 V3 端点），且 **`no_sync` 与之互斥**（源码显式抛 `ValueError`）③档案未记 **`influx3` 随包提供且仅有 `query` 一个子命令**（实测 `--help` 仅 `{query,q}`）。**【教学法视角】** 准确性：API 签名 / 默认值 / 枚举取值全部取自本机实跑 `inspect`，非推测；`host` 解析逻辑取自安装包源码，畸形连接串 `grpc+tcp://localhost:8181:443` 经 pyarrow 构造复现确认（`ArrowInvalid`）。完整性：三知识点六要素齐全。格式规范：九项结构件齐全 + 阶段 2 出口检查清单 10 项。**P1×1**＝实验 E「`flush()` 后读 `stats`」存在时序缺陷（回调在后台线程，`flush()` 不保证回调跑完）→ 已补 `sleep(2)` 与告警，并新增误区第 11 条。**P2×2**：①实验 A 未说明 python3.11 路径与 venv 可复用 → 已补；②示例代码残留调试写法（`if False else None`）且 `WriteType` 未导入 → 已修正为可照抄版本。一致性：table/measurement 已加注，72 小时限制与精度陷阱回扣 L3/L4 无冲突。**【学习者视角】** L1 吸引力：「照抄官方示例的 host 却崩，报错里冒出从没写过的 `:443`」——真实可复现且官方文档确实这么写，无问题。L2 无倒序。L3 认知阶梯：快递员类比（感知）→ 双协议（概念）→ host 解析源码（机制）→ 五组实验（实操）→ 落地视角（定位），无倒序。L4 故事弧：五幕完整，第二幕「三个反直觉事实」与三知识点一一对应。L5 困惑点：host 坑 / 默认 V2 端点 / 批量缓冲 / 毫秒单位 / influx3 vs influxdb3 / 8082 vs 8181 / Windows 证书 / Flight 只读 / Flight 不治慢 SQL / 批量失败静默 —— 全部覆盖（11 条误区 + 速查卡）。L6 全局定位：含阶段进度条 + L6 钩子 + 阶段 3 预告 + 回扣「向量」误解伏笔。**裁决**：两视角无冲突，全部采纳。**本批最有价值产出**：突破「无 Docker 无法核验」的限制，用 venv + inspect 拿到一手证据，不仅订正版本号，还新增两条档案完全没有的关键事实（默认 V2 端点、influx3 仅 query 子命令），二者都会直接导致生产行为与预期不符 | 通过，落盘 |
| 2026-08-31 | 多阶段大纲（6 阶段 / 19 课 / 55 知识点） | 主 agent 内联（pedagogy） | P0×0 / P1×0 / P2×1（阶段 2 上手篇前置到阶段 3 之前，已采纳）；依赖链无缺口（问题→实操→模型→引擎→生产→决策），工作落地导向与全面覆盖要求匹配 | 通过，落盘 |
| 2026-08-31 | **阶段 1 批次**（L1 为什么需要时序数据库 / L2 InfluxDB 是什么，共 6 知识点） | **主 agent 内联**（course-reviewer 子 agent 未创建，独立性受限；按技能要求跑**教学法视角 5 维度 + 学习者视角 L1-L6 两轮独立评审**） | **P0×0 / P1×1 / P2×4，全部当批修复。** **【第一轮 · 教学法视角】** 教学逻辑：L1→L2 依赖链无缺口（先建"时序数据特殊"认知，再讲 InfluxDB 解法）。准确性：版本时间线、FDAP、Core 不含 compactor、性能口径（<10ms / ~30ms）、无限基数均沿用开课前已核实条目，无新增事实风险。完整性：五招齐全、三笔账齐全。格式规范：五幕结构 / Mermaid / 🐞误区 / 一图总结 / 课后小测 / 📚官方文档 / 📋速查卡 / 🚀接力提示词 / 🧭课程导航九项齐全；SVG 仅用于路径图（固定 SVG 场景），配色浅底深字合规。一致性：L1 不涉具体产品、L2 展开，无冲突；table/measurement 术语已消歧。**P2×2**——①知识点缺显式「一句话定义」（知识点六要素之一）→ 3 处补齐；②五项优化中「③追加写路径」在第二幕三笔账中无对应项，读者易困惑 → 补 📌 说明（三笔账说的是关系库错在哪，③治的是写入吞吐这一独立维度）。**【第二轮 · 学习者视角】** L1 场景吸引力：崩溃之夜（900GB / 从库延迟 6h / DELETE 跑 4h）真实具体，代入感强，无问题。L2 由浅入深：定位→三代→SKU 递进，无问题。L3 认知阶梯：两课均感知→概念→机制→定位，无倒序，无问题。L4 故事弧：两课五幕完整，L1 结尾"下一课请解药登场"与 L2 开头呼应，无问题。L5 困惑点：**P1×1**——L2 通篇无一条 SQL 示例，「SQL 优先」卖点悬空（读者学完仍不知 InfluxDB 的 SQL 长什么样）→ 已在 Flux 处补 **2.x Flux vs 3.x SQL 同需求对照表**（`DATE_BIN(INTERVAL '1 minute', time)` 取自官方首页示例，真实语法）；**P2×1**——L1 知识点 3① 的字典编码 / RLE 只列术语未给白话 → 已为 delta-of-delta / 字典编码 / RLE / XOR 四项各补一句人话。L6 全局定位：两课均有 📍全局定位 + 🔗下一步，无问题。**裁决**：两视角无冲突（教学法关注六要素与结构，学习者关注具体化与困惑点），主 agent 全部采纳并当批修复 | 通过，落盘 |

| 2026-08-31 | **阶段 2 · 批次 1**（L3 环境搭建与第一次写入，3 知识点）+ **L2 同步回补** | **主 agent 内联**（course-reviewer 子 agent 未创建；教学法 5 维度 + 学习者 L1-L6 两轮独立评审） | **P0×0 / P1×1 / P2×2，全部当批修复。** **【第一轮 · 教学法视角】** 教学逻辑：L2→L3 依赖顺（SKU 选择 → 落地起 Core），三知识点递进合理，无问题。准确性：Docker 命令与 compose.yaml、原生安装脚本、token 创建与三 shell 环境变量写法、write/query CLI 全参数、72 小时限制、Flux 不受支持、LVC/DVC、原生 python/ 目录依赖、快速启动模式、8181 端口——**全部取自官方文档原文**，已逐条录入本档案事实核查表。**P1×1**＝第四幕「预期输出」为**构造内容，非实测**（编写环境无 Docker，`docker: command not found`）→ 已改为「判断成功的三条标准」（返回 2 行 / 含 room+temp+hum+co+time 五列 / co 显示为 0），并加 ⏳ 显式标注，档案同步记录为置信度「中」待真实环境验证。完整性：三知识点六要素齐全。格式规范：九项结构件齐全。一致性：8181 与 L2 一致、schema-on-write 回扣 L2；**并同步回补 L2 两处**——①Core 表格新增「查询范围约 72 小时」硬约束 + 🚨 提示块（呼应第三课验证实验）②性能指标表改为标注官方机制出处 **LVC / DVC**，并补⚠️「二者是需主动配置的可选项，默认状态达不到这两个数字」——这是对 L2 原表述「内存缓冲+WAL 直接服务」的**纠正**。**【第二轮 · 学习者视角】** L1 场景吸引力：开场「照教程起服务却拉到 v2 镜像」叠加 2026-09-15 latest 标签变更这一真实事件，代入感强，无问题。L2 由浅入深：起服务 → 读写 → 目录配置，无问题。L3 认知阶梯：参数含义（概念）→ 四步闭环（实操）→ 目录结构（机制），无倒序，无问题。L4 故事弧：五幕完整；**L2 埋的「Core 不含 compactor」伏笔在本课 72 小时限制处兑现并亲手验证**，无问题。L5 困惑点：**P2×1**＝Docker 部署时每条命令都要 `docker exec -it <容器名>` 前缀，课件中反复出现却未解释该模式 → 已在第四幕开头补统一说明。其余困惑点（72h 与「3-5 天」的口径差异、查不到数据的误判、Windows 三 shell 差异、精度位数）均已在正文或速查卡覆盖。L6 全局定位：第五幕含 📍 全局定位 + 🔗 下一步 + 🎯 落地视角四条，无问题。**裁决**：两视角无冲突，主 agent 全部采纳 | 通过，落盘 |

| 2026-08-31 | **阶段 2 · 批次 2**（L4 Line Protocol 与写入基本功，3 知识点） | **主 agent 内联**（course-reviewer 子 agent 未创建；教学法 5 维度 + 学习者 L1-L6 两轮独立评审） | **P0×0 / P1×1 / P2×1，全部当批修复。** **【第一轮 · 教学法视角】** 教学逻辑：L3→L4 递进（跑通闭环 → 深挖写入语法），三知识点递进合理，无问题。准确性：line protocol 四要素与解析规则、转义表、引号规则、五种类型及范围、类型冲突错误消息、HTTP API 端点参数与两类响应、`no_sync`、gzip multi-member、`cluster-uuid`、三端点选择——**全部取自官方文档原文**。完整性：三知识点六要素齐全，`#` 注释规则已覆盖。格式规范：九项结构件齐全，3 张 Mermaid 图。一致性：与 L3 的 CLI precision 说明一致（L4 是扩展非冲突）。**P2×1**＝时间戳有效范围数值来自第三方（阿里云）而非 Core 官方文档 → 已加 ⏳ 置信度标注与"不得写进正式文档而不加注"的提示。**【第二轮 · 学习者视角】** L1 场景吸引力：「42 vs 42i」类型冲突开场，是真实高频踩坑，无问题。L2 由浅入深：语法 → 类型 → API，无问题。L3 认知阶梯：面单类比（感知）→ 四要素（概念）→ 转义/类型/精度（机制）→ 五组实验（实操）→ 落地视角（定位），无倒序。L4 故事弧：五幕完整，第二幕「三个层次的坑」与第三幕三知识点一一对应，无问题。L5 困惑点：**P1×1**＝初稿称 `auto` 检测"边界附近猜错就是几十年误差"——**经换算验证为错误表述**：5e9 秒 / 5e12 毫秒 / 5e15 微秒**三者对应同一时刻 2128-06-11**，常规范围内 auto 可靠，真正风险是小数值与 1970 年前历史数据。已改写三处（正文记忆法段新增「阈值对齐」换算表、实验 E 补小数值反例、落地视角第 2 条改口径）并在误区新增 4b 条专门破除该误解。其余困惑点（42 为何是 float、CLI 与 API 参数差异、400 到底是成功还是失败）均已覆盖。L6 全局定位：第五幕含 📍 全局定位（指向第 12 课性能调优）+ 🔗 下一步 + 落地视角五条，无问题。**裁决**：两视角无冲突，主 agent 全部采纳。**本批最有价值的产出**：P1 那条是「凭直觉写的风险描述被算术推翻」的典型案例，已把验算过程写进课件，让读者能自己复现而非被动接受结论 | 通过，落盘 |

## 大纲调整记录

| 日期 | 调整内容 | 原因 |
|------|----------|------|
| 2026-08-31 | 初始大纲落盘（6 阶段 / 19 课 / 55 知识点） | 用户要求系统学习 InfluxDB（经澄清为时序数据库而非向量数据库），画像：零基础 + 工作落地 + 尽量全面 |
| 2026-09-02 | 新增 Phase 3 结课实战项目 `projects/时序数据平台落地/`（四份文档 + 7 个实现文件） | 19 课 / 55 知识点全部完成后按 topic-teach 规范默认进入；把 L6/L7/L11/L12/L13/L14/L16/L17/L18/L19 的知识点焊成一条可执行判断链，是终点产物《时序数据库选型与落地方案》的工程化版本（可挂 CI，退出码即结论） |

## 事实核查记录

| 条目 | 结论 | 置信度 | 核查时间 |
|------|------|--------|----------|
| 🔴 **【L19 · 阶段 6】InfluxDB 3 有五个 SKU，「选了 InfluxDB」不等于完成选型** | 官方 which-influxdb-3 页列出 Core / Enterprise / Cloud Serverless / Cloud Dedicated / Clustered。硬约束差异极大：Core 单节点无 HA / Serverless 无原生 v3 API 与处理引擎 | 高（官方原文） | 2026-09-02 |
| 🔴 **【L19 · 阶段 6】官方对新生产负载的推荐是 Enterprise，Core 定位为非生产/边缘/单节点** | which-influxdb-3 页原文：*"For new production workloads, use InfluxDB 3 Enterprise"*；Core 为 *"free, open-source, single-node release"*，用于 *"non-production, edge, or single-node deployments"* | 高（官方原文） | 2026-09-02 |
| 🔴 **【L19 · 阶段 6】Serverless 无原生 v3 写 API、无 Processing Engine** | 官方 which-influxdb-3 页原文：*"No native v3 write API—use v1 and v2 compatibility endpoints"*、*"No Processing Engine"*。**它不是 Core 的托管版，能力集合不同**（Core 有这两项，Serverless 有 compactor） | 高（官方原文） | 2026-09-02 |
| 🔴 **【L19 · 阶段 6】Core 可查窗口 3 天，AWS 官方 FAQ 独立印证** | AWS Timestream for InfluxDB 3 FAQ 对照表：*"Historical queries: Limited (~3 days)"*、*"query performance degrades as data ages"*。与 L11/L13 核实的 432 文件 × 10min 一致 | 高（双官方源印证） | 2026-09-02 |
| 🔴 **【L19 · 阶段 6】Cloud Serverless 四个单价与免费层额度** | 官方定价页：写入 $0.0025/MB、查询 $0.012/100 次、存储 $0.002/GB-hour、出流量 $0.09/GB；免费层写入 5MB/5min、查询 300MB/5min、保留 30 天、2 个库 | 高（官方原文） | 2026-09-02 |
| 🔴 **【L19 · 阶段 6】Enterprise 三种许可证：Trial / At-Home / Commercial** | 官方 license 页对照表：Trial = 256 核 / 30 天 / 多节点 / **不可商用**；At-Home = **2 核** / 永不 / 单节点 / **不可商用**；Commercial = 按合同 / 可商用 | 高（官方原文） | 2026-09-02 |
| 🔴 **【L19 · 阶段 6】Enterprise 按 CPU 核授权，按集群不按机器，批次 8/16/32/64/128** | 官方 license 页原文：*"The CPU limit is per cluster, not per machine"*、*"CPU cores are purchased in batches of 8, 16, 32, 64, or 128 cores"* | 高（官方原文） | 2026-09-02 |
| 🔴 **【L19 · 阶段 6】Core → Enterprise 无需搬数据，但降版不支持** | 官方 upgrade-to-enterprise 页：*"Your existing data and plugins are compatible with Enterprise–no data migration is required"*；*"After upgrading to InfluxDB 3 Enterprise, you **cannot downgrade** back to InfluxDB 3 Core"* | 高（官方原文） | 2026-09-02 |
| 🔴 **【L19 · 阶段 6】Enterprise 支持 100+ 库 / 10k+ 表** | ⚠️ 依据为官方**博客**（upgrading-influxdb-3-enterprise），非文档页；官方口径为「100+」「10k+」。**本课保守取其字面下限** | 中（博客口径，非文档页） | 2026-09-02 |
| 🔴 **【L19 · 阶段 6】性能调优页规格锚点：4核16G / 16核64G / 64核256G** | 官方 performance-tuning 页按系统规模给出的三档配置示例，本课用作核数推算的锚点 | 高（官方原文） | 2026-09-02 |
| ⚠️ **【L19 · 阶段 6】Enterprise 商业授权费不公开报价** | 官方定价页仅 *"Contact Sales"*，无公开价格。**任何「自托管更便宜」的结论在拿到报价前都不成立**，本课以 ❓ 标注、未做估算 | 高（结论）；价格不可得 | 2026-09-02 |
| 📌 **【L19 · 阶段 6】运维人力常常比机器贵（IoT 场景 31 倍）** | 本课推导：0.5 FTE × $15,000/月 = $7,500 vs 主机 $240/月。⚠️ 倍率依赖人力成本假设，但**结构关系稳定** | 中（依赖假设值） | 2026-09-02 |
| 📌 **【L19 · 阶段 6】Grafana 面板刷新频率直接决定 Serverless 账单** | 本课推导：某面板每 10 秒刷新 = 8,640 次/天 = 259,200 次/月 × $0.012/100 = $31/月；20 个面板 = $622/月。**按量计费下查询次数最容易被低估** | 中（基于官方单价推导） | 2026-09-02 |
| 🔴 **【L18 · 阶段 6】术语映射 `bucket = database`、`measurement = table`，官方用词是 is synonymous with** | 官方 schema-design 页原话用 **is synonymous with**（同义），不是「相似概念」—— 是同一个东西换了名字。v2 bucket = 3.x database；v1/v2 measurement = 3.x table | 高（官方原文） | 2026-09-02 |
| 🔴 **【L18 · 阶段 6】InfluxQL 查询需 DBRP 命名约定 `database_name/retention_policy_name`** | 官方 Core create database 页：rp 从数据模型里消失后，又从**命名规范**回归。用 InfluxQL 查询的库名必须写成 `库名/保留策略名` | 高（官方原文） | 2026-09-02 |
| 🔴 **【L18 · 阶段 6】`precision=m/h` 仅 v1 `/write` 端点支持，v2/v3 均不支持** | 官方 write-data 页精度矩阵：分钟/小时**只有 v1 端点支持**。迁移时改了端点不改参数，请求直接失败（报错型，非静默） | 高（官方原文） | 2026-09-02 |
| 🔴 **【L18 · 阶段 6】写入端点默认精度从「纳秒」变为「auto（按量级猜测）」** | v1 默认纳秒；v3 默认 `auto`。**换端点不改代码 = 换了解释方式**，且 auto 猜错时**不报错**（静默型） | 高（官方原文） | 2026-09-02 |
| 🔴 **【L18 · 阶段 6】v3 自动精度检测阈值：<5e9 秒 / <5e12 毫秒 / <5e15 微秒 / ≥5e15 纳秒** | 官方 write-data 页；L4 已核实并沿用 | 高（官方原文） | 2026-09-02 |
| 🔴 **【L18 · 阶段 6】导出必须带 `-lponly`，否则整批写入失败** | 官方迁移页原文 *"export line protocol **without InfluxQL DDL or DML**"*。不带它会导出 CREATE 等语句，行协议解析器不认，**整批**失败 | 高（官方原文） | 2026-09-02 |
| 🔴 **【L18 · 阶段 6】Flux 官方明确无直接兼容层，出路是 Explorer 1.9 的 Flux to SQL converter（beta）** | GA 博客原话 *"we currently **regrettably cannot offer a direct compatibility layer**"*。converter 为 **beta** 且官方自述 *"AI-generated, so its output can vary. **Review the converted SQL before running**"* | 高（官方原文） | 2026-09-02 |
| 🔴 **【L18 · 阶段 6】Flux `map()` 保留所有列，转出的 SQL 只选写的列** | 官方 Explorer 1.9 博客自标的行为差异：改写后列会「消失」，是 Flux→SQL 最常见的静默偏差 | 高（官方原文） | 2026-09-02 |
| 🔴 **【L18 · 阶段 6】插件系统是 CQ / Tasks / Kapacitor / Telegraf 的 natural successor** | 官方 GA 博客原话 *"the natural successor"*。迁移时「原来的定时任务搬到哪」的官方答案是插件，不是等价替换 | 高（官方原文） | 2026-09-02 |
| 🔴 **【L18 · 阶段 6】3.10 catalog v2→v3 单向迁移，恢复备份是回滚的 the only way** | 3.10.0 release notes 原文：*"Upgrading to InfluxDB 3.10 is a **one-way migration**"*、*"Restoring these objects is **the only way** to roll back to 3.9.x"* | 高（官方原文） | 2026-09-02 |
| 🔴 **【L18 · 阶段 6】catalog 备份路径按 3.4.0 分界；旧目录是残留、非有效回滚源** | ≥3.4.0 备 `catalog/v2/logs/` + `catalog/v2/snapshot`；<3.4.0 备 `catalogs/` + `_catalog_checkpoint`。官方明说旧目录 *"may still be present as **leftovers**... **aren't a valid rollback source**"* | 高（官方原文） | 2026-09-02 |
| 🔴 **【L18 · 阶段 6】v3 catalog 用紧凑二进制格式，比 v2 小 5-6 倍，迁移自动/幂等/崩溃安全** | 官方文档口径。**体积小 ≠ 可回退**，单向性由格式变更决定，与体积无关 | 高（官方原文） | 2026-09-02 |
| 🔴 **【L18 · 阶段 6】`influxdb3 debug catalog list\|snapshot\|sequence` 可离线检查 catalog（3.10+）** | 官方 3.10.0 release notes。可在**不停服**的前提下确认 catalog 状态，是升级前体检的官方手段 | 高（官方原文） | 2026-09-02 |
| 🔴 **【L18 · 阶段 6】Docker `latest` 将指向 InfluxDB 3 Core，官方建议改用固定版本标签** | 官方全站横幅。**注意：变更日期官方自身给出多个版本（2026-02-03 / 2026-09-15，中文镜像站 2026-04-07）→ 本课不引用具体日期，只采用行动建议** | 高（结论）；日期存冲突 | 2026-09-02 |
| 📌 **【L18 · 阶段 6】双写窗口上限 = 可查窗口（Core = 3 天）→ Core 上双写实际只有 1-3 天** | 本课推导：下限 = 覆盖对账周期（1-3 天），上限由 L11/L13 已核实的 432 文件约束反推。**双写写太久，最早的数据反而查不到，双写失去对照意义** | 中（基于已核实约束推导） | 2026-09-02 |
| 📌 **【L18 · 阶段 6】行协议为文本格式无压缩，导出体积通常大于 TSM 内部存储，建议按 1.2 倍预留磁盘** | 本课推导（官方推荐 `-compress`）。磁盘满会中断导出，而**半途生成的导出文件最容易在回填时出问题** | 中（推导值） | 2026-09-02 |
| 🔴 **【L17 · 阶段 6】Prometheus 官方自己说：拉模式只是「略好」，不是决定性因素** | 官方 FAQ《Why do you pull rather than push?》原文：*"we believe that pulling is slightly better than pushing, but it should not be considered a major point"*。拉模式的真正价值是**抓取失败 = 目标不可达**（天然健康检查），与性能无关 |
| 🔴 **【L17 · 阶段 6】Prometheus 官方：本地存储不集群不复制，不适合长期保存** | 官方 storage 页：*"it is not clustered or replicated"*、*"not meant as durable long-term storage"*，默认保留 **15d**，**1-2 字节/样本**。同页另有 *"possible to retain years of data"* —— 两句不矛盾：前者讲持久性，后者讲技术可行性 |
| ⚠️ **【L17 · 阶段 6】VictoriaMetrics 官网 20x / 10x RAM / 70x / 7x 全部是厂商自述且带 up to** | 全部出自官网首页 Prominent features，无中立第三方复现。按阶段 6 口径纪律**只取方向，不写数值** |
| 🔴 **【L17 · 阶段 6】ClickHouse 官方：分区不加速查询** | 官方 MergeTree 页原文：*"Partitioning does not speed up queries (in contrast to the ORDER BY expression)"*。剪枝靠 ORDER BY 的稀疏索引（`index_granularity` 默认 8192）；按客户 ID 分区会触发 `Too Many Parts`（`parts_to_throw_insert` 默认 3000）；**ORDER BY 建表后不可更改** |
| ⚠️ **【L17 · 阶段 6】TimescaleDB 连续聚合压缩率仅 61%，远低于超表的 >90%** | 官方博客实测：连续聚合用 `bytea` 存部分聚合结果，只能走字典压缩。**用 90% 估算连续聚合空间会严重低估** |
| **【L17 · 阶段 6】VictoriaMetrics 官方自述定位是「Prometheus 的长期远程存储」** | 官方原文 *"long-term remote storage for Prometheus"*；支持 InfluxDB line protocol；**Remote Write 2.0 不支持**（官方明示，协议尚处 experimental）；MetricsQL **不支持 JOIN** 且与 PromQL 有有意差异 |
| ⚠️ **【L17 · 阶段 6】官方文档冲突：Telegraf 插件数 400+ vs over 300** | 官方 Telegraf 文档页与产品页写 **400+ plugins**；官方 GitHub README（v1.39）写 **over 300 plugins** → 两者皆官方口径，相差约 100。本课正文统一取 **300+（保守口径）** 并标注冲突；**不影响任何决策结论**（采集生态广度是定性判断） |
| 🔴 **【L16 · 阶段 5】自监控数据自己也只受 3 天可查窗口，且文件数只跟墙钟有关** | `query-file-limit` 默认 432 ÷ 144 文件/天 = **3 天**；**文件数 = 天数 × 144，与写了多少数据无关**（哪怕一天只写 1 条，只要服务开着就是 144 个文件）→ **「自监控留 90 天」= 存得下但查不到**（90 天 = 12,960 文件，超 30 倍）。推论：**砍掉一半自监控指标，一个文件都不会少**。与 L15 的 WAL 触发器一天 86,400 次同构——都是「按时间触发，不看数据量」。出路：降采样（L14）或上 Enterprise（L13） | 高（432/144 为官方默认值相除；与数据量无关为官方 internals 机制推论） | 2026-09-02 |
| 🔴 **【L16 · 阶段 5】Grafana 用 SQL 查 Core 走 Flight SQL(gRPC)，必须 HTTP/2；InfluxQL 走 HTTP/1.1 无此要求** | 官方 Core 文档 Grafana 页原文：*"For SQL queries, Grafana uses the **Flight SQL protocol (gRPC)** to query InfluxDB 3 Core, which **requires HTTP/2**... **Without HTTP/2 support, SQL queries through Grafana will fail to connect.**"* → **排障第一分叉点：「SQL 面板连不上、InfluxQL 面板正常」的非对称现象直接指向 HTTP/2**（若是网络/token/端口问题，两类面板会一起挂） | 高（官方原文） | 2026-09-02 |
| 🔴 **【L16 · 阶段 5】`database_tag` 的目标库数量 = 该 tag 的去重值个数，撞 Core 5 库上限** | Telegraf `outputs.influxdb_v3` 的 `database_tag` 按 tag 值路由到不同库 → **库数量随业务增长而增长**，而 Core 硬上限 **5 个库**（L6 已核实）。失败模式：**上线时 3 个客户正常，第 6 个接入那天写入开始失败，且错误信息不提示「库超了」** → 排查成本极高。防法：`SELECT COUNT(DISTINCT("<tag>")) FROM <源表>` 先自数，> 5 改单库 + 降为普通 tag/field | 高（插件文档语义 + L6 硬限制） | 2026-09-02 |
| **【L16 · 阶段 5】Telegraf 多 URL = 故障转移，不是双写也不是负载均衡** | `urls` 配多个地址时，**每个 flush 周期只随机挑一个写入**，失败才换下一个，直到全部试完或成功。→ 要双写须写**两个 `[[outputs.influxdb_v3]]` 块**；要分摊须在 Telegraf 前做 LB 或拆多个 output 块配筛选。⚠️ 团队常误以为「配了两个 urls 所以有冗余写入」 | 高（官方插件文档） | 2026-09-02 |
| **【L16 · 阶段 5】`outputs.influxdb_v3` 自 Telegraf v1.38.0 引入；五个关键键值对的默认值** | 版本门槛 **≥ 1.38.0**（走 `/api/v3/write_lp` 原生端点），低于此版本配置写对也起不来。关键默认值：`content_encoding` = **`gzip`**、`sync` = **`true`**（`false` 即 L10 的 `no_sync=true` 换名）、`database_tag` = `""`、`exclude_database_tag` = `false`、`timeout` = **`5s`**。兼容路径：`influxdb_v2` 插件写 Core 时 **`organization` 必须留空串**（`bucket` = Core 的 database） | 高（官方插件文档） | 2026-09-02 |
| **【L16 · 阶段 5】Grafana 连 Core 时 Product 下拉要选 InfluxDB Enterprise 3.x** | 官方 Core 文档 Grafana 页原话：*"Product selection: **InfluxDB Enterprise 3.x (currently, no Core menu option)**"*。→ 插件目前**没有单独的 Core 选项**，官方直接让你选 Enterprise，URL 填 Core 地址即可。前置条件：**Grafana 12.2+** 且需开 `newInfluxDSConfigPageDesign` feature flag；本地无 TLS 用 SQL 需勾 **Insecure Connection** | 高（官方原文） | 2026-09-02 |
| **【L16 · 阶段 5】`/health`、`/ping`、`/metrics` 默认都需要认证；`/ping` 必须用 GET** | 官方 Server information 页：**`/health` 与 `/api/v1/health` 在 Core 上 "requires authentication by default"**；裸 curl 拿 401 常被误判为「服务挂了」。`/ping` 官方明确警告：*"**Important:** Use a GET request. **HEAD requests return 404 Not Found.**"* → 默认发 HEAD 的健康检查工具会误判服务不可用。`/health` 不返回版本，取版本用 `/ping`（JSON: version/revision/process_id） | 高（官方原文） | 2026-09-02 |
| 🔴 **【L16 · 阶段 5】`system_metrics` 官方插件采的是主机，不是 InfluxDB 自身** | 该插件产出 `system_cpu` / `system_memory` / `system_disk_*` 等 **measurement**，内部依赖 **`psutil`** → **名字里的 `system` 指 OS，不是 database**。它回答「这台机器健康吗」，而非「InfluxDB 服务得好吗」：主机 CPU 20% 不代表 InfluxDB 没在慢查询里挣扎。要监控 InfluxDB 自身看 `/metrics`、`system.*` 表与 `GET /ready` | 高（官方插件库页 + psutil 依赖） | 2026-09-02 |
| **【L16 · 阶段 5】Grafana 刷新间隔是查询放大器；引用官方算例必须对齐口径** | 每天查询次数 = 面板数 × 每面板查询数 × (86,400 ÷ 刷新间隔秒)。**5s → 30s，查询量降到 1/6**。⚠️ **口径陷阱**：官方博客「一个 10 秒刷新的面板 ≈ 259,200 次/月 ≈ $31」是**每面板 1 个 query** 的口径（86,400÷10×30 = 259,200）；若按每面板 5 个 query 计要 **×5**，混用会得出完全错误的账单。自建 Core/Enterprise 不按次计费，但**查询抢的是同一块 CPU**（回扣 L13 四功能竞争） | 高（公式为硬算术；口径差异为本课核算发现） | 2026-09-02 |
| **【L16 · 阶段 5】`system.queries` 是内存近期快照，不是审计日志** | 它是**内存中**的近期查询记录（保留条数由 `--query-log-size` 控制，默认 1000 条，L12 已核实），**不是永久审计日志**，且**不带时间范围查会全表扫**（回扣 L7 同款警告）。要留长期查询历史须自己定期抽取落库 | 高（官方口径 + L12 已核实） | 2026-09-02 |
| **【L16 · 阶段 5】遥测默认每小时上传一次，可显式关闭** | Core 默认**每小时**向 InfluxData 发送一次遥测；收集内容官方透明列出（系统 CPU/内存/核心数/OS/版本/运行时长每 60 秒，写入与查询计数按操作 60 秒汇总，Parquet 文件数/大小/行数与触发器计数为快照时，实例 ID/集群 UUID/存储类型/产品类型为一次性）。关闭：`influxdb3 serve --disable-telemetry-upload` 或 `export INFLUXDB3_TELEMETRY_DISABLE_UPLOAD=true`→ 内网/合规场景通常是必做项 | 高（官方 Telemetry 页） | 2026-09-02 |
| ⚠️ **【L16 · 阶段 5】官方文档冲突：Grafana / Flight SQL 端口 8181 vs 8082** | 官方 Core 文档 Grafana 页：URL 用 **`https://localhost:8181`**（与 HTTP API 同端口）；部分社区文档、DeepWiki 与开源社区版 docker-compose 示例：Flight SQL 在 **`8082`**。→ **以 Core 官方文档的 8181 为准**（与 L5 已核实的「统一服务、单端口按请求特征分流」架构互相印证）；**8082 属开源社区版/IOx 形态**。落地时用 `ss -lntp | grep influxdb3` 自行验证。**双面呈现，未单方面裁决** | 双面呈现 | 2026-09-02 |
| ⚠️ **【L16 · 阶段 5】端口三数字（版本指纹）** | **8181** = InfluxDB 3 Core / Enterprise 的 HTTP API（本课程环境，写入/查询/health/metrics 全在此端口）；**8086** = InfluxDB 1.x / 2.x（看到它基本可判断起的不是 3.x，回扣 L3）；**8082** = InfluxDB 3 开源社区版 / IOx 的 Flight 端口（社区文档混用） | 高（官方文档 + L3/L5 已核实） | 2026-09-02 |
| 🔴 **【L15 · 阶段 5】WAL 触发器一天 86,400 次，与数据量无关** | 官方原文：*"when the database flushes data to the Write-Ahead Log (WAL) (**by default, every second**)"* → 绑定刷盘事件而非行数。空转也会被调起，只是 `table_batches` 为空。是 `every:1d` 的 **8.6 万倍**；插件函数体每多 1 ms，一天就是 86.4 秒 | 官方一手 | 2026-09-01 |
| 🔴 **【L15 · 阶段 5】WAL 插件写回被监听的表 = 指数爆炸，且零报错**（第 n 秒 = `100×2ⁿ⁻¹`） | 15 秒破百万（1,638,400）、20 秒 **52,428,800**、21 秒 **104,857,600**。⚠️ 修正：初版写成「20 秒 1 亿」是 off-by-one（循环结束后的 `rows` 已是第 21 秒值）。**危险在于数据库视角一切正常**——插件在正确无误地执行一个错误的逻辑，日志无任何异常 | 实验 A 实测 | 2026-09-01 |
| 🔴 **【L15 · 阶段 5】`plugin-dir=""` 空字符串不禁用处理引擎** | config-options 原文：*"Setting `plugin-dir=""` or `INFLUXDB3_PLUGIN_DIR=""` (empty string) **does not disable** the Processing Engine. You must comment out, remove, or unset—**not set it to empty**"* | 官方一手 | 2026-09-01 |
| **【L15 · 阶段 5】`gh:` 前缀非 GitHub 专用，且每次触发器启动都重新拉取** | 官方：*`--plugin-repo` accepts **any HTTP/HTTPS URL***；且 *"fetches the plugin at trigger creation time... and again **each time the trigger starts**—for example, on server startup or when you re-enable a disabled trigger"* → 供应链风险；用 `system.plugin_files` 的 `last_modified` / `size_bytes` 验版本 | 官方一手 | 2026-09-01 |
| **【L15 · 阶段 5】Notifier 是告警的发送端，五个检测器都依赖它** | 仓库依赖表：Threshold Deadman / MAD / ADTK / State Change / Forecast Error Evaluator 的 `required_plugins` 均为 `Notification sender`。Notifier 自身是 HTTP 触发器（`request:notify` → `/api/v3/engine/notify`）→ **装告警必须检测器 + Notifier 两件套**，只装检测器 = 判定结果只落日志 | 官方仓库 | 2026-09-01 |
| **【L15 · 阶段 5】插件取消信号是 `KeyboardInterrupt`，`except Exception` 吞不掉** | 官方：服务器关闭 / 触发器被禁用或删除时，logging、write、query 抛 `KeyboardInterrupt`，它是 **`BaseException` 子类**。`QueryError` 由原生扩展定义，插件**无法 import**，`except QueryError` 会 `NameError` → 用 `except Exception` + `type(err).__name__` | 官方一手 | 2026-09-01 |
| **【L15 · 阶段 5】插件缓存：重启即清 + 生产不设 TTL 永不过期** | 缓存为内存态，非持久存储；用于告警冷却期（cooldown）时，服务重启后第一轮告警会全部重发。主机下线后未设 TTL 的 key 会永久驻留内存 | 官方一手 | 2026-09-01 |
| **【L15 · 阶段 5】Amazon Timestream 不允许自定义插件** | 官方：*"**Custom plugins are not currently supported.** Only InfluxData certified plugins... can be deployed"* → 自建 Core/Enterprise 无此限制；若「必须自定义插件」是硬需求，直接影响形态选型（回扣 L13） | 官方一手 | 2026-09-01 |
| ⚠️ **【L15 · 阶段 5】官方文档冲突：`table_batch["rows"]` 每行是扁平 dict 还是嵌套** | API reference：扁平 dict，取 `row["host"]`；官方博客《Preventing Alert Storms》示例用嵌套 `row["tags"]["host"]`。**采信 API reference**：照博客写会 `KeyError`，而 WAL 插件默认 `--error-behavior log` 只打日志不停触发器 → 表现为「插件没报错但就是不告警」。已双面呈现 | 双面呈现 | 2026-09-01 |
| ⚠️ **【L15 · 阶段 5】官方文档冲突：HTTP 入口函数名** | API reference：`process_request`；`influxdb3_plugins` 仓库 README / DeepWiki：`process_http_request`。**采信 API reference** | 双面呈现 | 2026-09-01 |
| ⚠️ **【L15 · 阶段 5】官方文档冲突：官方插件数量 11 vs 23** | Core 文档页列 **11 个**；官网 Plugin Directory 显示 **23 个**（多出 MQTT/Kafka/AMQP Subscriber、Schema Validator、Import、NWS Weather Sampler、River 系列等）。**两者皆官方口径，更新节奏不同**，未裁决 | 双面呈现 | 2026-09-01 |
| **【L15 · 阶段 5】触发器参数值全为字符串** | `--trigger-arguments "threshold=90"` 后直接比较会抛 `TypeError: '>' not supported between 'float' and 'str'` → 取值必须转型 `float(args["threshold"])` | 官方一手 | 2026-09-01 |
| **【L15 · 阶段 5】cron 表达式是 6 段（含秒）** | `秒 分 时 日 月 周`，每天 08:00:00 = `cron:0 0 8 * * *`（非 Linux crontab 的 5 段）。能用 `every:` 就别用 `cron:` | 官方一手 | 2026-09-01 |
| **【L15 · 阶段 5】`process_request` 返回 `None` = HTTP 204 而非 200** | 四种返回形式：`None`→204 / `str`→200+文本 / `dict`\|`list`→200+JSON / `tuple`→(body, headers[, status])。调用方判 `status == 200` 时会因 204 走错分支 | 官方一手 | 2026-09-01 |
| **【L15 · 阶段 5】插件必须按官方方式装依赖，不能用系统 Python 建 venv** | 官方：*"Creating a virtual environment with the system Python (for example, using `python -m venv`) can lead to runtime errors and plugin failures"*；`--package-manager` 自 Core 3.10 起废弃（pip 内置），但 `disabled` 值仍有效 → 先装依赖再以 `--package-manager disabled` 启动 | 官方一手 | 2026-09-01 |
| 🔴 **【L14 · 阶段 5】降采样层「能不能查」取决于调度周期，不取决于降采样精度** | `gen1-duration` 按**时间**分桶（非数据量），每次调度只落进 1 个桶 → **降采样层文件数 = 每天调度次数**。实测：90 天期限下 `every:1h`=2,160 ❌ / `every:6h`=360 ✅ / `every:1d`=90 ✅。反推约束：90×每天文件数 ≤ 432 → 每天 ≤ 4.8 次 → **调度周期 ≥ 5 小时（取 6h）**。⚠️ 该推理基于官方 internals 机制，非官方明文，可查对象存储 `dbs/<db>/<table>/` 文件数自行验证 | 中（机制推理 + 官方默认值为硬事实） | 2026-09-01 |
| 🔴 **【L14 · 阶段 5】Core 保留期 `0`（0d/0h）= 立刻全删，与 1.x/2.x 的 `0d`=永久保留完全相反** | 官方 data-retention 页明文：*"A retention period of `0`... deletes **all** data"*。旧版本 `0d` 表示永久 → **静默语义反转**，配置不报错、库能建起，然后数据消失。迁移脚本须逐条核对 | 高（官方文档明文） | 2026-09-01 |
| 🔴 **【L14 · 阶段 5】降采样省的是「行数」不是「字节」——聚合行比原始点更宽** | 原始点 = ts 8B + 1 值 8B + tag/开销 104B = **120 B**；聚合行 = ts 8B + 4 聚合值 32B + 3 元数据 24B + tag 60B + 开销 4B = **128 B**。行数降 1/60 时字节只降到 **1.78%**（净省 98.22%）。总量能省 95.9% 是因为**保留期才是最大乘数** | 高（行宽为建模假设，已标注；结论方向为硬算术） | 2026-09-01 |
| **【L14 · 阶段 5】官方保留期单位是固定换算，不是日历** | **`mo` = 30 天**（非日历月 30.44 天）、**`y` = 365 天**（非日历年，闰年少 1 天）、**不支持 `m`/`s`**、最短实际 **1h**、`none` = 无限。→ `3mo`=90 天比 3 个自然月（91.3 天）**少留 1.3 天**；`1y6mo`=545 天比 547.9 天少 2.9 天 → 合规场景必须按天数写 | 高（官方文档明文） | 2026-09-01 |
| **【L14 · 阶段 5】⚠️ 官方文档冲突：保留期 immutable vs v3.2+ 已可改** | data-retention 页明文 *"Retention periods are **immutable** in Core... cannot be changed afterward"*；但 **v3.2.0 release notes 新增 `update database --retention-period`**（PR #26520），GitHub `tests/cli/db_retention.rs` 存在 `test_update_db_retention_period` 用例。→ 判定**文档滞后于实现**。⏳ 实机用 `influxdb3 update database --help` 自查；**规划仍按不可改设计** | 中（文档与实现冲突，需实机确认） | 2026-09-01 |
| **【L14 · 阶段 5】Core 保留期是数据库级，故「每层分辨率必须独立建库」** | 表级保留期是 **Enterprise 3.2+** 能力。官方 falko 博客实证三库结构：`mqtt` 90 天 / `power_1h` 1 年 / `power_1d` 10 年 | 高（官方文档 + 官方博客） | 2026-09-01 |
| **【L14 · 阶段 5】Core 保留期是「查询时过滤」，后台才真删** | 超期数据**查不出来**但可能仍在存储中，直到 `retention-check-interval`（默认 **30m**）触发执行服务真正删除 → **「查不到」与「腾出空间」是两个时刻**，实际占用略高于理论值 | 高（官方 data-retention 页） | 2026-09-01 |
| **【L14 · 阶段 5】⚠️ 删除宽限期：config-options 的 24h 与 3.2 博客的 72h 冲突** | `--delete-grace-period` 默认 **24h**（config-options 页）vs 3.2 发布博客「default 72-hour grace period」→ **以 config-options 为准**。生命周期：T+0 软删并重命名 `<table>-<timestamp>` → T+0~+24h 宽限期内**仍可查**（误删后悔药）→ T+24h 硬删 → 空间回收。⚠️ issue **#27200**：节点删除后若不再接收写入（不触发 snapshot），清理可能**迟迟不发生** | 高（config-options 明文 + GitHub issue） | 2026-09-01 |
| **【L14 · 阶段 5】`--hard-delete-default-duration` 已废弃** | 该配置项**从未真正生效**，仅在启动时打印警告 → 不要试图用它缩短删除时滞 | 高（官方 config-options 页） | 2026-09-01 |
| **【L14 · 阶段 5】同窗口重跑降采样不会重复（覆写语义）** | 一行由 **measurement + tag set + timestamp** 唯一标识，重复写入同组是**覆写**非追加 → 任务失败补跑、窗口重叠均安全 | 高（官方数据模型） | 2026-09-01 |
| **【L14 · 阶段 5】官方 downsampler 插件要点** | 地址 `gh:influxdata/downsampler/downsampler.py`。两模式：**scheduled**（定时增量，**必填 `window`**）/ **HTTP**（历史回填，`batch_size` 默认 30d）；`interval` 默认 **10 分钟**；11 种 calculations（avg/sum/min/max/median/count/stddev/first_value/last_value/var/approx_median）；每行附 **record_count / time_from / time_to** 三元数据列 | 高（官方插件源码与文档） | 2026-09-01 |
| **【L14 · 阶段 5】冷数据后门：第三方引擎可直读对象存储里的 Parquet** | 所有持久化数据都是开放格式 Parquet → DuckDB / Spark / Athena 等可**绕过 InfluxDB 查询层（含 432 限制）**直读。→ 这是 Core「改保留期需迁移，而迁移 SELECT 受 432 限制」这一死结的解法。⚠️ 直读绕过 catalog，可能看到软删除/未清理数据 | 高（官方架构 + parquet 开放格式） | 2026-09-01 |
| 🔴 **【L13 · 阶段 5】Core 的查询上限不是「慢」是「报错」，且只有约 3 天** | `query-file-limit` 默认 **432 文件** × `gen1-duration` 默认 **10m** = 4,320 分钟 = **72 小时 ≈ 3 天**。7 天 = **1,008 文件（超 2.3 倍）**、90 天 = **12,960 文件（超 30 倍）**。L11 已核实：**超限是直接报错**（错误含 `exceeding the file limit`），官方列四条副作用并建议保持默认 | 高（官方默认值相乘 + L11 已核实报错行为） | 2026-09-01 |
| 🔴 **【L13 · 阶段 5】官方给「新生产负载」的默认答案是 Enterprise，不是 Core** | which-influxdb-3 页开篇原文：*"For **new production workloads**, use **InfluxDB 3 Enterprise**."*；Core 定位原文：*"Use Core for **edge or non-critical workloads**, or to develop against the v3 APIs."* → 「先上 Core 免费版，扛不住再买」在官方口径里从起点就站不住 | 高（官方原文） | 2026-09-01 |
| 🔴 **【L13 · 阶段 5】判据纠正：原生 v3 API / 处理引擎**只用于排除 Cloud Serverless，**不构成 Core → Enterprise 的升级理由** | Core 与 Enterprise **同源，二者都有**原生 v3 写入 API 与处理引擎。官方原文只说 Cloud Serverless：*"**No native v3 write API**—use v1 and v2 compatibility endpoints"*、*"**No Processing Engine**"*。⚠️ 本课选型模拟器初版把该判据写进了自托管分支，导致场景 1/5 误判为 Enterprise，已修正（`git` 无记录，属本次备课内部修正） | 高（官方原文 + 本机脚本三轮验证） | 2026-09-01 |
| **【L13 · 阶段 5】Enterprise 六个组件的扩展策略各不相同** | 官方存储引擎架构页原文：**Router / Ingester 水平扩展最有效**（提升写入吞吐）；**Querier** 水平扩展提升并发、垂直扩展提升计算密集查询；**Compactor 垂直扩展（加 CPU）最有效**（压实是计算密集）；**Garbage collector** ⚠️ *"not designed for distributed load and **should not be scaled horizontally**"*；Catalog 为 PostgreSQL 兼容库，垂直扩展均支持 | 高（官方原文） | 2026-09-01 |
| **【L13 · 阶段 5】Enterprise 五种 `--mode`，且全集群只能有一个 compact 节点** | 五种模式：`all`（Core 默认形态）/ `ingest` / `query` / `compact` / `process`，可逗号组合。⚠️ 官方原文：**每个集群只能有一个节点跑 `compact` 模式**。三种官方推荐拓扑：2 节点基础 HA、3 节点专职压实、按负载完全隔离 | 高（官方原文） | 2026-09-01 |
| **【L13 · 阶段 5】Cloud Serverless 四维度计费单价** | 官方定价页原文：数据入 **$0.0025/MB**、查询 **$0.012 / 100 次执行**（⚠️ 按次计，与查询复杂度无关）、存储 **$0.002/GB-hour**、数据出 **$0.09/GB**。官方算例：一个 10 秒刷新的 Grafana 面板月跑 **259,200 次**查询 ≈ **$31**，20 个面板 ≈ **$620/月** | 高（官方定价页 + 官方博客算例） | 2026-09-01 |
| **【L13 · 阶段 5】Amazon Timestream for InfluxDB 有多种引擎 SKU** | 该服务可选用 **InfluxDB 2.x / 3 Core / 3 Enterprise** 多种引擎 → **选错引擎等于选错代际**，签约前必须确认引擎版本。走 AWS 账单，可抵扣 EDP 承诺 | 高（AWS 与 InfluxData 官方文档） | 2026-09-01 |
| 🔴 **【L13 · 阶段 5】内存三个默认值相加 = 90%，只剩 10%** | 官方 config-options：`--exec-mem-pool-size` **20%** + `--parquet-mem-cache-size` **20%** + `--force-snapshot-mem-size` **50%** = **90%**。→ 4 GB 机器只剩 **0.4 GB** 给进程与 OS，这是「内存比 CPU 先到瓶颈」的根因。⚠️ **官方调优页的按负载区间（写密集 60-70% / 查询密集 80-90% / 混合 70%）只针对 `--exec-mem-pool-size`**，另两个参数官方未给同款区间表 | 高（官方原文） | 2026-09-01 |
| **【L13 · 阶段 5】IO 线程默认 2（官方称 often insufficient），每并发写入者约占 1 个** | 官方调优页原文：IO 线程处理 HTTP 请求与 line protocol 解析，**Default: 2 (often insufficient)**；*"if you have multiple concurrent writers (for example, Telegraf agents), the default of 2 IO threads can bottleneck write performance"*。估算公式：`IO = min(W + 2, N × 0.4)`、`DataFusion = N − IO` | 高（官方原文） | 2026-09-01 |
| 🔴 **【L13 · 阶段 5】3.10 的 catalog 迁移单向不可逆，且备份路径取决于升级前版本** | Core release notes 原文：*"Upgrading to InfluxDB 3.10 is a **one-way migration**... **3.9.x and older binaries are unable to read the new catalog**, and fail to start... **Before upgrading, back up your current catalog.**"* 迁移本身 *"automatic, idempotent, and crash-safe"*，但 **幂等 ≠ 可逆**，且官方明示 *"**Restoring these objects is the only way to roll back** to 3.9.x"*。路径：**3.4.0+** → `{prefix}/catalog/v2/logs/` + `{prefix}/catalog/v2/snapshot`；**<3.4.0** → `{prefix}/catalogs/` + `{prefix}/_catalog_checkpoint`。⚠️ 官方警告：3.4.0+ 集群上的旧目录**可能是早期格式残留，不是有效的回滚源** | 高（官方原文） | 2026-09-01 |
| **【L13 · 阶段 5】Core 没有内建备份命令；`debug catalog` 是 3.10+ 的离线检查工具** | `influxdb3 create backup` / `restore` 系列是 **Enterprise 专属**（3.10+，且需 `--use-pacha-tree` 与带 admin token 的压实节点）。Core 只能**停服务后打包 data-dir**（不停服务可能拿到 Parquet 与 WAL 不一致状态）。`influxdb3 debug catalog`（子命令 `list` / `snapshot` / `sequence`）**可离线直接从对象存储检查 catalog，无需运行中的服务器**，Core 与 Enterprise 都有 | 高（官方 release notes 与 Enterprise 备份页） | 2026-09-01 |
| **【L13 · 阶段 5】`GET /ready`（3.10+）校验对象存储连通，而非进程存活** | 官方原文：*"Returns **200 OK** when the server can reach object storage, **503** when it cannot; ideal for load balancer and orchestration readiness probes."* → Core 无盘架构下**进程活着 ≠ 能服务**，K8s readinessProbe 与负载均衡健康检查应配 `/ready` 而非 TCP 端口检查 | 高（官方原文） | 2026-09-01 |
| ⚠️ **【L13 · 阶段 5】官方没有给出 Core 的硬件基线表（诚实判断）** | 检索到的硬件建议只有三类来源：①**官方 v1 时代硬件页**（明确标注 *"This page documents an earlier version of InfluxDB OSS"*，不适用于 3.x）②**云市场镜像商建议**（如 Azure 推荐 `Standard_D2s_v3`：2 vCPU / 8 GB）③**第三方对比评测**（最低 512MB/1CPU，推荐 4GB/2CPU）。→ **本课不把任何一类当作官方基线**，改为教授「从默认值倒推」的方法 | 中（属「官方未给出」的否定性判断，已三重检索确认无官方表） | 2026-09-01 |
| ⚠️ **【L13 · 阶段 5】压缩比 10-100x 是官方博客口径，且是 "often" 而非 "always"** | 官方博客原文：*"the size of the file is often **10-100x smaller** than its raw form"*。→ 实验 A 取 **10x / 25x / 50x** 三档做保守/典型/乐观对照，**属假设值**；每点 **120 字节**亦为假设（L12 已核实典型区间 60–150 的中位）。**要抓的是线性关系与量级锚点「10 万点/秒 × 30 天 ≈ 1 TB」，不是绝对值**。✅ 对照 3（`144 × 天数`）与对照 4（内存三默认 90%）**不依赖任何假设** | 中（档位取自官方口径区间内，绝对值属假设） | 2026-09-01 |
| 🔴 **【L12 · 阶段 4】`SELECT *` 的官方门槛是 1000+ 列，而 Core 每表上限仅 500 列** | 官方 optimize-queries 页原文：*"If the table contains **10 columns**, the difference in performance between the two queries is **minimal**. In a table with over **1000 columns**, the `SELECT *` query is slower and less efficient."* 结合 L6 已核实的 Core 硬限制**每表最多 500 列**（1 个 time + 最多 499 个 tag/field）→ **在 Core 上改 `SELECT *` 基本不会让查询变快**。⚠️ 精确口径：500 列时可能已有可感知差距，但**远未达官方描述的「明显变慢」量级** | 高（官方原文 + 硬限制相乘得出） | 2026-09-01 |
| 🔴 **【L12 · 阶段 4】10 MB 批量阈值与 10 MB HTTP 请求上限恰好撞车** | Core config-options：`--max-http-request-size` **默认 10mb**（3.11 起推荐带单位后缀；旧版文档写作 `10485760` 字节，取值范围 1024 字节~16777216 字节）。官方推荐的批量上限恰为 **10 MB**。**官方未说明批量阈值指压缩前还是压缩后体积** → 本课采取保守策略：**始终启用 gzip，且永远不发未压缩的 10MB 批次** | 高（两个默认值）/ 中（撞车是否真实触发，取决于官方未明示的口径） | 2026-09-01 |
| **【L12 · 阶段 4】官方批量写入双阈值，实测四档行长全部是「行数先到」** | Core optimize-writes 原文：*"The optimal batch size is **10,000 lines of line protocol or 10 MBs, whichever threshold is met first**"*。10 MB ÷ 10,000 行 = **1048 字节/行**；本课实验 A 对照 4 以 60/120/300/1000 字节四档验证，**全部行数先到**。→ 典型监控点位（60–150 字节）下 **10,000 行是实际生效阈值**；**超宽表/超长 string field（每行 > 1048 字节）时体积阈值才成为主角**。判据：**算平均行长** | 高（阈值与除法为硬算术；四档行长假设为典型值） | 2026-09-01 |
| **【L12 · 阶段 4】gzip 官方口径是「速度提升」而非「压缩比」** | 官方 optimize-writes 原文：*"Benchmarks have shown up to a **5x speed improvement** when data is compressed."* ⚠️ 措辞为 **speed improvement**（速度）+ **up to**（最高），**不是**「体积压缩到 1/5」。实际收益取决于数据重复度（line protocol 中 measurement/tag key/field key 高度重复，通常压缩比好）与网络是否瓶颈。**本机 localhost 回环下可能看不出收益甚至变慢** | 高 | 2026-09-01 |
| **【L12 · 阶段 4】`system.queries` 字段、示例输出与两条限制** | Core HTTP API 页给出官方示例输出 `SELECT * FROM system.queries LIMIT 2`，字段含 `id`/`phase`/`issue_time`/`query_type`/`query_text`/`partitions`/`parquet_files`/`plan_duration`/`permit_duration`/`execute_duration`/`end2end_duration`/`compute_duration`/`max_memory`/`success`/`running`/`cancelled`/`trace_id`，时长为 ISO8601 期间格式（如 `PT0.032689S`）。**两条限制**（Clustered 页原文）：①*"Records are lost on **pod restarts**"*（易失）②*"Queries for one namespace can **evict records** from another namespace"*，保留条数由 `--query-log-size` 控制，**默认 1000 条**（Core config-options 原文）。→ **要留存慢查询证据必须定期外部化** | 高 | 2026-09-01 |
| **【L12 · 阶段 4】`--max-concurrent-queries` 可运行时调整（3.10 新增）** | Core config-options 原文：*"Limits the number of queries that can run concurrently. You can also update the limit **at runtime** with `POST /api/v3/configure/query_concurrency_limit`."* → 线上突发并发打满时**无需重启**即可调整。`system.queries` 的 **`permit_duration`** 是等待查询许可的耗时，持续偏高即为并发打满的信号 | 高 | 2026-09-01 |
| **【L12 · 阶段 4】tag 排序建议存在 SKU 差异（Core ≠ Clustered）** | **Core** optimize-writes 页：*"将最常用的查询标签放在最前面"*（**按查询优先级排序**）；**Clustered** optimize-writes 页：*"Sort tags by key in **lexicographic order**"*（**按 key 字典序**）。两者**不一致**。→ 按 L7 已核实事实（Core：*"首次写入决定物理列顺序，靠前的列过滤与访问通常更快"*），**Core 用户应按查询频率排序，不要照抄 Clustered 的字典序建议** | 高（两页原文均已见） | 2026-09-01 |
| 🔴 **【L11 · 阶段 4】「72 小时限制」实为 432 个文件限制** | 官方 config-options 原文：*`query-file-limit`* **Default: 432**；*"With the default 432 setting and the default gen1-duration setting of 10 minutes, queries can access up to a 72 hours of data"*。→ 代码里**没有任何时间判断**，72 小时 = 432 × 10min 的**派生值**。可解释社区「查 13 天不报错」：数据稀疏未凑够 432 个文件 | 高 | 2026-09-01 |
| 🔴 **【L11 · 阶段 4】超 432 是直接报错，不是变慢** | 第三方在 Core 3.11.0 实测错误原文：*`Query would scan 432 Parquet files, exceeding the file limit... Use a narrower time range, or increase the limit with --query-file-limit (this may cause slower queries or instability)`*。官方列四条副作用：**查询变慢 / 内存飙升 / 进程可能被 OOM kill / 对象存储每文件最多 2 次 GET**，并建议 *"We recommend keeping the default setting and querying smaller time ranges"* | 高（官方原文）/ 中（错误原文为第三方实测复现） | 2026-09-01 |
| 🔴 **【L11 · 阶段 4】`gen1-duration` 调小会缩小而非扩大可查范围** | 该参数**仅 1m/5m/10m 三档**（官方原文），10m 为默认且已是文件最少档。432 个文件覆盖：1m→**7.2 小时** / 5m→36 小时 / 10m→72 小时。→ **调小 gen1 绝不是「启用压实」或扩大范围的办法** | 高 | 2026-09-01 |
| **【L11 · 阶段 4】LVC 四条官方事实（影响可用性与内存）** | ①`--count` **上限 10**（官方 CLI 参数说明）；②**InfluxQL 不支持 `last_cache()`**（官方博客原文 *"InfluxQL does not support the last_cache() function"*）→ 用 LVC 只能走 SQL；③**服务器停止即清空，且不会自动回填历史**，只有新写入才填；④*"While the InfluxDB 3 storage engine is not limited by cardinality, **it does affect the LVC**"*，内存 ≈ `key_column_cardinality × count` | 高 | 2026-09-01 |
| **【L11 · 阶段 4】Parquet 内存缓存窗口默认 5 小时** | `--file-cache-recency`（原 `--parquet-mem-cache-query-path-duration`），**默认 5h**：只有数据时间戳落在「now 到 now-5h」内的文件才在查询时被缓存。→ **查最近几小时第二次明显变快（缓存命中），查昨天每次都要重新读盘** | 高 | 2026-09-01 |
| **【L11 · 阶段 4】DataFusion / Arrow 默认批大小 8192 行** | RecordBatch 是向量化执行的数据单位，DataFusion 默认 **8192 行/批**（`datafusion.execution.batch_size`），`EXPLAIN` 中表现为 `CoalesceBatchesExec: target_batch_size=8192`。→ **这是「向量化」一词在查询计划里留下的指纹** | 高 | 2026-09-01 |
| **【L11 · 阶段 4】列存与 SIMD 的关系（易混为一谈）** | 两个独立加速：**列存省 I/O**（查 avg(temp) 读取量为行存的 1/5，本机实测对照 1）、**向量化省 CPU**（SIMD 一条指令算一批值）。**列存是 SIMD 的前提**（SIMD 要求数据连续且同类型，行存无法直接喂寄存器），**SIMD 是列存的兑现**。⚠️ 本机实验 A 测得的 8.62 倍是 Python 层的**循环开销**差异，**不等于** InfluxDB 真实加速比——真正的 SIMD 发生在 Rust 编译的 DataFusion 内核里 | 高（原理）/ 中（8.62 倍为 Python 模拟值） | 2026-09-01 |
| 🔴 **【L10 · 阶段 4】Core 确实没有 compactor（三处官方证据）** | ①Core storage engine 页直接否定句：*"InfluxDB 3 Enterprise also offers an upgraded storage engine as an alternative; **InfluxDB 3 Core does not include it**"*；②Core 的 durability / storage-engine / data-retention **三页搜不到 compact 一词**；③配置页 `gen1-duration`：gen1 文件*"that the **compactor in InfluxDB 3 Enterprise** can merge into larger generations"*。→ **Core 的 Parquet 文件永远停留在 gen1，永不合并** | 高 | 2026-08-31 |
| 🔴 **【L10 · 阶段 4】不压实的量化后果：90 天 ≈ 12,960 个文件** | 每 10 分钟持久化一次 → 每天 144 个 → 90 天 **12,960 个**，Core 永不合并。官方 `query-file-limit` 说明文字直接拿 Enterprise 压实能力对比：*"If you need to query **longer time ranges**... **InfluxDB 3 Enterprise** optimizes data storage by **compacting and rearranging Parquet files**"*。→ **长周期查询慢的根因是「要打开的文件多」，不是「数据量大」**，且这是**架构天花板，调参解决不了**（`gen1-duration` 只有 1m/5m/10m 三档，10m 已是文件最少档） | 高 | 2026-08-31 |
| **【L10 · 阶段 4】写入五站与默认参数（官方 durability 页）** | ①写校验+内存缓冲（`no_sync` 决定 ACK 时机）→②**WAL 持久化每 1 秒**→③查询可用，**最多 900 个 WAL 文件 = 15 分钟**→④**Parquet 持久化每 10 分钟**，持久化**最老**的，**保留最近 5 分钟在内存**→⑤Parquet 内存缓存。→ **「刚写就能查」的上界是 15 分钟** | 高 | 2026-08-31 |
| **【L10 · 阶段 4】`no_sync` 默认值与语义（影响持久性）** | 官方原文：`no_sync=true` → *"acknowledge the write **without waiting for persistence**"*；`no_sync=false` **标注 (default)** → 等 WAL 持久化完成才 ACK。→ **`no_sync=true` 是用持久性换延迟**，崩溃时已 ACK 的数据可能丢 | 高 | 2026-08-31 |
| **【L10 · 阶段 4】WAL tail「是持久的」，别把它当不安全** | 官方定义：WAL tail 是 WAL 中「尚未被 Parquet 收走」的最新数据；因为 WAL 每秒刷对象存储，所以它 *"**is durable**"*，只是**仍留在 WAL 里**直到下次 Parquet 持久化。→ **「在 WAL 里」影响的是查询路径（性能），不是持久性（安全）** | 高 | 2026-08-31 |
| **【L10 · 阶段 4】`--object-store` 共 6 种取值** | `memory`（无持久化，重启即丢）/ `memory-throttled`（内存+模拟云存储延迟吞吐，**本地开发推荐**，避免对性能产生错误预期）/ `file`（本地文件系统，须配 `--data-dir`，**本课程环境**）/ `s3` / `google` / `azure`（后三者为生产持久化）。→ **`file` 模式逻辑上仍是对象存储抽象**，物理落本地盘 | 高 | 2026-08-31 |
| **【L10 · 阶段 4】优雅停机与 node-id 唯一性（运维硬规则）** | ①**SIGTERM/SIGINT 触发优雅停机**：停止接受写入 → **刷 WAL 到对象存储** → 等快照完成 → 标记 stopped（`docker kill` 发 SIGKILL 会**绕过刷 WAL**）；②catalog（库/表/节点元数据）**持久化在对象存储里**，独立于进程/容器/主机 → 计算节点近乎无状态；③`--node-id` *"should be **unique** for any hosts sharing the same object store"*，相同会导致**文件路径互相覆盖且不报错** | 高 | 2026-08-31 |
| **【L10 · 阶段 4】Core 节点状态只有 running / stopped 两种** | 官方 lifecycle 页：Core 是单节点、无节点管理命令，因此**只会读到 running 或 stopped**；`stopping` / `removing` 是 **Enterprise 集群**的概念 | 高 | 2026-08-31 |
| 🔴 **【L9 · 阶段 3】`NON_NEGATIVE_DIFFERENCE()` 在 InfluxQL 里受支持（修正 L8）** | L8 引用的官方原文限定语是 *"InfluxDB 3 **SQL** doesn't provide built-in equivalents..."*。但 Core 的 **InfluxQL feature support** 表中，`NON_NEGATIVE_DIFFERENCE()` 与 `NON_NEGATIVE_DERIVATIVE()` **均在 24 个全支持的变换函数内，标 ✅**。结论：**SQL 没有 ≠ 3.x 没有**；要走 `/query` 端点使用 | 高（勾选图标逐条提取自官方 HTML） | 2026-08-31 |
| 🔴 **【L9 · 阶段 3】3.x 只有两种查询语言，Flux 已被移出清单** | Core 官方 Query data 页面**只列 SQL 与 InfluxQL 两条路径，无 Flux**。官方 Explorer 1.9 公告原文：*"**Flux to SQL converter (beta)**: Convert Flux queries to SQL with an AI-assisted converter."* —— 连转换器都做了，是最明确的弃用信号 | 高 | 2026-08-31 |
| **【L9 · 阶段 3】InfluxQL 在 3.x 是「重建中」而非完整移植** | 官方原文：*"InfluxQL is being **rearchitected** to work with the InfluxDB 3 storage engine. This process is **ongoing**"*。In-progress features 仅两条：**`SLIMIT` / `SOFFSET` 不支持** | 高 | 2026-08-31 |
| **【L9 · 阶段 3】基数类元查询「大概率永不支持」及理由** | 官方原文：*"Cardinality-related metaqueries will **likely not be supported** with the InfluxDB 3 storage engine."* 理由：*"series cardinality is no longer a limiting factor for database performance"*。→ **不是没做完，是地基换了以后没必要做**。替代：SQL `COUNT(DISTINCT ...)`。此为 L7 结论的官方铁证 | 高 | 2026-08-31 |
| **【L9 · 阶段 3】InfluxQL 支持矩阵（Core，逐条核实勾选图标）** | ✅ 元查询：`SHOW MEASUREMENTS`/`TAG KEYS`/`TAG VALUES`/`FIELD KEYS`/`RETENTION POLICIES`；❌ `SHOW DATABASES`/`SHOW SERIES`/全部 4 个 CARDINALITY 类。✅ 聚合 9 个全支持；✅ 变换 24 个全支持；❌ 选择器 `SAMPLE()`；❌ 技术分析 10 个（HOLT_WINTERS 等）全不支持；✅ `now()`/`time()`/`tz()`/`fill()` | 高 | 2026-08-31 |
| **【L9 · 阶段 3】v1→v3 数据模型映射** | 官方原文：① v1 的 **database + retention policy 合并为 3.x 的单个 database**；② v1 的 **measurement = 3.x 的 table**。→ 按 rp 分冷热的写法须改为按库划分（叠加 L6 的 Core 仅 5 库限制） | 高 | 2026-08-31 |
| **【L9 · 阶段 3】两个查询端点的差异** | **SQL** → `/api/v3/query_sql`；**InfluxQL** → `/query`（v1 兼容，返回 JSON）。两者**参数完全一致**（`db=`、`q=`、`Authorization: Token`），**仅路径不同** → 灰度迁移成本极低。Flight+gRPC 客户端可查 **SQL 或 InfluxQL** 并返回 Arrow | 高 | 2026-08-31 |
| **【L9 · 阶段 3】`SHOW TAG VALUES` 不带 FROM 的稳定性警告** | 官方原文（strongly recommend）：须带 `FROM` 指定 **1-50 张表**；不带则引擎要读**所有表**，可能导致 *"poor query performance, **query timeouts**, or unnecessary resource allocation that **may affect other queries**"* —— 不是慢，是可能拖垮实例 | 高 | 2026-08-31 |
| 🔴 **【L8 · 阶段 3】UTC+8 按天分桶默认错位 8 小时** | `date_bin(interval, expression[, origin_timestamp])` 的 **origin_timestamp 默认 Unix epoch（1970-01-01T00:00:00Z）**。对 UTC+8 而言该时刻是北京时间 08:00，故 `DATE_BIN(INTERVAL '1 day', time)` 的「一天」**从北京时间 08:00 开始**——凌晨与清晨的数据被算进前一天。修法：显式 `TIMESTAMP '1970-01-01T16:00:00Z'`，或用 `date_bin_wallclock`（按指定时区挂钟时间分桶，注意避开夏时制切换点） | 高（origin 默认值为官方原文；UTC+8 错位为推导 + 本机模拟验证） | 2026-08-31 |
| 🔴 **【L8 · 阶段 3】`LAG` 是行偏移，不是时间偏移** | 官方定位：*"Window functions like LAG and LEAD let you access values from previous or subsequent rows"*。`LAG(temp, 1)` = **前一行**。数据规律时恰等于「前一小时」，**有缺失即分家**。需精确时间偏移（exactly 1 hour ago）时用官方给的自连接：`LEFT JOIN ... ON previous.time = current.time - INTERVAL '1 hour'` | 高 | 2026-08-31 |
| 🔴 **【L8 · 阶段 3】3.x SQL 无 `increase()` / `NON_NEGATIVE_DIFFERENCE()`** | 官方原文：*"InfluxDB 3 SQL doesn't provide built-in equivalents to Flux's `increase()` or InfluxQL's `NON_NEGATIVE_DIFFERENCE()` functions."* 官方替代模式：**`GREATEST(value - LAG(value) OVER (PARTITION BY ... ORDER BY time), 0)`**（处理计数器重置），累计用 CTE + `SUM(...) OVER (...)`，聚合时 `WHERE increase > 0` 过滤首行与重置行 | 高 | 2026-08-31 |
| **【L8 · 阶段 3】`date_bin` 与 `date_bin_gapfill` 单位支持不同** | `date_bin` 支持 ns/µs/ms/s/min/hour/day/week/**month/year/century**；`date_bin_gapfill` 与 `date_bin_wallclock` **明确不支持 months/years/century**（官方原文 "The following intervals are not currently supported"） | 高 | 2026-08-31 |
| **【L8 · 阶段 3】gapfill 的两条硬性要求** | ①*`date_bin_gapfill` requires time bounds in the WHERE clause*（必须有上下时间界，否则报错）；②*"The expression passed to interpolate or locf must use an aggregate function"*（须写成 `interpolate(avg(temp))`）。补值语义二选一：**`interpolate` 插值** / **`locf` 沿用前值**；若要填 0 须用 `COALESCE(COUNT(...), 0)` | 高 | 2026-08-31 |
| **【L8 · 阶段 3】窗口函数四要素与首行 NULL** | 官方步骤：窗口函数 + `OVER`、**`PARTITION BY` 按 tag 分组**（保证只在同设备内比较）、**`ORDER BY time`**、算术运算。**每个分组首行 `LAG` 返回 NULL**，要默认值用第三参数 `LAG(temp, 1, 0)` | 高 | 2026-08-31 |
| 🔴 **【L7 · 阶段 3】3.x 不再靠索引加速 tag（推翻 1.x 老经验）** | 1.x/2.x 官方原文「Tag values are indexed and field values are not」；而 **3.x Core 的 schema-design 页面全文 0 次提及「索引」**（本机抓取官方页面正文核实，`index` 与「索引」各 0 次）。**Clustered / Enterprise 版**明文：*"InfluxDB Clustered indexes tag keys, field keys, and other metadata to optimize performance. **It doesn't index tag values or field values.**"* → 结论：**需要按某字段查询 ≠ 该字段必须做 tag** | 高（Clustered 明文）/ 中（Core 为「未提及」推断） | 2026-08-31 |
| 🔴 **【L7 · 阶段 3】series cardinality 定义与依赖 tag** | 官方 Glossary 原文：*"The number of unique measurement, tag set, and field key combinations"*。**依赖 tag（dependent tags）被另一个 tag 所限定，不增加基数**——官方例子：`email`(3) × `status`(2) = 6，再加由 email 唯一决定的 `firstname`(3) 后**仍是 6 而非 18** | 高 | 2026-08-31 |
| 🔴 **【L7 · 阶段 3】首次写入决定物理列顺序且不可更改** | Core 官方原文：*"The first write to a table in InfluxDB 3 Core determines the physical column order in storage, and that order has a direct impact on query performance."* 靠前的列过滤与访问通常更快；**首次写入后新增的 tag 会排到最后**。→ 建表时须按查询频率排 tag 顺序，一次性决策 | 高 | 2026-08-31 |
| **【L7 · 阶段 3】宽 schema 的三条后果（官方原文）** | *"Increased resource use when persisting and compacting data during ingestion"*（摄入期持久化与压实资源占用上升）/ *"Reduced sort performance due to complex primary keys with too many tags"*（tag 过多导致主键复杂、排序变慢）/ *"Reduced query performance when selecting too many columns"* | 高 | 2026-08-31 |
| **【L7 · 阶段 3】稀疏 schema 的成因与后果** | 官方定义：*"A sparse schema is one where many rows contain null column values."* 成因两类：①非同质的表 schema ②**以不同时间戳上报单个字段**（同一 tagset 先报 fieldA 再报 fieldB 但时间戳不同 → 产生两行，各带一个 null）。后果：*"require the query engine to evaluate many null columns"* | 高 | 2026-08-31 |
| 🟡 **【L7 · 阶段 3】基数类元查询很可能不被 3.x 支持** | 官方 InfluxQL feature support 页面：*"With the InfluxDB 3 storage engine, series cardinality is no longer a limiting factor for database performance. **Cardinality-related metaqueries will likely not be supported** with the InfluxDB 3 storage engine."* → 不要指望有内置「查基数」命令，用 SQL `COUNT(DISTINCT ...)` 自数 | 高（该句本身）/ 中（Core 页面未逐条列出各 SHOW 命令支持状态） | 2026-08-31 |
| 🟡 **【L7 · 阶段 3】`SELECT DISTINCT` 必须带时间范围** | 官方 DVC 博客：不带 time bounds 或时间范围过大时，查询可能 *"very heavy"*，需要全表扫描。查询 distinct tag values 的替代方案是 **Distinct Value Cache（DVC）**，约 30ms 返回 | 高 | 2026-08-31 |
| **InfluxDB 不是向量数据库**：是时序数据库（TSDB）。InfluxDB 3 文档中的 "vectorized execution" 指列存 + SIMD 的查询执行技术，与向量数据库（存 embedding 做 ANN 检索）无关。InfluxDB 无原生向量索引/ANN 检索能力；时序相似性检索的通行做法是 InfluxDB + Milvus/Pinecone 组合 | 已核实 | 高 | 2026-08-31 |
| 当前版本：**InfluxDB 3 Core 3.11**（2026 年发布）。3.11 要点：压实与查询性能改进；**catalog 迁移——升级前须备份 catalog** | 已核实（InfluxData 官方文档首页公告 + Core release notes 入口） | 高 | 2026-08-31 |
| **InfluxDB 3.10 要点**（Core）：catalog 格式在首次 3.10 启动时自动从 v2 升级到 v3，**迁移单向不可逆，升级前须备份 catalog**；新增 `--max-concurrent-queries`（并发查询上限，运行时可调）；新增 `GET /ready` 就绪探针（校验节点能否连通底层对象存储，比 uptime 检查更可靠）；处理引擎支持跨库查询与 trigger lockdown 标志；并行压实（parallel compaction） | 已核实（InfluxData 官方 3.10 博客 + 文档站内公告） | 高 | 2026-08-31 |
| **InfluxDB 3 Enterprise 3.11 要点**：升级版存储引擎 GA（新集群默认 Parquet，升级用 `--upgrade-pacha-tree`）；原地增量备份恢复；集成 Explorer UI（`--mode all,webui`）；批量导入（从 S3 等远端对象存储导入，可配并发） | 已核实（官方文档公告） | 高 | 2026-08-31 |
| ⚠️ **Docker `latest` 标签变更**：**2026-09-15 起** InfluxDB 官方 Docker 镜像的 `latest` 标签将指向 **InfluxDB 3 Core**。官方明确警告：为避免意外升级，部署时必须使用具体版本标签（如 `influxdb:2` 而非 `influxdb:latest`） | 已核实（InfluxData 官方文档多页公告） | 高 | 2026-08-31 |
| **Python 客户端**：包名 `influxdb3-python`（导入模块名 `influxdb_client_3`），**最新版 0.20.0（2026-06-11 发布）**；要求 **Python >= 3.9**（官方建议 3.11+ 以获得最佳性能）；依赖 pyarrow（自动安装），pandas 可选（`to_pandas()` / `write_file()` 需单独装）；MIT 许可；封装 pyarrow.flight 客户端，走 gRPC Flight 协议 | 已核实（PyPI 官方页 classifiers + 官方文档） | 高 | 2026-08-31 |
| **客户端核心 API**：`InfluxDBClient3(token=, host=, database=)`；`Point("measurement").tag(k,v).field(k,v)`；`client.write(point)` 支持 Point 或 line protocol 字符串；`client.query(query, language="sql"|"influxql")` 返回 Arrow reader，`.read_all()` 后 `.to_pandas()`；三种写入模式：同步（默认，不重试不回调）、批量（batch_size/flush_interval/retry）、异步（已废弃 deprecated） | 已核实（官方文档 API reference） | 高 | 2026-08-31 |
| **influx3 CLI**（随 influxdb3-python 附带，面向读/查询工作流）：`influx3 query -d <db> "SELECT ..."`，输出格式支持 `json`（默认）/ `jsonl` / `csv` / `pretty`；配置优先级：CLI flags > `INFLUXDB3_*` 环境变量 > 旧版 `INFLUX_*` > 内置默认值（host 默认 `http://127.0.0.1:8181`） | 已核实（PyPI 项目描述 + readthedocs） | 高 | 2026-08-31 |
| **Line Protocol 语法**：`<measurement>[,<tag_key>=<tag_value>...] <field_key>=<field_value>[,...] [<timestamp>]`。measurement 必需；tag set 可选；field set 必需（至少一个字段）；timestamp 可选（缺省用主机 UTC 系统时间，默认纳秒精度）。**第一个未转义空格**分隔 measurement+tag set 与 field set，**第二个未转义空格**分隔 field set 与 timestamp。行协议对空白敏感，不支持标签/字段值中的换行符 | 已核实（InfluxData 官方 line protocol 参考文档） | 高 | 2026-08-31 |
| **Line Protocol 数据类型**：float（IEEE-754 64 位，**默认数值类型**，支持科学计数法）/ integer（有符号 64 位，**尾缀 i**）/ uinteger（无符号 64 位，**尾缀 u**）/ string（**必须双引号**，长度上限 64KB）/ boolean（`t,T,true,True,TRUE` / `f,F,false,False,FALSE`，**不可加引号**，加了变字符串）。measurement/tag key/tag value/field key 始终是字符串 | 已核实（官方文档） | 高 | 2026-08-31 |
| **Line Protocol 引号规则**：measurement、tag key、tag value、field key 加双引号或单引号时，**引号会被当作名称的一部分**（应避免使用）；field value 中只有 string 类型用双引号；timestamp 永不加引号（加了报 `bad timestamp`） | 已核实（官方文档 + 多份一致） | 高 | 2026-08-31 |
| **InfluxDB 3 术语变化**：官方文档（尤其 Enterprise/Core 新版 get-started）已改用 **table** 指代原 measurement，标签集/字段集/时间戳术语不变。SQL 查询中 `FROM` 后面跟 table 名。measurement 作为历史术语仍被广泛使用 | 已核实（官方 get-started/write 文档原文用 table） | 高 | 2026-08-31 |
| **schema-on-write**：InfluxDB 3 是 schema-on-write——可直接写入，库/表/schema 自动创建；**schema 创建后，后续写入请求会被校验**，不符合即拒绝。后续可新增 tag 与 field | 已核实（官方 Enterprise get-started/write 文档原文） | 高 | 2026-08-31 |
| **架构栈（FDAP）**：Rust 编写；Apache **F**light（高吞吐传输）+ **D**ataFusion（向量化 SQL 引擎）+ **A**rrow（列式内存格式）+ **P**arquet（压缩列式存储）。查询语言 **SQL + InfluxQL**（**Flux 已废弃**）；写入 line protocol over HTTP（默认端口 **8181**）；存储为对象存储（S3/Azure/GCP）或本地盘上的 Parquet | 已核实（官方 README + AWS 技术博客 + 多源一致） | 高 | 2026-08-31 |
| **性能特征**：近期数据查询 <10ms（last-value 查询）/ distinct metadata ~30ms；百万级点/秒写入；**无限 tag 基数**（unlimited cardinality，列存 + Parquet 消除了 2.x 的高基数瓶颈）；2.x 时代索引在基数超过约 100 万时崩溃 | 已核实（官方 README + InfluxData 官方技术分享 + 二手深评文章，二手部分已标注） | 高（官方 README）/ 中（100 万阈值来自二手） | 2026-08-31 |
| **2.x → 3.x 迁移代价**：API 与查询语言均变更（Flux 废弃、write API 端点路径变化）；Telegraf output 插件大体兼容，但**手写代码几乎都要重构**。这是 2026 年仍有大量用户停留在 2.x 的主因 | 已核实（二手深度调研文章，与官方文档 Flux 废弃事实一致；具体比例数字不得引用） | 中 | 2026-08-31 |
| **【L2 · 阶段 1】3.x 版本时间线（关键区分：云端产品与开源版分开 GA）**：2023-04-26 InfluxDB 3.0 产品线发布（首次引入无限基数 / SQL / 对象存储）→ 2023-09-06 InfluxDB Clustered 发布（本地与私有云商业形态）→ **2025-04-15 基于 IOx 引擎的 InfluxDB 3 Core 达到 GA** → 2025-04-17 Core 与 Enterprise 同时宣布上市 → 2025-09-30 3.5（Explorer 仪表板、缓存查询）→ 2025-10-16 Amazon Timestream for InfluxDB 上线（AWS 托管 Core/Enterprise） | 已核实（三源一致：官方 README「Core GA since April 2025」、百度百科词条带具体日期、2024 年行业月报称"去年 4 月发布 3.0"即 2023-04）。**核心结论：3.0 云端 2023 年先行，开源 Core 版 2025-04 才 GA** | 高 | 2026-08-31 |
| **【L2 · 阶段 1】三代架构对照**：1.x（2016-2020）= Go + TSM 引擎 + InfluxQL + TICK Stack + 单机（集群闭源）；2.x（2020-2024）= TSM + 自研 KV 索引 + Flux + 内置 UI/token 认证/task 调度；3.x = Rust + FDAP（Flight/DataFusion/Arrow/Parquet）+ SQL 主力 + InfluxQL 兼容层 + Flux 废弃。IOx 是 3.x 存储引擎的**研发代号** | 已核实（多源一致：官方 README 架构说明、AWS 官方技术博客、二手深度调研文章；1.x/2.x 年份区间来自二手深评，与官方版本线一致） | 高（3.x 架构）/ 中（1.x/2.x 年份区间） | 2026-08-31 |
| **【L2 · 阶段 1】Flux 被弃的两大动因**：①学习曲线陡峭——函数式管道语法（`\|>` 管道 + `filter(fn:)`）对熟悉 SQL 的数据工程师不友好，等于重学一门语言；②高基数问题未根治——索引在基数越过百万量级时仍会崩溃，集群仍为 Enterprise 专属。**"百万量级"这个阈值来自二手来源，仅作量级参考，不得作为精确指标引用** | 已核实（官方 README 确认 Flux 已废弃 + 无限基数为 3.x 突破点；"指数在百万级崩溃"与"Flux 学习曲线"两条为二手深评观点，标注层级） | 高（Flux 废弃）/ 中（百万阈值） | 2026-08-31 |
| **【L2 · 阶段 1】三个 SKU 定位**：Core = MIT/Apache 2.0 开源、**单机**、**不含 compactor**（摄入/查询/压实/处理四功能竞争同一实例资源）、定位近期数据（官方原文"typically the last 3-5 days"）、适合边缘/IoT/开发；Enterprise = 商业、集群 HA、历史数据长周期查询、完整运维工具；Cloud = Serverless（按用量）/ Dedicated（独占），托管于 AWS/GCP/Azure。另有 Amazon Timestream for InfluxDB（2025-10 上线，AWS 官方托管） | 已核实（AWS 官方技术博客对 Core/Enterprise 差异的详细说明 + 官方 README + 官方公告） | 高 | 2026-08-31 |
| 🔴 **【L3 · 阶段 2】Core 的 72 小时查询限制（关键硬约束）**：官方 Core get-started/query 文档原文 *"InfluxDB 3 Core limits query time ranges to approximately 72 hours (both recent and historical) to ensure query performance"*。**近期与历史数据皆受限**。→ 查 5 天前的数据会返回空，但**数据实际上已写入**，极易被误判为"写入失败"。此事实直接印证 L2「Core 只适合近期数据」的结论，且比 AWS 博客的"3-5 天"更精确（二者方向一致，72 小时为官方 Core 文档口径） | 已核实（官方 Core get-started/query 文档原文） | 高 | 2026-08-31 |
| **【L3 · 阶段 2】性能指标的官方出处 = 两个可选内存缓存**：<10ms 对应 **LVC**（Last Values Cache，缓存序列/列层级的最后 N 个值于内存）；~30ms 对应 **DVC**（Distinct Values Cache，缓存指定列的去重值于内存）。**关键推论：二者是需主动配置的可选优化项，默认状态查询走常规路径，达不到这两个数字** | 已核实（官方 Core get-started/query 文档「Optimize queries」章节原文） | 高 | 2026-08-31 |
| **【L3 · 阶段 2】Docker 启动命令（官方镜像页原文）**：`docker run --rm -p 8181:8181 -v $PWD/data:/var/lib/influxdb3/data -v $PWD/plugins:/var/lib/influxdb3/plugins influxdb:3-core influxdb3 serve --node-id=my-node-0 --object-store=file --data-dir=/var/lib/influxdb3/data --plugin-dir=/var/lib/influxdb3/plugins`。官方另给 compose.yaml 版本（image `influxdb:3-core`，command 用数组形式传参）。**镜像变体对照**：`influxdb:3-core`（最新 OSS）/ `influxdb:2`（v2）/ `influxdb:1.11`（v1）/ `influxdb:3-enterprise`（需许可） | 已核实（Docker Hub 官方 influxdb 镜像页原文） | 高 | 2026-08-31 |
| **【L3 · 阶段 2】serve 关键参数**：`--node-id`=实例唯一标识，**参与存储路径构成**（`/<bucket>/<node-id>/`），乱改会找不到原数据；`--object-store` 六取值 = `file`（本地，默认）/ `memory`（不持久）/ `memory-throttled`（模拟云存储延迟，压测用）/ `s3`（含 MinIO、Ceph 等兼容服务）/ `google` / `azure`；`--data-dir` 默认 `~/.influxdb`；`--plugin-dir` **给了才启用处理引擎** | 已核实（官方 Core get-started/setup 文档原文） | 高 | 2026-08-31 |
| **【L3 · 阶段 2】快速启动模式**：不带任何参数直接运行 `influxdb3`，系统按主机名自动生成 `node-id`（`{hostname}-node`，拿不到主机名则 `primary-node`）、`object-store=file`、`data-dir=~/.influxdb` 并打印警告。官方定性：**面向开发/测试/家庭环境，生产必须显式指定 `--node-id`**。环境变量优先级高于自动生成的默认值（如 `INFLUXDB3_NODE_IDENTIFIER_PREFIX`） | 已核实（官方 Core get-started/setup 文档原文） | 高 | 2026-08-31 |
| **【L3 · 阶段 2】原生安装的隐藏依赖**：官方安装脚本 `curl -O https://www.influxdata.com/d/install_influxdb3.sh && sh install_influxdb3.sh core`。**`influxdb3` 二进制依赖与其同目录的 `python/` 文件夹**（处理引擎需要）；手动从 tarball 解压时不可把二进制单独挪走，应把父目录加入 PATH | 已核实（InfluxData 官方处理引擎教程原文） | 高 | 2026-08-31 |
| **【L3 · 阶段 2】token 与认证**：`influxdb3 create token --admin` **只在创建时打印一次，之后无法从数据库找回**（官方原文 "you cannot retrieve it from the database later"）；**第一个创建的 admin token 即该服务器的 operator token**。环境变量三种写法：`export INFLUXDB3_AUTH_TOKEN=...`（Unix）/`$env:INFLUXDB3_AUTH_TOKEN="..."`（PowerShell）/`set INFLUXDB3_AUTH_TOKEN=...`（cmd，**官方文档要求末尾留一个空格**，否则空格会被吞进 token） | 已核实（官方 Core get-started/setup 文档原文） | 高 | 2026-08-31 |
| **【L3 · 阶段 2】写入 CLI**：`influxdb3 write --database <db> --token <token> --precision s '<line protocol>'`；从文件写加 `--file <path>`；`--accept-partial` 表示部分行格式错误时**仍接受合法行**而非整批拒绝。`--precision` 取值 `ns`（默认）/`us`/`ms`/`s` | 已核实（官方 Core get-started/write 文档原文） | 高 | 2026-08-31 |
| **【L3 · 阶段 2】查询 CLI**：`influxdb3 query --database <db> "SELECT * FROM home ORDER BY time"`；参数 `-H/--host`（默认 `http://127.0.0.1:8181`）、`-d/--database`（**必需**）、`-l/--language`（`sql` 默认 / `influxql`）、`-t/--token`。**官方原文明确：Flux 在 InfluxDB 3 中不受支持**（"Flux... is **not** supported in InfluxDB 3"） | 已核实（官方 Core get-started/query 文档原文） | 高 | 2026-08-31 |
| **【L3 · 阶段 2】端口是版本指纹**：InfluxDB 3 的 HTTP API 端口为 **8181**；**8086 是 1.x / 2.x 的端口**。看到 8086 即可判断起的不是 3.x | 已核实（官方 setup/write/query 文档 + Docker Hub 镜像页，多源一致） | 高 | 2026-08-31 |
| ⏳ **【L3 · 阶段 2】查询输出的精确格式**：课件第四幕给出的表格样式为**构造示意，非实测输出**（编写环境无 Docker，未能实跑）。已在课件中显式标注 ⏳ 并改为「判断成功的三条标准」（返回 2 行 / 含 room+temp+hum+co+time 列 / co 显示为 0），避免读者逐字比对表格样式。**待用户在真实环境验证后可回写实际输出** | ⏳ **置信度：中**（命令语法均取自官方文档，仅渲染格式未实测） | 中 | 2026-08-31 |
| **【L4 · 阶段 2】`/api/v3/write_lp` 端点规范（官方 v3-write-lp 文档原文）**：`POST /api/v3/write_lp?db=<db>&precision=<p>&accept_partial=<bool>&no_sync=<bool>`；Header `Authorization: Bearer <TOKEN>`，gzip 时加 `Content-Encoding: gzip`。成功响应 **204 No Content**，带 `cluster-uuid` 响应头（实例 catalog UUID，所有 HTTP 响应均含，可用于多实例环境定位处理请求的实例）。支持 **multi-member gzip**（RFC 1952，多个 gzip 文件直接拼接单次发送） | 已核实（官方 v3-write-lp 文档原文） | 高 | 2026-08-31 |
| 🔴 **【L4 · 阶段 2】CLI 与 HTTP API 的参数命名与默认值均不同（易错点）**：`precision` 取值——CLI 为 `ns`/`us`/`ms`/`s`，HTTP API 为 `nanosecond`/`microsecond`/`millisecond`/`second`/`auto`；缺省值——CLI 为 `ns`，HTTP API 为 `auto`。`accept_partial`——**CLI 默认严格**（给了 `--accept-partial` 才放宽），**HTTP API 默认 `true`（宽松）**。设计动因：CLI 交互式宁严勿纵，HTTP API 面向高吞吐批量宁可部分成功也免整批重来 | 已核实（官方 CLI write 文档 + v3-write-lp 文档对照） | 高 | 2026-08-31 |
| 🔴 **【L4 · 阶段 2】`accept_partial=true`（默认）时 400 ≠ 失败**：合法行**已写入**，仅非法行被拒；响应 `400` + body `error: "partial write of line protocol occurred"` + **`data` 为数组**（含 original_line / line_number / error_message）。`accept_partial=false` 时整批拒绝，body `error: "parsing failed for write_lp endpoint"` + **`data` 为对象**。**推论：写监控告警不能只看 HTTP 状态码，必须解析 body 判断 `data` 是数组还是对象** | 已核实（官方 v3-write-lp 文档「Partial writes」章节原文，含两种响应用例） | 高 | 2026-08-31 |
| **【L4 · 阶段 2】`no_sync` 是吞吐与持久性的取舍**：默认 `false` = 等 WAL 刷到对象存储（约每秒一次）后再确认，延迟高、丢失风险低；`true` = 校验+写 WAL 后立即响应，延迟低但**崩溃可能丢数据**。官方原文 *"Using `no_sync=true` is best when prioritizing high-throughput writes over absolute durability"* | 已核实（官方 v3-write-lp 文档原文） | 高 | 2026-08-31 |
| **【L4 · 阶段 2】三种写入端点的适用场景**（官方 http-api 索引页原文）：新建工作负载 → `/api/v3/write_lp`；迁移 v1 工作负载 → `/write`；迁移 v2 工作负载 → `/api/v2/write`；Telegraf → `outputs.influxdb_v3` 插件（v1/v2 老配置用 `outputs.influxdb` / `outputs.influxdb_v2`） | 已核实（官方 HTTP API 写入总览页原文） | 高 | 2026-08-31 |
| **【L4 · 阶段 2】字段类型冲突的官方错误消息**（原文）：`invalid column type for column 'temp', expected iox::column_type::field::float, got iox::column_type::field::string`。**规则：某列首次写入即锁定类型，后续类型不符一律拒绝**——这是 schema 必须事前规划的根因 | 已核实（官方 v3-write-lp 文档原文） | 高 | 2026-08-31 |
| 🎯 **【L4 · 阶段 2】`auto` 精度检测的阈值含义（本课经验算修正的重要结论）**：官方规则为按时间戳量级分桶（<5e9 判秒 / <5e12 判毫秒 / <5e15 判微秒 / 更大判纳秒）。**经换算验证：三个阈值对应同一时刻 2128-06-11 08:53:20 UTC**（5e9 秒 = 5e12 毫秒 ÷ 1000 = 5e15 微秒 ÷ 1e6 = 5,000,000,000 秒）。→ **在 2128 年前的常规时间范围内 `auto` 是可靠的，不会"差出几十年"**。真正的风险是两类：①**人为构造的小数值**（如测试数据 `v=1 1000`，本意 1000 毫秒但 < 5e9 被判为秒 → 落点 1970-01-01 00:16:40）②**1970 年前的历史数据**（负数时间戳）。**本课初稿曾误写"边界附近猜错就是几十年误差"，经验算后已修正**——这是评审环节拦下的准确性问题 | 已核实（官方 v3-write-lp 文档 auto 规则原文 + 本课换算验算，验算过程可复现） | 高 | 2026-08-31 |
| 🔴 **【L5 · 阶段 2】客户端 `host` 必须带 scheme（本机实测发现，官方文档示例未体现）**：客户端 `__init__` 用 `urlparse(host)` 解析，逻辑为 `hostname = parsed.hostname or host` / `scheme = parsed.scheme or "https"` / `port = parsed.port or 443`。传 `host="localhost:8181"` 时 `urlparse` 把 `localhost` 当作 **scheme**，`hostname` 与 `port` 均取不到 → 回退为 `hostname="localhost:8181"`、`port=443` → 拼出畸形连接串 `grpc+tcp://localhost:8181:443`，pyarrow 抛 `ArrowInvalid: Cannot parse URI ... syntax error`。**构造阶段即失败，尚未发生任何网络通信**。官方 Core 文档示例确有 `host="localhost:8181"` 写法（面向云端 443 场景）。**本机 v0.21.0 实测复现** | 已核实（安装包源码 `influxdb_client_3/__init__.py` + pyarrow 构造复现，双重证据） | 高 | 2026-08-31 |
| 🔴 **【L5 · 阶段 2】客户端默认写 V2 端点，且 `no_sync` 与之互斥**：`WriteOptions` 实测默认值 `use_v2_api=True` → 写入 `/api/v2/write`（**非** L4 学的 `/api/v3/write_lp`）；源码显式校验 `if self.use_v2_api and self.no_sync: raise ValueError("invalid write options: no_sync cannot be used with use_v2_api")`。→ **想用 V3 端点或 `no_sync` 必须先 `use_v2_api=False`**。服务端若不支持 V2 端点返回 405，客户端会翻译成提示 *"Set use_v2_api=False to use the V3 API endpoint"* | 已核实（本机 v0.21.0 实测 `inspect.signature(WriteOptions)` + 源码校验分支） | 高 | 2026-08-31 |
| **【L5 · 阶段 2】`influx3` CLI 随 `influxdb3-python` 附带，且仅有一个 `query` 子命令**：实测 `influx3 --help` 输出 `usage: influx3 [-h] {query,q} ...`；完整参数 `[-f FILE] [-H HOST] [-d DATABASE] [--token] [-l {sql,influxql}] [--format {json,jsonl,csv,pretty}] [-o OUTPUT] [--query-timeout]`。**默认输出 json**（与 `influxdb3 query` 默认 pretty 相反）。配置优先级 CLI flags > `INFLUXDB3_*` > 旧版 `INFLUX_*` > 默认值（host 默认 `http://127.0.0.1:8181`） | 已核实（本机 v0.21.0 实测 `--help` 全文 + PyPI 官方说明） | 高 | 2026-08-31 |
| **【L5 · 阶段 2】Flight 端口：Core 与 HTTP 共用 8181（非 8082）**：服务端架构为统一服务（`UnifiedService` 挂单一 `TcpListener`，按请求特征分流到 `HttpApi` 或 gRPC Flight），客户端源码用同一解析出的 port 构造 `grpc+tcp://host:port`（仅 `query_port_overwrite` 可改，无独立 flight 端口参数）。**8082 是 InfluxDB 3 开源社区版/IOx 的端口**，见于部分社区文档与 DeepWiki 页面，与 Core 不同 | 已核实（服务端架构资料 + 客户端源码 `connection_string` 构造逻辑双向印证） | 中高（Core 结论高；社区版端口以实际部署为准） | 2026-08-31 |
| **【L5 · 阶段 2】版本号订正**：档案原记 `influxdb3-python` 最新 **0.20.0**（2026-06-11）；**本机 2026-08-31 实测 pip 安装为 0.21.0**。0.21.0 新增 `influx3 query` CLI 子命令（PR #208）；0.20.0 新增 partial write 支持与默认写 V2 端点（PR #213）。依赖：certifi / pyarrow / python_dateutil / reactivex / urllib3（pandas、polars 为可选 extra） | 已核实（本机 `pip show` 实测 + changelog） | 高 | 2026-08-31 |
| **【L6 · 阶段 3】四要素与主键（官方 schema-design 原文）**：point = table + ≥1 field + timestamp + 可选 tag。**主键 = timestamp + tag set**，且「null tag 不进主键」→ `host=`（空值）与「干脆不写 host」**塌缩成同一行**。series = table + tag set + field key（field key **不进主键**）。tag 值**只能是字符串**；field 值可为 int/uint/float/string/bool；timestamp **永不为 null** | 已核实（官方 Core schema-design 文档原文） | 高 | 2026-08-31 |
| **【L6 · 阶段 3】存储列类型对照**：tag → **string dictionary（字典编码）**；field → `int64`/`float64`/`uint64`/`bool`/`string`；timestamp → `time`（纳秒）。官方 Core get-started 原文另指出：**表创建时（显式调用或首次写入）按 tag 到达顺序设置主键，且 tag 列定义不可变（immutable）** | 已核实（官方 Core get-started 数据模型章节原文） | 高 | 2026-08-31 |
| 🔴 **【L6 · 阶段 3】Core 硬限制（官方 admin/databases 原文）**：**数据库上限 5**；**表上限 2000**（跨所有库，单库不限但总数受限）；**每表列数上限 500**（1 个 time 列 + 最多 499 个 tag/field 列）。超列数上限 → 写入失败。→ **落地推论：分库策略不能按服务实例/按天/按客户划分**，5 个额度根本不够 | 已核实（官方 Core admin/databases 文档原文） | 高 | 2026-08-31 |
| **【L6 · 阶段 3】其他 SKU 的表/列上限**（对照，均可在配置调）：Enterprise 表 10000 / 列 500；Cloud Dedicated 表 500 / 列 250；Cloud Serverless 表 500 / 列 200。**只有 Core 那条与本地环境直接相关** | 已核实（官方 Enterprise config-options + Cloud 限额页） | 高 | 2026-08-31 |
| 🔴 **【L6 · 阶段 3】保留键规则（三类后果不同，务必分清）**：**`time`** → 不能作 tag key 或 field key，官方 Core InfluxQL 文档原文「rejects writes ... and returns an error」（**可以**作表名/库名）；**`_field` / `_measurement`** → 作 tag 或 field key 时**整个点被丢弃**（discarded，无报错）；**`_` 开头**与 **`iox_` 前缀** → 系统保留，官方警告可能「导致意外行为或数据丢失」。⏳ 注：1.x 时代文档曾记载 time 作 key 时「返回 204 但静默丢弃」，与 Core 的「返回错误」表述不同——**两条路都不能用，以 Core 口径为准** | 已核实（官方 Core naming-restrictions + InfluxQL reference 原文） | 高（Core 口径）/ 中（1.x 历史行为） | 2026-08-31 |
| **【L6 · 阶段 3】转义规则的不对称（易记错）**：measurement 转义 **逗号、空格**；tag key / tag value / field key 转义 **逗号、等号、空格**；**只有 string field value 需要转义双引号与反斜杠**（tag value 里的双引号**不需要**转义）。另：给 measurement/tag key 加引号会被当成**名字的一部分**，查询时需嵌套引号 | 已核实（官方 line protocol 参考文档原文 + 官方示例） | 高 | 2026-08-31 |
| 🔴 **【L6 · 阶段 3】大小写三套规则（写入与查询不一致）**：**line protocol 写入 → 敏感**（`Host` 与 `host` 是两个 key）；**SQL 未加引号 → 不敏感**，**SQL 加双引号 → 敏感**；**InfluxQL → 一律敏感**。另外 SQL 未加引号标识符**只能含字母、数字、下划线** → 含 `-` 的名字（如 `http-status`）**在 SQL 里必须加双引号**，否则被解析为减法。→ 官方最佳实践：命名用 `cpu_usage` 而非 `cpu-usage` 或 `cpuUsage` | 已核实（官方 Core naming-restrictions 文档「Query language specific considerations」原文） | 高 | 2026-08-31 |
| **【L6 · 阶段 3】tag/field 同名 → 列冲突（3.x 行为与 1.x 不同）**：官方 schema-design 原文「If you attempt to write a table that contains tags or fields with the same name, the write fails due to a column conflict」。⚠️ **1.x 的行为是改名成 `location_1` 而非拒绝**——别按老经验判断 | 已核实（官方 schema-design 文档原文） | 高 | 2026-08-31 |
| ⏳ **【L4 · 阶段 2】时间戳有效范围**：`-9223372036854775806`（1677-09-21）~ `9223372036854775806`（2262-04-11）。数值来自**第三方**（阿里云 TSDB for InfluxDB）的 line protocol 文档，**未在 InfluxDB 3 Core 官方文档核实到同表**。与 int64 纳秒表示范围逻辑自洽，日常使用不受影响，但课件已标注「不得写进正式文档而不加注」 | ⏳ **置信度：中**（第三方来源，物理逻辑自洽但未获官方一手确认） | 中 | 2026-08-31 |

## 学习记录

- 2026-08-31：用户提出「系统学习 InfluxDB 向量数据库」。**澄清概念**：InfluxDB 是时序数据库，非向量数据库；「向量」疑来自 vectorized execution（向量化执行）。用户确认学习目标 = InfluxDB 时序数据库，目标场景 = 工作需要落地，要求尽量全面。建档开课，大纲落盘并评审通过（6 阶段 / 19 课 / 55 知识点）。
- 2026-08-31：阶段 1（L1 + L2，6 知识点）交付。双视角内联评审 P0=0（P1×1、P2×4 当批修复）。配套补齐：6 个阶段 `overview.md`、7 张 SVG（1 张总路径图 + 6 张阶段路径图）、两课的 📚 官方文档与 📋 速查卡。
- 2026-08-31：用户指定使用 `topic-teach` 技能。据此对齐规范：画像字段改为技能标准字段（水平/目标/输出格式），故事主线按「主角/冲突/转折/收束」重写并加章节表，大纲补「关键点」层级（落在各阶段 overview），学习路径图与阶段路径图由 Mermaid 改为 SVG（技能规定的固定 SVG 场景），评审改为教学法 5 维度 + 学习者 L1-L6 双视角。
- 2026-08-31：**环境方式确定**——用户以「好，继续」推进、未另行指定，主 agent 决策为**以 Docker 为主线、原生安装作备选**并写入 L3（理由：官方镜像成熟、起服务最快，且贴近生产容器化部署；同时保留原生路径覆盖非容器场景）。已同步更新画像与阶段 2 overview。
- 2026-08-31：阶段 2 课 3《环境搭建与第一次写入》交付（3 知识点）。双视角内联评审 P0=0（P1×1、P2×2 当批修复）。**本批最重要的产出是核实到两条此前未掌握的官方硬事实**：① Core 查询范围限制约 **72 小时**（近期与历史皆受限）② <10ms / ~30ms 的官方出处是 **LVC / DVC 两个需主动配置的内存缓存**。二者均已回补至 L2 相应位置（Core 表格加 72 小时约束、性能指标表改标 LVC/DVC 出处并补「需主动配置」警示）。
- 2026-08-31：阶段 2 课 4、课 5 交付（6 知识点），阶段 2 收官。L4 修正了「auto 精度会差几十年」的错误表述（经换算三阈值实为同一时刻 2128-06-11）；L5 突破无 Docker 限制，用 venv + `inspect` 拿到一手证据，新增「客户端默认写 V2 端点」「`influx3` 仅 `query` 一个子命令」「`host` 必须带 scheme」三条关键事实。
- 2026-08-31：阶段 3 课 9《InfluxQL 与 Flux：遗产与迁移》交付（3 知识点），**阶段 3 收官（12/12）**。双视角内联评审 P0=0（P1×1、P2×2 当批修复）。**本批最有价值的产出是一次对既有结论的精确化修正**：L8 的「3.x 没有 `NON_NEGATIVE_DIFFERENCE()`」严格限定在 **SQL** 语境——官方 InfluxQL feature support 表里它 **标 ✅ 受支持**。即 **SQL 没有，但 InfluxQL 兼容层有**，换端点即可用。教训：引用官方结论时必须带上语境限定词。
- 2026-08-31：阶段 4 课 10《存储引擎：WAL、Parquet 与压实》交付（3 知识点），**阶段 4 开篇**。双视角内联评审 P0=0（P1×2、P2×1 当批修复）。**本批最有价值的产出是把「Core 没有 compactor」从推测变成了三处官方证据**：①Core storage engine 页直接否定句；②Core 三个核心页面搜不到 compact 一词；③配置页 `gen1-duration` 把 compactor 明确归属 Enterprise。**并把它量化成了运维可感知的数字：90 天 ≈ 12,960 个 Parquet 文件，永不合并** —— 长周期查询慢的根因是「要打开的文件多」而非「数据量大」。教训（复用 L9 的）：**实跑输出必须逐字回贴且代码与输出严格对应** —— 本次对照 5 的 `google`/`azure` 两行因数据元组缺元素导致列错位，把两个持久化存储错打成「重启即丢」，正是靠逐字比对才发现的。
- 2026-08-31：阶段 3 课 8《SQL 查询：从 SELECT 到窗口函数》交付（3 知识点）。双视角内联评审 P0=0（P1×2、P2×2 当批修复）。**本批最有价值的产出是一个会长期潜伏的错误**：**UTC+8 用户跑 `DATE_BIN(INTERVAL '1 day', time)`，默认 origin 让「一天」从北京时间 08:00 开始**——查询不报错、数字不缺失，只是每天的聚合结果都错位 8 小时。修法：显式 `TIMESTAMP '1970-01-01T16:00:00Z'` 或改用 `date_bin_wallclock`。另两条关键：**`LAG(temp,1)` 是「前一行」不是「前一小时」**（数据缺失时两者分家，精确偏移须自连接）；**3.x 无 `increase()` / `NON_NEGATIVE_DIFFERENCE()`**（官方明文），迁移用 `GREATEST` + `LAG` + CTE 三段式。实验 A `DATE_BIN` 模拟器本机实跑（实验 1 输出 `18:18:18 → 18:15` 与官方例子逐字一致）。
- 2026-08-31：阶段 3 课 7《Schema 设计与基数陷阱》交付（3 知识点）。双视角内联评审 P0=0（P1×2、P2×1 当批修复）。**本批最有价值的产出是推翻了一条 1.x 老经验**：Core 官方 schema-design 页面**全文 0 次提及「索引」**（本机抓取正文核实，「索引」/`index` 各 0 次），Clustered 版则明文 *"It doesn't index tag values or field values"*——故「tag 有索引所以快」在 3.x 不成立，由此得出本课最实用的一条结论：**需要按某字段查询 ≠ 该字段必须是 tag**，ID 类（trace_id / user_id / UUID）一律进 field。另核实到**首次写入决定物理列顺序且不可更改**、**基数类元查询很可能不被 3.x 引擎支持**（故用 SQL `COUNT(DISTINCT ...)` 自数）。实验 A 基数估算器本机实跑（3,000 → 3 亿 series 对照，点密度 1000 → 0.01）。
- 2026-08-31：阶段 3 课 6《数据模型：table、tag、field、timestamp》交付（3 知识点，阶段 3 首课）。- 2026-08-31：阶段 3 课 6《数据模型：table、tag、field、timestamp》交付（3 知识点，阶段 3 首课）。双视角内联评审 P0=0（P1×1、P2×1 当批修复），新增 7 条事实核查。**本批最有价值产出**：①核实到 Core 硬限制 **库 5 / 表 2000 / 列 500**，直接推翻「按实例/按天/按客户分库」的直觉方案 ②实验 E 命名自检脚本经本机 Python 3.11 实跑，是编写环境无 Docker 情况下唯一能立刻动手的一项，且可接 CI 做写入前校验。

> ⚠️ **知识点口径修正（2026-09-02，Phase 4 汇总时发现）**：全书实际交付 **57 个知识点**（19 课 × 3），而大纲规划值是 **55**。差异来源：阶段 6《对比与决策》规划时写的是 **3 课 / 7 知识点**，但 L17 / L18 / L19 的知识点表各 3 条，实际 **9 个**（6+9+12+9+12+7=55 → 6+9+12+9+12+9=**57**）。**以实际交付的知识点表为准**，故全书各处「55」已统一改为「57」。**评审记录表里 2026-08-31 的三条历史条目保留原样** —— 那是当时的规划值，改了等于伪造历史。

## 断点信息

- 当前位置：**阶段 3 进行中（6 / 19 课，18 / 57 知识点）**，L1-L6 均已交付
- 当前位置：**阶段 4 已收官（12 / 19 课，36 / 57 知识点）**，L1-L12 均已交付
- 当前位置：**🎉 全书已完成（19 / 19 课，57 / 57 知识点）**，L1-L19 全部交付
- 下一步：**🎉 无 —— 全书已收官**。若要产出正式的《时序数据库选型与落地方案》，改 `stages/6-对比与决策/assets/l19_tco_calculator.py` 的 `WORKLOADS` 常量为真实负载后重跑即可（实验 C 压测校准待真实环境）。**三课方法论产出可复用**：L17 Q0 分类 + 六问三档 ／ L18「失败时有没有声音」排序法 ／ L19「约束归属」检查法
- ✅ **L19 已交付**（2026-09-02）：双视角内联评审 **P0=2、P1=3、P2=4（全部当批修复）**。**本课最重要的一条：约束是有归属的**——432 文件上限是 **Core 的属性**，不是 InfluxDB 的属性。实验 B 初版把它无条件套到全部五个 SKU，对 IoT 场景（60min 调度 × 365 天 = 8,760 文件）输出「最小调度周期需 ≥ 1216.7 分钟」（≈20 小时）的荒谬结论；修复后按 SKU 分列，结论变成「只有选 Core 才需要改周期」。**把 A 的约束套到 B 上，和把 A 的优点算到 B 头上，是同一类错误**，此为「约束归属」检查法。**第二：选型是两次决策，不是一次**——「我们选了 InfluxDB」没有携带信息，五个 SKU（Core / Enterprise / Serverless / Dedicated / Clustered）硬约束差异极大：**Core 只有 3 天可查窗口、5 库上限、无 HA、无合规认证**；**Serverless 无原生 v3 写 API、无 Processing Engine**（官方 which-influxdb-3 页明说），**它不是 Core 的托管版，两者能力集合不同**。实验 A 四个场景三个把 Core 挡在门外。**第三：打分器会输出冠军，但不会告诉你约束本身有问题**——新增**约束冲突检测**（步骤 1.5），区分「某 SKU 不满足约束」（排雷）与「约束之间打架」（冲突）。hybrid 与 IoT 两个场景触发：想托管 + 原生 v3 API + 处理引擎，在 InfluxDB 3 里没有便宜解。这类冲突只能回到需求侧改约束。**第四：成本排序会被授权费翻转**——TCO 三方案中 Enterprise 未含商业授权费（官方 Contact Sales 不公开报价，按 CPU 核 8/16/32/64/128 批次售卖），IoT 场景测算下运维人力（0.5 FTE = $7,500/月）是主机费（$240/月）的 **31 倍**。**此表价值在于告诉你该算哪些项，不在于给你答案**。**官方一手核实（⭐）**：五种 SKU 定位与选择建议；Serverless 四个单价（$0.0025/MB、$0.012/100 次、$0.002/GB-hour、$0.09/GB）与免费层额度；三种许可证规格（Trial 256核/30天/**不可商用**、At-Home **2核**/单节点/非商用、Commercial 按合同）；CPU 按集群不按机器；Core→Enterprise 无需搬数据但 **Downgrading is not supported**；Enterprise 支持 100+ 库 / 10k+ 表（⚠️ 官方博客口径，非文档页）。
- 🧪 **L19 实验实跑情况**：实验 A（选型决策树引擎，六步流程：层次差检查 → 约束冲突检测 → 排雷 → 打分 → 许可证建议 → 成本落地，**245 行输出**）与实验 B（TCO 与容量规划器，容量估算 + 降采样按 SKU 校验 + 三方案成本 + 落地清单生成，**282 行输出**）**均已在本机 Python 3.12 实跑**，真实输出已逐字回贴讲义。**四向逐字一致性校验通过**：讲义 python 块 ↔ 脚本文件（**607 / 515 行**）、讲义 text 块 ↔ 运行时 stdout（**245 / 282 行**）全部逐字一致。**官方文档冲突 3 条双面呈现**：①Enterprise 库表上限（官方博客 100+/10k+ vs 文档页未给数字 → 取博客下限并标 ⚠️）②Docker `latest` 变更日期官方自给多个（2026-02-03 / 2026-09-15 / 中文站 2026-04-07）→ **不引用日期，只采用行动建议** ③Serverless 是否有长程查询 → 保守按「有 compactor」处理并标 ⚠️ 量级判断
- ✅ **L18 已交付**（2026-09-02）：双视角内联评审 **P0=0、P1=5、P2=5（全部当批修复）**。**本课最重要的一条：迁移风险要按「失败时有没有声音」排，不是按「会失败的概率」排**——报错型问题最坏是延期，静默反转最坏是丢数据且无人知晓。**第二重要的一条：「静默」必须再分两类，后果差一个量级**——`reverse` = 方向反了（`0d` 想永久结果立刻全删；降采样库 432 超限变成「存得下但查不到」），灾难级；`drift` = 幅度偏了（`3mo` = 90 天 vs 三个自然月 91.3 天），合规级；**混为一谈会让合规问题挤占对灾难问题的注意力**。**第三：不可逆点不止一个，且版本升级的不可逆点比数据迁移靠前得多**——数据迁移七阶段 S1-S5 随时可退、S6 收窄、S7 关死，而 3.9.x → 3.10+ 在**第 ④ 步（启动 3.10）就已不可逆**，**拿迁移的节奏去套升级是本课最容易犯的结构性错误**。**第四：双写窗口可以算出来**——下限 = 覆盖对账周期（1-3 天），上限 = 可查窗口（Core = 3 天）→ **Core 上双写实际只有 1-3 天**，写太久最早的数据反而查不到，双写失去对照意义，故 S5 对账脚本必须提前写好。**官方一手核实（⭐）**：术语映射官方用词 `is synonymous with`（bucket=database、measurement=table）；InfluxQL DBRP 倒灌命名 `db/rp`；`precision=m/h` 仅 v1 端点支持且默认精度 v1=纳秒 vs v3=auto；导出必须 `-lponly`（否则整批失败）；Flux **无直接兼容层**（官方 *"regrettably cannot offer"*），出路 Explorer 1.9 converter（beta，AI 生成须复核）；插件系统是 CQ/Tasks/Kapacitor/Telegraf 的 natural successor；3.10 catalog v2→v3 单向迁移（备份路径按 3.4.0 分界，旧目录是 leftovers 非有效回滚源）。
- 🧪 **L18 实验实跑情况**：实验 A（三代差异清单检查器，17 条 × 三档 + 保留期专项 7 案例两类静默 + 翻译器自检，**266 行输出**）与实验 B（迁移演练与回滚规划器，七阶段可逆性矩阵 + catalog 备份路径决策 + 回填批次规划 + 磁盘估算，**212 行输出**）**均已在本机 Python 3.12 实跑**，真实输出已逐字回贴讲义。**四向逐字一致性校验通过**：讲义 python 块 ↔ 脚本文件（**500 / 471 行**）、讲义 text 块 ↔ 运行时 stdout（**266 / 212 行**）全部逐字一致。**官方文档冲突 3 条双面呈现**：①v3 精度写法全拼 vs 缩写 → 取全拼 ②Docker `latest` 变更日期官方自给多个 → **不引用日期，只采用行动建议（固定版本标签）** ③v2 端点是否支持 m/h → 以官方 Core 页为准
- ✅ **L17 已交付**：双视角内联评审 **P0=2、P1=1、P2=2（均当批修复）**。**本课最重要的一条：层次差——InfluxDB 是发动机，Prometheus 是整车**。InfluxDB 3 Core 是**存储引擎**（需 Telegraf 采集、Grafana 展示、处理引擎告警），Prometheus 是**采集+存储+告警+服务发现的完整方案**；拿前者存储比后者全套 = 不公平对照，这是选型文档最常见也最致命的错误。**第二：四个反直觉事实全部是官方自己说的**——①Prometheus 官方说拉模式只是 *"slightly better"* 且 *"should not be considered a major point"*；②Prometheus 官方说本地存储 *"not meant as durable long-term storage"*（不集群不复制，默认 15d）；③VictoriaMetrics 官网 20x/70x/7x 全是**厂商自述 + up to 上界**；④ClickHouse 官方说 *"Partitioning does not speed up queries"*（剪枝靠 ORDER BY 稀疏索引，不是分区）。**第三：没有一款赢下全部场景**——实验 A 四个场景四个不同冠军（VM / VM / TimescaleDB / ClickHouse）；且**先排雷再打分**比打分本身重要：场景 1 里 InfluxDB Core 不是「分数低」而是「**查不到**」（3 天可查窗口超限报错）。**第四（本课自己的两个翻车现场，均已修正）**：①实验 A 给 VM 压缩打满分，理由却写「厂商自述慎引」——**嘴上说慎引、手上打满分**；②实验 B 把「Core 只能查 3 天」当性能宣称审，判成「⚠️ 不能裸写」——**而它是全课最硬的排雷依据**。根因是**审计器缺 Q0 分类**，故 v2 新增三类分治（benchmark 性能宣称六问全审 / spec 官方规格只审来源与版本 / misread 理解错误一律判死）。**教训：方法论懂了不代表手上不会做错，所以要把自检写进脚本输出**
- 🧪 **L17 实验实跑情况**：实验 A（五款候选选型决策打分器，4 场景 × 6 维度，**先排雷再打分**）与实验 B（基准数字口径审计器 v2，**Q0 分类** + 六问三档，11 条宣称）**均已在本机 Python 3.11.15 实跑**，真实输出已逐字回贴讲义。**四向逐字一致性校验通过**：讲义 python 块 ↔ 脚本文件（459 / 391 行）、讲义 text 块 ↔ 运行时 stdout（153 / 163 行）全部逐字一致。实验 C（用真实数据跑自己的基准）因编写环境无 Docker 未实跑，标 ⏳。**⚠️ 实验 A 的打分是决策辅助不是基准测试**——它回答「以我的约束哪条路最省事」，不回答「谁性能更强」
- ✅ **L16 已交付**：双视角内联评审 **P0=2、P1=1、P2=2（均当批修复）**。**本课最重要的一条：自监控数据自己也只受 3 天可查窗口**（432÷144=3），且**文件数只跟墙钟有关、跟数据量无关**（与 L15 的 WAL 触发器一天 86,400 次同构）→ **「自监控留 90 天」= 存得下但查不到**，且**砍掉一半指标一个文件都不会少**。**第二重要的一条：「SQL 面板连不上、InfluxQL 面板正常」是排障第一分叉点**——这个非对称现象直接指向 HTTP/2（SQL 走 Flight SQL/gRPC 必须 HTTP/2，InfluxQL 走 HTTP/1.1 不受限），可跳过 token/端口的无效排查。**第三：`database_tag` 是 Core 上最危险的 Telegraf 选项**——目标库数量 = 该 tag 的去重值个数，撞 Core **5 库上限**，失败模式是**上线时正常、规模上来后突然炸**，且错误信息不提示「库超了」。**第四：`system_metrics` 官方插件采的是主机不是数据库**（名字里的 system 指 OS，依赖 psutil）。**第五：Grafana 刷新间隔是被忽略的查询放大器**——5s → 30s 查询量降到 1/6（⚠️ 引用官方「10s 面板 ≈ $31/月」必须对齐口径：那是单 query，讲义表是 5 query，差 5 倍）。**第六：`/ping` 必须用 GET，HEAD 返回 404**；`/health`、`/ping`、`/metrics` **默认都需要认证**（1.x/2.x 的老经验会误导）
- 🧪 **L16 实验实跑情况**：实验 A（Telegraf 配置体检器，14 条规则）与实验 B（自监控覆盖率与查询成本模拟器，4 组对照）**均已在本机 Python 3.11.15 实跑**，真实输出已逐字回贴讲义。**四向逐字一致性校验通过**：讲义 python 块 ↔ 脚本文件（356 / 245 行）、讲义 text 块 ↔ 运行时 stdout（102 / 161 行）全部逐字一致。实验 C（真机跑通 TIG 全链路）因编写环境无 Docker 未实跑，按讲义「四个核心验证点」核对即可。**⚠️ 实验 B 的 120 字节/行、800 条时序/次抓取为假设值**（沿用 L13/L14 中位口径）；**不依赖假设的部分**：432 文件上限、144 文件/天、3 天可查窗口
- 🧹 **L15 教训已固化**：L15 的 5 个 P0 根因是「源码块手工粘贴进讲义、未做一致性校验」→ L16 编制了 `_l16_verify.py` 做**四向逐字比对**（源码/输出 × 讲义/运行时），并把它列为交付前的强制步骤。**后续每课都应沿用**
- ✅ **L15 已交付**：双视角内联评审 **P0=5、P1=0、P2=0（P0 全部当批修复，且同源：递归写回的 off-by-one）**。**本课最重要的一条：处理引擎唯一的「静默自毁」路径**——WAL 插件写回被监听的表，20 秒破 5,000 万行/秒、21 秒破 1 亿，而日志里一个错都没有（数据库视角一切正常，插件在正确无误地执行一个错误的逻辑）。配套的两条硬事实：① **WAL 触发器一天 86,400 次**（绑 WAL flush 事件，与数据量无关），是 `every:1d` 的 8.6 万倍 → 触发器选型第一原则是「能慢就别快」；② **告警 = 检测器 + Notifier 两件套**，且**死信检查往往比阈值告警更重要**（采集链路断裂时阈值告警表现为「一片祥和」）
- 🧪 **L15 实验实跑情况**：实验 A（触发器频次与资源占用模拟器，5 组对照）与实验 B（插件代码静态检查器，六条铁律 CI 门禁）**均已在本机 Python 3.11.15 实跑**，真实输出已逐字回贴讲义，讲义内嵌源码块/输出块已与脚本文件、运行时输出做逐字一致性校验（A：源码 278 行 / 输出 149 行，B：源码 397 行 / 输出 69 行，全部一致）；实验 C（真机跑通插件与告警链路）因编写环境无 Docker 未实跑，按讲义「四个核心验证点」核对即可
- ✅ **L14 已交付**：双视角内联评审 **P0=1、P1=1、P2=2（均当批修复）**。**本课最重要的一条：降采样层「能不能查」取决于调度周期，不取决于降采样精度**——`gen1-duration` 按**时间**分桶，每次调度只落进 1 个桶 → **降采样层文件数 = 每天调度次数**（与写了多少行无关）；反推 90 天可查需 90×每天文件数 ≤ 432 → **调度周期 ≥ 5 小时，取 `every:6h`**。故**「分钟级 90 天」配 `every:1h` = 2,160 文件，照样超限报错，等于白降**。**第二重要的一条（P1 评审发现）：「秒级 7 天」这个经典数字来自有 compactor 的环境**——Core 上 144 文件/天 × 7 = **1,008 > 432**，即**原始层在 Core 上最多只能查 3 天，配 7 天是「留着」不是「能查」**；要三层全可查，Core 须把原始层压到 3 天，或上 Enterprise。**第三：降采样省的是行数不是字节**——聚合行 **128 B > 原始点 120 B**（多 4 个聚合值 + 3 个元数据列），总量能省 95.9% 是因为**保留期才是最大乘数**（三层方案 58.3 GB vs 裸存 1.4 TB，其中仅 7 天原始层就占 46%）。**成本侧最有价值产出**：**降采样的存储成本是前置的**——第 90 天合计 **33.2 GB，比同期裸存 27.0 GB 多 23%**，容量必须按**峰值**（原始层满 + 聚合层满）算而非稳态。**迁移侧最高危的一条**：**1.x/2.x 的 `0d` = 永久保留，Core 的 `0d` = 立刻全删**（静默语义反转）；且 `mo`=30 天 / `y`=365 天是**固定换算非日历**（`3mo` 比 3 个自然月少 1.3 天）→ 合规场景必须按天数写。**文档冲突两处**：data-retention 页称保留期 **immutable**，但 v3.2.0 release notes 已有 `update database --retention-period`（#26520）→ 判为**文档滞后于实现**，已双面呈现并标 ⏳ 需 `--help` 自查；`--delete-grace-period` 以 config-options 的 **24h** 为准（3.2 博客的 72h 已过时）
- ✅ **L13 已交付**：双视角内联评审 P0=0（P1×1、P2×2 当批修复）。**本课最重要的一条是把「要不要查历史数据」确立为选型第一判据**——Core 的 `query-file-limit` 默认 **432 文件 × `gen1-duration` 10m = 72 小时 ≈ 3 天**，**超限是直接报错不是变慢**（L11 已核实），故 **7 天 = 1,008 文件已超限 2.3 倍**；同时官方 which-influxdb-3 页开篇原文 *"For **new production workloads**, use **InfluxDB 3 Enterprise**"*，而 Core 的定位被写成 *"edge or **non-critical** workloads"* → **「先上 Core 免费版，扛不住再买 Enterprise」在官方口径里从起点就站不住**。**第二重要的一条是纠正了一个极易张冠李戴的判据**：**原生 v3 API 与处理引擎是 Core 与 Enterprise 同源、二者都有**的，该需求只用于**排除 Cloud Serverless**（官方原文 *"No native v3 write API"*、*"No Processing Engine"*），**不构成 Core → Enterprise 的升级理由**——选型模拟器初版就写错了这一条，导致场景 1/5 误判为 Enterprise。**第三：内存三个默认值相加 = 90%**（exec 20% + parquet cache 20% + force snapshot 50%），4 GB 机器只剩 **0.4 GB**，这是「内存比 CPU 先到瓶颈」的根因。**备份侧最有价值产出**：**升级 3.10+ 前必须备份 catalog**（v2→v3 单向不可逆，官方原文 *"Restoring these objects is the only way to roll back to 3.9.x"*），且**必须先确认版本再选路径**——3.4.0+ 备份 `{prefix}/catalog/v2/logs/` 与 `{prefix}/catalog/v2/snapshot`，而旧目录 `catalogs/` 可能是**残留且无效**的回滚源
- 🧪 **L14 实验实跑情况**：实验 A（分层保留与降采样成本模拟器，6 对照）与实验 B（Core 保留期硬边界检查器，5 场景 + 10 项建库清单）**均已在本机 Python 3.11.15 实跑**，真实输出已逐字回贴讲义；实验 C（实跑降采样与保留期）因编写环境无 Docker 未实跑，按讲义「三个验证重点」核对即可。**⚠️ 实验 A 的每点 120 字节、聚合行 128 字节、压缩比 25x 为假设值**（每点字节取 L12 已核实区间 60-150 中位，压缩比取自官方博客 *"often 10-100x smaller"* 区间内）；**对照 3 的「文件数 = 每天调度次数」是基于官方 internals 机制的推理，非官方明文**——脚本内已打印 ⚠️ 标记。**不依赖假设的部分**：432 文件上限、144 文件/天、保留期单位换算（mo=30 天 / y=365 天）、0=全删。**要抓的是数量级与结构关系（省 95.9%、调度周期 ≥6h、峰值 +23%），不是绝对 GB 数**
- 🧪 **L13 实验实跑情况**：实验 A（容量规划模拟器，6 组对照）与实验 B（部署形态选型决策树模拟器，6 场景 + 五条选型口诀）**均已在本机 Python 3.11.15 实跑**，真实输出已逐字回贴讲义；实验 C（`GET /ready` 与 catalog 备份路径）因编写环境无 Docker 未实跑，按讲义中「判断成功的标准」核对即可。**⚠️ 实验 A 的每点 120 字节、压缩比三档 10x/25x/50x 为假设值**（压缩比档位取自官方博客口径 *"often 10-100x smaller"* 区间内）——讲义实验 A 开头与结尾「诚实说明」双重标注；**要抓的是线性关系与量级锚点「10 万点/秒 × 30 天 ≈ 1 TB」，不是绝对值**。**例外：对照 3（保留期 × 144 = 文件数）与对照 4（内存三默认 90%）不依赖任何假设**
- ✅ **L12 已交付**：双视角内联评审 P0=0（P1×2、P2×3 当批修复）。**本课最重要的一条是把流传最广的调优建议「改掉 `SELECT *`」证伪在 Core 上成立不了**——官方 optimize-queries 原文：*"If the table contains **10 columns**, the difference... is **minimal**. In a table with over **1000 columns**, the `SELECT *` query is slower"*，而 **Core 每表上限 500 列**（L6 硬限制）→ **在 Core 上改它基本不会让查询变快**。**第二重要的一条是发现 10 MB 批量阈值与 `--max-http-request-size` 默认 10mb 恰好撞车**（官方未说明批量阈值是压缩前还是压缩后 → 保守策略：始终 gzip）。**第三：官方批量双阈值（10,000 行或 10MB）在四档典型行长下全部是行数先到**（10MB÷10000行 = 1048 字节/行），只有超宽表/超长 string field 才轮到体积阈值。**诊断侧最有价值产出**：**七步倒查法** + **「plan 耗时 > exec 耗时 = 文件数太多」这一指纹**（官方四类「不受控瓶颈」里三条根因都是文件数，改 SQL 无效）
- 🧪 **L12 实验实跑情况**：实验 A（批量写入权衡模拟器，5 组对照）与实验 B（慢查询诊断决策树模拟器，5 场景 + 7 步总结）**均已在本机 Python 3.11.15 实跑**，真实输出已逐字回贴讲义；实验 C（EXPLAIN 三兄弟）/ 实验 D（批量写入对比）因编写环境无 Docker 未实跑，按讲义中「判断成功的标准」核对即可。**⚠️ 实验 A 的固定开销 2.0ms、每行 0.002ms、gzip 压缩比 0.2 均为拍脑袋假设值**——讲义实验 A 开头、结尾「诚实说明」、档案事实核查表三重标注；**要抓的是趋势（边际递减发生在哪一档），不是绝对值**。例外：对照 4 的结论只依赖「10MB÷10000行=1048字节」这一硬算术，不依赖假设
- 🔗 **阶段 4 三课闭环**：L10（写入路径：文件永不合并 → 90 天 12,960 个）→ L11（查询路径：432 文件上限 → 7 天即报错）→ L12（调优：**不能做的是改变文件数**（架构天花板），**能做且必须做的是**写入侧批量化、查询侧写时间范围、last-value 建 LVC、慢查询按七步倒查）。终极解法一致：**Enterprise 的 compactor**
- ✅ **L11 已交付**：双视角内联评审 P0=0（P1×1、P2×1 当批修复）。**本课最重要的一条是把流传甚广的「72 小时限制」还原为真相：它是 432 个 Parquet 文件的限制**，代码里**没有任何时间判断**，72 小时只是 432 × 10min 的派生值——这解释了一个社区高频疑问（为什么有人查 13 天不报错？因为数据稀疏，没凑够 432 个文件）。**量化边界：7 天 = 1,008 个文件 > 432 → 直接报错，不是变慢**。且**调小 `gen1-duration` 只会更糟**（1m → 432 个文件只覆盖 7.2 小时，10m 已是文件最少档）。另两条关键：**列存省 I/O、SIMD 省 CPU，是两个独立加速，列存是 SIMD 的前提**；**<10ms 来自 LVC，需主动创建、InfluxQL 用不了、重启即清空且不自动回填、引擎不怕基数但 LVC 怕**
- 🧪 **L11 实验实跑情况**：实验 A（列存/向量化模拟器，4 对照）与实验 B（分区裁剪 / 432 限制模拟器，4 对照）**均已在本机 Python 3.11.15 实跑**，真实输出已逐字回贴讲义；实验 C（EXPLAIN）/ 实验 D（LVC 配置）因编写环境无 Docker 未实跑，按讲义中「判断成功的标准」核对即可。**⚠️ 实验 A 的 8.62 倍是 Python 层循环开销差异，非 SIMD 真实加速比**（讲义实验 A 开头与事实核查表双重标注）
- ⚡ **开课误解已闭环**：L11 讲清了 *vectorized execution* 的「向量」是 **CPU 寄存器里的一批值**（DataFusion 默认 8192 行/批），与向量数据库的 embedding 语义向量毫无关系。InfluxDB 无原生向量索引、做不了 ANN 检索
- ✅ **L10 已交付**：双视角内联评审 P0=0（P1×2、P2×1 当批修复）。**本课最重要的一条是 Core 确实没有 compactor**，且有三处官方证据：①Core storage engine 页直接否定句 *"InfluxDB 3 Core **does not include it**"*；②Core 的 durability / storage-engine / data-retention 三页**搜不到 compact 一词**；③配置页 `gen1-duration` 原文把 compactor 明确归属 Enterprise（*"generation 1 files that the **compactor in InfluxDB 3 Enterprise** can merge into larger generations"*）。**后果量化：每 10 分钟一个 Parquet 文件 → 90 天约 12,960 个 → 永不合并 → 长周期查询慢的根因是「要打开的文件多」而非「数据量大」**，官方 `query-file-limit` 的存在正是这个约束的体现。另两条关键：**`no_sync=false` 是默认值**（表示「WAL 落盘才 ACK」），改成 `true` 是**用持久性换延迟**；**持久化的是最老的数据，最新 5 分钟留在内存**
- 🧪 **L10 实验实跑情况**：实验 A 写入路径与持久性模拟器**已在本机 Python 3.11 实跑**（6 个对照场景真实输出已逐字回贴讲义，代码与输出严格一一对应）；实验 B/C 因编写环境无 Docker 未实跑，按讲义中「判断成功的标准」核对即可
- ✅ **L9 已交付**：双视角内联评审 P0=0（P1×1、P2×2 当批修复）。**本课最重要的一条是对 L8 结论的精确化修正**：L8 说「3.x 没有 `NON_NEGATIVE_DIFFERENCE()`」——**这句话严格限定在 SQL 语境**。官方 InfluxQL feature support 表里，`NON_NEGATIVE_DIFFERENCE()` 与 `NON_NEGATIVE_DERIVATIVE()` **都标 ✅ 受支持**（在 24 个全支持的变换函数内）。即 **SQL 没有，但 InfluxQL 兼容层有**。另两条关键：**3.x 只有 SQL 与 InfluxQL 两种查询语言，Flux 已被移出清单**（官方出路是 Flux to SQL converter beta）；**带 `tz()` 的按天 InfluxQL 查询迁 SQL 时不能直译**，会静默错位 8 小时
- 🧪 **L9 实验实跑情况**：实验 A InfluxQL↔SQL 语义对照模拟器**已在本机 Python 3.11 实跑**（4 个对照场景真实输出已逐字回贴讲义）；实验 B/C 因编写环境无 Docker 未实跑，按讲义中「判断成功的标准」核对即可
- ✅ **L8 已交付**：双视角内联评审 P0=0（P1×2、P2×2 当批修复）。**本课最该记住的一条**：**UTC+8 用户按天分桶，默认 origin 会让「一天」从北京时间 08:00 开始**——查询不报错，只是每天的数字都偏 8 小时（修法：`TIMESTAMP '1970-01-01T16:00:00Z'` 或 `date_bin_wallclock`）。第二重要的是 **`LAG` 是「前一行」不是「前一小时」**，数据缺失时两者分家，精确偏移须用自连接。第三：**3.x 没有 `increase()` / `NON_NEGATIVE_DIFFERENCE()`**，迁移时用 `GREATEST` + `LAG` + CTE 三段式替代
- 🧪 **L8 实验实跑情况**：实验 A `DATE_BIN` 分桶模拟器**已在本机 Python 3.11 实跑**（4 场景真实输出已逐字回贴讲义，实验 1 输出 `18:18:18 → 18:15` 与官方例子完全一致）；实验 B/C/D 因编写环境无 Docker 未实跑，按讲义中「判断成功的标准」核对即可
- ✅ **L7 已交付**：双视角内联评审 P0=0（P1×2、P2×1 当批修复）。**本课最反直觉的一条**：3.x Core 官方 schema-design 页面**全文 0 次提及「索引」**（本机抓取核实），Clustered 版则明文「不索引 tag 值或 field 值」——所以"需要按某字段查询 ≠ 该字段必须是 tag"，ID 类一律进 field。另一条硬约束：**首次写入决定物理列顺序，且不可更改**（建表时要按查询频率排 tag）
- 🧪 **L7 实验实跑情况**：实验 A 基数估算器**已在本机 Python 3.11 实跑**（四场景真实输出已逐字回贴讲义，可接 CI）；实验 B/C/D 因编写环境无 Docker 未实跑，按讲义中「判断成功的标准」核对即可
- ✅ **L6 已交付**：双视角内联评审 P0=0（P1×1、P2×1 当批修复），新增 7 条事实核查。两条最值得记住的硬事实：① **Core 硬限制 库 5 / 表 2000 / 列 500** → 分库策略不能按实例/按天/按客户划分 ② **保留键三类后果不同**（`time` 拒绝 / `_field`·`_measurement` 静默丢弃整个点 / `_` 与 `iox_` 前缀系统保留）
- 🧪 **L6 实验实跑情况**：实验 E 命名自检脚本**已在本机 Python 3.11 实跑验证**（不依赖 Docker，可接 CI 做写入前校验）；实验 A-D 因编写环境无 Docker 未实跑，按讲义中「判断成功的标准」核对即可
- ✅ **L5 备课待办已闭环**：原记「Flight SQL 客户端部分尚未专项核实」→ 已于 2026-08-31 完成，且**突破无 Docker 限制**（本机 venv 实装客户端 + `inspect` 核验），共订正/新增 5 条事实核查记录
- 🧹 **环境清理**：备课期间在工作目录建了临时虚拟环境 `.venv-influxdb-check`（仅用于 API 核验，非学员环境）。**可随时删除**，学员按课件实验 A 自建 `.venv-influxdb` 即可
- ⏳ **待真实环境验证项**：①L3 第四幕的查询输出格式 ②L5 实验 B 的 DataFrame 渲染样式、实验 E 的批量计数。二者均因编写环境**无 Docker**（`docker: command not found`）未能实跑，已改为「判断成功的标准」+ ⏳ 标注。用户实跑后可回写实际输出，届时把档案中对应条目置信度从「中」升为「高」
- 💡 **阶段 2 已完成**：三课上完，建议先做一次[阶段出口检查](stages/2-上手篇/lessons/lesson-05-Python客户端与CLI工具.md)（10 项清单，位于 L5 讲义末尾），确认手感建立后再进阶段 3
- 🔑 **本机环境备忘**（后续备课复用）：`python3.11.exe` 位于 `C:\Users\v_wypgwu\.local\bin\`，由 **uv 管理**，直接 `pip install` 会触发 **PEP 668 拦截** → 须先 `python3.11 -m venv <dir>` 再用 venv 内的 pip；PowerShell 下建议设 `$env:PYTHONIOENCODING="utf-8"` 避免中文输出乱码
- 续学规则：先读 `00-评审清单.md`，存在未勾选条目时先补对应评审，再继续下一批
