#!/bin/bash
# 核验：三条 P1 报警是脚本误判还是真缺陷
F='/mnt/d/projects/learning/victoriametrics/stages/3-凭什么快凭什么省/6-压缩为什么能省7倍空间.md'

echo "=== 核验 1：后台合并干扰磁盘增量法 —— 讲义到底写没写 ==="
echo "-- 常见误区第 1 条（标题就是它）--"
grep -n '^### 1\.' "$F"
echo
echo "-- 正文含『合并』的行数 --"
grep -c '合并' "$F"
echo
echo "-- 关键句核对 --"
grep -n '后台合并（课 5 讲过）会顺带' "$F"
grep -n '它是单调累计计数器' "$F"
echo
echo "结论：脚本用关键字『磁盘增量』匹配，但讲义用的是"
echo "      『用磁盘文件增量测压缩率，会被后台合并坑』—— 语义相同，措辞不同。"
echo "      → 脚本误判，非真缺陷。"

echo
echo "=== 核验 2 & 3：知识点 2/3 篇幅 —— 是脚本算错了吗 ==="
echo "-- 各章节起始行号 --"
grep -n '^### 知识点\|^## 第四幕\|^## 第五幕\|^## 🚀\|^## 🐞' "$F"
