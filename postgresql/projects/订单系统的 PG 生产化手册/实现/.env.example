# 仅用于本地教学演练，不要把真实凭据填入仓库文件。
# 复制为 .env 后可按需修改；Compose 自带同样的演示默认值。
POSTGRES_DB=order_service
POSTGRES_PASSWORD=pg_demo_only
REPLICATION_PASSWORD=replica_demo_only
APP_PASSWORD=app_demo_only
REPORT_PASSWORD=report_demo_only
TENANT_PASSWORD=tenant_demo_only
PRIMARY_PORT=15432
STANDBY_PORT=15434
