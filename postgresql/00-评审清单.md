# 00-评审清单 · PostgreSQL

> **未勾选条目 = 评审未完成**，是断点续学发现漏评审的依据。每批内容评审 P0 清零后勾选对应条目。
>
> **评审方式**：当前 `course-reviewer` 子 agent 未创建，所有评审节点走「主 agent 内联」（教学法视角 `pedagogy` + 学习者视角 `learner` 双视角，冲突由主 agent 裁决）。独立性受限已提醒。

---

## 1. 大纲与总览评审（Phase 1）

- [x] 大纲评审（教学法视角，P0=0 后放行）✅ 2026-09-06 主 agent 内联（双视角：pedagogy=采纳无修订；learner=采纳无修订；P1=2 已计入大纲调整记录）
- [x] 学习路径总览评审 ✅ 2026-09-15 · 与课程手册、02-课程目录一并核验；阶段路径、总路径、17 课 / 53 知识点口径一致，P0=0

## 2. 阶段 1 · SQL 与关系基础（9 知识点，3 课 = 3 批）

- [x] 课 1 认识 PostgreSQL — **首批 1.1 + 1.2** ✅ 2026-09-06 · P0=0 · 1 项 P1 已修订（latest 描述）
- [x] 课 1 认识 PostgreSQL — **批次二 1.3 数据库与模式层级** ✅ 2026-09-06 · P0=0 · search_path 已核 （`'\"$user\", public'`） · 课程导航块已补全
- [x] 课 2 表与数据类型 — **首批整课 2.1 + 2.2 + 2.3** ✅ 2026-09-06 · P0=0 · 2 项事实核查 PASS（text/varchar/char 性能一致；PG 17 推荐 IDENTITY）；课程导航块已升级为完整版（含上下阶段说明）；⚠️ 命令速查卡新增 NUMERIC / TIMESTAMPTZ / RETURNING / WHERE / TRUNCATE 等 21 条
- [x] 课 3 查询基础 — **首批整课 3.1 + 3.2 + 3.3**（阶段 1 收官批） ✅ 2026-09-06 · P0=0 · 3 项事实核查 PASS（STRING_AGG vs LISTAGG / 键集分页 + row-value 比较 / GEQO-12 + NULLS 默认顺序 + PG 严格 SELECT 模式）；课程导航块升级为阶段 1 闭环版（含阶段 6 条能力清单 + 阶段 2 4 课预告）；⚠️ 命令速查卡再扩 WHERE/ORDER BY/LIMIT/JOIN/聚合 28 条

> **整课 1 评审是否要新增条目**：评估后建议保留以上两条勾选（首批 + 批次二），**不新增第三条目**——本课 3 个知识点分布合理（首批 2 个 = 装库，批次二 1 个 = 库内层级），分两次评审更清晰。

## 3. 阶段 2 · 数据建模与 SQL 进阶（12 知识点，4 课 = 4 批）

- [x] 课 4 关系建模 — **首批整课 4.1 + 4.2 + 4.3**（阶段 2 开篇） ✅ 2026-09-06 · P0=0 · 4 项事实核查 PASS（外键引用侧不自动建索引 / ON DELETE 5 动作 / 3NF 与三类反范式 / ltree vs 递归 CTE）；课程导航块升级为阶段 2 开篇版；⚠️ 命令速查卡新增 FK 5 动作 / 引用侧索引 / 递归 CTE / ltree / JSONB 等 30 条
- [x] 课 5 视图与函数 — **首批整课 5.1 + 5.2 + 5.3** ✅ 2026-09-06 · P0=0 · 4 项事实核查 PASS（视图不可建索引 / CONCURRENTLY 4 条件 / NEW-OLD 可用性 / FUNCTION vs PROCEDURE）；**纠正骨架错误**（`pg_new_trigger`/`pg_old` → `NEW`/`OLD`）；课程导航块含"与课 4 衔接"回收；⚠️ 命令速查卡新增三视图 / plpgsql / 触发器 30 条
- [x] 课 6 CTE 与子查询 — **首批整课 6.1 + 6.2 + 6.3** ✅ 2026-09-07 · P0=0 · 3 项事实核查 PASS（PG 12+ CTE 内联规则 / 递归 CTE 终止与 CYCLE 子句 / 优化器 unnest 边界）；**复现 2 个高频生产事故**（`UNION ALL` 遇环不终止、`NOT IN` 遇 NULL 返回 0 行）；课程导航块含"与课 4 / 课 5 衔接"回收；⚠️ 命令速查卡新增 CTE / 递归 / 子查询 30 条
- [x] 课 7 窗口函数 — **首批整课 7.1 + 7.2 + 7.3**（**阶段 2 收官课**） ✅ 2026-09-07 · P0=0 · 5 项事实核查 PASS（PG 11 帧子句 / 排名跳号官方定义 / 聚合默认帧累计语义 / FILTER / DISTINCT ON vs ROW_NUMBER）；**PG 17.11 实测推翻"DISTINCT ON 一定更快"的过时结论**（7.33 ms vs 5.14 ms，PG 15+ Run Condition）；**复现 2 个报错**（`WHERE rn` 列不存在、`DISTINCT ON` 与 ORDER BY 不匹配）与 **1 个陷阱值**（`LAST_VALUE` 返回当前行）；课程导航块回收课 3/4/5/6 四处伏笔；⚠️ 命令速查卡新增窗口函数 30 条

## 4. 阶段 3 · 索引与查询优化（9 知识点，3 课 = 3 批）

- [x] 课 8 索引原理 — **首批整课 8.1 + 8.2 + 8.3**（**阶段 3 开篇**） ✅ 2026-09-07 · P0=0 · 6 项事实核查 PASS（B-Tree 结构与叶子占比 / 非聚簇 TID / Hash PG 10+ WAL 化 / GIN pending list 4MB / BRIN pages_per_range / 组合索引官方规则 + INCLUDE + 可见性地图）；**纠正骨架术语**（B+Tree → 官方 **B-Tree**，并说明与 MySQL 有无聚簇索引的关键差异）；**纠偏 2 条网传结论**（缺最左列≠失效，实测 170/171 页；Index Only Scan≠不回表，实测 Heap Fetches 0→200→0）；**9 组实验全部实跑**（含 `pageinspect` 看真实页结构、BRIN 875 倍、GIN 全文 1470 倍、写入放大 4.1 倍）；课程导航块回收课 1/3/4/7 四处伏笔；⚠️ 命令速查卡新增索引 30 条
- [x] 课 9 执行计划（知识点 9.1–9.3） ✅ 2026-09-07 —— P0=0（8 项事实核查 PASS；主 agent 内联双视角评审 pedagogy + learner 均无 P0）
- [x] 课 10 慢查询优化实战（知识点 10.1–10.3） ✅ 2026-09-08 —— P0=0（7 项事实核查 PASS；**勘误骨架 1 处**；主 agent 内联双视角评审 pedagogy + learner 均无 P0）

## 5. 阶段 4 · 事务/锁与并发（10 知识点，3 课 = 3 批）

- [x] 课 11 事务与隔离级别（知识点 11.1–11.3） ✅ 2026-09-08 —— P0=0（**22 项**事实核查 PASS；**勘误骨架 2 处**；主 agent 内联双视角评审 pedagogy + learner 均无 P0）
  - 🔬 **勘误 1**：骨架说「PG 只有三个隔离级别」不严谨——PG 接受 **4 个名字**、`SHOW transaction_isolation` 也**原样回显 4 个值**（含 `read uncommitted`），只是**行为**只有 3 种。实测 S10
  - 🔬 **勘误 2**：骨架按 SQL 标准暗示「RR 会有幻读」——**PG 的 RR 连幻读都防住了**（官方原文：PostgreSQL's Repeatable Read implementation does not allow phantom reads）
  - 🏆 **BOSS 实验**：写偏斜（医生值班）——RR 最终在岗 **0 人**（规则被破坏），SERIALIZABLE 拦下一个 → 在岗 **1 人**（规则保住）
  - 💸 **丢失更新实测**：两次扣 100，余额 1000 → **900**（应为 800），`CHECK (balance >= 0)` 完全拦不住
- [x] 课 12 MVCC 与并发控制（知识点 12.1–12.3） ✅ 2026-09-10 —— P0=0（**15 项**事实核查 PASS；**勘误骨架 3 处**；主 agent 内联双视角评审 pedagogy + learner 均无 P0）
  - 🔬 **勘误 1**：骨架知识点 12.1 写的 `txid_current_snapshot()` **已被官方标记 Deprecated**（PG 13 起改名 **`pg_current_snapshot()`**，见官方 Table 9.84）。实测两者返回值相同
  - 🔬 **勘误 2**：官方术语是 **dead row versions**（不是社区口语 dead tuples）；膨胀的官方用词是形容词 **bloated**（文档里没有名词 "bloat"）
  - 🔬 **勘误 3**：课 11 断点里建议核查的 `old_snapshot_threshold`，**在 PG 17 已被移除** —— 实测 `SHOW old_snapshot_threshold` → `ERROR: unrecognized configuration parameter`。替代是 `idle_in_transaction_session_timeout`
  - 🏆 **BOSS 实验：版本链** —— `id=1` 连改 4 次，`pageinspect` 看到链 `lp1→lp4→lp5→lp6→lp7`，**逻辑 3 行 / 物理 7 元组**
  - 📈 **膨胀实测链**：4328 kB → **18 MB**（192 B/行）→ `VACUUM` → **仍 18 MB**（还空间不还磁盘）→ `VACUUM FULL` → **6592 kB**（52 B/行，**3.7 倍**）
  - 🔥 **HOT 完美对照**：改非索引列 **100.0%**（5000/5000） vs 改索引列 **0.0%**（0/5000）
- [x] 课 13 锁机制与死锁（知识点 13.1–13.4） ✅ 2026-09-10 —— P0=0（**18 项**事实核查 PASS；**勘误骨架 4 处**；主 agent 内联双视角评审 pedagogy + learner 均无 P0）
  - 🔬 **勘误 1**：骨架说「行锁两层」（`FOR UPDATE` / `FOR SHARE`），官方是**四种**：`FOR UPDATE` > `FOR NO KEY UPDATE` > `FOR SHARE` > `FOR KEY SHARE`（官方 Table 13.3）
  - 🔬 **勘误 2**：骨架把 `pg_locks.tuple` 列为行锁排查手段 —— **基本走不通**。官方原文：*information about row-level locks is stored on disk, not in memory, and therefore row-level locks normally do not appear in this view*。实测 G-2：`pg_locks` 里 `granted` 全是 `t`，看不到「在等什么」
  - 🔬 **勘误 3**：骨架说「用脚本抓死锁日志」—— 官方**不推荐**自己 join `pg_locks`（*very difficult to get right in detail*），应改用 **`pg_blocking_pids()`**
  - 🔬 **勘误 4**：骨架漏了 **PG 17 新增的 `transaction_timeout`**（实测存在、默认 0），它补上了 `idle_in_transaction_session_timeout` 只管「等客户端」的缺口
  - 🏆 **BOSS 实验：报警风暴** —— 复现「纯 SELECT 被 `ACCESS EXCLUSIVE` 卡 3.0 秒」，并一条 SQL 查出真凶：`waiting_pid=3977, blocked_by={3970}, holder_state=idle in transaction, holder_xact_s=3.0`（实验 S）
  - 🧪 **最有教育意义的失败**：行锁 4×4 冲突矩阵第一版**测反了**（用 `NOWAIT` 报错文本判断冲突，结果与官方 Table 13.3 矛盾）—— 根因是 psql 的 ERROR 走 stderr、合并读取会延迟到下次 read。改用**阻塞观察法**后与官方完全吻合。此教训已写入正文
  - 📊 **实测取证的硬数字**：`lock_timeout=2s` 实测 **2003.119 ms** 触发；行锁等待 `wait_event=transactionid` 而非 `relation`；会话级咨询锁 ROLLBACK 后 `bigint_key=999` 仍 `granted=t`；长事务钉住 `backend_xmin=1362` 使 VACUUM 报 **`10000 are dead but not yet removable`**；`transaction_timeout=3s` 对 `pg_sleep(6)` **3.0s 准时切**
  - 📁 实验脚本与输出已归档 `stages/4-事务锁与并发/labs/`（含脚本↔实验编号对照 README），本课起建立「并发实验可复现」惯例

## 6. 阶段 5 · 运维与生产化（13 知识点，4 课 = 4 批）

- [x] 课 14 备份与恢复（知识点 14.1–14.3） ✅ 2026-09-10 —— P0=0（**16 项**事实核查 PASS；**勘误骨架 3 处**；主 agent 内联双视角评审 pedagogy + learner 均无 P0）
  - 🔬 **勘误 1**：骨架说「PG 12 把 `recovery.conf` 改成了 `postgresql.auto.conf`」—— 准确说法是**恢复配置项移进 `postgresql.conf`**（`postgresql.auto.conf` 只是 `ALTER SYSTEM` 的落盘处）；官方要点是 **`recovery.conf` 这个文件不再存在**（PG 12 release notes：*the server will not start if that file exists*）
  - 🔬 **勘误 2**：骨架把「tar 格式 vs plain 格式」并列成"哪个更省"的选择—— 实测 **两者一样大（均 158M）**，因为 **`-Ft` 根本不支持压缩**（没有 `-Z` 之类的选项）。真正会小的是 `-Fc` / `-Fd`（默认 gzip，实测 45M）
  - 🔬 **勘误 3**：骨架写「备份的三个一致性（数据/归档/启动）」—— 官方口径是**数据可以不必一致**（`pg_basebackup` 用 `full_page_writes` + WAL 让它可恢复），真正的三个约束是：**数据可不必一致 · WAL 必须一起收 · `backup_label` 必须在**
  - 🏆 **BOSS 实验：PITR 完整演练（E-1~E-12）** —— 拍备份 → 写 3 行 → 记 T1 并命名恢复点 → 再写第 4 行 → `DROP TABLE` → 切 WAL → 复制备份 → 写恢复配置 + `recovery.signal` → 起 **5434** 实例 → 验证：**恢复到 3 行（id=4 没回来，因为它在 T1 之后）**、`recovery.signal` 被自动删、timeline **1 → 2**、日志 `recovery stopping before commit of transaction 2507`
  - 💥 **最有价值的反面对照（E-12）**：把 `recovery_target_time` 设成早于备份的 `2020-01-01` —— **预期"启动失败"，实测不报错**，静默停在最早可达点并进入 **`pausing`**（因 `recovery_target_action` 默认 `pause`）。**恢复目标写错不会给你报错，只会给你一个错误的世界** —— 已升级为第二幕陷阱 5 + 第五幕记忆锚点 5
  - 📊 **实测取证的硬数字**：661 MB 库 → plain **158M** / custom **45M** / directory **45M** / tar **158M**；完整恢复 **4.118 s**（24 表 / 100 万行）；`pg_restore` 不加 `-e` 报 **113** 个错跑完、加 `-e` 报 **1** 个即停；`-t` 恢复撞 `ERROR: schema "finance" does not exist`；2.0 GB 数据目录 → `pg_basebackup -Ft -z` **447 MB / 41.5 s**；`backup_manifest` Version 2 / **2633** 文件 / CRC32C；`archive_mode` 重启后 `archived_count=1, failed_count=0`
  - 📁 实验脚本与输出已归档 `stages/5-运维与生产化/labs/`（3 脚本 + 15 份原始输出 + README，含脚本↔实验编号对照表）
  - ⚠️ **本批附带修复**：发现 `00-学习档案.md` 进度表中**课 13 行漏改**（评审记录已写但进度表仍是 `⬜ 未开始`），已**补录**并标注
- [x] 课 15 复制与高可用（知识点 15.1–15.3） ✅ 2026-09-11 —— P0=0（**16 项**事实核查 PASS；主 agent 内联双视角评审 pedagogy + learner 均无 P0）
  - 🔬 **实测推翻预设 1**：`synchronous_standby_names` **不能会话级 SET**（实测 `ERROR: parameter "synchronous_standby_names" cannot be changed now`）——只能 `ALTER SYSTEM` + `pg_reload_conf()`；`synchronous_commit` 才是会话级可切。正文 B-2′ 与速查卡均已标注
  - 🔬 **实测推翻预设 2（本课最贵）**：同步提交挂起是**全局的**（停从库后其他会话 UPDATE 同样 4.0 s 卡死）；且客户端被杀**撤不回** in-doubt 事务——从库恢复后 id=301 最终落地（复插同主键报 `duplicate key`，实验 B-4）
  - 🏆 **BOSS 实验 1（同步挂起）**：停从库 → 同步 INSERT **6.67 s 不返回**（timeout 强杀、未提交）→ 从库拉起自动解除
  - 🏆 **BOSS 实验 2（双主分叉）**：promote 后旧主不隔离 → 旧主写的行新主 **0 条**看到、新主写的行旧主 **0 条**看到（D-3）——"HA 必须有 fencing"的最有力实证
  - 📊 **硬数字**：`pg_basebackup -R` 2.27 GB **14 秒**建从库；同步四档 local 2.372 / **on 5.177** / remote_write 1.798 / remote_apply 2.016 ms；三 lag（write 0.731 / flush 1.029 / replay 1.124 ms）；无槽 checkpoint 回收 **14 段**（52→38）vs 有槽 **0 段**；promote 后 timeline **1→2**、`standby.signal` 自动删除、主库槽转 inactive
  - 🕳️ **第一手环境坑（已进正文误区 6 + labs README）**：容器间 Docker 网桥直连报 `Cannot assign requested address`（非 refused）；经宿主映射的复制连接源 IP 是 `169.254.169.254`（Docker VM link-local）；级联从库继承上游 `primary_slot_name`（槽在上游不存在）→ walreceiver 静默反复重试
  - 🔗 **锚点勘误（5 处）**：PG 文档改版后 `warm-standby.html` 小节锚点不再带 `WARM-STANDBY-` 前缀（如 `#STREAMING-REPLICATION` / `#STREAMING-REPLICATION-SLOTS` / `#CASCADING-REPLICATION`）；26.3 Failover 在 PG 17 拆为独立页 `warm-standby-failover.html`（实测 200）——初稿 5 处锚点已全部修正
  - 📁 实验脚本与输出已归档 `stages/5-运维与生产化/labs/`（`l15_labs.sh` + `out_l15_full.md`，README 已扩展课 15 对照表与坑清单）
- [x] 课 16 监控与性能（知识点 16.1–16.3） ✅ 2026-09-11 —— P0=0（**13 项**事实核查 PASS；**实测推翻预设 3 处**；**PG 17 勘误 1 处**；主 agent 内联双视角评审 pedagogy + learner 均无 P0，2 处 P1 已修订）
  - 🏆 **实测反差 1（阈值教条破除）**：课程库真实命中率 **98.45%** 低于「99% 经验线」但无任何故障——阈值是参考不是规范
  - 🏆 **实测反差 2（context 悖论）**：pg_stat_io 同库同时刻 **normal 99.9% vs bulkread 13.6%**——大表扫描走环形缓冲，命中低是设计不是故障
  - 🏆 **实测反差 3（排行榜≠判决）**：pg_stat_statements TOP1 `UPDATE accounts` mean **34.9s** 是课 13 等锁实验的痕迹——等锁时间计入 total/mean_exec_time
  - 🔬 **PG 17 勘误（骨架未料）**：`pg_stat_bgwriter` 拆成 checkpointer/bgwriter/wal 三视图且**列名全改**（实测 `checkpoints_timed` 报 column not exist → 新名 `num_timed`）——旧版监控 SQL/exporter 配置在 17 上静默失效
  - 🔬 **实测推翻预设**：auto_explain **可会话级 `LOAD 'auto_explain';` 无需重启**（官方 F.3）；统计延迟实测 INSERT 后查 0 → force_next_flush → 1000
  - 🕳️ **从库冲突分流实测（回收课 15 伏笔）**：同一句报错 `conflict with recovery` 两种 DETAIL——DROP 表 → **confl_lock**（holding a relation lock）/ DELETE+VACUUM → **confl_snapshot**（row versions that must be removed），计数器列各 +1（标本保留在从库）
  - 📊 **硬数字**：800 万行 VACUUM 进度条 5 采样（19%→96%）；auto_explain 抓到并行计划（Workers Launched: 2）；WAL 累计 3.7 GB / checkpoint num_timed=604
  - 📁 实验脚本与输出已归档 `stages/5-运维与生产化/labs/`（l16_labs.sh + out_l16_full.md，README 已扩展）
- [x] 课 17 扩展与安全（知识点 17.1–17.4） ✅ 2026-09-11 —— P0=0（**12 项**事实核查 PASS；主 agent 内联双视角评审 pedagogy + learner 均无 P0）**——阶段 5 收官 · 全课程 17 课 53 知识点闭环**
  - 🔬 **备课误记被核查推翻（本课最重要）**：「PG 17 的 pg_upgrade 保留 statistics」是**错的**——官方原文 *statistics are not transferred by pg_upgrade…*；PG 17 实际保留**逻辑复制槽与订阅状态**。已做成正文陷阱 5
  - 🏆 **RLS 四象限全实测**：app 隔离 ✓ / WITH CHECK 拦截（`new row violates row-level security policy`）/ owner 默认绕过 4 行 → FORCE 后 0 行 / superuser 永远穿透（FORCE 无效）
  - 🏆 **trusted 双对照**：普通用户装 pg_trgm/citext ✓、装 pg_freespacemap 报 `permission denied to create extension`（HINT: Must be superuser）
  - 📊 **连接账单**：50 空闲连接净增 **787 MB（≈15.7 MB/连接）**，断开精确回落；`max_connections` 直接挂钩共享内存（官方原文）
  - 🕳️ **实测坑 4 处进正文**：schema USAGE 是第一道门 / RLS 租户值须与角色名逐字相等 / FORCE 管不住 superuser / `ALTER EXTENSION … OWNER TO` 不存在（用 REASSIGN OWNED）
  - 📁 实验输出归档 `labs/out_l17_full.md`；实验后角色与表全部清理，pg_trgm 资产 REASSIGN 回 postgres

## 7. 收尾节点（课程制默认必做）

- [x] **综合实战项目**（Phase 3，跨阶段整合 + 设计决策 + 反例对照 + 验收清单）✅ 2026-09-15；专项 A1–A6 与 pedagogy/learner 双视角 P0=0
- [x] **实战经验 + 排障速查手册 + 场景解法库**（Phase 5，三份一次生成）✅ 2026-09-15 · B1–B8 / C1–C7 P0=0
- [x] **课程手册汇总**（Phase 4，含实战项目收录）✅ 2026-09-15 · P0=0 · 覆盖 17 课 / 53 知识点 / 结课项目 / Phase 5 三件套

## 8. 可选节点（用户要求时触发）

- [ ] 知识点对齐（Phase 6，选择题自测）

---

## 进度统计

| 节点 | 必做数 | 已勾选 |
|---|---|---|
| 大纲评审 | 1 | **1** ✅ |
| 阶段 1 批次 | 4 | **4** ✅（课 1 首批 + 批次二 + 课 2 首批 + 课 3 收官批）—— **阶段 1 全闭环** |
| 阶段 2 批次 | 4 | **4** ✅（课 4 + 课 5 + 课 6 + 课 7）—— **阶段 2 全闭环** |
| 阶段 3 批次 | 3 | **3** ✅（课 8 + 课 9 + 课 10，**阶段 3 闭环**） |
| 阶段 4 批次 | 3 | **3** ✅（课 11 + 课 12 + 课 13）—— **阶段 4 全闭环** |
| 阶段 5 批次 | 4 | **4** ✅（课 14–17）—— **阶段 5 全闭环** |
| 收尾节点 | 3 | **3** ✅ |
| **总计** | **22** | **22** |

### 结课综合实战项目评审结论（2026-09-15）

- [x] A1 跨阶段映射：README 映射实际 53 / 53 个知识点，并回链到 17 课正文。
- [x] A2 非功能约束：业务正确性、可恢复性、可观测性、安全边界、可复现性，超过 2 项。
- [x] A3 设计决策：备份组合、异步/同步/自动 HA、原子扣库存、RLS、升级路径，超过 2 个真权衡。
- [x] A4 多文件工程：Compose、初始化 SQL/shell、备份恢复脚本、监控/报表 SQL、SVG 与四份文档。
- [x] A5 反例对照：9 条看似合理的反例，逐条给出风险、不变量和项目做法。
- [x] A6 可运行性：版本、端口、依赖、启动/清理命令齐备；Compose、脚本和 SQL 已实测。
- [x] 教学视角：任务→冲突→机制→证据→决策→验收闭环；复杂拓扑使用浅底 SVG；明确非目标避免范围惊喜。
- [x] 学习者视角：快速开始、失败边界、按症状验收、反例对照和口头答辩问题齐备。
- [x] 事实核查：沿用 PostgreSQL 17 官方 web-index 与本机 PostgreSQL 17.11 实测；P0=0。

### Phase 5 收尾三件套评审结论（2026-09-15）

- [x] B1–B3 来源与真实性：10 个故障模式均回链课 14–17、结课项目或官方文档；项目数字只引用本机实测；未发现虚构凭据、私网地址或未经标注的生产数据。
- [x] B4–B6 实战与互补：08 每项均有症状 / 根因 / 诊断 / 修复 / 预防；09 有症状索引、一眼识别、先止血、分支动作和升级出口；10 专注设计权衡，三份职责不重叠。
- [x] B7–B8 角色与案例：覆盖应用、数据库、业务、安全和变更负责人边界；案例来自结课项目和课程实验，演示环境与生产凭据明确脱钩。
- [x] C1–C4 解法质量：8 个场景各有 3 条候选路线，共 24 条；均写适用边界、代价、推荐进阶，并以 details 折叠实现“先想后看”，每条路线回链相关课件。
- [x] C5–C7 场景真实性：每场景均有不适用提醒、反例和验收出口，覆盖备份/PITR、HA、并发、RLS、读扩展、性能、升级和连接治理等真实生产决策。
- [x] 教学视角：从故障事实收束到可执行动作，再上升到多方案决策；同一 F01–F10 素材贯穿三份文档，避免跨文档口径漂移。
- [x] 学习者视角：先给入口和安全边界，再给分支和解释；场景文档保留思考停顿与渐进推荐。该结论为主 agent 角色扮演推演，不等同真实用户测试。
- [x] 静态验收：107 条新文档内部链接 0 断链；F/S 10 对 10 映射；8 组 details、三份代码围栏均配对；P0=0。

### Phase 4 课程手册评审结论（2026-09-15）

- [x] 内容覆盖：手册覆盖 5 阶段 / 17 课 / 53 知识点；每课均有三句话记忆、决策动作、常见误区和 PostgreSQL 17 官方入口。
- [x] 课程导航：学习路径 SVG、阶段总览、02-课程目录、17 课正文、结课项目与 Phase 5 三件套之间的相对链接均可达，内部链接 0 断链。
- [x] 教学视角：主线保持“场景引入 → 认知冲突 → 机制揭示 → 实操证据 → 设计决策 → 验收收束”；手册承担汇总与导航，不重复粘贴每课全文。
- [x] 学习者视角：入口先说明阅读方式，再按阶段降低密度；项目实测数字、五张决策表和 09 / 10 的工作态入口让“学完后怎么用”可落地。该结论为主 agent 角色扮演推演，不等同真实用户测试。
- [x] 版本与证据：命令口径统一 PostgreSQL 17.x；项目实测口径为 PostgreSQL 17.11 / macOS arm64 + Docker；未发现虚构凭据、敏感信息或未标注的生产数据。
- [x] 结构卫生：代码围栏成对、Markdown 相对链接可解析、目录条目已转为链接；`course-reviewer` 子 agent 尚未创建，独立性受限，主 agent 已完成 pedagogy + learner 双视角复核。
- [x] 结论：**P0=0，P1=0，Phase 4 放行**。
