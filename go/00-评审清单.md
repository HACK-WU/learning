# Go 语言课程评审清单

> **强制检查点**：每批内容生成后，先在本文件找到对应未勾选条目，完成双视角评审（pedagogy + learner）且 **P0 清零**后，才能勾选并交付。
> **未勾选 = 评审未完成，禁止进入下一批。**
>
> 与 `00-学习档案.md` 的分工：本清单管**待办勾选**，学习档案管**评审结论摘要**（历史档案）。

## 待评审清单

- [x] 大纲评审（Phase 1）— 主 agent 内联 pedagogy 视角，P0=0 通过（2026-09-06）
- [x] 阶段 1 · 课 1《Go 是什么、环境怎么跑起来》 — pedagogy + learner 双视角，P0=0 / P1=1 / P2=3 全部当批修复（2026-09-06）。实测 6 组数据全部贴入正文（二进制 2,429,714 → -s -w 1,587,714，−34.6%；交叉编译 linux/amd64 2,346,817 且 `statically linked`；`-s -w` 不影响 panic 堆栈行号；macOS 即使 CGO_ENABLED=0 仍链 libSystem；未使用变量/导入报错原文；编译冷 1.70s / 热 0.09s）。新增配图 `stages/1-语言地基/assets/compile-model-and-workspace.svg`（900×660）
- [x] 阶段 1 · 课 2《变量、类型与控制流》 — pedagogy + learner 双视角，P0=0 / P1=1 / P2=2 全部当批修复（2026-09-06）
- [x] 阶段 1 · 课 3《数组、切片与 map》 — pedagogy + learner 双视角，P0=0 / P1=1 / P2=2 全部当批修复（2026-09-06）。**阶段 1 闭环（9/45）**
- [x] 阶段 2 · 课 4《函数与错误处理》 — pedagogy + learner 双视角，P0=0 / P1=2 / P2=2 全部当批修复（2026-09-06）。**P1**：①LIFO 说明与自己的演示程序自相矛盾（实测执行顺序与文字描述相反）→ 已重写；②知识点 2 引用的 3 处实测输出在第四幕无对应步骤 → 已补 `errors.go` 第 1–3 段输出。**P2**：①defer 参数立即求值的代码片段与真实跑过的程序不是同一份 → 已统一为逐字输出；②`os.Exit` 段原写「退出码: 1」非真实打印（`go run` 打印 `exit status 1`）→ 已改逐字。3 处示例演示**全部重新逐字跑过验证**
- [x] 阶段 2 · 课 5《结构体与方法》 — pedagogy + learner 双视角，P0=0 / P1=1 / P2=2 全部当批修复（2026-09-07）。**P1**：**写作前差点教错一条已作废的规则** —— 原计划讲「提升字段不能写在复合字面量里」，实测 go1.27.1 能编译通过，追查确认 **Go 1.27 新增 Struct literal field selectors**（官方 Release Notes + spec 已删旧例外句）→ 改写为新语法 + 三条边界 + 版本提示，旧版本报错标 ⏳ 置信度：中。**P2**：①两处断言未实测（嵌入字段加 tag 变嵌套 / 小测 Q1 输出）→ 补跑并补入正文；②**网上两篇 Go 1.27 文章的说法在本机不成立**（指针嵌入自动 new / 歧义报 ambiguous field）→ 加 ⚠️「以实测为准」。3 处示例演示**全部逐字跑过验证**
- [x] 阶段 2 · 课 6《接口与泛型》 — pedagogy + learner 双视角，P0=0 / P1=0 / P2=2 全部当批修复（2026-09-07）。**阶段 2 闭环（18/45）**。P2：①**猜错一条报错原文**（把 `type parameter T cannot use operator >` 误写成 "is not comparable with >"）→ 按实测订正；②type switch 输出格式把切片内容塞进宽度 + `len(v)` 写漏 → 改用 `%-14T` 并修正。3 处示例演示**全部逐字跑过验证**，小测 Q1 另跑程序验证
- [x] 阶段 3 · 课 7《goroutine：廉价的并发单位》 — pedagogy + learner 双视角，P0=0 / P1=4 / P2=3 全部当批修复（2026-09-08）。**P1×4**：①`threadcreate.Count()` 被误读为"累计创建线程数" → 查源码 `runtime/mprof.go` 确认是**遍历 allm 的当前存活 M 数**，标注与探针全部改正重跑；②循环变量演示的代码片段与实测输出**不是同一份程序** → 补全 main.go + 两份 go.mod 并重跑（`[a b c]` / `[c c c]`）；③实操 3.1/3.2 的 CPU 基准无完整源码、输出不可复现 → 补折叠源码块；④骨架知识点 3 写「GOMAXPROCS 控制可同时执行的 goroutine 数」**是流传很广的错误说法** → 按官方 runtime 原文改为「限制可同时执行用户级 Go 代码的 OS 线程数」，并写入误区表 + 小测陷阱题。**P2×3**：①"channel 收发几十到上百纳秒"等无实测的对照数字 → 删除，改由实测 805ns/个 换算；②poka-yoke 误归为官方说法 → 改归社区；③演示 1-D 循环次数会波动 → 改用最新实测值并加注量级。**12 个实测程序全部逐字跑过验证**
- [x] 阶段 3 · 课 8《channel：用通信共享内存》 — pedagogy + learner 双视角，P0=0 / P1=3 / P2=2 全部当批修复（2026-09-10）。**P1×3**：①**小测第 1 题的 console 块是凭印象写的，与实测方向相反**（原写 `主：收到 数据` 在前，实测 `A：发送完成` 在前，3 次稳定）→ 新建 `verify/q1_quiz.go` 原样跑过并替换；**顺带挖出更好的教学点**：`GOMAXPROCS=1` 时 `A：发送完成` **压根不打印**（main 返回即全灭，现场复现课 7 知识点 2），已写入答案；②**「缓冲开多大」没给阈值**，违反本仓库"比较结论必须给临界点/区间"的硬纪律 → 补跑容量基准得出**甜点区 16~128**（相对无缓冲快 3.6~4.3 倍，cap 1024 反比 128 慢 9%），写入核心原理 / 速查卡 / 误区表（新增 21、22 条）/ 第五幕收束四处；③演示 1-A 只断言"后两行顺序不保证"却无证据 → 连跑 5 次，第 2 次即翻转，实测次数写入正文。**P2×2**：①随机命中数 / Timer 分配数 / 耗时每次运行都波动，读者复跑对不上 → 3 处补「复跑波动」注（附第二次真实输出作样本）；②Timer 复用版复跑出现 `存活对象=-5` 负值（GC 干扰）不宜进讲义 → 保留首轮值并明确"看量级不看精确值"。**20 个实测程序 + 1 组基准全部逐字跑过验证**
- [x] 阶段 3 · 课 9《同步、竞争与泄漏》 — pedagogy + learner 双视角，P0=0 / P1=3 / P2=2 全部当批修复（2026-09-10）。**阶段 3 闭环（27/45）**。**P1×1（方法论级）**：验证「Go 1.22 循环变量按 go.mod 门控」时**用文件模式 `go run x.go` 跑出错误结论**——同一份代码在 `go 1.21` 模块里也得到新语义，差点推翻课 7 已落盘的 `[c c c]`；追查确认文件模式下文件属 `command-line-arguments` 伪包、**拿不到 go.mod 语言版本** → 全部探针改用**包模式**重建才正确给出 `[  c]`。已写进正文警示段 + 回写学习档案 + 补挂 topic-teach 实测证据闸门。**P1×2**：「读多写少用 RWMutex」无阈值 → 补跑读写比扫描基准（8×80 万次，固定工作量 + 校验和）得出**甜点区 5%~25% 写（快 4.9~7.6 倍）**，并发现两个反直觉端点（纯读仅 1.21×、全写仅 1.26×），另补"临界区有真实工作量"对照（1.1~1.9×）。**P1×3**：只断言"锁不能复制"却没证明 `go test` 为何抓不到 → 补对照实验（同包放 printf 问题立刻 `build failed`，copylocks 静默通过）。**P2×2**：波动数字 3 处补注；`sync.Map` 表述按官方两场景改写。**21 组实测 + 3 组基准全部逐字跑过验证**
- [x] 阶段 4 · 课 10《io 与 context》 — pedagogy + learner 双视角，P0=0 / P1=3 / P2=2 全部当批修复（2026-09-10）。**阶段 4 开篇（30/45）**。**P1×1（技术断言不实）**：撞墙 2 的并列清单里混进了 **`database/sql`**，还挂着"实测全部正常工作"——`database/sql` 根本不接受 `io.Reader`，这一项**从未跑过**，是"为了排比好看"多写的一个词 → 换成本轮真跑过的 `csv.Reader`。**P1×2**：embed 体积倍率 `1.008:1` 是上一批的旧数、**不在本轮证据文件里** → 补做 A/B 对照实测 **2467858 → 4597922，增量 2130064 ≈ 1.0157 倍**，全文 5 处统一改正。**P1×3**：`ctx.Value(userKey{{}})` 被解释成"`fmt` 打印空 struct 的结果"，**翻源码发现那是格式串里写死的转义** → 改写为准确解释。**P2×2**：探针计数与证据文件行数（18+2+1 个包 / 767 行）与实际不符 → 已订正。**证据链机械化核对（本课新增做法，建议沿用）**：写脚本把正文 **76 个 `console` 块共 725 行**逐行与 `ALL_OUTPUT.txt` 归一化比对，**真实不符 0 处**；剩余差异全部归入可解释的三类（`>` 引用块前缀 8 行 / `...` 省略 11 行 / 我的旁注 1 行）—— 脚本当场抓出了上面那处 P1×2。**准确性**：`io.Reader` 的 *"process the n > 0 bytes returned before considering the error err"*、`io` 包 *"should not assume they are safe for parallel execution"*、`context` 四条约定原文、`Cause` 规范原文、`bufio.Scanner.Buffer` 的 *"larger of max and cap(buf)"*、`ioutil` 的 Deprecated 原文均从 `$GOROOT` 源码抓取并标 `（核查于 2026-09）`；版本归属**全部查 `api/*.txt` 而非凭记忆**（io=1.0 / MaxScanTokenSize=1.1 / context=1.7 / embed+ioutil废弃=1.16 / Cause=1.20 #51365 / WithTimeoutCause=1.21 #56661 / `(*os.File).WriteTo`=1.22 #58808）；`context` 起源（Google 内部包 → 2014-07-29 官方博客公开 → Go 1.7 进标准库）联网核实。**18 组实测 + 2 组静态验证 + 1 组基准**：一个 Read 接 6 种消费方、`Read` 契约陷阱（4096 buffer 时 **27 字节全丢**）、`io.Copy` 快车道（目标 Write 被调用 **0** 次）+ 包装器堵死、返回值七条（不关任何一端 / 剩 98304 字节未读 / `empty buffer` panic）、缓冲基准 8 档两轮（512 B 慢 1.8×、**4 KiB 后平坦**）、128 MB 六种读法（**128.01 vs 0.0041 MB，差 4071 倍**）、四层流水线（gzip 后加 bufio **35→35**；明文加 64 KiB bufio **134→10**）、取消树七项、超时截断（父 200 ms 子 5 s → 实测 **200 ms**）、`go vet` 三条 + `build` 退出 0、`WithValue` 深度扫描（500 层 **1.095 µs** = 深度 1 的 365 倍）+ 字符串 key 跨包覆盖、`Cause` 八项、`ctx.Done()` 收口课 9 泄漏（201→401→**201**）、fd 三写法（+200/+200/+0）、`defer` LIFO 与**函数级非块级**、权限 umask（0777→0755）、`Glob` 花括号返空、五种关闭/错误行为（含 `nil *os.File` **不 panic**）、`embed` 三重证据。**跨平台项全部标 ⏳ 未实测**（Windows 路径分隔符、`defer` 先删后关）。
- [x] 阶段 4 · 课 11《net/http 服务端》 — pedagogy + learner 双视角，P0=0 / P1=6 / P2=4 全部当批修复（2026-09-10）。**阶段 4 进行中（33/45）**。**P1×1（事实性错误）**：§2.9 原写「`encoding/json/v2` 是普通包、**不需要 `GOEXPERIMENT`**」，依据之一是"在 `internal/buildcfg/` 与 `goexperiment/` 里 grep 不到 `jsonv2` 条目"——**grep 错了目录**。开关在 `$GOROOT/src/internal/goexperiment/exp_jsonv2_on.go` / `_off.go`。真相：**`jsonv2` 是 GOEXPERIMENT，但在 go1.27.1 默认基线里是「开」的**；`GOEXPERIMENT=none go list encoding/json/v2` → **`build constraints exclude all Go files`**；默认下 `go list -f '{{.GoFiles}}' encoding/json` 列出 **`v2_*.go` 七个文件**（**v1 由 v2 实现**），关掉后换成 **`decode.go` / `encode.go` / `stream.go` 等八个老实现**（标 `//go:build !goexperiment.jsonv2`）。→ 三处改正 + 新增证据链 + **提炼出通用判定手段（判实验开关用 `GOEXPERIMENT=none go list <pkg>`，不要 grep 目录）**，并把这次翻车写进正文与误区表（新增 3 条）。**P1×2（小测答案错误）**：小测第 2 题（5 条路由注册哪些 panic）**我凭推理给的答案两条都错**（原判「② 不冲突、⑤ 因路径相同而冲突」）→ 新建 `p2c_conflict` 探针跑 **12 组边界**定案，**实测 ② ⑤ 都 panic，且都不是彼此相邻的两条打架，而是都撞在最早的 `GET /` 上**；顺带提炼出最有工程价值的一条：**`"GET /"` 与任何"不带方法"的模式都冲突、且两个注册方向都 panic → 别混用带方法/不带方法**。判据四句（空交集→不冲突 / 严格包含→更具体的赢 / 相等→panic / 有交集但互不包含→panic）写入知识点 1 §1.5 与实操 1.3。**P1×3（报错原文抄错）**：`RejectUnknownMembers(true)` 的原文写成 `json: **unable** to unmarshal`，证据文件是 **`cannot`** → 已改。**P1×4（漏字）**：`/right-order` 那行漏掉 `body="{\"ok\":true}"` → 已补。**P1×5（数值过期）**：`p6b` 的 8 个内存数字是上一轮的 → console 块内更新为本轮逐字值，**并把正文里所有易变数值一律改为约数**（`≈26.5 MB` / `≈0.26 MB` / `约 1/100`）。**P1×6（"逐字"声明不成立）**：原写"逐字来自 `ALL_OUTPUT.txt`"却**没交代哪些内容天然不可复现** → 新增 **9 类漂移说明**（随机端口 / `Date` 与日志时间戳 / 基准数字 / 耗时 / 堆占用 / 内存地址与 goroutine 编号 / **JSON v2 的 map 顺序** / `-race` 的地址与栈偏移 / **竞态类输出**）。**P2×4**：探针计数 18→**19**、证据文件 812→**897** 行（两处）、**常见误区表编号重复**（44/45/46 各出现两次）→ 脚本重编号全表 **71 条**、清理重复水平线与一处误改的锚链接。**证据链机械化核对（沿用课 10 并升级）**：**94 个 `console` 块 / 1028 行输出 → 873 行直接命中 + 132 行归入 9 类 → 真实残差 0 条**；这条脚本当场抓出 P1×3 / P1×4 / P1×5 三处真问题并触发 P1×1 的溯源复查。**准确性**：`ListenAndServe` 只设两个字段（源码原文）、四个超时零值语义逐字、`DefaultMaxHeaderBytes = 1<<20`、**`DefaultMaxHeaderValueCount = 500`（Go 1.27 新增 #79936）**、`ServeMux` 模式语法全表、`Handler`/`HandlerFunc`/`ResponseWriter`/`Request`/`Shutdown`/`PathValue` 契约原文、`internal/godebugs/table.go:48` 的 `httpmuxgo121` 条目均从 `$GOROOT` 抓取并标 `（核查于 2026-09）`；**版本归属逐条查 `api/*.txt` 不凭记忆**（1.0 / 1.3 / **1.8 加 `Shutdown`+`ReadHeaderTimeout`+`IdleTimeout`** / 1.9 / 1.13 / 1.20 / 1.22 `PathValue`#61410 / 1.24 / **1.27 `MaxHeaderValueCount`**）；`net/http` 起源（源码版权头 2009、Brad Fitzpatrick 2011-02 已在提交）联网核实。**19 个探针包（16 可运行 + 2 故意失败 + 1 组基准）+ 真机 `curl` 9 条**全部逐字跑过验证：路由 8 组 + 冲突边界 12 组 + GODEBUG 两套行为 + 洋葱 trace 两序 + `StripPrefix`/`TimeoutHandler`、请求字段全量 + body 读两遍 + `r.Context()` 取消 + `-race` 3 段（`exit status 66`）、响应顺序三态 + `superfluous` 日志 + JSON 嗅成 `text/plain`、tag 全表 + 三类报错原文 + 大数精度、三读法内存 + v1/v2 六组差异、超时 A~E 五组 + `Shutdown` 四结局 + 431 三处边界 + 全局 mux 两种结局、中间件层数与 ctx 值成本两轮基准。**⚠️ 新增两条方法论警示已回写学习档案**：①本地 `curl` 一律加 `--noproxy '*'`（沙箱设了 `http_proxy` 会把 localhost 也走代理，"服务关了却返 502"的假象根因）；②故意失败的探针包要排除在 `go vet` 之外（两个口径分开写）
- [x] 阶段 4 · 课 12《数据访问与客户端》 — pedagogy + learner 双视角 + 机械化证据链核对 + `$GOROOT/api/*.txt` 版本回查，P0=0 / P1=6 / P2=4 全部当批修复（2026-09-13）。**阶段 4 收官（36/45）**。**P1×1（版本归属错 4 处）**：`DefaultMaxIdleConnsPerHost` / `Transport.MaxIdleConnsPerHost` / `time.RFC3339` 初稿写「Go 1.1」、`time.AfterFunc` 写「Go 1.21」——**回查 `api/*.txt` 发现全错**：前三个与 `time.AfterFunc` 都属 **Go 1.0**（`go1.txt`），Go 1.21 新增的是 **`context.AfterFunc`**。**根因是 api 文件格式坑**：Go 1.1 起 api 文件才开始记录**常量值**，于是**所有老常量在 `go1.1.txt` 里被重录**（该文件 50454 行 > `go1.2.txt` 32484 行），"重录"被当成了"新增"。→ 两处版本表改写 + 两个坑（`method (*T)` 写法 / `go1.1.txt` 非增量）写进正文。**P1×2（照抄文档导致语义错，本课最典型的一条）**：`Timer.Stop()` 返回值语义抄老版文档 *"false if the timer has already **expired** or been stopped"* 并演绎成"两种含义"，**但 `p12_timer` 实测"已到期后 `Stop()` → `true`"**（探针自己那行括号文案 `（false = 没拦住）` 与它自己的实测值矛盾）。追查 `time/sleep.go` 发现 Go 1.23 补的新句 *"if the program has not received from t.C already and the timer is running, Stop is guaranteed to return true"* → **判据是"值有没有被接收过"，不是"有没有到期"**。**另建探针 `p18_timerstop` 把 8 种情形测全**（`NewTimer` 6 格 + `AfterFunc` 3 格 + `C` 字段），结论：`NewTimer` 的 `false` ⟺ **值已被接收过 / 已被 `Stop` 过**；`AfterFunc` 的 `false` 才是"没拦住"（回调已跑）。正文 3 处 + 速查卡 + 自检清单 + Mermaid 节点全部改写。**P1×3**：第四幕自述「正文 `console` 块数 **86 个**」，脚本实测 **118** → 改为精确账（118 块 / 653 行 / 653 行直接命中 / 1 行占位 / 证据文件 1484 行）。**P1×4**：`## 🐞 常见误区` 引言写「共 62 条」，表内实际 **82 条** → 已改；并查出**合并表有 30 条与三张分表逐字一致**，于是把"合并去重后汇总"这个**不成立的说法**改成如实描述。**P1×5**：第四幕开头写"正文中凡引用这些易变数值**一律写成约数**"，但 `console` 块里逐字保留着 `37.56 MB` → 改为"`console` 块内逐字 / 叙述用约数"的准确口径。**P1×6**：第一幕伏笔表写对照版"查询**完整跑完**（耗时见服务端日志）"，证据文件里有确切值 → 补为 `err=<nil> 耗时=1.425s`。**P2×4**：①探针计数与证据文件行数（17 个包 / 16 可运行 / 1455 行）→ 新探针后改为 **18 / 17 / 1484**；②**两处重复水平线**（第二幕末、知识点 3 📚 末）→ 已删；③`§2.6 重试：三道闸门，一道都不能少` 与正文"四道闸门"打架 → 改为 `三道显性闸门 + 一道最容易被忽略的`；④`Timer.Reset` 的 `true/false` 同属被误读项 → 随 P1×2 一并改写（`p18` A 组 ⑤⑥ 实测"已到期但值未接收 → `Reset` 仍返回 `true`"；`p12_timer` 4) 的 `false` 是因为它前面已 `<-t5.C`）。**证据链机械化核对**：**118 个 `console` 块 / 653 行输出 → 653 行直接命中、1 行结构化占位、真实残差 0 条**（归一化抹掉 **10 类漂移**：随机端口 / 各种耗时 / 基准数字 / 堆占用与存活对象数 / 累计分配量 / 时钟读数 / 并发日志交错顺序 / `WaitDuration` / sqlite 临时路径与地址 / 代理端口）。**脚本抓出了 P1×3 / P1×4 / P1×5；但 P1×1 与 P1×2 是"另开一路回查源码"才抓到的** —— 说明**脚本只能保证"正文与证据文件一致"，保证不了"证据文件本身对"**，两条路都得走（已作为方法论写进学习档案）。**准确性**：`database/sql` 侧全部从 `$GOROOT/src/database/sql/sql.go` 抓取并标 `（核查于 2026-09）`（`DB` = *"a pool of zero or more underlying connections"* / `Open` *"may just validate its arguments without creating a connection"* + *"should be called just once"* / **`const defaultMaxIdleConns = 2`** / `SetMaxOpenConns` 的 `n <= 0` = 不限且默认 0 / `SetMaxIdleConns` 会被**自动压到 ≤ `MaxOpen`** / `connectionCleaner` 的 **`const minInterval = time.Second`** / `Rows.Close` *"idempotent"*）；`http.Client` 侧（`A Timeout of zero means no timeout.` / `DefaultTransport` **逐字段** / **`MaxIdleConnsPerHost` 不在字面里** → 落 `const DefaultMaxIdleConnsPerHost = 2`，而 `MaxIdleConns = 0` 是"不限"，**两个 0 含义相反** / **Go 1.27 的 `maxPostCloseReadBytes = 256 << 10` + `maxPostCloseReadTime = 50ms` + `maybeDrainBody`** / `httpproxy` 的 `isLoopback` 豁免）；`time` 侧（Monotonic Clocks 全节 strip 清单 / `t.Round(0)` 规范剥法 / `Since` 快车道 / **Go 1.23 的 GC 承诺** / `Ticker.Stop` 不关 channel / 布局常量全表含 **`DateTime`** / `Parse` 两位年 **69 分界** + 无时区当 UTC / 官方测试 `tick_test.go:400` 的 `cap == 0` 断言 / `runtime.time.go` 的 `newTimer` throw）。**版本归属逐条回查 `api/*.txt`**（Go 1.0 四项 / 1.1 / 1.2 / 1.3 / 1.5 / 1.6 / 1.7 / 1.8 / 1.12 / 1.13 / 1.15 / 1.20 #52746+#50770 / **`context.AfterFunc` 1.21 #57928**）。**探针 18 个包（17 可运行 + 1 基准）全部逐字跑过**；`p16_downstream` 另跑 **`go run -race` 全绿**；`gofmt -l` 无输出、`go vet ./...` 退出 0、`go build ./...` 退出 0。**两处"不结案"如实标注 ⏳**：①`AfterFunc` 未 `Stop` 的内存例外（30 万个 ≈37.5 MB，机制未查清）；②`cap(timer.C)` 的文档 / 官方测试 / 源码三方字面不一致
- [x] 阶段 5 · 课 13《模块、测试与规范》 — pedagogy + learner 双视角，P0=0 / P1=0 / P2=0（2026-09-15）。五幕、三知识点六要素、固定一眼全局 SVG、三张 Mermaid、本课地图、官方文档核对、7 个子测试和本机第四幕实测输出齐全；Go 1.27.1 darwin/arm64 下 race / gofmt / vet / build 全通过。golangci-lint 本机未安装，正文只说明定位并链接官方文档，未伪造运行结果。
- [x] 阶段 5 · 课 14《性能与诊断》 — pedagogy + learner 双视角，P0=0 / P1=0 / P2=0（2026-09-16）。五幕、三知识点六要素、固定一眼全局 SVG、本课地图、官方文档核对和本机第四幕实测输出齐全；Go 1.27.1 darwin/arm64 下 test / race / gofmt / vet / build 全通过，五类 profile 成功生成并由 `go tool pprof -top` 读取；benchstat 未安装，正文未伪造输出。
- [x] 阶段 5 · 课 15《构建部署与选型决策》 — pedagogy + learner 双视角，P0=0 / P1=0 / P2=0（2026-09-16）。五幕、三知识点六要素、固定一眼全局 SVG、本课地图、官方文档核对和本机第四幕实测输出齐全；Go 1.27.1 darwin/arm64 下 test / race / gofmt / vet / build 全通过，linux/amd64 静态 ELF 交叉构建通过；Docker 多阶段 build/run 真实通过，镜像 7880864 bytes，临时镜像已清理；选型表明确标注为工程判断框架，不把 2019 年官方定位页的历史统计当作当前数字。
- [x] **结课实战项目**（Phase 3）— pedagogy + learner 双视角 + **复杂度四门槛核查**，P0=0（2026-09-16）。订单服务多文件工程真实通过功能链路、测试/竞态/静态检查、benchmark + CPU profile、linux/amd64 静态交叉构建和 Docker build/run；四门槛均满足。
- [ ] **实战经验 / 排障速查手册 / 场景解法库**（Phase 5）— pedagogy + learner 双视角 + **证据纪律核查**（三份一次生成）
- [x] **课程手册汇总**（Phase 4）— pedagogy + learner 双视角 + **汇总完整性核查**，P0=0（2026-09-16）。手册 596 行，覆盖 5 阶段、15 课、45 个知识点、每课入口要素、阶段路径图、官方文档入口、结课项目与真实验收摘要；Phase 5 三件套明确标记为尚未生成。

> 课程制的两条收尾占位（结课实战项目 / 收尾三件套）在 Phase 1 就占位 —— 它们在流程末尾，此刻不占位后续极易漏掉。

## 每课交付必查项

> 沿用本仓库既有约定（Docker / ZooKeeper 课程补齐时确认的高频漏项）。课文件交付前逐项核对：

- [x] 课文件末尾含「🚀 下一批接力提示词」段（2026-09-16）
- [x] 整课完成时（不再追加小节）末尾含「🧭 课程导航」段（上一课 / 下一课 / 返回目录）（2026-09-16）
- [x] 知识点状态已回写 00-学习档案.md 进度表（2026-09-16）
- [x] 02-课程目录.md 中本课已由纯文本改为可点击链接（2026-09-16）
- [x] 已过事实核查闸门（版本号 / 官方行为 / 报错原文等须核实并标注（核查于 YYYY-MM））（2026-09-16）
- [x] 课文件含「📌 知识点导航」段（2026-09-16）
- [x] 课文件含「📋 速查卡」段（2026-09-16）
- [x] 本课涉及的语法或标准库 API 已回写 02-课程目录.md 的「🔧 语法与标准库速查索引」（2026-09-16）
- [x] **本课示例代码已在临时目录真实跑通并贴出实测输出**（本机 Go 1.27.1，Docker build/run 也已真实通过）（2026-09-16）

## 评审记录（勾选后追加结论摘要）

| 日期 | 评审对象 | 评审方式 | P0 数 | 意见摘要 |
|------|----------|----------|-------|----------|
| 2026-09-16 | Go 课程手册汇总（Phase 4） | 主 agent 内联（pedagogy + learner 双视角）+ 汇总完整性、相对链接与仓库卫生核查 | 0 | **P0×0 / P1×0 / P2×0。** 15 课与 45 个知识点逐项出现并可回链；5 阶段概览、阶段 SVG 与每课一图入口齐全；每课保留一句话本质、处境对照、知识点地图、实操入口、常见误区与官方文档入口；结课项目、订单服务真实验收摘要与完整输出文件均已收录；Go / Rust / Java / Python 与部署边界的决策清单齐全；Phase 5 三件套诚实标记为尚未生成。主 agent 内联评审，course-reviewer 子 agent 尚未创建。 |
| 2026-09-13 | 阶段 4 · 课 12《数据访问与客户端》（**阶段 4 收官课**） | 主 agent 内联（pedagogy 5 维度 + learner L1-L6 逐项）+ 机械化证据链核对 + `$GOROOT/api/*.txt` 版本回查 | 0 | **P1×6：①版本归属错 4 处** —— `DefaultMaxIdleConnsPerHost` / `Transport.MaxIdleConnsPerHost` / `time.RFC3339` 写「Go 1.1」、`time.AfterFunc` 写「Go 1.21」，**回查 `api/*.txt` 发现四处全错**：都属 **Go 1.0**，Go 1.21 新增的是 **`context.AfterFunc`**。根因是**把 `api/go1.1.txt` 里的"常量值重录"当成了"1.1 新增"**（Go 1.1 起才记录常量值，于是老常量被整体重录，该文件 50454 行 > `go1.2.txt` 32484 行）。**②照抄文档导致语义错** —— `Timer.Stop()` 返回值的初稿抄老版文档的 *"false if the timer has already **expired** or been stopped"*，**但实测"已到期但值没被接收"时返回 `true`**；Go 1.23 在同段补了新句 *"…has not received from t.C already and the timer is running, Stop is guaranteed to return true"* → **判据是"值有没有被接收过"，不是"有没有到期"**。**另建探针 `p18_timerstop`** 测全 8 种情形（`NewTimer` 6 格 + `AfterFunc` 3 格 + `C == nil`），正文 3 处 + 速查卡 + 自检清单 + Mermaid 改写。**③自述块数错** ——「86 个 `console` 块」实为 **118**（新脚本给出精确账：118 块 / 653 行 / 653 行直接命中 / 1 行占位 / 证据文件 1484 行）。**④自述条数错** —— 常见误区引言「共 62 条」实为 **82 条**；并查出**合并表 30 条与分表逐字一致**，把"合并去重"这个**不成立的说法**改成如实描述。**⑤口径自相矛盾** —— 「正文一律写成约数」与 `console` 块里逐字保留 `37.56 MB` 打架 → 改为"块内逐字 / 叙述用约数"。**⑥散文引用不可核对的描述** —— 伏笔表写"耗时见服务端日志"，证据文件里有确切值 → 补 `err=<nil> 耗时=1.425s`。**P2×4**：探针计数与证据文件行数（17/16/1455）→ **18/17/1484**；**两处重复水平线**已删；`§2.6 重试：三道闸门，一道都不能少` 与正文"四道闸门"打架 → 改为 `三道显性闸门 + 一道最容易被忽略的`；`Timer.Reset` 的 `true/false` 同属被误读项 → 随 ② 一并改写。**证据链机械化核对**：**118 块 / 653 行 → 直接命中 653、结构化占位 1、真实残差 0 条**（归一化抹掉 10 类漂移）。**关键结论：脚本抓出了 ③④⑤，但 ① ② 是"另开一路回查源码"才抓到的 —— 脚本只能保证"正文与证据文件一致"，保证不了"证据文件本身对"，两条路都得走。** **准确性**：`database/sql` / `http.Client` / `time` 三个包的关键契约与全部默认值**逐条从 `$GOROOT/src` 与 `$GOROOT/api/*.txt` 抓取核实并标 `（核查于 2026-09）`**（含 **`const defaultMaxIdleConns = 2`**、`connectionCleaner` 的 **`minInterval = 1s`**、**`A Timeout of zero means no timeout.`**、`DefaultTransport` 逐字段、**`MaxIdleConnsPerHost` 走 `const DefaultMaxIdleConnsPerHost = 2` 而 `MaxIdleConns = 0` 是"不限"**、**Go 1.27 的 `maxPostCloseReadBytes = 256<<10` / `maxPostCloseReadTime = 50ms` / `maybeDrainBody`**、`httpproxy` 回环豁免、Monotonic Clocks strip 清单、**Go 1.23 的计时器 GC 承诺**、`Parse` 两位年 **69 分界**）。**探针 18 个包（17 可运行 + 1 基准）全部逐字跑过**，`go run -race` 全绿、`go vet ./...` 退出 0、`go build ./...` 退出 0。**两处"不结案"如实标 ⏳**：`AfterFunc` 未 `Stop` 的内存例外（≈37.5 MB / 30 万对象）与 `cap(timer.C)` 的三方字面不一致 —— **"不知道"可以接受，编一个解释不可以** |
| 2026-09-10 | 阶段 4 · 课 11《net/http 服务端》 | 主 agent 内联（pedagogy 5 维度 + learner L1-L6 逐项） | 0 | **P1×6：①事实性错误（最"打脸"的一条）** —— 原写「`encoding/json/v2` 是普通包、不需要 `GOEXPERIMENT`」，依据之一是"在 `internal/buildcfg/` 与 `goexperiment/` 里 grep 不到 `jsonv2`"——**搜错了目录**：开关在 `internal/goexperiment/exp_jsonv2_on.go` / `_off.go`。真相是**它是 GOEXPERIMENT，只是默认基线里开着**；`GOEXPERIMENT=none go list encoding/json/v2` → **`build constraints exclude all Go files`**。**"搜不到"≠"不存在"** —— 已在三处改正并提炼出通用判定手段（**判实验开关用 `GOEXPERIMENT=none go list <pkg>`，不要 grep 目录**），把翻车写进正文与误区表（新增 3 条）。**②小测答案错误** —— 小测第 2 题（5 条路由注册哪些 panic）**凭推理给的答案两条都错**（原判「② 不冲突、⑤ 因路径相同而冲突」）→ 新建 `p2c_conflict` 跑 **12 组边界**定案：**② ⑤ 都 panic，且都撞在最早的 `GET /` 上**（不是彼此相邻两条打架）；判据四句写进知识点 1 §1.5 与实操 1.3；最有工程价值的一条：**`"GET /"` 与任何"不带方法"的模式都冲突、两个注册方向都 panic → 别混用**。**③报错原文抄错** —— `json: **unable** to unmarshal` 应为 **`cannot`**。**④漏字** —— `/right-order` 漏掉 `body="{\"ok\":true}"`。**⑤数值过期** —— `p6b` 的 8 个内存数字是上一轮的 → console 块更新为本轮逐字值，且**正文所有易变数值改约数**（`≈26.5 MB` / `约 1/100`）。**⑥"逐字"声明不成立** —— 未交代"哪些内容天然不可复现" → 新增 **9 类漂移说明**（随机端口 / `Date` 与日志时间戳 / 基准数字 / 耗时 / 堆占用 / 内存地址与 goroutine 编号 / **JSON v2 的 map 顺序** / `-race` 地址与栈偏移 / **竞态类输出**）。**P2×4**：探针计数 18→**19**、证据文件 812→**897** 行、**常见误区表编号重复（44/45/46 各两次）→ 脚本重编号全表 71 条**、清理重复水平线与一处误改锚链接。**证据链机械化核对：94 个 `console` 块 / 1028 行 → 873 行直接命中 + 132 行归入 9 类 → 真实残差 0 条**；脚本当场抓出 ③④⑤ 三处真问题并触发 ① 的溯源复查。**准确性**：`ListenAndServe` 只设 `Addr`+`Handler`（源码原文）、四个超时零值语义逐字、`DefaultMaxHeaderBytes = 1<<20`、**`DefaultMaxHeaderValueCount = 500`（Go 1.27 新增 #79936）**、`ServeMux` 模式语法全表、`Shutdown` 四步 + `shutdownPollIntervalMax = 500ms` + `ErrServerClosed` 均从 `$GOROOT` 抓取并标 `（核查于 2026-09）`；**版本归属逐条查 `api/*.txt`**（1.0 / 1.3 / **1.8 加 `Shutdown`+`ReadHeaderTimeout`+`IdleTimeout`** / 1.9 / 1.13 / 1.20 / 1.22 `PathValue`#61410 / 1.24 / **1.27**）；**19 个探针包 + 真机 `curl` 9 条**全部逐字跑过（路由 8 组、冲突边界 12 组、GODEBUG 两套行为、洋葱 trace、请求全字段、body 读两遍、`r.Context()` 取消、`-race` **`exit status 66`**、响应顺序三态、tag 全表、三读法内存、v1/v2 六组差异、超时 A~E、`Shutdown` 四结局、431 三处、全局 mux 两种结局、两轮基准）。**两条新方法论警示已回写**：①本地 `curl` 必加 `--noproxy '*'`；②故意失败的包要排除在 `go vet` 外（两口径分开写） |
| 2026-09-10 | 阶段 4 · 课 10《io 与 context》（**阶段 4 开篇课**） | 主 agent 内联（pedagogy 5 维度 + learner L1-L6 逐项） | 0 | **P1×3：①技术断言不实** —— 撞墙 2 的并列清单混进 **`database/sql`** 且挂着"实测全部正常工作"，而 `database/sql` 根本不接受 `io.Reader`、该项从未跑过（"为了排比好看"多写的词）→ 换成真跑过的 `csv.Reader`；**教训已写进学习档案断点区**（"实测"二字不能挂在没跑过的项上）。**②教材与实测脱节** —— embed 体积倍率 `1.008:1` 是上一批的旧数、不在本轮证据文件里 → 补做 A/B 对照实测 **2467858 → 4597922（增量 2130064 ≈ 1.0157 倍）**，全文 5 处统一改正，实验追加进 `ALL_OUTPUT.txt`。**③描述与源码不符** —— `ctx.Value(userKey{{}})` 被解释成"`fmt` 打印空 struct"，实为**格式串里写死的转义**（`{{`/`}}`）→ 改写。**P2×2**：探针计数（18 可运行 + 2 静态验证 + 1 基准）与证据文件行数（767）订正。**新增做法：证据链机械化核对** —— 脚本把正文 **76 个 `console` 块共 725 行**逐行与 `ALL_OUTPUT.txt` 归一化比对，**真实不符 0 处**，差异全部归入三类可解释来源（`>` 前缀 / `...` 省略 / 旁注）；该脚本当场抓出 P1×2。**准确性**：`io.Reader` 契约原文、`io` 包并发安全原文、`context` 四条约定原文、`Cause` 规范原文、`Scanner.Buffer` 原文、`ioutil` Deprecated 原文均抓取核对并标 `（核查于 2026-09）`；**版本归属全查 `api/*.txt` 不凭记忆**（io=1.0 / context=1.7 / embed+ioutil废弃=1.16 / Cause=1.20 / WithTimeoutCause=1.21 / `(*os.File).WriteTo`=1.22）；`context` 起源联网核实（Google 内部包 → 2014-07-29 官方博客 → Go 1.7）。**跨平台项全部 ⏳ 标注未实测**（Windows 路径分隔符、`defer` 先删后关）。**合拢了课 9 的两处前向引用**（`ctx.Done()` 当作"能被关闭的 channel"、context 系统讲解在课 10.2） |
| 2026-09-10 | 阶段 3 · 课 9《同步、竞争与泄漏》（**阶段 3 收官课**） | 主 agent 内联（pedagogy 5 维度 + learner L1-L6 逐项） | 0 | P1×3：①**方法论级陷阱** —— 验证「Go 1.22 循环变量按 go.mod 门控」时用**文件模式** `go run x.go` 得出错误结论（`go 1.21` 模块也跑出新语义），差点推翻课 7 已落盘的 `[c c c]`；追查为文件模式下文件属 `command-line-arguments` 伪包、拿不到 go.mod 语言版本 → 全部探针改**包模式**重建，`go 1.21` 正确给出 `[  c]`；已写入正文警示 + 学习档案 + topic-teach 闸门。②「读多写少用 RWMutex」无阈值 → 补跑读写比扫描（8×80 万次，固定工作量 + 校验和）：Mutex 在全部比例下都是 67.9~74.8 ms 平线，RWMutex **甜点区 5%~25% 写**（9.5~14.9 ms，快 4.9~7.6×），**纯读仅 1.21× / 全写仅 1.26×**；临界区有真实工作量时加速比降到 1.1~1.9×。③"锁不能复制"未证明 `go test` 为何抓不到 → 补对照实验（同包放 printf 问题立刻 `build failed`；copylocks 静默 `ok`）证明是**默认 vet 子集问题**。P2×2：波动数字 3 处补注（无保护计数器 12.3 万~20.3 万列出全部样本）；`sync.Map` 表述按官方两场景改写。**准确性**：Go 1.22 loopvar 门控原文、Go 1.27 leaked goroutine 定义原文、errgroup v0.23.0 源码注释原文（零值 Group 不取消）均核实并标注；**另如实记录一次"没复现"** —— 20 万轮未能复现"ready=1 但 data 不完整"，正文明确写出"这不代表安全" |
| 2026-09-10 | 阶段 3 · 课 8《channel：用通信共享内存》 | 主 agent 内联（pedagogy 5 维度 + learner L1-L6 逐项） | 0 | P1×3：①**小测 Q1 的输出是凭印象写的，与实测相反**（原写 `主：收到 数据` 在前，实测 `A：发送完成` 在前，连跑 3 次稳定）→ 新建 `q1_quiz.go` 原样跑过替换；**额外挖到更好的教学点** —— `GOMAXPROCS=1` 时 `A：发送完成` **压根不打印**（main 返回即全灭，复现课 7 知识点 2），已写入答案并回扣课 7 演示 1-A；②**「缓冲开多大」无阈值** → 补跑容量基准（固定 20 万次交接，只改容量）：cap 0/1/16/128/1024 = 22.2/15.8/6.2/5.2/5.7 ms，**甜点区 16~128，cap 1024 反比 128 慢 9%**；突发场景 128→4096 只再快 3.5% 但内存多 32 倍 → 写入核心原理 / 速查卡 / 误区表 21、22 / 第五幕收束四处；③演示 1-A 断言"后两行顺序不保证"却无证据 → 连跑 5 次（第 2 次翻转）写入正文。P2×2：①随机命中数 / Timer 分配数 / 耗时每次波动 → 3 处补「复跑波动」注（附第二次真实输出）；②Timer 复用版复跑 `存活对象=-5`（GC 干扰）不宜进讲义 → 保留首轮值并说明"看量级不看精确值"。**准确性**：select 随机规范原文、comma-ok 规范原文、Go 1.23 Timer/Ticker 两条变更原文、Go 1.27 移除 `asynctimerchan`（含实测 fatal error 原文）、`goroutineleak` profile（含 `Count()` 恒为 0 的坑）均已抓取核对并标注核查日期；**20 个实测程序 + 1 组 benchmark 全部逐字跑过** |
| 2026-09-06 | 大纲评审（Phase 1） | 主 agent 内联（pedagogy 5 维度） | 0 | P1×1：零基础读者缺环境准备环节 → 阶段 1 overview 增「🛠 课前环境准备」章节（不计入知识点），已采纳；P2×1：课 9.3 用到 context 但其系统讲解在课 10.2 → 两处须显式标注前向引用，已采纳 |
| 2026-09-06 | 阶段 1 · 课 1《Go 是什么、环境怎么跑起来》 | 主 agent 内联（pedagogy 5 维度 + learner L1-L6 逐项） | 0 | P1×1：「2012-03 Go 1 发布」日期未核实 → 查官方 Release History 改为 2012-03-28；P2×3：①pclntab 命名加 ⏳ 置信度标注 ②补 `package main`/`import "fmt"`/`func main` 三行最小解释（零基础 L5）③`stat -f%z` 加 Linux 写法提示（L5） |
| 2026-09-07 | 阶段 2 · 课 6《接口与泛型》（**阶段 2 收官课**） | 主 agent 内联（pedagogy 5 维度 + learner L1-L6 逐项） | 0 | P1×0。P2×2：①**自己又差点犯"猜报错原文"的错** —— 先写了 `type parameter T is not comparable with >`，实测是 `cannot use operator >`（课 5 刚立的纪律，本课差点破功）→ 已订正；②type switch 输出用 `%-24v` 导致 `[]int` 那行把内容塞进宽度、且 `len(v)` 漏写 → 改 `%-14T` + 补 `len(v)`；另删掉一处自相矛盾的死代码分支。**阶段 2 三课（4/5/6）全部闭环，18/45** |
| 2026-09-07 | 阶段 2 · 课 5《结构体与方法》 | 主 agent 内联（pedagogy 5 维度 + learner L1-L6 逐项） | 0 | P1×1：**差点教错一条已作废的规则** —— 通行教材的「提升字段不能写在复合字面量里」在 go1.27.1 上不成立，追查为 Go 1.27 新增 Struct literal field selectors（提案 #9859），spec 旧例外句已删 → 改写并标版本与置信度。P2×2：①两处断言未实测（嵌入 tag 嵌套 / 小测 Q1）→ 补跑补入；②网上两篇 Go 1.27 文章的说法（指针嵌入自动 new、歧义报 `ambiguous field`）**本机实测证伪** → 课内加「以实测为准」警示 |
| 2026-09-06 | 阶段 2 · 课 4《函数与错误处理》 | 主 agent 内联（pedagogy 5 维度 + learner L1-L6 逐项） | 0 | P1×2：①知识点 3 ① 的 LIFO 解释与演示程序实测相反（文字说"先关数据库…最后解锁"，实测是 `1. 解锁 / 2. 关文件 / 3. 关数据库连接`）→ 重写为"注册顺序与执行顺序相反，按申请顺序写 defer 即得到逆序释放"；②知识点 2 引用的 `errors.New: 写死了的错误` / `fmt.Errorf: 带格式的错误: abc-42` / `pe.Error() = 第 7 行解析失败` 三处输出在第四幕无对应步骤 → 补全 `errors.go` 第 1–3 段实测输出。P2×2：①defer 立即求值片段与真实程序不符 → 统一为逐字；②「退出码: 1」非真实打印 → 改为 `exit status 1` 并加注。**另统一**"包了 3 层/两层"措辞为"包了两层（链上 3 个节点）"，探针重跑验证 |
| 2026-09-06 | 阶段 1 · 课 3《数组、切片与 map》 | 主 agent 内联（pedagogy 5 维度 + learner L1-L6 逐项） | 0 | P1×1：知识点 2「示例演示」的「有序」输出原写成 `apple=1 cherry=3`（漏了 banana —— 把 delete 后的状态提前代入了排序那一步），**实测应为 `apple=1 banana=2 cherry=3`**（实测 3 次连跑验证）→ 已修正并补"三个观察"说明；P2×2：①**实测新发现的教学点** —— `fmt.Println(m)` 输出**有序**（fmt 内部排序，4 次稳定）但 `for range` **无序**（4 次出现 2 种顺序），这是调试时极易上当的"假象陷阱"，已作为 ⚠️ 反直觉陷阱写入知识点 2 ③ 与第四幕步骤 3；②遍历无序的证据强度 —— 用 3 键看不出随机性，已改为 **10 键跑 5 次**（实测 3 种不同顺序）作为证据 |
