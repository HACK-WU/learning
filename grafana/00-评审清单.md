# Grafana 评审清单

> 强制检查点：每批内容生成后，先在本文件找到对应未勾选条目，完成双视角评审且 P0 清零后，才能勾选并交付。
> **未勾选 = 评审未完成，禁止进入下一批。**

## 待评审清单

- [x] 大纲评审（Phase 1）— pedagogy 视角 — P0=0（2026-09-04）
- [x] 阶段 1·课 1《Grafana 是谁：一个不存数据的看图工具》 — pedagogy + learner 双视角 — P0=0（2026-09-04）
- [x] 阶段 1·课 2《第一个面板：从零到看得见》 — pedagogy + learner 双视角 — P0=0，P1×3 已修（2026-09-04）
- [x] 阶段 1·课 3《变量与 Dashboard 组织：一张图服务 N 台机器》 — pedagogy + learner 双视角 — P0=0，P1×3 已修 + 死链 5 条已修（2026-09-04）
- [x] 阶段 2·课 4《查询编辑器与数据源协议：一次查询的完整旅程》 — pedagogy + learner 双视角 — P0=0（P0×1 已修 + P1×3 已修，2026-09-04）
- [x] 阶段 2·课 5《Transformations：把查出来的数据捏成想要的形状》 — pedagogy + learner 双视角 — P0=0（P1×4 已修，2026-09-04）
- [x] 阶段 2·课 6《变量进阶与动态仪表盘》 — pedagogy + learner 双视角 — P0=0（2026-09-04；澄清课 4 一处 + 新增判据一处；自身预判被推翻 2 处已按实测改写）
- [x] 阶段 3·课 7《告警架构：规则在哪求值、状态怎么迁移》 — pedagogy + learner 双视角 — P0=0（2026-09-04；修正阶段 3 概览 2 处表述 + 拼写修正 1 处；3 次判真伪其中 2 次为自我纠错）
- [x] 阶段 3·课 8《告警规则与通知策略实战》 — pedagogy + learner 双视角
- [ ] 阶段 3·课 9《日志与链路：指标之外的另外两只眼》 — pedagogy + learner 双视角
- [x] 阶段 4·课 11《权限与服务账号：谁能看、谁能改、程序怎么访问》 — pedagogy + learner 双视角
- [x] 阶段 4·课 12《性能、高可用与升级运维》 — pedagogy + learner 双视角 — P0=0（2026-09-07；三个反转直觉：面板数非瓶颈/时间窗拉长数据反减/渲染比查询慢 500 倍；降级实验坐实 unified storage 双向不可见 + token 落库为哈希恢复不了明文；死链 2 条已修；评审判为脚本误报未改文档×1）
- [ ] 阶段 4·课 10《Provisioning：把点击变成配置文件》 — pedagogy + learner 双视角
- [x] 阶段 4·课 11《权限与服务账号：谁能看、谁能改、程序怎么访问》 — pedagogy + learner 双视角
- [x] **结课实战项目**（Phase 3）— pedagogy + learner 双视角 + 复杂度四门槛核查（A1-A6）— P0=0（2026-09-07；一键重建复验通过；事实核查抓出 3 处过时数字已修：错误率 6.31%→52%~61%、P90 4.208s→2.6~4.3s 范围、QPS 0.67→约1.5；新增踩坑第 8 条「比率型告警分子为空导致模板 nil 报错」为真实 P0 并已修复）
- [x] **final-课程手册**（Phase 4）— 索引型手册完整性核查 — P0=0（2026-09-07；12 课 36 知识点全部收录，链接 46 条全部可达、锚点 8 条全部命中；知识点计数脚本核对=36 与声称一致；实测数字与 00-学习档案 逐条比对一致；核查脚本锚点逻辑缺陷致误报 1 条已修脚本未改文档）
- [x] **实战经验 / 排障速查手册 / 场景解法库**（Phase 5）— pedagogy + learner 双视角 + 证据纪律核查（B1-B8 / C1-C7）— **P0=0**（2026-09-07；三态分工：08 学习态 15 条故障模式+6 类反模式+18 项 Checklist / 09 使用态 15 条症状倒查 / 10 设计态 10 场景先想后看；素材经 2 个脚本从 12 课提取 601 行后成文；机器核查链接 135 条全可达、锚点 0 问题、围栏全偶数、数字三件套一致；**P1×2 已修**：09 症状 14/15 原理链接错指→08 补故障模式 14/15、修复时误删症状 15 标题已恢复；**脚本误报×1 未改文档**：slug 空格处理错误致 26 处假报；P2×1：症状 14/15 补「若无效」出口至 15/15）

## 每课交付必查项（沿用 learning 仓库既有约定）

> 来源：本仓库 `promql` L1（2026-08-27）与 `zookeeper` L1（2026-08-28）两次首次交付均因遗漏结尾两段被用户指出，
> 后经 `elasticsearch` 课 14（2026-09-01）再次确认——**数据事实组与结构合规组必须分开查**。

### A 组 · 结构合规（格式全不全）

- [ ] 五幕结构齐全：场景引入 → 认知冲突 → 层层揭示 → 实操验证 → 体系收束
- [ ] 课尾三段式齐全：`## 📌 本课速览` + `## 🧭 课程导航` + `## 🚀 下一批接力提示词`
- [ ] 课尾「课程导航」含上一课 / 下一课相对链接 + 返回课程目录、阶段概览、学习路径三链接
- [ ] 每个知识点按「六要素」展开：一句话定义 / 直觉建立 / 核心原理 / 示例演示 / 常见误区 / 一句话记住
- [ ] 核心概念配生活化类比，且**指出类比失效的边界**
- [ ] 图表合规：默认 Mermaid，命中 A/B 类信号才用 SVG；配色浅底深字

### B 组 · 数据事实（内容对不对）

- [ ] **讲义里每条命令都按出现顺序真跑过一遍**（必查项 #11，优先级最高）
  > 来源：`elasticsearch` 课 15 教训——格式评审 P0=0 但照抄必失败。
- [ ] 凡引用脚本 / 配置文件（如 `bash playground/l02-*.sh`）**必须确认文件真实存在且可执行**
- [ ] 时效敏感内容（版本号 / 默认值 / API 行为）已联网核查并标注「核查于 YYYY-MM」
- [ ] 起源类历史事实已联网核实（课 1 涉及 Grafana 诞生背景，必查）
- [ ] 引用的实测数字前后自洽，不在不同章节出现矛盾口径
- [ ] 跨课引用的章节号 / 文件名真实存在且可达
- [ ] **Grafana 专属必查**：命令须在 WSL 内用脚本文件方式跑（PowerShell 内联中文命令会被截断）

### 评审报警处理纪律

> 来源：`elasticsearch` 课 15——5 项报警里 4 项是**评审脚本自身的缺陷**而非文档问题。
> **评审报警必须先判定真伪再动手改，不可盲改文档。**
>
> 2026-09-04 固化（AI 协作设定）：每条「缺失 / 错误 / 遗漏」判定，都必须先回读原文或用脚本核验，确认是真问题后才能写入结论。

## 评审记录（勾选后追加结论摘要）

| 日期 | 评审对象 | 评审方式 | P0 数 | 意见摘要 |
|------|----------|----------|-------|----------|
| 2026-09-07 | 课 12《性能、高可用与升级运维》 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | 0 | **三个反转直觉（全有实测支撑）**：①**面板数不是瓶颈**——N=1→80（80 倍）耗时 7ms→32ms（仅 4.5 倍），亚线性且绝对值可忽略，面板数是**优先级最低**的排查项；②**时间窗拉长数据反而变少**——1h→7d（168 倍）响应体 9680→4093 字节，因 Grafana 自动放大步长（30s→2h）；③**慢在渲染不在查询**——API 9ms vs PNG 4.9s，**500 倍**。**真开关是 `maxDataPoints`**（面板宽度决定）：100→2000 时响应体 0.98MB→14.3MB（14.5 倍）。**12.2**：SQLite 并发写 10 次全 200，但日志实锤 **`SQLITE_BUSY` 重试至 retry=2、退避 199ms**（锁被重试掩盖，用户无感）；SQLite 硬边界＝**文件无法共享**（`grafana-lab` 无挂载）；换 Postgres 仅改环境变量、应用层零改动（91 表）；**HA 三结论**：配置双向可见、会话默认共享（pg1 cookie 访问 pg2 返 200，**无需配 Redis**）、迁移带锁幂等（`performed=0, skipped=719`）。**12.3（本课最大价值）**：token 明文 `glsa_...` 落库为 32 位哈希 `e34d8990...`，**备份恢复不了明文**（坐实课 11 伏笔）；**降级实验**——12.0.0 开 13.2.1 的库返回 `database: ok` 但 dashboard **404**，且**旧版继续改库**（719→731）；根因 13.x 用 **`resource` 表（unified storage）**，12.x 读老表，**双向不可见**（12.0.0 建的 `old-created`，13.2.1 读也 404）；**回滚＝恢复备份**（实测恢复到 719、dashboard 200）。**P1 已修**：死链 2 条（`../../`→`../../../`，课 2/3 同坑）。**评审判为脚本缺陷未改文档×1**：A3 报「12.2/12.3 缺六要素」，回读 grep 确认**误报**（18 处齐全，awk 区间正则缺陷，课 4/10 同款）。**未实测已标注×4**：告警 HA 去重、12→13 正向迁移、SQLite 并发击穿阈值、完整 SQLite→PG 迁移演练。**课 4 P0 未复发**：代码块外 0 续行。**环境坑×3**：5432 被 `xpert-db-1` 占（改用 5433）；8081 被占（改 8082）；`GF_RENDERING_RENDERER_TOKEN` 用默认值 Grafana **拒绝启动**，renderer 侧须设同名 `AUTH_TOKEN` 否则 401；`gf-render` 须同时接 `l12net`+`grafana-net` 否则查询 8 秒超时 |
| 2026-09-04 | 大纲评审 | 主 agent 内联（pedagogy，子 agent 未创建，独立性受限） | 0 | P1×2 已采纳：①课 4 新增知识点 4.3「状态码与真实错误的分离」（本机实测 HTTP 400 内含 status 502，是面板报错看不懂的根源）；②课 12 拆出「升级与备份」独立知识点。P2×1：课 9 需拉 Loki 镜像，已标注待办。无阻塞项 |
| 2026-09-04 | 课 1《Grafana 是谁》 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | 0 | **实测三发现**：①`grafana.db` 92 张表、时序特征表 0 张，实锤「存配置不存时序」；②`meta.type` 随 `instant` 在 `numeric-multi`↔`timeseries-multi` 切换，而 `resultType` 与 Prometheus 原生 `vector`/`matrix` 一致 → 形态由后端决定；③20 核 CPU 返回 20 个独立 frame 而非宽表（课 5 伏笔）。**P1×2 已修**（回读原文确认真缺陷）：错字「影呏」→「影响」；「range 仅 3 个样本点」补环境瞬时状态说明 + `count_over_time` 判据。**P2×1 已修**：插件自作主张（传 intervalMs=15000 → calculatedMinStep=20000）。**环境坑×2 入讲义**：9091 被 pushgateway 占且对 `/-/ready` 也返 200（只看状态码的探测假成功，判据已升级为身份校验）；13.2.1 首启联网装五个插件约 60 秒（等待循环已延至 180 秒）。全程真跑，30 条链接零死链 |
| 2026-09-04 | 课 3《变量与 Dashboard 组织》 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | 0 | **两个推翻性实测发现**：①**面板 `timeFrom` 是前端概念、后端不认**——三重取证（API 读回确认持久化／后端查询步长对照／数据库直查）：dashboard 窗口 6h 时，声明 `timeFrom='10m'` 的面板 B 与跟随 dashboard 的面板 A 步长**同为 20000ms**，而 10m 窗口基准应为 1000ms。②**自定义变量与内置变量插值位置不同**——探针抓包实测后端真实 HTTP 报文：`$__rate_interval` 已被替换为 `1m0s`，`$host` **原样透传**（body 中 `%24host`）。**多值写法实测**：`=~` → 3 帧 3 点；误用 `=` → 1 帧 0 点（静默无数据，不报错）。**P1×3 已修**：`timeFrom` 补三重取证；3 台 node-exporter 来源补环境说明；Prometheus 未开 lifecycle 已标注原因。**死链 5 条已修**（与课 2 同坑）：根目录链接 `../../`→`../../../`；课 4 链接 `../2-查得到/`→`../../2-查得到/`。**脚本缺陷×3、未改文档**：探针日志路径容器内外不共享／`bisect.py` 与 Python 标准库冲突致循环导入／转发探针未处理 gzip 致 500。**校验脚本缺陷 1 项已修**：B3 用 `if grep -q` 判空，未匹配时也报 ✅，与 B2 死链报告自相矛盾 → 改显式判定。**环境变更跨课有效**：新增 `grafana-node2`/`grafana-node3`；PromLab 加 `Accept-Encoding: identity` 绕开 gzip |
| 2026-09-04 | 课 4《查询编辑器与数据源协议》 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | 0 | **P0×1 已修（本轮最重要）**：命令续行写法——正文用两行 bash 风格反斜杠续行，读者 shell 是 PowerShell（续行符为反引号非反斜杠）。实测 `stderr: bash: line 3: \\: command not found`，12 条命令全部无法执行。对照课 3 为 15/15 单行，课 4 写成 12/12 两行，属偏离规范。已改单行并实测复跑通过。**实测三发现**：①**`editorMode` 是纯前端字段**——写入 `bogus-mode` 也被后端原样保留（不校验/不改写），13 种表达式 × 2 种模式 = 26 次往返全部逐字节一致；②**13.2.1 实为两模式 + Explain 开关**——前端代码证据：`"code"` 6 次、`"builder"` 1 次、`"explain"` 模式字面量 **0 次**，而 `explainHandler`/`Explainer` 存在；文档 9.x 三态 vs 10.x+ 两态，PR #53062 明确改过措辞；③**抓包坐实第 ③ 跳**——`User-Agent: Go-http-client/1.1`（Go 后端非浏览器）、`Content-Type: x-www-form-urlencoded`（与第 ② 跳 JSON 不同）、`$__rate_interval`→`1m0s`、`$host` 原样透传。**后端自作主张**：`intervalMs=1000` 被无视（仍 30000）；时间窗 5m→24h 步长 15s→900s。**4.3 复现**：后端不可达/语法错外层均 400，内层分别 502/400；语法错 frames=1（空）vs 后端不可达 frames=0，可区分「没发出去」与「发出被拒」；指标名不存在为 200+200+0 点（静默）。**评审判真伪 1 次**：首轮前端代码搜索全 0 命中，系脚本路径错误（插件在 `data/plugins-bundled/` 非 `public/plugins/`）。**A3 报「4.1 有 7 项」为误报**：六要素齐全，多出的是额外示例小节。**跨课发现 2 项**：数据源配的 `Accept-Encoding: identity` 被后端覆盖为 gzip；插件版本 13.1.7 独立于主体 13.2.1 |
| 2026-09-04 | 课 5《Transformations》 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | 0 | **课 4 P0 未复发**：命令全单行（14 处），代码块外 0 续行。**决定性实测：Transform 在【前端】执行**——三重取证：①API 层——三种塞法（顶层/query 内/单数 key）后端全无视，返回皆 60 帧且数值一致，响应顶层只有 `results`；②存储层——配置持久化且逐字节回读，但挂在 **panel** 上非 target 上；③代码层——`transformations` 在主程序 828 次，插件仅 9 次。**与课 4 `editorMode` 同一模式**：存后端、跑前端、后端不校验（瞎编 id 也被接收）。**5.2**：60 帧 = 20(cpu)×3(instance)，解开课 1 伏笔；merge 实测 A/B 两 key 不自动合并。**5.3 量化**：甲 3720 字节 vs 乙 75842 字节 = **20.4 倍**；数值差 **0.0556**（聚合顺序不可交换）。**自我纠错 1 项**：初稿「reduce 与 `avg()` 类似」经实测推翻——`avg()` 压【序列维】（每帧仍 21 点），`reduce` 压【时间维】（21→0）。**评审判真伪 3 次**：①`JSONDecodeError` 系取错列；②「value 是二进制」系再次误判（首字节 `7b` 是明文 JSON，活 dashboard 在 `resource_history`）；③`group` 是 SQL 保留字致语法错误。**P1 已修 4 项** |
| 2026-09-04 | 课 6《变量进阶与动态仪表盘》 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | 0 | **课 4 P0 未复发**：命令全单行（13 处），代码块外 0 续行。**6.1**：四种类型保存后 `options=0`（前端现查现填）；链式变量读回 `$job` 仍是字面量（前端替换的递归）。**6.2 核心认知升级：变量不是填空，是拼正则**——`$host` 默认逗号分隔，多值必须 `${host:regex}`；实测逗号 → 1 帧 0 点（空帧），正则 → 2 帧 42 点；`=` 与 `=~` 单值都正常、多值逗号都失败。**All 陷阱**：`.*` 得 4 帧而 `(a|b|c)` 得 3 帧，多出 Prometheus 自己（`localhost:9090`）——是实打实的数据差异非语义差异。**基数爆炸**：3×20×8=480 序列，单台单 mode → All 全 mode 增长 **21.8 倍**（25525→556265 字节），100 台推算约 726 倍。**三类失败模式**：F1 静默无数据(200/1 空帧/无 error)、F2 语法错(**400**/1 空帧/bad_data)、F3 变量未替换(200/1 空帧/无 error，与 F1 无法区分)、F4 数据源不存在(**404**/**0 帧**)。**6.3**：Repeat **前端展开**（变量 3 取值读回仍 1 panel）；`no_such_var` 也被接受；Row 也支持 repeat；**N 份 = N 次独立查询不合并**（1/3/10 面板 → 76177/228531/761770 字节）。**跨课收束（本课最有价值）**：`editorMode`(课4)/`transformations`(课5)/`repeat`(课6) 三者同为【存后端、跑前端、后端不校验】→ 升格为 Grafana 通用设计原则。**自身预判被实测推翻 2 处**：①空帧连字段结构都没有（字段数=0），原以为「有字段无数据行」；②`.*` 与 `(a|b|c)` 原以为相同，实测不同。**评审判真伪 3 次，其一为误判课 4**：正文初稿称「修正课 4 说语法错 0 帧」，回读课 4 第 422 行发现**课 4 写的就是 `1（空）`，课 4 本来正确，是我记错**——已改为「澄清+新增」并保留误判记录。本课真增量是 F4（uid 不存在 → 404/0 帧），与课 4 测的「后端进程不可达」（400/0 帧）帧数同、状态码不同 |
| 2026-09-04 | 课 7《告警架构：规则在哪求值、状态怎么迁移》 | 主 agent 内联（pedagogy + learner，子 agent 未创建，独立性受限） | 0 | **课 4 P0 未复发**：命令全单行（14 处），代码块外 0 续行。**7.1**：三条证据证明求值在【Grafana 内部】——Prometheus 侧 0 规则组且无 `rule_files`、状态存 Grafana annotations（Prometheus 侧 `GRAFANA_ALERTS`=0）、内嵌 Alertmanager。代码级证据：条件判断用数据源类型 **`__expr__`**。`folderUID` 强制（已建 `l07alerts`）。**7.2（核心）**：`for` 是考勤不是闹钟。实测 `for=20s`+`interval=60s` → **Normal→Pending 55.1s、Pending→Alerting 60.1s、Alerting→Normal 仅 10.0s**。实际延迟=⌈for/interval⌉×interval；恢复不受 for 约束（不对称是设计）。状态从 **`/metrics`** 读（ruler API 读不到，踩坑两次），暴露 **6 种** state。**7.3**：合法取值各 4 种，**拼写是 `KeepLast` 非 `KeepLastState`**。**修正概览 2 处**：①provisioning API 无默认值、两字段必填；②**真实宕机不是 No Data**——`up` 返回 0（序列仍在）走 Alerting 分支，停容器实测 `nodata=0`，两组配置行为一致。故 `up` 类指标配 `noDataState` 无效。状态标签是「**状态 (原因)**」如 `Alerting (NoData)`，排查须看 annotations。**评审判真伪 3 次（2 次自我纠错）**：`noDataState=OK` 首测成 alerting（仅等 5s），清理+等 70s 归零重测确认是 normal，首次为**上一条规则残留**；「机器宕机=No Data」被实测推翻。**新增方法约束**：告警离散采样 60s，测量前必须清理规则并等状态归零 |
| 2026-09-07 | 课 9 | 主 agent 内联 | 0 | P1×3 已修（detected_level 放大 stream / 时间窗 0 行易误读 / exemplar 不校验缺证据） | detected_level 内容推断（3 同标签日志→2 stream）、exemplar Content-Type 陷阱（text/plain→down, openmetrics→up） |
| 2026-09-07 | 课 10 | 主 agent 内联 | 0 | P1×3 已修（阶段 overview 的 version 表述实为 allowUiUpdates / allowUiUpdates=true 初稿结论不完整 / 文件夹冲突未给规避办法）。**三个反转直觉**：①alerting provisioning 写错=进程崩溃（非跳过）；②allowUiUpdates=false 是硬拒 400 而非"保存后覆盖"，true 能存但重启被覆盖，同实例改配置有数据库残留；③dashboards 用 folderUid、alerting 用 folder(名字) 产生两个同名文件夹。**10.3 量化**：顶层仅 15 字段，只回填 id+version，剥离后两实例 diff 0 行。判为脚本缺陷未改文档×3（转义/吞$/||）。自我纠错 1 处 |
| 2026-09-07 | 课 11 | 主 agent 内联 | 0 | P1×3 已修（阶段概览「文件夹权限 vs dashboard 权限」表述有误 / `folderUid` 位置误判差点写成权限失效 / `hasAcl` 差点当判据）。**三个反转直觉**：①Team 成员资格不带权限（dave 干净对照）；②`hasAcl` 恒 false（表里无此列）；③文件夹 Edit 顺带授予 dashboards:create。**核心产出＝两道闸门模型**：角色给动作权限、文件夹给资源范围，两种 403 错误信息不同可定位。**11.3**：token 明文仅一次、API Key 接口已移除但表还在。判为脚本/环境问题未改文档×4。自我纠错 2 处（均靠干净用户 dave 对照）。**课 4 P0 复发 1 次已修**：bash 块内 15 处续行已合并 |
### 课 8《告警规则与通知策略实战》（2026-09-04 周四）

- **评审方式**：pedagogy + learner 双视角，逐条回读原文核验（非凭记忆判断）
- **P0 阻塞项**：无
- **P1 已处理**：
  1. `keep_firing_for` 与 `recovering` 未经实测 → 已加 ⚠️ 未实测声明 + 实验记录坑七
  2. 三个时间参数的区分 → 已补三组隔离实验，推断模型标注 ⚠️ 边界
- **数据一致性核验**（正文 vs 实测输出，全部吻合）：
  - classic→1 条 / reduce→3 条 ✅
  - `group_by=[alertname]`→2 条通知 / `[alertname,instance]`→6 条通知 ✅
  - 静默期间通知 0 条但 alerting=3 ✅
  - repeat=1m→90s / repeat=40s→60s ✅
  - contact point 13 可用 / 7 缺参数 ✅
- **硬约束**：bash 代码块 0 处反斜杠续行（课 4 P0 未复发）
- **结论**：交付合格，可进入课 9
