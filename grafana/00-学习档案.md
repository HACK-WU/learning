# Grafana 学习档案

> 断点续学依据：进度表 + 大纲调整记录 + 评审记录。

## 学习者画像

| 项 | 值 |
|---|---|
| 主题 | Grafana 系统学习 |
| 当前水平 | 入门 |
| 学习目标 | 动手实操 |
| 输出格式 | Markdown（Mermaid 默认，复杂图 SVG） |
| 产物目录 | `D:/projects/learning/grafana/` |
| 建档日期 | 2026-09-04 |
| 已有基础 | Prometheus / PromQL / VictoriaMetrics / OpenTelemetry 已结课 |

## 故事主线

- **主角**：一张 dashboard
- **冲突**：指标、日志、链路散在三个系统，出事要开三个控制台、手工对齐时间戳
- **收束**：同一时间窗口内指标 → 日志 → 链路三级下钻，不再手工换算时间

---

## 知识点级进度表

### 阶段 1：看得见

| # | 知识点 | 所属课 | 状态 |
|---|--------|--------|------|
| 1.1 | Grafana 的定位：查询与展示层，不存数据 | 课 1 | ✅ 已完成 |
| 1.2 | 数据源插件模型：data plane 与 query 协议 | 课 1 | ✅ 已完成 |
| 1.3 | 面板查询的三种数据形态（time series / table / 单值） | 课 1 | ✅ 已完成 |
| 2.1 | 安装与初始化：容器启动、管理员口令、health 接口 | 课 2 | ✅ 已完成 |
| 2.2 | 加第一个 Prometheus 数据源并验证连通 | 课 2 | ✅ 已完成 |
| 2.3 | 第一个 Panel：查询、图例、单位、阈值 | 课 2 | ✅ 已完成 |
| 3.1 | Dashboard 与 Panel 的关系：时间选择器是共享的 | 课 3 | ✅ 已完成 |
| 3.2 | 变量入门：`$host` 从哪来、怎么注入查询 | 课 3 | ✅ 已完成 |
| 3.3 | Row 与折叠、面板复用与 JSON Model 初见 | 课 3 | ✅ 已完成 |

### 阶段 2：查得到

| # | 知识点 | 所属课 | 状态 |
|---|--------|--------|------|
| 4.1 | 查询编辑器三态：Code / Builder / Explain | 课 4 | ✅ 已完成 |
| 4.2 | 一次查询的完整旅程：前端 → 后端代理 → 数据源 | 课 4 | ✅ 已完成 |
| 4.3 | 状态码与真实错误的分离：HTTP 400 里的 status 502 | 课 4 | ✅ 已完成 |
| 5.1 | Transformations 的定位：查出来之后、画出来之前 | 课 5 | ✅ 已完成 |
| 5.2 | 常用 Transform：合并、归约、按字段分组、重命名 | 课 5 | ✅ 已完成 |
| 5.3 | Transform 与查询的分工边界 | 课 5 | ✅ 已完成 |
| 6.1 | 变量类型：Query / Custom / Interval / 链式依赖 | 课 6 | ✅ 已完成 |
| 6.2 | 多值与 All 值：查询里怎么写才不出错 | 课 6 | ✅ 已完成 |
| 6.3 | 动态仪表盘：模板化 + 重复面板（Repeat） | 课 6 | ✅ 已完成 |

### 阶段 3：叫得醒

| # | 知识点 | 所属课 | 状态 |
|---|--------|--------|------|
| 7.1 | 统一告警架构：规则求值在 Grafana 内部 | 课 7 | ✅ 已完成 |
| 7.2 | 告警状态机：Normal → Pending → Firing → Resolved | 课 7 | ✅ 已完成 |
| 7.3 | No Data 与 Error：告警的第三种、第四种状态 | 课 7 | ✅ 已完成 |
| 8.1 | 告警规则三要素：查询、条件、评估行为 | 课 8 | ✅ 已完成 |
| 8.2 | 通知策略树与静默：告警怎么路由到人 | 课 8 | ✅ 已完成 |
| 8.3 | 分组与抑制：为什么一条故障只该响一次 | 课 8 | ✅ 已完成 |
| 9.1 | Loki 数据源与 LogQL 入门 | 课 9 | ✅ 已完成 |
| 9.2 | 从指标到日志：时间窗对齐与下钻链接 | 课 9 | ✅ 已完成 |
| 9.3 | 链路下钻：Jaeger 数据源与 exemplar | 课 9 | ✅ 已完成 |

### 阶段 4：管得住

| # | 知识点 | 所属课 | 状态 |
|---|--------|--------|------|
| 10.1 | Provisioning 的三类文件：datasources / dashboards / alerting | 课 10 | ✅ 已完成 |
| 10.2 | Dashboard as Code 与 UI 改动的冲突处理 | 课 10 | ✅ 已完成 |
| 10.3 | JSON Model 结构与可 diff 化 | 课 10 | ✅ 已完成 |
| 11.1 | Org / User / Team 三层模型 | 课 11 | ✅ 已完成 |
| 11.2 | RBAC 与文件夹权限 | 课 11 | ✅ 已完成 |
| 11.3 | 服务账号与 API Key：程序怎么安全访问 | 课 11 | ✅ 已完成 |
| 12.1 | 性能：面板数量、查询并发与渲染压力 | 课 12 | ✅ 已完成 |
| 12.2 | 数据库后端与高可用部署 | 课 12 | ✅ 已完成 |
| 12.3 | 升级与备份：升级前查什么、回滚靠什么 | 课 12 | ✅ 已完成 |

**进度统计**：36 / 36 知识点（阶段 1：9/9 ✅已完成，阶段 2：9/9 ✅，阶段 3：9/9 ✅，阶段 4：9/9 ✅）**全部结课**

---

## 大纲调整记录

| 日期 | 调整内容 | 原因 |
|------|----------|------|
| 2026-09-04 | 建档：4 阶段 12 课 36 知识点 | 用户确认采用默认画像（入门 + 动手实操 + 指标/日志/链路三级下钻主线） |

---

## 评审记录

| 日期 | 评审对象 | 评审方式 | P0 数 | 意见摘要 |
|------|----------|----------|-------|----------|
| 2026-09-04 | 大纲评审（Phase 1） | 主 agent 内联（pedagogy，子 agent 未创建，独立性受限） | 0 | P1×2 已采纳：①课 4 新增知识点 4.3「状态码与真实错误的分离」（源自本机实测 HTTP 400 内含 status 502）；②阶段 4 课 12 拆出「升级与备份」独立知识点，因 Grafana 升级是高频运维动作且与 VM 课 12 的备份恢复形成呼应。P2×1：课 9 的 Loki 依赖新镜像，已标注待拉。无阻塞项 |
| 2026-09-04 | 课 1《Grafana 是谁》 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | 0 | **三个高价值实测发现**：①拷出 `grafana.db` 用 python sqlite3 读表清单，92 张表按 `sample\|chunk\|block\|series\|metric\|tsdb\|wal\|index` 搜索**零命中**，实锤「存配置不存时序」；②同一表达式仅改 `instant` 参数，`meta.type` 在 `numeric-multi`↔`timeseries-multi` 间切换，而 `custom.resultType` 与 Prometheus 原生返回的 `vector`/`matrix` 一致——证明**形态由后端决定、Grafana 只加类型标注**；③20 核 CPU 的 `rate(node_cpu_seconds_total{mode="idle"}[2m])` 返回**20 个独立 frame 而非一张宽表**，这是 Table 面板「不听话」的根源（课 5 伏笔）。**P1×2 已修**：①pedagogy 报正文错字「不影呏」→「不影响」（回读原文确认真缺陷）；②learner 报第四幕「range 只返回 3 个样本点」未说明是环境瞬时状态，读者隔天重跑会看到 15 个点与讲义不符 → 已补环境说明与 `count_over_time` 判据。**P2×1 已修**：插件「自作主张」实测——传 `intervalMs=15000`，Prometheus 插件算出 `calculatedMinStep=20000`，已在知识点 1.2 类比失效边界处点明。**环境坑×2 已入讲义**：9091 端口被 pushgateway 占用（且 pushgateway 对 `/-/ready` 也返 200，导致「只看状态码」的探测假成功）；Grafana 13.2.1 首次启动联网装五个插件约 60 秒，60 秒等待循环误判未就绪。**未实测项已标注**：`access=direct` 的 CORS 行为课 2 展开 |
| 2026-09-04 | 课 2《第一个面板》 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | 0 | **推翻课 1 一处错误归因（本课最大收获）**：分离变量实测证明 `/api/health` **6 秒**就绪（非 60 秒），5 个插件在 t+11s~t+19s 才陆续装完——**插件装完晚于 health 就绪 13 秒，不阻塞可用性**。课 1 判定的「装插件慢 60 秒」为误判，真根因是探测用了紧凑写法 `grep '"database":"ok"'`，而 Grafana 实际返回**带空格**的 `"database": "ok"`——**判据恒假**。若按「等待时长不够」去改，会把循环从 30 次加到 90 次却永远修不好。已修 `l00-env-up.sh`（`tr -d ' \n'` 压平后匹配），并写 `l02-readyfix.sh` 验证：旧判据 ❌／新判据 ✅。**新增实测发现×3**：①direct 模式报错 `dial tcp [::1]:9201` 证明 **Grafana 后端仍自己发请求**，direct 并非「浏览器绕过后端直连」，该类比失效边界由实测支撑；②CORS 对照——Grafana 对外站 Origin 返 **403**，Prometheus 返 200 且**回显** Origin（宽松）；③unit 改为 bytes 后后端返回值**完全一致**，证明单位是前端格式化。**P1×3 已修**：①`l00-env-up.sh` 注释「最多 60 秒」与实际 90×2s=180 秒不符；②uid 论证缺「不可变性」这一关键前提；③正文用了 5 个端口（3001/3011~3014）却未解释切换原因（各实验需全新实例以避免状态污染），已补端口对照表。**评审中两次判真伪**：①CORS 实验首轮三项全 401 → 系脚本漏带 cookie，非 CORS 结论，补 cookie 重测得 403/200；②实验 C 断言报「不一致」→ 系比较了含时间戳的数组（时间戳每次都变），改比数值字段后为「完全一致」。两次均为**脚本缺陷，未改文档** |
| 2026-09-04 | 课 3《变量与 Dashboard 组织》 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | 0 | **两个推翻性实测发现**：①**面板 `timeFrom` 是前端概念，后端不认**——三重取证（API 读回确认持久化 + 后端查询步长对照 + 数据库直查）：dashboard 窗口 6h 时，声明 `timeFrom='10m'` 的面板 B 与跟随 dashboard 的面板 A 步长**同为 20000ms**，而 10m 窗口基准应为 1000ms。结论：`timeFrom` 只裁剪显示、不改取数窗口。②**自定义变量与内置变量的插值位置不同**——探针抓包（在 Grafana 与数据源之间放记录型代理）实测：后端真正发出的 HTTP 报文里，`$__rate_interval` 已被替换成 `1m0s`，而 `$host` **原样透传**（body 中为 `%24host`）。即自定义变量由**前端**替换、内置时间变量由**后端**替换。直接调 `/api/ds/query` 传 `scopedVars` 不生效（放 query 里、放顶层 payload 里均试过），这正是"用 API 查不出数据"的根因。**多值写法实测**：`up{instance=~"$host"}` → 3 帧 3 点；误用 `=` → 1 帧 **0 点**（静默无数据，不报错）。**P1×3 已修**：①`timeFrom` 初稿仅单一接口证据，已补三重取证；②3 台 node-exporter 来源未说明（课 2 交付时只有 1 台），已补环境说明块；③Prometheus 未开 lifecycle、改配置需重启，已在第四幕标注原因。**死链 5 条已修**（与课 2 同一个坑）：返回根目录链接写成 `../../` 应为 `../../../`（`lessons/` 距根三层）；课 4 链接写成 `../2-查得到/` 应为 `../../2-查得到/`。**评审中判定为脚本缺陷、未改文档×3**：①探针日志写在容器内 `/tmp` 而检查读宿主机 `/tmp`（路径不共享）→ 改 bind mount；②测试脚本命名 `bisect.py` 与 Python 标准库冲突致循环导入 → 重命名；③转发探针未处理 gzip 致 500 → 解压后转发即正常。**校验脚本自身缺陷 1 项已修**：B3 用 `if grep -q` 判空，未匹配时也报 ✅，与 B2 死链报告自相矛盾 → 改为显式判定存在性 + 单独检测两级误写。**环境变更（跨课有效）**：为讲"N 台机器"新增 `grafana-node2`(9102,hostname=node-alpha)、`grafana-node3`(9103,hostname=node-beta)，`prometheus.yml` 的 node job 改为 3 个 targets；另修 gzip 问题（Prometheus 返回 gzip 而本环境插件解压失败，导致空 frame 不报错），已给 PromLab 加 `Accept-Encoding: identity` 请求头。 |
| 2026-09-04 | 课 4《查询编辑器与数据源协议》 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | 0 | **P0×1 已修（本轮最重要）**：命令续行写法——正文原用两行 bash 风格反斜杠续行，而读者 shell 是 PowerShell（续行符为反引号非反斜杠）。实测粘贴后 `stderr: bash: line 3: \\: command not found`，12 条命令全部无法执行。对照课 3 为 15/15 单行写法，课 4 写成 12/12 两行，属偏离既有规范。已统一改单行并实测复跑通过。**三个实测发现**：①**`editorMode` 是纯前端字段**——写入 `bogus-mode` 也被后端原样保留（不校验、不改写），13 种表达式 × 2 种模式 = 26 次往返全部逐字节一致；②**13.2.1 实为两模式 + Explain 开关**——前端代码层证据：`/usr/share/grafana/data/plugins-bundled/prometheus/module.js` 中 `"code"` 6 次、`"builder"` 1 次、`"explain"` 作为模式字面量 **0 次**，而 `explainHandler`/`Explainer` 存在；文档口径 9.x 三态 vs 10.x+ 两态，PR #53062 明确改过措辞；③**抓包坐实第 ③ 跳**——记录型代理抓到 `POST /api/v1/query_range`，`User-Agent: Go-http-client/1.1`（Go 后端发，非浏览器）、`Content-Type: application/x-www-form-urlencoded`（与第 ② 跳 JSON 不同）、`$__rate_interval`→`1m0s`、`$host` 原样透传。**后端自作主张实测**：`intervalMs=1000` 被无视（仍得 calculatedMinStep=30000）；时间窗 5m→24h 步长 15s→900s。**4.3 复现**：后端不可达/语法错外层均 HTTP 400，内层分别 502/400；语法错 frames=1（空）而后端不可达 frames=0，可区分「没发出去」与「发出被拒」；指标名不存在为 HTTP 200 + 内层 200 + 0 点（静默失败）。**评审判真伪 1 次**：首轮前端代码搜索全 0 命中，系脚本路径错误（插件在 `data/plugins-bundled/` 非 `public/plugins/`），非功能不存在。**A3 校验报「4.1 有 7 项」经查为误报**：六要素齐全，多出的是额外示例小节。**跨课发现 2 项**：数据源配的 `Accept-Encoding: identity` 被后端覆盖为 gzip（抓包实测）；内置 Prometheus 插件版本 13.1.7 独立于 Grafana 主体 13.2.1 |
| 2026-09-04 | 课 5《Transformations》 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | 0 | **课 4 P0 未复发**：命令全部单行（14 处），代码块外 0 处反斜杠续行，PowerShell 兼容已验证。**决定性实测：Transform 在【前端】执行**——三重取证：①API 层——给 `/api/ds/query` 塞 `transformations`（顶层 / query 内 / 单数 key 三种塞法）后端**全部无视**，返回皆 60 帧且数值完全一致，响应顶层字段只有 `results`（后端根本不接这个参数）；②存储层——配置**确实持久化**且逐字节回读一致，但挂在 **panel** 上而非 **target**（query）上；③代码层——`transformations` 在主程序 `/usr/share/grafana/public/build/*.js` 出现 **828 次**，而 Prometheus 插件仅 9 次（且是引用非实现）。**与课 4 的 `editorMode` 同一模式**：存后端、跑前端、后端不校验——连瞎编的 `bogus-transform-xyz` 也被原样接收回读。**5.2 量化**：60 帧 = 20(cpu) × 3(instance) × 1(job) × 1(mode)，解开课 1 埋的伏笔；merge 场景实测两查询返回 `results` 里 A/B 两个独立 key **不会自动合并**。**5.3 分工边界量化**：甲（PromQL `avg by`）3 帧/63 点/**3720 字节** vs 乙（原始 + 前端 reduce）60 帧/1260 点/**75842 字节** = **20.4 倍**流量差；且两者**数值差 0.0556**——因为【聚合顺序不可交换】：甲 = avg over cpu 再 mean over time，乙 = mean over time 再 avg over cpu。对看趋势不重要，对**设阈值告警很重要**。**自我纠错 1 项（如实记录）**：初稿写「reduce 与 `avg()` 效果类似」经实测**推翻**——`avg()` 压的是【序列维】（每帧仍 21 点，能画时序图），`reduce` 压的是【时间维】（时刻数 21→0）。已补 `l05_probe_reduce_semantics.py` 并在正文记录纠错过程。**评审中判真伪 3 次**：①数据库层取证 `JSONDecodeError` → 脚本取错列（命中 `name` 而非 `value`）；②续报「value 是二进制格式」→ **再次误判**，实测首字节 `7b`(`{`) 是明文 JSON，只是存的是 CheckType 等内置资源，活 dashboard 在 `resource_history`(79 行) 而非 `resource`(13 行)；③`SELECT ... group as grp` 报语法错误 → `group` 是 SQL 保留字。**方法论结论**：不要直接读 Grafana 数据库验证 dashboard，可靠判据是 API 读回。**P1 已修 4 项**：reduce/avg 维度区分、两步 reduce 表述精确化、耗时数据降级为定性参考、60 帧数字标注依赖机器核数 |
| 2026-09-04 | 课 6《变量进阶与动态仪表盘》 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | 0 | **课 4 P0 未复发**：命令全单行（13 处），代码块外 0 续行。**6.1 实测**：四种类型（Query/Custom/Interval/Datasource）保存后 `options 数量 = 0`——取值由【前端】现查现填，后端只存定义（与课 5 Transform 同一模式）；链式变量读回时 `$job` **仍是字面量** → 确认前端替换，链式依赖即前端替换的【递归】。**6.2 核心（本课最重要认知升级：变量不是填空，是拼正则）**：`$host` 默认逗号分隔，多值必须 `${host:regex}`（或 `:pipe`）。实测 `${host}` 多值 → **1 帧 0 点（空帧）**，`${host:pipe}`/`${host:regex}` → **2 帧 42 点**。`=` 与 `=~` 单值都正常（21 点）、多值逗号都失败（0 点），只有正则格式才行。**All 陷阱**：`.*` 得到 **4 帧** 而 `(a|b|c)` 只有 **3 帧**——`.*` 多匹配出 Prometheus 自己（`localhost:9090`），是【实打实的数据差异】非语义差异。**基数爆炸量化**：3 台×20 核×8 mode=480 序列；单台单 mode → All 全 mode 增长 **21.8 倍**（25525→556265 字节），100 台机器推算约 **726 倍**。**三类失败模式判据**：F1 静默无数据（200/1 空帧/无 error）、F2 语法错（**400**/1 空帧/bad_data）、F3 变量未替换（200/1 空帧/无 error，与 F1 无法区分，只能看 expr 里有无 `$`）、F4 数据源不存在（**404**/**0 帧**）。**6.3**：Repeat **前端展开**——变量 3 取值时 API 读回仍 **1 个 panel**；`repeat="no_such_var"` 也被接受（后端不校验）；Row 也支持 repeat 且子面板跟着复制；**N 份 = N 次独立查询不合并**（1/3/10 面板 → 76177/228531/761770 字节）。**跨课收束（本课最有价值发现）**：`editorMode`(课4) / `transformations`(课5) / `repeat`(课6) 三者同为【**存后端、跑前端、后端不校验**】——升格为 Grafana 通用设计原则。**预判被实测推翻 2 处**：①空帧【连字段结构都没有】（字段数=0），原以为「有字段无数据行」；②`.*` 与 `(a|b|c)` 原以为结果相同，实测不同（4 帧 vs 3 帧）。**评审判真伪 3 次，其中一次是误判课 4**：正文初稿称「修正课 4 说语法错 0 帧」，**回读课 4 第 422 行原文发现课 4 写的就是 `1（空）`，课 4 本来正确，是我记错**——已改为「澄清 + 新增」并在正文保留误判记录。本课本课真正的增量是 F4（datasource uid 不存在 → **HTTP 404/0 帧**），课 4 测的是「后端进程不可达」（HTTP 400/0 帧），两者帧数同、状态码不同。**教训固化**：若未回读原文就动手，会在修复 0 个真问题的同时引入 1 处新缺陷 |
| 2026-09-04 | 课 7《告警架构：规则在哪求值、状态怎么迁移》 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | 0 | **课 4 P0 未复发**：命令全单行（14 处），代码块外 0 续行。**7.1 统一告警架构**：三条证据证明求值在【Grafana 内部】——①Prometheus 侧 `/api/v1/rules` 规则组数=0 且配置无 `rule_files`；②状态存 Grafana 自己的 annotations（`stateHistory.backend=annotations`，Prometheus 侧 `GRAFANA_ALERTS` 序列数=0）；③Grafana 内嵌 Alertmanager（`/api/alertmanager/grafana/`，`cluster.status=disabled`）。代码级证据：规则的 `data` 含两条查询，`refId=B` 的 datasource 是 **`__expr__`**（Grafana 内部表达式引擎），条件判断不交给 Prometheus。**坑**：Grafana 13 强制 `folderUID`（空串报 400），已建 `l07alerts` 文件夹；重复建同 uid 文件夹报「the folder has been changed by someone else」（误导性错误，实为已存在）。**推论**：Grafana 挂→告警不响；Prometheus 挂→进 Error；node 挂→视配置进 Alerting 或 NoData。**7.2 状态机（核心：for 是考勤不是闹钟）**：用 `vector(0)`/`vector(1)` 配 `last()<1` 做可控开关精确测量。实测 `for=20s`+组 `interval=60s` 时：**Normal→Pending 55.1s、Pending→Alerting 60.1s**、**Alerting→Normal 仅 10.0s**。结论：实际告警延迟 = **⌈for/interval⌉ × interval**，`for<interval` 时无意义；**恢复不受 for 约束**（下次求值即恢复），故 10s vs 60s 的不对称是设计使然（出事防抖动、恢复要快）。`minInterval=10s` 只是下限，实际组默认 **1m**。**状态读取路径踩坑两次**：ruler API 按旧版 `{data:{groups:[]}}` 解析读不到（Grafana 13 顶层即 `{namespace:[groups]}`）；修正后 `grafana_alert` 里**根本没有 state 字段**。最终正确读法是 **Grafana `/metrics` 的 `grafana_alerting_alerts{state=...}`**，且暴露 **6 种**状态（alerting/error/nodata/normal/pending/recovering），比档案写的 4 种多；`recovering` 本轮 7 次实验**未触发**，行为待课 8 补测。**7.3 No Data 与 Error**：合法取值各 4 种——`NoData`/`Alerting`/`OK`/`KeepLast`（NoData 侧）、`Error`/`Alerting`/`OK`/`KeepLast`（Error 侧）。实测映射：NoData→nodata、Alerting→alerting、OK→normal、Error→error。**拼写修正**：正确是 **`KeepLast`**，不是 `KeepLastState`（后者 400，穷举 10 个候选确认）。**修正阶段 3 概览 2 处**：①「默认是保持上一个状态」——provisioning API **无默认值、两字段必填**（不传报 400），UI 路径 `/api/ruler` 才接受空值；②**真实宕机不是 No Data**——停 `grafana-node3` 后 Prometheus 的 `up` 返回 **0** 而非无数据，序列仍在，**走 Alerting 分支**，两组配置（KeepLast 与 NoData）行为完全一致（都 alerting，`nodata=0`）。**故 `up` 类指标配 `noDataState` 无效；真正会消失的是业务指标**（如 `sum(rate(http_requests_total[5m]))`在零流量时序列消失），那里才必须显式配 `noDataState=Alerting`。**状态标签真实写法**：annotations 里是「**状态 (原因)**」，如 `Alerting (NoData)`、`Alerting (Error)`、`Normal (Error)` —— 状态与原因是两个独立维度；`/metrics` 只给状态不给原因，排查「真告警 vs 查不到数据」**必须看 annotations**。**评审判真伪 3 次（2 次为自我纠错）**：①状态读取路径两次失败才找对；②`noDataState=OK` 首测成 alerting（仅等 5s 不足一轮求值），**起疑后清理规则+等 70s 归零重测**，确认正确是 normal，首次为**上一条规则残留**；③「机器宕机=No Data」是错的，实测推翻开课预设。**新增方法约束**：告警是离散采样（60s 一轮），每次测量前**必须清理规则并等状态归零**（本课 65-70s）。**跨课收束**：`editorMode`(课4)/`transformations`(课5)/`repeat`(课6)/**告警求值**(课7) 同属「Grafana 把活揽到自己手里」，前三次前端干、本次整个子系统自己干，代价是 Grafana 从「挂了看不了图」变成「挂了收不到告警」 |
| 2026-09-07 | 课 11《权限与服务账号》 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | 0 | **三个反转直觉**：①**Team 成员资格不带任何权限**——干净用户 dave 加入 TeamBGroup 前后权限列表完全一致（均 `dashboards:read, folders:read`），权限只来自「Team + 资源 + 级别」授权记录；②**`hasAcl` 恒为 false 已不可信**——给了 Edit 权限后 `/permissions` 有记录但 `hasAcl=false`，追到数据库发现 **`folder` 表根本没有 `has_acl` 列**，属遗留计算字段；③**阶段概览「给了文件夹权限忘了给 dashboard 权限」说法有误**——实测文件夹 Edit 会**顺带授予** `dashboards:create`（dave 从 2 个权限变 8 个），真正的漏配是「只给 Viewer 角色又想让人改」。**两道闸门模型**（本课核心）：角色给「动作权限」、文件夹给「资源范围」，都要过；两种 403 错误信息不同可据此定位——`Permissions needed: dashboards:create` = 第一道（角色不够），`folders.folder.grafana.app "teamb" is forbidden: access denied to folder` = 第二道（文件夹未授权）。**11.1**：跨 Org 读资源返回 **404**（装作不存在，非 403）；新建用户默认角色 **Viewer**；角色是 **User × Org 二元关系**；建 Team 时创建者自动成 Team Admin（`permission: 4`）。**11.3**：服务账号 token **明文只出现一次**（只存哈希，列接口无 `key` 字段）；**API Key 接口已全部移除**（`/api/auth/keys`、`/api/apikeys`、`/api/org/apikeys` 均 404），但 92 张表中仍保留 `api_key` 遗留表（1 行）——准确表述是「接口死了，表还在」；`service_account` 表已迁出传统表。权限真身在 `permission` 表，scope 用 **k8s 风格** `{group}/{resource}:{verb}`。**P1×3 已修**：阶段概览表述修正、`folderUid` 位置误判、`hasAcl` 差点当判据。**判为脚本/环境问题未改文档×4**：三个列表接口响应形态差异、RBAC 管理接口 404、服务账号 id 猜测错误（1→6）、建 token 时序。**自我纠错 2 处**：初判「carol 能写未授权文件夹=权限失控」实为她当时仍是 Editor（时序问题）；初判「Team 成员资格自带权限」实为授权记录作用。两次都靠**新建干净用户 dave** 对照纠正——课 10 沉淀的「先怀疑状态残留，起干净对照」纪律的第二次成功应用。**课 4 P0 复发 1 次已修**：bash 块内 15 处反斜杠续行，已合并为单行 |
| 2026-09-07 | 课 10《Provisioning》 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | 0 | **三个反转直觉的实测**：①**alerting provisioning 写错 = 整个 Grafana 崩溃**（非跳过）——`__dashboardUid__` 未配 `__panelId__` 致 `Exited (1)`，且 api.HTTPServer/DashboardUpdater 等全部级联失败（因依赖 provisioning 模块），与课 4/5/6/9「后端不校验」相反，根因是**启动期写入 vs 运行时写入**；②**`allowUiUpdates: false` 是硬拒 400**（`Cannot save provisioned dashboard`）而非「保存后覆盖」；`true`（干净实例）能保存（version 1→2）但**重启仍被文件覆盖**（→3，title 回滚）；同实例先 false 后 true 仍 400 = **数据库残留**，靠起干净实例 `grafana-prov2`(3003) 对照才定位，否则会写出「配置无效」的错误结论；③**dashboards 用 folderUid、alerting 用 folder(名字)**，产生两个同名 "Provisioned" 文件夹（provfolder 装 1 个 dashboard，dfxhsw8b57pxcd 空）。**10.3 精确量化**：顶层仅 15 字段，Grafana **只回填 id+version**（源文件 13→库中 15，panel.id 原样保留），剥离顶层 id+version 后两实例 JSON **逐字节相同（diff 0 行）**。`disableDeletion` 对照：false→删文件即删 dashboard（2→1）；true→留孤儿（保持 2）。`editable` 写入 → `readOnly` 读出。`updateIntervalSeconds=10` 热加载实测新增文件 14 秒自动出现。**P1×3 已修**：阶段 overview 原写「version 字段决定是否覆盖」实为 `allowUiUpdates`、allowUiUpdates=true 初稿结论不完整、文件夹冲突未给规避办法。**评审判为脚本缺陷未改文档×3**：l10-probe4.sh 转义致 $u 未展开、PowerShell 吞 $、PowerShell 5.1 不支持 `||`。**自我纠错 1 处**：初判 allowUiUpdates=true 无效，实为数据库残留。**课 4 P0 未复发**：命令全落盘为脚本，代码块外 0 续行 |
| 2026-09-07 | 课 12《性能、高可用与升级运维》 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | 0 | **三个反转直觉（全部实测支撑）**：①**面板数不是瓶颈**——N=1→80（80 倍）耗时仅 7ms→32ms（4.5 倍），亚线性且绝对值可忽略；②**时间窗拉长数据反而变少**——1h→7d（168 倍）响应体 9680→4093 字节，因 Grafana 自动放大步长（30s→2h）；③**慢在渲染不在查询**——API 查询 9ms vs PNG 渲染 4.9s，**500 倍**。**真正的性能开关是 `maxDataPoints`**：100→2000 时响应体 0.98MB→14.3MB（**14.5 倍**），由面板宽度决定。**12.2**：SQLite 并发写 10 次全 200，但日志实锤 **`SQLITE_BUSY` 重试至 retry=2、退避 199ms**（锁竞争被重试掩盖，用户无感）；SQLite 硬边界是**文件无法共享**（`grafana-lab` 无挂载）；换 Postgres 仅改环境变量、应用层零改动（91 表）；**HA 实测三结论**：配置双向可见、会话默认共享（pg1 的 cookie 访问 pg2 返 200，**无需配 Redis**）、迁移带锁幂等（第二实例 `performed=0, skipped=719`）。**12.3（本课最大价值）**：token 明文 `glsa_...` 落库变 32 位哈希 `e34d8990...`，**备份恢复不了明文**（坐实课 11 伏笔）；**降级实验**——12.0.0 打开 13.2.1 的库返回 `database: ok` 但 dashboard **404**，且**旧版继续改库**（migration_log 719→731 跑 12 条迁移）；根因是 13.x 用 **`resource` 表（unified storage）** 而 12.x 读老表，**双向不可见**（12.0.0 建的 `old-created`，13.2.1 读也 404）；**回滚＝恢复备份**（实测恢复到 719、dashboard 200）。**P1 已修**：死链 2 条（`../../` 应为 `../../../`，与课 2/3 同坑）。**评审判为脚本缺陷未改文档×1**：A3 报「12.2/12.3 缺六要素」经 grep 回读确认为**误报**（18 处六要素齐全，awk 区间正则缺陷，与课 4/10 同款）。**未实测已显式标注×4**：告警 HA 去重、12→13 正向迁移、SQLite 并发击穿阈值、完整 SQLite→PG 迁移演练。**课 4 P0 未复发**：代码块外 0 续行。**方法论**：健康 ≠ 业务正常（`database: ok` 但 404）；破坏性实验前必先 `pg_dump` |
| 2026-09-07 | 课 9《日志与链路》 | 主 agent 内联（pedagogy + learner 双视角，子 agent 未创建，独立性受限） | 0 | **两个高价值实测发现**：①**`detected_level` 从日志【内容】推断并放大 stream 数**——3 条标签完全相同的日志（仅文本不同）被拆成 2 个 stream（含 ERROR→error，含 DEBUG→unknown），推翻「stream 仅由标签唯一决定」的直觉，是 Loki 3.x 新风险点（日志格式改版可能致 stream 暴涨）；生产应显式打 level 标签。②**exemplar 的 Content-Type 陷阱**——exporter 返回 `text/plain` 时 Prometheus 把 `#` 当注释，target down 且 lastError=`got "#"`，改 `application/openmetrics-text` 后立刻 `health=up` 且 query_exemplars 拿到 traceID。**端到端闭环已验证**：指标桶 → exemplar(traceID) → Jaeger 2 个 span。**跨课收束第六次**：`exemplarTraceIdDestinations` 与 editorMode(课4)/transformations(课5)/repeat(课6) 同为「存后端、跑前端、后端不校验」——实测 PUT `bogus_trace_field`+`no-such-uid` 返回 200 且原样保存，配错不报错。**P1×3 已修**：detected_level 放大 stream 未点明、9.2 时间窗 0 行易误读为故障、exemplar 不校验缺回读证据。**评审判为脚本缺陷未改文档×4**：count 显示 15/35/30 实为打印数据点个数、targets 读取失败系 bash 引号嵌套、exemplar 初次误归因重复桶、LogQL 400 误以为代理错（实为 instant 不支持日志查询）。**自我纠错 1 处**：查 flags 用 `enableFeatures`（驼峰）得 None 险些误判「不支持 exemplar」，回读确认真实字段是 `enable-feature`，日志明写 enabled——**第三次验证「先验证再下结论」**。**环境坑×2**：docker -v 挂载时文件不存在会静默建成目录（须先写文件再起容器）；Jaeger OTLP 端口连撞 14317/24317（otelcol-lab06），最终用 44318；打到 UI 端口 16687 返回 200+HTML |
| 2026-09-07 | **Phase 5 三件套**（08-实战经验 / 09-排障速查手册 / 10-场景解法库） | 主 agent 内联（pedagogy + learner 双视角 + 证据纪律 B1-B8 / C1-C7，子 agent 未创建，独立性受限） | 0 | **三态分工不混**：08 学习态讲原理 / 09 使用态只给动作 / 10 设计态给权衡——写前读了 victoriametrics 与 celery-django 的同类产物确认体例。**素材全部来自真实讲义提取**：用 2 个脚本扫描 12 课的踩坑/误区/静默/实测数字，得到 322 行 + 279 行素材后再动笔，无凭记忆复述。**交付规模**：08 = **15 条故障模式**（五段式：症状→根因→正解→预防→手册链接）+ 6 类反模式 + 四组上线 Checklist（18 项）；09 = **15 条症状**按索引倒查，每条含 止血→定位→修复→若无效；10 = **10 个场景**，每个「🤔 先想一想」`<details>` 折叠 + 多解法对照表 + 代价 + 适用边界 + 知识点挂钩。**机器核查全通过**：链接 **135 条全部可达**（08×24 / 09×20 / 10×43 / final×48）；锚点 **0 问题**；代码围栏全偶数；关键实测数字（3063 / c57731cc / 2.71s+1.71s / 75842→3720 / 21.8 / 4.5 倍 / 2.6~4.3s / 18ms）三件套与反例对照、final 手册**逐条比对一致**。**P1×2 真缺陷已修**：①09 症状 14/15 的原理链接错指「故障模式 13 性能排查」（08 当时无对应模式）→ 为 08 **新补故障模式 14（降级不是回滚）与 15（token 静默 401）**，链接改正；②修复链接时 `replace_in_file` 的 old_string 把「症状 15」标题行一并替换掉，导致正文挂在症状 14 下（核查脚本报「索引有 15 但正文无 15」抓出）→ 已恢复标题并核实未引入错别字。**脚本误报×1 未改文档**：锚点校验首轮报 26 处不存在，回读确认系我脚本的 slug 把**空格删除**而 GitHub 锚点是**空格转连字符**，修正脚本后归零——**第六次验证「先验证再下结论」**。**P2×1 已修**：症状 14/15 缺「若无效」升级出口，违反排障手册硬特征，已补齐至 15/15。**新增跨课收束**：08 开头把 12 课的「静默失败」提炼为**三种不校验形态表**（运行时不校验 / 启动期严格校验 / 静默降级），是对 final 手册「Grafana 把活揽到自己手里」主线的进一步细化 |
| 2026-09-07 | **final-课程手册**（Phase 4） | 主 agent 内联（索引型手册完整性核查，子 agent 未创建，独立性受限） | 0 | **定位为索引型**：不重复 12 课正文，只提供地图/结论/判据（已读 docker 课程手册确认体例一致）。**内容全部来自真实讲义提取而非记忆**：写前用 3 个脚本扫描 12 课的标题、「一句话记住」、第五幕收束与跨课关联句，逐课摘要据此生成。**完整性核查全通过**：链接 **46 条全部可达**（初查 2 条指向 Phase 5 未写文件，已改标注「待编写」而非死链）；锚点 8 条全部命中；12 课讲义文件全部存在；知识点计数脚本统计表格 = **36**，与声称一致；关键实测数字（52%~61% / 2.6~4.3s / 3063 exemplar / trace_id c57731cc / 2.71s+1.71s / 80 倍面板只慢 4.5 倍）**逐条与 00-学习档案 比对一致**。**新增两条跨课收束（此前分散各课、未显式提炼）**：①**三句话方法论**——先问「这一步在哪执行」（课3/5/10）· 先问「状态存在哪」（课1/10/12）· 先问「它真的报错了吗」（课4/9/11）；②**「Grafana 把活揽到自己手里」主线表**——课4/5/6/9 运行时不校验 → 课 10 启动期严格校验（反转）→ 课 11 静默降级（升级），首次汇总为一张表。**评审判为脚本缺陷未改文档×1**：锚点校验报「结课实战项目从告警到定位」不存在，回读确认系脚本把《》替换为 `-` 而 Markdown 锚点是**删除标点**，修正脚本后 8 条全绿——**第五次验证「先验证再下结论」，且本次是脚本缺陷而非文档缺陷**。**P1×2 已修**：实战项目标题与引用块间缺空行（渲染时会被并入标题）；「怎么用这份手册」中 2 条指向 Phase 5 未写文件的死链。**课 4 P0 未复发**：代码块外续行 = 0（436 行全检） |
| 2026-09-07 | **结课实战项目《从告警到定位》**（Phase 3） | 主 agent 内联（pedagogy + learner 双视角 + 复杂度四门槛 A1-A6，子 agent 未创建，独立性受限） | 0 | **复杂度四门槛达成**：①跨阶段 **4 阶段 / 15 知识点**（阶段1×4、阶段2×4、阶段3×4、阶段4×3，逐个回指课时，已用脚本统计表格行数核对）；②非功能约束 3 项（性能 `maxDataPoints:1000` + Loki 低基数标签、可维护性 脚本生成 JSON + 全量 provisioning、错误处理 `ctx.is_valid` 判空 + 推送兜底）；③真权衡 3 个（日志采集 Promtail/直推/Docker 驱动、告警设计 症状型/原因型/三类分级、配置管理 纯UI/UI+导出/全量provisioning），每个五段式写清选了什么/为什么/放弃的差在哪/代价/翻转条件；④规模 多文件工程（app/prometheus/provisioning/webhook 分层，含 up.sh/down.sh 一键启停）。**一键重建已复验**：`down.sh` → `up.sh` 跑通，3 数据源 + 9 面板 + 2 变量 + 3 规则 + 策略树全部由 provisioning 恢复，无需点 UI。**三级下钻闭环实测**：同一 trace_id `c57731cc...` 在指标 exemplar（24 组/3063 个）→ Loki 日志（命中 2 条）→ Jaeger 链路（spans=2，根 `HTTP /checkout` 2.71s / 子 `call-payment` 1.71s）三处全部命中。**【P0×1 已修 · 本轮最重要 · 事实核查新发现】**：错误率表达式 `A/B` 在 A 无数据时返回**空**而非 0 → reduce 得 nil → 日志报 `humanizePercentage: can't convert <nil> to float`。**危险在于只在系统健康时暴露**——测告警一律在故障态（分子有值），故此前 12 课 + 项目多轮全部漏掉。修法 `(A/B) or (B*0)`，实测无故障时 5 条 route 全返 0、有故障时 0.6667 不变，重载后近 3 分钟 0 报错。已同时写入设计决策踩坑第 8 条 + 反例对照「只在健康时暴露的坑」+ 验收清单新增 3.6 判据。**事实核查修正过时数字 3 处**：错误率 6.31%→**52%~61%**（6 次采样，与源码 35%+30% 两处失败概率吻合）；P90 4.208s→**2.6~4.3s 范围**（原文写死单值，实测波动近 2 倍）；QPS 0.67→**约 1.5 req/s**（原文是早期单接口值）。**自测题 2 论证重写**：原文「错误率只有 6.31%，大部分请求成功」与实测矛盾，改为三行对照表（本项目故障注入 / 真实慢查询 0% 错误率 / 真实快速失败），并点明本项目是「两条都响」演示的是配合、真正说明「只配错误率不够」的是慢但成功那一行。**评审判为环境问题未改文档×2**：F13 直连 Loki 3100 返回 HTTP=000（实际映射 3101，应走 Grafana 代理，已用 `/api/ds/query` 验证取到 3 行日志）；Jaeger 16686 返 404 系宿主机端口顺延至 **16687**（非数据缺失），已在验收清单注明。**死引用 1 条已修**：卫生检查删掉临时脚本后，验收清单里对 `p3-drill-full.sh` 的引用失效，改为内联三步命令。**课 4 P0 未复发**：产物中的命令均为单行或代码块内，代码块外 0 续行 |

---

## 断点信息

- **当前位置**：阶段 4 课 12 已交付（36/36 知识点，全部阶段 9/9）——**12 课全部结课；结课实战项目（Phase 3）已交付 P0=0；final-课程手册（Phase 4）已交付 P0=0；Phase 5 三件套（08 实战经验 / 09 排障速查手册 / 10 场景解法库）已交付 P0=0**
- **下一批**：**全课程已收官**，无待办。如需可另行补充专题（如 Grafana 与其它可观测栈的对比、更大规模压测等）
- **课 12 新增环境（跨阶段可复用）**：`l12-pg`(Postgres 16，5433) / `gf-pg1`(3004) / `gf-pg2`(3005，共享同一库) / `l12-renderer`(8082) / `gf-render`(3006) / `gf-old`(3007，Grafana 12.0.0) / `gf-restore`(3008，备份恢复验证)；网络 `l12net`
- **课 9 前置**：①需 `docker pull grafana/loki`（端口 3101）；②Jaeger 本机已有镜像 → 16687；③课 8 资产可复用：文件夹 `l08alerts`(uid=l08alerts)、webhook 容器 `l08-webhook`（grafana-net 内，端口 9999）
- **课 8 前置**：文件夹 `l07alerts`(uid=l07alerts) 已创建，告警规则可直接复用
- **环境状态**：`grafana-lab`(3001) + `grafana-prom`(9201) + 3 台 node-exporter(9101/9102/9103) 在 `grafana-net` 内运行中；数据源 `PromLab` 已建（uid=afx7x6dx803y8e，已加 `Accept-Encoding: identity` 头绕开 gzip 问题）
- **待办**：课 9 的 Loki 镜像 ✅已解决；课 10 的 `grafana-prov`(3002) ✅已建；课 12 的 Postgres 镜像 ✅已拉取（`postgres:16-alpine`）+ Grafana 12.0.0 ✅已拉取。剩余待办：结课实战项目（Phase 3）、Phase 5 三份手册（08-实战经验 / 09-排障速查 / 10-场景解法库）

### 实验环境资产（可跨课复用，勿重复搭建）

| 资产 | 值 |
|---|---|
| 网络 | `grafana-net` |
| 容器 | `grafana-lab`(3001) / `grafana-prom`(9201) / `grafana-node`(9101) / `grafana-node2`(9102,hostname=node-alpha) / `grafana-node3`(9103,hostname=node-beta) |
| 启动脚本 | `playground/l00-env-up.sh`（含身份校验，非仅状态码） |
| 数据源 | `PromLab`，uid=`afx7x6dx803y8e`，url=`http://grafana-prom:9090`，access=proxy |
| 登录 | admin/admin，字段名是 `user` 不是 `username` |
| 一次性测试实例 | 课 2 用 3011=`gf-l02`(全新默认)、3012=`gf-l02b`(计时)、3013=`gf-l02c`(环境变量口令/不挂卷)、3014=`gf-l02d`(挂卷 `gf-l02d-vol`) |
| 内置/外装插件 | 内置 13 个（prometheus/loki/jaeger/elasticsearch/tempo/zipkin 等，在 `/usr/share/grafana/data/plugins-bundled`）；外装 5 个（metricsdrilldown/exploretraces/pyroscope/advisor/lokiexplore，在 `/var/lib/grafana/plugins`） |
| 环境配置 | `playground/prometheus.yml`（scrape_interval 5s，job=node/prometheus） |
| 端口避让 | 9091 已被 pushgateway 占、9092 kafka、9093 alertmanager、9094-9099 prometheus 课程，**Grafana 课程 Prometheus 固定用 9201** |
| 多实例环境 | 课 3 起 node 共 3 台，脚本 `playground/l03-nodes-up.sh`（幂等）；`prometheus.yml` 的 node job 含 3 个 targets，改完需 `docker restart grafana-prom`（未开 lifecycle） |
| gzip 规避 | PromLab 数据源 jsonData 设 `httpHeaderName1=Accept-Encoding` / `httpHeaderValue1=identity`，脚本 `playground/l03-fix-gzip.sh`（幂等）。症状：面板无数据但不报错 |
| 存储层变化 | Grafana 13.2.1 的 dashboard 存在 **`resource`** 表（k8s 风格，`group=dashboard.grafana.app`），旧 `dashboard` 表 0 行；`resource_history` 保存历史版本 |
| 代理路径 | 13.2.1 用 `/api/datasources/proxy/uid/{uid}/...`，按 id 的旧路径已 **404** |
| 插件版本独立 | 内置 Prometheus 插件 **13.1.7**，与 Grafana 主体 13.2.1 不同步；前端代码在 `/usr/share/grafana/data/plugins-bundled/prometheus/module.js`（750KB） |
| 请求头被覆盖 | 数据源配的 `Accept-Encoding: identity` **被后端覆盖为 gzip**（抓包实测）。验证请求头只能抓包，不能信配置界面 |
| 错误码双层 | `/api/ds/query` 外层 HTTP 与 `results.<refId>.status` 分离：后端不可达/DNS 失败→外层 400 + 内层 502；语法错→外层 400 + 内层 400；指标名不存在→外层 200 + 内层 200 + 0 点（静默） |
| 抓包方法 | 记录型代理容器（`python:3.11-slim`，约 60MB）坐入 `grafana-net`，日志需挂卷到宿主机（容器内 /tmp 不共享，课 3 踩过）。脚本 `playground/l04-sniff.sh` |
| Transform 执行位置 | **纯前端**。`transformations` 代码在主程序 `public/build/*.js`（828 次），不在数据源插件（9 次）。配置存后端但挂在 **panel** 上非 target 上 |
| 面板配置类字段通用模式 | `editorMode` 与 `transformations` 同为「**存后端、跑前端、后端不校验**」——瞎编的取值（`bogus-mode` / `bogus-transform-xyz`）也被原样接收回读 |
| 数据库读取警告 | 不要直读 `grafana.db` 验证 dashboard：`dashboard` 表 0 行；`resource` 表 13 行全是 CheckType 等内置资源；活 dashboard 在 `resource_history`(79 行)。value 列是明文 JSON（首字节 `7b`）。**可靠判据是 API 读回** |
| SQL 坑 | `resource` 表有 `group` 列，是 SQL **保留字**，需转义（`"group"`）否则语法错误 |
| 帧数规律 | `node_cpu_seconds_total` 帧数 = **核数 × 机器数**（本环境 20×3=60）。课 1 埋的伏笔，课 5 解开 ，课 6 扩展到 8 种 mode = 480 帧 |

---

## 环境约束（本机实测，2026-09-04）

| 项 | 说明 |
|---|---|
| 运行环境 | WSL Ubuntu 24.04.4 + Docker 29.4.1（Windows 宿主 PowerShell 无 docker，须走 `wsl -d Ubuntu -- bash -lc`） |
| 命令执行注意 | PowerShell 内联长中文命令易被截断，讲义实操一律落盘为 `playground/lXX-*.sh` 再用 `wsl bash <脚本>` 执行 |
| 端口规划 | Grafana 3001 / Prometheus 9091 / node-exporter 9101 / Loki 3101 / Jaeger 16687，网络 `grafana-net` |
| 端口避让（实测） | 9091 被 pushgateway 占用且它对 `/-/ready` 也返 200，导致「只看状态码」的探测假成功；本课程 Prometheus 固定用 **9201** |
| 登录字段名 | 13.2.1 的 `/login` 用 `user` 字段，`username` 返回 400 `form-auth.invalid`（实测） |
| 冒烟容器 | `gf-smoke` 映射 3011:3000，仅用于写课期间验证，正式实验改用 `grafana-lab` 映射 3001 |

### 产物仓库卫生（2026-09-04 课 1 交付后）

`.gitignore` 末尾新增 `grafana` 分节，采用**精确文件名规则**（6 条），刻意不用 `grafana/playground/` 一刀切——沿用 ES 课 15 教训，避免吞掉教学脚本。

| 分类 | 文件 |
|---|---|
| 🚫 已忽略（一次性过程脚本） | `l00-smoke.sh` `l00-smoke2.sh` `l01-probe-store.sh` `l01-probe-store2.sh` `l01-forms.sh` `l01-forms2.sh` |
| ✅ 已保留（教学产物） | `l00-env-up.sh`（跨课复用的环境起停）、`l01-forms.py`（课 1 第四幕实验）、`prometheus.yml`、`l01-verify.sh`、`l01-verify-diag.sh`、`l01-hygiene.sh`、`l01-ignore-verify.sh` |
| ⚠️ 存疑（暂不加规则） | `l00-gfcheck.sh` `l00-portcheck.sh` `l00-linkcheck.sh` —— 后续课可能复用，等确认无用再判 🚫 |

校验脚本 `playground/l01-ignore-verify.sh`，四组检查全部通过：A 组 6 个应忽略项命中、B 组 14 个教学产物无 `check-ignore` 命中、C 组 `git status` 仅 `?? grafana/`、D 组抽查 ES/SurrealDB/Doris 既有规则未被波及。

> 纪律：临时脚本清理禁用 `git clean -fd`，一律手工确认后删除。

| 变量取值填充位置 | 变量 `options` 保存时为空数组，由【前端】打开 dashboard 时现查现填。链式变量（B 引用 $A）的 `$A` 以【字面量】存后端 |
| 多值插值格式 | `$var` 默认**逗号**分隔（`a,b`），在 PromQL 正则里**不代表「或」**；多值必须用 `${var:regex}` → `(a|b|c)` 或 `${var:pipe}` → `a|b` |
| All 值陷阱 | `allValue=.*` 会匹配【选项之外】的序列——本环境 `.*` 得 4 帧而 `(a|b|c)` 得 3 帧，多出的是 Prometheus 自身 `localhost:9090` |
| 失败模式判据 | 语法错=**400/1 空帧/bad_data**；后端不可达=**400/0 帧**（课 4）；datasource uid 不存在=**404/0 帧**（课 6 新增）；静默无数据与变量未替换=**200/1 空帧/无 error**（两者无法区分，只能看 expr 里有无 `$`） |
| 空帧结构 | 空帧【连字段结构都没有】（字段数=0）。判据：`frames.length==0` → 请求没发出去；`==1 且 points==0` → 发出去了没结果 |
| Repeat 展开位置 | **前端展开**——变量 3 取值时 API 读回仍 1 个 panel；N 份 = N 次独立查询不合并 |
| 课 10 provisioning 实例 | `grafana-prov`(3002) 主实验、`grafana-prov2`(3003) 干净对照。启动脚本 `playground/l10-env-up.sh`（**先校验 4 个文件存在再起容器**，否则 docker -v 静默建目录） |
| provisioning 三类文件 | `playground/provisioning/{datasources/ds.yaml, dashboards/dash.yaml, alerting/alerts.yaml}`；dashboard JSON 在 `playground/provisioning/dashboards-json/prov-dash.json`（provider 的 options.path 指向它） |
| Provisioning 本质 | 启动时一次性 **INSERT 进数据库**，之后运行时读库不读文件（唯一例外：`updateIntervalSeconds` 定期重扫） |
| alerting 写错后果 | **整个进程崩溃** Exited(1)，非跳过。api.HTTPServer/DashboardUpdater/SSO/Zanzana 全部级联失败（依赖 provisioning 模块）。排查：`docker logs grafana-prov \| grep -i provisioning` |
| allowUiUpdates 真实语义 | `false`=**硬拒 400** `Cannot save provisioned dashboard`（非"保存后覆盖"）；`true`（干净实例）=能保存但**重启被文件覆盖**；同实例先 false 后 true 仍 400 = **数据库残留**，判据看 `meta.provisioned` |
| disableDeletion | `false`=删文件即删 dashboard（2→1）；`true`=删文件 dashboard 留孤儿（保持 2） |
| dashboards vs alerting 文件夹 | dashboards 用 **`folderUid`**、alerting 用 **`folder`（名字）** → 产生两个同名文件夹。建议 folder+folderUid 成对写 |
| editable 字段转换 | 写入 `editable`，读出 **`readOnly`**。查 provisioning 是否锁定看 `readOnly` 不看 `editable` |
| dashboard JSON 可 diff | 顶层仅 **15 字段**；Grafana **只回填 `id`+`version`**（源文件 13→库中 15，panel.id 原样保留）。剥掉顶层 id+version + `sort_keys=True` 后两实例**逐字节相同（diff 0 行）** |
| uid 必须写死 | 不写则 Grafana 随机生成（如 `dfxhsw8b57pxcd`），重建环境即变，所有引用全断 |
| 课 11 权限实验主体 | `alice`(2)/`bob`(3)/`carol`(4)/`dave`(5) 均 Viewer；`admin`(1) Admin。团队 `OpsTeam`(1)/`TeamBGroup`(2)；文件夹 `teama`/`teamb`；服务账号 `ci-bot`(id=6, login `sa-1-ci-bot`) |
| Org 隔离强度 | 跨 Org **读**资源 → **404**（装作不存在，非 403）；跨 Org 写 → 403；跨 Org 列表 → `[]` |
| Team 不带权限 | dave 加入 TeamBGroup 前后权限列表**完全一致**，权限只来自授权记录 |
| 两道闸门 | 角色给「动作权限」+ 文件夹给「资源范围」，**都要过**；两种 403 错误信息不同可定位 |
| `hasAcl` 不可信 | 恒 `false`；`folder` 表**无 `has_acl` 列**（遗留计算字段）。判据看 `/permissions` 或做写入测试 |
| `folderUid` 位置 | 必须放 **payload 顶层**；放 dashboard 内会**返回 200 但落到 General**（本课最隐蔽的坑） |
| API Key 现状 | **接口已全部移除**（三路径 404），但 `api_key` 表仍在 92 张表中（1 行遗留）。**接口死了，表还在** |
| 服务账号 token | `glsa_` 前缀；**明文只出现一次**（只存哈希），列接口无 `key` 字段 |
| 权限真身 | `permission` 表，scope 用 **k8s 风格** `{group}/{resource}:{verb}`；`service_account` 已迁出传统表 |
| 列表接口规律 | `GET /api/teams`、`GET /api/serviceaccounts` 均 **404**，须用 `/search` 后缀（本课第三个同类，课 7/10 同族） |

| 告警求值位置 | Grafana 13 统一告警在【Grafana 内部】求值：Prometheus 侧 0 个规则组、无 `rule_files`；状态存 Grafana annotations；内嵌 Alertmanager（`/api/alertmanager/grafana/`）。条件判断用数据源类型 **`__expr__`**（Grafana 内部表达式引擎） |
| 告警状态怎么读 | **不要**从 ruler API 读（Grafana 13 顶层即 `{namespace:[groups]}`，且 `grafana_alert` 无 state 字段）。正确读法：`curl -s -u admin:admin http://localhost:3001/metrics \| grep '^grafana_alerting_alerts{'`，暴露 **6 种** state：alerting/error/nodata/normal/pending/recovering |
| for 的真实语义 | `for` = 条件**连续**满足多久（考勤，非闹钟）。实际告警延迟 = **⌈for/interval⌉ × interval**，故 `for < interval` 时无意义。**恢复不受 for 约束**（下次求值即恢复）。组默认 interval=**1m**（`minInterval=10s` 只是下限） |
| 告警状态标签写法 | annotations 里是「**状态 (原因)**」：`Alerting (NoData)`、`Alerting (Error)`、`Normal (Error)`。`/metrics` 只给状态不给原因——排查「真告警 vs 查不到数据」必须看 annotations |
| No Data vs 真实宕机 | **机器宕机 ≠ No Data**。Prometheus 的 `up` 对挂掉的 target 返回 **0**（序列仍在），**走 Alerting 分支**。`up` 类指标配 `noDataState` **无效**；只有业务指标（零流量时序列消失）才需要显式配 `noDataState=Alerting` |
| noDataState/execErrState 拼写 | 正确是 **`KeepLast`**（不是 `KeepLastState`，后者 HTTP 400）。合法取值各 4 种：NoData/Alerting/OK/KeepLast 与 Error/Alerting/OK/KeepLast。provisioning API **两字段必填无默认值**（不传报 400），UI 路径 `/api/ruler` 才接受空值 |
| 告警实验方法约束 | 告警是离散采样（60s 一轮），每次测量前**必须清理规则并等状态归零**（65-70s），否则读到上一条规则的残留。本课 `noDataState=OK` 首测错误即源于此 |
| Grafana 13 folderUID | 告警规则**强制要求** `folderUID`（空串报 400）。本课创建 `l07alerts`(uid=l07alerts) 供课 8 复用。重复建同 uid 报「the folder has been changed by someone else」（误导性错误，实为已存在） |
| Grafana 13 reduce vs classic | 条件写法决定告警实例数：`classic_conditions` 降维时**丢掉序列标签**（3 序列→1 告警，无 instance）；`reduce`+`threshold` **保留标签**（3 序列→3 告警，有 instance）。这直接决定 8.3 的 `group_by: [instance]` 能否生效 |
| Grafana 13 评估间隔 | 间隔由**评估组**决定，规则级 `intervalSeconds` **被忽略**（回读 `null`）。想让单条规则更灵敏要挪到另一个组 |
| Grafana 13 通知策略树 | 会校验 receiver 存在性，引用不存在的 receiver 整棵树 400。正确顺序：contact point → mute timing → 策略树 |
| Grafana 13 静默 | DELETE 返回 200 `{message:silence deleted}` 但**静默仍在列表**（state 变 expired）。Alertmanager 保留历史，创建时务必设好 `endsAt` |
| Grafana 13 repeat_interval | 实际间隔**大于**配置值（配 1m 实测 90s，配 40s 实测 60s），会向上跳到 `group_interval` 的检查点。它是下界不是闹钟 |

### 结课实战项目《从告警到定位》实测结论（2026-09-07）

| 一键重建 | `down.sh` + `up.sh` 跑通：`p3-shop`/`p3-prom`/`p3-webhook`/`p3-grafana` 全部 Up，3 数据源 + 9 面板 + 3 规则 + 策略树全由 provisioning 恢复，无需点 UI |
|---|---|
| 三级下钻闭环 | 同一 trace_id `c57731cce9f6077bc0b628fd175563fa` 在**指标 exemplar → Loki 日志（2 条）→ Jaeger 链路（spans=2：根 `HTTP /checkout` 2.71s / 子 `call-payment` 1.71s）** 三处全部命中 |
| exemplar 生效条件 | 三缺一不可且全部静默：①应用显式 `observe(..., exemplar={'trace_id':...})` ②Prometheus `--enable-feature=exemplar-storage` ③数据源 `exemplarTraceIdDestinations`。实测 24 组 / 3063 个，组数随运行时间增长 |
| 告警分级实测 | 故障注入后：critical（错误率）10s 内到 `/critical`，warning（P90）走 `/alert`；summary 渲染为 `接口 /checkout 5xx 错误率 61.22%`，无裸 `{{ }}` |
| **新坑：比率告警分子为空** | `A / B` 在 A 无数据时返回**空**（不是 0）→ reduce 得 nil → `humanizePercentage: can't convert <nil> to float`。**只在系统健康时暴露**（测告警都在故障态，故长期潜伏）。修法：`(A / B) or (B * 0)`，实测无故障时 5 条 route 全返 0、有故障时比例不变 |
| 数值波动范围 | checkout P90 = **2.6~4.3s**（6 次采样）、错误率 = **52%~61%**、总 QPS ≈ **1.5 req/s**。初稿写死的 4.208s / 6.31% / 0.67 均为早期单测值，已按多次采样改为范围 |
| Jaeger 端口 | 宿主机映射可能因冲突顺延：16686 → **16687**。直连 API `curl localhost:16687/api/traces/<tid>` 返回 404 是端口不对，不是没数据 |
