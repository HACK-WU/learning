# Docker 课程评审清单

> **强制检查点**：每批内容生成后，先在本文件找到对应未勾选条目，完成双视角评审（pedagogy + learner）且 **P0 清零**后，才能勾选并交付。
> **未勾选 = 评审未完成，禁止进入下一批。**
>
> 与 `00-学习档案.md` 的分工：本清单管**待办勾选**，学习档案管**评审结论摘要**（历史档案）。

## 待评审清单

- [x] 大纲评审（Phase 1）— pedagogy 视角，P0=0 通过（2026-09-05）
- [x] 阶段 1 · 课 1《为什么需要Docker》 — pedagogy + learner 双视角，P0=0 通过（2026-09-05）
- [x] 阶段 1 · 课 2《跑起来第一个容器》 — pedagogy + learner 双视角，P0=0 通过（2026-09-05）
- [x] 阶段 1 · 课 3《镜像的里子：分层与仓库》 — pedagogy + learner 双视角，P0=0 通过（2026-09-05）
- [x] 阶段 2 · 课 4《Dockerfile入门》 — pedagogy + learner 双视角，P0=0 通过（2026-09-05）
- [x] 阶段 2 · 课 5《启动命令与配置注入》 — pedagogy + learner 双视角，P0=0 通过（2026-09-05）
- [x] 阶段 2 · 课 6《多阶段构建与镜像瘦身》 — pedagogy + learner 双视角，P0=0 通过（2026-09-05）
- [x] 阶段 3 · 课 7《数据持久化》 — pedagogy + learner 双视角，P0=0 通过（2026-09-05）
- [x] 阶段 3 · 课 8《容器网络》 — pedagogy + learner 双视角，P0=0 通过（2026-09-05）
- [x] 阶段 3 · 课 9《Compose编排多容器》 — pedagogy + learner 双视角，P0=0 通过（2026-09-05）
- [x] 阶段 4 · 课 10《资源限制与进程管理》 — pedagogy + learner 双视角，P0=0 通过（2026-09-06）
- [x] 阶段 4 · 课 11《日志与可观测性》 — pedagogy + learner 双视角，P0=0 通过（2026-09-06）
- [x] 阶段 4 · 课 12《容器安全边界》 — pedagogy + learner 双视角，P0=0 通过（2026-09-06）
- [x] 阶段 4 · 课 13《CI-CD与交付流水线》 — pedagogy + learner 双视角，P0=0 通过（2026-09-06）
- [x] 阶段 5 · 课 14《Docker在容器生态中的位置》 — pedagogy + learner 双视角，P0=0 通过（2026-09-06）
- [x] 阶段 5 · 课 15《决策清单与学习地图》 — pedagogy + learner 双视角，P0=0 通过（2026-09-06）
- [x] **结课实战项目**（Phase 3）— pedagogy + learner 双视角 + **复杂度四门槛核查**，P0=0 通过（2026-09-06）。四门槛：①跨 3 阶段（阶段 2 镜像工程 / 阶段 3 数据与网络 / 阶段 4 生产落地）②5 项非功能约束（镜像 <150MB / 非 root / 只读根+cap_drop+no-new-privileges / 内存 ≤256MB / 日志 ≤30MB）③4 个真权衡决策（基础镜像 slim vs alpine vs distroless、日志 local vs json-file vs 远端、数据库容器化 vs 托管、compose vs K8s）④9 文件多文件工程。产出位于 `projects/订单服务生产化/`；架构图 `assets/capstone-architecture.svg`
- [x] **实战经验 / 排障速查手册 / 场景解法库**（Phase 5）— pedagogy + learner 双视角 + **证据纪律核查**，P0=0 通过（2026-09-06）。三份**一次生成**共 1215 行：10 条故障模式 + 10 条 QRH 条目 + 8 个设计场景。五项自动化核查全通过：锚点 23/23、相对链接 26/26、手册五硬特征（索引表入口 / 症状可观测 / 先止血后定位 / 若无效转 X / 升级出口）、场景库四要素（先想后看 / 多解法 / 知识点挂钩 / 推荐路径+不适用+坑）、同源不漂移（08 模式 ↔ 09 条目 10:10 一一对应，10 场景地图逐条挂钩模式编号）。**证据纪律**：本课**无 🟢 本机实测**（守护进程未运行），已如实声明；事故案例章节**留空不虚构**；⏳ 不确定项仅留在 08/10，不进 09 手册；二手案例沿用 zookeeper 判定排除
- [x] **课程手册汇总**（Phase 4）— pedagogy + learner 双视角 + **汇总完整性核查**（四项硬要求），P0=0 通过（2026-09-06）。索引型手册 `final-课程手册.md` 536 行。**四项硬要求全达标**：①15 课全覆盖（脚本校验课条目数 = 15，未纳入 0）②学习路径总览与阶段速查均在开头（行 41 / 行 80）③各课配图 15 张全部保留且文件存在，未收录 0 ④实战项目已收录且四门槛表完整。**自动化核查全通过**：全量链接 + 锚点 **336 条 / 失效 0**、15 张图片全部存在、9 项关键数字（10 秒·30 秒 / 137·143 / `127.0.0.11` / 20m×5=100MB / 14 条 capabilities / 3 条默认网络 / 1000 容器 / K8s v1.24 / Cache API v1 停用 2025-04-15）与讲义原文逐项交叉核对一致、22 枚 SVG 全部合法。**P2×1 当批修复**——手册中阶段 5 的目录名误写为 `5-生态与决策`，**实际目录是 `5-定位与决策`**（该错误会导致课 14/15 的两条讲义链接与其两张 SVG 全部 404，属硬失效）→ 已修正全部 6 处（含阶段名表格与章节标题）。**顺带修复两处陈旧状态**：①`01-学习路径总览.md` 中 5 个阶段全部仍标「（未开始）」→ 已一并改为「（已完成）」；②`02-课程目录.md` 的「汇总手册」仍标「未编写」→ 已改为可点击链接。**手册自主新增四块内容**（原课无，手册首次跨课整合）：①**「全课程知识点地图（45/45）」**——从学习档案进度表自动提取 45 个知识点并按阶段/课分组，每行直达对应讲义；②**「五个『看起来没坏其实很危险』的坑」**汇总表（跨课 4/8/9/10/11/3/6 提炼：`EXPOSE` 不发布端口、`depends_on` 短语法不等就绪、默认日志不轮转、137 未必是 OOM、容器内 `rm` 不减体积），共同规律是"命令全都对了，错的是你对系统的假设"；③**决策清单**（该不该容器化五反模式 / 挂载选型 / 基础镜像选型 / 重启策略选型 / 编排选型，每个含代价与触发升级的信号）；④**考点速记**（9 项高频数字 + 10 组易混点对照 + 常考「唯一·第一·默认」+ 常考「不能·不支持」11 条）。**pedagogy 5 维度**：准确性——手册为**索引型**，不重复正文，所有技术论断均回指已核实的课，未引入需新核实的事实性论断；关键数字四条核查路径（手册→讲义原文）全部一致；格式规范——开头「全课程一句话 + 三句话方法论 + 怎么用这份手册」三层导航，15 课每课统一为「一句话 → 核心要点 → 配图 → 关键命令 → 完整讲义链接」五段式；完整性——覆盖 5 阶段 15 课 45 知识点 + 结课项目 + Phase 5 三件套；一致性——阶段名/目录名与 `01-学习路径总览.md` 和实际目录结构三者对齐（已通过校验脚本比对）；难度梯度——由导航到总览到分课到速查，读者按需取用。**learner L1-L6**：L1——「三句话方法论」把 15 课压缩成三问，可直接用于任何容器问题；L2——先给地图（路径总览 + 知识点地图）再给细节（分课），符合检索习惯；L3——每课配图保留，视觉锚点不断裂；L4——从"为什么学"到"学完了"完整闭环；L5——「怎么用这份手册」表按读者状态分派（刚学完 / 要设计 / 出事了 / 要复习 / 想动手），不用读者自己猜该看哪；L6——导航表把手册与三件套、课程目录、学习档案全部串起来 | 通过，落盘 |
- [x] **web index 交叉核对补录**（课 3 / 9 / 10 / 12 / 13 / 15 共 6 处遗漏）— 主 agent 内联（先验证再下结论 + 官方一手核查 + 结构回归），**P0=0 通过**（2026-09-10）。补入：课 3 可信内容辨识（Official/DVP/DSOS/普通镜像）/ 课 9 知识点 2.5 多文件拆分（merge·extends·include）/ 课 10 live restore / 课 12 签名校验（cosign，**DCT 2026-12-08 退役**）/ 课 13 Hub 拉取限额 / 课 15 docker context + 已废弃写法对照表。核查：10 条官方链接全 200、7 文件围栏全偶数。**P1×1 当批修复**——课 12 历史遗留围栏错位（440–457 行命令裸露），已修复。同步回写：6 课导航表 + 6 张速查卡 + `2-课程目录.md + `0-学习档案.md 四处

## 本次规则回溯（2026-09-15）

- [x] **topic-teach 规则回溯与场景补丁** — P0=0：15 课入口三件套、课首官方核对标记、知识点承接导航、15 张课级静态 SVG、**46/46 知识点图示**（C 型关系图用独立 SVG，流程 / 状态图用 Mermaid）、46 个知识点口径、5 个新增场景与 Phase 5 的 12 / 12 / 9 收尾产物均已核对；官方链接按 web index 定位并复核；SVG XML、代码围栏和本地链接回归通过；Docker daemon 未运行，未编造实测输出。

## 子教程（2026-09-20 追加）

- [x] **子教程 · 运维专项 大纲（overview.md）** — 主 agent 内联（pedagogy + 边界核查 + 链接核查 + 环境实测），**P0=0 通过**（2026-09-20）。范围：6 课 / 18 知识点，落盘 `子教程/运维专项/overview.md`。四项核查全通过：①**边界核查**——逐个知识点与主线阶段 4（课 10–13）比对，「同一事物的两个视角」映射表 6 行，无重复知识点；②**链接核查**——新增 15 条 docs.docker.com 链接 **15/15 返回 200**（HEAD 实测）；③**环境实测**——WSL Ubuntu 24.04 / Docker 29.4.1 / overlayfs / cgroup v2 systemd / json-file / iptables-nft / DOCKER-USER 已存在 / `daemon.json` 不存在，数据均为真实输出且已标注「教程实验机」；④**结构回归**——overview 模板六段齐全（定位 / 与主线的关系 / 课清单 / 学习路径 / 学习进度 / 生成与评审规则），Mermaid 依赖图 6 节点 6 边合法。**课 3 知识点②「指标语义三步核验」**按用户 2026-09-14 固化的跨课程铁律落地为正式知识点（源自 Kafka 课 `RequestHandlerAvgIdlePercent` 照抄阈值致告警永不触发的教训）。**未编造实测输出**：`daemon.json` 写操作属改环境，按规矩留待写课时单独请示，本次仅读取确认其不存在。同步回写四处：`01-学习路径总览.md`（子教程条目）、`02-课程目录.md`（子教程章节 + 6 课表）、`00-学习档案.md`（角色视角与子教程规划 + 独立进度表）、本清单。
- [x] **子教程 · 运维专项 课 1《守护进程与主机视角》** — pedagogy + learner 双视角评审，**P0=0 通过**（2026-09-20）。交付：讲义 897 行 + [三层配置覆盖关系 SVG](../子教程/运维专项/assets/lesson-01-config-layers.svg) + [暴露面与提权路径 SVG](../子教程/运维专项/assets/lesson-01-exposure.svg)。评审核查四项：①**实测真实性**——全部 `🟢` 标记均有对应真实输出（引擎 29.4.1 / overlayfs / json-file / systemd cgroup v2 / 数据根 118G / `live-restore=false` / socket `srw-rw---- root:docker` 且 docker 组无成员 / 2375·2376 均未监听 / `default` context 唯一）；**刻意不给会过时的数字**（error 日志条数），转而要求学员自己跑，符合「不编造实测输出」纪律。②**安全边界**——`-v /:/host` 提权命令、写 `daemon.json`、建 drop-in、开 2375 四项均标注「不执行」并说明原因（改环境须先请示），第四幕步骤 6 专设「不执行的部分」。③**与主线边界**——`log-opts` 呼应课 11（容器级 vs 引擎级）、`live-restore` 呼应课 10（讲哪个文件开）、socket 呼应课 12（一句话 → 展开为暴露面盘点），三处均只补「运维侧落点」，未重讲主线内容。④**结构回归**——围栏 90（偶数）、2 张 SVG 经 UTF-8 解析为合法 XML（20 / 28 个 text 节点）、8 条本地链接全部可达。

  **本轮修复的两个真问题**：①**相对链接层级错误**——`lessons/` 到课程根应为 `../../../`（三级），初稿误写 `../../`（二级），5 条链接全部不可达，已批量修正并逐条验证。②**避免了一次误改**——首轮校验脚本用 PowerShell 默认编码（GBK）读 UTF-8 的 SVG，报「text 标签未闭合」；改用 `UTF8Encoding` 复验后确认 XML 合法（20 / 28 节点），**属脚本误报而非文件缺陷**，按既有「误报不等于真缺陷」铁律收紧了校验方式而非改图。临时取证脚本 `_probe-l1.sh` 已清理。

- [x] **子教程 · 运维专项 课 2《磁盘与空间治理》** — pedagogy + learner 双视角评审，**P0=0 通过**（2026-09-20）。交付：讲义 969 行 + [数据根目录结构 SVG](../子教程/运维专项/assets/lesson-02-datadir-layout.svg) + [df 盲区对比 SVG](../子教程/运维专项/assets/lesson-02-df-blindspot.svg)。四轮取证（日志总量 / 大日志归属 / 增速 / prune 预演）全部为真实输出。评审核查四项：①**实测真实性**——数据根 117G（rootfs 60G / volumes 44G / containers 14G）、containers 目录 13797 MB 中日志 13795 MB（99.99%）、`doris-learn` 日志 10660 MB 且 `LogConfig.Config={}`、60 秒增速 +366654 字节（≈6 KB/s）、197 个容器全部无轮转、分区 24%——**每项 `🟢` 均可回溯到取证脚本输出**。②**如实记录不支持项**——实测三种 `prune` 均报 `unknown flag: --dry-run`，**未编造干跑输出**，改为给出「用查询命令预演」的替代方案并列入误区表。③**安全边界**——`truncate`、`prune`、写 `daemon.json`、重建 `doris-learn` 四项全部标注「不执行」并说明（破坏性/改环境需请示）；`volume prune` 危险度单列「很高」，点明 `--volumes` 会去掉 Docker 默认保护。④**结构回归**——围栏 94（偶数）、2 张 SVG 以 `UTF8Encoding` 解析为合法 XML（24 / 32 个 text 节点）、6 条本地链接全部可达。

  **本课最有价值的教学发现**：`docker system df` **不统计容器日志**——账本报 Containers 20.32GB / 可回收 4.652GB，而 `containers` 目录 13.5GB 几乎全是日志且**一行未入账**，导致 RECLAIMABLE **系统性低估**。该结论由 `du -sb` 与 `find` 两法交叉验证坐实，并成为全课主干。另一发现：`46GB 卷仅可回收 673MB` 并非"清不动"，而是**在用业务数据**（11G 卷被 `doris-mysql-demo` 引用），据此写入「空间大 ≠ 可回收」判据，避免学员误删。

  **回写中修正的两处「预期与实际不符」**：大纲与课程目录中课 2 原描述为「四分法找真凶」，与实测得出的「四分法的盲区」不符；按既有铁律**先 grep 回读原文再改**，据实修正为「与它的盲区（日志不入账）」，而非沿用旧表述。

- [x] **子教程 · 运维专项 课 3《监控指标与告警》** — pedagogy + learner 双视角评审，**P0=0 通过**（2026-09-20）。交付：讲义 960 行 + [观测路径图](../子教程/运维专项/assets/lesson-03-observability-paths.svg) + [三步核验流程图](../子教程/运维专项/assets/lesson-03-three-step-verify.svg)。四轮取证（指标暴露状态 / stats 语义 / PSI 与事件流 / OOM 与健康检查）全部为真实输出。评审核查四项：①**实测真实性**——`dockerd` 无 `metrics-addr`、9323 返回 `HTTP 000`、`memory.current=1099018240`≈`1.01GiB`、BlockIO 5 次不变、PSI `avg10` 0.28→0.23→0.15、2 个容器 `OOMKilled=true`、健康检查 49 none/1 healthy、内存限制覆盖率 1/197——**每项 `🟢` 均可回溯到取证脚本输出**。②**如实记录未开启项**——本机未开 Prometheus 指标，讲义**未编造任何 `engine_daemon_*` 输出**，官方文档示例明确标注「非本机输出」，并改用 cgroup/`events`/`inspect`/PSI 四条已验证路径取证。③**安全边界**——写 `daemon.json`、重启 `dockerd`（**会中断 121 个运行中容器**）、补 `HEALTHCHECK`、设 `--memory` 四项全部标注「不执行」，并给出 A/B/C 三方案待用户选择。④**结构回归**——围栏 82（偶数）、2 张 SVG 以 `UTF8Encoding` 解析为合法 XML（24 / 37 个 text 节点）、7 条本地链接全部可达。

  **本课三个高价值发现**：其一，**同一条 `docker stats` 里混装两种语义**——`CPUPerc`/`MemUsage`/`PIDs` 是瞬时值（实测 1.43%→1.61%→23.34%），`NetIO`/`BlockIO` 是累积值（5 次采样不变、另轮只增不减）。其二，**`MemPerc` 的分母是主机内存**——`l15-kafka-1` 因 `Memory=0` 未设限，报 `1.01GiB/31.07GiB=3.25%`，照抄 `>80%` 的告警**数学上几乎永不触发**，与 Kafka 课 `RequestHandlerAvgIdlePercent` 属同一类静默失效。其三，**`OOMKilled=true` 是历史标记而非当前状态**——2 个容器标记 true 却 `running`、重启 0 次、退出码 0 且未设内存上限；据此写入「可作线索、不可作告警条件」的判据，避免永久误报。

  **方法论落地**：「指标语义三步核验」（看值域 → 查语义出处 → 连续采样 3~5 次看会不会降）在本课**作为正式知识点**落地（大纲第 35 行已注明不因"偏方法论"降级），并给出正反两个本机案例（PSI 窗口均值 ✅ / BlockIO 累积值 ⛔），实现从 Kafka JMX 到 Docker cgroup 的**跨技术栈复用**。另提出一条高实操价值建议：**宁可对 PSI 告警，也不对 `free` 使用率告警**——本机内存 75.1% 看似危险，但 `memory.some=0.00` 说明无实际压力。

- [x] **子教程 · 运维专项 课 4《备份恢复与迁移》** — pedagogy + learner 双视角评审，**P0=0 通过**（2026-09-20）。交付：讲义 1010 行 + [备份一致性图](../子教程/运维专项/assets/lesson-04-backup-consistency.svg) + [搬迁与演练图](../子教程/运维专项/assets/lesson-04-migration-drill.svg)。三轮取证（卷/镜像/compose 家底 → 具名·匿名分类与 pg 一致性 → 孤儿卷与 save 量级）全部为真实输出。评审核查五项：①**实测真实性**——218 卷（15 具名/203 匿名）、6 个孤儿具名卷、`l12-pg` 数据卷为匿名 `1fe9255...`（82MB/94 表）、`postmaster.pid` 存在、10 分钟内 5 文件被改、`save busybox`=2.15MB vs `images` 6.81MB、57 bind/41 volume——**每项 `🟢` 均可回溯到取证脚本输出**。②**如实声明取证副作用**——为验证热备份标记函数，在 `l12-pg` 上执行了 `pg_backup_start()` 并立即 `stop()`，会**强制一次检查点**；该事实已在讲义开头「如实声明」段落显式记录，未隐瞒。③**无虚构可执行命令**——第一幕故事里的 `docker run --rm -v pgdata:/from ...` 与 `/backup/pgdata-*.tar.gz` 均属叙事示例，已补「虚构示例，你这台机器上没有 `pgdata` 卷与 `/backup/` 目录」豁免注；回归脚本确认 `order-service` 类虚构镜像 0 处。④**安全边界**——`pg_dump` 落盘、`docker save -o`、卷恢复解压、`docker stop`、`volume prune` 五项全部标注「不执行」，并给出 A/B/C 三方案待用户选择。⑤**结构回归**——围栏 90（偶数）、2 张 SVG 以 `UTF8Encoding` 解析为合法 XML（37 / 57 个 text 节点）、7 条本地链接全部可达。

  **本课三个高价值发现**：其一，**备份的第一道坎不是"怎么备"而是"备哪个"**——218 个卷里 203 个是匿名卷（93%），名字是 64 位哈希，无法判断内容；本机真实数据库 `l12-pg` 的 82MB 数据就躺在匿名卷 `1fe9255...` 里，**任何按卷名备份的脚本都必然漏掉它**。其二，**`psql -U postgres` 报 `role "postgres" does not exist`**——容器创建时指定了 `POSTGRES_USER=grafana`，**备份脚本的用户名必须从 `docker inspect` 的 Env 读取**，想当然会静默失败或备到错的库；同时 `pg_backup_start()`/`stop()` 用两次 `psql -c` 分开调用时第二次必报 `backup is not in progress`，**必须同会话成对调用**。其三，**57 个 bind mount 完全不在 `docker volume ls` 里**——它们是宿主机路径（dashboards、prometheus.yml、provisioning 配置），遍历卷的备份脚本会**完全忽略**，"备份了卷就万事大吉"是错的。

  **与既有课程的闭环**：课 2 的「卷 46GB、可 reclaim 仅 673MB」在本课查明成因——**那 673MB 正是孤儿卷**；课 3 的「197 容器仅 3 个 healthcheck」在本课延伸为**没有健康检查就无法完成恢复演练的第 3 步（校验）**，即"恢复后甚至不知道服务是否真的起来了"。本课另给出本机诚实的 RPO/RTO 评估：**零备份 → RPO=∞、RTO=未知**，并列出 3-2-1 原则（备份落 D 盘 2.7T 可用，但同盘故障会一起没）。

- [x] **子教程 · 运维专项 课 5《网络与主机防火墙》** — pedagogy + learner 双视角评审，**P0=0 通过**（2026-09-20）。交付：讲义 948 行 + [iptables 路径与 DOCKER-USER 位置图](../子教程/运维专项/assets/lesson-05-iptables-path.svg) + [暴露面与 MTU 图](../子教程/运维专项/assets/lesson-05-exposure-surface.svg)。八轮取证全部为真实输出。评审核查五项：①**实测真实性**——FORWARD 策略 DROP / DOCKER-USER 位于第 1 条 / 计数器 1375917709 包·794002241861 字节（与 DOCKER-FORWARD 完全相同）/ 新版 6 链无 `DOCKER-ISOLATION`（实测报 `No chain/target/match by that name`）/ iptables v1.8.10 (nf_tables) / 27 非回环 IP / 87 个 0.0.0.0 唯一端口 / 175 条绑定（87+85+3）/ 24 个 br-* 网桥全为 MTU 1500 / 容器内 MTU 1500 / `ip_forward=1` / `bridge-nf-call-iptables=1` / host 容器 1 个（`prom-learn`）——**每项 `🟢` 均可回溯到取证脚本输出**。②**如实记录测不出的项**——同主机网关上 `ping -M do -s 1473` 仍通（不跨网段），**未编造"MTU 黑洞复现成功"**，改为明示"需跨网段/云网络才能复现"；overlay 本机 `Swarm: inactive`、网络数 0，**明确标注为官方口径非本机实测**。③**安全边界**——加 `DOCKER-USER` 规则（含 IPv6）、写 `daemon.json`、起容器做冲突复现、建网络、重启 dockerd 五项全部标注「不执行」并给出 A/B/C 方案；**特别提示本机 27 IP×87 端口禁止一次性全封**、操作失误可能锁死 SSH。④**结构回归**——围栏 66（偶数）、2 张 SVG 以 `UTF8Encoding` 解析为合法 XML（50 / 51 个 text 节点）、7 条本地链接全部可达、六要素 ×3 知识点齐全。⑤**官方一手**——8 条 docs.docker.com 链接 **8/8 返回 200**，新链结构口径以官方 `firewall-iptables` 页为准。

  **本课三个高价值发现**：其一，**DNAT 先于过滤**是比"写错链"更隐蔽的坑——包到达 `DOCKER-USER` 时目的端口已被改写成容器端口，🟢 实测 175 条绑定中 **133 条（76%）发布端口≠容器端口**，照抄 `--dport 19192` **规则存在、计数器永远 0、无任何报错**，与课 3 `MemPerc`、Kafka 课 `RequestHandlerAvgIdlePercent` 同属**静默失效**家族；正解是 `--ctorigdstport`（官方推荐）。其二，**暴露面是乘法不是加法**——一个 `-p` 在 27 个 IP 的主机上等于开 27 个入口（27×87=**2349**），而 `127.0.0.1:` 前缀可收敛到 1（🟢 实测 `172.26.238.136:40271` 返回 `Connection refused`）；**这是治本，比事后过滤可靠**。其三，**`inspect` 里 `HostIp` 为空不是"没绑定"**，而是未指定 → Docker 双栈展开为 `0.0.0.0`+`::` 两条，且 IPv6 侧有**独立的 ip6tables 六链**（Docker 27+ 默认管理），只封 IPv4 等于留一半后门。

  **本轮修正的两个真问题**（按「先核验再下结论」铁律）：①**照抄会算错**——第四幕步骤 4 原用 `sort -un | wc -l` 输出 **88**（模板渲染多一个空行），而讲义正文写 87；核验后确认 **87 为正确值**，已改命令为 `grep -c .` 并加注"差 1 陷阱"。②**两处表述不自洽**——原写「24 个 br-* 网桥」与「25 个 bridge 网络」并列无解释，实测查清缺的是**默认 bridge 未被使用时内核不创建网桥设备**（懒加载），已补说明并写入判据「**数网桥 ≠ 数网络**」。另修复评审中发现的 P1：知识点 2 缺「示例演示」段（六要素不齐），已补齐 8 条可照抄命令。

## 每课交付必查项

> 本仓库既有约定（ZooKeeper 课程补齐时发现课 1 曾漏写以下两段）。课文件交付前逐项核对：

- [ ] 课文件末尾含「🚀 下一批接力提示词」段
- [ ] 整课完成时（不再追加小节）末尾含「🧭 课程导航」段（上一课 / 下一课 / 返回目录）
- [ ] 知识点状态已回写 `00-学习档案.md` 进度表
- [ ] `02-课程目录.md` 中本课已由纯文本改为可点击链接
- [ ] 已过事实核查闸门（版本号 / 官方行为 / 报错原文等须核实并标注 `（核查于 YYYY-MM）`）
- [ ] 课文件含「📌 知识点导航」段（**写正文时最容易被丢掉**——课 1、课 2 都曾漏写，事后补）
- [ ] 课文件含「📋 命令速查卡」段（**技术域每课必带**——本课用到的命令与其坑）
- [ ] 本课涉及的命令已回写 `02-课程目录.md` 的「🔧 命令速查索引」（新增命令补行、本课已写完则把课号改成链接）

## 评审记录（勾选后追加结论摘要）

| 日期 | 评审对象 | 评审方式 | P0 数 | 意见摘要 |
|------|----------|----------|-------|----------|
| 2026-09-05 | 大纲评审（Phase 1） | 主 agent 内联（pedagogy） | 0 | P1×1：入门水平缺环境准备环节 → 阶段 1 overview 增「课前环境准备」章节，已采纳 |
| 2026-09-10 | **web index 交叉核对补录**（课 3 / 9 / 10 / 12 / 13 / 15 共 6 处） | 主 agent 内联（先验证再下结论 + 官方一手核查 + 结构回归） | 0 | P1×1（已修复）：课 12 历史遗留的代码块围栏错位，第 438 行多余闭合围栏致 440–457 行共 18 行命令裸露在代码块外；核查确认非本次引入（改动前围栏数即奇数 47），已删除错位围栏，全文件 50 个配对正常。补录内容：课 3 可信内容四类辨识 / 课 9 多文件拆分（merge·extends·include）/ 课 10 live restore / 课 12 签名校验（cosign；**DCT 正在退役，Notary v1 计划于 2026-12-08 关停**）/ 课 13 Hub 拉取限额（6h 窗口·匿名 100·Personal 200）/ 课 15 `docker context` + 已废弃写法对照表。核查：新增 10 条官方链接全部 200；7 个改动文件围栏全偶数（26/54/34/50/50/24/0）；证据全部为 🔵 官方一手，无 🟢 实测（守护进程未运行），已如实声明未编造实测输出。归档：6 课导航表 + 6 张速查卡 + 02-课程目录（新增 7.5 命令组）+ 学习档案四处均已回写 |

| 2026-09-20 | **运维专项课 6《变更升级与多机运维》**（子教程收官课，1070 行） | 主 agent 内联（pedagogy 5 维度 + learner L1-L6 双视角） | 0 | **P1×2 / P2×2，全部当批修复。** **P1①**——知识点 ③ 缺「示例演示」段（六要素不齐，与课 5 知识点 2 同类）→ 已补「三项高频巡检的照抄版」（特权容器逐个说理由 / docker.sock 挂载扫描 / 三行看缺失率），并点明「能出数的检查才能进清单」。**P1②**——导航区两条主线链接层级错误（写作 `../../`，实际应为 `../../../`，lessons→运维专项→子教程→docker 共三级）→ 已修正，复验 5 条本地链接全 OK。**P2①**——端口数 87（课 5 遗留）与实测 84(通配)+3(回环)=90(并集) 不一致 → 核实为**快照浮动**（容器持续启停），已加「与课 5 的差异说明」段，明确「关注量级与构成、与自己比」，不硬凑数字。**P2②**——管道命令在 PowerShell 侧转义易错 → 取证与校验全部改走 WSL bash 脚本 + 写文件读取。**pedagogy 5 维度**：① 准确性——live restore 三条边界（只保补丁版／跳版本可能失联／配置变更不保／不管 Swarm）引官方原文，`dh_installsystemd` postinstall 无条件 restart 与五包同升建议引社区实践并与官方交叉核对，`DOCKER_HOST` 覆盖 context 已**本机实测验证**（export 后报 Cannot connect），无虚构；② 格式规范——五幕齐全、六要素各 3 次、6 处 Mermaid + 2 张 SVG（均过 XML 校验）、3 道折叠答案小测；③ 完整性——3 知识点全覆盖，升级／回滚／context／自启／巡检均有可执行判据；④ 一致性——课 1（无 drop-in）、课 4（备份托底）、课 5（prom-learn host 网络不受管）三条伏笔全部兑现；⑤ 难度梯度——事故→概念→机制→实操→收官，无倒序。**learner L1-L6**：L1 凌晨升级后 119 容器全没，代入强；L2 先讲最痛的「升级会停机」再讲多机最后讲巡检；L3 两张 SVG 分别承载「升级路径与边界」「遥控器 vs 电器记忆」两个易读串对比；L4 五幕闭环，第四幕步骤 3 直接产出第一幕的「真实损失清单」；L5「开了 live restore 就安全吗」「切了 context 为什么没生效」「扫出 privileged 是不是出大事」均已覆盖；L6 给出运维专项六课收官表与三条可带走结论。**事实核查**：8 条官链 6×200 + 2×301（可达）；10 个关键实测数字（29.4.1／29.8.1／2.2.3／2.3.5／1.3.5／184／119／194／248／128G）**逐一回溯到取证文件 _l6p1~p4.txt**，全部有支撑；围栏 100 偶数。**不执行声明**：升级命令、deb 缓存、远端 context 创建全部标注「不执行」，未擅自改环境 |
| 2026-09-10 | **web index 交叉核对补录**（课 3 / 9 / 10 / 12 / 13 / 15 共 6 处） | 主 agent 内联（先验证再下结论 + 官方一手核查 + 结构回归） | 0 | P1×1（已修复）：课 12 历史遗留的代码块围栏错位，第 438 行多余闭合围栏致 440–457 行共 18 行命令裸露在代码块外；核查确认非本次引入（改动前围栏数即奇数 47），已删除错位围栏，全文件 50 个配对正常。补录内容：课 3 可信内容四类辨识 / 课 9 多文件拆分（merge·extends·include）/ 课 10 live restore / 课 12 签名校验（cosign；**DCT 正在退役，Notary v1 计划于 2026-12-08 关停**）/ 课 13 Hub 拉取限额（6h 窗口·匿名 100·Personal 200）/ 课 15 `docker context` + 已废弃写法对照表。核查：新增 10 条官方链接全部 200；7 个改动文件围栏全偶数（26/54/34/50/50/24/0）；证据全部为 🔵 官方一手，无 🟢 实测（守护进程未运行），已如实声明未编造实测输出。归档：6 课导航表 + 6 张速查卡 + 02-课程目录（新增 7.5 命令组）+ 学习档案四处均已回写 |
